{
  "name": "Reproducible Dotfiles (AMD64)",
  "containerName": "dotfiles-dev-container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "args": {
      "USERNAME": "${localEnv:USER}",
      "USER_UID": "${localEnv:UID}",
      "USER_GID": "${localEnv:GID}"
    }
  },
  "runArgs": [
    "--platform",
    "linux/amd64"
  ],
  "containerEnv": {
    "FORCE_PLATFORM": "linux-64"
  },
  "forwardAgent": true,
  "mounts": [
    "source=${localEnv:HOME}/.ssh/config,target=/home/${localEnv:USER}/.ssh/config,type=bind,readonly",
    "source=${localEnv:HOME}/.ssh/known_hosts,target=/home/${localEnv:USER}/.ssh/known_hosts,type=bind,readonly",
    "source=${localEnv:HOME}/.ssh/authorized_keys,target=/home/${localEnv:USER}/.ssh/authorized_keys,type=bind,readonly",
    "source=${localEnv:HOME}/.claude,target=/home/${localEnv:USER}/.claude,type=bind,readonly",
    "source=${localEnv:HOME}/.codex,target=/home/${localEnv:USER}/.codex,type=bind,readonly",
    "source=${localEnv:HOME}/.gemini,target=/home/${localEnv:USER}/.gemini,type=bind,readonly",
    "source=${localEnv:HOME}/.config/gcloud,target=/home/${localEnv:USER}/.config/gcloud,type=bind,readonly"
  ],
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": {
      "user": "${localEnv:USER}"
    }
  },
  "forwardPorts": [
    2222
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "mend.renovate-vscode",
        "jkillian.vscode-bats",
        "charliermarsh.ruff",
        "tamasfe.even-better-toml"
      ]
    }
  },
  "remoteUser": "${localEnv:USER}",
  "containerUser": "${localEnv:USER}"
}