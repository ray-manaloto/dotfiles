# Receipt — #568: Correct the harness ledger and `docs/agent-team.md` from the seven-report sweep

**Verdict:** All five ticket items applied, plus #565's fold-in. (1) The `claude attach` row is
corrected **in place** — attach/stop/kill/logs/rm/respawn/daemon all exist, hidden from root
`--help`; the old row's probe was bounded to the visible command list (the founding incident's
shape). (2) The plugin-agent row now names all SEVEN lost fields (four dropped silently), the
`isolation` narrowing, and `color` as the 12th kept field. (3) The spawn-pin names needed **no
replacement** — #567 already pinned the real names in `.claude/settings.json:6-8` and the guessed
names appear nowhere in `.claude/` or `docs/` (control-armed grep below); the ledger now carries
the real-names row with the refutation of the guessed ones. (4) **65 rows appended** (62 from the
five reports that drafted ledger-format rows + 3 from #565: terminal predicate, fleet-gate-is-local,
respawn-comes-back-idle), 67 row-level ledger changes in total. (5) `docs/agent-team.md` carries a
new dated correction block retiring the no-dead-node-detection premise, scoped to an eligible
`--bg` worker under a live supervisor with the non-recovered cases listed. Codex adversarial
review: 9 findings — 3 accepted (edits applied), 2 accepted-in-part, 4 refuted (2 by probe).

**Resolved:** 2026-08-05

## Sources — what I actually opened

- `.claude/agents/claude-code-expert.md` — the ledger under correction; rows 195 (`claude attach`)
  and 212 (plugin three-field under-count) located verbatim before editing.
- `docs/research/kb/reports/agents/wf-dag-recovery.md` §"Ledger entries to append" — the corrected
  attach row (three routes) + 15 recovery rows; §13b SUSPECT row superseded by #565.
- `docs/research/kb/reports/agents/wf-dag-context-gate.md` §6 — the correction statement and 16 rows.
- `docs/research/kb/reports/agents/wf-dag-model-routing.md` — 17 rows.
- `docs/research/kb/reports/agents/wf-dag-plugin-extraction.md` — the seven-field row (used as the
  in-place row-212 correction) + 9 rows, incl. the real spawn-control env names.
- `docs/research/kb/reports/agents/wf-dag-protocol-verbs.md` — 6 rows.
- `docs/research/kb/reports/agents/wf-dag-symphony-gaps.md`, `wf-dag-codex-lane.md`,
  `wf-dag-research-synthesis.md` — checked for drafted ledger rows: **none in ledger format**
  (claim tables only), so nothing pasted from these three; the synthesis §2 names the
  "implicit premise that nothing detects a dead node" this receipt's agent-team.md row retires.
- `docs/receipts/565.md` — verdict + Notes; source of the two folded-in rows (terminal predicate,
  fleet-gate-is-local) and the respawn-comes-back-idle fact.
- `docs/agent-team.md` — the existing 2026-08-05 corrections blockquote whose format the new
  block mirrors.
- `~/dev/github/ray-manaloto/knowledge-base/python/src/kb_setup/md_budget.py` — `classify()`
  returns `None` for `.claude/agents/*.md` (only CLAUDE.md entry points, rules and skills are
  budgeted), so the ledger growth does not trip `md_size_budget`.

## Prior art — the search I ran

**Query:** `grep -rn -E 'MAX_AGENT_DEPTH|CONCURRENT_AGENTS([^_]|$)' .claude/ docs/` (excluding
`docs/research/kb/` **and this receipt** — the first run after writing the skeleton matched only
the receipt's own Query line, a live demonstration of a published term polluting its corpus) —
do the GUESSED spawn-pin names survive anywhere the ticket's item (3) would need to fix?
**Corpus:** `.claude/`, `docs/`, `.claude/settings.json`

### Control arm — REQUIRED, and run it before you believe any result

**Known-present term:** `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` → **6** hits across
`.claude/settings.json`, `docs/agent-team.md`, `docs/receipts/567.md`
**Known-absent term (fresh):** `qvorlath5529` → **0** hits

The guessed names returned **0 hits (rc=1)** against a discriminating probe ⇒ ticket item (3) was
already satisfied by #567, which pinned the REAL names (`CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`,
`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`, `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION`) in
`.claude/settings.json:6-8`. What remained for #568 is the ledger row RECORDING the real names
(with the refutation of the guessed ones), which is in the appended batch.

| Hit | What I did with it |
|---|---|
| `.claude/settings.json:6-8` | read — real names already pinned; nothing to replace |
| `docs/agent-team.md:355-357` | read — real names + measured defaults already correct |
| `docs/receipts/567.md` | read — the pin decision's receipt; confirms provenance |

## Adversarial review

**Lens:** codex-reviewer (`codex exec --ephemeral --sandbox read-only`, diff-only brief)
**Verdict:** needs-attention → edits applied, then clean
**Findings:** 9 — persisted verbatim at `docs/research/kb/reports/agents/codex-review-568.md`
**Disposition:** every finding that could be settled by running something was probed first:

| # | Finding | Disposition |
|---|---|---|
| 1 | hung-node "recovery is attach-triggered" overclaims | **accepted-in-part** — the doc route is explicit (*"When you open a session that has stopped responding, the supervisor restarts its process"*), so the ledger row (drafted verbatim) stands; the agent-team.md block now says "docs say opening the session restarts its process — not live-probed" |
| 2 | dead-worker auto-respawn generalized beyond evidence | **accepted** — block rescoped to "an eligible `--bg` worker under a live supervisor", header retitled to "no-dead-node-detection premise", no-flushed-messages exclusion added |
| 3 | "conversation intact" contradicts the interrupted-turn loss row | **refuted** — different scopes: post-respawn session/transcript continuity (live-probed) vs in-flight `tool_use` unwind semantics (code-route). Both rows are faithful transcriptions; agent-team.md's block nevertheless adopted the more precise "flushed history preserved" phrasing |
| 4 | `fallbackModel` 429-exclusion evidence unauditable (set contains 429) | **refuted by probe** — `$CC/model-config.md:359` read live: *"Authentication, billing, rate-limit, request-size, and transport errors never trigger a switch"*; `tR_` is the exclusion set, polarity settled by the docs route the reviewer (diff-only by brief) could not open |
| 5 | `REFUTED (as a candidate)` and `CONFIRMED (code) / not probe-exercised` malformed | **refuted** — matches existing ledger usage where REFUTED marks an overturned candidate/claim (cf. the team-dirs-cleanup row), and the hybrid label is more honest than either CONFIRMED or NEEDS-PROBE alone; both verdicts are the researchers' drafted text |
| 6 | many CONFIRMED rows lack a named control arm | **refuted** — the ledger's own convention: "Keep entries one or two lines; the full evidence lives in the run's report". Control arms live in the wf-dag reports each row cites |
| 7 | duplicates: `--bg`+`--print`; respawn existence; formula/Math.min pair | **accepted-in-part** — the `--bg`+`--print` row reworded to lead with the novel collision and reference the retained row; respawn-existence subordinate clause and the formula/consequence split kept (distinct claims, drafted separately) |
| 8 | receipt accounting ambiguous | **accepted, settled by probe** — diff decomposition: ledger +67/−2 table rows = 65 appended + 2 corrected in place; drafts: 66 = 62 appended + 2 attach drafts deduped into 1 in-place correction + 1 plugin draft → in-place correction + 1 §13b draft superseded by #565's settled row; +3 #565 rows = 65 |
| 9 | receipt placeholders + inexact grep record | **accepted** — resolved in this revision; the exact command was `grep -rn -E 'MAX_AGENT_DEPTH\|CONCURRENT_AGENTS([^_]\|$)' .claude/ docs/ --exclude-dir=kb --exclude=568.md` → rc=1, present arm 6 hits, fresh absent arm 0 |

## Notes

- `typos.toml` gained `gae = "gae"` under `[default.extend-identifiers]` — the minified binary
  symbol quoted in two pasted evidence anchors trips typos' dictionary. Same shape as the three
  existing entries (`ba41c3748e298012`, `fpr`, `sherif`): an identifier not ours to respell, allowlisted
  exactly so genuine typos are still caught. Rewording the anchors would break re-derivation
  against the binary.
- Dedup decisions while pasting (the drafts total 66 rows; 67 row-level ledger changes = 65
  appended + 2 corrected in place):
  - recovery's attach row and context-gate's attach row state the same correction — applied ONCE,
    **in place at the old row's position** (ledger convention: "Correct or delete an entry the
    moment a probe overturns it"), keeping the recovery version's three-route evidence.
  - plugin-extraction's seven-field row replaced old row 212 in place rather than appending a
    duplicate.
  - recovery's §13b row (`state:'done'` did NOT suppress — SUSPECT) was NOT pasted as drafted:
    #565 settled it same-day; the appended row is the refined terminal predicate
    (`state ∈ {done,failed,stopped}` AND `tempo ≠ "active"` AND no `queuedPrompt`), citing
    `docs/receipts/565.md`, per the #565 receipt's Notes assigning that refinement here.
- #565 fold-in is THREE rows, not two: terminal predicate, fleet-gate-is-local, and
  respawn-comes-back-idle (attempt 2→1 reset) — the third is settled by the same 7/7-arm probe
  and is load-bearing for #573's premises, so leaving it out would re-derive it later.
- The "~40 ready-to-append rows" in the ticket body was an estimate; the five reports that
  drafted rows in ledger format drafted 66. Counting method: rows between each report's
  "Ledger entries to append" heading and its next heading.
