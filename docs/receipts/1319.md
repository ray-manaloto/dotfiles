# Receipt — #1319: Prove fable-orchestrator removal parity with live arms

**Verdict:** PARTIAL. The four Claude-only `Agent` arms (`premise-verifier` and `claude-advisor`, once in each repo)
all spawned and returned a verdict on 2026-09-25, and each premise-verifier refuted its planted control. Still OPEN:
the codex arms (codex usage limit until 2026-09-30 4:05 PM CDT), the `sdlc-team` review dispatch (blocked on #1362),
the knowledge-base tool-review run to its Review phase, the `verify`/`rule-sync`/`plugin-health` + live-path-grep
arms re-run under this receipt, and the #1293 map comment. #1319 stays open.

**Resolved:** not yet (partial, 2026-09-25)

## Arms run

| Arm | Command / invocation | rc | Output |
|---|---|---|---|
| dotfiles `premise-verifier` | `Agent` tool, `subagent_type: premise-verifier`, session `e0054614-0df9-4dff-9b50-af3b534871e1` | returned (3 CONFIRMED / 1 REFUTED; P3 = planted control) | `docs/research/kb/reports/agents/1319-live-arm-premise-verifier-dotfiles-2026-09-25.md`; transcript `~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/e0054614-0df9-4dff-9b50-af3b534871e1/subagents/agent-a7fa1858a7734c0da.jsonl` |
| dotfiles `claude-advisor` | `Agent` tool, `subagent_type: claude-advisor`, same session | returned (KEEP-BROKEN, reword only; see F13 below) | `docs/research/kb/reports/agents/1319-live-arm-claude-advisor-dotfiles-2026-09-25.md`; transcript `…/subagents/agent-a259bbb7249af4b18.jsonl` |
| knowledge-base `premise-verifier` | headless `claude -p --output-format json --permission-mode bypassPermissions` in the KB checkout (claude 2.1.283), which made one `Agent` call | parent rc=0, `is_error=false`; returned (2 CONFIRMED / 1 REFUTED; P3 = planted control) | `docs/research/kb/reports/agents/1319-live-arm-knowledge-base-2026-09-25.md`; transcript `~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-knowledge-base/b3a5b5da-6a0a-49fe-a4c3-71d6097f36a9/subagents/agent-a8d386ef8146c95f4.jsonl` |
| knowledge-base `claude-advisor` | same headless session, second `Agent` call | returned (AGREE: close KB#794 on the skill-level default) | same report; transcript `…/subagents/agent-ad9f39535ec66de0f.jsonl` |

## Sources — what I actually opened

- `gh issue view 1319` — the arm list and acceptance criteria.
- Both `.claude/agents/{premise-verifier,claude-advisor}.md` files exist in each repo (`ls` of both `.claude/agents/`).
- The four subagent transcripts above — the KB reports were recovered from them, not from the headless parent.
- `knowledge-base/python/src/kb_setup/review.py:200,864-880`, `cli.py:795,830-850` — spot-check of the KB advisor's
  citations (hold).
- `python/src/dotfiles_setup/claude_doctor.py:509-518`, `.claude/skills/claude-doctor/hooks/register.ts:375`,
  `schemas/sources.toml:53`, `tests/fixtures/claude_doctor_hook/harness.ts:480` — spot-check of the dotfiles advisor's
  citations (hold).

## Prior art — the search I ran

**Query:** `git grep -l '1319' -- docs/receipts docs/research/kb/reports/agents`
**Corpus:** `docs/receipts/`, `docs/research/kb/reports/agents/`

### Control arm — REQUIRED, and run it before you believe any result

**Known-present term:** `1319` in tracked `docs/receipts` + `docs/research/kb/reports/agents` → **17** files
(unbounded `wc -l`; a first `| head` view showed 10 and was a display bound, not the count). Narrowed to `#1319` →
**11** files, each line read.
**Known-absent term:** `docs/receipts/1319.md` → absent before this receipt (`ls docs/receipts` → 578, 593, 602,
graphify, TEMPLATE).

| Hit | What I did with it |
|---|---|
| `fable-orchestrator-removal-session-2026-09-24.md:136,249,258,265` | read — defines these arms as OWED because "new agent types load only at session start"; no arm run there |
| `removal-review-spec-2026-09-24.md:9` | read — confirms the `claude-advisor`/`premise-verifier` spawns were not run on 2026-09-24 |
| `session-audit-vagueness-2026-09-25b.md:63,72` | read — the source of the "Claude-only now, codex after 09-30" split this receipt follows |
| `session-audit-missing-requests-2026-09-25.md:174,185` | read — #1319 OPEN; sdlc-team arm waits on #1362; keep the fable-removal backups until #1319 closes |
| the other 7 `#1319` files (`codex-native-installer-status`, `fable-orchestrator-still-used`, `session-2026-09-23d-agent-briefs`, `session-audit-missing-requests-2026-09-24`, `session-audit-vagueness-2026-09-24`, `ship-session-{lessons,results}-2026-09-24`) | dismissed — they name the ticket range or state "keep #1319 open"; none ran an arm |
| the 6 files matching bare `1319` only | dismissed — line numbers or counts, not this issue |

## Adversarial review

**Lens:** none
**Verdict:** not-run
**Findings:** 0
**Disposition:** —

**Not run:** this receipt records live invocations; the evidence is the transcripts, not an argument to review.

## Notes

- ⚠️ **The headless KB parent's own summary is not evidence.** It wrote two `result` records. The first (6 turns)
  says earlier "notifications" (a "`:612`" premise text and an "AGREE" advisor text) were "not a genuine
  notification". The second says both arms were ok. The subagents' own final messages in their transcripts are the
  record. A future headless arm should read `subagents/*.jsonl`, not the parent `result`.
- The KB `claude-advisor` runs `model: fable` (`knowledge-base/.claude/agents/claude-advisor.md:4`).
- The dotfiles advisor answered Ray's owed ruling 23(f). Ray ruled 2026-09-25: keep enforcing, reword only. The
  reword ships in the same PR as this receipt.
