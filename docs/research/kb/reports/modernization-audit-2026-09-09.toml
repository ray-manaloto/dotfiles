# Modernization audit findings — deterministic aggregate (no LLM)
# units=121 findings=743 retire=35 (survived 33) refactor=392 (survived 368) keep=271 unknown=45 refuted=26 unverified_contested=0
# Survival: a retire/refactor finding survives with >=2 lens verdicts and <2 refuted; keep/unknown are not voted on.
# malformed_verdict_files=0 (every verdict file parsed)
# `correction` / `correction_custom` carry synthesis-stage citation repairs from .agent/kb/audit/corrections.json.
# The finder's own evidence/alternative_evidence strings are NEVER edited; a correction is an additional field,
# so the defective pointer and its repair travel together in this file exactly as they do in the .md report.

[[finding]]
id = "agents-1"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.90
custom_thing = "codex-advisor declares `color: teal`, a value outside the documented display-color allowlist."
evidence = ".claude/agents/codex-advisor.md:6 (`color: teal`). The other four codex-* wrappers use documented values: .claude/agents/codex-adversarial-critic.md:6 `red`, .claude/agents/codex-operator.md:6 `orange`, .claude/agents/codex-claude-code-expert.md:7 `cyan`, .claude/agents/codex-staleness-auditor.md:6 `orange`."
native_alternative = "The `color` frontmatter field accepts exactly `red`, `blue`, `green`, `yellow`, `purple`, `orange`, `pink`, `cyan`. `teal` is not among them, so the field is silently ineffective (or falls back) and the lane loses the visual identity the field was added for."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:306 — \"`color` | No | Display color for the subagent in the task list and transcript. Accepts `red`, `blue`, `green`, `yellow`, `purple`, `orange`, `pink`, or `cyan`.\""
rationale = "One-token fix: change `teal` to `cyan` (its nearest documented neighbour, and already the value codex-claude-code-expert uses). This is the only outright invalid frontmatter value in the unit. Confidence is 0.9 rather than 1.0 because the doc states the accepted set but does not state the behaviour for an unaccepted one — the fix is correct either way."
probe = "Spawn codex-advisor and read its row in `/tasks`; if no colour is applied the field is being dropped."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "agents-2"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.90
custom_thing = "dockerfile-reviewer.md carries a two-field frontmatter (`name`, `description`) only — no `model`, no `tools`, no `disallowedTools`, no `color`, no `effort`. Because `tools` is omitted it inherits EVERY tool available to subagents, including Edit, Write and NotebookEdit, despite being a review-only agent whose entire body is a read-only checklist."
evidence = ".claude/agents/dockerfile-reviewer.md:1-4 (frontmatter is `---`/`name:`/`description:`/`---`); body .claude/agents/dockerfile-reviewer.md:22-35 is a 12-item \"Review Checklist\" with no write step. Contrast the three Claude-side siblings which at least deny edits: .claude/agents/adversarial-critic.md:6, .claude/agents/claude-code-expert.md:5, .claude/agents/staleness-auditor.md:5 (`disallowedTools: Edit, NotebookEdit`)."
native_alternative = "Native frontmatter fields `tools` / `disallowedTools` (tool scoping), `model` (this repo's other reviewers pin `opus`), `color`, and `effort` — all supported at the host's 2.1.266 and all omitted here."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:294 — \"`tools` | No | Tools the subagent can use. **Inherits every tool available to subagents if omitted**\"; :295 `disallowedTools`; :296 `model`; :304 `effort`; :306 `color`."
rationale = "The file predates the rest of the set (mtime 2026-08-13 vs 2026-09-01/09-03 for the others) and never picked up the field conventions the siblings adopted. Setting `tools: Read, Grep, Glob, Bash` (or at minimum `disallowedTools: Edit, Write, NotebookEdit`) makes the read-only contract machine-enforced instead of prose-enforced, and `model:` stops it silently inheriting whatever the session happens to be on. Not 'retire': no built-in or plugin agent reviews Dockerfile/bake config (see agents-11)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "agents-3"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.85
custom_thing = "All five codex-* wrappers inline the same `codex exec` invocation recipe in prose — the stdin form, `--ephemeral`, `--sandbox`, `--model gpt-5.6-sol`, `-c model_reasoning_effort=xhigh`, `-o <file>`, and the `PLANNING_DISABLED=1` rationale — duplicated per file rather than shared."
evidence = "`grep -c 'codex exec' .claude/agents/*.md` -> codex-advisor 5, codex-claude-code-expert 4, codex-adversarial-critic 4, codex-staleness-auditor 4, codex-operator 2 (Claude-side agents: 0, control arm). The near-identical `PLANNING_DISABLED=1 codex exec --ephemeral --sandbox … -c model_reasoning_effort=\"xhigh\" -o …` block appears at .claude/agents/codex-advisor.md:88-95 and again at .claude/agents/codex-operator.md:51-58, with the same two paragraphs of justification pasted after each."
native_alternative = "The `skills` frontmatter field preloads the FULL content of a named skill into the subagent's context at startup, which is precisely the 'shared invocation contract' shape. The enabled `codex@openai-codex` plugin's own agent does exactly this: `skills:\\n  - codex-cli-runtime\\n  - gpt-5-4-prompting`, keeping its body down to forwarding rules."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:299 (`skills` field) and :537-556 (\"Preload skills into subagents\" — \"The full content of each listed skill is injected into the subagent's context at startup\"); worked precedent at /Users/rmanaloto/.claude/plugins/cache/openai-codex/codex/1.0.6/agents/codex-rescue.md:6-8."
rationale = "Five copies of one recipe is five places to update when a codex flag drifts — and `.claude/rules/ai-cli-invocation.md` records that these flags DO drift between releases (the repo already carries wrong documented forms as a historical case). A single `.claude/skills/codex-lane-invocation/SKILL.md` referenced by `skills:` in all five collapses that to one, and matches the three-layer skill->task->library doctrine in .claude/rules/agent-artifact-conventions.md rule 6. Caveat that keeps this below 0.95: a preloaded skill must not set `disable-model-invocation: true` (sub-agents.md:551), so the new skill has to be model-invocable."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "agents-4"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.60
custom_thing = "No agent in the unit sets `memory:`. staleness-auditor, claude-code-expert and dockerfile-reviewer each re-derive the same repo facts (which rules exist, which harness version, which bake/Dockerfile conventions) from scratch on every invocation; the dockerfile-reviewer body is a hand-maintained 12-item knowledge dump that a human must keep current."
evidence = "Control-armed grep over .claude/agents/*.md: `^memory:` -> 0/9 files, control `^name:` -> 1 in each of 9/9. dockerfile-reviewer's frozen knowledge is .claude/agents/dockerfile-reviewer.md:8-20 (\"Project-Specific Patterns\" / \"docker-bake.hcl Patterns\"), all hand-written prose."
native_alternative = "The `memory` frontmatter field gives a subagent a persistent directory that survives across conversations — scope `project` writes to `.claude/agent-memory/<name>/` and is version-controllable, which is the documented recommended default."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:302 (field) and :563-598 (\"Enable persistent memory\", scope table at :580-584, \"`project` is the recommended default scope\" at :596)."
rationale = "Real capability, real fit for the auditor/reviewer agents — but it carries a collision this unit must resolve first, which is why confidence is 0.6 rather than 0.9: sub-agents.md:591 states that when memory is enabled \"Read, Write, and Edit tools are automatically enabled so the subagent can manage its memory files\". That would silently re-enable Edit on adversarial-critic / claude-code-expert / staleness-auditor, whose `disallowedTools: Edit, NotebookEdit` is the mechanical half of their 'never edits what it audits' contract. So: adopt `memory: project` on dockerfile-reviewer and the codex-* wrappers (which already list Write in `tools`) freely; for the three read-only Claude auditors, decide explicitly whether memory's Edit grant is acceptable before adopting."
probe = "Add `memory: project` to a scratch copy of adversarial-critic, spawn it, and have it attempt an Edit on a tracked file — confirm whether `disallowedTools: Edit` still wins over memory's automatic tool enablement."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "agents-5"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.70
custom_thing = "No agent sets `maxTurns`. Every lane runs unbounded, and the repo's own case history records agents that ran ~40 minutes, died, and left nothing."
evidence = "Control-armed grep over .claude/agents/*.md: `^maxTurns:` -> 0/9, control `^name:` -> 9/9. The failure mode is documented in .claude/rules/agent-report-persistence.md rule 1b (\"Two agents that held everything in memory died silently after ~40 minutes and left nothing\"), and each codex-* file works around it in prose instead — e.g. .claude/agents/codex-advisor.md:38-54 (\"Create the tracked report BEFORE you build the prompt\" / \"Deliver before you go idle\")."
native_alternative = "`maxTurns` caps agentic turns; when the cap is hit Claude Code returns the subagent's output MARKED AS PARTIAL and the agent can be resumed to continue — a bounded, recoverable stop instead of an open-ended run. The partial marking requires v2.1.246+; the host is 2.1.266."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:298 — \"`maxTurns` | No | Maximum number of agentic turns before the subagent stops. When the subagent reaches the limit, Claude Code returns its output marked as partial, and Claude can resume it to continue. The partial marking requires Claude Code v2.1.246 or later\"; resume mechanics at :1019."
rationale = "`maxTurns` does not replace the incremental-persistence rule (a killed process still needs its file on disk), so this is 'refactor', not 'retire' of that rule — but it converts the unbounded-run half of the hazard from prose into a harness-enforced bound with a documented resume path. Highest value on the codex-* wrappers, whose Claude-side turn budget should be tiny by design (\"Your own turns should do little more than gather the evidence codex cannot reach, build the prompt, shell out, and relay the verdict\", .claude/agents/codex-advisor.md:16-18) — a wrapper that has taken 30 turns has stopped being a wrapper."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "agents-6"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.80
custom_thing = "The three Claude-side auditors omit `tools:` entirely and narrow only via `disallowedTools: Edit, NotebookEdit`. They therefore inherit the Agent tool and can spawn their own subagents (this session pins depth 3), plus Skill, Write and full Bash — far wider than their read-only 'reports with evidence and never edits' contracts."
evidence = ".claude/agents/adversarial-critic.md:6, .claude/agents/claude-code-expert.md:5, .claude/agents/staleness-auditor.md:5 — each `disallowedTools: Edit, NotebookEdit` with no `tools:` line. The spawn depth is set repo-wide at .claude/settings.json (env `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH: \"3\"`, `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS: \"20\"`)."
native_alternative = "An explicit `tools:` allowlist is the native narrow — omitting `Agent` from the list removes subagent spawning entirely, and listing tools explicitly is what the five codex-* siblings already do (`tools: Bash, Read, Grep, Glob, Write`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:294 (\"Inherits every tool available to subagents if omitted\") and :452 — \"If you omit `Agent` from the `tools` list entirely, the agent can't spawn any subagents with the Agent tool\"; :448-450 note that inside a subagent definition, listing `Agent` permits its own spawns while the depth limit allows."
rationale = "Deny-listing two write tools is a much weaker statement than allow-listing the four the agent actually uses, and it leaves an auditor able to fan out 20 concurrent subagents at depth 3 — a blast radius nobody chose. The fix is the sibling convention already in this same directory: give each Claude auditor an explicit `tools:` line. Not 'retire' — the agents themselves stay."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "agents-7"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.90
custom_thing = "The three dead grok lanes shipped by the fable-orchestrator plugin (grok-implementer, grok-reviewer, grok-researcher) are still registered and model-invocable on this host; the only thing stopping their use is a prose ban in the project memory file."
evidence = "Prose ban at .claude/CLAUDE.md:56 (\"### There is no `grok` here — codex lanes only, stop asking\") and :61 (\"do not propose, dispatch, or 'fall back to' `grok-implementer`, `grok-reviewer` or `grok-researcher`\"). The agents exist at /Users/rmanaloto/.claude/plugins/cache/fable-orchestrator/fable-orchestrator/1.21.0/agents/grok-{implementer,researcher,reviewer}.md, the plugin is enabled at .claude/settings.json (`\"fable-orchestrator@fable-orchestrator\": true`), and a control-armed read of the deny array returns NO `Agent(...)` rule at all (`[r for r in permissions.deny if 'grok' in r.lower() or 'Agent' in r or 'Task' in r]` -> `[]`)."
native_alternative = "`permissions.deny` with `Agent(<subagent-name>)` disables specific subagents — built-in or custom — at the harness level, so the agent cannot be spawned at all rather than merely being discouraged."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:656-672 — \"You can prevent Claude from using specific subagents by adding them to the `deny` array in your settings. Use the format `Agent(subagent-name)`… This works for both built-in and custom subagents.\""
rationale = "This is the exact shape .claude/rules/mise-tasks-only.md calls out — markdown alone is 'relying on the LLM, never the only layer'. The repo already machine-enforces its other invariants through hooks and permission rules; three lines in `permissions.deny` (`Agent(grok-implementer)`, `Agent(grok-reviewer)`, `Agent(grok-researcher)`) would make a lane that cannot possibly work impossible to dispatch, and would let .claude/CLAUDE.md:56-70 shrink to a pointer. The prose stays useful for the 'why'; the deny carries the enforcement."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "agents-8"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.60
custom_thing = "The five codex-* wrappers are an explicitly time-boxed cost workaround, not a durable design: four of them (codex-advisor, codex-adversarial-critic, codex-claude-code-expert, codex-staleness-auditor) are declared twins of Claude-side originals that are to be used again 'once tokens reset'. Each hand-rolls its own foreground `codex exec` + `-o <file>` capture, with no background/status/result/cancel path."
evidence = "Time-box at .claude/CLAUDE.md:79-84 (\"Until Claude tokens reset (from 2026-08-31), advisor consults route to the `codex-advisor` subagent… The originals are intact and are the ones to use once tokens reset.\"); each wrapper repeats it in its own description, e.g. .claude/agents/codex-advisor.md:4 (\"use instead of fable-orchestrator:fable-advisor while Claude tokens are constrained\"), .claude/agents/codex-staleness-auditor.md:4. Hand-rolled invocation at .claude/agents/codex-advisor.md:88-95 and .claude/agents/codex-operator.md:51-58."
native_alternative = "The `codex@openai-codex` plugin (v1.0.6) is ENABLED in this repo and ships a maintained companion runtime plus a forwarding subagent: `codex-rescue` (model sonnet, `tools: Bash`, `skills: [codex-cli-runtime, gpt-5-4-prompting]`) forwarding to `scripts/codex-companion.mjs task` with first-class `--background`/`--wait`, `--effort`, `--model`, `--write`, `--resume-last`, and sibling verbs `status`, `result`, `cancel`, `review`, `adversarial-review`."
alternative_evidence = "/Users/rmanaloto/.claude/plugins/cache/openai-codex/codex/1.0.6/agents/codex-rescue.md:1-9 (frontmatter) and :14-40 (forwarding rules incl. background/effort/model/write/resume); verb surface at /Users/rmanaloto/.claude/plugins/cache/openai-codex/codex/1.0.6/scripts/codex-companion.mjs:81 and :1038, and commands at /Users/rmanaloto/.claude/plugins/cache/openai-codex/codex/1.0.6/commands/{adversarial-review,review,status,result,cancel}.md; plugin enabled at .claude/settings.json (`\"codex@openai-codex\": true`)."
rationale = "Not 'retire': the plugin runtime does NOT cover what these lanes are actually FOR. codex-operator deliberately needs `--sandbox danger-full-access` because every narrower mode blocks the `.git` writes `mise run land` performs (measured table at .claude/agents/codex-operator.md:20-28), and the companion's `--write` is not documented as granting that; and codex-adversarial-critic attacks a PROPOSAL, whereas `/codex:adversarial-review` targets a diff by ref. What IS superseded is the plumbing: background execution with a job id, status/result/cancel, and effort/model routing are all solved by the companion, while these wrappers reimplement a foreground-only subset in prose. The durable refactor is to (a) fold the shared invocation into one preloaded skill (agents-3), and (b) re-decide the four twins at the token reset the files themselves name — a set of agents whose stated reason to exist has an expiry date needs a scheduled review, not indefinite drift."
probe = "`node \"$CLAUDE_PLUGIN_ROOT/scripts/codex-companion.mjs\" task --help` (codex plugin 1.0.6) to confirm whether any flag reaches `--sandbox danger-full-access`; if it does, codex-operator can become a thin forwarder and its bespoke invocation block retires."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "agents-9"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.65
custom_thing = "`effort` is set inconsistently and, in one case, on the wrong side of the lane: codex-claude-code-expert sets `effort: high` on `model: haiku` while ALL of its reasoning happens inside `codex exec` at a separately-pinned `xhigh`; adversarial-critic sets `effort: high` on opus, but its two same-class opus siblings (claude-code-expert, staleness-auditor) set nothing."
evidence = ".claude/agents/codex-claude-code-expert.md:3-4 (`model: haiku` then `effort: high`) versus its body's codex pin `-c model_reasoning_effort=\"xhigh\"`; .claude/agents/adversarial-critic.md:4-5 (`model: opus`, `effort: high`); .claude/agents/claude-code-expert.md:4 and .claude/agents/staleness-auditor.md:4 (`model: opus`, no `effort`). The other three codex-* wrappers set no `effort` at all (.claude/agents/codex-advisor.md, codex-operator.md, codex-staleness-auditor.md frontmatter key lists)."
native_alternative = "`effort` overrides the session effort level for the subagent, with available levels depending on the model, and `/tasks` now displays the effort level on the subagent's row when the definition sets one (v2.1.242+; host 2.1.266) — so the field is both a real control and now observable."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:304 — \"`effort` | No | Effort level when this subagent is active. Overrides the session effort level… Options: `low`, `medium`, `high`, `xhigh`, `max`; available levels depend on the model\"; :369 — \"/tasks … adds the effort level when the subagent's definition, or the skill it forked from, sets `effort`\"."
rationale = "Two distinct defects under one field. (1) On a codex wrapper whose stated job is 'gather evidence, build the prompt, shell out, relay' the Claude-side effort should be LOW, not high — `effort: high` there buys reasoning nobody uses and reads as if it were the codex effort, which is pinned separately in the body; it is a live source of confusion about which knob does what. (2) Among the three opus auditors, only one sets effort, so two of them silently ride whatever the session is on — the exact non-determinism `effort` exists to remove. Confidence is 0.65 because the doc says available levels depend on the model and does not state haiku's set, so whether `high` is even accepted there is unprobed."
probe = "Spawn codex-claude-code-expert and read its `/tasks` row (v2.1.242+ prints the effort level) to see whether `high` was accepted on haiku or silently dropped."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "agents-10"
unit = "agents"
kind = "agents"
disposition = "refactor"
confidence = 0.50
custom_thing = "No agent sets `permissionMode`. Every lane — including five unattended codex wrappers that shell out and wait, and one that drives `mise run land`/`sync` against real git state — starts in whatever mode the parent session happens to be in."
evidence = "Control-armed grep over .claude/agents/*.md: `^permissionMode:` -> 0/9, control `^name:` -> 9/9. The unattended shape is explicit at .claude/agents/codex-operator.md:4 (\"Use when an operational command must run off this session's clock\") and .claude/agents/codex-advisor.md:57-64 (\"Deliver before you go idle\")."
native_alternative = "The `permissionMode` frontmatter field pins the subagent's mode independently of the session: `plan` for genuinely read-only exploration, or `dontAsk` to auto-deny anything that would prompt while explicitly-allowed tools keep working — the correct posture for a lane with no human at the keyboard."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:297 (field, incl. the `manual` alias needing v2.1.200+) and the mode table at :518-525 — `dontAsk` \"Auto-deny permission prompts. Explicitly allowed tools still work\", `plan` \"Plan mode (read-only exploration)\"."
rationale = "Worth setting, but not uniformly, which is why confidence is 0.5. `plan` is wrong for every agent here — all of them must Write their tracked report under docs/research/kb/reports/agents/ per .claude/rules/agent-report-persistence.md, which plan mode forbids. `dontAsk` is the right fit for the five codex wrappers: an unattended lane that hits a permission prompt cannot answer it (project memory records that AskUserQuestion is stripped from every subagent), so today it stalls where `dontAsk` would fail fast and be reported. codex-operator is the one that needs a deliberate decision rather than a default, since its whole purpose is a state-mutating task."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "agents-11"
unit = "agents"
kind = "agents"
disposition = "keep"
confidence = 0.85
custom_thing = "The four Claude-side role agents themselves — adversarial-critic (attacks a PROPOSAL), claude-code-expert (harness ground truth on THIS machine at THIS version), staleness-auditor (audits this repo's instruction prose for claims reality outgrew), dockerfile-reviewer (this repo's Dockerfile/bake conventions)."
evidence = ".claude/agents/adversarial-critic.md:3, .claude/agents/claude-code-expert.md:3, .claude/agents/staleness-auditor.md:3, .claude/agents/dockerfile-reviewer.md:3 — each description names a repo-specific review lens, and the bodies are built on this repo's own artifacts (e.g. dockerfile-reviewer.md:8-20 encodes mise-system.toml/docker-bake.hcl/bake-action facts that exist nowhere upstream)."
native_alternative = "None. The built-in subagent roster is Explore, Plan, general-purpose, claude, statusline-setup and claude-code-guide; the enabled fable-orchestrator 1.21.0 ships only codex-implementer, codex-reviewer, fable-advisor, grok-implementer, grok-researcher, grok-reviewer and premise-verifier; the enabled codex plugin ships only codex-rescue."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:29-88 (built-in roster incl. the 'Other' table of claude / statusline-setup / claude-code-guide); plugin roster enumerated at /Users/rmanaloto/.claude/plugins/cache/fable-orchestrator/fable-orchestrator/1.21.0/agents/ (7 files) and /Users/rmanaloto/.claude/plugins/cache/openai-codex/codex/1.0.6/agents/ (1 file)."
rationale = "Required justification: none of the roles above is covered by a built-in or by either enabled plugin, and the gap is categorical, not incidental. claude-code-guide (haiku) answers generic 'how does Claude Code work' questions; claude-code-expert exists to answer 'what does THIS host at THIS version do' with a probe and a control arm — a different job with a different evidence standard. fable-orchestrator's codex-reviewer/grok-reviewer review a DIFF by ref; adversarial-critic attacks a PROPOSAL and asks whether it would have caught its own motivating defect — .claude/CLAUDE.md:86-90 explicitly rules that same-family critique is not the cold-review lens, so they are not interchangeable. premise-verifier checks a spec's premises pre-dispatch; staleness-auditor checks shipped prose post-hoc. And no upstream agent knows this repo's bake/Dockerfile invariants. The files stay; agents-1 through agents-10 are how they should be modernised, not arguments to delete them."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "agents-12"
unit = "agents"
kind = "agents"
disposition = "unknown"
confidence = 0.40
custom_thing = "Each codex-* lane is a PAIR: the Claude wrapper in .claude/agents/ plus a codex-side role definition in .codex/agents/, with a bespoke Python consistency gate holding the two halves in sync. Nine .toml files exist there, including mirrors of the four Claude-only agents."
evidence = ".codex/agents/ contains adversarial-critic.toml, claude-code-expert.toml, codex-adversarial-critic.toml, codex-advisor.toml, codex-claude-code-expert.toml, codex-operator.toml, codex-staleness-auditor.toml, dockerfile-reviewer.toml, staleness-auditor.toml. The gate is python/src/dotfiles_setup/codex_agent_parity.py, whose own module docstring states: \"The `codex exec` CLI path the `.md` wrappers actually invoke does NOT read this directory; it is given the prompt on stdin\" — and that the Codex Desktop app both reads and WRITES the directory (four mirrors produced in one batch at an identical 18:52:09 mtime)."
native_alternative = "Possibly none — but the premise is version-bound. That measurement was taken against codex 0.146.0; the host is now codex-cli 0.152.1, and the 0.148-0.152 line added an interactive `codex agents` dashboard (0.149.0) and `@`-mention referencing of other Codex tasks (0.150.0), both of which are agent-surface features that could have given .codex/agents/ a consumer on the CLI path."
alternative_evidence = ".agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md — \"Interactive `codex agents` dashboard: search/start/open/rename/stop tasks with configurable shortcuts — shipped in 0.149.0\" and \"Reference other Codex tasks with `@` mentions; ask agents to read/create/message tasks from the terminal — shipped in 0.150.0\"."
rationale = "The whole justification for maintaining a second surface plus a hand-written parity gate rests on one claim about who reads .codex/agents/, and that claim was measured six minor versions ago on a CLI whose agent surface has since grown. Per .claude/rules/probes-need-a-control-arm.md rule 6, an inherited measurement is not a measurement — it must be re-derived before it is relied on again. If 0.152.1's `codex exec` still ignores the directory, the four NON-prefixed mirrors (adversarial-critic.toml, claude-code-expert.toml, staleness-auditor.toml, dockerfile-reviewer.toml) have no consumer on any path this repo drives and are candidates to retire outright; if it now reads them, the parity gate is under-specified rather than redundant."
probe = "Control-armed, on the host: (1) `printf 'reply with the single word CANARY-A\\n' | PLANNING_DISABLED=1 codex exec --ephemeral --sandbox read-only -` from the repo root with a deliberately absurd instruction planted in .codex/agents/codex-advisor.toml, and see whether it leaks; (2) positive arm — `codex agents --help` and `codex exec --help | grep -i agent` to enumerate whether any flag names an agents directory at 0.152.1; (3) `ls -l --time-style=full-iso .codex/agents/` to check whether Codex Desktop has rewritten any mirror since the 2026-08-31 batch."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-1"
unit = "hk-steps-1"
kind = "hk"
disposition = "refactor"
confidence = 0.90
custom_thing = "`[\"ruff\"]` and `[\"ruff_format\"]` are hand-written SHELL-STRING steps (`check = \"uv run --project python ruff check {{files}}\"` etc.) instead of the hk builtins, with an in-file comment asserting that `(Builtins.ruff) { prefix = ... }` PANICS because \"structured commands can't be combined with `shell` or `prefix`\" (#1067)."
evidence = "hk.pkl:55-65 (the blocker comment + the custom `[\"ruff\"]` block), hk.pkl:84-89 (custom `[\"ruff_format\"]` block with `exclusive = true`), hk.pkl:19 (`min_hk_version = \"1.49.0\"`), docs/hk-builtins-audit.md:152-153 (the generated audit lists the `ruff` and `ruff_format` BUILTINS under \"Not yet considered (100)\", i.e. unwired; the `| ruff | hk.pkl |` rows at docs/hk-builtins-audit.md:94-95 are the *defined step name* table, which also lists hook names like `pre-commit`/`check`/`fix`, so they are not evidence the builtin is adopted)."
native_alternative = "`(Builtins.ruff) { prefix = List(\"uv\", \"run\", \"--project\", \"python\") }` and the same for `Builtins.ruff_format`. hk gained ARGV prefixes for structured commands in 1.55.0 (#1175) — two releases BEFORE this repo's 1.57.0 pin. `prefix` is typed `(String | List<String>)?`: only the STRING form is rejected against an argv command; a `List<String>` prefix is prepended to the argv."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:145 (\"**(step)** support argv prefixes for structured commands ... #1175\", under the `## [1.55.0]` heading at CHANGELOG.md:127 — re-verified this round); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:616-626 (\"Use a string for shell commands or a list of explicit arguments for structured commands\", `prefix: (String | List<String>)?`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/step/types.rs:503-518 (argv branch accepts `CommandPrefix::Argv`, `eyre::bail!` ONLY on `CommandPrefix::Shell(_)` at :507-509); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ruff.pkl:13-27 (`argv = List(\"ruff\", \"check\", \"--force-exclude\", \"{{files}}\")` + a `fix` arm) and .../pkl/builtins/ruff_format.pkl:13-26 (`check_diff` with `--diff` + `effect = \"read\"`, `fix` with `effect = \"write\"`). LIVE PROBE, BOTH ARMS, run against the host's pinned hk 1.57.0 in a scratch repo: LIST prefix -> hk rendered `uv run --project python ruff check --force-exclude bad.py` and `uv run --project python ruff format --quiet --force-exclude --diff bad.py`, no panic (the only error was uv's `Project directory \\`python\\` does not exist`, expected in a scratch dir); STRING prefix on the same config -> `Step 'ruff' can't combine structured argv commands with a shell \\`prefix\\`.` — so the probe discriminates and the in-file claim is true only of the string form."
rationale = "The stated blocker is version-stale, not a property of hk: hk.pkl's comment was written against hk 1.51 (where #1067 introduced argv commands with no prefix escape) and the argv-prefix escape landed in 1.55.0, below the 1.57.0 pin. Adopting the builtins (a) removes the last shell-string commands in this unit, exactly the shape `.claude/rules/zero-bash-logic.md` wants replaced by a structured mechanism; (b) picks up `--force-exclude`, so a future `exclude` in ruff.toml/pyproject would be honoured when hk passes explicit paths (neither config declares one today, so this is robustness, not a live gap); (c) gives ruff_format hk's `check_diff` + `effect = \"read\"` path instead of the current `check`, which is what lets hk apply and re-check the format diff. Two things MUST be carried across or the refactor regresses behaviour: `exclusive = true` on ruff_format (hk.pkl:84-88 — the #268 ordering barrier that replaced the deadlocking `depends`), and a decision on scope, since the builtins select `types = List(\"python\")` repo-wide while the custom steps pin `glob = List(\"python/src/**/*.py\", \"tests/**/*.py\", \"plugins/**/*.py\")`. `min_hk_version` must move to at least 1.55.0 (see hk-steps-1-7) so an older binary refuses the config rather than silently dropping the prefix."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-2"
unit = "hk-steps-1"
kind = "hk"
disposition = "retire"
confidence = 0.90
custom_thing = "`[\"gitleaks\"] = (Builtins.gitleaks) { batch = true }` — a `batch` override on a builtin whose command no longer takes a file list, plus a 15-line comment asserting that the builtin runs `gitleaks dir ... {{ files }}` and silently scans the whole cwd when handed 2+ files."
evidence = "hk-common.pkl:133-137 (the step and its `batch = true`), hk-common.pkl:27-41 (the stale comment: \"The builtin runs `gitleaks dir … {{ files }}` … TWO OR MORE files are silently discarded and it scans the whole cwd instead\"), hk-image.pkl:35 (the same `...common.security` group is spread into the image config, so the image smoke pays it too)."
native_alternative = "Delete the `batch = true` override and use `Builtins.gitleaks` bare. At hk 1.57.0 the builtin already hardcodes the single path `.` — upstream fixed the exact quirk the repo's comment documents, and cites the same gitleaks 8.30.1 source line the repo measured."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/gitleaks.pkl:12-20 — `// gitleaks dir accepts exactly one path. Passing multiple files makes it silently scan \".\". Reference: https://github.com/gitleaks/gitleaks/blob/v8.30.1/cmd/directory.go#L22-L30`, with `argv = List(\"gitleaks\", \"dir\", \"--redact\", \"--verbose\", \"--no-banner\", \".\")` — NO `{{files}}` placeholder (re-verified this round); its test at gitleaks.pkl:26-35 (\"check whole tree beyond selected files\") pins that behaviour upstream. /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/step/job_builder.rs:163-167 — `else if self.batch { files.chunks((files.len() / Settings::get().jobs().get()).max(1)).map(|chunk| StepJob::new(...)) }`: `batch` splits the matched files into one JOB PER CHUNK regardless of whether the command references the files. LIVE PROBE, BOTH ARMS, on this repo with the real config (507 files matched): `HK_JOBS=4 hk check --all --step gitleaks -vv` executed the IDENTICAL `gitleaks dir --redact --verbose --no-banner .` FIVE times (chunks of 126/126/126/126/3); `HK_JOBS=1` executed it ONCE. Host `hw.ncpu = 12`, so the default job count makes every `mise run lint` run ~12 duplicate whole-tree secret scans of a tree the repo's own comment measures at 45.87 MB."
rationale = "`batch` is only meaningful for a command that consumes `{{files}}`; the 1.57.0 gitleaks builtin does not, so every extra chunk is a byte-for-byte repeat of the same whole-tree scan. This is not cosmetic — it is a measured ~12x duplication of the most expensive step in the lint gate, and it scales with the machine's core count, so it gets worse on bigger hosts and on CI runners. Removing the override is behaviour-preserving (coverage is identical: one scan of `.`) and is the only change needed. The retirement must also rewrite hk-common.pkl:27-41, whose premise (\"the builtin runs `gitleaks dir … {{ files }}`\") is now false at the pinned version — the operative half of that comment (that `excludePaths` does NOT constrain gitleaks, which is scoped by `.gitleaks.toml`'s `[[allowlists]] paths`, .gitleaks.toml:12-40) stays true and must be kept."
probe = ""
correction = ""
correction_custom = "**⚠ CITATION CORRECTED (round 5) — RANGE END PAST EOF.** This row's prose elsewhere cites `.gitleaks.toml:12-40`; that file is **34** lines (`wc -l` -> 34) and the `paths` allowlist array closes with `]` at `:34`. Read it as **`:12-34`** — to EOF, which is what the span meant. Found by a mechanical existence check over every anchor in every row, not by a lens; no prior critic reported it."
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-3"
unit = "hk-steps-1"
kind = "hk"
disposition = "retire"
confidence = 0.88
custom_thing = "`[\"shellcheck\"] = (Builtins.shellcheck) { batch = true }` in the image config — a `batch` override that restates the builtin's own default, and that the project config already deleted."
evidence = "hk-image.pkl:40-45 (`// \\`batch\\` is orthogonal and stays.` + `batch = true`), against hk.pkl:188-195 where the same override was removed (`#160 T12 drops the redundant batch override.`) and the step is a bare `[\"shellcheck\"] = Builtins.shellcheck`."
native_alternative = "`[\"shellcheck\"] = Builtins.shellcheck` — the builtin sets `batch = true` itself."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/shellcheck.pkl:13 — `batch = true` as the first field of the builtin Step (followed by `match_any` at :14-18) — re-verified this round. Control arm for the \"redundant\" claim: the same read over the other builtins this unit uses (`typos.pkl:9-24`, `python_check_ast.pkl:9-14`, `gitleaks.pkl:12-20`, `trailing_whitespace.pkl:9-22`) shows NO `batch = true` line in any of them, so the check discriminates and shellcheck is genuinely the one that ships it — which is also why the repo's `batch = true` on `typos` (hk-common.pkl:141-143) is NOT redundant."
rationale = "The two configs disagree about the same builtin for no reason: hk.pkl dropped the override at #160 T12 and hk-image.pkl kept it, so the image config carries a line that changes nothing and reads as a deliberate difference. hk-common.pkl's whole design premise is that host and image must not drift (hk-common.pkl:20-25); a no-op override is the cheapest kind of drift to remove. The autofix consequence of assigning this builtin at 1.57 is a separate finding (hk-steps-1-8) and applies to BOTH configs."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-4"
unit = "hk-steps-1"
kind = "hk"
disposition = "unknown"
confidence = 0.45
custom_thing = "`[\"python_check_ast\"] = (Builtins.python_check_ast) { glob = ..., batch = true }` runs over exactly the same three trees as the `ruff` step, asserting only that each file parses."
evidence = "hk.pkl:90-93 (`glob = List(\"python/src/**/*.py\", \"tests/**/*.py\", \"plugins/**/*.py\")`) versus hk.pkl:61-65 (the `ruff` step's identical glob)."
native_alternative = "`ruff check` itself, already wired in the same hook: ruff emits an `invalid-syntax` diagnostic and exits non-zero on unparseable Python, which is the entire contract of `hk util python-check-ast`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/python_check_ast.pkl:9-14 (the builtin is exactly `argv = List(\"hk\", \"util\", \"python-check-ast\", \"{{files}}\")` over `glob = \"**/*.py\"`, `effect = \"read\"`) and its tests at :17-32 (a `def hello(:` file fails, a valid file passes) — re-verified this round; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/CHANGELOG.md:206 (ruff reports syntax errors as diagnostics). PARTIAL live probe on the repo's pinned ruff: `uv run --project python ruff check --no-cache <file with 'def hello(:'>` -> rc=1 with `invalid-syntax: Expected a parameter or the end of the parameter list`. The probe is NOT yet conclusive: the control file (valid syntax) also exited 1, on unrelated CPY001/INP001 because it sat outside the project tree, so the pass arm was contaminated and this has not been run through the hk steps themselves."
rationale = "The redundancy is plausible and the glob overlap is exact, but it is not proven at the STEP level, and two things could break it: the repo's `ruff` step does not pass `--force-exclude` today (so the two steps' effective file sets could diverge once the builtin adoption in hk-steps-1-1 lands), and `python_check_ast` runs as `hk util` with no Python/uv dependency at all, so it still reports when the uv environment is broken — a cheap failure mode ruff cannot cover. Settle it with the probe before retiring anything; if it holds, the step belongs in the NOT_ADOPTED table in python/src/dotfiles_setup/hk_builtins_audit.py:75 rather than being silently deleted."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && cp python/src/dotfiles_setup/lint.py /tmp/lint.py.bak && printf 'def broken(:\\n' >> python/src/dotfiles_setup/lint.py && mise exec -- hk check --all --step ruff > /tmp/arm_ruff.log 2>&1; echo \"ruff rc=$?\" >> /tmp/arm_ruff.log; mise exec -- hk check --all --step python_check_ast > /tmp/arm_ast.log 2>&1; echo \"ast rc=$?\" >> /tmp/arm_ast.log; cp /tmp/lint.py.bak python/src/dotfiles_setup/lint.py; grep -H 'rc=' /tmp/arm_ruff.log /tmp/arm_ast.log   # redundant iff BOTH arms are rc=1; then re-run both on the clean tree as the pass arm"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-5"
unit = "hk-steps-1"
kind = "hk"
disposition = "refactor"
confidence = 0.50
custom_thing = "One `gitleaks` step (whole-tree `gitleaks dir .`) is spread into BOTH the pre-commit hook and the check/fix hooks, so every commit pays a full-repository secret scan to look at a handful of staged files — and the image config pays it again inside the devcontainer smoke."
evidence = "hk-common.pkl:133-137 (the single `security` group), hk.pkl:38 (`...common.security` inside the hoisted `allSteps`), hk.pkl:738/750/756 (`...allSteps` spread into `pre-commit`, `check` and `fix`), hk-image.pkl:35 (same group in the image pre-commit hook, which runs with `fix = true` at hk-image.pkl:30)."
native_alternative = "`Builtins.gitleaks_staged` — hk ships a second, pre-commit-shaped secrets builtin that runs `gitleaks git --pre-commit --redact --staged --verbose --no-banner`, i.e. scans the staged CONTENT rather than the working tree. Wiring `gitleaks_staged` into pre-commit and leaving `gitleaks` on the `check`/CI path is the upstream-intended split."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/gitleaks_staged.pkl:9-25 (the step and its argv, referencing gitleaks' own `.pre-commit-hooks.yaml` at v8.30.1) and :26-35+ (its tests prove it reads staged content and ignores unstaged files) — re-verified this round. The builtin is currently unwired here: docs/hk-builtins-audit.md:145 lists `gitleaks_staged` under \"Not yet considered (100)\"."
rationale = "This is a scoping choice, not a defect, so it is offered rather than asserted — but the cost is real and compounds with hk-steps-1-2: today each commit runs a 45.87 MB whole-tree scan (x12 while the `batch` override stands). `gitleaks_staged` is the mechanism upstream provides for the commit boundary, and keeping `gitleaks` on `check --all` preserves the whole-tree guarantee that CI and `mise run lint` depend on. Two things to verify before adopting: `gitleaks git --staged` reads the index, so it will NOT see the working-tree-only edits that `hk run check --all` looks at (that is the point, but it changes what a local `hk fix` sees), and `.gitleaks.toml`'s `[[allowlists]] paths` (.gitleaks.toml:12-40) must apply identically on the `git` subcommand as on `dir` — the config's own header (.gitleaks.toml:1-6) records that `gitleaks git` already reports ZERO real secrets on this repo, which is a favourable prior but not the same measurement."
probe = ""
correction = ""
correction_custom = "**⚠ CITATION CORRECTED (round 5) — RANGE END PAST EOF.** Same defect as `hk-steps-1-2`: `.gitleaks.toml:12-40` against a **34**-line file; read it as **`:12-34`**. Found by the round-5 mechanical anchor sweep."
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-6"
unit = "hk-steps-1"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "The bulk of this unit — `trailing_whitespace`, `newlines`, `mixed_line_ending`, `fix_smart_quotes`, `check_added_large_files`, `check_merge_conflict`, `check_case_conflict`, `check_symlinks`, `check_executables_have_shebangs`, `byte_order_marker`, and `typos` — carries no custom logic at all: each is a bare `= Builtins.<name>` assignment, or a builtin plus a single scoping/`batch` override."
evidence = "hk-common.pkl:101-106 (hygiene: four bare Builtins assignments), hk-common.pkl:109-123 (safety: seven bare assignments, incl. the migrated `byte_order_marker` at :118 and the documented omission of `no_commit_to_branch` from this group at :119-122, which hk.pkl:737 instead wires into pre-commit ONLY), hk-common.pkl:140-144 (`typos` + `batch = true`)."
native_alternative = "None needed — these ARE the native mechanism. No newer hk builtin supersedes any of them, and no other linter in the repo's pin set (agnix, claude-code-lint, cclint, agents-lint, zizmor, ghalint, actionlint, ast-grep, ruff, rumdl, biome, taplo, yamllint) covers whitespace/BOM/line-ending/large-file/merge-conflict/case-conflict/symlink/shebang/smart-quote checks. The one adjacent candidate, `editorconfig-checker` (wired at hk.pkl:42), cannot overlap: there is NO `.editorconfig` in this repo (`ls .editorconfig` -> `No such file or directory`; `git ls-files | grep -i editorconfig` -> 0 rows, against a control of `git ls-files | grep -c pkl` -> non-zero), so it enforces no whitespace/newline rule that could duplicate these builtins."
alternative_evidence = ".agent/kb/raw/hk-releases-since-pin.digest.md — the only hk release newer than the 1.57.0 pin is v1.58.1, whose body states verbatim \"A small documentation-only release. There are no changes to hk's runtime behavior.\", so the builtin surface at the pin IS the current surface (the digest records its own control arm: the pin 1.57.0 itself appears in the fetched list). Cross-checked against the corpus tree, whose HEAD is exactly the 1.57.0 release (`git -C .../sources/hk log -1` -> `6189ceff … chore: release v1.57.0 (#1223)`, and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:3 `## [1.57.0] … - 2026-08-30`); its 153 builtin files are enumerated at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ and none duplicates these eleven. Per-builtin re-verification this round: trailing_whitespace.pkl:9-22, newlines.pkl:9-22, mixed_line_ending.pkl:9-22, byte_order_marker.pkl:9-20, fix_smart_quotes.pkl:9-20 (each a `check_diff` read arm + a `fix` write arm over `types = List(\"text\")`), check_added_large_files.pkl:8-15 (`glob = \"**/*\"`, default 500KB limit per its test at :17-22), detect_private_key.pkl:9-14, python_check_ast.pkl:9-14. `batch = true` is legitimate on `typos` (unlike on `gitleaks`) because that builtin DOES consume the file list: typos.pkl:11-24 renders `typos --force-exclude --diff {{files}}` / `typos --force-exclude --write-changes {{ files }}`. The deprecated aliases the repo already migrated off are still present upstream and still deprecated — check_byte_order_marker.pkl:6-10 and fix_byte_order_marker.pkl:6-10 both carry `@Deprecated { since = \"1.30.0\" message = \"Use \\`Builtins/byte_order_marker.pkl\\`\" }` — so hk-common.pkl:116-118's migration is current, not stale."
rationale = "Required justification, not a default: these steps already ARE the native feature — there is nothing homegrown to retire, the assignment form is the one #154 mandates (`= Builtins.name`, never a bare `[\"name\"] {}` block, which constructs an empty Step hk silently skips), and the only overrides present are a legitimate `batch` on a file-consuming command and the shared `excludePaths` list. No suites.toml contract duplicates any of them either: a grep of python/verification/suites.toml for these step names returns only prose mentions inside `no_env_dump`/`hk_audit`/`lint-delta` descriptions (suites.toml:1696, :1737, :2362), never a `[[suite]]` binding one of these gates — so there is no contract-level redundancy to collapse. The one adjacent risk is not a supersession but a coverage question the exclude list creates: hk's `exclude` does not honour `!`-negation (hk-common.pkl:72-76, measured both arms), which is why `.codex/` had to be enumerated file-by-file — any future broad entry added to `excludePaths` silently blinds all of these builtins at once, and that is a review-time hazard rather than a modernization one."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-7"
unit = "hk-steps-1"
kind = "hk"
disposition = "refactor"
confidence = 0.78
custom_thing = "`min_hk_version = \"1.49.0\"` in both configs — the declared floor is FOUR minor versions below the builtin shapes the configs actually consume, so hk's own binary-vs-config drift guard cannot fire on the drift it was added for."
evidence = "hk.pkl:19 (`min_hk_version = \"1.49.0\"` with the comment \"an older hk binary refuses this config instead of silently misreading 1.49 features\"), hk-image.pkl:19 (same value, same rationale), against hk.pkl:1/8/11 and hk-image.pkl:11/14 which import the hk **1.57.0** pkl package, and hk.pkl:190-194 which itself states the shellcheck builtin's `match_any` selector arrived in hk 1.51 (#1055). The steps in this unit that depend on post-1.49 Step fields: `shellcheck` (`match_any`, hk.pkl:195 / hk-image.pkl:43) and, at 1.57, `check_after_diff` (inherited by the same assignment)."
native_alternative = "hk's own `min_hk_version` field — set it to the version whose Step surface the config actually uses (1.57.0 at today's pin, or at minimum 1.55.0 if hk-steps-1-1's argv `prefix` lands). This is not new custom code; it is the native guard, correctly parameterised."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:13-15 (`min_hk_version` documented, defaulting to `{{version | truncate(length=1)}}.0.0`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/config.rs:129-133 (`if let Some(min_hk_version) = &self.min_hk_version { version::version_cmp_or_bail(min_hk_version)?; }` — the guard is enforced at config init, so raising the value is sufficient and self-executing). WHY THE LOW FLOOR IS UNSAFE: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/step/types.rs:140 — `#[cfg_attr(debug_assertions, serde(deny_unknown_fields))]` on `pub struct Step`, i.e. unknown-field rejection is compiled in ONLY for debug builds, so a RELEASE hk binary silently ignores Step fields it does not know. `match_any` is `Step` field types.rs:171, added by #1055 in 1.51.0 (CHANGELOG.md:311 under the `## [1.51.0]` heading at CHANGELOG.md:306); `check_after_diff` is `Step` field Config.pkl:510, added by #1243 in 1.57.0 (CHANGELOG.md:13). A released hk 1.49.x or 1.50.x binary therefore ACCEPTS these configs (min_hk_version satisfied) and then drops `match_any` on the floor — shellcheck's file selection silently becomes empty/default rather than erroring."
rationale = "The guard is the right native mechanism and it is simply mis-set: the comment at hk.pkl:15-18 says the point is that \"an older hk binary refuses this config instead of silently misreading\" it, and at 1.49.0 an older binary does exactly the misreading, because hk's release builds do not deny unknown Step fields. The blast radius is not hypothetical for this repo: hk-image.pkl ships into the devcontainer image, where the hk binary comes from `.config/mise/conf.d/shared.toml` and can lag a host-side pkl bump until the base image is rebuilt — that is precisely the host/image skew this file exists to guard. Raise it in the same change as any builtin adoption (hk-steps-1-1 needs >=1.55.0; the shellcheck/`check_after_diff` surface needs >=1.57.0), and keep it in lockstep with the three pkl import URLs, which the existing `hk_version_parity` step (hk.pkl, check-H) already binds — note that step compares the URL pins to the shared.toml binary pin, NOT to `min_hk_version`, which is why this value could drift unnoticed."
probe = "grep -n 'min_hk_version' hk.pkl hk-image.pkl && grep -n 'hk@1\\.' hk.pkl hk-image.pkl hk-common.pkl && grep -n 'hk_version_parity' -A 12 hk.pkl   # confirm hk_version_parity does not already bind min_hk_version before adding the raise"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-8"
unit = "hk-steps-1"
kind = "hk"
disposition = "refactor"
confidence = 0.72
custom_thing = "The `shellcheck` step is documented in-repo as \"lint only\" and is assigned bare from the builtin — but at the 1.57.0 pin that builtin now carries an AUTOFIX path (`check_diff` via `shellcheck --format=diff` plus `check_after_diff = true`), and every hook this repo spreads it into runs with `fix = true`."
evidence = "hk.pkl:188-195 (`// --- Shell (lint only; #154 drops shfmt — it restyles hand-formatted scripts). shellcheck still enforces correctness. …` immediately above `[\"shellcheck\"] = Builtins.shellcheck`), hk.pkl:710-711 (`[\"pre-commit\"] { fix = true`), hk.pkl:753-754 (`[\"fix\"] { fix = true` — the hook `mise run fmt` drives), hk-image.pkl:29-30 (`[\"pre-commit\"] { fix = true`) with the same builtin at hk-image.pkl:43. The repo also runs two custom whole-tree shell gates over the same scripts, `require_pipefail` (hk.pkl:206-210) and `no_grep_q_under_pipefail` (hk.pkl:212+), whose subjects are now writable by the step above them."
native_alternative = "The capability is already adopted implicitly (it rides the bare `= Builtins.shellcheck` assignment). The decision to make explicit is whether to KEEP it — and if kept, to delete the now-false \"lint only\" comment; if not wanted, to override `check_diff = null` / pin `check_after_diff = false` on the step rather than leave the config's prose contradicting its behaviour."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/shellcheck.pkl:23-32 — the upstream comment \"shellcheck has no in-place write mode; `--format=diff` is its only autofix path, and only some findings are fixable. `check_after_diff` reruns `check` once hk has applied the patch…\", with `check_diff` = `argv = List(\"shellcheck\", \"--format=diff\", \"{{files}}\")` and `check_after_diff = true`. Provenance: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:10 (\"**(builtins)** add shellcheck fix via check_diff … #1237\") and CHANGELOG.md:13 (\"**(step)** recheck applied diffs … #1243\"), both under `## [1.57.0]` at CHANGELOG.md:3 — i.e. both landed AT this repo's pin, after the #154/#160-era comments were written. The `check_after_diff` field itself: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:510."
rationale = "This is the inverse of the usual audit finding: nothing custom needs retiring, but a native behaviour changed under a config whose prose still describes the old one, and the prose is what a reviewer trusts. `mise run fmt` and every commit hook can now rewrite tracked `*.sh`/`*.bash` files (and, via the builtin's `match_any` types selector, extensionless shebang scripts) using shellcheck's patch output — a class of edit the repo previously excluded on purpose when it dropped shfmt for restyling hand-formatted scripts. It interacts directly with this repo's shell posture: `.claude/rules/zero-bash-logic.md` treats the remaining `scripts/*.sh` as a frozen, line-budgeted surface (`bash_logic_budget`), and an automatic patch that shrinks or grows one of those files moves it against its budget without anyone deciding to. Confirm the behaviour is wanted, then either keep it and rewrite hk.pkl:188-194, or disable it explicitly — either way the comment and the behaviour must agree."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && cp scripts/ensure-docker-up.sh /tmp/eds.bak && printf '\\nfoo=$(echo hi)\\necho $foo\\n' >> scripts/ensure-docker-up.sh && mise exec -- hk fix --step shellcheck > /tmp/sc_fix.log 2>&1; echo \"rc=$?\" >> /tmp/sc_fix.log; git diff --stat -- scripts/ensure-docker-up.sh; cp /tmp/eds.bak scripts/ensure-docker-up.sh   # a non-empty diff on a file hk was NOT asked to format proves the autofix path is live under fix=true"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-1-9"
unit = "hk-steps-1"
kind = "hk"
disposition = "keep"
confidence = 0.80
custom_thing = "`[\"detect_private_key\"] = Builtins.detect_private_key` sits alongside two whole-tree secret scanners (`gitleaks` in the same `common.security` group, `betterleaks` in hk.pkl), so three steps in one hook overlap on \"is there a private key in this repo\"."
evidence = "hk-common.pkl:110 (`[\"detect_private_key\"] = Builtins.detect_private_key`), hk-common.pkl:133-137 (`gitleaks`), hk.pkl:320-322 (`[\"betterleaks\"] = (Builtins.betterleaks) { batch = true }`, described at hk.pkl:311-319 as \"a SECOND OPINION rather than a replacement\"), .gitleaks.toml:9-10 (`[extend] useDefault = true`)."
native_alternative = "gitleaks' own default ruleset already carries a `private-key` rule, and this repo opts into it via `useDefault = true` — so on paper the dedicated hk builtin is a third detector of the same class."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/config/gitleaks.toml:2785 (`id = \"private-key\"` in the generated default config) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/cmd/generate/config/rules/privatekey.go:14 — the rule's regex is `(?i)-----BEGIN[ A-Z0-9_-]{0,100}PRIVATE KEY(?: BLOCK)?-----[\\s\\S-]{64,}?KEY(?: BLOCK)?-----`, i.e. it requires at least 64 characters of key BODY between the markers. The hk builtin has no such floor: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/detect_private_key.pkl:17-25 fails (`code = 1`) on a three-line RSA block whose body is the literal `MIIEpAIBAAKCAQEA...` (well under 64 chars), and :35-37 passes on an `ssh-rsa` PUBLIC key — so the two detectors are NOT interchangeable in the direction that matters."
rationale = "Required justification: the overlap is real but the coverage is not a superset. gitleaks' default `private-key` rule needs >=64 characters of key body, so a truncated, redacted, or line-wrapped-into-a-doc key header that hk's `detect_private_key` flags would pass gitleaks silently — the classic \"a second probe of the same fact by a different route\" case that `.claude/rules/probes-need-a-control-arm.md` asks for, with the two arms differing in a documented, citable way. Two further reasons to keep it: it runs as `hk util` with no external binary (detect_private_key.pkl:11-14), so it still fires when the gitleaks tool is missing or wedged, which is exactly the failure mode `hk-steps-1-2`'s whole-tree scan is most exposed to; and its `{{files}}` scoping means it reports the offending PATH, where `gitleaks dir .` reports against the whole tree. It costs nothing measurable (one `hk util` invocation on already-matched text files) and, unlike the `batch` override in hk-steps-1-2, adds no duplicated work. Record it in the NOT_ADOPTED-style rationale rather than deleting it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-1"
unit = "hk-steps-2"
kind = "hk"
disposition = "refactor"
confidence = 0.85
custom_thing = "`docker_bake_check` (hk.pkl:134-138) is named for docker-bake.hcl but never reads it, and its command duplicates `devcontainer_json_validate`'s layer 2 verbatim."
evidence = "hk.pkl:135 `glob = List(\"docker-bake.hcl\", \".devcontainer/Dockerfile\")`; hk.pkl:136-137 `check = \"scripts/ensure-docker-up.sh && devcontainer read-configuration --workspace-folder .\"`. The identical pair already runs inside `devcontainer_json_validate` -> scripts/validate-devcontainer-json.sh:48 (`\"$(dirname \"$0\")/ensure-docker-up.sh\"`) and :51-56 (`devcontainer read-configuration --workspace-folder .`). `devcontainer read-configuration` parses .devcontainer/devcontainer.json, whose only build input is `\"dockerfile\": \"Dockerfile.host-user\"` (.devcontainer/devcontainer.json:92) — docker-bake.hcl is not referenced anywhere in that path (probed: `grep -rn 'docker-bake' scripts/*.sh` returns only scripts/devcontainer-smoke.sh:75, a comment). So staging docker-bake.hcl triggers a check that cannot see it, and the devcontainer parse (which may cold-start Docker Desktop for up to 60s — scripts/ensure-docker-up.sh:20,60-68) runs twice per pre-commit."
native_alternative = "Delete the duplicated `devcontainer read-configuration` invocation (it is already covered by `devcontainer_json_validate`) and validate the bake file with a real bake-aware tool: hk 1.57 ships an HCL builtin, `Builtins.hclfmt`, whose selector already covers docker-bake.hcl."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/hclfmt.pkl:8-13 — `hclfmt = new Config.Step { glob = \"**/*.hcl\" ... argv = List(\"hclfmt\", \"-w\", \"{{files}}\") }` (hk tree is at the v1.57.0 tag: `git log -1` -> 6189ceff `chore: release v1.57.0 (#1223)`). Note hclfmt is a `fix`-only formatter, so it covers HCL syntax, not bake semantics — hence the probe below for the semantic half."
rationale = "Two defects in one step: (a) the glob promises bake-file coverage the command cannot deliver — a check that can only pass for docker-bake.hcl edits, exactly the shape .claude/rules/probes-need-a-control-arm.md rejects; (b) the command is a verbatim duplicate of validate-devcontainer-json.sh's layer 2, paying the Docker-daemon preflight twice. Refactor rather than retire because the bake file genuinely needs a gate — just not this one."
probe = "docker buildx bake -f docker-bake.hcl --print dev >/dev/null; echo \"rc=$?\"  # then arm it: `sed 's/target \"dev\"/target dev/' docker-bake.hcl > /tmp/bad.hcl && docker buildx bake -f /tmp/bad.hcl --print dev; echo rc=$?` must be non-zero. Confirms whether `--print` is a usable semantic validator for the bake half (no docker docs in the offline corpus)."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 3
survived = false
schedule = ""

[[finding]]
id = "hk-steps-2-2"
unit = "hk-steps-2"
kind = "hk"
disposition = "refactor"
confidence = 0.90
custom_thing = "`devcontainer_json_validate` layer 3 fetches the devcontainer spec JSON schema over the network from an UNPINNED `main` ref on every run."
evidence = "hk.pkl:149-152 `check = \"scripts/validate-devcontainer-json.sh\"`; scripts/validate-devcontainer-json.sh:32 `SCHEMA_URL=\"https://raw.githubusercontent.com/devcontainers/spec/main/schemas/devContainer.schema.json\"` and :71 `check-jsonschema --schemafile \"${SCHEMA_URL}\" \"${tmp}\"`. `main` is a moving ref, so the gate's verdict can change with no diff in this repo, and it is network-dependent inside a pre-commit hook."
native_alternative = "The repo already owns a vendored-schema mechanism with a pinned tag + sha256 and an offline drift check: `schemas/sources.toml` + `dotfiles_setup.schema_vendor`, driven by `mise run schema-vendor-refresh` (network, CI) and `mise run schema-vendor-check` (offline, wired into `mise run verify`). Add a fourth `[[schema]]` entry for the devcontainer spec and point layer 3 at the vendored file."
alternative_evidence = "schemas/sources.toml:1-16 (module docstring: \"Read by `dotfiles_setup.schema_vendor` — both the offline drift check (wired into `mise run verify`) and the network-using `schema-vendor refresh` command\") and :18-40 (three existing entries — mise, ruff, typos — each with `version`, `source` at a pinned tag, and `sha256`). AGENTS.md Quick Start / .claude/rules/mise-tasks-only.md table row: \"hand-editing `schemas/*.json` or its `version` in `schemas/sources.toml` -> `mise run schema-vendor-refresh`\"."
rationale = "A pinned-and-hashed vendored schema is strictly better than a live `main` fetch on both axes the repo already cares about (reproducibility and no-network-in-a-gate), and the mechanism exists and is already wired into `verify`. Probed the cheaper alternative first and ruled it out with a control arm: `check-jsonschema --builtin-schema` offers 26 vendored schemas (vendor.azure-pipelines ... vendor.woodpecker-ci) and NONE of them is a devcontainer schema — so the tool's own vendoring cannot substitute, and the repo's must."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-3"
unit = "hk-steps-2"
kind = "hk"
disposition = "refactor"
confidence = 0.80
custom_thing = "`no_lint_skip` (hk.pkl:110-127) is a 9-alternation `grep -rn` inline-bash check banning every Python lint suppression."
evidence = "hk.pkl:125-126 `check = \"! grep -rn --include='*.py' 'noqa\\\\|type: ignore\\\\|ty: ignore\\\\|ty:ignore\\\\|ruff:ignore\\\\|ruff:file-ignore\\\\|ruff:disable\\\\|pylint: disable\\\\|nosec' python/src/ tests/ plugins/\"`. The `glob` at hk.pkl:111 is decorative — the command ignores `{{files}}` and re-scans three whole trees on every trigger."
native_alternative = "ruff already enforces the BLANKET half natively, and it is already switched on here: PGH004 `blanket-noqa` and PGH003 `blanket-type-ignore` (plus RUF100 unused-noqa) are all selected by `select = [\"ALL\"]`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff_linter/src/rules/pygrep_hooks/rules/blanket_noqa.rs:11-15 — \"## What it does / Check for `noqa` annotations that suppress all diagnostics, as opposed to targeting specific diagnostics.\"; .../crates/ruff_linter/src/checkers/tokens.rs:48 `pygrep_hooks::rules::blanket_type_ignore(context, comment_ranges, locator);`; .../crates/ruff_linter/src/rules/pygrep_hooks/mod.rs:19 `#[test_case(Rule::BlanketNOQA, Path::new(\"PGH004_0.py\"))]`. Repo side: python/pyproject.toml:63-64 `[tool.ruff.lint]` / `select = [\"ALL\"]`. Control arm for the corpus grep: `grep -rln RUF100` in the same tree returns hits (ruff.schema.json, crates/ruff/tests/cli/lint.rs), so a 0-result would have been meaningful."
rationale = "Native covers PART of it — ruff rejects bare `# noqa` and bare `# type: ignore` but deliberately BLESSES coded ones (`# noqa: F403` is the rule's own recommended fix), which is precisely what this repo bans. So the step keeps a real job, but its body should shrink to the residue ruff cannot express (coded suppressions, `nosec`, `pylint: disable`, the `ty:`/`ruff:` spellings) and move out of an escaped shell string into python, per the house pattern in finding hk-steps-2-6."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-4"
unit = "hk-steps-2"
kind = "hk"
disposition = "refactor"
confidence = 0.80
custom_thing = "`require_pipefail` (hk.pkl:206-210) and `no_grep_q_under_pipefail` (hk.pkl:232-236) are multi-statement bash programs embedded as escaped pkl strings."
evidence = "hk.pkl:208-209 — `\"rc=0; for f in $(git ls-files '*.sh'); do first=$(head -n1 \\\"$f\\\"); case \\\"$first\\\" in \\\"#!\\\"*) ;; *) continue ;; esac; grep -qE 'set -[a-z]*o pipefail' \\\"$f\\\" || { ...; rc=1; }; done; exit $rc\"`; hk.pkl:234-235 — a ~400-character single-line loop with nested escaping (`\\\\\\\"\\\\$out\\\\\\\"`) over `.devcontainer/Dockerfile` + `git ls-files '*.sh'`. Both declare a `glob` but ignore `{{files}}` and enumerate the whole index instead."
native_alternative = "The house pattern for a whole-tree gate in this repo: `check = \"uv run --project python dotfiles-setup <subcommand>\"`, backed by a module with a pure `find_violations(repo_root)` and a suites.toml wiring contract. Four sibling steps in this same file already do exactly this."
alternative_evidence = "hk.pkl:249 `check = \"uv run --project python dotfiles-setup bash-budget\"`; hk.pkl:269 `... dotfiles-setup platform-literals`; hk.pkl:297 `... dotfiles-setup classifier-axes`; hk.pkl:308 `... dotfiles-setup env-blob-scan`. The rationale is written into hk.pkl itself at :244-246 — \"The check LOGIC lives in python (dotfiles_setup.bash_budget) — a big inline-bash grep here would itself violate the policy it enforces\" — and into python/src/dotfiles_setup/bash_budget.py:28-31. Each of those four also carries a suites.toml contract (python/verification/suites.toml:1389/1401 `workflow.bash-logic-enforcement`, :2145/2159 classifier axes, :1710 `no_env_dump\"`); a grep of suites.toml for `require_pipefail` / `no_grep_q` returns nothing, so these two have no wiring contract at all. No hk builtin covers either check: the v1.57.0 builtin set has exactly three shell entries (shellcheck.pkl, shellharden.pkl, shfmt.pkl) out of ~140 in /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/."
rationale = "Nothing native supersedes the CHECKS — they encode two repo-specific invariants (#142, #289) that no linter ships — so this is not a retire. But the IMPLEMENTATION is the exact shape .claude/rules/zero-bash-logic.md routes to python, and the file's own comment at :244-246 states the principle while three steps below it violate it. Moving them also buys them the suites.toml wiring contract every python-backed sibling has and these two lack."
probe = "uv run --project python pytest tests/ -x -q -k 'pipefail or grep_q'  # confirm no existing python-side coverage before writing the module; expect 0 collected (control arm: -k 'bash_budget' collects)."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-5"
unit = "hk-steps-2"
kind = "hk"
disposition = "refactor"
confidence = 0.75
custom_thing = "`dockerfile_host_user_thin_overlay` (hk.pkl:158-162) is a second, hand-rolled per-file line-budget enforcer, with the cap hard-coded inside an escaped shell string."
evidence = "hk.pkl:160-161 `check = \"lines=$(wc -l < .devcontainer/Dockerfile.host-user); [ \\\"$lines\\\" -le 89 ] || { echo \\\"Dockerfile.host-user is $lines lines, cap is 89 (baseline 79 + 10). ...\\\"; exit 1; }\"`. The cap `89` and its justification (\"baseline 79 + 10\") live only in this string, in two places that must agree."
native_alternative = "The repo already owns a declarative per-file line-budget registry in python — `bash_budget.py`'s `ALLOWLIST: dict[str, BashAllowance]`, where each entry is `(max_lines, one-line justification)`, with growth-past-baseline failing, shrinking always allowed, and a `stale` kind so the map cannot rot after a file is deleted. Generalise its scope (or add a sibling registry) instead of a second implementation."
alternative_evidence = "python/src/dotfiles_setup/bash_budget.py:54-56 `@dataclass(frozen=True) class BashAllowance:  \"\"\"A permitted in-scope shell script: its line budget + why bash is OK.\"\"\"`; :66-84 the `ALLOWLIST` map with `BashAllowance(171, \"docker build/pull timing harness ...\")`-style entries; :17-26 the docstring stating the two mechanisms (allowlist gates NEW files, per-file budget flags GROWTH, stale entries FAIL). Consumed by hk.pkl:249."
rationale = "This is the same enforcement concept implemented twice, once declaratively in python with staleness detection and once as an inline `wc -l` with a magic number. Folding it into the existing registry gets the budget, its justification, and its staleness guarantee in one reviewable diff-shaped place — and removes an inline-bash body the zero-bash-logic policy would reject anywhere else."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-6"
unit = "hk-steps-2"
kind = "hk"
disposition = "refactor"
confidence = 0.80
custom_thing = "The zero-bash-logic enforcer cannot see the bash it most needs to see: `bash_budget.py` scopes itself to two shell-script directories, so inline `check = \"<shell>\"` bodies in hk.pkl / hk-image.pkl are invisible to it."
evidence = "python/src/dotfiles_setup/bash_budget.py:48-51 `SCOPE_PATHSPECS: tuple[str, ...] = (\"scripts/*.sh\", \".devcontainer/scripts/*.sh\",)`; hk.pkl:248 `glob = List(\"scripts/*.sh\", \".devcontainer/scripts/*.sh\")`. Four members of this unit are bash logic living OUTSIDE that scope: `no_lint_skip` (hk.pkl:125-126), `require_pipefail` (:208-209), `no_grep_q_under_pipefail` (:234-235), `dockerfile_host_user_thin_overlay` (:160-161) — the last three each longer than several allowlisted scripts' justifications, and none of them counted against any budget."
native_alternative = "Extend the existing python enforcer to also parse the `check`/`fix` string bodies out of the tracked `.pkl` configs and apply the same allowlist+budget rules — the same `find_violations(repo_root)` seam, one more source of shell."
alternative_evidence = "python/src/dotfiles_setup/bash_budget.py:28-31 — \"The check logic lives here (not in a big inline-bash hk step) so the enforcer does not itself violate the policy it enforces\"; .claude/rules/zero-bash-logic.md §'Applies to' — \"Every `scripts/*.sh` and `.devcontainer/scripts/*.sh` in this repo\", i.e. the rule's own scope has the same hole. The chain is contract-asserted at python/verification/suites.toml:1389 (`workflow.bash-logic-enforcement`) — which asserts the WIRING exists, not that the scope is complete."
rationale = "A gate that cannot see the largest concentration of the thing it forbids is a check that can only pass in that region (.claude/rules/probes-need-a-control-arm.md rule 3 — a bound-limited search turns 'absent' into 'unreachable'; the bound here is `SCOPE_PATHSPECS`). This is distinct from findings 3/4/5, which are about the individual steps: this one is about why nothing flagged them."
probe = "uv run --project python dotfiles-setup bash-budget; echo \"rc=$?\"  # then arm it: append a 30-line inline `check = \"...\"` step to hk.pkl and re-run — it must still return rc=0, proving the blind spot rather than assuming it."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-7"
unit = "hk-steps-2"
kind = "hk"
disposition = "refactor"
confidence = 0.90
custom_thing = "hk-image.pkl's `shellcheck` step overrides `batch = true`, which the v1.57.0 builtin already sets — and hk.pkl dropped the identical override for that exact reason."
evidence = "hk-image.pkl:41-45 — comment \"`batch` is orthogonal and stays\" followed by `[\"shellcheck\"] = (Builtins.shellcheck) { batch = true }`. hk.pkl:188-195 says the opposite for the same builtin — \"#160 T12 drops the redundant batch override\" — and uses the bare form `[\"shellcheck\"] = Builtins.shellcheck` at hk.pkl:195."
native_alternative = "`Builtins.shellcheck` as-is: the builtin's own definition sets `batch = true` on its first property."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/shellcheck.pkl:12-18 — `shellcheck = new Config.Step { batch = true; match_any = List(FileSelector { glob = List(\"**/*.sh\", \"**/*.bash\") }, FileSelector { types = List(\"sh\", \"bash\") }) ... }` (tree at tag v1.57.0). The same file also ships `check_diff` + `check_after_diff = true` at :28-32, which the bare form inherits."
rationale = "A dead override plus a comment asserting it is load-bearing, contradicted by the sibling file's comment about the same builtin. Harmless today, but it is the kind of stale local override that survives a builtin change and then silently pins old behaviour. Replace with `[\"shellcheck\"] = Builtins.shellcheck` so hk.pkl and hk-image.pkl read identically."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-8"
unit = "hk-steps-2"
kind = "hk"
disposition = "refactor"
confidence = 0.80
custom_thing = "Two builtins carry hand-enumerated `glob` overrides that today merely restate (hadolint) or narrow (python_debug_statements) the builtin's own default, so new files silently escape."
evidence = "hk.pkl:131-133 `[\"hadolint\"] = (Builtins.hadolint) { glob = List(\".devcontainer/Dockerfile\", \".devcontainer/Dockerfile.host-user\") }` — probed `git ls-files | grep -i dockerfile`: exactly those two tracked Dockerfiles exist, so the override is currently a no-op enumeration that will go stale the moment a third is added. hk.pkl:94-97 `[\"python_debug_statements\"] = (Builtins.python_debug_statements) { glob = List(\"python/src/**/*.py\", \"tests/**/*.py\", \"plugins/**/*.py\"), batch = true }` — probed `git ls-files '*.py' | grep -v '^python/src/\\|^tests/\\|^plugins/'`: empty, so this too is currently equivalent to the builtin's `**/*.py`."
native_alternative = "Drop the `glob` overrides and take the builtins' defaults, which are broader and self-maintaining: hadolint `glob = \"**/Dockerfile*\"`, python_debug_statements `glob = \"**/*.py\"`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/hadolint.pkl:13 `glob = \"**/Dockerfile*\"`; .../python_debug_statements.pkl:10 `glob = \"**/*.py\"` (both at tag v1.57.0). Overriding a builtin property is the documented customisation path either way — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/builtins.md:28-33 shows `[\"prettier\"] = (Builtins.prettier) { glob = List(...) }` — so keeping only `batch = true` is a supported shape."
rationale = "Both overrides are coverage ceilings that happen to be flush with the floor today. The failure mode is silent — a new Dockerfile or a `.py` outside the three trees is simply not linted, and nothing goes red — which is why the enumerations should be replaced by the builtins' globs rather than re-audited later. `batch = true` on python_debug_statements is NOT redundant (the builtin does not set it) and must stay."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-9"
unit = "hk-steps-2"
kind = "hk"
disposition = "keep"
confidence = 0.85
custom_thing = "`py_ty` (hk.pkl:103-106) is a custom whole-directory step (`uv run --project python ty check --project python python/src tests plugins`) while hk 1.57 ships a `ty` builtin."
evidence = "hk.pkl:98-106 — the step plus its rationale comment: \"py_ty STAYS custom whole-dir (#160 T12 probe): ty is a cross-module analyzer — `ty check pkg/a.py` passes on a broken call in pkg/b.py; file-scoped staged runs would miss cross-module breakage. --project attaches python/pyproject.toml's [tool.ty] config (probe-verified: without it, cwd=repo-root discovery found NO ty config at all).\""
native_alternative = "`Builtins.ty` exists in the pinned version — but it is file-scoped and config-discovery-dependent, which is exactly what the recorded probe rules out."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ty.pkl:9-14 — `ty = new Config.Step { glob = List(\"**/*.py\", \"**/*.pyi\"); check = ... argv = List(\"ty\", \"check\", \"{{files}}\") ... }` (tag v1.57.0). The builtin passes `{{files}}` (the staged set) and no `--project`, and does not run under `uv`, so adopting it would reintroduce both failures the comment documents. Adopting it as `(Builtins.ty) { check = ... }` would override the only thing it contributes."
rationale = "JUSTIFICATION (not a default): the builtin exists but is strictly weaker for this repo on two independently-probed axes — cross-module analysis (a file-scoped `{{files}}` run cannot see a caller in an unstaged sibling) and config attachment (`--project python` is what binds `[tool.ty]`). The one thing the builtin would add for free is `**/*.pyi` coverage, and that is not a live gap: `git ls-files '*.pyi'` returns nothing. Revisit if ty gains a project-scoped hk builtin or if `.pyi` stubs appear."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-2-10"
unit = "hk-steps-2"
kind = "hk"
disposition = "keep"
confidence = 0.85
custom_thing = "The remaining members are already the native mechanism or the sanctioned python-wrapper pattern: `taplo`, `yamllint`, `pkl`, `bash_logic_budget`, `no_platform_literals`, `classifier_axes`, `no_env_dump`."
evidence = "hk.pkl:167-169 `[\"taplo\"] = (Builtins.taplo) { batch = true }`; :174-176 `[\"yamllint\"] = (Builtins.yamllint) { batch = true }`; :186 `[\"pkl\"] = Builtins.pkl`; :247-250, :268-270, :289-298, :307-309 the four `uv run --project python dotfiles-setup ...` wrappers. `no_env_dump`'s justification is recorded at hk.pkl:300-306: \"Covers only what gitleaks and betterleaks cannot — measured 2026-07-27, both score 0 on a compressed dump they both flag in plaintext.\""
native_alternative = "None supersedes them at the pinned versions. hk 1.58.1 — the only release newer than the 1.57.0 pin — adds no capability at all."
alternative_evidence = ".agent/kb/raw/hk-releases-since-pin.digest.md — \"Releases newer than the pinned 1.57.0: only v1.58.1 ... 'A small documentation-only release. There are no changes to hk's runtime behavior.' ... No digest bullets to report; the pin (1.57.0) is current with respect to any new hk capability.\" Builtin definitions confirming the three overrides are still needed/minimal: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/taplo.pkl:9-18 (glob `**/*.toml`, no `batch`), yamllint.pkl:9-14 (glob `**/*.yml`/`**/*.yaml`, no `batch`), pkl.pkl:9-14 (`types = List(\"pkl\")`, `pkl eval {{files}} >/dev/null`). No builtin in the ~140-entry v1.57.0 set decompresses an encoded blob (the four secret-scanning entries are gitleaks.pkl, gitleaks_staged.pkl, betterleaks.pkl, kingfisher.pkl — all regex-over-plaintext scanners), so `no_env_dump` has no native peer."
rationale = "JUSTIFICATION per member: taplo/yamllint/pkl ARE the builtins, and their only local overrides (`batch = true`) add a property the builtin does not set — verified against each builtin's source at the pinned tag, and the earlier `types` overrides were already retired. bash_logic_budget / no_platform_literals / classifier_axes / no_env_dump are one-line wrappers over python modules, each with a suites.toml wiring contract (python/verification/suites.toml:1389, :1710, :2145) — i.e. they already are the target shape findings 3-6 recommend for the rest. Nothing in the hk gap digest changes any of this."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-1"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.95
custom_thing = "Unit-wide premise check: 'is any of these 17 steps now covered by an hk Builtin added since the 1.57.0 pin?'"
evidence = "hk.pkl:1,8,11 + hk-common.pkl:17-18 + hk-image.pkl:11,14 all pin `hk@1.57.0`; `.config/mise/conf.d/shared.toml` pins the binary at hk 1.57.0 (REPO PINS)."
native_alternative = "None — there is no hk release with new builtins after the pin. The offline hk source tree in the corpus IS 1.57.0, and the only newer release (v1.58.1) is documentation-only."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:3 (`## [1.57.0] ... - 2026-08-30`, newest entry) and .agent/kb/raw/hk-releases-since-pin.digest.md (v1.58.1 body: 'A small documentation-only release. There are no changes to hk's runtime behavior.'). Builtin inventory control-armed: `ls sources/hk/pkl/builtins | grep -ci ruff` = 2 and `grep -ci '^mise'` = 1 (probe works), while `uv` = 0, `chezmoi` = 0, `renovate` = 0, `agnix` = 0."
rationale = "The 'has hk shipped a builtin for this since we pinned' question is closed for the whole unit at once: the corpus hk tree is exactly the pinned version and the only newer release adds no runtime behaviour. So no member of this unit can be retired on grounds of a new hk builtin, and every remaining finding below is judged against non-hk alternatives."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-2"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.95
custom_thing = "Six of the 17 members are already bare/near-bare native builtin adoptions with no custom body: `betterleaks` (hk.pkl:320-322), `actionlint` (hk.pkl:373), `ghalint_workflow` (hk.pkl:374), `ghalint_action` (hk.pkl:375), `zizmor` (hk.pkl:376), `mise` (hk.pkl:429-431)."
evidence = "hk.pkl:373-376 are bare `= Builtins.X` assignments; hk.pkl:320-322 and hk.pkl:429-431 are `(Builtins.X) { … }` extensions carrying only `batch`/`glob`. All follow the #154 assignment form documented at hk.pkl:3-7."
native_alternative = "They ARE the native mechanism — hk builtins `betterleaks`, `actionlint`, `ghalint_workflow`, `ghalint_action`, `zizmor`, `mise`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/betterleaks.pkl:13, actionlint.pkl, ghalint_workflow.pkl, ghalint_action.pkl, zizmor.pkl, and mise.pkl:9-37 (the `mise fmt --check` / `mise fmt` CommandSpec pair)."
rationale = "Nothing to modernize: these already delegate 100% to upstream builtins, and the only local overrides are selectors (`glob`, `batch`), not reimplementations. The three GHA steps are complementary rather than redundant — actionlint is syntax/shell, ghalint_* is policy, zizmor is supply-chain security — so collapsing them would lose coverage."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-3"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "`betterleaks` step sets `batch = true` (hk.pkl:321) on a builtin whose argv interpolates `{{files}}`, in a repo whose own hk-common.pkl comment warns that the sibling secret scanner silently ignores a multi-file list and rescans the whole cwd."
evidence = "hk.pkl:320-322 (`[\"betterleaks\"] = (Builtins.betterleaks) { batch = true }`); the adjacent warning it could have inherited is hk-common.pkl:27-41 ('`gitleaks dir` takes only ONE positional path … TWO OR MORE files are silently discarded and it scans the whole cwd instead')."
native_alternative = "No change needed — `betterleaks dir` genuinely accepts N paths, unlike `gitleaks dir`, so the builtin's `{{files}}` + `batch = true` is the correct native shape."
alternative_evidence = "Builtin argv: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/betterleaks.pkl:16-20 (`betterleaks dir --redact --verbose --no-banner {{files}}`). Live probe (betterleaks 1.8.1 via `mise x`), fully control-armed on synthetic files in the scratchpad: 1 file -> 'scanned ~17 bytes', 2 files -> '~34 bytes', 3 files -> '~97 bytes' (linear, so each listed path is scanned and none is discarded); an unlisted planted `ghp_…` token in the SAME directory was NOT found (rc=0) while the same file listed explicitly WAS found (rc=1, 'leaks found: 2') — so the probe discriminates in both directions."
rationale = "Explicitly justified rather than defaulted: the one plausible defect in this step (inheriting gitleaks' one-positional-path trap) was measured and refuted with both arms. betterleaks honours the file list, which also means hk-common.pkl's `excludePaths` DOES constrain this step even though it does not constrain gitleaks — worth recording so the next reader does not generalise the gitleaks caveat across the group."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-4"
unit = "hk-steps-3"
kind = "hk"
disposition = "refactor"
confidence = 0.75
custom_thing = "hk-common.pkl's `security` group overrides the gitleaks builtin with `batch = true` (hk-common.pkl:134-136), and carries 15 lines of comment (hk-common.pkl:27-41) documenting that the builtin silently rescans the whole cwd when handed more than one file."
evidence = "hk-common.pkl:133-137 (`[\"gitleaks\"] = (Builtins.gitleaks) { batch = true }`) and hk-common.pkl:27-41 (the measured 1.85 KB vs 45.87 MB write-up)."
native_alternative = "hk 1.57.0's `gitleaks` builtin already fixed this upstream: its argv hard-codes `.` and no longer interpolates `{{files}}` at all, with the same one-path finding recorded as a source comment and pinned by a regression test."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/gitleaks.pkl:13-14 ('gitleaks dir accepts exactly one path. Passing multiple files makes it silently scan \".\".' / reference to gitleaks v8.30.1 cmd/directory.go#L22-L30), argv at :16-20 (`gitleaks dir --redact --verbose --no-banner .`), and the upstream test `[\"check whole tree beyond selected files\"]` at :26-36."
rationale = "With the pinned builtin's argv containing no `{{files}}`, the local `batch = true` is a dead override, and the long comment now describes a defect the pinned upstream already documents and pins itself. The refactor is to drop the override (leaving `= Builtins.gitleaks`) and shrink the comment to a pointer at gitleaks.pkl:13-14, keeping only the repo-specific half (that `.gitleaks.toml`, not `excludePaths`, is what scopes it). NOTE: `gitleaks` is not on this unit's member list — it lives in one of this unit's FILES, so flag for the synthesis stage to dedupe against whichever unit owns the security group."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && pkl eval -x 'security[\"gitleaks\"].check' hk-common.pkl   # confirm the resolved command still ends in `.` and never expands a file list, with and without the `batch = true` line"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-5"
unit = "hk-steps-3"
kind = "hk"
disposition = "refactor"
confidence = 0.80
custom_thing = "`hk_version_parity` implements cross-file pin comparison as a multi-command inline shell body (`set -- $(grep -ohE … | sort -u); pin=$(grep -E … | grep -oE …); [ $# -eq 1 ] && [ \"$1\" = \"hk@${pin}\" ] || { … exit 1; }`)."
evidence = "hk.pkl:523-527 (the step) — the `check =` string at hk.pkl:526 is a positional-parameter reset plus two greps, a sort, and a two-clause comparison."
native_alternative = "No native mechanism replaces the CHECK (hk only enforces a `min_hk_version` FLOOR, not equality, and 1.57.0 makes the pin load-bearing in a new way). The applicable in-repo standard is `.claude/rules/zero-bash-logic.md`: non-trivial logic lives in `python/` behind a thin `dotfiles-setup` subcommand, which is precisely what this file's own sibling step already argues for."
alternative_evidence = "hk floor-only semantics: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/config.rs:131-132 (`if let Some(min_hk_version) … version_cmp_or_bail`). New 1.57.0 consequence of a mismatched pin: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/config.rs:761-765 ('A config pinning this version then needs no network on a cold cache; any other version keys a different URL and is fetched as usual.', gating `preload_package(embedded_pkl_package_url(), …)`), documented at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/environment_variables.md:220 (`HK_PKL_EMBEDDED`). In-repo precedent for the refactor: hk.pkl:389-393 ('The check LOGIC lives in python … a big inline-bash grep here would itself violate zero-bash-logic')."
rationale = "The step must stay — hk's own guard is a floor, and since 1.57.0 a pin that disagrees with the running binary silently downgrades from the embedded pkl package to a network fetch, which is a NEW reason the equality check earns its keep and is not yet recorded in its comment. What should change is the body: it is exactly the inline-grep shape zero-bash-logic bans, and the ban is currently unenforced here because `bash_budget.py`'s allowlist only governs `scripts/*.sh` and `.devcontainer/scripts/*.sh` files, not hk `check` strings. Move it to `dotfiles-setup hk-pin-parity` alongside the existing `lock-check` / `token-audit` / `workflow-hooks` siblings, and add the embedded-package consequence to the rationale."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-6"
unit = "hk-steps-3"
kind = "hk"
disposition = "refactor"
confidence = 0.60
custom_thing = "`chezmoi_template_render` shells out to `scripts/check-chezmoi-templates.sh`, a 48-line bash script that builds a hermetic fake HOME (`mktemp -d`, `mkdir -p`, symlink of `command -v mise`), renders the config template in `--init` context, then loops every tracked template accumulating an rc."
evidence = "hk.pkl:452-455 (the step); scripts/check-chezmoi-templates.sh:21-48 (set -euo pipefail, tmpdir+trap, fake-HOME construction at :25-27, two-phase render at :33-34 and :42-47, rc accumulation at :36-46). Its own header at :6 claims 'Thin wrapper by design (zero-bash-logic rule): iterate + render, no logic.'"
native_alternative = "The chezmoi half is already native (`chezmoi execute-template --init`) and should stay. There is no hk builtin for chezmoi (control-armed: `ls sources/hk/pkl/builtins | grep -ci chezmoi` = 0 against a working control of `ruff` = 2). The applicable standard is `.claude/rules/zero-bash-logic.md` — move the orchestration into `python/src/dotfiles_setup/` and keep the CLI call."
alternative_evidence = ".claude/rules/zero-bash-logic.md ('Bash is restricted to thin check/smoke wrappers … A new recurring workflow ships its logic as a `python/` module + a mise task'); python/src/dotfiles_setup/bash_budget.py:79-80 (this file is allowlisted at `max_lines` 48, i.e. it is at its ceiling and any added hermeticity fix fails the budget)."
rationale = "Not a native-supersession finding — chezmoi genuinely has no batch render-check command, so the capability must stay custom. The modernization is location: the file's self-description ('no logic') is contradicted by lines 23-34, where the environment-construction that makes the check hermetic (temp HOME, mise symlink, two-phase config render) is real logic that PR #169 had to debug twice, and it sits exactly at its bash budget so the next fix cannot land without a budget bump. Confidence is mid because this is a policy/placement judgement, not a superseded mechanism, and the script does work today."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-7"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.80
custom_thing = "`doc_refs` — a custom `dotfiles-setup check-doc-refs` that resolves every backtick span looking like a file path in AGENTS.md, `**/AGENTS.md`, `.claude/rules/*.md` and `.claude/skills/**/*.md`, with exact/basename/suffix matching and an allowlist."
evidence = "hk.pkl:508-517 (step + glob + the rationale that 'agnix validates only @import stubs and rumdl MD057 only real links — backtick citations were parsed by NOTHING')."
native_alternative = "agnix rule CDX-AG-005 ('AGENTS.md references missing file `%{reference}`') does parse backtick file references and check existence — but only for AGENTS.md, only under the codex toolset, with exact relative resolution, no allowlist, and it stops after the first hit per file."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/codex.rs:840-874 (`find_backtick_file_refs(line)`, `if !fs.exists(&resolved)`, and `break; // One diagnostic per file is enough`), message at crates/agnix-core/locales/en.yml:1036-1037. Not active in this repo: `.agnix.toml:62` sets `tools = [\"claude-code\"]`, so the codex rule family does not run; the only near sibling that would, `kiro_009` (locales/en.yml:762), is scoped to Kiro steering files."
rationale = "Justified, not defaulted: the one native rule that does the same thing was found and read, and it covers a strict subset — one of four glob classes, one diagnostic per file, no basename/suffix resolution, no allowlist — and is disabled here by the `tools` setting. Enabling the codex toolset to reach it would import the whole CDX rule family plus its AGENTS.md size rule, so it is not a cheap swap. The step's own comment should be corrected though: 'backtick citations were parsed by NOTHING' is now false at agnix 0.52.1 for AGENTS.md specifically."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && printf '\\nSee `docs/definitely-missing-file.md`.\\n' >> /tmp/agents-probe.md && agnix --verbose . 2>&1 | grep -c CDX-AG-005   # expect 0 today (tools = [\"claude-code\"]); re-run with tools = [\"claude-code\",\"codex\"] to confirm the rule fires and to measure its overlap with check-doc-refs"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-8"
unit = "hk-steps-3"
kind = "hk"
disposition = "refactor"
confidence = 0.55
custom_thing = "Four markdown/agent-doc linters are pinned in mise.toml but wired into NO hk step — `npm:@contextlint/cli` 1.1.1, `npm:markdownlint-cli2` 0.23.2, `aqua:jackchuka/mdschema` 0.15.2, `rumdl` 0.2.63 — while hk.pkl only mentions them in comments."
evidence = "mise.toml:39-42 (the four pins); hk.pkl:358-364 (the only occurrences in hk.pkl are the `#154`/`#160 T12` comments deferring adoption: 'rumdl reports 591 findings across 56 files (a real burn-down project), and mdschema requires authoring a schema file that does not exist yet'); hk.pkl:512 (the doc_refs comment referencing rumdl MD057). Control arm: the same grep finds `agnix` wired as a real step at hk.pkl:491."
native_alternative = "hk 1.57.0 ships builtins for three of the four: `rumdl` / `rumdl_format`, `mdschema`, and `contextlint` — the last of which additionally carries reference rules (`ref001`, `ref005`) adjacent to what `doc_refs` does by hand."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/rumdl.pkl, rumdl_format.pkl, mdschema.pkl, and contextlint.pkl:12-18 (`contextlint {{files}}`, `workspace_indicator = \"contextlint.config.json\"`) with `ref001`/`ref005` in its test config at contextlint.pkl:77-81. Live probe of the pinned CLI: `mise x -- contextlint --help` (rc=0) lists `lint`, `impact`, `slice`, `graph`, `compile`, `init`."
rationale = "A ci-local-parity gap adjacent to this unit's files: four tools are installed on every `mise install` and gated by nothing, so their pins are maintenance cost with zero enforcement, and three of them now have first-class hk builtins the deferral comments predate. The refactor is either to wire the builtins (contextlint needs `contextlint.config.json`, which does not exist; mdschema needs a schema; rumdl needs the 591-finding burn-down) or to drop the pins. Confidence is mid because the deferrals were evidence-backed at the time and the burn-down cost is real — this is a decision to re-open, not a defect to fix blind."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && mise x -- rumdl check . 2>&1 | tail -3   # re-measure the 591-finding baseline at rumdl 0.2.63 before deciding whether the burn-down is still the blocker"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-9"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "`mise_lock_integrity` — `dotfiles-setup lock-check`, an artifact-vs-HEAD coverage comparison of `mise.lock`."
evidence = "hk.pkl:324-339 (step + the measured write evidence: bare lock took conda entries 962 -> 427 and linux-x64 628 -> 80)."
native_alternative = "None. mise's own `mise lock --dry-run` cannot expose the loss, and there is no newer mise release than the host's."
alternative_evidence = "hk.pkl:331-333 ('`mise lock --dry-run` reports \"would update ... for linux-x64\" for all 11 platforms and changes nothing, so it cannot expose this — the write had to be measured'); .agent/kb/raw/mise-releases-since-pin.digest.md ('the newest release in the fetched list is `v2026.9.3` … identical to the host's installed version … ZERO releases strictly newer'). No hk builtin: `ls sources/hk/pkl/builtins | grep -ci 'mise.*lock'` = 0 against control `^mise` = 1."
rationale = "Required justification: the native candidate was tried and is provably blind (dry-run reports a change it never makes), the upstream bug is open as jdx/mise#7700, and the mise gap digest confirms nothing shipped since. The check also deliberately binds the ARTIFACT rather than a command, which no tool feature can do for us because `auto_install = true` lets any task reach the destructive path."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-10"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.85
custom_thing = "`hk_audit` — `dotfiles-setup hk-builtins-audit --check`, a staleness gate on the generated `docs/hk-builtins-audit.md`."
evidence = "hk.pkl:341-347 (step + the drift history: a hand-written snapshot claimed 15 builtins were 'used' that were wired nowhere, betterleaks among them)."
native_alternative = "hk's `hk builtins` CLI supplies the authoritative list and the generator already consumes it; hk has no doc-staleness gate of its own."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/cli/builtins.rs (the `hk builtins` subcommand) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/builtins.md (the rendered upstream catalogue). Wiring already contracted: python/verification/suites.toml:1736-1768 (`workflow.hk-builtins-audit`)."
rationale = "The native half — 'what builtins exist' — is already delegated to `hk builtins`; only the repo-specific half (which of them THIS repo wires, across three .pkl files, and whether the doc still matches) is custom, and nothing upstream can answer that. Not redundant with the suites.toml contract either: the contract asserts the chain EXISTS, the step asserts the generated content is CURRENT."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-11"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "`contract_token_uniqueness` — `dotfiles-setup token-audit`, asserting each `per_path_tokens` entry in `python/verification/suites.toml` matches its target file exactly once."
evidence = "hk.pkl:349-356 (step + the #394 rationale: 'the three holes that were live in #394 all survived their anchor because the token matched somewhere OTHER than the site it meant')."
native_alternative = "None — `suites.toml` is a repo-local verification format with no upstream schema, linter, or builtin. The nearest generic tools (ast-grep, ruff) operate on source ASTs, not on this contract file's token semantics."
alternative_evidence = "No hk builtin exists for it (the 1.57.0 builtin inventory at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ contains no contract/token/verification entry; control: `ruff` = 2 hits). The predicate and its allowlist live only here: python/verification/suites.toml:2272-2284 (`workflow.token-preflight`) and :1658-1690 (`workflow.token-audit-wiring`)."
rationale = "Justified: the artifact it validates is invented by this repo, so no external tool can know what a 'token that binds one call site' means here. Nor is it redundant with its own suites.toml contracts — those bind the wiring, this one runs the predicate."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-12"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "`workflow_hk_skip_hooks` — `dotfiles-setup workflow-hooks`, deriving which GHA jobs write to git and requiring them to skip hk's hooks (ADR-0001)."
evidence = "hk.pkl:377-414 (step, glob, and the note that the check is whole-tree so the glob decides only WHEN it runs)."
native_alternative = "None. The three workflow linters already wired in this same unit — actionlint, ghalint_workflow/ghalint_action, zizmor — model GHA syntax, org policy and supply-chain security respectively; none can know about hk's own git hooks or `HK_SKIP_HOOKS`."
alternative_evidence = "Those three builtins' definitions carry no hk-hook awareness: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/actionlint.pkl, ghalint_workflow.pkl, zizmor.pkl. The rule being enforced is repo-local: docs/adr/0001-hk-hooks-do-not-run-in-ci.md, contracted at python/verification/suites.toml:1801-1813."
rationale = "Required justification: the invariant is 'a CI job that commits or pushes must set HK_SKIP_HOOKS, because mise postinstall installed hk's hooks on the runner' — a fact about THIS repo's own tooling that no upstream GHA linter can see. It is also already the zero-bash-logic-correct shape (python module behind a one-line hk step, hk.pkl:389-393), so there is nothing to move."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-13"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "`uv_lock_check` — a hk step running `uv lock --check --project python`, globbed to `python/pyproject.toml` and `python/uv.lock`."
evidence = "hk.pkl:433-439."
native_alternative = "It IS the native command; uv ships `uv lock --check` and there is no hk builtin wrapping it, so a bare-string step is the only way to run it under hk."
alternative_evidence = "No `uv*` builtin in hk 1.57.0 — control-armed: `ls /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins | grep -ci '^uv'` = 0 while the same command shape returns 2 for `ruff` and 1 for `^mise`. Nothing in the uv gap digest changes lock-check behaviour: .agent/kb/raw/uv-releases-since-pin.digest.md lists only publish-token revocation, `missing-exclude-newer-package-lock`, `uv tree --invert` cycles, and two `pylock.toml` items across 0.12.10-0.12.11."
rationale = "Zero custom logic — the step body is the vendor command verbatim, and the two releases past the pin add nothing that touches it. The only conceivable modernization (an `uv` hk builtin) does not exist upstream."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-14"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.80
custom_thing = "`dependency_ownership` — `dotfiles-setup dependency-ownership`, rejecting a second pipx/uvx declaration of a package that `python/pyproject.toml` already owns."
evidence = "hk.pkl:441-446 ('Python packages have one owner: pyproject.toml. mise [deps.uv] may materialize that lock, but a second pipx/uvx declaration is drift.')."
native_alternative = "None. mise does not cross-validate its `pipx:`/`uvx:` backend entries against a uv project's `pyproject.toml`, and uv has no view of mise's tool table."
alternative_evidence = "No mise release since the pin could have added it: .agent/kb/raw/mise-releases-since-pin.digest.md ('ZERO releases strictly newer than the baseline'). No hk builtin (builtin inventory has `mise` for `mise fmt` only — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/mise.pkl:30-37)."
rationale = "Justified: this is a cross-tool invariant spanning two package managers, and neither manager can see the other's manifest. It is also the repo-local expression of `.claude/rules/use-tool-builtins.md`'s ownership principle, which no vendor ships."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-15"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "`renovate_config_validate` — `dotfiles-setup renovate-validate`, which asserts the RE2 engine with a lookahead canary BEFORE delegating to the native `renovate-config-validator`."
evidence = "hk.pkl:457-488 (the full #644 rationale: 're2 is an OPTIONAL npm dep … the validator warns \"RE2 not usable, falling back to RegExp\" and **still exits 0**')."
native_alternative = "The validation itself is already 100% native (`renovate-config-validator --strict --no-global`); only the engine assertion is custom, and Renovate ships no flag that makes the degraded path fail."
alternative_evidence = "Nothing in the Renovate gap digest addresses the silent-degradation shape: .agent/kb/raw/renovate-config-options.digest.md lists only PEP 735 `dependency-groups`, `pixi.lock` support, `allowedUnsafeExecutions` gating, and array-form `tool.uv.sources` (the last one, Renovate 44.62.2, is NEWER than this repo's 44.59.2 pin and is a manager parser change, not a validator behaviour change). Contract: python/verification/suites.toml:1535-1546."
rationale = "Required justification: the wrapper exists solely because the native tool reports success while degraded, and `.claude/rules/probes-need-a-control-arm.md` rule 9 is explicit that a gate must assert the capability rather than sniff for a symptom. The custom code parses no regex — it feeds the vendor tool an input it must reject — so there is nothing here that a Renovate feature could take over."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-3-16"
unit = "hk-steps-3"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "`agnix` — a hk step running `agnix . --strict` over CLAUDE.md/AGENTS.md/`.claude/**/*.md`/`.claude/settings.json`/`.agnix.toml`."
evidence = "hk.pkl:490-506 (step, `--strict` rationale at :492-497, glob at :499-505)."
native_alternative = "It IS the native tool; hk has no `agnix` builtin, so a bare-string step is the only way to run it, and `--strict` is agnix's own documented warnings-as-errors flag."
alternative_evidence = "No agnix builtin in hk 1.57.0 — control-armed: `grep -ci agnix` over /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ = 0 against `ruff` = 2. Rule suppressions are expressed in agnix's own config format rather than in the step: .agnix.toml:102 (`disabled_rules = [\"XP-003\", \"AGM-004\", \"AGM-006\", \"PE-001\"]`), :117 (CC-HK-025), :151 (CC-SK-017)."
rationale = "Zero custom logic: the step is the vendor command, and every local carve-out lives in the vendor's own config file rather than being re-implemented. One adjacency worth carrying to synthesis: `.agnix.toml:62` narrows `tools = [\"claude-code\"]`, which is what makes agnix's codex rule family (including CDX-AG-005, see finding hk-steps-3-7) inert here."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-4-1"
unit = "hk-steps-4"
kind = "hk"
disposition = "unknown"
confidence = 0.85
custom_thing = "hk step `no_hk_depends` — a hard ban on the `depends` key across all three pkl files, implemented as an inline `grep -nE '^[[:space:]]*depends[[:space:]]*='` over hk.pkl/hk-common.pkl/hk-image.pkl"
evidence = "hk.pkl:552-556 (the step); hk.pkl:529-551 (its rationale — 'hk never releases a dependent whose dependency FAILED … reproduced on BOTH 1.50.0 and 1.51.0 (a version bump does NOT fix it)'); hk.pkl:546-547 states the retirement condition verbatim: 'Retire this step ONLY when upstream releases a dependent on a failed dependency and a control-armed probe confirms it.'"
native_alternative = "hk itself: jdx/hk#1099 `fix(step): unblock dependents after failed dependency`, released in hk 1.53.0 (2026-07-23) — i.e. AFTER the 1.50.0/1.51.0 reproductions the ban rests on, and BEFORE this repo's pinned hk 1.57.0. The scheduler now calls `depends.mark_done()` on the failed step whenever `fail_fast` is off, which is exactly this repo's configuration (hk.pkl fail_fast = false, hk-image.pkl:21)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:250 (entry sits under the `## [1.57.0]`-preceded `## [1.53.0]` header at CHANGELOG.md:241); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/step_group.rs:215-216 `if (!fail_fast || failure_allowed) && result.is_err() { step_ctx.depends.mark_done(&step.name)?; }`; same release path for the errored branch at src/step_group.rs:198-199; snapshot version confirmed 1.57.0 at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/Cargo.toml:23."
rationale = "The ban's entire premise — 'a failed dependency wedges its dependents forever under fail_fast = false' — is fixed in the source of the version this repo actually pins. The step is therefore a candidate for retirement (or narrowing to a doc note), which would also un-ban the only ordering primitive hk offers besides the coarse `exclusive = true` the repo now uses. I do NOT mark this 'retire' because the step's own text, and .claude/rules/probes-need-a-control-arm.md, require a control-armed probe on the pinned binary before believing an upstream fix — the same discipline that was applied to `no_commit_to_branch` when hk 1.52.0 fixed its detached-HEAD bug (hk.pkl:723-729). Confidence 0.85 is confidence that the probe will confirm; the source reading itself is unambiguous."
probe = "cd \"$(mktemp -d)\" && git init -q . && cat > hk.pkl <<'EOF'\namends \"package://github.com/jdx/hk/releases/download/v1.57.0/hk@1.57.0#/Config.pkl\"\nfail_fast = false\nhooks { [\"check\"] { steps {\n  [\"a\"] { check = \"exit 1\" }\n  [\"b\"] { depends = List(\"a\"); check = \"echo B-RAN\" }\n} } }\nEOF\ntouch f.txt && git add -A && timeout 90 hk run check --all > /tmp/hkdep.log 2>&1; echo \"rc=$?\" >> /tmp/hkdep.log; cat /tmp/hkdep.log\n# PASS arm (fix present): log contains B-RAN and rc=1 (a's failure), NOT rc=124/timeout.\n# CONTROL arm (probe can see the bug): rerun the identical config with the OLD binary — `mise x hk@1.51.0 -- hk run check --all` — and require the timeout/`waiting for a` wedge. If the old binary also prints B-RAN, the probe is not discriminating and its answer is void."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-4-2"
unit = "hk-steps-4"
kind = "hk"
disposition = "refactor"
confidence = 0.90
custom_thing = "hk step `graphify_skill_surface` — an inline three-clause shell chain whose last clause is `! grep -q 'use the installed graphify skill' AGENTS.md`"
evidence = "hk.pkl:669-673: `check = \"test -f .claude/skills/graphify/SKILL.md && grep -q 'DELIBERATE STUB' .agents/skills/graphify/SKILL.md && ! grep -q 'use the installed graphify skill' AGENTS.md\"`"
native_alternative = "None native — but the repo's own #299 fix pattern already exists two steps away and is not applied here: `no_hk_depends` opens with a `test -f` preamble precisely because `! grep …` on a MISSING file exits 2, which `!` flips to 0, so the assertion silently passes. The zero-bash-logic seam (a `dotfiles-setup <subcommand>` python gate, as `codex_agent_parity` and `doc_refs` already use) is the established mechanism."
alternative_evidence = "hk.pkl:548-551 documents the exact class in this same file: \"`test -f` first: bare `! grep …` would PASS on a deleted file (grep exits 2 on error and `!` flips that to 0) — the silent-false-negative class #299 closed.\" Repo policy: .claude/rules/zero-bash-logic.md (non-trivial logic lives in python; bash is thin wrappers only). Existing python seams to copy: hk.pkl:638-640 (`dotfiles-setup codex-agent-parity`), hk.pkl:515-516 (`dotfiles-setup check-doc-refs`)."
rationale = "A negative-grep assertion with no existence precondition is a check that can only pass once its target file moves or is renamed — the vendor-installer append this clause exists to catch (do-not.md #8) would go unreported if AGENTS.md were ever relocated or the step run from a different cwd. The sibling step in the same file already carries the fix and names the issue number. Refactor the three clauses into the python gate (one `dotfiles-setup graphify-skill-surface`, or fold into the consolidated parity gate of hk-steps-4-3) so the existence preconditions and the failure messages are testable. This is not a superseded-by-native finding; it is the repo's own documented defect class recurring in a step that predates the fix."
probe = "cd \"$(git rev-parse --show-toplevel)\" && cp AGENTS.md /tmp/AGENTS.md.bak && mv AGENTS.md /tmp/gone.md && sh -c \"test -f .claude/skills/graphify/SKILL.md && grep -q 'DELIBERATE STUB' .agents/skills/graphify/SKILL.md && ! grep -q 'use the installed graphify skill' AGENTS.md\"; echo \"missing-AGENTS.md rc=$?\"; mv /tmp/gone.md AGENTS.md; # expect rc=0 (the false negative). CONTROL: with AGENTS.md restored, append the banned line and re-run — expect rc=1."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-4-3"
unit = "hk-steps-4"
kind = "hk"
disposition = "refactor"
confidence = 0.70
custom_thing = "Three separate inline-shell surface-parity/presence steps that are one concern: `no_global_skill_leakage` (two `test -f`), `session_review_skill_parity` (`test -f` + `cmp -s`), `graphify_skill_surface` (`test -f` + two `grep -q`)"
evidence = "hk.pkl:626-629 (`no_global_skill_leakage`); hk.pkl:644-647 (`session_review_skill_parity`); hk.pkl:669-673 (`graphify_skill_surface`). All three assert the same invariant class — 'the multi-harness skill/agent surface is present and in its reviewed shape' — in three different ad-hoc shell dialects."
native_alternative = "The repo's own fourth member of that class, `codex_agent_parity`, which already lives in python behind a single subcommand and carries a suites.toml contract, tests, a sentinel design and mutation-proved failing arms. Consolidating the other three behind the same seam is adopting an existing in-repo mechanism rather than inventing one. No hk builtin covers cross-file presence/parity: the 1.57.0 builtin catalogue has no `sibling`/`pair`/`stub`/`parity` linter (control arm: the same `ls | grep -iE` over that directory returns `no_commit_to_branch.pkl` and `check_conventional_commit.pkl`, the two builtins this repo does use)."
alternative_evidence = "hk.pkl:638-640 (`codex_agent_parity` -> `uv run --project python dotfiles-setup codex-agent-parity`); python/verification/suites.toml:1514-1530 (the contract binding that gate's hk step, CLI registration, module, task and failing-arm tests); builtin catalogue listing at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ (140 files; no presence/parity builtin)."
rationale = "Three ad-hoc shell assertions of one invariant means three places for the #299 class (hk-steps-4-2 is one live instance), three shapes of failure message, and no test coverage for any of them — while the fourth member of the same class is fully gated in python. Consolidation buys testability and one place to add a new surface. Marked 0.7 rather than higher because `no_global_skill_leakage` and `session_review_skill_parity` are genuinely two-token `test -f`/`cmp` calls that zero-bash-logic tolerates as thin wrappers; the win is coherence and coverage, not a defect in those two."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "hk-steps-4-4"
unit = "hk-steps-4"
kind = "hk"
disposition = "refactor"
confidence = 0.75
custom_thing = "hk step `claude_md_import_stub` -> `scripts/check-claude-md-stub.sh`, whose body is a 33-line embedded awk state machine that strips single- and multi-line HTML comments and then string-compares the residue to `@AGENTS.md\\n`"
evidence = "hk.pkl:683-685 (the step, `check = \"scripts/check-claude-md-stub.sh\"`); scripts/check-claude-md-stub.sh:21-54 (the awk comment-stripping state machine, `in_comment` flag, `index(line, \"<!--\")` / `index(line, \"-->\")` scanning); allowlisted at python/src/dotfiles_setup/bash_budget.py:85-87 with the justification 'hk check wrapper — CLAUDE.md thin-import-stub assertion'"
native_alternative = "No linter in the pinned set covers it — agnix 0.52.1's rule catalogue has no CLAUDE.md-must-be-an-import-stub rule (control arm: the same corpus DOES carry AGM-003, the 12,000-char cap this repo relies on elsewhere). So the alternative is not a tool but the repo's own zero-bash-logic seam: a python gate behind `dotfiles-setup`, matching `doc_refs` and `codex_agent_parity`, where a comment-stripping parser can be unit-tested (nested/unterminated comments, CRLF, a comment containing the literal `@AGENTS.md`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/SPEC.md:165-186 (full AGM/CC rule table — AGM-003 at SPEC.md:174, AGM-005 at :175; no stub/pairing rule); repo seams to copy at hk.pkl:515-516 and hk.pkl:638-640; policy at .claude/rules/zero-bash-logic.md ('Non-trivial logic … lives in python/ … Bash is restricted to thin check/smoke wrappers')."
rationale = "A hand-rolled HTML-comment tokenizer is not a thin wrapper by any reading of zero-bash-logic — it is the kind of parser the rule names, and it is the gate that keeps the root CLAUDE.md byte-exactly `@AGENTS.md` against repeated re-appends by graphify and the fable-orchestrator wizard (.claude/CLAUDE.md documents both). It currently has no tests: a silent parser bug (e.g. a nested `<!--` inside a comment, which the state machine mishandles by design) would make the gate pass on a drifted file. The invariant itself is right and no native tool provides it, so this is refactor, not retire."
probe = "printf '<!-- a <!-- b -->\\n@AGENTS.md\\nSTRAY LINE\\n' > /tmp/CLAUDE.md && awk -f /dev/stdin /tmp/CLAUDE.md <<'AWK'\n# paste the awk body from scripts/check-claude-md-stub.sh:21-53\nAWK\necho \"rc=$?\"  # PASS arm: must exit 1 (STRAY LINE present). CONTROL arm: same file without STRAY LINE must exit 0."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-4-5"
unit = "hk-steps-4"
kind = "hk"
disposition = "keep"
confidence = 0.80
custom_thing = "hk step `claude_agents_md_pairs` -> `scripts/check-claude-agents-md-pairs.sh` (git ls-files sweep + `git ls-files --error-unmatch` sibling existence check, both directions, `.claude/**` exempt)"
evidence = "hk.pkl:693-695 (the step); scripts/check-claude-agents-md-pairs.sh:20-45 (the whole body — one helper, two `while read` loops over `git ls-files`); allowlisted at python/src/dotfiles_setup/bash_budget.py:82-84 (45-line budget, 'hk check wrapper')"
native_alternative = "None. Searched the hk 1.57.0 builtin catalogue (no `sibling`/`pair`/existence-relation linter; the nearest, `ls_lint`, validates file/dir NAMING conventions, not cross-file existence relations) and the agnix rule table (no pairing rule). The check is a 3-command git wrapper already, so zero-bash-logic's 'thin wrapper' carve-out genuinely applies."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ (140-entry catalogue; `ls_lint.pkl` present, no pair/sibling builtin — control arm: the two builtins this repo does consume, `no_commit_to_branch.pkl` and `check_conventional_commit.pkl`, are both in that listing, so the enumeration is not blind); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/SPEC.md:165-186 (no pairing rule)."
rationale = "Required justification: the invariant is repo-specific (CLAUDE.md imports AGENTS.md, so an orphan on either side is either a dangling @import or a scope Claude never loads) and no shipped linter models cross-file existence relations at all — ls-lint is the closest and only constrains naming. The implementation is genuinely thin: `git ls-files` plus `git ls-files --error-unmatch`, no parsing, and it stays inside its allowlisted budget. Moving it to python would buy tests but no correctness — unlike hk-steps-4-4, there is no parser to get wrong. Keep as-is; it is the correct shape for this class."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-4-6"
unit = "hk-steps-4"
kind = "hk"
disposition = "keep"
confidence = 0.85
custom_thing = "hk step `md_size_budget` -> `uv run --project python kb-setup md-budget` (per-load-class line/byte budgets for eager/lazy/cond/skill markdown)"
evidence = "hk.pkl:616-618 (the step, now a one-line seam); hk.pkl:568-615 (the load-class model and the misattribution archaeology); python/verification/suites.toml:1317-1327 (the contract binding the seam, the SHA-pinned kb-setup dep and the rule doc)"
native_alternative = "Partially covered and DELIBERATELY not duplicated: agnix AGM-003 enforces the 12,000-char cap on AGENTS.md via the already-wired `agnix . --strict` step. hk's own `check_added_large_files` builtin is a flat 500KB byte cap over `**/*` and is neither line-based nor load-class aware, so it cannot express the 200/400/500-line gradient or the 1,536-char SKILL description cap."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/SPEC.md:174 `| AGM-003 | WARN | Character limit exceeded (12000 chars) |`; already consumed at hk.pkl:491-498 (`check = \"agnix . --strict\"`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/check_added_large_files.pkl:7-15 (`description = \"Prevent committing large files\"`, `argv = List(\"hk\",\"util\",\"check-added-large-files\",\"{{files}}\")`, default 500KB per its own test case at :18-23)."
rationale = "Required justification: the load-class gradient (eager 200 / lazy 400 / cond 400 / skill 500 lines, plus the 1,536-char description truncation that silently strips matcher keywords) has no expression in any shipped linter — agnix models one flat character cap for one vendor's engine, hk's builtin models one flat byte cap. The step is also already at its modernized end state: the enforcer MOVED to the shared `kb_setup.md_budget` engine in 2026-07-25 and the dotfiles copy plus its tests were deleted, so there is no second implementation left to retire. Nothing to do."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-4-7"
unit = "hk-steps-4"
kind = "hk"
disposition = "unknown"
confidence = 0.75
custom_thing = "The comment at hk.pkl:697-703 deferring hk's native `tests {}` block + the `hk_test` builtin + `hk test` in CI, on the stated grounds that '`hk test` executes test cases IN THE REAL PROJECT DIRECTORY (no sandbox — the probe left written test files behind and tests observed each other's files)'"
evidence = "hk.pkl:697-703 (the deferral and its probe rationale, tagged #160 T12, 'Needs upstream sandboxing or a {{files}}-scoped step redesign first. Tracked as a follow-up issue.')"
native_alternative = "hk's step-test sandbox already exists and is OPT-IN via the `{{tmp}}` placeholder: a test whose `write`/`before`/`files` entries are `{{tmp}}`-prefixed runs against a temporary directory, not the project tree. Landed as jdx/hk#182 'make sandbox opt-in via {{tmp}}; clean fixtures on success; fix builtins to use {{tmp}}' (hk 1.9.1, 2025-08-19) and extended by #663 'add tmpdir step test option' (hk 1.36.0, 2026-02-09) — both long before the pinned 1.57.0. Every shipped builtin now uses it."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:1919 (under the `## [1.9.1]` header at CHANGELOG.md:1915) and CHANGELOG.md:916 (under `## [1.36.0]` at CHANGELOG.md:903); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/reference/examples/custom-linters.md:66-79 (documented `{{tmp}}`-scoped `write`/`files` in a step `tests` block); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/check_added_large_files.pkl:16-27 (a shipped builtin's tests writing only to `{{tmp}}`)."
rationale = "The deferral's premise — 'no sandbox' — is refuted by the source: the sandbox is opt-in and the probe that produced the deferral almost certainly wrote unprefixed paths. That matters directly to this unit, because it is why steps like `no_hk_depends`, `graphify_skill_surface` and `no_global_skill_leakage` have no FAIL-direction coverage at all today (probes-need-a-control-arm rule 2: a gate verified only on clean code is decoration). Adopting `tests {}` with `{{tmp}}` fixtures would give every inline-shell step in these files a native, in-config failing arm. 'unknown' because the deferral was recorded WITH probe evidence and must be re-probed on the pinned binary, not overturned on a changelog reading."
probe = "cd \"$(mktemp -d)\" && git init -q . && cat > hk.pkl <<'EOF'\namends \"package://github.com/jdx/hk/releases/download/v1.57.0/hk@1.57.0#/Config.pkl\"\nhooks { [\"check\"] { steps { [\"canary\"] {\n  glob = \"**/*.canary\"\n  check = \"! grep -l FORBIDDEN {{files}}\"\n  tests {\n    [\"clean passes\"] { run = \"check\"; write { [\"{{tmp}}/ok.canary\"] = \"fine\" }; files = List(\"{{tmp}}/ok.canary\"); expect { code = 0 } }\n    [\"dirty fails\"]  { run = \"check\"; write { [\"{{tmp}}/bad.canary\"] = \"FORBIDDEN\" }; files = List(\"{{tmp}}/bad.canary\"); expect { code = 1 } }\n  }\n} } } }\nEOF\nhk test > /tmp/hktest.log 2>&1; echo \"rc=$?\" >> /tmp/hktest.log; cat /tmp/hktest.log; echo '--- leaked into project dir? ---'; ls -a\n# PASS arm: both cases pass AND `ls -a` shows no *.canary in the project dir.\n# CONTROL arm: repeat with the write keys UNPREFIXED (\"ok.canary\") and require the files to appear in the project dir — proving the probe can still see the 2026 failure mode and that {{tmp}} is what changes it."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "hk-steps-4-8"
unit = "hk-steps-4"
kind = "hk"
disposition = "keep"
confidence = 0.90
custom_thing = "`codex_agent_parity` (hk.pkl:638-640), `no_commit_to_branch` (hk.pkl:737), `check_conventional_commit` (hk.pkl:765), the `check`/`fix` hooks spreading `allSteps` (hk.pkl:748-758), `ghcr_publish_prereqs` (hk.pkl:774-777) and `test` (hk.pkl:778-781) in pre-push, and hk-image.pkl's shared-group spread + `Builtins.shellcheck` overlay (hk-image.pkl:28-47)"
evidence = "hk.pkl:737 `[\"no_commit_to_branch\"] = Builtins.no_commit_to_branch` (with the 1.52.0 probe record at hk.pkl:722-736 and the deliberate pre-commit-only placement at :732-736); hk.pkl:765 `[\"check_conventional_commit\"] = Builtins.check_conventional_commit`; hk.pkl:748-758 the canonical jdx `check`/`fix` pair; hk.pkl:776 `uv run --project python dotfiles-setup ghcr-check`; hk.pkl:780 `env -u MISE_IGNORED_CONFIG_PATHS mise --cd \"$MISE_PROJECT_ROOT\" run test-hook-isolated`; hk-image.pkl:33-45 (`...common.hygiene/safety/security/typos` + `Builtins.shellcheck` with `batch = true`)"
native_alternative = "Already native, at the pinned version. Both `no_commit_to_branch` and `check_conventional_commit` exist as builtins in hk 1.57.0's catalogue and are consumed as `Builtins.*` assignments rather than re-implemented; `check`/`fix` is hk's own documented hook pair; `shellcheck` is the builtin with only the orthogonal `batch` overlay (the `match_any`/`types` conflict from hk 1.51 #1055 is already noted and worked around at hk-image.pkl:40-42). `ghcr_publish_prereqs` and `test` are one-line seams into python/mise per zero-bash-logic. Nothing here is a reimplementation."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/no_commit_to_branch.pkl, .../check_conventional_commit.pkl, .../shellcheck.pkl (all present in the 1.57.0 snapshot, version confirmed at .../hk/Cargo.toml:23); python/verification/suites.toml:1773-1796 records the 1.52.0 probe (feature branch rc=0, main rc=1, detached HEAD rc=0, master rc=1) that justified adopting the builtin."
rationale = "Required justification: these six members are the unit's already-modernized half — each is either an hk builtin consumed as a builtin, hk's own canonical hook shape, or a single-command seam into the python/mise layer that zero-bash-logic prescribes. The one subtlety worth preserving rather than 'fixing': `no_commit_to_branch` is deliberately NOT in `allSteps` (hk.pkl:732-736) because that mapping feeds `check`/`fix`, so `mise run lint` and CI's lint job — which check out a real `main` — would fail on every push to main. A future tidy-up that folds it into `allSteps` for symmetry would break CI; the comment and the suites.toml contract both pin it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-1"
unit = "mise-tasks-1"
kind = "mise"
disposition = "retire"
confidence = 0.85
custom_thing = "[tasks.dev] hand-rolls sequential task ordering in a shell body (`set -euo pipefail; mise run up; mise run smoke`) with a comment explaining that `depends` runs in parallel so a shell body was used instead"
evidence = "mise.toml:441-456 (comment at 442-445: \"NOTE: `depends = [...]` in mise runs tasks in parallel when independent — not what we want here. A sequential shell body forces ordering\"; body at 452-456)"
native_alternative = "mise task `run` as an array of task references — `run = [{ task = \"up\" }, { task = \"smoke\" }]` — which runs steps strictly in series (a `{ tasks = [...] }` entry is the opt-in parallel form)"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/running-tasks.md:188-213 (\"You can also define a mise task to run other tasks in parallel or in series\"; :206 `{ task = \"example1\" }, # will wait for example1 to finish before running the next step`; :211-213 explains `depends` \"would drop the sequential/parallel ordering\") and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:12-30 (the `run` type signature `string | (string | { task: string, ... } | { tasks: string[] })[]`)"
rationale = "The exact constraint the comment names — depends is unordered, we need up-then-smoke — is what `run = [{task},{task}]` was added for. Replacing the body removes a shell wrapper, keeps both sub-tasks independently runnable (the other stated requirement), and keeps the `[shell_alias] dcdev` entry (mise.toml:188) working. Liveness: `mise run dev` has 0 textual callers outside mise.toml (control arm: the same grep shape returned 282 for `mise run lint` and 0 for a bogus `mise run zzqfrp9`), but it is NOT dead — `dcdev` is its interactive entrypoint. One caveat to carry into the change: the array form's steps are not graph edges, so `mise tasks deps dev` will show a leaf (running-tasks.md:210-211)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-2"
unit = "mise-tasks-1"
kind = "mise"
disposition = "retire"
confidence = 0.70
custom_thing = "[tasks.check] = `depends = [\"pre-commit\", \"test\"]`, described as \"Run full quality check (pre-commit + test)\""
evidence = "mise.toml:222-224. Liveness grep for callers outside mise.toml found exactly one hit, in an archived agent report (docs/research/kb/reports/agents/kb-standalone-readiness.md:110) — no skill, rule, hk step, hook, workflow or python caller. Control arm: the same grep shape returned 282 hits for `mise run lint` and 0 for a bogus task name, so the near-zero result is a real absence."
native_alternative = "mise `run` step arrays for ordered composition (`run = [{ task = \"lint\" }, { task = \"test\" }, { task = \"verify\" }]`), since `depends` deliberately imposes no order and mise \"will run whatever it can in parallel\""
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:105 (\"mise will run whatever it can in parallel (up to `--jobs`)\") and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/running-tasks.md:206-213"
rationale = "Three problems in three lines. (a) It is orphaned — nothing in the enforced surfaces invokes it. (b) Its gate set contradicts the project's own documented three gates: AGENTS.md \"Validate before committing\" is `mise run lint` + pytest + `mise run verify`, and this task substitutes the mutating `hk run pre-commit --all` for the read-only lint gate and omits `verify` entirely, so a green `check` is not the green the rules mean. (c) `depends` is unordered, so a fix-applying pre-commit run and pytest can execute concurrently over the same tree. Retire it; if a composite verb is still wanted, the native ordered form above expresses it correctly against the real gates."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-3"
unit = "mise-tasks-1"
kind = "mise"
disposition = "refactor"
confidence = 0.75
custom_thing = "[tasks.pre-commit] = `hk run pre-commit --all`, with no `timeout` and no log-tail diagnostics"
evidence = "mise.toml:203-205; documented as a user entrypoint at .claude/skills/devcontainer-workflow/SKILL.md:30 (`mise run pre-commit    # hk run pre-commit --all`)"
native_alternative = "mise's per-task `timeout` field (accepts `30s`/`5m`/`1h`, fails the task loud on expiry) — the same native mechanism already adopted one task above, at `[tasks.lint] timeout = \"700s\"`"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060-1077 (`### timeout` … \"The task fails if it does not complete within the configured duration\"); in-repo adoption at mise.toml:213"
rationale = "The PreToolUse guard denies a raw `hk run pre-commit` precisely because \"hk has none [no timeout] of its own\" and a wedged run once sat at 0% CPU for ~7 hours (python/src/dotfiles_setup/hook_guard.py:266-273; .claude/rules/long-running-command-hangs.md). `mise run pre-commit` executes that exact command while matching none of the guard's patterns, so this task is a standing bypass of the rule the guard enforces. Minimum fix: give it a native `timeout` like `lint` has. Better: retire it in favour of `mise run lint` (read-only, ≡ CI) and `mise run fmt` (the fix path), and drop the SKILL.md line — `mise-tasks-only.md`'s canonical map already routes `hk run pre-commit --all` to `mise run lint`, so this task contradicts the map it is supposed to obey."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-4"
unit = "mise-tasks-1"
kind = "mise"
disposition = "refactor"
confidence = 0.55
custom_thing = "The long-running Mac-side container tasks carry no execution bound: `up`, `dev-rebuild`, `smoke`, `verify-image`, `persistence` (and `dev`) all run `devcontainer up` / `docker run` / a full stop-up cycle with no `timeout`; only `[tasks.lint]` declares one"
evidence = "mise.toml:240-321 (up), 344-363 (dev-rebuild), 375-397 (verify-image), 399-413 (smoke), 457-571 (persistence) — none declares `timeout`; the sole declaration in the whole member set is mise.toml:213 (`timeout = \"700s\"` on lint)"
native_alternative = "mise per-task `timeout` (Tera-templated, so the bound can be overridden per clone via a `mise.local.toml` `[env]` value rather than by redefining the task), plus the global `task.timeout` setting / `mise run --timeout` for a whole-run bound"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060-1080 (`timeout` \"accepts durations such as `30s`, `5m`, or `1h` and supports Tera templates\"; \"Use `mise run --timeout` or the `task.timeout` setting to limit the entire task run\"; \"the shorter of the two wins\")"
rationale = "`.claude/rules/long-running-command-hangs.md` requires every command that can block on network/IO/a lock to carry a hard bound, and mise.toml:207-212 records that a native task `timeout` was probe-verified to \"kill the child and fail loud\". That probe was applied to exactly one task. These five are the repo's most hang-prone surface (a documented ~38GB buildkit pull, a devcontainer stop/up cycle, `docker run` against a possibly-absent tag). The bounds must be generous and per-task — hours for `dev-rebuild`, which `.claude/rules/verify-before-advancing.md` explicitly says to wait out — so this is a wide-bound backstop against a wedge, not a runtime budget. Confidence is mid because picking values that never abort a legitimately slow pull needs measured baselines, not a guess."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-5"
unit = "mise-tasks-1"
kind = "mise"
disposition = "refactor"
confidence = 0.80
custom_thing = "[tasks.persistence] is ~115 lines of inline bash in mise.toml: an inline `python3 -c` tomllib parser deriving EXPECTED_MIN, two `mise ls --json --installed` captures, host-side `jq`+`diff` snapshot comparison, a canary write/read/compare, and a multi-line heredoc of in-container `[ -f ... ] ||` assertions"
evidence = "mise.toml:457-571; the inline parser at mise.toml:483-489 (`EXPECTED_MIN=$(python3 -c \"import tomllib ...\")` — note bare `python3`, not the repo's pinned `uv run --project python`), the jq/diff drift check at mise.toml:541-547, the assertion heredoc at mise.toml:555-569"
native_alternative = "The repo's own established shape — a `dotfiles_setup` python module invoked by a one-line task, exactly as `[tasks.verify]`, `[tasks.names]`, `[tasks.ssh-port]` and `[tasks.migrate-home-volume]` already do — which is what `.claude/rules/zero-bash-logic.md` mandates for \"environment detection, tool config, validation, orchestration\""
alternative_evidence = ".claude/rules/zero-bash-logic.md (\"Non-trivial logic … lives in `python/` (`dotfiles_setup`). Bash is restricted to thin check/smoke wrappers\"); in-repo pattern at mise.toml:364-374 (verify), 323-327 (names), 329-335 (ssh-port), 336-343 (migrate-home-volume); the enforcing gate's scope at python/src/dotfiles_setup/bash_budget.py (allowlist + per-file budget over tracked `scripts/*.sh` and `.devcontainer/scripts/*.sh` only)"
rationale = "This is the single largest reservoir of inline logic in the unit and it escapes the project's own bash gate on a technicality: `bash_logic_budget` measures `.sh` FILES, so a 115-line task body in mise.toml is invisible to it — the policy is doc-only for exactly this shape. Moving the body to `dotfiles-setup devcontainer persistence` gets it into pytest (the drift comparison and the canary round-trip are unit-testable; today neither is), removes the duplicated tomllib parse (dotfiles_setup already reads mise-system.toml elsewhere), and stops the task from shelling to a bare `python3` whose version is whatever the host resolves. Keep the task as the one-line entrypoint so `.claude/rules/persistence-gate-retry.md`'s `mise run persistence` reference and `verify-local`'s call site keep working unchanged."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-6"
unit = "mise-tasks-1"
kind = "mise"
disposition = "unknown"
confidence = 0.40
custom_thing = "The same 2-4 line bash preamble — `_dc_env=\"$(uv run --project python dotfiles-setup devcontainer env)\"; eval \"${_dc_env}\"` (plus `ssh-keygen -R \"[localhost]:${DEVCONTAINER_SSH_PORT}\"` in two of them) — is copy-pasted into six task bodies"
evidence = "mise.toml:303-304 (up), 355-356 (dev-rebuild), 408-409 (smoke), 419-420 (sh), 474-475 (persistence), 1026 (verify-ssh-inbound, outside this unit but the same copy); the ssh-keygen duplication at mise.toml:311 and 357; `[tasks.ps]` (mise.toml:426-429) is a seventh variant calling the same CLI three separate times for individual names"
native_alternative = "mise task-level `env` with the Tera `exec()` function — `env = { DEVCONTAINER_ID_FLAGS = \"{{ exec(command='uv run --project python dotfiles-setup devcontainer name id-flags') }}\", ... }` — or `[env] _.source` pointing at a generated env script; both let mise populate the variables so the task body starts at the real work"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/templates.md:289 (\"`exec(command) -> String` – Runs a shell command and returns its output as a string\", listed under General Functions available in all tasks) and :345 (worked example `current = \"{{ exec(command='node --version') }}\"`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/environments/index.md:343 (`_.source = \"scripts/env.sh\"`)"
rationale = "The duplication is real and mise has a native env-population mechanism, but the load-bearing property here is FAILURE PROPAGATION, and the corpus does not state it for `exec()`. mise.toml:296-302 records that this preamble is written the way it is deliberately: a resolver that raises must abort the task, because `eval \"$(cmd)\"` discards the exit status and would let `devcontainer up` proceed with EMPTY container/volume names. Adopting `exec()` is only correct if a non-zero `exec()` command aborts config/task evaluation rather than yielding an empty string — and `env._.source` would additionally reintroduce a shell script, which [[zero-bash-logic]] pushes against. Probe first; if it fails loud, this collapses six bodies to zero preamble lines and removes the `# shellcheck disable=SC2086` word-splitting hazard at mise.toml:400-403."
probe = "printf '[tasks.armfail]\\nenv = { X = \"{{ exec(command=\\x27sh -c \\\"echo partial; exit 3\\\"\\x27) }}\" }\\nrun = \\x27echo \"GOT=[$X]\"\\x27\\n' > /tmp/mise-exec-probe/mise.toml && cd /tmp/mise-exec-probe && mise run armfail; echo \"rc=$?\"   # control arm: repeat with `exit 0` and confirm GOT=[partial] with rc=0, so the probe can produce both answers"
correction = "**⚠ CITATION CORRECTED (round 5) — WRONG FILE, RIGHT LINE.** Both native anchors name `sources/mise/docs/**tasks/**templates.md`; that file is **173 lines** and contains neither quotation (`grep -c 'exec(command' …/tasks/templates.md` -> **0**, rc=1). Control arm on the same file with the same command shape: `grep -c Tera` -> **2**, and a bogus token -> 0, so the probe discriminates and the file really does not hold the text. The quoted sentences are real and live one directory up, at the SAME line numbers: `/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/templates.md:289` (`- `exec(command) -> String` – Runs a shell command and returns its output as a string.`) and `:345` (`current = \"{{ exec(command='node --version') }}\"`). **Drop the `tasks/` path segment; the line numbers are already correct.** This was the worst-shaped anchor in the report — the wrong file exists, is about templates, and reads plausibly, so a reader following it concludes the claim is fabricated rather than mis-pointed. The claim's substance is unaffected; the row remains `unknown` because what is unproven is whether a `{{ exec() }}` template can carry this preamble in a mise task, not where the function is documented."
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-7"
unit = "mise-tasks-1"
kind = "mise"
disposition = "refactor"
confidence = 0.50
custom_thing = "[tasks.verify-image] runs an inline `docker run --rm --pull=never \"$BASE_IMAGE\" bash -lc '...'` whose body is a hand-written assertion script (ls of cargo/rustup cookbook dirs, `test ! -d /opt/cargo`, `cargo/rustc/clang --version`)"
evidence = "mise.toml:375-397 (assertions at 386-396)"
native_alternative = "The repo's existing containerized image-assertion lane: `[tasks.smoke-exec]` → `uv run --project python pytest -m image_exec tests/test_image_smoke_exec.py`, a pytest-marker-selected suite that already runs real-toolchain checks against the same local `:dev` image"
alternative_evidence = "mise.toml:234-238 (`[tasks.smoke-exec]` … \"`-m image_exec` re-selects the tests the root pytest.ini deselects by default\"), tests/test_image_smoke_exec.py, pytest.ini (the `image_exec` marker deselection)"
rationale = "Two parallel mechanisms assert facts about the same `:dev` image: one as pytest cases with names, per-assertion failure reporting and the deselect-by-default marker, the other as an un-testable `bash -lc` string embedded in TOML. Folding the six cookbook-path assertions into the image_exec suite gives them the same reporting and lets them run in the one place image assertions already live. Constraint on any such change: `python/verification/suites.toml:155` requires `verify-image` (with up and dev-rebuild) to keep exporting `DOCKER_DEFAULT_PLATFORM` resolved from `{{ env.DOTFILES_PLATFORM }}`, and `.claude/skills/devcontainer-workflow/SKILL.md:57` plus the `dcverify` shell alias (mise.toml:193) name the task — so keep the task name and its `env` block, and replace only the body. Confidence is mid because `verify-image` is deliberately cheap and does NOT need a running devcontainer, while the image_exec suite is documented as taking minutes under Rosetta; preserving a fast path may be worth more than consolidation."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-8"
unit = "mise-tasks-1"
kind = "mise"
disposition = "refactor"
confidence = 0.45
custom_thing = "[tasks.ps] is inline shell: three separate `dotfiles-setup devcontainer name ...` subprocesses, a `${_stem%-home}` suffix strip, an escaped Go-template `docker ps --format` string, and `docker volume ls --format '{{.Name}}' | grep -E \"^${_stem}\" || true`"
evidence = "mise.toml:424-440 (name resolution at 426-429, the volume grep at 439)"
native_alternative = "docker's own server-side name filter — `docker volume ls --filter name=<stem>` — instead of piping a full listing through `grep`; and, for the rest, the repo's one-line-task-over-a-python-module pattern (`dotfiles-setup devcontainer ...`) already used by `names`/`ssh-port`"
alternative_evidence = "In-repo pattern at mise.toml:323-327 (`[tasks.names]` = a single `dotfiles-setup devcontainer env` call) and mise.toml:329-335; the docker filter flag is asserted from `docker volume ls --help` on this host and is NOT in the offline corpus — see probe"
rationale = "This is the smallest of the inline-logic findings but it has a live correctness edge: `grep -E \"^${_stem}\"` under `set -euo pipefail` is the shape `.claude/rules/gh-cli-watch.md` and hk's `no_grep_q_under_pipefail` step exist to police — here it is defused by the trailing `|| true`, which also swallows a genuine docker failure, so a docker daemon error and an empty volume list are indistinguishable. Three subprocess calls to the same CLI to fetch three names from one resolver is also pure duplication of what `dotfiles-setup devcontainer env` already returns in one call (the form every other lifecycle task uses). Confidence mid: this is a read-only diagnostic, so the blast radius of leaving it is small, and the docker-filter half of the alternative needs the probe below because the corpus carries no docker documentation."
probe = "docker volume ls --help 2>&1 | grep -n -- '--filter'   # control arm: the same command shape grepped for a token known present ('--format') must also hit, and a bogus token ('--zzqfrp9') must not"
correction = "**⚠ SURVIVES ON LENS SCOPING, NOT ON REBUTTAL (round 4) — re-run its probe before acting.** In round 3 this row was **refuted 2/3** and sat under “Refuted”. A verdict re-run of `mise-tasks-1` (verdict files rewritten 15:42–15:47 local, finding text unchanged) flipped one lens and it now survives at 1/3. Both non-refuting lenses still concede the defect in their own words: `currency` writes “docker's substring name filter is not exactly equivalent to the anchored `grep -E \\\"^${_stem}\\\"` in the current code, but that is a correctness/behavior question, not a currency/availability one”, and `risk` answered blast radius rather than semantics. The erased round-3 refutations were live and control-armed — `docker volume ls --filter name=amd64-home` returned **12 unrelated volumes across seven other clones**, because docker's `name=` filter is an unanchored SUBSTRING match while `grep -E \\\"^${_stem}\\\"` is prefix-anchored on purpose (mise.toml:432-438, “Scoped to this clone's hashed volumes”). Verbatim text preserved at `.agent/kb/audit/round4-lost-verdicts.md` and reproduced in Method → Round 4. The surrounding refactor (dedupe the three `dotfiles-setup devcontainer name` subprocesses) is unaffected; only the `--filter name=` half is in doubt."
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-9"
unit = "mise-tasks-1"
kind = "mise"
disposition = "keep"
confidence = 0.70
custom_thing = "The thin-delegation group — `lint`, `test`, `test-hook-isolated`, `smoke-exec`, `names`, `ssh-port`, `migrate-home-volume`, `verify`, `autofix-apply`, `tool-currency` (one-line `uv run --project python ...` calls) and `gha-dispatch`, `gha-rerun`, `sh` (one-line `gh` / `devcontainer exec` calls)"
evidence = "mise.toml:207-220 (lint), 226-228 (test), 230-232 (test-hook-isolated), 234-238 (smoke-exec), 323-327 (names), 329-335 (ssh-port), 336-343 (migrate-home-volume), 364-374 (verify), 572-575 (autofix-apply), 576-585 (gha-dispatch/gha-rerun), 586-594 (tool-currency), 414-423 (sh)"
native_alternative = "Checked and rejected, each against the mise feature that could have applied: `sources`/`outputs` up-to-date skipping (task-configuration.md; already used correctly by `[tasks.\"deps:python\"]` at mise.toml:196-201); `confirm` prompts for destructive/slow tasks; `depends_post` cleanup chains; `wait_for` soft dependencies"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:349-360 (`confirm`, incl. the warning that it \"only guards the task's own `run` command\" while `depends` runs first), :224-244 (`depends_post`), :246-266 (`wait_for`); in-repo counter-evidence for `confirm`: python/src/dotfiles_setup/sync.py:778 invokes `[\"mise\", \"run\", \"dev-rebuild\"]` programmatically"
rationale = "Justification, not a default. (a) These are already the shape the project's rules require — a mise task on top of a python module or a native CLI, with zero logic in the task body — so there is nothing custom left to retire. (b) `sources`/`outputs` must NOT be added to any of them: they are gates (lint, test, verify, smoke-exec) and a cached skip would report green without running, which is precisely the false-pass `.claude/rules/verify-before-advancing.md` forbids; the file-list of inputs for `verify`/`lint` is also effectively the whole repo. (c) `confirm` looks attractive for the destructive/slow members but is disqualified here: `sync.py:778` drives `dev-rebuild` non-interactively, and the docs note confirm does not gate `depends`, so it would guard less than it appears to while risking a wedge in automation. (d) `depends_post`/`wait_for` have no cleanup or soft-ordering need in this group. (e) Liveness was checked: every member has callers except `sh`, whose entrypoint is the `[shell_alias] dcsh` binding at mise.toml:189 (`gha-rerun` has one reference, in `.claude/rules/mise-tasks-only.md:26`, which is its canonical-map row — that IS its enforcement surface, not a dead mention). Control arm for the liveness grep: `mise run lint` → 282 hits, a bogus `mise run zzqfrp9` → 0, so the probe discriminates."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-10"
unit = "mise-tasks-1"
kind = "mise"
disposition = "keep"
confidence = 0.85
custom_thing = "[tasks.lint] keeps a python timeout wrapper (`dotfiles-setup lint`, default 600s, hk-debug-log tail on expiry) even though it also declares mise's native `timeout = \"700s\"`"
evidence = "mise.toml:207-220 — the native bound at :213 and the written retire-justification at :207-212 (\"probe showed mise's task timeout kills the child and fails loud, but it lacks lint.py's hk-log-tail diagnostics, so the wrapper STAYS (decision 15's retire condition was full equivalence; it is not equivalent)\"); implementation at python/src/dotfiles_setup/lint.py"
native_alternative = "mise per-task `timeout` — already adopted here as the outer backstop, deliberately set 100s above the wrapper's own bound so the inner, diagnostic-producing timeout fires first"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060-1077 (`timeout`, \"The task fails if it does not complete within the configured duration\")"
rationale = "This is the one member where the native-vs-custom question was already asked, probed, and answered in writing — and the answer is a layered adoption, not a rejection: mise's `timeout` is used, and the wrapper survives only for the capability mise does not provide (killing hk's whole process group and printing the tail of the hk debug log, which is what turned a 7-hour silent wedge into a diagnosable failure per `.claude/rules/long-running-command-hangs.md`). Nothing in the mise gap digest for 2026.9.1-2026.9.3 adds log-tail-on-timeout or process-group-kill reporting (the only task-adjacent items are an inline-task shell-wrapper optimisation and a redesigned install progress reporter), so the retire condition stated at mise.toml:210-212 is still unmet at the host's 2026.9.3. Re-check it when mise's `timeout` gains failure diagnostics."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-1-11"
unit = "mise-tasks-1"
kind = "mise"
disposition = "unknown"
confidence = 0.50
custom_thing = "`[tasks.\"deps:python\"]` — a hand-written mise task that re-declares, field for field, mise's BUILT-IN `uv` dependency provider: `dir = \"python\"`, `sources = [\"pyproject.toml\", \"uv.lock\"]`, `outputs = [\".venv/\"]`, `run = \"uv sync\"`. This is the one member of mise.toml's 87 `^[tasks.` blocks that no group's member list contained (86 audited), so no finder took it as a subject in rounds 1-5."
evidence = "mise.toml:196-201 (the whole task block, verbatim: `[tasks.\"deps:python\"]` / `description = \"Sync python venv when pyproject.toml or uv.lock changes\"` / `dir = \"python\"` / `sources = [\"pyproject.toml\", \"uv.lock\"]` / `outputs = [\".venv/\"]` / `run = \"uv sync\"`). The SAME FILE already declares the native provider 188 lines above it at mise.toml:5-8 — `[deps.uv]` / `auto = true` / `dir = \"python\"` / `run = \"uv sync --locked\"`. Liveness, control-armed: an unscoped `grep -rn 'deps:python'` over the tree (excluding `.agent/` and this report) returns exactly TWO hits — its own definition at mise.toml:196 and a quoted string inside a test fixture at tests/test_handoff_check.py:205 — i.e. no `depends`, no python caller, no workflow step, and no row in `.claude/rules/mise-tasks-only.md`'s canonical task map. Control arm, same command shape: `grep -rn 'run dev-rebuild'` returns mise.toml:190, :271, :449, :1059 and python/src/dotfiles_setup/sync.py:60, so the probe does find a referenced task when one exists."
native_alternative = "mise's built-in `uv` deps provider, which the repo ALREADY declares and gates: `[deps.uv] auto = true` runs before every `mise run` and `mise x`, with the provider's own default sources/outputs/command being `pyproject.toml`,`uv.lock` -> `.venv/` -> `uv sync` — identical to the three keys the task hand-writes. Downstream of that, uv itself locks and syncs on every `uv run`, which is the form every other task in this file uses (`uv run --project python ...`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/dev-tools/deps.md:68 — the built-in provider table row `| `uv` | `pyproject.toml`, `uv.lock` | `.venv/` | `uv sync` |`, i.e. the provider's defaults ARE the task's three keys; :158 `| `auto` | bool | Auto-run before `mise x` and `mise run` (default: false) |`; :236-240 \"When `auto = true` is set on a provider, it will automatically run before: - `mise run` (task execution) - `mise x` (exec command)\". And /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/docs/concepts/projects/sync.md:9-11 \"Locking and syncing are _automatic_ in uv. For example, when `uv run` is used, the project is locked and synced before invoking the requested command.\" (opt-outs `--locked`/`--frozen`/`--no-sync` at :14-32). Control arm for the corpus greps: `grep -c 'uv run' .../uv/docs/guides/projects.md` -> 10, and `grep -n '^### sources'` in `.../mise/docs/tasks/task-configuration.md` -> 0 hits, so a 0-result is distinguishable from a blind probe."
rationale = "SYNTHESIS-AUTHORED AT ROUND 6, DELIBERATELY FILED `unknown` RATHER THAN `retire`. On the evidence above this row is retire-SHAPED and unusually well cited on both sides: the task duplicates the built-in provider's defaults exactly, the provider is already declared with `auto = true` at mise.toml:5-8, and the task has no caller. It is filed `unknown` because it was authored by the SYNTHESIS stage, not the finder, and no verification lens has ever voted on it — filing it `retire` would either enter the report as a `contested_unverified` row (breaking the run's completeness invariant) or, worse, be read as confirmed. `unknown` is the honest bucket: the claim is published in full with the probe that settles it, and it cannot inflate the survived-retire count. TWO SUBSTANTIVE POINTS A LENS SHOULD TEST. (1) The task's `run = \"uv sync\"` is the UNLOCKED spelling; this repo's own `dependency_ownership` gate exists to force the LOCKED one on the provider (python/src/dotfiles_setup/dependency_ownership.py:85-91 asserts `uv_provider.get(\"run\") == \"uv sync --locked\"`, error text at :109 \"mise [deps.uv] must auto-run uv sync --locked\"), and that gate reads `mise_values['deps']['uv']` ONLY — it cannot see this task. So `mise run deps:python` is a supported route to the exact unlocked sync the gate forbids one layer up, and it is invisible to the gate. (2) `[deps.uv]` has no `sources`/`outputs` of its own in mise.toml:5-8, so if anything is genuinely wanted from this block it is those two keys moved ONTO the provider, not a second task. NOT a citation defect: both sides are cited to line. What is unproven is only the disposition, and one lens pass settles it."
probe = "Settle the disposition in three commands, all read-only. (a) Confirm the provider already covers it: `mise deps ls` (or `mise deps --help` on 2026.9.3) and `grep -n -A3 '^\\[deps.uv\\]' mise.toml` -> expect `auto = true`, `dir = \"python\"`, `run = \"uv sync --locked\"`. (b) Confirm nothing invokes the task: `grep -rn 'deps:python' --include='*.toml' --include='*.md' --include='*.yml' --include='*.py' --include='*.pkl' . | grep -v '^./.agent/' | grep -v '^./docs/research/'` -> expect exactly mise.toml:196 and tests/test_handoff_check.py:205; CONTROL ARM, same shape: `grep -rn 'run dev-rebuild' ...` must return >=5 hits, or the grep is blind. (c) Confirm the unlocked-sync hole is real: `uv run --project python dotfiles-setup dependency-ownership` (or whatever the CLI verb is at main.py:1065) exits 0 today, while `mise run deps:python` runs `uv sync` UNLOCKED in the same dir — i.e. re-run the gate after temporarily setting `[deps.uv] run = \"uv sync\"` and confirm it FAILS (the gate's own control arm, per tests/test_dependency_ownership.py:56 which does exactly that substitution), then confirm it still passes with the task's unlocked `run` untouched. If (a)+(b)+(c) hold, the disposition is `retire`: delete mise.toml:196-201, and if the change-detection is wanted, add `sources`/`outputs` to `[deps.uv]` instead."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-2-1"
unit = "mise-tasks-2"
kind = "mise"
disposition = "refactor"
confidence = 0.90
custom_thing = "tool-currency-check, doctor and command-audit are documented as \"silent unless drift\" and are wired as the SessionStart/SessionEnd hook bodies, but none declares mise's `quiet` option, so mise prints its own `[<task>] $ <cmd>` banner on EVERY session regardless of whether the task itself found anything."
evidence = "mise.toml:595-602 (`[tasks.tool-currency-check]`, description at :596 says \"SessionStart hook; silent unless drift\", comment at :597 \"silent when clean\", run at :602); mise.toml:604-620 (`[tasks.doctor]`, description at :605 same phrase); mise.toml:701-711 (`[tasks.command-audit]`, run at :711). Wiring: .claude/settings.json hooks.SessionStart -> `mise -C \"${CLAUDE_PROJECT_DIR:-.}\" run tool-currency-check; DOTFILES_AMBIENT_PATH=\"$PATH\" mise -C \"${CLAUDE_PROJECT_DIR:-.}\" run doctor`, hooks.SessionEnd -> `mise -C ... run command-audit -- --output ...`. LIVE PROBE (2026-09-09, host mise 2026.9.3): `mise run tool-currency-check` emitted `[tool-currency-check] $ uv run --project python kb-setup currency check` before any task output, rc=0. CONTROL ARM: `mise run mise-config-context` (mise.toml:1262-1276, the ONE task in this file that sets `quiet = true` at :1276) emitted NO banner, rc=0 — so the banner is caused by the missing option, not by the harness."
native_alternative = "mise task option `quiet = true` — \"Suppress mise's output for the task such as showing the command that is run, e.g.: `[build] $ cargo build`. When this is set, mise won't show any output other than what the script itself outputs.\" (sibling `silent` suppresses the task's own output too, and `output` is the orthogonal style knob.)"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1179 (`### quiet`), :1199 (`### silent`), :1205 (`### output`). Already adopted in-repo at mise.toml:1276, which is what the control arm above exercised."
rationale = "The tasks' own descriptions state a contract (silent when clean) that the task definitions do not implement; the noise is mise's, not the python module's, so no python change is needed. Adding `quiet = true` to `[tasks.tool-currency-check]`, `[tasks.doctor]` and `[tasks.command-audit]` makes the definition match the description and removes three lines of per-session banner from the SessionStart/SessionEnd hook surface. Do NOT reach for `silent` here: the whole point of these tasks is to print when they DO find drift."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-2-2"
unit = "mise-tasks-2"
kind = "mise"
disposition = "refactor"
confidence = 0.80
custom_thing = "Every network- or subprocess-bound task in this unit runs unbounded: renovate-dryrun (documented ~80s, and its own comment warns a `deb` datasource pointed at Ubuntu's archive downloads the whole main,universe Packages index), verify-apt-pins (throwaway `docker run` + apt solver, ~60s), apt-repo (fetches a remote Packages index), graphify-prs (concurrent `gh pr diff` network calls), bakeoff (repeated model extraction runs), codex-lane and codex-lane-e2e (real `codex exec`, network + credits), and `doctor -- --live` (spawns each stdio MCP server). None declares a `timeout`."
evidence = "mise.toml:844-877 (`[tasks.renovate-dryrun]`; the slow-datasource note is the comment block ending at :876, `tools.node = \"24\"` at :876, run at :877); mise.toml:806-826 (`[tasks.verify-apt-pins]`); mise.toml:679-699 (`[tasks.apt-repo]`); mise.toml:770-779 (`[tasks.graphify-prs]`, comment: \"makes NETWORK calls (gh pr list / gh pr diff)\"); mise.toml:781-804 (`[tasks.bakeoff]`); mise.toml:654-669 (`[tasks.codex-lane]`) and :671-677 (`[tasks.codex-lane-e2e]`, \"COSTS CREDITS\"); mise.toml:604-620 (`[tasks.doctor]`, comment at :617 \"`-- --live` adds the MCP server spawns\"). Grep over mise.toml for `^timeout =` returns exactly ONE hit, mise.toml:213 on `[tasks.lint]` — none of this unit's 22 members."
native_alternative = "mise per-task `timeout` (\"Maximum execution time for this task... accepts durations such as `30s`, `5m`, or `1h`... The task fails if it does not complete within the configured duration\"), plus the `mise run --timeout` CLI flag and the global `task.timeout` setting."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060-1078 (`### timeout`). LIVE PROBE on the installed binary (2026.9.3): `mise run --help` prints `--timeout <TIMEOUT>  Timeout for the task to complete`. In-repo proof the mechanism works: mise.toml:207-213, whose comment records \"probe showed mise's task timeout kills the child and fails loud\"."
rationale = "`.claude/rules/long-running-command-hangs.md` requires every command that can block on network/IO to carry a hard bound, and mise.toml:213 already proves the native option delivers one for this repo — the pattern is established, just not applied here. Declaring a generous `timeout` (e.g. `10m` on renovate-dryrun/verify-apt-pins/apt-repo/graphify-prs, longer on bakeoff/codex-lane) converts a wedge into a loud failure at zero maintenance cost. EXCLUDE `ship` (mise.toml:887-894) and `land` (:896-900): they deliberately wait on remote GitHub CI, which the same rule exempts from local bounding, and a wrong bound there would abort a merge mid-flight."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mise-tasks-2-3"
unit = "mise-tasks-2"
kind = "mise"
disposition = "refactor"
confidence = 0.75
custom_thing = "`graphify-update` rebuilds graphify-out/graph.json from the repo tree but declares neither `sources` nor `outputs`, so mise cannot tell whether the graph is up to date; and the repo's own freshness gate cannot either — `graphify_health` never compares the graph against source mtimes/content. Its only STALE path is a build-receipt mismatch, and `graphify-first.md` states this repo writes no receipt, so a graph arbitrarily behind the tree reports FRESH and every `graphify-query`/`graphify-affected` caller is silently served stale answers."
evidence = "mise.toml:737-747 (`[tasks.graphify-update]`, run at :747) — no `sources`/`outputs` keys anywhere in the block. python/src/dotfiles_setup/graphify.py:224-242 (`def graphify_health`): the branches are MISSING (no file), CORRUPT (parse/schema), VERSION_DRIFT (runtime != 0.9.53), `_receipt_problem`, else FRESH — no source-vs-graph comparison; `_graph_stale`/mtime grep over the module returns 0 hits for any mtime/newer comparison (control arm: grepping `stale` in the same file returns hits at :53 and :407, so the grep shape works)."
native_alternative = "mise `sources` + `outputs`: \"if this and `outputs` is defined, mise will skip executing tasks where the modification time of the oldest output file is newer than the modification time of the newest source file\" and \"This is also used in `mise watch` to know which files/directories to watch\" — with `mise watch graphify-update` then rebuilding on change."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:431-445 (`### sources`) and :586 (`### outputs`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md:183-197 (\"skip executing a task if certain files haven't changed\"). LIVE PROBE: `mise watch --help` on 2026.9.3 prints \"Run task(s) and rerun them when files change / Uses `watchexec` to watch task sources\". Already adopted in-repo at mise.toml:196-200 (`[tasks.\"deps:python\"]`, `sources = [\"pyproject.toml\", \"uv.lock\"]`, `outputs = [\".venv/\"]`)."
rationale = "Declaring `sources = [\"python/src/**/*.py\", \"docs/**/*.md\", ...]` and `outputs = [\"graphify-out/graph.json\"]` makes a redundant `mise run graphify-update` a no-op and gives the repo a real freshness signal at the task layer, which graphify_health structurally does not provide. It also unlocks `mise watch graphify-update` as the SANCTIONED substitute for graphify's own `--watch`, which `.claude/rules/do-not.md` #8 bans outright. Scope the source globs deliberately: too wide and every doc edit invalidates the graph, which is arguably correct but costly."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mise-tasks-2-4"
unit = "mise-tasks-2"
kind = "mise"
disposition = "unknown"
confidence = 0.50
custom_thing = "`dag-tick` is a 60s launchd-fired watchdog that censuses background agents, classifies ALIVE/DEAD/WEDGED/DONE, respawns DEAD and stops lingering DONE, guarding overlap with a hand-held fcntl flock at ~/.local/state/dotfiles/dag-tick.lock — i.e. supervision, auto-restart and interval scheduling implemented in python."
evidence = "mise.toml:622-634 (`[tasks.dag-tick]`; description at :623 names the ALIVE/DEAD/WEDGED/DONE classification and the respawn/stop behaviour, comment at :628-631 names the LaunchAgent `dev.mise.dotfiles-dag-tick` declared in `[bootstrap.macos.launchd.agents.dotfiles-dag-tick]` and the `fcntl flock`, run at :634)."
native_alternative = "The SCHEDULING half is already native — mise's own launchd bootstrap (`mise bootstrap macos launchd-agents apply`) is what the task's comment already uses. The SUPERVISION half (daemon management, auto-restart, readiness checks, cron scheduling) is explicitly delegated by mise to its sister project pitchfork, not implemented in mise itself."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/bootstrap/launchd.md (launchd agent declaration, the mechanism the task already uses) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/watch.md:14 — \"For more advanced process management (daemon management, auto-restart, readiness checks, cron scheduling), see mise's sister project: https://pitchfork.jdx.dev\"; also /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/faq.md:198-200 (\"What is pitchfork?\" — \"a process manager for developers\"). Confirmed on the installed binary: `mise watch --help` prints the same pitchfork pointer."
rationale = "mise itself provides nothing that supervises processes, so nothing here can be retired against mise. pitchfork is the author-sanctioned adjacent tool and is NOT pinned in this repo, so whether it can supervise dag-tick's subjects is unsettled: pitchfork supervises daemons IT starts, while dag-tick's subjects are Claude Code background agents spawned by the harness, which it would have to adopt rather than launch. If pitchfork cannot adopt foreign PIDs, only the flock/interval plumbing is replaceable and the census/classify/respawn logic stays custom — probably not worth a dependency."
probe = "mise x aqua:jdx/pitchfork -- pitchfork --help 2>&1 | head -40   # then: does any subcommand supervise/restart a process it did not itself start (adopt an existing PID), and does it expose a per-run mutual-exclusion lock equivalent to the fcntl flock at ~/.local/state/dotfiles/dag-tick.lock?"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-2-5"
unit = "mise-tasks-2"
kind = "mise"
disposition = "keep"
confidence = 0.80
custom_thing = "Outward-facing / irreversible tasks gate themselves in python rather than at the task layer: `dag-project` requires exactly one of `--dry-run` or `--write` and REFUSES when neither is given; `ship` refuses main/dirty trees before pushing; `land` treats invocation as the merge approval."
evidence = "mise.toml:636-652 (`[tasks.dag-project]`; description at :637 \"Pass exactly one of --dry-run or --write — neither REFUSES, because writing is outward-facing\", run at :652); mise.toml:887-894 (`[tasks.ship]`, comment at :890-891 \"Requires committed work on a feature branch (refuses main / dirty tree)\", run at :894); mise.toml:896-900 (`[tasks.land]`, comment at :898 \"Invocation IS the merge approval\", run at :900). Grep over mise.toml for `^confirm =` returns 0 hits (control arm: `^timeout =` in the same command shape returns mise.toml:213, so the grep discriminates)."
native_alternative = "mise task `confirm` — \"A message to show before running the task. This is useful for tasks that are destructive or take a long time to run. The user will be prompted to confirm before the task's own `run` command executes\", with the `{ message, default }` form and Tera templating over usage args."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:349-377 (`### confirm`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md:199-209 (\"### Confirmation\")."
rationale = "REQUIRED JUSTIFICATION: `confirm` is an interactive TTY prompt, and all three of these tasks must work non-interactively — `dag-project` is the scheduler slice destined for a LaunchAgent (mise.toml:643-648 assigns the schedule to phase 4), and `ship`/`land` are invoked by agents through the PreToolUse guard's redirect, where a prompt would hang the turn rather than protect anything. The explicit `--dry-run`/`--write` pair is strictly stronger than `confirm`: it is machine-checkable, it records intent in the command line the transcript keeps, and it fails closed when neither is passed instead of defaulting on a keystroke. Do not adopt `confirm` on these three; it would trade a deterministic gate for a prompt no automation can answer."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-2-6"
unit = "mise-tasks-2"
kind = "mise"
disposition = "keep"
confidence = 0.65
custom_thing = "Every flag-taking member documents its arguments only in a TOML comment and forwards `--` args straight to a python argparse CLI: `mise run doctor -- --verbose|--live|--strict`, `dag-project -- --dry-run|--write`, `codex-lane -- --node <id> --prompt-file <f>`, `apt-repo -- --llvm-version <v> --toml`, `memory-index -- --refs <name>`, `verify-apt-pins -- --json`, `renovate-dryrun -- --check|--json`, `graphify-affected -- \"<node>\" --depth 3`, `graphify-prs -- 822 --base main`, `bakeoff -- --repeats 5`."
evidence = "mise.toml:614-618, :645-646, :666-667, :685-690, :719-720, :810-811, :849-851, :765-766, :776-777, :800-802 (the usage lines are TOML comments in each block). No member declares a `usage` field; grep over mise.toml for `^usage = ` returns 0 hits (control arm: `^tools\\.` returns mise.toml:876, so the anchored grep shape finds keys that are present)."
native_alternative = "mise task `usage` spec — declares args/flags with help text, exposes them as `usage_*` env vars, and drives shell completion including custom `complete \"<name>\" run=\"...\"` sources."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1210-1240 (`### usage`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md:458-473 (\"Recommended: Using the Usage Field\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-arguments.md:321-333 (`### Completion (complete)`)."
rationale = "REQUIRED JUSTIFICATION: adopting `usage` here would not replace anything — the flags are parsed by argparse inside each `dotfiles_setup` entrypoint, and a `usage` spec exposes values as `usage_*` env vars that the `run` line must re-interpolate, so the repo would carry TWO parsers for one flag set. That is the exact shape the repo has already been burned by (a third rule parser, #951 in project memory). The only real gain is shell completion, which is not worth a duplicated, silently-divergable argument surface. Revisit only if mise gains pure pass-through arg documentation that does not intercept the args."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-2-7"
unit = "mise-tasks-2"
kind = "mise"
disposition = "keep"
confidence = 0.85
custom_thing = "`doctor` hand-resolves the effective MCP server set from four registration sources (.mcp.json, each enabled plugin's bundled .mcp.json resolved through `~/.claude/plugins/marketplaces/<mkt>/.claude-plugin/marketplace.json`, and `~/.claude.json`'s user-global AND per-project blocks), then checks `${VAR}` interpolation for values that resolve EMPTY and fnox's env mode against doctor.toml."
evidence = "mise.toml:604-620 (`[tasks.doctor]`; comment at :607-611 names the seam \"MCP `${VAR}` interpolation that silently resolves EMPTY, an MCP scope declaration the harness's roots replace, and fnox's env mode\"; run at :620). python/src/dotfiles_setup/doctor.py:296-322 (`plugin_mcp_path`, walking the marketplace manifest), :334-347 (`claude_json_servers`, whose docstring records the measured defect: \"``check_mcp_duplicate`` reported PASS while ``context7`` and ``filesystem`` were each registered twice\" and \"a same-name user-global entry **shadows** the project one, so the broken wrapper won and ``claude mcp list`` stopped showing the project's ``filesystem`` server at all\")."
native_alternative = "The nearest native surfaces are `claude mcp list` / `claude mcp get`, `claude doctor` and `/status` (2.1.261 added an \"Organization policy\" line explaining why a policy could not be loaded), and `claude plugin validate --json` (2.1.259, machine-readable manifest validation, `--strict` fails on unrecognized fields)."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (bullets: \"Added an 'Organization policy' line\" in `/status` and `claude doctor`, version 2.1.261; \"Added `--json` to `claude plugin validate`\", version 2.1.259). LIVE PROBE on host 2.1.266: `claude plugin validate --help` -> \"Validate a plugin or marketplace manifest, or the skills, agents, and commands in a directory\", options `--json`, `--strict`."
rationale = "REQUIRED JUSTIFICATION: none of the native surfaces answers doctor's question. `claude plugin validate` validates a manifest's SHAPE, not which servers a given session ends up with; `claude doctor`/`/status` report harness health, not a declared-vs-real diff against doctor.toml; and `claude mcp list` is the exact tool this module's docstring records as having gone BLIND to a shadowed server — the defect class doctor exists to catch. The measured failure is written into doctor.py:345-346, so this is a justified custom check, not an unexamined one. Adjacent, not superseding: `claude plugin validate --strict --json` is new since the offline corpus and would make a good ADDITIONAL gate over `.claude/plugins`/marketplace manifests; that is a new check to consider, not a replacement for anything in this unit."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-2-8"
unit = "mise-tasks-2"
kind = "mise"
disposition = "keep"
confidence = 0.85
custom_thing = "`apt-repo` enumerates an apt repository's published packages by reading the Packages index directly via python-debian, and `verify-apt-pins` simulates apt's solver against the digest-pinned base image in a throwaway container to prove every `[bootstrap.packages]` version still resolves."
evidence = "mise.toml:679-699 (`[tasks.apt-repo]`; the comment at :683-686 already carries the required native-first justification: \"mise itself cannot do this (`mise bootstrap packages` has no search; `import`/dpkg-query only ever see INSTALLED packages), and apt-cache would require adding the repo first\"; run at :699). mise.toml:806-826 (`[tasks.verify-apt-pins]`; comment at :816-819 records the cost it replaces — \"until this task the ONLY thing that noticed was a cold base rebuild (~2.5h of CI)\" — and :821-825 records why the devcontainer is the wrong probe environment; run at :826)."
native_alternative = "mise's own package bootstrap surface (`mise bootstrap packages`, `[bootstrap.packages]`) is the only native candidate, and it installs/imports declared packages — it exposes no repository search and no resolve-only simulation."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/bootstrap/packages/index.md (the bootstrap packages surface — declaration and installation; no search or dry-resolve subcommand documented). Version context: .agent/kb/raw/mise-releases-since-pin.digest.md — \"There are ZERO releases strictly newer than the baseline\" (host 2026.9.3), so no post-corpus mise release adds one."
rationale = "REQUIRED JUSTIFICATION: both tasks answer questions about packages that are NOT installed — 'what does this repo publish' and 'would this exact version still resolve' — and every native mechanism (mise bootstrap packages, dpkg-query, apt-cache) can only speak about a configured repo or an installed package. The justification is already written at the call site (mise.toml:683-686), which is what `.claude/rules/use-tool-builtins.md` requires, and the mise gap digest confirms no newer release closes it. Both remain thin `uv run` callers with all logic in python/src/dotfiles_setup/{apt_repo,apt_pins}.py."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-2-9"
unit = "mise-tasks-2"
kind = "mise"
disposition = "keep"
confidence = 0.80
custom_thing = "`codex-lane` writes a lane record + verdict JSON schema, runs `codex exec` with BOTH `--output-schema` and `-o`, scrubs the environment via `LANE_ENV_OVERRIDES = {\"PLANNING_DISABLED\": \"1\"}`, and settles the lane so dag-tick's reaper can find it; `codex-lane-e2e` is its credit-spending real-invocation arm."
evidence = "mise.toml:654-669 (`[tasks.codex-lane]`; comment at :660-664 records the native-first justification lives once, in the module docstring; run at :669) and mise.toml:671-677 (`[tasks.codex-lane-e2e]`, run at :677). python/src/dotfiles_setup/codex_lane.py:121-136 (the PLANNING_DISABLED rationale: \"The planning-with-files hooks fire in any process that inherits this session's environment... `PLANNING_DISABLED=1` is the plugin's own per-invocation opt-out\", with a measured two-arm probe \"unset -> 1100 bytes of injected plan context, set -> 0 bytes\"), :469 (`env={**os.environ, **LANE_ENV_OVERRIDES}`), :113-119 (`READ_ONLY_SANDBOX`)."
native_alternative = "Candidates from the codex 0.148-0.152 gap: `codex exec fork` (0.148.0), the interactive `codex agents` dashboard (0.149.0), `codex queue` (0.149.0), `codex doctor` (0.149.0), async/MCP-invoking hooks (0.148.0) and Interrupt hooks (0.150.0), and — the closest-looking one — \"The planning tool is now disabled by default; enable via `tools.update_plan.enabled = true`\" (0.152.0)."
alternative_evidence = ".agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md (bullets for `codex exec fork` @0.148.0, `codex agents` dashboard @0.149.0, `codex queue` @0.149.0, Interrupt hooks @0.150.0, and \"The planning tool is disabled by default\" @0.152.0, anchor `## rust-v0.152.0`)."
rationale = "REQUIRED JUSTIFICATION, and specifically a warning against a wrong retire: codex 0.152.0's `tools.update_plan.enabled` default does NOT supersede `PLANNING_DISABLED=1`. They are different subsystems — `update_plan` is codex's own planning TOOL, while `PLANNING_DISABLED` is the planning-with-files CLAUDE PLUGIN's opt-out that stops its hooks from injecting this session's `task_plan.md` into any child process (codex_lane.py:121-129, measured both arms). Removing the env scrub because codex changed a default would silently re-open plan leakage into the lane. The other new codex surfaces are interactive (`agents` dashboard, `queue`, `/export`) or session-shaped (`exec fork`) and none of them writes the lane record + verdict schema + settle marker that `codex_verdict.lane_is_settled` and dag-tick's reaper read, so the producer half stays custom. Both tasks are thin: single-line `uv run`/`pytest` callers."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-2-10"
unit = "mise-tasks-2"
kind = "mise"
disposition = "keep"
confidence = 0.85
custom_thing = "Unit-level shape check across all 22 members (tool-currency-check, doctor, dag-tick, dag-project, codex-lane, codex-lane-e2e, apt-repo, command-audit, memory-index, graphify-query, graphify-health, graphify-update, graphify-skill-install, graphify-affected, graphify-prs, bakeoff, verify-apt-pins, instructions-report, renovate-dryrun, renovate-status, ship, land): inline-logic audit and dead-task audit."
evidence = "SHAPE: every member's `run` is a single line delegating to python — mise.toml:602, 620, 634, 652, 669, 677, 699, 711, 722, 731, 735, 747, 758, 768, 779, 804, 826, 842, 877, 885, 894, 900. Zero shell logic, zero multi-line scripts, zero `[ ... ]`/`if`/`for` in any member body (grep for `^run = ` in the 580-910 range returns 24 single-line hits, 22 of them these members plus tool-currency and gha-rerun). DEAD-TASK GREP (`git grep -l \"mise run <name>\" -- ':!mise.toml'`, control-armed against `mise run lint` which returns many hits incl. .agents/skills/*/SKILL.md): ship 61, land 41, graphify-query 31, doctor 17, graphify-health 16, memory-index 9, codex-lane 8, command-audit 7, graphify-update 7, graphify-skill-install 7, dag-tick 6, verify-apt-pins 6, graphify-affected 5, codex-lane-e2e 5, renovate-dryrun 5, tool-currency-check 4, graphify-prs 4, instructions-report 3, apt-repo 2, dag-project 2, renovate-status 2 — and bakeoff 1, whose single hit is a hook-guard test fixture string (tests/test_hook_guard.py:60), not a caller."
native_alternative = "For the shape itself, mise's `hide = true` (keep a task out of `mise tasks` listings) is the only relevant native knob for a task nobody invokes; for the wrapper shape there is nothing to adopt — a one-line `run` delegating to a library IS the documented `run` form."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:335 (`### hide`), :10 (`### run`)."
rationale = "REQUIRED JUSTIFICATION: this unit is already in the shape `.claude/rules/zero-bash-logic.md` and `.claude/rules/agent-artifact-conventions.md` §6 prescribe (skill -> mise task -> python library) — 22/22 members are thin `uv run --project python` callers with every mechanic in `python/src/dotfiles_setup/`, so there is no inline-logic finding to report and nothing for mise to absorb. No member is dead: 21 have live callers in skills, rules, CI, hooks or python. `bakeoff` is the one with no live caller — it is a one-off research harness whose deliverable (docs/research/kb/reports/bake-off-extraction-models.md) has already been produced, and it retains its module (python/src/dotfiles_setup/graph_bakeoff.py) and tests (tests/test_graph_bakeoff.py). Keep it (it is re-runnable and its null-arm noise-floor design is the reason its numbers mean anything), but flag it to the synthesis stage as the unit's only zero-caller task — a candidate for `hide = true` rather than deletion."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-1"
unit = "mise-tasks-3"
kind = "mise"
disposition = "refactor"
confidence = 0.85
custom_thing = "`[tasks.fmt]` runs `hk fix` with no time bound (mise.toml:1204-1206), while its read-only sibling `[tasks.lint]` carries BOTH a python hard-timeout wrapper and mise's native `timeout = \"700s\"` (mise.toml:213). hk has no timeout of its own — the 7h wedge in .claude/rules/long-running-command-hangs.md was an `hk run` invocation, and `hk fix` is the same binary with a write path."
evidence = "mise.toml:1206 `run = \"hk fix\"` (no `timeout` key in the `[tasks.fmt]` block, lines 1204-1206); contrast mise.toml:213 `timeout = \"700s\"` on `[tasks.lint]`. Control arm for the grep: `grep -n \"timeout\\s*=\" mise.toml` returns 213 only, and the same command shape returns hits for `sources =` (199) and `depends =` (224), so the 0-result for fmt is real, not a broken probe."
native_alternative = "mise per-task `timeout` (accepts `30s`/`5m`/`1h`, fails the task loud on expiry). Available at the host's mise 2026.9.3 and already exercised in this very file."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060 (`### \\`timeout\\``, \"Maximum execution time for this task... The task fails if it does not complete within the configured duration\"); the mise gap digest .agent/kb/raw/mise-releases-since-pin.digest.md records ZERO releases newer than the host's 2026.9.3, so the corpus is current for this key."
rationale = "One-line adoption of a native key the repo has already validated on the neighbouring task (mise.toml:207-212 records the #160 T12.5 probe: 'mise's task timeout kills the child and fails loud'). Set `timeout` on `fmt` at or above the lint budget. Note the lint task's own comment explains why the python wrapper stays there (log-tail diagnostics); `fmt` has no wrapper at all, so native timeout is a strict improvement over nothing rather than a redundancy."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-2"
unit = "mise-tasks-3"
kind = "mise"
disposition = "refactor"
confidence = 0.70
custom_thing = "`[tasks.verify-local]` is an inline bash body that re-enters mise eight times (`mise run verify-image` / `up` / `ps` / `verify-arch` / `smoke` / `persistence` / `verify-ssh-inbound` / `verify-secrets`), with a comment stating it is bash specifically because `depends = [...]` would parallelise the steps."
evidence = "mise.toml:1048-1071 — comment at 1050-1051 (\"Sequential shell body (not `depends = [...]`) so mise can't parallelize the steps\"), body at 1060-1071 (`set -euo pipefail` then the eight `mise run <task>` lines, 1062-1071). Same shape at mise.toml:443 (\"NOTE: `depends = [...]` in mise runs tasks in parallel when independent\")."
native_alternative = "mise `run` accepts an ARRAY of execution steps that may be task references: `run = [{ task = \"verify-image\" }, { task = \"up\" }, ...]`. Array commands \"are run in series\" and `{ task }` steps are execution steps rather than `depends` edges, so they do not get the parallel scheduling the comment is avoiding — the exact semantics this bash body hand-rolls, without a nested mise process per step."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:10-33 (`### \\`run\\``, type includes `{ task: string, args?, env? }`; \"`{ task }` and `{ tasks }` are execution steps for this task, not `depends`\"; `{ tasks = [...] }` is the explicit PARALLEL form, so the bare `{ task }` list is the serial one) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md:100 (\"Commands are run in series. If a command fails, the task will stop\")."
rationale = "The native mechanism reproduces the stated requirement (serial, fail-fast, not the parallel `depends` graph) and removes an inline `set -euo pipefail` shell body per [[zero-bash-logic]]. Not 'retire' because one behaviour must be re-checked before the swap: the current body re-enters `mise run`, so each step re-resolves env/tools; `{ task }` steps run inside the parent task's context. Probe before landing."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-3"
unit = "mise-tasks-3"
kind = "mise"
disposition = "retire"
confidence = 0.85
custom_thing = "`[tasks.outdated]` is a 1:1 re-spelling of a native mise command: `run = \"mise outdated\"` with description \"Check for outdated tool versions\"."
evidence = "mise.toml:1208-1210. Dead: `git grep \"mise run outdated\"` across all tracked files returns 0 hits, while every real consumer calls the NATIVE command directly — .github/workflows/ci.yml:135 `run: mise outdated || true`, .claude/skills/tool-currency-check/SKILL.md:48,50,76 and .claude/rules/tool-currency-and-native-first.md:45 all say `mise outdated` / `mise outdated --bump` / `mise outdated hk`, never the task. Control arm: the same `git grep -c \"mise run <name>\"` shape returns 49 files for `sync` and 50 for `lint-docs` and 0 for the invented token `zzqqrare9`, so the 0 for `outdated` discriminates."
native_alternative = "`mise outdated` (the CLI command the task shells out to), including the `--bump`, `--json` and `--inactive` flags the task's fixed `run` string cannot pass through."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/outdated.md:2-27 (`# \\`mise outdated\\``, usage `mise outdated [FLAGS] [TOOL@VERSION]…`, flags `-b --bump`, `-J --json`, `--inactive`)."
rationale = "The task adds nothing over the native command, has zero callers, and actively LOSES capability: `mise run outdated -- --bump` would append after a fixed string rather than being the documented flag path, and the skill that owns this workflow (tool-currency-check) already tells readers to run `mise outdated --bump` directly. Delete the task; the two documented call sites already bypass it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-4"
unit = "mise-tasks-3"
kind = "mise"
disposition = "retire"
confidence = 0.75
custom_thing = "`[tasks.build]` = `docker buildx bake dev-load` — a task whose only action is the exact command this repo's invariants forbid running locally."
evidence = "mise.toml:1200-1202. .claude/rules/do-not.md:13 — \"2. **Do NOT `mise run build` or `docker buildx bake dev-load` locally.** CI-only. Base image is published by `main` workflow.\" AGENTS.md:67 repeats it. No CI caller: `git grep bake -- .github/workflows .github/actions` shows the workflows drive `docker buildx bake dev` / `base` / `p2996-cache` through build-publish.yml, never `mise run build`. The only surviving references are documentation of the ban (do-not.md:13, AGENTS.md:67) plus .claude/skills/devcontainer-workflow/SKILL.md:22 and .agents/skills/devcontainer-workflow/SKILL.md:22, which still advertise it as a usable local step and therefore contradict the ban."
native_alternative = "The CI-published base plus the existing local convergence path: `mise run sync` (mise.toml:928-940, digest-aware buildkit refresh) and `mise run dev-rebuild` (mise.toml:344). For a legitimate CI build the native mechanism is `docker buildx bake` in build-publish.yml, not a mise task."
alternative_evidence = ".claude/rules/do-not.md:13 (the invariant), AGENTS.md:64-70 (\"**CI-only** — never `mise run build` or `docker buildx bake dev-load` locally … Local devcontainer flows pull `:dev` and build only the thin host-user overlay\"), mise.toml:928 `[tasks.sync]`. Native-side doc for the mitigation if it is kept instead: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:335 (`### \\`hide\\``, \"Hide the task from help, completion, and other output like `mise tasks`. Useful for deprecated or internal tasks\")."
rationale = "A named, tab-completable task is an invitation; this one's only effect is a multi-hour local build the project forbids and whose output nothing consumes. Deleting it (and the two SKILL.md:22 lines that advertise it) removes the footgun at the layer that can't be forgotten. If Ray wants the escape hatch kept for a maintainer, the minimum native mitigation is `hide = true` plus `confirm`; that is why this is 0.75 and not higher — the call is a policy one, not a technical one."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-5"
unit = "mise-tasks-3"
kind = "mise"
disposition = "refactor"
confidence = 0.70
custom_thing = "`[tasks.prune]` carries a 99-line inline bash body (49 non-comment lines) that orchestrates the destructive teardown: resolve stem/hash/container-ids/overlay-images, print a plan, `docker rm -f`, `docker volume ls | grep -E` + `xargs docker volume rm`, a second legacy-volume `grep -E` over `$(basename \"$PWD\")-${USER}`, `xargs docker rmi`, `docker builder prune -f`."
evidence = "mise.toml:1072-1172 (`run = '''` at 1074; measured 99 body lines, 49 non-comment). The volume-selection logic is bash regex, not python: mise.toml:1147 `hashed_volumes=\"$(echo \"${all_volumes}\" | grep -E \"^${_stem}\" || true)\"` and mise.toml:1157 the `^dotfiles-$(basename \"$PWD\")-${USER}-(mise-user|cargo-user|rustup-user)$` legacy grep — while the container and image sets were correctly moved into python (`devcontainer teardown` / `teardown-images`, mise.toml:1108-1109). The body's own comment at mise.toml:1113-1118 states the printed plan is \"not a confirmation gate. Nothing pauses between this print and the removals\", and records that this set has been wrong twice (#803: an unscoped tag grep took 6 of 8 sibling clones' overlays)."
native_alternative = "Two native mechanisms, both unused here: (a) mise task `confirm` — `confirm = { message = \"...\", default = \"no\" }` prompts before the task's own `run` executes, which is exactly the missing pause the comment describes; (b) [[zero-bash-logic]]'s own python seam, already half-adopted in this task, for the two remaining `grep -E` volume selections."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:349-366 (`### \\`confirm\\``, \"useful for tasks that are destructive… The user will be prompted to confirm before the task's own `run` command executes\", `confirm = { message = ..., default = \"no\" }`) plus the caveat at :357 that `confirm` does not guard `depends` (irrelevant here — prune has none). For (b): .claude/rules/zero-bash-logic.md and python/src/dotfiles_setup/bash_budget.py:49-50, whose ALLOWLIST scope is `scripts/*.sh` + `.devcontainer/scripts/*.sh` ONLY — so this 99-line body is invisible to the gate that exists to stop exactly this."
rationale = "Native `confirm` covers PART of it (a real pause before a destructive run, one line, default \"no\") but cannot show the RESOLVED plan — its Tera message can only reference usage args, and the container/volume/image sets are computed inside `run`. So: adopt `confirm` for the pause, and move the two volume `grep -E` selections into `devcontainer teardown-volumes` alongside the sibling calls already in python. Not 'retire' — nothing native replaces the teardown orchestration itself."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-6"
unit = "mise-tasks-3"
kind = "mise"
disposition = "refactor"
confidence = 0.70
custom_thing = "`[tasks.verify-arch]` (R3) is ~31 lines of inline bash doing the actual verification: `docker ps -q --filter`, `docker inspect --format` for the image id, `docker image inspect --format` for `.Architecture`, `docker exec … uname -m`, `docker exec … arch`, and three string comparisons with FAIL branches."
evidence = "mise.toml:951-996 (`run = '''` at 964; the docker calls and comparisons at 974-994). Only the triple→arch/machine MAPPINGS were moved to python (mise.toml:966-967 call `dotfiles-setup platform arch|machine`; python/src/dotfiles_setup/platform_target.py:122,771 confirm those exist for this task) — the probe-and-compare logic stayed in the task body."
native_alternative = "This repo's own established seam, already used by the immediately following task: `[tasks.verify-ssh-inbound]` delegates the whole probe to `uv run --project python dotfiles-setup docker verify-ssh-inbound --port … --user …` (mise.toml:1013-1014), implemented at python/src/dotfiles_setup/docker.py:440 `def verify_ssh_inbound(self, port: int, user: str) -> None`. The equivalent is a `docker verify-arch` subcommand."
alternative_evidence = "python/src/dotfiles_setup/docker.py:440 (the sibling R-invariant probe living in python) and mise.toml:1013-1014 (the thin-caller shape); policy: .claude/rules/zero-bash-logic.md (\"Non-trivial logic (environment detection, tool config, validation, orchestration) lives in `python/`\"). Gate-scope evidence that this body escapes enforcement: python/src/dotfiles_setup/bash_budget.py:49-50 (`\"scripts/*.sh\"`, `\".devcontainer/scripts/*.sh\"`)."
rationale = "R1 and R3 are the same kind of check and R1 is already a thin python caller, so this is an internal-consistency gap rather than a missing capability. Consequence is concrete: the three-signal comparison and its FAIL messages have no unit tests today and cannot get any while they live in a TOML string, and the #677 container-selection subtlety recorded in the comment (mise.toml:968-973) is exactly the kind of decision the rule says must be testable."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-7"
unit = "mise-tasks-3"
kind = "mise"
disposition = "refactor"
confidence = 0.60
custom_thing = "`[tasks.verify-secrets]` (S1) is ~25 lines of inline bash: `eval` of a python-emitted env block, file-existence check, `wc -l` count with a `< 1` threshold, and a `for key in … grep -q \"^${key}=\"` canary loop over the doppler env file."
evidence = "mise.toml:1019-1047 (`run = '''` at 1022; `eval \"${_dc_env}\"` at 1027, `envfile=` 1028, `count=$(wc -l < ...)` 1033, the `grep -q` canary loop 1038-1043). Only the PATH resolution was moved to python (mise.toml:1026 `dotfiles-setup devcontainer env`, per the #893 comment at 1024-1025)."
native_alternative = "Same repo seam as finding -6: a `dotfiles-setup docker verify-secrets` (or `devcontainer verify-secrets`) subcommand alongside python/src/dotfiles_setup/docker.py:440's `verify_ssh_inbound`, keeping the task a thin caller."
alternative_evidence = "python/src/dotfiles_setup/docker.py:440 and mise.toml:1013-1014 (the thin-caller precedent); .claude/rules/zero-bash-logic.md (validation logic belongs in `python/`); python/src/dotfiles_setup/bash_budget.py:49-50 (the budget gate cannot see inline task bodies)."
rationale = "Lower confidence than -6 because the body is smaller and the `eval` of the python-emitted block is a deliberate, documented seam. Still a finding on two counts: (a) it is untestable validation logic per [[zero-bash-logic]]; (b) `grep -q` inside a `set -euo pipefail` body is the shape .claude/rules/gh-cli-watch.md and hk's `no_grep_q_under_pipefail` step exist to police — here it is not piped so it is currently safe, but it sits one edit away from the 141 trap. Moving it to python removes both."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-8"
unit = "mise-tasks-3"
kind = "mise"
disposition = "refactor"
confidence = 0.55
custom_thing = "`[tasks.validate]` = `docker buildx bake validate`, described as \"Validate Docker and CI contracts\" — but nothing runs it."
evidence = "mise.toml:1212-1214; the bake target it names is docker-bake.hcl:265-268 (`target \"validate\" { inherits = [\"dev\"]; call = \"check\" }`). Caller sweep: `git grep \"mise run validate\"` returns exactly one hit, docs/research/mintlify-catalog-validation-log.md:542, which is about an unrelated `validate-mintlify` task — i.e. ZERO real callers (control arm: the same command shape returns 18 files for `pin-actions`, 12 for `parity`). It is not an hk step either: hk.pkl:134-138 `docker_bake_check` runs `devcontainer read-configuration`, NOT `bake validate`, while hk.pkl:425-426 asserts docker-bake.hcl is \"already validated by `docker_bake_check` … and `mise run` bake validate\" — a coverage claim no gate delivers."
native_alternative = "buildx's native bake check (`call = \"check\"`, i.e. `docker buildx bake --check`) wired into the lint gate as an hk step next to `docker_bake_check`, per .claude/rules/ci-local-parity.md — rather than an orphan manual task."
alternative_evidence = "docker-bake.hcl:265-268 (the `call = \"check\"` target — the native mechanism is already declared); hk.pkl:134-138 (the adjacent hk step that DOES run, showing the wiring shape); .claude/rules/ci-local-parity.md (\"Every CI lint step has a local hk equivalent\")."
rationale = "Either wire it (hk step / CI) so hk.pkl:425-426's claim becomes true, or retire it as dead. I do not pick between those without a probe because `call = \"check\"` may need a docker daemon and named contexts that the lint job lacks — which would explain why it was left manual and would make retire-plus-fix-the-comment the right answer instead."
probe = "docker buildx bake validate > /tmp/bake-validate.log 2>&1; echo \"rc=$?\" >> /tmp/bake-validate.log; then compare its findings against `devcontainer read-configuration --workspace-folder .` to see whether it reports anything hk's docker_bake_check does not."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-9"
unit = "mise-tasks-3"
kind = "mise"
disposition = "keep"
confidence = 0.90
custom_thing = "`[tasks.stop]` (alias `down`) — inline `docker rm -f` teardown, justified by a comment asserting the devcontainer CLI has no `down` verb \"in v0.85.0\", verified 2026-04-06."
evidence = "mise.toml:1174-1198; the version claim is at mise.toml:1176-1178 (\"devcontainer CLI v0.85.0 has no 'down' verb (verified 2026-04-06 via `devcontainer --help`; verbs: up, set-up, build, run-user-commands, read-configuration, outdated, upgrade, features, templates, exec)\"). The repo now pins npm:@devcontainers/cli 0.89.0, so the cited probe is four minor versions stale."
native_alternative = "None — re-probed at the CURRENT pin: @devcontainers/cli 0.89.0 still exposes only `up, set-up, build, run-user-commands, read-configuration, outdated, upgrade, features, templates, exec`. There is no `down`, `stop`, `rm` or `prune` verb, so lifecycle teardown has no official-CLI path and `docker rm -f` remains the only route."
alternative_evidence = ".agent/kb/raw/devcontainer-cli-0.89.0-help.md (saved `devcontainer --help` capture at the pinned 0.89.0, command list under `## devcontainer --help`; control arm: the same capture DOES list `exec` and `outdated`, whose `--help` bodies are included, so the absence of `down` is a real absence and not a truncated capture)."
rationale = "Justified keep: the capability genuinely does not exist upstream at the pinned version, and this repo's do-not.md #3 bans raw docker for lifecycle EXCEPT where the CLI has no verb — this is that exception, and the container-selection DECISION is already delegated to python (`dotfiles-setup devcontainer teardown`, mise.toml:1185). One doc-only follow-up: the comment's \"v0.85.0 … verified 2026-04-06\" should be restamped to 0.89.0 / 2026-09-09 so the next reader does not have to re-derive what I just re-derived."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-10"
unit = "mise-tasks-3"
kind = "mise"
disposition = "keep"
confidence = 0.85
custom_thing = "The thin-caller majority of this unit: `automerge` (mise.toml:911), `session-state` (916), `plan-attest` (921), `handoff-check` (926), `sync` (940), `verify-container-latest` (949), `parity` (1227), `eval` (1245), `hk-audit` (1249), `codex-agent-parity` (1255) each delegate to a `dotfiles-setup` subcommand; `pin-actions` (1218) and `lint-docs` (1260) are one-line calls to the pinned native binaries `pinact run --verify` and `agnix . --strict`."
evidence = "mise.toml:902-949, 1216-1260 — every one is a single `run = \"uv run --project python dotfiles-setup <verb>\"` or a single pinned-binary invocation, with the logic in python/src/dotfiles_setup/{pr,session_state,plan_attest,handoff_check,sync,container,parity,eval_cases,hk_builtins_audit,codex_agent_parity}.py. All are live: `git grep -c \"mise run <name>\"` gives automerge 18 files, sync 49, verify-container-latest 7, parity 12, eval 8, hk-audit 10, codex-agent-parity 5, pin-actions 18, lint-docs 50, session-state 8, plan-attest 8, handoff-check 6 — against 0 for the invented control token `zzqqrare9`."
native_alternative = "None. mise's task surface (task-configuration.md's full key list: run/depends/wait_for/depends_post/env/vars/tools/dir/hide/confirm/raw/sources/outputs/cache/shell/timeout/quiet/silent/output/usage/deny_*) provides scheduling, caching, confirmation and bounding — it provides no substitute for the DOMAIN logic these verbs carry (PR provenance gating, registry-vs-local digest comparison, cross-repo parity assertion, plan attestation)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:10-1608 (the exhaustive key list; every `### \\`key\\`` heading enumerated — no key expresses domain logic), and the mise gap digest .agent/kb/raw/mise-releases-since-pin.digest.md (\"ZERO releases strictly newer than the baseline\", host already on 2026.9.3) confirming no post-corpus task feature exists to reconsider."
rationale = "Required justification: these are already the shape the audit is trying to reach — a mise task as a named seam over a tested python module or a pinned binary, with zero inline logic. Retiring them would not delete code, it would only delete the discoverable entrypoint and push callers back to raw `uv run` / raw `gh` invocations, which is precisely what .claude/rules/mise-tasks-only.md's PreToolUse guard exists to prevent. `pin-actions` and `lint-docs` are one-line native-tool calls whose only added value is CI/local parity (hk.pkl:419-424 explains why pinact is a task and not an hk step) — that is a real reason, not inertia."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-3-11"
unit = "mise-tasks-3"
kind = "mise"
disposition = "unknown"
confidence = 0.40
custom_thing = "`[tasks.hk-audit]` regenerates a tracked artifact (docs/hk-builtins-audit.md) from `hk builtins` + the three pkl configs on every invocation, with no up-to-date check."
evidence = "mise.toml:1247-1249 (`description = \"Regenerate docs/hk-builtins-audit.md from \\`hk builtins\\` + the hk configs\"`, `run = \"uv run --project python dotfiles-setup hk-builtins-audit\"`); no `sources`/`outputs` keys in the block. The repo already uses those keys elsewhere: mise.toml:199-200 on `[tasks.\"deps:python\"]`."
native_alternative = "mise `sources` / `outputs` — \"If you want to skip executing a task if certain files haven't changed (up-to-date), you should specify `sources` and `outputs`\" — e.g. sources = [\"hk.pkl\", \"hk-common.pkl\", \"hk-image.pkl\", \"mise.toml\"], outputs = [\"docs/hk-builtins-audit.md\"]."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:431 (`### \\`sources\\``) and :586 (`### \\`outputs\\``), summarised at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md:186-196; in-repo precedent mise.toml:199-200."
rationale = "The generator shape is exactly what `sources`/`outputs` is for, but a WRONG skip is worse than a redundant regen here: the output also depends on the `hk` BINARY's builtin set, which no file in `sources` tracks directly (the hk pin lives in .config/mise/conf.d/shared.toml, not hk.pkl), so a hk bump could leave a silently stale tracked doc. Settle it before adopting."
probe = "uv run --project python -c \"import inspect, dotfiles_setup.hk_builtins_audit as m; print(inspect.getsource(m))\" | grep -n \"open(\\|read_text\\|subprocess\\|hk \" — enumerate every input the generator actually reads (files AND the hk binary), then decide whether the shared.toml hk pin must join `sources`; confirm with a two-arm test: touch hk.pkl -> task runs; bump the hk pin only -> task must still run."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-4-1"
unit = "mise-tasks-4"
kind = "mise"
disposition = "unknown"
confidence = 0.50
custom_thing = "The `lock` task's documented workaround for a stale lockfile entry: after editing an exact pin you must hand-delete the `[[tools.X]]` block from `mise.lock` before `mise run lock -- <tool>` will re-resolve it. The task shells `[\"mise\", \"lock\", tool]` with no format flag."
evidence = "mise.toml:1285-1298 (`[tasks.lock]`); python/src/dotfiles_setup/lock_integrity.py:292-296 (`subprocess.run([\"mise\", \"lock\", tool], ...)`, no `--bump`, no `--upgrade`); the workaround is recorded in the auto-memory hook `feedback_mise_lock_reuses_locked_version` (\"DELETE the `[[tools.X]]` block from `mise.lock` first, then re-lock\"). Both lockfiles are format 0: neither `mise.lock` nor `.config/mise/mise.lock` carries a top-level format/version field (head of each file shows only the `# @generated` banner, then data tables)."
native_alternative = "`mise lock --upgrade` — \"Upgrade legacy lockfiles to the latest format. Existing unversioned lockfiles use format version 0 and are otherwise preserved to avoid unexpected lockfile drift. This flag upgrades them to the latest format with request-specific version bindings.\" A request-specific version binding is exactly the missing link that makes a changed config pin invalidate its locked entry."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/lock.md:58-64; confirmed present at the host's live version in .agent/kb/raw/mise-lock-help-2026.9.3.md:84-92 (`mise lock --help`, mise 2026.9.3 macos-arm64)."
rationale = "The flag's stated purpose maps onto the repo's known trap, but I have NOT proven that an upgraded-format lockfile actually re-resolves on a pin edit — and the flag carries a hazard that makes a blind adoption dangerous: `--upgrade` 'always process[es] every configured tool and cannot be combined with tool arguments', i.e. it is the WHOLE-FILE form that is destructive on this macOS host (#370: conda entries 962 -> 427, linux-x64 628 -> 80, silent, invisible to `--dry-run`). So the probe must run against a scratch copy or inside the amd64 devcontainer, never against the tracked lockfile on the host. Related but NOT a live defect: `lock` also omits `--bump` while its two siblings pass it (image_lock.py:275, lock_refresh.py:267); `--bump` only re-resolves FUZZY selectors and this repo has zero of them (grep for `= \"latest\"`/`= \"lts\"` in mise.toml + shared.toml -> 0 hits; control arm, same command shape, `= \"[0-9]` -> 38 hits), so adding it today would be a no-op."
probe = "cp mise.lock /tmp/probe.lock && cd $(mktemp -d) && git -C /Users/rmanaloto/dev/github/ray-manaloto/dotfiles archive HEAD mise.toml mise.lock .config | tar -x && mise lock --upgrade --dry-run 2>&1 | head -40   # then, in the amd64 devcontainer on the scratch copy: bump one exact pin in mise.toml, run `mise lock --upgrade`, edit the pin again, run `mise lock <tool>` and diff the [[tools.X]] version field — it must track the config pin without a manual block deletion. Control arm: repeat on a format-0 copy and confirm the stale version survives."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-4-2"
unit = "mise-tasks-4"
kind = "mise"
disposition = "refactor"
confidence = 0.60
custom_thing = "Six tasks in this unit can block indefinitely on the network or on a devcontainer round-trip and declare no bound: `lock` (`mise lock` -> registry), `lock-refresh-root`, `lock-shared` (devcontainer exec + asset resolution), `lock-image` (routes into the amd64 devcontainer, multi-minute), `p2996-refresh` (GitHub API), `schema-vendor-refresh` (schema downloads)."
evidence = "mise.toml:1285-1298, 1278-1284, 1299-1317, 1331-1349, 1354-1357, 1362-1365 — none of these `[tasks.*]` blocks carries a `timeout` key (grep `^timeout` over mise.toml returns exactly 1 hit, mise.toml:213, which is `[tasks.lint]`). `.github/workflows/refresh.yml:509` runs `mise run schema-vendor-refresh` unattended in CI."
native_alternative = "mise's per-task `timeout` option (`\"timeout\": {\"description\": \"timeout for this task\", \"type\": \"string\"}`), the same mechanism the repo already uses as an outer backstop above a Python wrapper."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/schema/mise.json:3416-3419 (per-task `timeout`); note the global `task_timeout` setting at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/schema/mise.json:2054-2058 is marked `\"deprecated\": true`, so the per-task field is the supported surface. In-repo precedent: mise.toml:207-213 (`[tasks.lint] timeout = \"700s\"`, described as \"native outer backstop ABOVE the wrapper's 600s\")."
rationale = "Nothing custom is superseded here — there is no hand-rolled timeout to delete — but `.claude/rules/long-running-command-hangs.md` requires every network/IO-bound command to carry a hard bound, and the lint task already shows the accepted shape for satisfying that natively rather than by adding Python. Adopting `timeout` on these six is additive and cheap. Two exclusions are deliberate, not oversights: `cc` (mise.toml:1366-1414) is `raw = true` and launches an interactive tmux session that is expected to outlive any bound, and `session-review-gate` + the two mutation sentinels already carry their bounds inside the attestation runner (python/src/dotfiles_setup/session_gate.py:23 `DEFAULT_TIMEOUT = 600`, :292 `timeout=120`), where the rc and the digest have to be recorded — a mise-level kill would destroy the receipt those tasks exist to produce."
probe = "Set a deliberately tiny bound and confirm it fires loud rather than silently: `mise run lock-image` with `timeout = \"1s\"` temporarily added must exit non-zero naming the timeout; control arm, the same task with `timeout = \"3600s\"` must reach its normal first output line."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-4-3"
unit = "mise-tasks-4"
kind = "mise"
disposition = "refactor"
confidence = 0.55
custom_thing = "Five machine-invoked internal tasks are listed in `mise tasks` alongside the human-facing verbs, with nothing marking them as internals: `session-review-gate`, `session-review-mutation-credential-launcher`, `session-review-mutation-git-hook-contamination`, `session-review-focused-gate`, `skillopt-provenance-check`. The mutation/gate three are invoked only by the attestation runner, which hard-codes their names so the argv is not caller-controlled."
evidence = "mise.toml:1506-1525 (the five task blocks); python/src/dotfiles_setup/session_gate.py:64-84 (`PREVENTIONS` binds `session-review-mutation-credential-launcher` / `session-review-mutation-git-hook-contamination` / `session-review-focused-gate` as `mutation_task`/`gate_task`) and :290, :401-402 (`[\"mise\", \"run\", registration.gate_task]`, `[\"mise\", \"run\", registration.mutation_task]`)."
native_alternative = "mise's per-task `hide` option — `\"hide\": {\"default\": false, \"description\": \"do not display this task\", \"type\": \"boolean\"}`. A hidden task is still runnable by name, which is all `session_gate.py` needs; it simply stops appearing in the task list/selector."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/schema/mise.json:3230-3234; the same block at :3491-3495 shows `hide`/`quiet`/`raw` as siblings in the resolved task defaults."
rationale = "`hide` is used nowhere in this repo (grep `^hide` over mise.toml -> 0; control arm, same command shape: `^quiet` -> 1 hit at mise.toml:1276, `^raw = true` -> 1 hit at mise.toml:1371, so the probe can see these keys when they are present). The cost of not using it is that the task list — the surface `.claude/rules/mise-tasks-only.md` trains agents to shop from — mixes four sentinels an agent must never invoke by hand with the verbs it should. This is presentation only: it changes no behaviour and cannot break the runner, which addresses the tasks by name."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-4-4"
unit = "mise-tasks-4"
kind = "mise"
disposition = "retire"
confidence = 0.55
custom_thing = "`[tasks.skillopt-provenance-check]` — 'Verify SkillOpt GitHub objects and three present-day hostile replays'. It has no caller anywhere in the repo outside its own definition."
evidence = "mise.toml:1518-1521. A whole-worktree fixed-string search for `skillopt-provenance-check` (excluding `.git/`) returns exactly two files: `mise.toml` itself and `python/src/dotfiles_setup/skillopt_provenance.py:395`, where the string is only a log message (`LOGGER.log(logging.ERROR, \"skillopt-provenance-check failed\")`). No skill, rule, workflow, hook, or `python/verification/suites.toml` entry names it. Control arm, same command shape and search space: `mise run token-check` returns `mise.toml`, `.claude/skills/token-check/SKILL.md`, `.agents/skills/token-check/SKILL.md` and six report files — so the probe does find task callers when they exist."
native_alternative = "None needed — the module the task fronts is already exercised on a live path: `tests/test_skillopt_provenance.py` runs in CI's `contract-preflight` pytest step (`.github/workflows/ci.yml:186` names `skillopt_provenance` among the merge-base-resolving tests that require `fetch-depth: 0`), and its coverage is catalogued at `tests/TEST-INDEX.md:78`."
alternative_evidence = ".github/workflows/ci.yml:185-186; tests/TEST-INDEX.md:78; tests/test_skillopt_provenance.py:12."
rationale = "This is the dead-task limb of the audit question, not a native-supersession one: the task is an unreferenced manual entry point whose logic already has a wired, gated caller through pytest. Retiring it removes a runnable verb nothing documents and nothing invokes. The residual doubt — and why this is 0.55 rather than higher — is that the task runs the module's live GitHub provenance replay, which the pytest path may exercise differently from a bare `python -m` run; if an operator genuinely needs that on demand, the correct outcome is to give it a skill/rule citation (making it non-dead) rather than to leave it undocumented. Either resolution is fine; the current state — runnable, unreferenced, unexplained — is not."
probe = "uv run --project python pytest tests/test_skillopt_provenance.py -q  # then compare its assertions against `python -m dotfiles_setup.skillopt_provenance` (main at skillopt_provenance.py:~380-400): if the test path covers every replay the module's __main__ runs, retire the task; if it does not, wire the task into a rule/skill instead."
correction = "**⚠ CITATION CORRECTED (round 2, re-verified round 5):** the CI pytest step is `- name: Run pytest` at `.github/workflows/ci.yml:227`, with `run: uv run --project python pytest tests/ -q` at `:237`; the cited `:185-186` is a comment inside the job's `actions/checkout` block (`# Full history, NOT the default shallow clone:`). Re-probed with `sed -n '185,186p;227p;237p'`; control arm `sed -n '99991p'` returns nothing, so the probe discriminates. Independently verified: `pytest.ini:15` is `addopts = -m \"not image_exec and not codex_exec\"` and the test file carries neither marker, so it is not deselected. **BUT** the `coverage` lens refuted this row on substance (every test injects a fake fetcher, so the pytest path never exercises the live GitHub replay the task runs) — see “Surviving retire rows with a dissenting lens” below. Run the row's own probe before acting on it."
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-4-5"
unit = "mise-tasks-4"
kind = "mise"
disposition = "keep"
confidence = 0.80
custom_thing = "Four tasks refuse their unsafe form in Python rather than prompting: `lock` errors out on a bare/undeclared invocation, `lock-image` refuses to run on macOS unless it can route into the container, `reap` is dry-run unless `--kill`, and the two `*-refresh` tasks write/download unconditionally."
evidence = "mise.toml:1285-1298 (`lock`), 1331-1349 (`lock-image`), 1538-1551 (`reap`: \"DRY RUN unless --kill\"), 1354-1357 and 1362-1365 (`p2996-refresh`, `schema-vendor-refresh`); python/src/dotfiles_setup/lock_integrity.py:270-291 (the two refusal branches, each returning 1 before shelling out)."
native_alternative = "mise's per-task `confirm` option — a confirmation message shown before the task runs."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/schema/mise.json:3138-3147; documented at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md:199-209. Its behaviour off a TTY is the disqualifier: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/ui/prompt.rs:78-80 returns `Confirmation::Unavailable` when stderr is not user-attended, and :87-91 routes a non-terminal stdin through `read_confirm_from_stdin` precisely so that \"no answer means no\"."
rationale = "Required justification, stated rather than defaulted: `confirm` is the nearest native mechanism and it is the wrong one here. These refusals must hold in non-interactive contexts — `.github/workflows/refresh.yml:509` runs `mise run schema-vendor-refresh` on a runner with no TTY, and `session_gate.py` drives `mise run` from a Python subprocess — where `confirm` either degrades to `Unavailable` or reads EOF as a refusal, i.e. it would turn a CI-safe task into a task CI cannot run. It also answers a different question: `confirm` gates the whole task on a human, while the Python guards discriminate WHICH argv is safe (a named backend-qualified tool is fine, a bare `mise lock` is destructive) and then verify the artifact afterwards, because `mise lock` exits 0 having done nothing for a name it does not recognise (lock_integrity.py:277-291) — an exit code `confirm` cannot repair."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-4-6"
unit = "mise-tasks-4"
kind = "mise"
disposition = "refactor"
confidence = 0.45
custom_thing = "Platform sets are derived and passed imperatively from Python on every image/host lock path: `image_lock.py` builds `--platform` argv, and `lock_integrity.py` passes none at all, relying on whatever platforms already exist in the lockfile."
evidence = "python/src/dotfiles_setup/image_lock.py:275-278 (`argv = [str(mise_bin), \"lock\", \"--bump\"]`, then `argv += [\"--platform\", name]`) and :398 (`extra = tuple(arg for name in platforms for arg in (\"--platform\", name))`); python/src/dotfiles_setup/lock_integrity.py:292-296 (no platform argument); tasks at mise.toml:1331-1349 (`lock-image`) and mise.toml:1285-1298 (`lock`)."
native_alternative = "The `lockfile_platforms` setting: per the live `mise lock --help`, \"Otherwise mise uses `lockfile_platforms` plus the current platform when that setting is configured, existing lockfile platforms when available, or the common platform defaults for a new lockfile.\" Declaring it in the root config would make the platform set a reviewable declaration instead of a value computed at call time."
alternative_evidence = ".agent/kb/raw/mise-lock-help-2026.9.3.md:9-11 (saved live `mise lock --help`, mise 2026.9.3); in-repo precedent that the setting is already trusted for the image config: .devcontainer/mise-system.toml:319 (`lockfile_platforms = [\"linux-x64\", \"linux-arm64\"]`). The setting is NOT in the offline docs snapshot (/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/configuration/settings.md -> 0 hits), which is why the saved live help is the citation."
rationale = "Partial coverage only, and the boundary matters. `lockfile_platforms` could retire the platform-derivation half of `lock`/`lock-image` — the root `mise.toml` and `.config/mise/conf.d/shared.toml` declare it nowhere, so the host paths currently depend on 'existing lockfile platforms', which is a property of the artifact rather than of the config. It CANNOT retire `lock-shared`: that task exists because the RESOLVING host, not the target platform, picks the release asset (mise on macOS wrote a gnu tarball into the linux-x64 entry where linux mise resolves musl — python/src/dotfiles_setup/lock_shared.py:20-40), and no platform declaration changes which host resolves. Confidence is held at 0.45 because I have not shown mise's derived set equals the set `image_lock.py` computes."
probe = "Add `lockfile_platforms` to mise.toml's `[settings]`, then compare: `mise lock --dry-run --json` (derived) against `mise lock --dry-run --json --platform <each platform image_lock.py derives>`. The two must name the same platform set. Run inside the amd64 devcontainer, never on the macOS host — a whole-file lock there is destructive (#370)."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mise-tasks-4-7"
unit = "mise-tasks-4"
kind = "mise"
disposition = "refactor"
confidence = 0.50
custom_thing = "Four tasks bypass the packaged `dotfiles-setup` console-script dispatch and invoke a module directly: `session-review-gate`, the two `session-review-mutation-*` sentinels, and `skillopt-provenance-check` all run `uv run --project python python -m dotfiles_setup.<module>`, while the other sixteen tasks in this unit run `uv run --project python dotfiles-setup <verb>`."
evidence = "mise.toml:1506-1521 (the four `python -m` bodies) versus mise.toml:1276 (`dotfiles-setup mise-config-context`), :1284, :1298, :1317, :1330, :1349, :1353, :1357, :1361, :1365, :1494, :1537, :1551. Neither module is registered in the CLI: a grep of `python/src/dotfiles_setup/main.py` for `skillopt` and `session_gate` returns nothing, while the same grep shape returns registrations for `session-review` (main.py:589), `token-audit` (:625), `reap` (:538-539), `lint-delta` (:516), `schema-vendor` (:1355), `p2996-hash` (:1706) and `p2996-refresh` (:1746)."
native_alternative = "The project's own packaged entry point — the `dotfiles-setup` console script whose subcommand table lives in `python/src/dotfiles_setup/main.py` — which is the seam `.claude/rules/agent-artifact-conventions.md` rule 6 prescribes (skill -> mise task -> python library, reusable by parameter)."
alternative_evidence = "python/src/dotfiles_setup/main.py:516 (`\"lint-delta\"`), :538-539 (`reap`), :589 (`\"session-review\"`), :625 (`\"token-audit\"`), :1355 (`\"schema-vendor\"`), :1706/:1746 (`p2996-*`) — the registration pattern the four outliers skip. Rule text: .claude/rules/agent-artifact-conventions.md (\"skill -> mise task -> python library\")."
rationale = "Not a native-mise question and not a defect today — `python -m` works — but it is a real structural inconsistency in exactly the layer this audit is about. Two costs: an agent reading the task list cannot discover these verbs through `dotfiles-setup --help` the way it can every sibling, and the argv shape differs between the tasks the attestation runner drives and the tasks a human drives, which is one more thing that has to be right in two places. The counter-argument to weigh before moving them, and why this is 0.5: `session_gate.py` deliberately hard-codes non-caller-controlled argv (session_gate.py:64-84), and routing through a dispatcher adds a layer between the recorded command and what executes — that is a property the attestation may want to keep."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mise-tasks-4-8"
unit = "mise-tasks-4"
kind = "mise"
disposition = "keep"
confidence = 0.85
custom_thing = "The unit as a whole: all twenty tasks are one-line thin wrappers around `python/src/dotfiles_setup` (or `kb_setup`) — zero inline shell logic, zero hand-rolled command sequences. Seven of them front capabilities mise has no equivalent for at all: `reap` (process reaper with ancestor-chain protection), `lint-delta` (runs the previous pinned linter version as a control arm), `token-check` (contract-token uniqueness), `p2996-hash` (content hash of Docker build inputs), `schema-vendor-check`/`-refresh` (vendored JSON-Schema drift), `session-review`, and `cc` (verified-environment launcher)."
evidence = "mise.toml:1262-1551 — every `run =` in the unit is a single `uv run --project python ...` line; the only non-`run` task keys used are `quiet` (mise.toml:1276), `raw` (:1371) and `usage` (:1499-1502), all three native mise task options used correctly. `session-requirements` (mise.toml:1495-1505) already uses the recommended native `usage` spec with `${usage_*}` templating rather than positional Tera args."
native_alternative = "None for these seven. The mise task options that could in principle apply were each checked and do not fit: `depends`/`depends_post` (no task in this unit fans out); `sources`/`outputs` (up-to-date skipping is wrong for tasks whose entire job is to PRINT a value or RE-CHECK drift — `p2996-hash` must emit its hash on every call, `schema-vendor-check` must compare on every call); `mise watch` (no file-watching workflow here); `mise doctor`/`mise sync` (unrelated surfaces — `mise sync` imports from other version managers, and there is no `mise tool-sync` in this version's CLI at all)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md:154-166 (`depends`), :183-195 (`sources`/`outputs`), :458-475 (the recommended `usage` field, which `session-requirements` follows); the CLI surface at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/ contains `sync.md`, `doctor.md` and `watch.md` but no `tool-sync.md` (directory listing, control-armed: `lock.md` is present in the same listing and is the file every lock finding above cites)."
rationale = "Required justification rather than a default: the unit is already in the shape `.claude/rules/zero-bash-logic.md` and `.claude/rules/mise-tasks-only.md` prescribe — the task is the seam, the library holds the mechanics, and the two tasks that DO use a native task option (`raw` on `cc`, `quiet` on `mise-config-context`) each carry an in-file measurement explaining why (mise.toml:1367-1371: without `raw` the child gets no TTY and tmux dies; mise.toml:1268-1275: mise's banner goes to stderr so stdout stays a clean JSON hook channel). Nothing native in mise 2026.9.3 supersedes a process reaper, a linter-version differ, a contract-token checker, a Docker-input content hash, a schema-drift checker, a transcript miner, or an environment-verifying launcher. The four narrower opportunities that DO exist are filed separately above (timeout, hide, lockfile format, lockfile_platforms); none of them retires a line of this repo's custom code."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-__init__-1"
unit = "mod-__init__"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "_project_root() — a 4-line __file__-relative repo-root resolver (Path(__file__).parent x4), the package's single public capability. Consumed at ~20 call sites: verify.py:40,126,357,572,635,654; image.py:80,98,264,305,322,1140,1465,2083,2097,2131; schema_vendor.py:123,141,182,355; parity.py:204; and tests/test_image_smoke_exec.py:35,188."
evidence = "python/src/dotfiles_setup/__init__.py:7-13"
native_alternative = "Four native project-root mechanisms were evaluated and each fails to preserve the semantics: (a) mise's MISE_PROJECT_ROOT task env var; (b) mise hook env MISE_PROJECT_ROOT; (c) Claude Code's CLAUDE_PROJECT_DIR; (d) git rev-parse --show-toplevel (already the mechanism branch_guard.py:177 uses)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/index.md:56 ('MISE_PROJECT_ROOT: The root of the project that defines the task ... stable regardless of the directory the task is invoked from'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/hooks.md:173; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:593 ('${CLAUDE_PROJECT_DIR}: the project root where the session started'); python/src/dotfiles_setup/branch_guard.py:177 (git rev-parse --show-toplevel, in-repo precedent)."
rationale = "Required justification: every native candidate resolves the AMBIENT project/session/checkout, whereas this helper must resolve the repo THIS PACKAGE'S SOURCE LIVES IN — the two diverge whenever the CLI is invoked from a sibling repo, which is exactly the C1b/#343 failure class the sibling resolver's own docstring guards against (python/src/dotfiles_setup/instructions_observer.py:90-96: 'hooks run in the session's CURRENT directory, not the project root ... never to Path.cwd()'). Control-armed probe of the strongest candidate: inside `mise run <task>` MISE_PROJECT_ROOT was SET (printed the scratch project dir), and under `mise exec --` in the same directory it was UNSET (empty) — so the var is task-scoped only, and is absent for the pytest call site (tests/test_image_smoke_exec.py:35,188) and for a bare `uv run --project python dotfiles-setup ...` (probed: MISE_PROJECT_ROOT in env = False). CLAUDE_PROJECT_DIR is hook-scoped and is already adopted where it is correct (instructions_observer.py:105, instructions_report.py:107); no `_project_root()` consumer is reachable from a hook entrypoint (control-armed: hook_guard.py / branch_guard.py / script_guard.py import none of verify, image, parity, schema_vendor — empty result against a grep shape that returns hits elsewhere). git rev-parse would cost a subprocess at each of ~20 call sites and fails outright for a package installed outside a checkout. The mise gap digest confirms no newer mise capability applies (.agent/kb/raw/mise-releases-since-pin.digest.md: host 2026.9.3 is the newest release, zero new capabilities), and neither Claude Code digest mentions a project-dir capability. Nothing native supersedes this; it stays."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-__init__-2"
unit = "mod-__init__"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "The canonical helper is BYPASSED rather than imported: main.py:2553 re-implements the identical expression byte-for-byte (`project_root = Path(__file__).parent.parent.parent.parent`) instead of `from dotfiles_setup import _project_root`, and four further siblings carry their own private copy of the same 4-level walk. Seven independent derivations of one concept exist in the package."
evidence = "python/src/dotfiles_setup/__init__.py:13 (canonical); python/src/dotfiles_setup/main.py:2553 (byte-identical duplicate, no import); python/src/dotfiles_setup/lint.py:82; python/src/dotfiles_setup/instructions_observer.py:105; python/src/dotfiles_setup/instructions_report.py:107; python/src/dotfiles_setup/script_guard.py:86. lint.py:77 even documents the duplication as intentional ('by the same convention `main.py` uses for `project_root`') — i.e. the convention is copied, not shared."
native_alternative = "ast-grep structural linting: an `sgconfig.yml` with a `ruleDirs` entry expressing a python rule that forbids a bare `Path(__file__).parent.parent.parent.parent` outside `__init__.py`, wired as an hk step. ast-grep is ALREADY PINNED in this repo but wired into nothing."
alternative_evidence = "mise.toml:47 (`\"github:ast-grep/ast-grep\" = \"0.45.3\"` — pinned); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/.github/copilot-instructions.md:70 (`cat > sgconfig.yml`, the rule-config entrypoint) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/CHANGELOG.md:564 ('allow sgconfig.yml to not have required ruleDirs field')."
rationale = "This is a genuine adopt-a-native-mechanism gap, not a style nit: the repo pins a structural linter that can express exactly this invariant and does not use it. Control-armed: `grep -n 'ast-grep|ast_grep' hk.pkl hk-common.pkl mise.toml` returns ONLY the mise.toml:47 pin — zero hk steps — while `grep -c Step hk.pkl` returns 8, so the empty hk result is a real absence, not a broken grep. The consolidation itself is cheap (main.py:2553 imports the canonical helper; the four `.resolve()`-ing siblings collapse onto one shared definition parameterised by whether the CLAUDE_PROJECT_DIR override applies), and the gate is what stops the eighth copy landing. Confidence is not 1.0 because the two env-first variants (instructions_observer.py:105, instructions_report.py:107) legitimately differ in behaviour and must remain a distinct function, so this is a partial consolidation plus a rule, not a single deletion."
probe = "mise x -- ast-grep --version && printf 'id: no-adhoc-project-root\\nlanguage: python\\nrule:\\n  pattern: Path(__file__).parent.parent.parent.parent\\n' > /tmp/r.yml && mise x -- ast-grep scan --rule /tmp/r.yml python/src/dotfiles_setup/ ; # expect 3 hits (__init__.py:13, main.py:2553) plus the .resolve() variants NOT matching — confirms the rule discriminates before wiring it into hk.pkl"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-__init__-3"
unit = "mod-__init__"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "_project_root() omits `.resolve()` while four of its five siblings call it, so the canonical resolver returns a possibly-unresolved path where lint.py / script_guard.py / instructions_observer.py / instructions_report.py return a symlink-resolved one."
evidence = "python/src/dotfiles_setup/__init__.py:13 (`return Path(__file__).parent.parent.parent.parent` — no `.resolve()`); contrast python/src/dotfiles_setup/lint.py:82, script_guard.py:86, instructions_observer.py:105, instructions_report.py:107 (all `Path(__file__).resolve().parent.parent.parent.parent`)."
native_alternative = "None — no Claude Code, mise, hk, uv, gh, renovate, graphify, pytest or linter feature normalises this; it is the stdlib `pathlib.Path.resolve()` call the siblings already make, and the only enforcement mechanism available is the same ast-grep rule proposed in mod-__init__-2."
alternative_evidence = "mise.toml:47 (ast-grep pin, the sole available enforcement surface); no doc citation exists for a native normaliser because none was found."
rationale = "Reported at reduced confidence because the divergence is LATENT, not live, and I armed that claim rather than asserting it. Probe on this host: `_project_root()` returned /Users/rmanaloto/dev/github/ray-manaloto/dotfiles and `str(r) != str(r.resolve())` was False — the two agree today. The one path where a mismatch would be load-bearing is already absorbed downstream: devcontainer_names.py:304 hashes `resolved_workspace`, not the caller's raw path, so `workspace_hash` cannot fork on it. A prior cold review recorded the same divergence as INFORMATIONAL (docs/research/kb/reports/agents/2026-09-01-cold-review-895b.md:99 — '`_default_project_root()` and `main.py`'s `project_root` are written differently but agree in practice'). It becomes real only under a symlinked checkout or a non-editable install, so the correct action is to fold `.resolve()` in during the mod-__init__-2 consolidation rather than to ship a standalone change."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ai-1"
unit = "mod-ai"
kind = "module"
disposition = "retire"
confidence = 0.85
custom_thing = "AIOrchestrator.ensure_ai_clis() hand-rolls a PATH presence check for the AI CLIs by shelling out to a login shell per tool: subprocess `[\"bash\", \"-lc\", f\"command -v {tool}\"]` for claude/codex/gemini, relying on ToolManager.run_command's check=True -> SystemExit to signal absence."
evidence = "python/src/dotfiles_setup/ai.py:20-30 (loop over (\"claude\",\"codex\",\"gemini\") calling self.tool_manager.run_command([\"bash\",\"-lc\",f\"command -v {tool}\"], capture=False)); python/src/dotfiles_setup/audit.py:40-80 (run_command raises SystemExit on non-zero / FileNotFoundError). The tools it asserts are declared only in the IMAGE tier: .devcontainer/mise-runtime.toml:65 (claude-code) and :66 (npm:@google/gemini-cli); neither is declared in mise.toml or .config/mise/conf.d/shared.toml."
native_alternative = "mise's own tool-resolution commands: `mise which <bin>` (resolves the bin path AND reports when a bin is a mise tool that is not active in this directory) and `mise ls --missing` (lists tool versions declared in config but not installed); `mise doctor` reports uninstalled/incomplete tools."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/which.md:8 (\"Shows the path that a tool's bin points to.\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/ls.md:26 (\"-m --missing — Display missing tool versions\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor.md:9,22-24 (\"Check mise installation for possible problems\" / \"[WARN] plugin node is not installed\"). Digest .agent/kb/raw/mise-releases-since-pin.digest.md confirms the host mise 2026.9.3 IS the newest release, so the corpus docs are current for this pin."
rationale = "The custom check is both redundant and strictly WEAKER than the native one, and it is a bash shell-out inside python (the shape .claude/rules/zero-bash-logic.md exists to remove). Measured in this repo directory: `command -v claude` succeeds (a mise shim is on PATH) while `mise which claude` returns `ERROR claude is a mise bin however it is not currently active` — i.e. the custom probe passes on a tool mise itself reports as not active here, exactly the false-negative class .claude/rules/probes-need-a-control-arm.md warns about. Control arm for the probe: the same loop over codex/claude/gemini/omx returned PRESENT for all four, and `mise which gemini` resolved to /Users/rmanaloto/.local/share/mise/installs/gemini/0.59.0/bin/gemini, so the shell probe can return both answers — it is discriminating for 'on PATH', just not for 'declared and active', which is the property the repo actually cares about. Retire rather than refactor because the only caller is itself dead (mod-ai-3); if the capability is ever wanted again it is one `mise ls --missing` in a mise task, not a python class."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ai-2"
unit = "mod-ai"
kind = "module"
disposition = "retire"
confidence = 0.80
custom_thing = "AIOrchestrator.setup_omx() invokes `omx setup --force --scope user` (Oh My Codex post-install configuration) as an imperative step of a python orchestrator."
evidence = "python/src/dotfiles_setup/ai.py:32-36 (cmd = [\"omx\", \"setup\", \"--force\", \"--scope\", \"user\"]). `omx` is declared in NO tracked mise config in this repo: git grep for the token across the tree returns only .agnix.toml:6, .gitignore:51, .gitleaks.toml:26, hk-common.pkl:92, python/src/dotfiles_setup/doc_refs.py:183 and two plugins/ paths — all of which are the unrelated `.omx/` state DIRECTORY, plus this one call site. The binary resolves only from the user-global config outside this repo's review: ~/.config/mise/config.toml:286 (\"npm:oh-my-codex\" = { version = \"0.21.3\", ... }), confirmed by `mise which omx` -> ~/.local/share/mise/installs/npm-oh-my-codex/0.21.3/bin/omx."
native_alternative = "mise tool-level `postinstall` option — a per-tool script that runs immediately after that tool is installed — declared alongside the tool pin itself; or the config-level `[hooks] postinstall` for a whole-config post-install step."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/hooks.md:83-90 (\"## Tool-level postinstall — Individual tools can define their own postinstall scripts using the `postinstall` option. These run immediately after each tool is installed\", e.g. `node = { version = \"20\", postinstall = \"npm install -g pnpm\" }`); same file :41-57 for the config-level `[hooks] postinstall` and :71 (\"A `mise install` that finds nothing to install ... still runs the `postinstall` hook\") which gives the idempotent re-run semantics this method hand-rolls with `--force`."
rationale = "Two independent reasons. (a) Native coverage: 'run this tool's setup right after installing it' is exactly mise's tool-level `postinstall`, and it would live next to the pin so the declaration and its setup cannot drift — which is what the repo's mise-first / use-tool-builtins rules require. (b) The custom code cannot be correct as written anyway: `omx` is a tool this repo never declares, so on any clone without the operator's personal ~/.config/mise/config.toml the call hits FileNotFoundError -> SystemExit (audit.py:78-80). Retiring it removes a dependency on an out-of-repo, unreviewed pin. If Oh My Codex should be part of the environment, the reviewed change is a pin in .config/mise/conf.d/shared.toml or .devcontainer/mise-runtime.toml carrying `postinstall = \"omx setup --force --scope user\"` — not a python method."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ai-3"
unit = "mod-ai"
kind = "module"
disposition = "retire"
confidence = 0.90
custom_thing = "The whole module (dotfiles_setup.ai / AIOrchestrator.run_all) and the `dotfiles-setup ai-setup` CLI subcommand that is its only entry point."
evidence = "python/src/dotfiles_setup/ai.py:13-42 (the entire module); python/src/dotfiles_setup/main.py:18 (import), :1671-1672 (subparsers.add_parser(\"ai-setup\", help=\"Install Claude Code and AI extensions\")), :2333-2335 and :2358 (the _ai_setup handler and its dispatch entry). A repo-wide `grep -rn \"ai-setup\"` (excluding .git/.agent) returns 6 hits: those 3 in main.py plus 3 in historical planning docs (docs/maestro/plans/2026-03-22-ai-agent-orchestration-plan.md:42-43, docs/research/trail/findings/gap-analysis-discovery-2026-03-28.yaml:99). No mise task, no hk step, no .claude/settings.json hook, no .github workflow, no .devcontainer script, no python/verification/suites.toml contract, and no tests/test_ai.py (tests/ listing has no ai test file)."
native_alternative = "Nothing needs to replace it: the two capabilities it wraps are covered by mise natively (mod-ai-1: `mise which` / `mise ls --missing`; mod-ai-2: tool-level `postinstall`), and the repo's own canonical surface for a recurring workflow is a mise task wrapping a python library (.claude/rules/mise-tasks-only.md), which this subcommand never became."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/ls.md:26 and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/hooks.md:83-90 (as cited in mod-ai-1 and mod-ai-2); repo-side: .claude/rules/mise-tasks-only.md (canonical task map — every recurring workflow has a mise task; `ai-setup` has none)."
rationale = "Dead code with a broken step: unreachable from every automated path, untested, uncontracted, and its second method depends on a tool no tracked config declares. Control arm for the 'no callers' grep (per .claude/rules/probes-need-a-control-arm.md): the identical command shape against the sibling subcommand `ensure-ssh` returns 7 hits, and against `p2996-hash` returns real call sites in docker-bake.hcl, mise.toml, python/verification/suites.toml, python/src/dotfiles_setup/pr.py and tests/test_p2996_hash.py — so the grep can find callers when they exist; the 'only main.py + historical docs' result for ai-setup is real, not a blind probe. Retire = delete ai.py, the import at main.py:18, the parser at main.py:1671-1672, the _ai_setup handler and its dispatch entry at main.py:2333-2335/:2358. Deleting a CLI subcommand is user-visible, so confirm with the operator before removal; the audit's recommendation is unambiguous but the removal itself is a reviewed diff."
probe = "uv run --project python dotfiles-setup ai-setup; echo \"rc=$?\"   # expected today: fails or no-ops depending on whether the operator's user-global omx pin is installed — run it before deleting, to record the behaviour being removed"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-apt_pins-1"
unit = "mod-apt_pins"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The whole point of the module: render a bash probe that runs apt's REAL solver (`apt-get install --simulate` over every pin in one transaction) inside a throwaway `docker run --rm` on the digest-pinned base, and fail rc=1 when the pinned set is unresolvable."
evidence = "python/src/dotfiles_setup/apt_pins.py:122-151 (probe_script, single `apt-get install --simulate -qq {specs}`), :154-175 (docker_command, `docker run --rm --platform <os/arch> <base_image> bash -c`), :208-243 (apt_pins_main, rc from `APT_PINS_OK`); wired at mise.toml:806-826 and pr.py:363-364"
native_alternative = "mise's own apt manager surface — `mise bootstrap packages status [--json|--missing]`, `mise bootstrap packages apply --dry-run`, and `mise bootstrap plan --json --detailed-exitcode` — is the only candidate that already understands `[bootstrap.packages]` pins. None of them invokes apt's solver."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/system/packages/apt.rs:240-246 (apply --dry-run only PRINTS `sudo::argv_with_env(\"apt-get\", ...)`, then `return Ok(())` — apt is never run); :174-200 (`installed()` shells `dpkg-query`, i.e. INSTALLED state only); :33-58 and :210-225 (`available()` is `apt-cache policy`, an index lookup); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/packages/status.md:4-13 (status is read-only over installed state); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/plan.md:1-14 (plan shows declared-vs-current changes, no resolvability). Host mise is already at the newest release (.agent/kb/raw/mise-releases-since-pin.digest.md: zero releases newer than 2026.9.3), so no post-pin capability closes this."
rationale = "REQUIRED JUSTIFICATION: the failure mode this gate exists for is invisible to every native mechanism at the host's pins. `apt-cache policy` (mise `available()`) and Renovate's `deb` datasource both answer 'is version V of package P in the index?' — the module's header (apt_pins.py:11-31) records the clean-room case where `curl=8.18.0-1ubuntu2` IS in the index and still fails to install because it hard-depends on its co-versioned `libcurl4t64`. Only apt's solver, over the whole pin set in ONE transaction, sees that; mise's dry-run stops at printing the argv. Renovate is already adopted natively for the currency half (renovate.json:177-185, `deb` datasource + `deb` versioning), which is the complementary capability, not this one."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-apt_pins-2"
unit = "mod-apt_pins"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "The module re-parses `[bootstrap.packages]` itself and hand-renders the apt operand list (`name=version`, sorted, space-joined) that the probe simulates — a second implementation of mise's own pin rendering and manager scoping, alongside the sibling parser in bootstrap_packages.py."
evidence = "python/src/dotfiles_setup/apt_pins.py:96-119 (pinned_apt_packages / unpinned_apt_packages re-read the TOML), :130 (`specs = \" \".join(f\"{name}={version}\" ...)`), :149 (`apt-get install --simulate -qq {specs}`) — note the generated line omits the `--` operand terminator mise itself emits"
native_alternative = "`mise bootstrap packages apply --manager apt --dry-run` prints the exact argv mise would run (including the `--` terminator and the `manager:package` scoping), so the probe could ask mise for the command instead of re-deriving it, then re-issue that argv with `--simulate`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/packages/apply.md:22-30 (`-m --manager <MANAGER>`, `-n --dry-run` — \"Print the commands that would run without running them\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/system/packages/apt.rs:228-246 (the printed argv: `install -y -- name=version`)"
rationale = "Native covers the 'what would apt be asked to install' half exactly, and adopting it removes both the duplicate TOML parse and a real fidelity gap (the probe's operand list has no `--` terminator and no manager scoping, so it can drift from what `mise bootstrap packages apply` actually issues in the Dockerfile at line 210). The cost is honest and not small: the probe container is the bare base image with no mise and no COPYd config, so adopting this means installing mise + copying mise-system.toml into the throwaway container, which is exactly what the Dockerfile bootstrap stage does — a fidelity gain paid for in probe runtime. Worth a design decision, not an automatic swap."
probe = "docker run --rm --platform linux/amd64 <BASE_IMAGE> bash -c 'set -e; curl -fsSL https://mise.run | sh; export PATH=$HOME/.local/bin:$PATH; mise bootstrap packages apply --manager apt --dry-run' # then diff the printed argv against probe_script()'s `specs`"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-apt_pins-3"
unit = "mod-apt_pins"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "The probe script hand-rolls the apt.llvm.org third-party repository: fetch the signing key, assert its fingerprint against the Dockerfile-pinned value, write a deb822 `.sources` file with the codename read from /etc/os-release, then `apt-get update` — a deliberate mirror of the Dockerfile base stage."
evidence = "python/src/dotfiles_setup/apt_pins.py:131-148 (keyring install, `gpg --show-keys` fingerprint compare, printf of the `.sources` stanza), mirroring .devcontainer/Dockerfile:183-210"
native_alternative = "None found in mise 2026.9.3: `mise bootstrap` manages packages, git repos, dotfiles, files, services, systemd units, secrets and accounts — there is no apt-repository/keyring resource. `[bootstrap.repos]` is git checkouts, not apt sources."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/repos.md:7 (\"Manage git repo checkouts from `[bootstrap.repos]`\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/ (full subcommand set: accounts, compose, dotfiles, files, firewall, launchd, linux, macos, packages, plugins, remote, repos, secrets, services, systemd, user — no apt-source resource); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/linux.md:14 (the only `bootstrap linux` subcommand is systemd-units). Control arm for the search: the same `grep -rn 'sources.list|keyring'` shape DOES hit /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/contributing.md:975-978, so the 0-result over src/system and the bootstrap docs is a real absence, not a blind grep."
rationale = "REQUIRED JUSTIFICATION: nothing native manages an apt repository + pinned signing fingerprint, and the duplication with the Dockerfile is load-bearing rather than accidental — the module's header (apt_pins.py:44-49) records that the seed-unpinned-then-add-llvm ordering is what makes a pin-below-candidate failure reproducible; a probe on a pristine image would miss it. The residual risk is drift between the two copies, which is already covered by pr.py's `_APT_PIN_PATTERNS` including the Dockerfile (pr.py:173-176) so a base/key change re-runs the probe."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-apt_pins-4"
unit = "mod-apt_pins"
kind = "module"
disposition = "unknown"
confidence = 0.35
custom_thing = "`dockerfile_arg()` — a regex reader for a top-level `ARG name=value` default, used to pull BASE_IMAGE and LLVM_APT_SIGNING_FINGERPRINT out of the Dockerfile. It is the third hand-rolled ARG regex in the package (gcc_sha.py and lock_refresh.py each carry their own)."
evidence = "python/src/dotfiles_setup/apt_pins.py:77 (`_ARG_RE`), :80-93 (dockerfile_arg), :214-215 (both reads); siblings at python/src/dotfiles_setup/gcc_sha.py:54-62 and python/src/dotfiles_setup/lock_refresh.py:58"
native_alternative = "BuildKit's frontend introspection — `BUILDX_EXPERIMENTAL=1 docker buildx build --print=outline` — is documented (outside this corpus) to emit a target's build args with their default values, which would make the ARG defaults a buildx query rather than three regexes."
alternative_evidence = "NO CITATION AVAILABLE: the offline corpus carries no docker/buildx doc tree (the per-tool map in this run lists mise, hk, uv, gh, … but no docker), and no gap digest was fetched for buildx. Per rule 4 this stays 'unknown' until the probe below runs at the host's docker-cli 29.7.2 pin."
rationale = "The capability (read an ARG default) is small and the current regex is deliberately anchored to TOP-LEVEL ARGs (tests/test_apt_pins.py:70-84 pins that a `BASE_IMAGE` mention inside a RUN must not win), so a swap is only worth it if the native path gives the same guarantee without a daemon round-trip on every gate run. Settle it with the probe; if buildx prints the defaults reliably, the consolidation target is one shared reader for all three call sites, not three swaps."
probe = "BUILDX_EXPERIMENTAL=1 docker buildx build --print=outline -f .devcontainer/Dockerfile .devcontainer 2>&1 | grep -i -A2 'BASE_IMAGE\\|LLVM_APT_SIGNING_FINGERPRINT'   # control arm: same command against a Dockerfile with the ARG deleted must NOT print a default"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-apt_pins-5"
unit = "mod-apt_pins"
kind = "module"
disposition = "refactor"
confidence = 0.40
custom_thing = "The gate's path-conditional dispatch is hand-rolled: `_APT_PIN_PATTERNS` plus `changes_apt_pin_inputs(paths)` decide, from a diff-vs-main path list, whether ship runs `mise run verify-apt-pins`."
evidence = "python/src/dotfiles_setup/pr.py:162-176 (`_APT_PIN_PATTERNS`), :355-364 (`if changes_apt_pin_inputs(paths): gates.append(Gate(\"verify-apt-pins\", ...))`); asserted by python/verification/suites.toml:1911-1929"
native_alternative = "hk already selects steps by changed files natively: a step's `glob` filters the file set and the step is SKIPPED when nothing matches, and `hk check --pr` is the documented shortcut for `--from-ref DEFAULT_BRANCH --to-ref HEAD` — i.e. 'run only what the branch changed vs main', which is precisely what changes_apt_pin_inputs computes by hand."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:53-55 (\"the files to run the linter on, if no files are matched, the linter will be skipped … this will filter the staged files\", `glob = List(...)`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/check.md:34 (`--pr` — \"Check only files changed in the current PR/branch (shortcut for --from-ref DEFAULT_BRANCH --to-ref HEAD)\"); :31,:45 (`--from-ref`/`--to-ref`). hk pin 1.57.0; .agent/kb/raw/hk-releases-since-pin.digest.md records no capability removal here."
rationale = "Native covers PART of it — the 'which files changed vs main' selection — but not the placement. Moving the probe into hk makes it a step of `mise run lint`, and `hk run check --all` (what the lint gate and CI actually run) globs the whole tree, so the two pin-input files always match and every lint run would pay a ~60s docker probe; the current design deliberately fires it only on ship when those paths changed. So the honest form is: keep the ship gate, and consider hk `glob` + `--pr` only if the probe is ever wanted in the pre-commit/PR lane. Flagged here because the unit's wiring is the only remaining hand-rolled path-matching in its chain; the module itself (this unit's files) needs no change."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-apt_repo-1"
unit = "mod-apt_repo"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "render_toml() renders `[bootstrap.packages]` declaration lines (`\"apt:<name>\" = \"latest\"`, or the exact version under --pin) for the operator to paste into .devcontainer/mise-system.toml; exposed as the --toml/--pin flags of `mise run apt-repo`."
evidence = "python/src/dotfiles_setup/apt_repo.py:197-209 (render_toml, and AptPackage.declaration at :118-121); flags registered at python/src/dotfiles_setup/main.py:192-202; handler python/src/dotfiles_setup/main.py:2182-2187; documented usage `mise run apt-repo -- --llvm-version dev --toml --exclude-runtime` at mise.toml:692 and `--toml --pin` at .devcontainer/mise-system.toml:188."
native_alternative = "`mise bootstrap packages use apt:<pkg>[@<version>] --path <file> [--dry-run]` — mise itself writes `\"apt:<pkg>\" = \"latest\"` (or the pinned version) into a named config file's [bootstrap.packages], and explicitly tolerates the manager being unavailable so that apt entries can be authored from a Mac; --dry-run prints exactly the `\"key\" = \"version\"` line instead of writing."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/packages/use.md:9 (\"Add bootstrap packages to [bootstrap.packages]\"), :15-16 (pin with `@`, otherwise no pin), :27 (--dry-run), :28 (--path <PATH>); source /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/system/use.rs:68 (`unwrap_or_else(|| \"latest\".into())`), :96 (dry-run prints `\"{key}\" = \"{version}\"`), :118-124 (\"an unavailable manager is not an error here: writing apt: entries from a mac into a shared repo config is the point of a declarative file\"). Corpus mise is 2026.9.0 (Cargo.toml:14); host pin is current per .agent/kb/raw/mise-releases-since-pin.digest.md (zero releases newer than 2026.9.3, no new capabilities)."
rationale = "The module's own docstring rules mise out for SEARCH (apt_repo.py:18-20), and that is still true — but it rules out too much: the WRITE half of the workflow is native. `bootstrap packages use` produces the same key/value shape apt_repo renders, targets an arbitrary file with --path, and was deliberately built to be run from a machine without apt. apt_repo should keep the enumeration/filtering and hand the chosen names to `mise bootstrap packages use -p .devcontainer/mise-system.toml`, retiring render_toml and the --toml/--pin flags, rather than emitting TOML text for a human to paste. Not a full retire: mise's dry-run line is prefixed with the config path so it is not paste-ready as-is, and a real write reformats the target file, so the change needs a look at how mise-system.toml's heavy comment block survives a toml_edit save."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && mise exec -- mise bootstrap packages use --dry-run --path .devcontainer/mise-system.toml apt:clang-22 apt:libomp-22-dev  # expect two `<path>: \"apt:clang-22\" = \"latest\"` lines, rc=0, nothing written; then repeat without --dry-run on a COPY of the file and diff to see whether comments/ordering survive"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-apt_repo-2"
unit = "mod-apt_repo"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Enumeration of every package an apt repository publishes: RepoQuery (repo/suite/component/arch -> dists/<suite>/<component>/binary-<arch>/Packages.gz), the apt.llvm.org suite-naming rule incl. the unnumbered development suite, available_packages(), and filter_packages()'s `Section: libs` drop."
evidence = "python/src/dotfiles_setup/apt_repo.py:49-58 (LLVM_REPO_TEMPLATE, LLVM_DEV), :61-77 (llvm_suite), :80-106 (RepoQuery.packages_url / for_llvm), :170-180 (available_packages), :183-194 (filter_packages); sole consumers are the thin mise task mise.toml:679-699 and the local-first probe table row docs at .claude/rules/local-devcontainer-first.md:42."
native_alternative = "None. `mise bootstrap packages` exposes only apply / brew / import / prune / status / upgrade / use — there is no search or list-available verb; `import` reads INSTALLED packages and is brew-only. apt-cache/apt-get indextargets require the repository to already be configured and /var/lib/apt/lists populated, which is backwards for deciding whether to adopt a repo. Renovate's `deb` datasource resolves versions for a NAMED package, never an enumeration."
alternative_evidence = "Subcommand set enumerated by directory listing (shape, not an alternation of expected names): /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/packages/ contains only apply.md, brew/, brew.md, import.md, prune.md, status.md, upgrade.md, use.md. /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/bootstrap/packages/apt.md — grep -n 'search|available|index|Packages' returns 1 hit and it is the prose 'newest available version' at :42; control arm on the same file with the same command shape: grep -c 'apt' returns 13, so the search is not blind. /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/bootstrap/packages/import.md:11-12 ('Currently supports Homebrew formulae only')."
rationale = "Required justification: no tool in the audited set answers 'what does this repository publish, before I add it'. mise has no search verb at the pinned/host version, apt's own tooling needs the repo configured first, and Renovate's deb datasource is a per-depName lookup driven by an existing declaration — it cannot discover a name nobody has written down yet, which is the entire point of #251 (libmlir-22 / libomp-22-dev / the unnumbered dev suite are all unguessable). The one soft spot is coverage, not supersession: unlike apt_pins there is NO suites.toml contract binding this chain (grep 'apt' over python/verification/suites.toml returns apt-pins and bootstrap-packages suites only), so the task->CLI->module wiring could rot silently."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-apt_repo-3"
unit = "mod-apt_repo"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "deb822 `Packages` index parsing into AptPackage records (parse_packages), with gzip decompression of the fetched index."
evidence = "python/src/dotfiles_setup/apt_repo.py:147-167 (parse_packages via `Packages.iter_paragraphs(raw, use_apt_pkg=False)`), :180 (gzip.decompress); dependency declared at python/pyproject.toml:13-19."
native_alternative = "python-debian (Debian's own maintainers; Ubuntu ships the identical code as python3-debian) — already the mechanism in use; `use_apt_pkg=False` selects its pure-python parser so no libapt is needed. Nothing is hand-rolled here."
alternative_evidence = "python/pyproject.toml:13-19 declares the dependency with that provenance; the module's use of it is python/src/dotfiles_setup/apt_repo.py:42 (`from debian.deb822 import Packages`) and :154. python/AGENTS.md:109 records python-debian as the project's deb822 parser."
rationale = "Required justification: this capability is ALREADY the native path — .claude/rules/use-tool-builtins.md is satisfied, not violated. The only custom code is the ~15-line adaptation of paragraphs into a frozen dataclass plus a stable sort, which no library can supply because the record shape (name/version/section/first-line description + the `apt:` declaration property) is this repo's, not Debian's. Listed so the audit's record shows the parser was checked rather than assumed."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-apt_repo-4"
unit = "mod-apt_repo"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "The --pin path of render_toml: emitting each package's EXACT current version for the initial [bootstrap.packages] declaration."
evidence = "python/src/dotfiles_setup/apt_repo.py:197-209 (the `pin` branch, whose own docstring warns apt.llvm.org 'rotates its single build daily, so a pin written here goes stale'); the documented seeding recipe is .devcontainer/mise-system.toml:188 (`mise run apt-repo -- --llvm-version 22 --toml --pin`)."
native_alternative = "Renovate's `deb` datasource driven by this repo's existing customManager already owns every pinned `apt:` entry in mise-system.toml (extraction + lookup proven end-to-end 2026-07-15/16), so after the first write no human ever re-runs --pin; and the first write itself is available natively as `mise bootstrap packages use apt:<pkg>@<version>` (finding 1)."
alternative_evidence = "renovate.json:177-185 (the `[bootstrap.packages]` customManager, datasourceTemplate `deb`, versioningTemplate `deb`) and renovate.json:51-56 (the apt-ubuntu-pockets rule naming the apt.llvm.org registryUrl `https://apt.llvm.org/resolute?suite=llvm-toolchain-resolute-22&components=main&binaryArch=amd64` and citing lib/modules/datasource/deb/index.ts). `deb` is a first-class Renovate versioning at .agent/kb/raw/renovate-config-options.md:20648. NOTE: the saved Renovate page is the configuration-options reference; it does not itself document the deb DATASOURCE's index behaviour, so that half rests on this repo's own probed record rather than a vendor doc line — hence the reduced confidence and the probe below."
rationale = "The pin RENDER is the thin part of a capability whose maintenance is already native and already wired. Folding --pin into `mise bootstrap packages use apt:<pkg>@<version>` (finding 1) would leave Renovate as the single owner of pin currency and delete a flag whose own docstring says its output is stale on arrival. Kept at 'refactor' rather than 'retire' because the seeding step still needs the enumeration (finding 2) to know WHICH names to pin, and because the native-side citation for the deb datasource is a saved config-options page plus this repo's probe record, not a vendor doc line for the datasource itself."
probe = "curl -fsS https://docs.renovatebot.com/modules/datasource/deb/ -o .agent/kb/raw/renovate-datasource-deb.md && grep -n 'Packages\\|registryUrl\\|suite=' .agent/kb/raw/renovate-datasource-deb.md   # settles whether the vendor doc states the per-depName-only lookup and the Packages.gz index contract; control arm: grep -c 'deb' on the same file must be non-zero"
correction = "**⚠ CITATION CORRECTED (round 2, re-verified round 5):** `\"deb\"` is at `.agent/kb/raw/renovate-config-options.md:20652`, not `:20648` (`:20648` is `\"cargo\",`; the `allowedValues` array opens at `:20642`). Re-probed with `sed -n '20642p;20648p;20652p'`. The claim is true, the pointer was four lines short. Second correction: this row's `probe` field names `.agent/kb/raw/renovate-datasource-deb.md`, which **does not exist on disk** — it is a proposed future fetch, not a saved source (`ls .agent/kb/raw/ | grep renovate` -> `renovate-config-options.md`, `renovate-datasource-git-refs.md`, `renovate-manager-pep621.md` + three per-unit probe files)."
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ask_quality-1"
unit = "mod-ask_quality"
kind = "module"
disposition = "keep"
confidence = 0.93
custom_thing = "Deterministic content validation of an AskUserQuestion payload inside a PreToolUse command hook: a first-and-only `(Recommended)` label on single-select questions, `PRO:`/`CON:` in every option description, and a citation marker (backticked path / #NNN / URL) or the `[no prior evidence]` escape, returned as a deny reason."
evidence = "python/src/dotfiles_setup/ask_quality.py:83-92 (markers + _CITATION_RE), :138-189 (the three checks), :215-227 (decide -> deny reason); dispatched at python/src/dotfiles_setup/hook_guard.py:800-801 (`if tool_name == \"AskUserQuestion\": return ask_quality.decide(tool_input)`); wired at .claude/settings.json:54 (`\"matcher\": \"Bash|AskUserQuestion|Edit|Write|NotebookEdit\"`); contract python/verification/suites.toml:1254-1268; tests tests/test_ask_quality.py:65-292."
native_alternative = "Claude Code permission rules (`Tool(param:value)` deny/ask rules), the hook handler `if` field (permission-rule syntax), and the `dontAsk` permission mode are the only native mechanisms that can act on an AskUserQuestion call. None can read the payload's nested content."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:137 — \"The parameter name must be a direct field of the tool's input ... Fields nested inside an object or array are not matchable\" (AskUserQuestion's payload is `questions[]` -> `options[]` -> `description`, three levels of nesting); .../hooks.md:427 — the `if` field \"Uses the same syntax as permission rules\", so it inherits the same limit; .../permissions.md:86 — `dontAsk` only denies AskUserQuestion wholesale; .../hooks.md:1542 confirms PreToolUse still matches `AskUserQuestion` by name and .../hooks.md:1744-1745 that `\"deny\"` prevents the call with the reason shown to Claude — i.e. the custom hook is still the only seam."
rationale = "Nothing native inspects tool-input content below the top level, so the three checkable components of Ray's standard cannot be expressed as settings. The fourth component (free-form answer) is already native and correctly NOT implemented here — tools-reference.md:127 documents the `Other` row and notes field — which is exactly the shape a native-first audit wants: custom code only where the platform has no reach. The module is also small, pure, fail-open on unparsable shapes (ask_quality.py:204-212), and end-to-end pinned by suites.toml:1254."
probe = ""
correction = ""
correction_custom = "**⚠ CITATION CORRECTED (round 5) — RANGE END PAST EOF.** `tests/test_ask_quality.py:65-292` overshoots by three: the file is **289** lines and ends at `test_cli_allows_a_compliant_ask`, the control arm for the deny test. Read it as **`:65-289`** (to EOF). Found by the round-5 mechanical anchor sweep."
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ask_quality-2"
unit = "mod-ask_quality"
kind = "module"
disposition = "refactor"
confidence = 0.90
custom_thing = "The module's 'Harness contract' docstring pins seven vendor-doc line anchors ($CC/hooks.md:1394, :246, :616, :1523, :1529, :1544; $CC/settings.md:177; $CC/agent-sdk__python.md:2560) as the proof that the seam is documented rather than guessed."
evidence = "python/src/dotfiles_setup/ask_quality.py:48-70 (all seven anchors); the same three are mirrored in .claude/rules/clarify-before-acting.md:81-83."
native_alternative = "The same facts in the CURRENT offline corpus, which has been refreshed since those anchors were written (it now carries the 2026-w34 what's-new run per .agent/kb/raw/claude-code-llms-index.digest.md): PreToolUse matcher values incl. AskUserQuestion, matcher-runs-against-tool_name, settings hot-reload, deny semantics, and the AskUserQuestion tool_input schema."
alternative_evidence = "/Users/rmanaloto/.../docs/claude-code/hooks.md:1542 (AskUserQuestion among PreToolUse matcher values — was cited as 1394); hooks.md:329 (\"For tool events, that field is `tool_name`\" — was 246); hooks.md:1723 (the AskUserQuestion `questions` schema row, which is also where the `{\"label\": \"React\"}` description-less example now lives — was 1523/1529); hooks.md:1744-1745 (deny prevents the call, reason shown to Claude — was 1544); settings.md:646 (settings watched and reloaded, \"including edits to `permissions`, `hooks`\" — was settings.md:177); agent-sdk__python.md:2521-2540 (`description: str`, `preview: str | None` on each option — was 2560). Control arm: `grep -c PreToolUse hooks.md` = 70, so the grep shape finds present terms; `grep -n AskUserQuestion hooks.md` returns 1542/1717/1744/1773/1785/1787/1803 and nothing at 1394."
rationale = "Every cited anchor has moved; a line number into a rolling doc corpus rots and, once wrong, a future session cannot tell 'we verified this' from 'we made it up' (the exact failure `.claude/rules/probes-need-a-control-arm.md` warns about for inherited claims). The claims themselves all still hold, so this is a citation refresh, not a behaviour change: re-anchor to the section headings (`### PreToolUse`, `##### AskUserQuestion`, `#### PreToolUse decision control`) or to quoted sentences rather than to line numbers, and fix .claude/rules/clarify-before-acting.md:81-83 in the same change."
probe = ""
correction = "**⚠ CITATION CORRECTED (round 5) — ELIDED PATH, UNRESOLVABLE AS WRITTEN.** The anchor is written `/Users/rmanaloto/.../docs/claude-code/hooks.md:1542`, with a literal `...` standing in for the corpus prefix; nothing can open that path. The full path is `/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md`, which is **3769** lines, and `:1542` lands exactly on the PreToolUse description (`Runs after Claude creates tool parameters and before processing the tool call. Matches on any tool name except `EndConversation` …`). The claim is unaffected; the pointer was unusable. One of **five** elided anchors found by the round-5 mechanical sweep — see Method → “Citation discipline”."
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ask_quality-3"
unit = "mod-ask_quality"
kind = "module"
disposition = "unknown"
confidence = 0.60
custom_thing = "`_citation_corpus` searches each option's `preview` field as well as its `description`, header and question text, so a citation living only in a preview satisfies the gate."
evidence = "python/src/dotfiles_setup/ask_quality.py:119-124 (`parts.append(_text(_field(opt, \"preview\")))`); ask_quality.py:61-64 claims the shape is the documented one."
native_alternative = "`preview` is an SDK-gated, opt-in field: the TypeScript SDK option `askUserQuestion.previewFormat` \"Opts into the `preview` field ... When unset, Claude does not emit previews\", and the Python SDK types it `str | None`. Newer payload fields the module does not read (`annotations`, `metadata`) also now exist."
alternative_evidence = "/Users/rmanaloto/.../docs/claude-code/agent-sdk__typescript.md:1016 (`askUserQuestion.previewFormat` — \"When unset, Claude does not emit previews\"); agent-sdk__python.md:2538-2539 (`description: str`, `preview: str | None`); agent-sdk__python.md:2545-2552 (`annotations`, `metadata` on the input); hooks.md:1723, which documents the CLI-side `questions` row with only `question`/`header`/`options`/`multiSelect` and an option example of `{\"label\": \"React\"}` — no `preview` at all."
rationale = "If the interactive CLI never sets `previewFormat`, the `preview` branch of the citation corpus is dead code that has never widened the ALLOW direction, and the docstring's claim that the read shape is 'the documented one' is true only of the SDK surface, not of hooks.md. Harmless either way (it can only allow, never deny), but it should either be exercised or dropped with a note. Settle it with a live payload capture before touching it."
probe = "Temporarily point the PreToolUse AskUserQuestion handler at a tee: `cp .claude/settings.json /tmp/s.bak` then add a second handler `bash -c 'cat > /tmp/ask-payload.json'` under the existing matcher, issue one compliant ask from an interactive session, and run `jq '.tool_input.questions[0].options[0] | keys' /tmp/ask-payload.json` — the key list says whether `preview` (and `annotations`/`metadata`) ever reach the hook in the CLI. Control arm: the same jq must show `description`, which is known present. Restore settings.json afterwards."
correction = "**⚠ CITATION CORRECTED (round 5) — ELIDED PATH, UNRESOLVABLE AS WRITTEN.** `/Users/rmanaloto/.../docs/claude-code/agent-sdk__typescript.md:1016` resolves to `/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__typescript.md` (**5158** lines); `:1016` is the `askUserQuestion.previewFormat` table row, exactly as quoted. Substance unaffected."
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ask_quality-4"
unit = "mod-ask_quality"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "The module deliberately gates only shape, leaving 'is the recommendation good, is the citation relevant, are the cons honest' to human review — and its own docstring names an un-audited risk: `[no prior evidence]` being reached for routinely would defeat the gate rather than satisfy it, with nothing measuring that."
evidence = "python/src/dotfiles_setup/ask_quality.py:26-30 (the named risk, 'a thing to audit'), :39-41 ('Deliberately NOT gated ... Those are review, not lint'); nothing in python/verification/suites.toml:1254-1268 or tests/test_ask_quality.py asserts anything about escape frequency."
native_alternative = "Prompt-based hooks (`type: \"prompt\"`, an LLM call that allows or blocks) and agent hooks (`type: \"agent\"`, a subagent with tool access), both supported on `PreToolUse` — a hook type that did not exist when this module was written."
alternative_evidence = "/Users/rmanaloto/.../docs/claude-code/hooks.md:3399 — \"Claude Code supports prompt-based hooks (`type: \\\"prompt\\\"`) that use an LLM to evaluate whether to allow or block an action, and agent hooks (`type: \\\"agent\\\"`) that spawn an agentic verifier with tool access\"; hooks.md:3401-3410 lists `PreToolUse` among the events supporting all five handler types; hooks.md:3535-3539 marks agent hooks experimental and says to prefer command hooks for production."
rationale = "The judgment half of Ray's standard is now expressible natively as a second `PreToolUse` handler on the same matcher — a prompt hook could judge whether a citation is actually relevant and whether `[no prior evidence]` is being used as an escape, which no deterministic check can. Refactor rather than retire: the deterministic command hook must stay as the fast, offline, always-correct layer (finding 1), and hooks.md:3535 itself warns agent hooks are experimental, so the LLM layer would be additive and advisory (`\"ask\"` rather than `\"deny\"`). Cost is one extra model call per ask, which is why this is a proposal, not a defect."
probe = ""
correction = "**⚠ CITATION CORRECTED (round 5) — ELIDED PATH, UNRESOLVABLE AS WRITTEN.** `/Users/rmanaloto/.../docs/claude-code/hooks.md:3399` resolves to `…/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:3399`, which is the prompt-based-hooks paragraph (`In addition to command, HTTP, and MCP tool hooks, Claude Code supports prompt-based hooks (`type: \"prompt\"`) that use an LLM to evaluate …`). Substance unaffected."
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ask_quality-5"
unit = "mod-ask_quality"
kind = "module"
disposition = "refactor"
confidence = 0.40
custom_thing = "`decide` has exactly two outcomes — a deny string or None — so a mechanically repairable defect (the `(Recommended)` marker present but not on the first option) costs a full model round trip to re-ask."
evidence = "python/src/dotfiles_setup/ask_quality.py:215-227 (deny-or-None); the ordering rule at :176-180; hook_guard emits only `permissionDecision: \"deny\"` (python/src/dotfiles_setup/hook_guard.py:816-828)."
native_alternative = "PreToolUse `hookSpecificOutput.updatedInput`, which replaces the tool's input before execution and can be combined with `\"allow\"` or with `\"ask\"` to show the modified input to the user."
alternative_evidence = "/Users/rmanaloto/.../docs/claude-code/hooks.md:1746 — \"`updatedInput` | Modifies the tool's input parameters before execution. Replaces the entire input object ... Combine with `\\\"allow\\\"` to auto-approve, or `\\\"ask\\\"` to show the modified input to the user\"; hooks.md:1029 confirms it sits directly under `hookSpecificOutput` for PreToolUse; hooks.md:1773 documents the AskUserQuestion-specific allow+updatedInput contract."
rationale = "Only the ordering violation is mechanically repairable (reorder `options` so the `(Recommended)` option is first); PRO:/CON: text and citations cannot be synthesized by a deterministic hook and must stay denies. Worth it only if that violation is observed in practice — and there is a real counter-argument to record if it is skipped: a silent auto-repair teaches the model nothing, whereas the deny reason is the revision instruction (hooks.md:1745, 'For \\\"deny\\\", shown to Claude'), which is the pedagogical mechanism the module was built around. Low confidence is about desirability, not about the feature's existence."
probe = ""
correction = "**⚠ CITATION CORRECTED (round 5) — ELIDED PATH, UNRESOLVABLE AS WRITTEN.** `/Users/rmanaloto/.../docs/claude-code/hooks.md:1746` resolves to `…/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1746`, the `updatedInput` row (`Modifies the tool's input parameters before execution. Replaces the entire input object, so include unchanged …`). Substance unaffected."
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-audit-1"
unit = "mod-audit"
kind = "module"
disposition = "retire"
confidence = 0.90
custom_thing = "ToolManager.sync_versions() — a hand-rolled regex bumper that slices the [tools] section out of home/dot_config/mise/config.toml.tmpl, matches `tool = \"<numeric>\"` lines, shells out to `mise latest <tool>` per tool, and string-replaces the version back into the file."
evidence = "python/src/dotfiles_setup/audit.py:95-153 (regex at :109 and :119-121, `mise latest` at :92, write-back at :151-152); wired as `dotfiles-setup sync-versions` (python/src/dotfiles_setup/main.py:1061, :2044, :2517) and run as part of `handle_install` (python/src/dotfiles_setup/main.py:2026-2035). CONTROL-ARMED no-op: re-running the module's own two regexes against its only target file yields 0 numeric-version matches while the identically-shaped `\"latest\"` arm yields 28 — every overlay tool in that file is deliberately pinned to `latest` (home/dot_config/mise/config.toml.tmpl:6-8, :14-38), so the bumper can never match anything it is written to bump."
native_alternative = "mise's own pin rewriters: `mise upgrade --bump` (upgrades and rewrites the version in mise.toml), `mise outdated --bump` (compares against latest available, `--json`), `mise lock --bump` (re-resolves fuzzy selectors like `latest`); plus Renovate's NATIVE mise manager via the `github>jdx/renovate-config` preset already adopted in this repo, and the shared currency engine behind `mise run tool-currency`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/upgrade.md:23 (`-b --bump` — Upgrades to the latest version available, bumping the version in mise.toml); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/outdated.md:18; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/lock.md:32,39,74-75; repo-side adoption at renovate.json:4 (`github>jdx/renovate-config`) and mise.toml (tool-currency task, `.claude/rules/tool-currency-and-native-first.md`)."
rationale = "Three separate native mechanisms now rewrite mise pins (mise upgrade/outdated/lock --bump, Renovate's mise manager, the currency engine), and the custom one is additionally a proven no-op against its only target file — it matches 0 of 28 tool lines because that overlay is intentionally unpinned. Retiring it also removes `mise latest` network calls from the install path. `tool-currency-and-native-first.md` rule 3 says a superseded custom path is deleted in the same change, not kept 'just in case'."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-audit-2"
unit = "mod-audit"
kind = "module"
disposition = "refactor"
confidence = 0.95
custom_thing = "_audit_codex() probes Codex auth by running `codex login --status` and then string-sniffing stdout+stderr for the substrings \"logged in\" / \"active\"."
evidence = "python/src/dotfiles_setup/audit.py:590-611 (invocation at :596-598, substring sniff at :604)."
native_alternative = "`codex login status` — a SUBCOMMAND, documented to exit 0 when credentials are present, explicitly 'helpful in automation scripts'. Read the exit code; do not parse text."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/cli__reference.md:204-210 (`### codex login` … '`codex login status` exits with `0` when credentials are present'). LIVE PROBE on this host (codex-cli 0.152.1), both arms: `codex login --status` -> rc=2 (usage error; `codex login --help` lists subcommand `status` and no `--status` flag — grep for `--status` = 0 hits, control arm `--with-api-key` = 2 hits), while `codex login status` -> rc=0."
rationale = "The custom probe calls a flag that does not exist, so `ToolManager.run_command` raises SystemExit on rc=2 and the auditor reports `codex_auth: failed` on a machine that IS logged in — a check that can only fail (`probes-need-a-control-arm.md`, the inverse case). The native mechanism is an exit code the vendor documents for exactly this use, which also removes the rule-9 symptom sniff on wording the repo does not own."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-audit-3"
unit = "mod-audit"
kind = "module"
disposition = "refactor"
confidence = 0.93
custom_thing = "_audit_claude() probes Claude Code auth by running `claude --help` and scanning the combined output for the phrases \"not logged in\", \"expired\", \"unauthorized\", \"login required\"."
evidence = "python/src/dotfiles_setup/audit.py:559-588 (help invocation at :569, auth_fail_terms at :575-580)."
native_alternative = "`claude auth status` — shows authentication status as JSON (`--text` for humans) and exits 0 if logged in, 1 if not; `claude doctor` for read-only install/settings diagnostics (which since 2.1.261 also prints an 'Organization policy' line explaining a policy-load failure)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/cli-reference.md:27 (`claude auth status` … 'Exits with code 0 if logged in, 1 if not'); same file :34 (`claude doctor`); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (2.1.261 'Organization policy' line in `/status` and `claude doctor`). LIVE PROBE on this host (Claude Code 2.1.266): `claude auth --help` lists `status [options]  Show authentication status`; `claude auth status --help` rc=0."
rationale = "`--help` output is not an auth surface — a logged-out CLI still prints usage and exits 0, so the sniff's failure arm depends entirely on wording Anthropic can change at any release, exactly the binding `probes-need-a-control-arm.md` rule 9 forbids. `claude auth status` is a documented machine-readable status with a documented exit code, available at the host's version."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-audit-4"
unit = "mod-audit"
kind = "module"
disposition = "refactor"
confidence = 0.78
custom_thing = "_audit_gemini() audits a `gemini` binary by running `gemini --version` and `gemini --help` and declaring auth failed when the output contains the word \"warning\" alongside any of \"auth\"/\"login\"/\"credential\"/\"token\"."
evidence = "python/src/dotfiles_setup/audit.py:613-640 (heuristic at :619-622 and :631-635); called unconditionally from audit_ai_agents (python/src/dotfiles_setup/audit.py:651)."
native_alternative = "The lane doctors that ship with the adopted plugins: `/fable-orchestrator:doctor` ('Validate the CLI lanes — presence, auth, and model access via a few tiny live calls per installed CLI'), and `/antigravity:setup` ('Verify the Antigravity (agy) CLI is installed and authenticated'). The host's Gemini lane is `antigravity-cli`, not `gemini`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fable-orchestrator/commands/doctor.md:2 (frontmatter description) and :7-11 (runs scripts/doctor.sh, relays FAIL lines); repo pin evidence: mise.toml:97 pins `antigravity-cli = \"1.1.24\"` on the host while `gemini` appears in NO host config — `npm:@google/gemini-cli` exists only inside the container runtime tier (.devcontainer/mise-runtime.toml:66)."
rationale = "Two defects in one capability: the probe binds a warning STRING nobody here owns (rule 9), and it audits a CLI that is not part of the host toolchain at all — the Gemini lane on this host is `agy`/antigravity-cli. Plugin-shipped lane doctors already do presence+auth+model-access with live calls, so the module should drop `_audit_gemini` and, if a Gemini-lane check is still wanted, point at those. Marked refactor rather than retire because no equivalent doctor exists for the in-container `npm:@google/gemini-cli`."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && { command -v gemini >/dev/null && echo 'gemini PRESENT on host' || echo 'gemini ABSENT on host'; }; command -v agy >/dev/null && echo 'agy PRESENT (control arm)' || echo 'agy ABSENT'"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-audit-5"
unit = "mod-audit"
kind = "module"
disposition = "retire"
confidence = 0.82
custom_thing = "DevEnvironmentAuditor.ensure_ssh() — creates ~/.ssh 0700, harvests `ssh-add -L` plus host-provided keys, writes ~/.ssh/authorized_keys 0600, then `pgrep -x sshd` and, if absent, `sudo mkdir -p /run/sshd`, `sudo ssh-keygen -A`, `sudo /usr/sbin/sshd`. Exposed as `dotfiles-setup ensure-ssh`."
evidence = "python/src/dotfiles_setup/audit.py:394-456; subcommand registered at python/src/dotfiles_setup/main.py:1666-1668, :2332-2334, :2357. Nothing invokes it: `git grep -n 'ensure-ssh'` returns only main.py plus an archived design doc (docs/ultrapowers/specs/2026-03-29-devcontainer-host-user-migration-design.md:240,343,519) — control arm, same command shape: `git grep -n 'dotfiles-setup doctor'` returns a live wiring hit at mise.toml:620."
native_alternative = "The devcontainer feature `ghcr.io/devcontainers/features/sshd:1` installs and runs openssh-server (listening on its hardcoded 2222, exposed via `appPort`), and `postCreateCommand` installs the host's authorized_keys/known_hosts while `postStartCommand` re-chowns the agent socket on every start."
alternative_evidence = ".devcontainer/devcontainer.json:223-224 (`\"features\": { \"ghcr.io/devcontainers/features/sshd:1\": {} }`), :27-36 (comment documenting that the feature provides sshd and that postCreateCommand copies the host authorized_keys), :237 (postCreateCommand installing ~/.ssh/authorized_keys + known_hosts), :244 (postStartCommand)."
rationale = "Every step ensure_ssh performs is now performed by the devcontainer lifecycle: the sshd feature owns daemon install/start and host-key generation, and postCreate/postStart own authorized_keys and the socket ownership. `do-not.md` #3 also says lifecycle belongs to @devcontainers/cli, not to a sudo-shelling python helper. The subcommand is dead wiring — no mise task, hook, hk step or suite calls it — so retiring it removes a second, divergent SSH bootstrap path that can silently disagree with the one the R1/R2 gates actually test."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-audit-6"
unit = "mod-audit"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "audit_ssh() re-implements the R1/R2 devcontainer invariants inline: `ssh -T -o BatchMode=yes git@github.com` interpreted by exit code (R2 outbound), and `ssh -p <port> -o BatchMode=yes localhost true` (R1 inbound), plus a 3s-bounded `ssh-add -l`."
evidence = "python/src/dotfiles_setup/audit.py:458-557 (github round-trip at :512-532, localhost round-trip at :534-555, ssh-add timeout guard at :488-501)."
native_alternative = "The repo's own gated equivalents, which are the ones CI and `verify-before-advancing.md` actually require: `mise run verify-ssh-inbound` for R1 (resolves the port through the same resolver `mise run up` uses) and devcontainer smoke tier 3 for R2 (`ssh -T git@github.com` asserted against 'successfully authenticated')."
alternative_evidence = "mise.toml:1005-1017 ([tasks.verify-ssh-inbound] -> `dotfiles-setup docker verify-ssh-inbound --port --user`, prints 'OK: R1 inbound ssh …'); scripts/devcontainer-smoke.sh:126 ('[tier3] SSH agent forwarding + github auth') and :146 (`grep -q \"successfully authenticated\"`); AGENTS.md 'Devcontainer success criteria' table binds R1 to `mise run verify-ssh-inbound` and R2 to smoke tier 3."
rationale = "Two implementations of the same invariant exist, and only one is gated — the smoke/verify pair runs in `verify-local`, `verify-container-latest`, `ship` and `land`, while audit_ssh runs from a command no task invokes. Duplicated invariants drift silently (the module still carries #77-era proxy commentary at :481-487). The bounded `ssh-add -l` guard is the one piece worth keeping and should move to the gated path rather than being deleted."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && git grep -n 'verify-ssh-inbound' mise.toml .github/workflows | head"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-audit-7"
unit = "mod-audit"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "MANAGED_PREFIXES + _audit_tool()/audit_environment() — a hard-coded list of four home-relative prefixes (~/.local/bin, ~/.local/share/mise, ~/.pixi/bin, ~/.cargo/bin) used to decide whether a resolved binary is 'managed', plus an `all(p in PATH.split(os.pathsep))` check for ~/.local/bin and the mise shims dir."
evidence = "python/src/dotfiles_setup/audit.py:182-187 (MANAGED_PREFIXES), :284-288 (prefix match on shutil.which), :315-327 (PATH_managed)."
native_alternative = "`mise doctor path` prints exactly the PATH entries mise is providing (`--full` for all), `mise which <bin>` resolves a bin to its managed install (`--version`/`--plugin` variants), and `mise doctor [--json]` reports installation problems machine-readably. Repo-side, doctor.toml's `[path_drift]` block plus `mise run doctor` already own 'is the tool on PATH the pinned one'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor/path.md:1-23 ('Print the current PATH entries mise is providing', `-f --full`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/which.md:1-27; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor.md:3-12 (`-J --json`); repo-side doctor.toml:196-213 ([path_drift], gate_tools) and mise.toml:620 (`dotfiles-setup doctor`)."
rationale = "A frozen prefix list is a restatement of mise's own layout and goes stale the moment install roots move (MISE_DATA_DIR, a conda/pixi relocation, the container's ~/.local/share/mise overlay) — and it silently degrades to 'unmanaged' rather than erroring, so the failure is invisible. mise answers both questions natively and machine-readably, and doctor.toml already owns the drift half on this host; the audit should ask mise instead of hard-coding its filesystem."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-audit-8"
unit = "mod-audit"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "ToolManager.install() — a fixed four-step installer sequence: `mise install node bun`, then `mise install -y`, then `uv tool install ruff`, then `pixi install`."
evidence = "python/src/dotfiles_setup/audit.py:155-169; reached via `handle_install` (python/src/dotfiles_setup/main.py:2026-2035)."
native_alternative = "`mise install` with no arguments already installs every tool the merged config declares, resolving inter-tool dependencies itself (the repo's own bootstrap is `mise install`), and `uv sync --locked` / `uv run --project python ruff` provide the project-pinned ruff. mise's conda backend is how pixi-provided tools are declared here in the first place."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/install.md (install with no args installs all tools in the config); AGENTS.md 'Quick Start' (`mise install  # Install all tools`) and 'Tool management' ('All tools declared in mise.toml … use mise binaries directly, not npx'); ruff is pinned by the python project, not by mise — python/pyproject.toml:59-141 configures ruff and it is run as `uv run --project python ruff`, while doctor.toml:213 lists `ruff` among `gate_tools` whose staleness weakens a gate."
rationale = "The `node bun` pre-step is redundant with mise's own dependency resolution, and `uv tool install ruff` installs a SECOND, unpinned ruff outside the project lock — precisely the drift doctor.toml's `[path_drift]` gate_tools entry exists to catch (a ruff version mismatch is what `lint-delta` was built for). One `mise install` plus `uv sync --locked` is the native, lock-respecting path; keep only whatever step mise genuinely cannot express, with the written justification `use-tool-builtins.md` step 3 requires."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-audit-9"
unit = "mod-audit"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "The `dotfiles-setup audit` command as a whole — a six-category environment report (Identity/Environment/Toolchain/SSH/AI Agents/Shell) whose pass/fail is derived by counting truthy values, with no gate wiring and a test that only asserts the exit code is 0 or 1."
evidence = "python/src/dotfiles_setup/audit.py:711-757 (run_all + the `v == 'ok' or v is True or (isinstance(v, str) and v != 'failed')` scoring at :743) and :760-777 (main); tests/test_audit.py:47-58 ('Exit 0 = all passed, 1 = some failed. Both valid.'). No mise task runs it — `git grep 'dotfiles-setup audit'` hits only docker.py:412 (the in-container `docker test` path), an archived plan and the test; control arm `git grep 'dotfiles-setup doctor'` hits mise.toml:620. No verification suite names audit.py (`git grep 'dotfiles_setup/audit' python/verification/suites.toml` -> 0 hits; control arm: token_audit.py appears at suites.toml:1667)."
native_alternative = "The two mechanisms that already carry this job under gates: `mise run doctor` / the SessionStart hook checking reality against the declarative doctor.toml, and `scripts/devcontainer-smoke.sh` tiers 1-3 (image identity + exact tool-set, secrets, sanitizers/SSH) invoked by `mise run verify-local`, `verify-container-latest`, `ship` and `land`."
alternative_evidence = "mise.toml:620 (`uv run --project python dotfiles-setup doctor`); doctor.toml:1-14 (the declared host setup, checked by `mise run doctor` and the SessionStart hook); scripts/devcontainer-smoke.sh:21 ([tier1] image identity + exact tool-set), :51 ([tier2] Doppler), :68 ([tier3] sanitizers), :155 ('devcontainer smoke: tiers 1-3 OK'); AGENTS.md 'Devcontainer success criteria' gates table."
rationale = "The command duplicates the declarative doctor and the smoke tiers while being invoked by nothing except the in-container `docker test` path, and its scoring treats any non-'failed' string as a pass, so a version string counts as a passing check — a summary that is hard to make red. The remaining unique checks (login-shell reachability of mise/chezmoi at :664-709, the AI-lane checks once fixed per mod-audit-2/3/4) should be folded into doctor.toml's declarative set or a smoke tier so they run under an existing gate; a report nothing gates is documentation, not verification."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-audit-10"
unit = "mod-audit"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "audit_identity() — compares live UID/GID/username against the configured expectations and asserts `sole_non_root_user`: exactly one pwd entry with uid >= 1000 (excluding `nobody`), and it is the current user."
evidence = "python/src/dotfiles_setup/audit.py:197-265 (NON_SYSTEM_UID_MIN at :23, the pwd.getpwall() sweep at :222-227, expectations from DotfilesConfig at :203-213)."
native_alternative = "None found. `mise doctor`, the devcontainer CLI, the sshd feature and the smoke tiers all check tools, image identity and connectivity — none enumerates the container's passwd database or asserts a single non-system user. `remoteUser`/`containerUser` in devcontainer.json only SET the user; they do not assert that no second one exists."
alternative_evidence = "Absence is control-armed: grep for the passwd-sweep concept across the offline corpora returns nothing while the same-shaped grep for a concept that IS covered returns hits — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor.md:1-23 documents only mise-installation problems, and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor/path.md:1-23 only PATH entries; the repo's own R3 arch gate (AGENTS.md success-criteria table, mise run verify-arch) covers architecture identity but not user identity."
rationale = "This is the one capability in the module with no native counterpart: it encodes a repo-specific invariant from the host-user migration (#677-era DEVCONTAINER_USER work) — that the image ships exactly one UID-1000 human user matching the host's `$USER`, so a leftover `vscode` account would be caught. No tool in the corpus enumerates /etc/passwd for this. Keeping it is justified, but it belongs in a gated surface (see mod-audit-9) rather than in an unwired report; the probe would settle whether smoke tier 1 already asserts the user set."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-autofix-1"
unit = "mod-autofix"
kind = "module"
disposition = "refactor"
confidence = 0.62
custom_thing = "The whole recovery path: `mise run autofix-apply -- <run-id>` downloads the CI-computed fix set and writes it into the working tree, as the App-down fallback for autofix.ci (python/src/dotfiles_setup/autofix.py:1-82; task at mise.toml:572-574; subcommand at python/src/dotfiles_setup/main.py:1821-1826 and 2383-2384; rule row at .claude/rules/mise-tasks-only.md:25)."
evidence = "python/src/dotfiles_setup/autofix.py:55-82 (`autofix_apply_main` shells `gh run download <id> --name autofix.ci`, then writes every base64 addition); mise.toml:572-574; .claude/rules/mise-tasks-only.md:25 ('autofix artifact recovery by hand | mise run autofix-apply -- <run-id>')."
native_alternative = "`hk fix` — hk's first-class top-level fix command, already wired here as `mise run fmt`. The artifact this module replays is produced by running the SAME hk pipeline in CI (`hk run pre-commit --all --stash none` with `HK_FIX: \"1\"`), over the same tools pinned in `.config/mise/conf.d/shared.toml`, so the operator can regenerate the identical fix set locally instead of downloading it."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/fix.md:3 (`# hk fix`), :8 ('Fixes code'), :16 (`-a --all` run on all files instead of just staged), :40 (`--stash <STASH>` choices git|patch-file|none); repo side: mise.toml:1204-1206 (`[tasks.fmt] run = \"hk fix\"`) and .github/workflows/autofix.yml:63-69 (the CI step that COMPUTES the artifact is `hk run pre-commit --all --stash none` under `HK_FIX: \"1\"`)."
rationale = "The module's value is 'get CI's auto-fixes into my tree'. That is a deterministic recomputation, not a retrieval: the same pinned formatters over the same tree produce the same bytes, and `hk fix --all` does it locally in seconds with no network, no run id, and no dependency on an artifact that upstream sets to expire in ONE day (.agent/kb/raw/autofix-ci-action-artifact.md, `uploadArtifact(\"autofix.ci\",[s],\".\",{retentionDays:1})`). Refactor rather than retire outright because full byte-equivalence between the macOS host run and the ubuntu-latest CI run has not been measured here — the honest end state is: `autofix-apply` first tells the operator to run `mise run fmt`, and the artifact replay survives only as the documented tie-breaker for a fix CI produced that the host cannot reproduce. Settle it with the probe, then retire the module if the trees match."
probe = "gh run list --workflow 'autofix.ci' --json databaseId,headSha,createdAt --limit 5   # pick a run <24h old that produced fixes\nuv run --project python dotfiles-setup autofix-apply <run-id>   # writes CI's fixes\ngit stash && git checkout <headSha> -- . && hk fix --all --stash none && git diff --stat   # local recomputation\n# then diff the two resulting trees: identical => retire the module, different => keep the replay and record WHICH step diverged"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-autofix-2"
unit = "mod-autofix"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Hand-rolled base64/JSON applier for the autofix.ci v1 payload: shape validation (`version != 1`), per-addition base64 decode + `mkdir -p` + `write_bytes`, path-traversal/absolute-path refusal, and an explicit refusal of `deletions`."
evidence = "python/src/dotfiles_setup/autofix.py:26-52 (`apply_autofix_artifact`); tests at tests/test_autofix.py:21-57 (five arms: applies, rejects `..`, rejects absolute, rejects unknown shape, refuses deletions)."
native_alternative = "None. `gh run download` retrieves the artifact but has no apply verb; `git apply`/`git am` consume unified diffs, not autofix.ci's `{version, changes:{additions:[{path,contents:base64}]}}` JSON; hk, mise, uv and the linters in the pin set have no reader for a third-party service's artifact format."
alternative_evidence = "Upstream format confirmed verbatim in .agent/kb/raw/autofix-ci-action-artifact.md (index.dist.js at the pinned SHA c5b2d67: `JSON.stringify({version:1,changes:n,...})`, `uploadArtifact(\"autofix.ci\",[s],\".\",{retentionDays:1})`, and the `additions`/`deletions` construction) — i.e. a bespoke JSON envelope, not a patch; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/docs (gh's run subcommands expose download only, no apply)."
rationale = "Required justification: the decoder binds a vendor-specific wire format that no tool in the pin set parses, and the two safety rules it adds (refuse `..`/absolute paths, refuse deletions) are guards over untrusted-ish CI output that no native applier would supply — a `git apply` of an equivalent patch would happily honour a path outside the tree only because the patch format constrains it differently. Keep is conditional on mod-autofix-1: if the recomputation probe shows `hk fix --all` is equivalent, this decoder goes with the module."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-autofix-3"
unit = "mod-autofix"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "Transport design: recovery is defined over autofix.ci's proprietary artifact, so the repo owns a decoder for someone else's format and inherits its 1-day retention."
evidence = "python/src/dotfiles_setup/autofix.py:59 (`--name autofix.ci`) and :68 (`autofix.json`); .github/workflows/autofix.yml:77 pins autofix-ci/action@c5b2d67 (v1.3.4), which is the only producer of that artifact."
native_alternative = "Have `autofix.yml` also `git diff > autofix.patch` after its fix step and upload it with `actions/upload-artifact` (already used in the same workflow), making recovery two native commands: `gh run download <id> --name autofix-patch --dir .` then `git apply autofix.patch`. That retires `apply_autofix_artifact` entirely and lets the repo choose its own `retention-days`."
alternative_evidence = ".github/workflows/autofix.yml:79-88 (the workflow already uploads an artifact via `actions/upload-artifact@043fb46 # v7.0.1` with `if-no-files-found: ignore`), so the producer half needs one step, not a new mechanism; consumer half is `git apply`, a git builtin. Retention constraint that motivates it: .agent/kb/raw/autofix-ci-action-artifact.md (`{retentionDays:1}` in the vendor bundle at the pinned SHA)."
rationale = "This is the fallback-of-the-fallback and should be judged only after mod-autofix-1: if local recomputation is equivalent, neither transport is needed. If it is not, a self-produced patch artifact is strictly better than decoding a vendor envelope — the repo controls the name, the retention and the format, and both ends become tools we already depend on. Held at 0.5 because it ADDS a CI step (cost on every PR) to remove ~30 lines of Python that currently cost nothing per run."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-autofix-4"
unit = "mod-autofix"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "Failure diagnosis: on a failed download the module prints only `gh run download failed: <stderr>` and returns 1, with no distinction between 'wrong run id', 'this run produced no fixes' and 'the artifact expired'."
evidence = "python/src/dotfiles_setup/autofix.py:65-67 and :69-71; the task description (mise.toml:573) likewise says nothing about a time window."
native_alternative = "`gh api repos/{owner}/{repo}/actions/runs/{run_id}/artifacts --jq '.artifacts[] | {name, expired, expires_at}'` answers all three cases natively — the Actions artifacts API carries an `expired` boolean per artifact — so the module can classify the failure by asking gh instead of echoing stderr."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh (the `gh api` passthrough to the GitHub REST API is the documented escape hatch; the artifacts endpoint's `expired` field is what makes the classification possible). Why it matters here: .agent/kb/raw/autofix-ci-action-artifact.md records the vendor's `{retentionDays:1}`, so 'expired' is the MOST likely failure for any run the operator did not react to within a day."
rationale = "Low-cost, real: the dominant failure mode of this command is a >24h-old run, and the current message points the operator at gh rather than at the cause. One `gh api` call before or after the download turns an opaque rc=1 into 'the autofix.ci artifact for run <id> expired (1-day retention) — run `mise run fmt` instead'. Not a retire: the surrounding command still has to exist for this to be worth doing, and it is subsumed if mod-autofix-1 retires the module."
probe = "gh api repos/ray-manaloto/dotfiles/actions/runs/<old-run-id>/artifacts --jq '.artifacts[] | {name, expired, expires_at}'   # arm both ways: a <24h run must show expired=false, a >24h run expired=true"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-bash_budget-1"
unit = "mod-bash_budget"
kind = "module"
disposition = "keep"
confidence = 0.87
custom_thing = "Per-path ALLOWLIST of sanctioned in-scope shell scripts, each with a written justification, plus the two map-integrity kinds derived from it: `unlisted` (a tracked scripts/*.sh or .devcontainer/scripts/*.sh with no entry) and `stale` (an entry whose path is no longer tracked)."
evidence = "python/src/dotfiles_setup/bash_budget.py:48 (SCOPE_PATHSPECS), :66-124 (ALLOWLIST, 11 entries with `reason` strings), :165-177 (`unlisted`), :189-199 (`stale`); wired by hk.pkl:247-250 (`bash_logic_budget` -> `uv run --project python dotfiles-setup bash-budget`), python/src/dotfiles_setup/main.py:23,648,2406, asserted by python/verification/suites.toml:1389-1404, armed by tests/test_bash_budget.py:43,87,99."
native_alternative = "Nearest natives at the pins, all checked and all insufficient: hk builtin `ls_lint` (ls-lint) constrains what file NAMES may exist in a directory but reads names only and has no per-file allowance or justification field; hk builtin `check_added_large_files` gates added files by BYTE size; editorconfig-checker/shellcheck/shfmt judge shell content quality, never whether a script is permitted to exist. No hk builtin, mise task option, uv/gh/renovate/graphify feature, or marketplace linter (agnix, claude-code-lint, cclint, agents-lint, ast-grep, zizmor, ghalint) provides a per-path allowlist-with-reason plus stale-entry pruning."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ls_lint.pkl:7 (\"Directory and filename naming linter\") and :20 (\"ls-lint only reads names, never contents\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/check_added_large_files.pkl:7 (\"Prevent committing large files\"); the full hk 1.57.0 builtin set was enumerated (139 .pkl files under /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/) and those two are the only Special-Purpose 'may this file exist' steps; .agent/kb/raw/hk-releases-since-pin.digest.md records ZERO new hk capabilities after the 1.57.0 pin."
rationale = "The mechanism being enforced is a POLICY DECISION per path ('bash is acceptable here because it is a devcontainer lifecycle hook / a fail-open shim that must run when Python cannot'), carried as reviewable data. No linter models that: naming linters answer 'is this name shaped right', size linters answer 'is this file big'. The `stale` kind additionally requires diffing the declared map against `git ls-files`, which no builtin does for a hand-written map. Control arm on the enumeration: grepping the same builtins directory for the terms that ARE present ('large', 'naming linter') returned hits, so the 0-result for an allowlist builtin is a real absence, not a broken probe."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-bash_budget-2"
unit = "mod-bash_budget"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Per-file LINE-count growth budget: `BashAllowance.max_lines` is each script's baseline `wc -l`, growth past it fails (`growth`), shrinking always passes; budgets are bumped only in a reviewed diff carrying the reason (see the #673 and #343 inline justifications)."
evidence = "python/src/dotfiles_setup/bash_budget.py:54-59 (BashAllowance), :71-78 and :96-104 (budget bumps with recorded reasons), :147-149 (line_count == wc -l), :178-187 (`growth` violation); tests/test_bash_budget.py:58,71,109."
native_alternative = "hk's `check_added_large_files` builtin (`hk util check-added-large-files {{files}}`) is the only native growth guard at the pin: a single global BYTE ceiling (default 500 KB, `--maxkb`) applied to every added file. It has no per-file budget, no line semantics, and is not a ratchet against a recorded baseline."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/check_added_large_files.pkl:8-15 (glob `**/*`, argv `hk util check-added-large-files {{files}}`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/cli/util/check_added_large_files.rs:5 (`const DEFAULT_MAX_SIZE_KB: u64 = 500`), :11 (`--maxkb`), :50 (`metadata.len() > max_size`) — bytes, one threshold for the whole repo."
rationale = "A 500 KB byte ceiling and a 42-line ratchet on scripts/pretooluse-guard.sh are not the same control: every allowlisted script would pass check_added_large_files forever while doubling in logic. Nothing at the pins (hk builtins, editorconfig-checker's per-LINE max_line_length, ast-grep's pattern matching, mise, uv) expresses 'this specific file may not exceed N lines, and lowering N is free'. Keeping it is the justified answer, not the default one."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-bash_budget-3"
unit = "mod-bash_budget"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "The hk step that invokes this module declares its command as a bare shell string, so hk records the step's effect as unknown."
evidence = "hk.pkl:247-250 — `[\"bash_logic_budget\"] { glob = List(\"scripts/*.sh\", \".devcontainer/scripts/*.sh\"); check = \"uv run --project python dotfiles-setup bash-budget\" }`. Control-armed repo-wide: `grep -n 'CommandSpec|effect =' hk.pkl hk-common.pkl hk-image.pkl` returns 0 hits while `grep -c 'check ='` on hk.pkl returns 34, so no step in this repo has adopted the structured form."
native_alternative = "hk's `Config.CommandSpec` — `check = new Config.CommandSpec { command = ...; effect = \"read\" }` — shipped as 'add command effect safety' in hk 1.55.0 and is present at the 1.57.0 pin. `hk run --safe` (\"Reject commands with unknown or destructive effects before execution\") refuses the ENTIRE run when any step's command has an unknown effect, and hk's own MCP server passes `--safe` on its safe run kinds, so an agent driving hk over MCP cannot run this repo's hooks at all today."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:44 (`typealias CommandEffect = \"read\" | \"write\" | \"destructive\"`), :47-52 (`CommandSpec`, and the note \"Commands that do not use CommandSpec remain valid and have an unknown effect\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/hk.usage.kdl:51 (`flag --safe help=\"Reject commands with unknown or destructive effects before execution\"`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/hook.rs:1890,1907,1919 (`validate_safe_commands`, `effect is unknown` blocker, `--safe refused to run` bail); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/cli/mcp.rs:394-396 (`if kind.safe() { command.arg(\"--safe\") }`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:127,139 (feature landed in 1.55.0, i.e. before the 1.57.0 pin); example of the builtin form: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/check_added_large_files.pkl:9-14."
rationale = "`dotfiles-setup bash-budget` is provably read-only (it only runs `git ls-files` and reads files — bash_budget.py:136-149), so declaring `effect = \"read\"` is free and turns an unknown into a truthful assertion hk can act on. This is not specific to this module — it is a repo-wide hk.pkl adoption whose per-step cost is two lines — but this unit's step is one of the 34 that block `--safe`. Flagged at moderate confidence because the only measured consumer of `--safe` today is hk's MCP server, which this repo does not use; the value is the declaration itself plus future-proofing, not a broken gate."
probe = "uv run --project python - <<'PY'\nimport subprocess\nprint(subprocess.run([\"hk\",\"run\",\"check\",\"--all\",\"--safe\"],capture_output=True,text=True).stderr[:2000])\nPY"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-bash_budget-4"
unit = "mod-bash_budget"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`tracked_in_scope()` re-derives the in-scope file set itself by shelling out to `git ls-files -- scripts/*.sh .devcontainer/scripts/*.sh`, instead of consuming the file list hk already computed for the step."
evidence = "python/src/dotfiles_setup/bash_budget.py:136-144 (subprocess `git -C <root> ls-files -- *SCOPE_PATHSPECS`), :162 (find_violations starts from it), :189-199 (the tracked SET is what makes `stale` computable)."
native_alternative = "hk's `{{files}}` template interpolation — hk filters the staged/changed set against the step's `glob` and passes the matches to the command (`check = \"custom-tool --check {{files}}\"`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/builtins.md:118-121 (custom step with `{{files}}`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:54-55 (\"this will filter the staged files and return the subset matching these globs\")."
rationale = "Adopting `{{files}}` would silently weaken two of the three violation kinds: hk hands over only the files in the current change, so `stale` (an allowlisted path that is no longer tracked ANYWHERE) and the whole-tree invariant asserted by tests/test_bash_budget.py:99 (`set(ALLOWLIST) == tracked_in_scope(REPO_ROOT)`) are both uncomputable from that list. The independent `git ls-files` enumeration is the mechanism that makes the map unable to rot, so it is load-bearing rather than a reinvention. Related nuance checked and found sound: hk's step globs without a `dir` are compiled with `literal_separator=false` (/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/glob.rs:8-10,16-37), so hk's `scripts/*.sh` matches nested `scripts/sub/x.sh` exactly as the git pathspec does — the step's trigger scope and the enforcer's scan scope agree, and no `**` change is needed."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-bootstrap_packages-1"
unit = "mod-bootstrap_packages"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "A hardcoded bad-state allowlist `_BAD_STATES = (\"missing\", \"version_mismatch\")` and the loop that flags any status entry in it, i.e. a re-implementation of mise's own \"is this package out of its desired state\" predicate."
evidence = "python/src/dotfiles_setup/bootstrap_packages.py:30 (`_BAD_STATES`), :67-75 (the per-entry loop); the CI leg that feeds it deliberately drops mise's own exit-code gate: .github/workflows/build-publish.yml:1019-1022 runs `mise bootstrap packages status --json` with NO `--missing` (comment at :1012-1014 says plain `status --json` exits 0 even with gaps, so the JSON is always captured and the Python assert gates). The same predicate IS used natively at build time: .devcontainer/Dockerfile:210-211."
native_alternative = "mise `bootstrap packages status --missing` — mise sets `any_missing` for Missing, NeedsRepair AND VersionMismatch and exits 1, so the bad-state set is mise's own and cannot drift. mise serialises states with `state.replace(' ', \"_\")`, so `needs repair` reaches JSON as `needs_repair`, a value the custom tuple does not contain."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/system/status.rs:66-77 (Missing/NeedsRepair/VersionMismatch each set any_missing), :87 (state string serialisation), :135-137 (`if self.missing && any_missing { request_exit(1) }`); the state enum incl. NeedsRepair at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/system/packages/mod.rs:50-77. Corpus is current for the host: .agent/kb/raw/mise-releases-since-pin.digest.md records zero releases newer than the installed 2026.9.3."
rationale = "Only this half of gap_report_failures duplicates a native predicate, and it duplicates it WEAKER: mise counts `needs_repair` as out-of-desired-state, the custom tuple does not, so such an entry would be reported green by the CI gap report. Today that is latent rather than live — the apt manager never constructs NeedsRepair (grep of NeedsRepair over src/system/packages/apt.rs returns 0 while the same grep shape returns Missing/VersionMismatch hits in that file, and returns NeedsRepair hits in mod.rs:64 and plugin.rs:292, so the 0 is real, not a broken probe) — but the custom list is an allowlist of BAD states, so any state mise adds for apt lands silently in the good bucket. Refactor: have CI run `mise bootstrap packages status --json --missing` (capture the JSON to the artifact path, then read the recorded rc), and reduce the Python check to the two things below that mise cannot do — or, at minimum, invert the test to `state not in {\"installed\", \"skipped\"}` so new states fail closed."
probe = "docker run --rm <image> mise bootstrap packages status --json > /tmp/s.json; echo rc=$?  # then: docker run --rm <image> sh -c 'mise bootstrap packages status --json --missing >/tmp/s.json; echo rc=$?' — confirms --missing still emits the full JSON on stdout before exiting 1"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-bootstrap_packages-2"
unit = "mod-bootstrap_packages"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "Detection of an unexpectedly unavailable apt manager: `if not apt.get(\"available\", False): failures.append(f\"apt manager unexpectedly unavailable: {reason}\")`, plus the absent-manager case that returns a single failure when the report has no `apt` key at all."
evidence = "python/src/dotfiles_setup/bootstrap_packages.py:61-66 (absent key + `available: false`); wired as the CI half of the T4 switch by python/verification/suites.toml:608-619 (`ci.bootstrap-gap-report-wired`) and .github/workflows/build-publish.yml:1006-1022."
native_alternative = "None. `mise bootstrap packages status --missing` explicitly does NOT fail on an unavailable manager: when `unavailable_reason_async()` (or the `system_packages.managers` exclusion) yields a reason, the code emits `{\"available\": false, \"reason\": ...}` and `continue`s WITHOUT touching `any_missing`, so the process exits 0 with the whole apt set unchecked."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/system/status.rs:31-53 (reason -> `json!({ \"available\": false, \"reason\": reason })` then `continue`), contrasted with :135-137 where the exit code is decided only by `any_missing`. Same shape in the aggregate `bootstrap status` collector: .../mise/src/cli/bootstrap.rs:2886-2917."
rationale = "Required justification: this is the exact hole in mise's native gate that the module was written for, and the native source proves the hole rather than merely failing to mention it — an apt manager that goes unavailable inside the image (mise dropping the manager, `system_packages.managers` narrowing, apt-get vanishing from PATH) produces state \"skipped\" for every package and a green `--missing`. No other tool in the repo's stack (hk builtins, zizmor/ghalint on the workflow, check-jsonschema on the config) inspects a runtime status report. Note the shape asymmetry worth preserving: the standalone `packages status --json` unavailable branch emits NO `packages` array (status.rs:38-41), whereas the aggregate `bootstrap status` one does (bootstrap.rs:2899-2917); the module tolerates both via `apt.get(\"packages\", [])`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-bootstrap_packages-3"
unit = "mod-bootstrap_packages"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Bidirectional set-drift check between the host-side declared `[bootstrap.packages]` apt set and the set the in-image status report actually enumerates (`declared - reported` and `reported - declared`), which is what makes an empty or partial report fail loudly instead of trivially green."
evidence = "python/src/dotfiles_setup/bootstrap_packages.py:76-85; docstring rationale at :53-57; regression arms at tests/test_bootstrap_packages.py:96-107 (`test_gap_report_flags_set_drift_both_directions`) and :115-125 (load-bearing declared set)."
native_alternative = "None found. Every native status path derives the declared set from the SAME config resolution it then reports on (`system::packages_from_config(&config)`), so a config-discovery failure inside the image yields zero requests, zero rows, an empty JSON object and exit 0 — mise has no notion of an external expected set to diff against. `mise bootstrap ... status --missing` is the only exit-code gate and it is driven purely by per-package state."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/system/status.rs:24-29 (declared set == the config mise just loaded) and :120-137 (empty `json_out` still prints `{}` and returns Ok); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/system/mod.rs:418-421 (`packages_from_config`)."
rationale = "Required justification: the check's value is precisely that its expected set comes from a SECOND, independent source (the host repo's .devcontainer/mise-system.toml, parsed without mise) while the observed set comes from mise inside the image. Any native mechanism would collapse both arms onto one config load and lose the failure mode it exists to catch — a probe that can only pass. Nothing in the repo's linter/plugin stack (agnix, claude-code-lint, ast-grep, zizmor, ghalint, renovate, hk builtins) compares a declared config table to a runtime report."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-bootstrap_packages-4"
unit = "mod-bootstrap_packages"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`apt_packages_from_bootstrap()` — a 4-line `tomllib` read of `[bootstrap.packages]` that filters keys on the `apt:` prefix and strips it; `APT_PREFIX` is also re-used by the apt-pin module."
evidence = "python/src/dotfiles_setup/bootstrap_packages.py:29 and :33-43; consumed by :76 and by python/src/dotfiles_setup/apt_pins.py:63 (`from dotfiles_setup.bootstrap_packages import APT_PREFIX`); CLI entry at python/src/dotfiles_setup/main.py:24, :1735-1743, :2147-2162."
native_alternative = "mise `config get --file <path> bootstrap.packages` reads one explicitly-named TOML file with no config merging (`ConfigGet.file` -> `resolve_target_config_path`, then a plain `fs::read_to_string` + `toml::de::from_str`), which is the closest native equivalent to this parse."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/config/get.rs:11-30 (the `--file` flag and its semantics), :36-41 and :54-62 (single-file read, key walk), :88-90 (a Table value is printed as TOML text, not JSON)."
rationale = "Required justification: the native call returns re-serialised TOML for a table (get.rs:88-90), so adopting it would trade `tomllib.loads` for a subprocess plus a second TOML parse — strictly more machinery — and it would make the gap report's expected side depend on the very binary whose report it is auditing, destroying the independence that finding -3 rests on. `apt:`-prefix filtering has no native surface at all (mise resolves manager prefixes internally and exposes no 'list declared packages for manager X from file Y' command; `bootstrap packages import`/`prune` operate on INSTALLED packages, per the note already recorded at mise.toml:685-686). Keep as a pure stdlib parse."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-bootstrap_packages-5"
unit = "mod-bootstrap_packages"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "Implicit key-shape policy: any `[bootstrap.packages]` key not starting with `apt:` is silently dropped from the declared set (`if key.startswith(APT_PREFIX)`), so a mistyped manager prefix (`apt-curl`, `atp:curl`) disappears from the expected side — and mise, not recognising the manager either, never reports it, leaving the bidirectional drift check trivially satisfied while the package is absent from the image."
evidence = "python/src/dotfiles_setup/bootstrap_packages.py:41-43 (the silent filter); the drift comparison that a dropped key cannot fail: :76-85. No test asserts behaviour for a malformed key — tests/test_bootstrap_packages.py:61-69 only covers the well-formed `brew:` case."
native_alternative = "JSON-Schema `propertyNames` validation of the table: the vendored mise schema constrains every `[bootstrap.packages]` key to `^[A-Za-z0-9_-]+:.+$`, which rejects `apt-curl` outright. The repo already runs taplo over all TOML (hk.pkl:167-169, `Builtins.taplo`, batch) and pins check-jsonschema, so the validator exists — the question is only whether it is bound to this file, since taplo associates a schema from a `#:schema` directive (no space) while .devcontainer/mise-system.toml:1 carries `# :schema https://mise.jdx.dev/schema/mise.json` (space, remote URL) and there is no .taplo.toml to associate by glob."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/schemas/mise.json:4161-4167 (`\"packages\"` -> `\"propertyNames\": {\"pattern\": \"^[A-Za-z0-9_-]+:.+$\"}`); taplo directive form documented at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/taplo/site/site/configuration/directives.md:16 (`#:schema ./foo-schema.json`)."
rationale = "The native validator and the exact constraint both exist and are cited, but whether they are ACTIVE on the file this module parses is unproven: the file's first line is a commented-out remote directive, not taplo's `#:schema` form, and no .taplo.toml exists. If the probe shows taplo does not flag a malformed key, the fix is native and cheap (make .devcontainer/mise-system.toml:1 a real `#:schema ./schemas/mise.json` directive like mise.toml:1, or add a check-jsonschema step) rather than adding validation code to this module. If taplo already flags it, nothing to do here. Either way the module's silent filter should not grow its own validator."
probe = "cp .devcontainer/mise-system.toml /tmp/ms.toml && printf '\\n[bootstrap.packages]\\naptcurl = \"latest\"\\n' >> /tmp/ms.toml && mise exec -- taplo lint /tmp/ms.toml; echo rc=$?   # control arm (schema known-bound): same bogus key appended to a copy of mise.toml, whose line 1 is '#:schema ./schemas/mise.json' — that run MUST report a propertyNames violation, or the probe itself is blind"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-branch_guard-1"
unit = "mod-branch_guard"
kind = "module"
disposition = "keep"
confidence = 0.88
custom_thing = "Branch-conditional pre-write denial: decide()/handles() deny an Edit/Write/NotebookEdit on a tracked, in-repo path while HEAD is the default branch, exempting git-ignored paths and anything outside the worktree, and failing open."
evidence = "python/src/dotfiles_setup/branch_guard.py:274-293 (decide/handles), :143-155 (is_ignored via `git check-ignore`), :235-271 (_protected); wired at python/src/dotfiles_setup/hook_guard.py:801-807 and .claude/settings.json PreToolUse matcher \"Bash|AskUserQuestion|Edit|Write|NotebookEdit\"; asserted by python/verification/suites.toml:1772 (workflow.branch-guard-enforcement)."
native_alternative = "Claude Code permission deny rules (`Edit(path)` in permissions.deny) + sandbox filesystem denyWrite — the only native pre-write blockers besides hooks."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:316 (file rules are consulted only as `Edit(path)`), :322-328 (rules are gitignore-style PATH patterns anchored to a settings source — no git-state predicate exists); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:997 (\"Environment state: the current branch\" is listed as something a HOOK supplies, not something the harness knows); hooks.md:1544 + :1025 (PreToolUse is the only file-write event with decision control; FileChanged runs after the write and has none)."
rationale = "The capability is a PREDICATE ON GIT STATE, and every native pre-write control in Claude Code 2.1.266 is a static path pattern: permission rules match paths, sandbox denyWrite matches paths, and neither can say 'only while HEAD == main'. The docs themselves place branch awareness on the hook side (hooks.md:997). The exemption half is already native — is_ignored shells out to `git check-ignore` rather than re-implementing gitignore semantics — so there is no homegrown parser to retire. hk cannot substitute: it is a git-hook system that only sees a commit, which is precisely the gap the module's docstring (branch_guard.py:4-15) exists to close."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-branch_guard-2"
unit = "mod-branch_guard"
kind = "module"
disposition = "refactor"
confidence = 0.74
custom_thing = "Tool coverage: _TOOLS is exactly {Edit, Write, NotebookEdit}, so a file written on the default branch by the Bash tool (heredoc, `sed -i`, `>`), by PowerShell, or by any subprocess is never seen — hook_guard routes those to the mise-tasks command matcher, which has no branch check."
evidence = "python/src/dotfiles_setup/branch_guard.py:44 (`_TOOLS = frozenset({\"Edit\", \"Write\", \"NotebookEdit\"})`); python/src/dotfiles_setup/hook_guard.py:801-808 — the `else` arm sends every other tool, Bash included, to `decide(command)`, which is the mise-tasks redirect set only. Live exposure: this repo's own bypass-permissions instruction directs agents to 'make file changes with sed, heredocs, or short scripts, rather than using the dedicated Read, Edit, or Write tools'."
native_alternative = "The `FileChanged` hook event — a filesystem watcher that fires 'no matter what changed the file: an Edit or Write tool call, a script Claude runs with Bash, or a process outside Claude Code entirely', with `watchPaths` settable dynamically from SessionStart/CwdChanged."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2787-2789 (FileChanged semantics), :60 (event table), :1544 ('To run a hook when a specific file changes on disk, whatever wrote it, use FileChanged … they have no decision control, so they can't block the write'), :1025 (FileChanged is in the 'None / no decision control' row), :1153 (`watchPaths` output field), :2828 (dynamic watch list)."
rationale = "Native covers the DETECTION half of the uncovered class and nothing else: FileChanged is the only mechanism that sees a Bash-authored write, but it is async and cannot deny, so it can only warn after the fact — exactly the 'work accumulated on main and nothing said a word' failure the module was built for, caught one step later instead of never. The right shape is to keep the PreToolUse deny for Edit/Write/NotebookEdit and add a FileChanged group calling the same `branch_guard.decide` logic in warn mode over repo-root files, rather than widening _TOOLS to Bash (which would require parsing shell for write targets — the thing the guard deliberately does not do)."
probe = "printf '{\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"echo x >> AGENTS.md\"}}' | uv run --project python dotfiles-setup hook pretooluse; echo \"rc=$?\"   # control arm: the same payload with tool_name=Write and file_path=AGENTS.md must print a deny"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-branch_guard-3"
unit = "mod-branch_guard"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "The originating incident the module encodes: two sub-agents wrote their reports straight into the working tree while the session sat on `main`, and only a write-time guard noticed."
evidence = "python/src/dotfiles_setup/branch_guard.py:9-15 (docstring: 'mise run land leaves you on main; two sub-agents then wrote their reports straight into the working tree there')."
native_alternative = "Subagent frontmatter `isolation: worktree` — runs the subagent in a temporary git worktree 'branched by default from your default branch rather than the parent session's HEAD', auto-removed when it makes no changes; also settable per-call on the Agent tool and for forks."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:305 (frontmatter table entry for `isolation`), :269-271 (subagent Bash/PowerShell also run inside the worktree), :1127 (forks can pass `isolation: \"worktree\"`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/worktrees.md:100-113 (worked `.claude/agents/` example)."
rationale = "Native structurally prevents the exact sub-agent case that produced this module — a subagent with `isolation: worktree` cannot write into the main checkout at all, on any branch — but it does not cover the main session, which is where the guard's other denials fire, and sub-agents.md:873 records a live carve-out (with agent teams enabled, a named subagent launches as a teammate in the main working directory and frontmatter `isolation` does NOT prevent it — and .claude/settings.json sets CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 and teammateMode=auto here). So adopt `isolation: worktree` on the findings-bearing agents in .claude/agents/ as a second layer; do not retire the guard on its strength."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-branch_guard-4"
unit = "mod-branch_guard"
kind = "module"
disposition = "keep"
confidence = 0.72
custom_thing = "Enforcing 'all work belongs on a branch that can become a PR' for the MAIN session, by denying the write rather than by relocating it."
evidence = "python/src/dotfiles_setup/branch_guard.py:68-79 (_REASON: 'branch FIRST, then edit … git checkout -b'), :17-18."
native_alternative = "`claude --worktree`/`-w` — starts the session in an isolated git worktree at `<repo>/.claude/worktrees/<name>` on its own branch (accepts a PR/MR number to branch from)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/cli-reference.md:135 (`--worktree`, `-w`), :131 (`--tmux` companion); worktrees.md:81 ('How Claude Code enforces isolation')."
rationale = "`-w` is a LAUNCH-TIME OPT-IN, not an invariant: it changes where a session that chose it works, and says nothing about the session that did not — including every session started by `claude` in the main clone right after `mise run land` leaves the user on `main`, which is the documented trigger. Nothing in settings can force `--worktree` for interactive sessions (worktrees.md documents isolation for subagents and for sessions launched with the flag; there is no `isolation` setting at session scope), so the guard remains the only layer that fires whether or not the human remembered. Recommend `-w` as a habit; do not retire."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-branch_guard-5"
unit = "mod-branch_guard"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "Git-fact resolution: one combined `git rev-parse --show-toplevel --abbrev-ref HEAD --quiet --verify refs/remotes/origin/HEAD` with a classify() state machine, a separate-invocation fallback, a ('main','master') fallback pair, and a nearest-existing-ancestor probe dir for not-yet-created files."
evidence = "python/src/dotfiles_setup/branch_guard.py:56-66 (_COMBINED_FACTS), :112-128 (_probe_dir), :131-140 (default_branch), :169-184 (_separate_facts), :195-232 (classify)."
native_alternative = "Hook payload fields / harness-supplied repo state (hooks.md common input fields) and `gh repo view --json defaultBranchRef` for the advertised default."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:722 (### Common input fields) and the PreToolUse input example at :729 — the payload carries `session_id`, `transcript_path`, `cwd`, `permission_mode`, `tool_name`, `tool_input`; no repo root, branch, or default-branch field exists at 2.1.266, and hooks.md:997 tells hooks to obtain 'the current branch' themselves."
rationale = "The harness supplies only `cwd`, which is the wrong fact twice over — the target file often sits in a different worktree from the session cwd, and a Write can name a path whose directory does not exist yet (:112-128 exists for exactly that). The native tool for these facts IS `git rev-parse`/`git check-ignore`, and the module already calls it rather than re-implementing it; what is custom is only the batching into one invocation (#527) and the classify() table, which buys the hot path 1 subprocess instead of 3 on every Edit. `gh repo view` is a network call and is unusable in a 20s PreToolUse hook. Nothing to retire."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-child_env-1"
unit = "mod-child_env"
kind = "module"
disposition = "refactor"
confidence = 0.90
custom_thing = "GIT_CONTEXT_NAMES: a hand-maintained frozenset of 6 repository-routing Git variables, stripped by without_git_context() at the package's six live git/gh spawn sites."
evidence = "python/src/dotfiles_setup/child_env.py:36-45 (the 6-name frozenset), :71-74 (without_git_context); live call sites python/src/dotfiles_setup/sync.py:389, sync.py:411, session_state.py:85, session_state.py:155, pr.py:242, pr.py:257"
native_alternative = "`git rev-parse --local-env-vars` — Git itself enumerates every variable that is local to a repository. It is already wrapped in this same package by process_env.git_local_env_names(), whose docstring states the exact justification ('Git owns this list. Deriving it avoids a hand-maintained list that silently misses a variable added by a later Git release')."
alternative_evidence = "Live probe on the host (rc=0) returned 15 names: GIT_ALTERNATE_OBJECT_DIRECTORIES, GIT_CONFIG, GIT_CONFIG_PARAMETERS, GIT_CONFIG_COUNT, GIT_OBJECT_DIRECTORY, GIT_DIR, GIT_WORK_TREE, GIT_IMPLICIT_WORK_TREE, GIT_GRAFT_FILE, GIT_INDEX_FILE, GIT_NO_REPLACE_OBJECTS, GIT_REPLACE_REF_BASE, GIT_PREFIX, GIT_SHALLOW_FILE, GIT_COMMON_DIR. In-repo wrapper: python/src/dotfiles_setup/process_env.py:65-90."
rationale = "Two definitions of one concept live in the same package and the hand-maintained one is the one wired to production. Git's own list is 15 names; GIT_CONTEXT_NAMES has 6, so nine are missed — including GIT_CONFIG, GIT_CONFIG_COUNT and GIT_CONFIG_PARAMETERS, which Git sets for hook children and which can inject arbitrary config (core.hooksPath, include.path) into the spawned git/gh. That is precisely the 'caller's environment redirects the child at the wrong repository' hazard the helper exists to close, so the gap is load-bearing, not cosmetic. Refactor rather than retire: without_git_context() should keep its name and its __MISE_DIFF drop, but source its Git names from process_env.git_local_env_names() (with the current 6 as the closed-failure fallback, since that function raises when git cannot be asked and a partial scrub is the destructive state)."
probe = "uv run --project python python -c \"from dotfiles_setup.child_env import GIT_CONTEXT_NAMES; from dotfiles_setup.process_env import git_local_env_names; print(sorted(git_local_env_names() - GIT_CONTEXT_NAMES))\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-child_env-2"
unit = "mod-child_env"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "CREDENTIAL_NAME / is_credential(): a hand-written name-shaped regex (SECRET|PASSWORD|PASSWD|TOKEN|API_KEY|ACCESS_KEY|PRIVATE_KEY|CREDENTIAL|CREDENTIALS|APP_PASSWORD|CLIENT_SECRET) that decides what clean_env() strips from a child."
evidence = "python/src/dotfiles_setup/child_env.py:49-57 (the regex), :77-92 (clean_env), consumed at python/src/dotfiles_setup/process_env.py:20,102 (git_isolated_env)"
native_alternative = "The repo already carries a reviewed, declarative inventory of exactly which names are credentials — doctor.toml's [fnox].env_true set (56 entries, added in a reviewed diff whenever a secret is added, per .claude/rules/secrets-out-of-the-shell-env.md rule 5) — and fnox ships a names-only enumeration of the same set: `fnox list` (aliases `ls`, `secrets`), which prints values only under the opt-in --values flag."
alternative_evidence = "doctor.toml:39 (`env_true = [` ... 56 quoted names); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/docs/cli/list.md:5 (`fnox list [FLAGS]`, aliases ls/secrets, 'List all secrets') and :14 (`-V --values` — values are opt-in, so a names-only listing is the default)."
rationale = "Measured gap, not hypothetical: of the 56 names doctor.toml declares as ambient (`env = true`), 22 are NOT matched by is_credential(). Most are correctly non-secret (AWS_REGION, the GEMINI_TELEMETRY_* and OTEL_EXPORTER_* endpoints, *_S3_BUCKET, BSKY_HANDLE, GOOGLE_CLIENT_ID), but three are real credentials the name shape cannot see: GITHUB_MCP_PAT (a PAT — 'PAT' is not in the alternation), CT0 (an X/Twitter CSRF cookie), REPO_RECOVERY_AGE_IDENTITY_20260813 (an age identity, i.e. a private key). NVIDIA_20260705 is unclassifiable from its name at all. Do NOT make the runtime path shell out to `fnox list`: fnox's doppler provider shells out to the doppler CLI, which has hung background processes on this host (.claude/rules/secrets-out-of-the-shell-env.md), so a per-spawn subprocess is a hang primitive. The right shape is to keep the regex for runtime and add a test/suites.toml contract that fails when a doctor.toml-declared secret is not matched — turning the reviewed declaration into the control arm the regex currently lacks. Cross-check ONLY on names; never read values."
probe = "uv run --project python python -c \"import re,sys;sys.path.insert(0,'python/src');from dotfiles_setup.child_env import is_credential;names=re.findall(r'\\\"([^\\\"]+)\\\"',re.search(r'env_true\\s*=\\s*\\[(.*?)\\]',open('doctor.toml').read(),re.S).group(1));print([n for n in names if not is_credential(n)])\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-child_env-3"
unit = "mod-child_env"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "The module's own framing: the docstring calls itself 'containment, not the fix', names fnox's `env = \"exec\"` (v1.30.0+) as the real fix, and says 'Until that is set' — i.e. it presents itself as a stopgap awaiting a native mechanism."
evidence = "python/src/dotfiles_setup/child_env.py:9-12"
native_alternative = "fnox's top-level `env = \"exec\"` — 'Only inject into `fnox exec` subprocesses; never the interactive shell' — which would keep credentials out of every child by construction and make this module's clean_env() redundant."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/docs/reference/configuration.md:130 (the three env modes) and :133 ('Setting env = \"exec\" at the top level keeps every secret out of the interactive shell by default — useful when AI coding agents or other tools run in your shell and would otherwise inherit all injected secrets')."
rationale = "The native feature exists and is documented, but its adoption was DELIBERATELY REVERSED by the repo owner on 2026-08-02: all credentials are `env = true` (56 sanctioned, one carve-out for CLAUDE_CODE_OAUTH_TOKEN), with the stated requirement that they be available to all terminals and agents — see .claude/rules/secrets-out-of-the-shell-env.md, whose opening block records the reversal and warns 'do not fix back'. So this module is not a stopgap: it is the permanent boundary, and .claude/rules/secrets-out-of-the-shell-env.md's own note that rules 1/4/7 are 'the only things between 50 credentials and a transcript or a commit' makes it more load-bearing than when it was written, not less. Refactor is doc-only: rewrite lines 9-12 so a future session does not read 'until that is set' as pending work and re-propose the reverted posture. No code change."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-child_env-4"
unit = "mod-child_env"
kind = "module"
disposition = "retire"
confidence = 0.80
custom_thing = "dropped_names(): returns the sorted names clean_env() would remove, 'for logging. Never values.'"
evidence = "python/src/dotfiles_setup/child_env.py:95-98"
native_alternative = "None — this is not superseded by a native feature; it is unwired. It has zero production call sites."
alternative_evidence = "Repo-wide grep for `dropped_names` (--include *.py *.toml *.pkl *.json *.md *.sh, excluding .agent/ and graphify-out/) returns only its own definition at child_env.py:95-96 and tests/test_child_env.py:103-104. Control arm, identical command shape: `without_git_context` in the same sweep returns six production hits (sync.py:389,411; session_state.py:85,155; pr.py:242,257), so the probe discriminates."
rationale = "This is the exact defect the repo has already convicted itself of twice: docs/rules-evidence/secrets-out-of-the-shell-env.md:655-658 records `clean_env()` being cited as an active gate while it had ZERO call sites (it has since acquired one, process_env.py:102). dropped_names() is now the last member of that class in this module — a logging helper nothing logs, kept alive only by its own test, which means the test asserts nothing about the running system. Either wire it (the honest place is a debug log at the git_isolated_env boundary in process_env.py:99-103, names only) or delete it with its test. Retiring is the default answer because an unwired helper is what lets a future doc claim a gate that does not run."
probe = "grep -rn 'dropped_names' --include='*.py' --include='*.toml' --include='*.pkl' --include='*.json' . | grep -v '^./.agent/' | grep -v '^./graphify-out/'"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-child_env-5"
unit = "mod-child_env"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "ENV_DIFF_NAME / without_env_diff(): drops mise's `__MISE_DIFF` (zlib+base64 env delta) from every child environment this package spawns — the default strength at four spawn sites."
evidence = "python/src/dotfiles_setup/child_env.py:33-35, :60-68; call sites python/src/dotfiles_setup/graphify.py:26,283 and graph_bakeoff.py:61,153 (plus transitively every without_git_context() site)"
native_alternative = "None. mise's nearest feature is `redactions` (config/task-level, glob patterns like `SECRET_*`, `*_TOKEN`), which masks values in mise's own captured task OUTPUT — it does not remove anything from a child process's environment, and it is bypassed entirely by `raw = true` tasks. mise exposes no setting to suppress the `__MISE_DIFF` export from an activated shell."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/environments/index.md:144-167 and docs/tasks/task-configuration.md:1635-1651 (redactions = output masking; index.md:193 states raw=true bypasses it). Control-armed negative on a suppression setting: grep -i 'diff' over /Users/.../knowledge-base/sources/mise/settings.toml returns 0 hits while the same file yields 317 '^[' section headers, so the file was read and the absence is real. Second control-armed negative on the harness side: grep -in 'env|secret|redact|credential' over .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md returns 0 against a 48-line file — nothing in the 2.1.258-2.1.266 gap adds child-env scrubbing."
rationale = "Justification, not a default: no tool in the stack offers 'hand me a copy of my environment minus mise's delta blob'. Python's subprocess has no env filter, mise's redactions are output-side, hk/uv/gh have no equivalent, and the Claude Code settings `env` block only ADDS variables. The blob is also uniquely worth removing — it is the single field carrying every other secret at once, in a compressed form no secret scanner can read (gitleaks 2 -> 0, betterleaks 1 -> 0, per .claude/rules/do-not.md #10), which is why the repo's own no_env_dump gate exists. The function is 3 lines, has six-plus live call sites, and cannot break a child because nothing but mise reads the variable and mise re-derives it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-classifier_tables-1"
unit = "mod-classifier_tables"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "The interprocedural axis-derivation engine: `derive_axes` walks the classifier with `ast`, follows same-module callees transitively (`_SubjectWalk.visit`/`_follow`), propagates subject-hood through local rebinding to a fixpoint (`_subject_aliases`), derives which enum classes each axis can decide (`_gated_classes`/`_decision_at`, reading both `If.body`+`If.orelse` and `ast.Match`), resolves enum-holding locals (`_class_holding_locals`), and fails CLOSED on any unreadable return or unfollowable subject flow (`_unreadable_returns`, `unresolved_subject`)."
evidence = "python/src/dotfiles_setup/classifier_tables.py:945 (`def derive_axes`), :550 (`class _SubjectWalk`), :445 (`def _subject_aliases`), :881 (`def _gated_classes`), :911 (`def _decision_at`), :812 (`def _unreadable_returns`); wired at hk.pkl:297 (`check = \"uv run --project python dotfiles-setup classifier-axes\"`) and python/src/dotfiles_setup/main.py:2439 (`\"classifier-axes\": lambda: sys.exit(classifier_axes_main(project_root))`); asserted by python/verification/suites.toml:2145 and tests/test_classifier_tables.py:711 (`test_the_real_registry_is_clean`)."
native_alternative = "None. The two candidate native engines are ruff (which has no third-party/user-authored rule mechanism at all) and ast-grep 0.45.3 (whose rule language is purely syntactic/relational: `pattern, kind, regex, nthChild, range, inside, has, precedes, follows, all, any, not, matches` — node-position matchers with no interprocedural call-following, no alias/dataflow analysis, and no fail-closed 'I could not read this branch' state). ty is a type checker with no custom-rule surface either."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/docs/faq.md:259 — \"Ruff does not yet support third-party plugins, though a plugin system is within-scope for the\" (control arm: `flake8` returns 48 hits in the same corpus, so the grep discriminates). /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/schemas/rule.json — `SerializableRule.properties` enumerates exactly [pattern, kind, regex, nthChild, range, inside, has, precedes, follows, all, any, not, matches]; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/wasm/README.md:173-174 defines `precedes`/`follows` as SIBLING matchers."
rationale = "Required justification: the gate's whole value is deriving an axis set that is a sound over-approximation across call boundaries and refusing a pin whose gated-class set is unknown. Every native linter available at these pins matches single nodes within one file; none carries a call graph, an alias fixpoint, or a fail-closed unknown state, and ruff cannot host a user rule at all. Re-implementing this in ast-grep YAML would silently downgrade it to the syntactic-substring class of check the module's own docstring (:1163-1172) records itself having been convicted of."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-classifier_tables-2"
unit = "mod-classifier_tables"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "The `classifier_axes` hk step's hand-maintained `glob` list, which names 5 paths (classifier_tables.py, dag_tick.py, branch_guard.py, tests/test_dag_tick.py, tests/test_branch_guard.py) and whose comment asserts it 'covers all THREE files that must move together' and 'names BOTH registered classifiers'. REGISTRY now holds SEVEN entries across FOUR classifier modules, and `find_unlisted` scans `python/src/dotfiles_setup/*.py` wholesale."
evidence = "hk.pkl:289-298 (step + glob + check); the uncovered registry targets are python/src/dotfiles_setup/classifier_tables.py:231 (`codex_verdict:edge_for`, table tests/test_codex_verdict.py) and :264/:279/:292/:305 (four `session_ledger` entries, table tests/test_session_ledger.py); SCAN_GLOB at :324 is `python/src/dotfiles_setup/*.py`. Control-armed: `grep -c codex_verdict\\|session_ledger hk.pkl` = 0 while `grep -c dag_tick hk.pkl` = 3, so the absence is real, not a broken grep."
native_alternative = "hk's own step-selection semantics: `glob` is documented as an optional filter over the staged files, so a step that omits `glob` runs on every change. This repo already uses precisely that native form, for precisely this 'it can land in any file' reason, at hk.pkl:307 (`[\"no_env_dump\"] { check = \"…env-blob-scan\" }` — no glob, with the comment 'Deliberately NOT scoped by glob'). No custom code is needed; the fix is deleting the `glob` (or extending it to the four uncovered paths)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:54-55 — \"// this will filter the staged files and return the subset matching these globs\" / `glob = List(\"*.js\", \"*.ts\")`; in-repo precedent hk.pkl:307-309. hk gap digest (.agent/kb/raw/hk-releases-since-pin.digest.md) confirms 1.58.1 adds no new step options, so 1.57.0's semantics are current."
rationale = "The step is the pre-commit-time layer of a gate whose registry outgrew its glob: an edit to `session_ledger.py` or `codex_verdict.py` — or a brand-new classifier anywhere under SCAN_GLOB, which is what the `unlisted` kind exists to catch — changes no globbed file, so the step does not run. Blast radius is bounded because tests/test_classifier_tables.py:711 runs `find_violations(_repo_root())` and always executes, so detection is delayed to the pytest gate rather than lost; but the step's own comment now states something false about its coverage, which is the 'declared but nothing makes it hold' shape the step exists to reject."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-classifier_tables-3"
unit = "mod-classifier_tables"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "`classifier_shaped` / `find_unlisted`: the discovery predicate that flags a function whose return annotation names an enum defined in the SAME module, then diffs the hits against REGISTRY to force the map to grow."
evidence = "python/src/dotfiles_setup/classifier_tables.py:994 (`def classifier_shaped`), :1190 (`def find_unlisted`), :1204 (`registered = {(spec.module_path, spec.function) …}`); armed by tests/test_classifier_tables.py:859 (scan over `_repo_root().glob(SCAN_GLOB)`) and :923 (`test_unlisted_is_silent_for_the_registered_ones`)."
native_alternative = "ast-grep 0.45.3 rule YAML is the only candidate: a `pattern` matching a `def $F(…) -> $E:` combined with a `follows`/`precedes` relational sub-rule matching `class $E(Enum)` as a sibling top-level node. Whether the metavariable `$E` unifies across the main pattern and a relational sub-rule is not stated by the rule schema, and even a working rule emits matches only — the diff against `REGISTRY` still has to happen in this module."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/schemas/rule.json — `SerializableRule.properties` = [pattern, kind, regex, nthChild, range, inside, has, precedes, follows, all, any, not, matches]; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/wasm/README.md:173-174 (`precedes`/`follows` are sibling matchers)."
rationale = "Required justification: the predicate's output is only useful set-differenced against REGISTRY inside the same process (`find_unlisted`:1204-1228), so even a working ast-grep rule would have to be shelled out to and re-parsed, adding a second engine and a second language to save ~40 lines of `ast` that already share `_annotation_name`/`_ENUM_BASES` with the derivation engine. The unification question below is worth settling only if a second, independent consumer of the predicate ever appears."
probe = "printf 'from enum import Enum\\nclass K(Enum):\\n    A = 1\\ndef f() -> K:\\n    return K.A\\ndef g() -> int:\\n    return 1\\n' > /tmp/ag_probe.py && printf 'id: cls\\nlanguage: python\\nrule:\\n  pattern: |\\n    def $F($$$ARGS) -> $E:\\n        $$$BODY\\n  follows:\\n    pattern: |\\n      class $E($B):\\n        $$$M\\n    stopBy: end\\n' > /tmp/ag_rule.yml && mise exec -- ast-grep scan -r /tmp/ag_rule.yml /tmp/ag_probe.py   # PASS arm: matches f() only. Control arm: it must NOT match g(); if it matches both, $E does not unify across the relational rule."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-classifier_tables-4"
unit = "mod-classifier_tables"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`_binds_symbol`: an AST check that the declared truth-table symbol is really ASSIGNED in its file, replacing a `symbol not in source` substring test that a mere comment satisfied."
evidence = "python/src/dotfiles_setup/classifier_tables.py:1163-1187 (`def _binds_symbol`, walking `ast.Assign|AnnAssign|NamedExpr` targets), called at :1290; both arms bound by tests/test_classifier_tables.py (`test_table_missing_rejects_a_mere_mention_of_the_symbol`, `test_table_missing_accepts_a_real_binding`, per suites.toml:2159)."
native_alternative = "The repo's own verification engine (`require_tokens` / `per_path_tokens` in python/verification/suites.toml) is the in-house native mechanism for 'this file must contain this token', and suites.toml:2159 already carries `per_path_tokens` entries such as `\"tests/test_dag_tick.py\" = ['_AXIS_VALUES: dict[', …]`. It is substring-based, so it cannot distinguish an assignment from a comment or a docstring mention."
alternative_evidence = "python/verification/suites.toml:2159 (the `per_path_tokens` block for this contract) and python/AGENTS.md § 'Verification contracts' — \"in 11 of them the sole stand-in was a comment — so a file's own documentation can satisfy a contract about its wiring\"."
rationale = "Required justification: the only native-to-this-repo alternative is strictly weaker along the exact axis this function exists to close. `per_path_tokens` matches text; `_binds_symbol` matches an assignment node, which is what makes `table_missing` a real check rather than one a comment can satisfy. Nothing in ruff, ast-grep, hk builtins or the verify engine at the current pins provides an anchored 'this name is bound here' assertion, so replacing it would reintroduce the documented defect."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codec-1"
unit = "mod-codec"
kind = "module"
disposition = "keep"
confidence = 0.92
custom_thing = "A project-owned msgspec enc_hook/dec_hook pair plus encode()/decode() wrappers that pass them on every call, so the conversion table for unsupported types (seeded with pathlib.PurePath) lives in exactly one module."
evidence = "python/src/dotfiles_setup/codec.py:206-207 (_ENCODERS/_DECODERS seeded with PurePath), :293-336 (enc_hook/dec_hook), :339-384 (encode/decode passing the hooks); callers python/src/dotfiles_setup/session_ledger.py:3246,3253,3727 and python/src/dotfiles_setup/graphify.py:191; contract python/verification/suites.toml:2399-2410."
native_alternative = "None. msgspec still has no native pathlib support (not in supported-types nor in the unreleased changelog), and its hooks are per-call keyword arguments of encode/decode/Encoder/Decoder with no global registration API — so 'one place that owns the table' is not something the library can provide."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/extending.rst:8-25 (hooks are per-call kwargs), :109-110 (passed to Encoder/Decoder), :87 (hooks raise NotImplementedError); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/changelog.md:3-35 (Unreleased: frozendict, decimal_format callable, Literal[True] — no pathlib); grep 'pathlib' over msgspec docs/*.rst + changelog.md = 0 hits with control arm 'datetime' = 70 and 'uuid' = 22 in docs/supported-types.rst (probe saved at .agent/kb/raw/mod-codec-native-probes.md P1/P2)."
rationale = "The justification is the library's own design: because hooks are per-call arguments, every direct msgspec call would carry its own copy of the conversion table, which is the drift #675 exists to prevent — nothing native forces centralisation. And the one type that forces the issue, pathlib.Path, is unsupported in both directions on msgspec main, so upgrading the pin cannot retire this module. Retiring or thinning it would reintroduce the silent-corruption case the tests pin (a Path-annotated field decoding to str)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codec-2"
unit = "mod-codec"
kind = "module"
disposition = "keep"
confidence = 0.72
custom_thing = "A hand-rolled MRO-walking type registry: _ENCODERS/_DECODERS dicts, register()/unregister(), and _lookup() which walks resolved.__mro__ so a base-type registration reaches subclasses (PurePath -> PosixPath)."
evidence = "python/src/dotfiles_setup/codec.py:206-249 (tables + register/unregister), :271-290 (_lookup walks __mro__), :252-268 (_runtime_class unwraps typing.get_origin for parametrized generics)."
native_alternative = "functools.singledispatch (CPython 3.14 stdlib) provides exactly MRO-based dispatch plus a .register() seam and a base implementation that can raise, and would replace _ENCODERS + _lookup + enc_hook for the ENCODE direction."
alternative_evidence = "/Users/rmanaloto/.local/share/uv/python/cpython-3.14-macos-aarch64-none/lib/python3.14/functools.py:875 (singledispatch); live probe (saved .agent/kb/raw/mod-codec-native-probes.md P3): register(PurePath, str) then enc(Path('/tmp/x')) -> '/tmp/x' via MRO, control arm enc(object()) -> NotImplementedError."
rationale = "singledispatch covers only half the seam and breaking the halves apart would destroy the module's stated invariant. dec_hook is handed an ANNOTATION, not an instance — codec.py:314-336 — and an annotation can be a typing._GenericAlias with no __mro__ (codec.py:252-268, review F1), which singledispatch cannot dispatch on at all; it also takes the target type as a second argument (codec.py:87-94), a shape singledispatch's single-argument dispatch does not express. register() deliberately binds both directions in one call (codec.py:210-239) because a half-registration is the silent-corruption case, so adopting singledispatch for encode only would split the atomic registration into two mechanisms with two registries. The dict+MRO walk is ~20 lines and keeps both directions symmetric."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codec-3"
unit = "mod-codec"
kind = "module"
disposition = "keep"
confidence = 0.88
custom_thing = "_native_handling(): deciding whether msgspec already supports a type, to refuse an unreachable registration with AlreadyNativeError."
evidence = "python/src/dotfiles_setup/codec.py:180-196 (_native_handling), :146-177 (AlreadyNativeError), :235-237 (register refuses)."
native_alternative = "msgspec.inspect.type_info + msgspec.inspect.CustomType — the library's own introspection API, which is exactly what this function already calls rather than keeping a hand-maintained list of supported types."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/inspect.rst:21-24 (type_info is the main entry point); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/api.rst:168 (CustomType); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/src/msgspec/inspect.py:318,1041 (CustomType returned for anything unsupported)."
rationale = "The native mechanism is already adopted — this is a five-line wrapper around msgspec.inspect.type_info whose only custom part is mapping CustomType to None and catching the two errors type_info can raise (codec.py:190-196). msgspec offers no 'is this type natively supported?' predicate of its own, so the mapping has to live somewhere; nothing to retire and nothing further to adopt."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codec-4"
unit = "mod-codec"
kind = "module"
disposition = "refactor"
confidence = 0.45
custom_thing = "The tree-clean half of the codec's enforcement: a hand-rolled rglob sweep over python/src/dotfiles_setup/*.py with a regex matcher asserting no module except codec.py imports msgspec, plus its own control-arm test."
evidence = "tests/test_codec.py:486-503 (test_no_module_outside_the_codec_calls_msgspec_directly, rglob + _IMPORTS_MSGSPEC_RE), :506-540 (control arm); the rule-fires half is already delegated natively to ruff TID251 at python/pyproject.toml:106-139 with the per-file allowance at :97."
native_alternative = "ruff TID251 banned-api with the bare module key \"msgspec\" — banned_api() fires from a NameMatchPolicy over import statements, so `import msgspec` in any non-exempt file becomes a lint error, while banned_attribute_access() matches only exact dotted paths and so would leave `msgspec.Struct` free. That is the same assertion the sweep makes, enforced by the linter the repo already runs in hk and CI."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff_linter/src/rules/flake8_tidy_imports/rules/banned_api.rs:44-58 (banned_api over imports) and :60-84 (attribute access requires exact qualified-name equality); saved at .agent/kb/raw/mod-codec-native-probes.md P4."
rationale = "Half the gate is already native (TID251 catches the calls) while the 'no import anywhere else' half is a bespoke filesystem sweep that only runs under pytest and carries its own control arm to stay honest. Adding the bare \"msgspec\" key would move it onto the linter that hk already runs. It is a refactor rather than a retire because the trade-off is unproven: tests/ legitimately declare msgspec.Struct models (tests/test_codec.py:62, tests/test_graphify.py), so the key needs a per-file-ignore for tests, and a blanket TID251 ignore there would also un-ban direct msgspec.json.encode calls in tests — which may be worth keeping the sweep for."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && python - <<'PY'\nimport pathlib,re,subprocess,tempfile,tomllib\np=pathlib.Path('python/pyproject.toml'); orig=p.read_text()\npatched=orig.replace('[tool.ruff.lint.flake8-tidy-imports.banned-api]','[tool.ruff.lint.flake8-tidy-imports.banned-api]\\n\"msgspec\".msg = \"import via dotfiles_setup.codec (#675)\"',1)\nPY\n# then, without committing: apply that key to a scratch copy of pyproject and run\n# uv run --project python ruff check --no-cache python/src tests\n# EXPECT: 0 findings in python/src (the sweep's claim, now native) and N findings in tests/ naming the files that legitimately declare msgspec.Struct — that N is the cost of the refactor."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-codec-5"
unit = "mod-codec"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "encode()/decode() dispatch on a Format enum and call msgspec's module-level json/msgpack encode/decode on every invocation."
evidence = "python/src/dotfiles_setup/codec.py:97-109 (Format), :339-384 (match on Format, module-level msgspec calls)."
native_alternative = "msgspec.json.Encoder / msgspec.json.Decoder (and the msgpack pair) created once and reused — msgspec's own documented performance path, and already permitted inside codec.py by the TID251 per-file allowance."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/perf-tips.rst:11-29 ('create an Encoder once and reuse it for all encoding calls'; decoders are typed so one per type); saved at .agent/kb/raw/mod-codec-native-probes.md P5."
rationale = "The native reuse pattern exists and codec.py is the one module allowed to use it, but it does not pay here: every call site is low-volume and one-shot (session_ledger.py:3246,3253,3727 write/read a single ledger record; graphify.py:191 reads one build receipt), and Decoders are typed, so adopting the pattern means a type-keyed decoder cache — new state and a cache-invalidation question added to a module whose whole value is being the one obvious place. Format itself has no native equivalent: msgspec exposes json/msgpack as separate modules with no format parameter, and the enum is what keeps 'add a wire format' a reviewed decision (the yaml/toml bans at python/pyproject.toml:133-139 depend on it). Revisit only if a hot loop appears."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-1"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "`.claude/agents/codex-*.md` frontmatter must parse and must carry a `name:` key — the `md-no-frontmatter` and the missing-`name` half of the `md-name-mismatch` violation."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:123 (`_FRONTMATTER_RE`), :166-176 (`_frontmatter`), :280-296 (`md-no-frontmatter`, `md-name-mismatch`)"
native_alternative = "agnix rules CC-AG-007 (Agent Parse Error) and CC-AG-001 (Missing Name Field), both HIGH severity, both already running over `.claude/agents/**` via the wired `agnix . --strict` hk step — `.agnix.toml` excludes `.codex/**` but NOT `.claude/agents/**`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/website/docs/rules/index.md:33 (CC-AG-001) and :39 (CC-AG-007); implementation /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/agent.rs:372-406 (CC-AG-007) and :416-450 (CC-AG-001); wiring hk.pkl:498 `check = \"agnix . --strict\"`; scope proof .agnix.toml `exclude` list contains `.codex/**` but no `.claude/agents` entry."
rationale = "Two of the module's five `.md` checks are duplicates of agnix rules that already fail this repo's lint gate at HIGH severity. Drop `md-no-frontmatter` and the name-PRESENCE arm, keep the stem CROSS-CHECK (finding -2, which agnix does not have). This is a partial retirement, not a whole-check one: `_frontmatter()` is still needed to read the value for the stem comparison."
probe = "printf 'no frontmatter here\\n' > /tmp/probe/.claude/agents/codex-probe.md && agnix /tmp/probe --strict; expect a CC-AG-007/CC-AG-001 diagnostic and a non-zero exit, with a control arm on a well-formed agent file that must exit 0."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-2"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`md-name-mismatch`: the `.md` frontmatter `name:` must equal the filename stem."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:288-296"
native_alternative = "None. Claude Code documents the OPPOSITE as permitted — `name` is the identifier and 'The filename doesn't have to match' — and agnix has no name-vs-filename rule for Claude agents (AS-017 'Name Must Match Parent Directory' is Agent-Skills-scoped, and agnix's CHANGELOG records it being scoped OUT for Claude Code skills)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:292 ('The filename doesn't have to match'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/website/docs/rules/index.md:32 (AS-017 category = Agent Skills, the ONLY 'must match' rule in the 454-row table; control arm: `grep -c 'Missing Name Field'` on the same file returns 1, so the 'match' grep discriminates); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/CHANGELOG.md:387 (AS-017 scoped out for Claude Code skills)."
rationale = "Required justification: this is a repo-chosen convention that the harness explicitly does not require, so by construction no native tool will ever enforce it. It is still load-bearing here because the pairing check in `_pairing_violations` keys on the STEM while both harnesses key on the `name` FIELD — without the cross-check, a renamed `name:` would silently detach a lane from its pair while the gate stayed green."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-3"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`name-mismatch` on the codex side: the toml's `name` key must equal the filename stem."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:248-254"
native_alternative = "None — and codex documents the same non-requirement as Claude Code: 'Codex identifies the custom agent by its `name` field. Matching the filename to the agent name is the simplest convention, but the `name` field is the source of truth.'"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/agent-configuration__subagents.md:281-283"
rationale = "Required justification: codex explicitly permits the mismatch, so no codex-native validation can ever fire on it. The check exists to keep the two-surface pairing (which is stem-keyed) honest, which is a property of THIS repo's layout, not of either harness."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-4"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`unparsable`: catching `tomllib.TOMLDecodeError` on each `.codex/agents/codex-*.toml` and reporting it as a named violation."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:242-245"
native_alternative = "hk's `taplo` builtin already lints every tracked `*.toml` in the repo, including these five files (they are the only un-ignored entries under `.codex/agents/`). The builtin's glob is `**/*.toml` and its check is `taplo lint {{files}}`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/taplo.pkl:9 (`glob = \"**/*.toml\"`) and :15 (`argv = List(\"taplo\", \"lint\", \"{{files}}\")`); wiring hk.pkl:167-169 (`[\"taplo\"] = (Builtins.taplo) { batch = true }`); scope proof .gitignore:77-79 un-ignores `.codex/agents/codex-*.toml` only, so the exported mirrors stay untracked and taplo sees exactly the in-scope set."
rationale = "A malformed in-scope toml already fails `mise run lint` at the taplo step, so the bespoke `unparsable` REPORT is redundant. The `tomllib.loads` call itself is not — it is how the module reads `name` and `model_reasoning_effort` — so this is a message-level simplification (let it raise, or keep it as a defensive branch), the lowest-value item in this set."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-5"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "Field-level validation of the codex toml: `model_reasoning_effort` must be exactly `\"xhigh\"` (and, implicitly, the codex-required `name`/`description`/`developer_instructions` keys, which the module does NOT check)."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:83 (`REQUIRED_EFFORT`), :256-267 (`effort` violation)"
native_alternative = "taplo's `#:schema` header directive binds a TOML document to a JSON Schema, and `taplo lint` (already wired, finding -4) then validates against it. A schema in the repo's existing `schemas/` tree could express `model_reasoning_effort: {const: \"xhigh\"}` plus codex's three required keys declaratively, replacing the hand-written comparison and CLOSING the required-key gap the module leaves open."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/taplo/site/site/configuration/directives.md:11-17 (the `schema` header directive, relative path or URL); codex's required-field table at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/agent-configuration__subagents.md:277-283; the repo already vendors JSON Schemas under `schemas/` (schemas/mise.json, ruff.json, typos.json, sources.toml)."
rationale = "Declarative-schema-over-hand-rolled-comparison is exactly the native-first swap `.claude/rules/use-tool-builtins.md` asks for, and it is strictly wider (it would also catch a missing `developer_instructions`, which today ships unvalidated). Two recorded caveats keep confidence below 0.7: a prior in-repo probe found `#:schema` is an ACTIVE directive under the repo's config-less taplo run, so a URL-valued directive would add a network dependency to `mise run lint` (a repo-relative path avoids this, and is untested here); and the schema cannot express the stem cross-check of finding -3, so the module does not disappear."
probe = "Add `#:schema ../../schemas/codex-agent.json` to one `.codex/agents/codex-*.toml` with a local schema pinning `model_reasoning_effort` to const \"xhigh\", then run `mise run lint` (both arms: the correct file must pass, and a copy with `model_reasoning_effort = \"medium\"` must fail taplo), and confirm no network fetch occurs."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-6"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`SENTINEL` (`dotfiles-hand-authored-codex-lane`) presence per toml — the primary 'the Codex Desktop app overwrote our file' gate — plus the secondary whitespace-flattened `CORRUPTION_MARKERS` scan."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:90 (`SENTINEL`), :94-100 (`CORRUPTION_MARKERS`), :219-240 (both checks)"
native_alternative = "No native gate exists. But codex's own importer is idempotent by construction: `import_subagents_with_rewrite_profile` skips any target that already exists, so the codex-rs migration path cannot overwrite a hand-authored `codex-*.toml` — it can only CREATE the non-prefixed mirrors, which is exactly what was observed."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/codex/codex-rs/external-agent-migration/src/subagents.rs:72 (`if target.exists() || is_redirected_destination(&target)? { continue; }`), with the same guard mirrored in the read-only enumerator at :50."
rationale = "Required justification: nothing in codex, Claude Code, agnix or taplo gates 'these bytes are still the ones this repo authored', and the sentinel is the only check in the module that survives an arbitrary rewrite of the file's prose. Worth recording alongside it, though, that the hazard is narrower than the docstring states — the importer in the corpus source skips existing targets, so the realistic failure is a hand edit or a DIFFERENT writer (the Desktop app binary is not in the corpus and its version is not pinned by this repo)."
probe = "Determine whether the Codex Desktop app's writer shares the skip-if-exists guard: `codex --version` for the shipped CLI, then re-run the app's agent import against a scratch clone whose `.codex/agents/codex-advisor.toml` carries a unique marker, and diff. Control arm: a scratch clone with the file DELETED must have it recreated."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-7"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "`_pairing_violations`: every `codex-*` lane must exist on BOTH surfaces (`.claude/agents/*.md` and `.codex/agents/*.toml`), with an explicit fail-loud 'empty' arm so a vacuous pass is impossible."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:179-207 (incl. the `empty` sentinel violation at :184-194)"
native_alternative = "None exposed as a gate. The closest native thing is codex-rs's `missing_subagent_names(source_agents, target_agents)`, which enumerates `.claude/agents/*.md` with no `.codex/agents/*.toml` counterpart — but it is an internal library function behind the migration UX, one-directional (never reports an orphaned toml), and keyed on the source stem."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/codex/codex-rs/external-agent-migration/src/subagents.rs:36-53 (`missing_subagent_names`); no CLI surface for it appears in the codex-cli release digest .agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md, whose `codex agents` entry (0.149.0) is an interactive TASK dashboard, not file validation."
rationale = "Required justification: cross-directory, cross-format pairing between two different vendors' agent layouts is a property of this repo's dual-surface convention; neither harness knows the other's directory is supposed to mirror it, and no linter in the pinned set (agnix, taplo, claude-code-lint, cclint, agents-lint, ast-grep, zizmor, ghalint) does set-difference across two trees. The `empty` arm is a control arm baked into the gate itself and has no native equivalent at all."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-8"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "`.md` content requirements: a `model:` frontmatter key must be PRESENT (value deliberately unchecked), and the body must carry three repo-authored strings — `--model gpt-5.6-sol`, `model_reasoning_effort=\"xhigh\"`, and the no-substitute prohibition — matched on whitespace-flattened text."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:108-121 (`MD_REQUIRED_MARKERS`), :298-317 (`md-no-model`, `md-missing-marker`)"
native_alternative = "mdschema (pinned `aqua:jackchuka/mdschema` 0.15.2, currently UNWIRED) validates markdown declaratively: its schema exposes a `frontmatter` block with required/optional fields (`FrontmatterConfig` / `FrontmatterField`, incl. `enum` for allowed values) and a per-section `required_text` constraint — between them, `model:` presence and the three body strings are expressible without Python. agnix does NOT cover this: CC-AG-003 only validates a `model` value when one is present, never its absence."
alternative_evidence = "`mise exec -- mdschema schema` (probed 2026-09-09, binary at the repo pin) emits `$defs.FrontmatterConfig` (Schema.frontmatter, 'YAML frontmatter validation'), `$defs.FrontmatterField` with `optional`/`type`/`enum`, and `Section.required_text`; hk.pkl:361-363 records mdschema adoption as DEFERRED only because 'mdschema requires authoring a schema file that does not exist yet'; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/website/docs/rules/index.md:35 (CC-AG-003 is 'Invalid Model Value', a value check, not a presence check)."
rationale = "The presence-and-literal-string half of `_md_violations` is the shape mdschema exists for, and adopting it would retire hk.pkl's own recorded 'no schema file exists yet' blocker for a tool the repo already pays to pin. Confidence is held at 0.5 because two things are unprobed: whether mdschema can scope a schema to the `codex-*.md` subset of `.claude/agents/` (rather than all agent files), and whether `required_text` matches across a line wrap the way this module's `flatten()` deliberately does — if it cannot, the reflow defence the module was rebuilt around is lost."
probe = "Author a scratch `.mdschema.yml` requiring frontmatter `name` + `model` and a `required_text` for the prohibition sentence, then `mise exec -- mdschema check --schema <file> .claude/agents/codex-advisor.md` with both arms: the shipped file must pass, and a copy with the prohibition sentence REFLOWED across a line break must also pass (if it fails, mdschema cannot replace `flatten()`), while a copy with the sentence DELETED must fail."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-codex_agent_parity-9"
unit = "mod-codex_agent_parity"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "A hand-rolled frontmatter parser (`_FRONTMATTER_RE` + line `partition(\":\")`) and a `_read` that decodes with plain `utf-8` — the fourth independent frontmatter parser in `dotfiles_setup`."
evidence = "python/src/dotfiles_setup/codex_agent_parity.py:123, :145-152, :166-176; siblings at python/src/dotfiles_setup/rule_registry.py:58, python/src/dotfiles_setup/instructions_report.py:59, python/src/dotfiles_setup/listing_budget.py:86"
native_alternative = "The repo's own hardened reader, `rule_registry._read_source`, which decodes with `utf-8-sig` and documents precisely the failure this module is exposed to: without it 'a BOM'd file's `\\A---\\n` never matches'. Promoting one shared frontmatter helper is the `use-tool-builtins.md` / `mise-tasks-only.md` layering the repo already asks for; no third-party or harness-native markdown-frontmatter reader is wired in this package."
alternative_evidence = "python/src/dotfiles_setup/rule_registry.py:278-292 (`_read_source` docstring naming the BOM failure mode and the deliberate divergence from the BOM-blind sibling); python/src/dotfiles_setup/codex_agent_parity.py:152 (`path.read_text(encoding=\"utf-8\", errors=\"replace\")` — BOM-blind)."
rationale = "Four copies of the same regex have already diverged on a shape one of them documents as real: a BOM'd `.claude/agents/codex-*.md` fails `_FRONTMATTER_RE` here and reports `md-no-frontmatter`. That direction is loud rather than silent, so this is correctness hygiene rather than a live hole — but the divergence is exactly the drift the codec/TID251 precedent in python/AGENTS.md was written to stop, and consolidating removes the flat `partition(\":\")` parser (which mis-reads any quoted value containing a colon) at the same time."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_lane-1"
unit = "mod-codex_lane"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`build_codex_argv` hand-builds a GENERIC `codex exec` invocation and the review target is whatever the caller pipes in as the prompt (`git diff | mise run codex-lane -- --node <id>`); the module carries the ARG_MAX/stdin reasoning for that itself."
evidence = "python/src/dotfiles_setup/codex_lane.py:380-394 (argv: `exec`, `--ephemeral`, `--sandbox read-only`, `--output-schema`, `-o`, trailing `-`); python/src/dotfiles_setup/codex_lane.py:352-378 (docstring: prompt-from-stdin to avoid ARG_MAX on a long diff); mise.toml:668 (`git diff | mise run codex-lane -- --node <id>`)"
native_alternative = "`codex exec review` — a first-class review subcommand that DERIVES the diff itself (`--uncommitted`, `--base <BRANCH>`, `--commit <SHA>`, `--title`) and still accepts the two flags this contract depends on, `--output-schema <FILE>` and `-o/--output-last-message <FILE>`, plus `--ephemeral`."
alternative_evidence = ".agent/kb/raw/codex-exec-help-0.152.1.md:120 (`Usage: codex exec review [OPTIONS] [PROMPT]`), :135 (`--uncommitted`), :138 (`--base <BRANCH>`), :144 (`--commit <SHA>`), :183 (`--output-schema <FILE>`), :189 (`-o, --output-last-message <FILE>`), :174 (`--ephemeral`) — live host binary codex-cli 0.152.1; cross-checked in /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/codex-docs/docs/codex_cli_surface.json (codex_cli_version 0.153.4) where `codex exec` carries subcommands ['fork','help','resume','review']"
rationale = "Native covers PART of this: for the common case (review commit X / review the branch against main) `codex exec review --commit|--base` removes the diff-plumbing the module documents around — no diff on stdin, no ARG_MAX argument, and no race between a diff file and a moving worktree — while keeping the provider-enforced verdict schema that #580/#613 chose. It is NOT a retire, for three verified reasons: (a) custom instructions (the `-` prompt) are mutually exclusive with `--commit`/`--base` (verified by the fable plugin against codex 0.144.1 and still reflected in its comment, ~/.claude/plugins/marketplaces/fable-orchestrator/scripts/run-lane.sh:104-110), so a lane that must carry bespoke review instructions still needs plain `exec`; (b) the subcommand has NO `--sandbox` flag — `READ_ONLY_SANDBOX`, which codex_lane.py:113-119 calls an invariant rather than a knob, would have to move to `-c sandbox_mode=\"read-only\"`, and an unrecognised `-c` key is silently dropped; (c) it has no `--cd`, so the `cwd=` on `subprocess.run` (codex_lane.py:466) stays load-bearing. Treat as an additive lane mode (`--review-target`) rather than a replacement of `build_codex_argv`."
probe = "codex exec review --help | grep -c -- '--sandbox'   # expect 0, control arm: codex exec --help | grep -c -- '--sandbox' == 1;  then arm the config path: printf 'reply ok' | codex exec review --ephemeral -c 'sandbox_mode=\"read-only\"' --commit HEAD -o /tmp/r.json --output-schema /tmp/s.json - ; echo rc=$? ; and the negative arm: same call with -c 'sandbox_modez=\"read-only\"' to see whether an unknown key is dropped silently or refused"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_lane-2"
unit = "mod-codex_lane"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "`build_codex_argv` exposes only `--model`; the lane runs at whatever `model_reasoning_effort` codex's own config happens to carry, even though `.claude/CLAUDE.md` pins `fable-orchestrator: codex effort = xhigh` for every codex lane in this repo."
evidence = "python/src/dotfiles_setup/codex_lane.py:380-394 (argv has no `-c`/effort element; `model` is the only optional knob); python/src/dotfiles_setup/main.py:1636-1641 (`--model` is the only model-shaped CLI arg registered)"
native_alternative = "`codex exec -c <key=value>` config override — `-c model_reasoning_effort=\"xhigh\"` is the documented per-invocation form, and the fable-orchestrator lane script uses exactly it."
alternative_evidence = ".agent/kb/raw/codex-exec-help-0.152.1.md:25-32 (`-c, --config <key=value>` … `Use a dotted path … parsed as TOML`); ~/.claude/plugins/marketplaces/fable-orchestrator/scripts/run-lane.sh:91 (`codex exec --model \"${MODEL:-gpt-5.6-sol}\" -c model_reasoning_effort=\"$EFFORT\"`); .agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md (0.149.0: SDK/CLI config overrides incl. `max`/`ultra` reasoning effort)"
rationale = "The repo's own routing doctrine states an effort level for codex lanes and this producer is the one call site that cannot honour it, so the mandate is satisfied only on the plugin's hand-dispatched path and silently not on `mise run codex-lane`. The native mechanism already exists and needs no custom code — one optional `--effort` CLI arg appended as `-c model_reasoning_effort=<v>`, defaulted from the repo's pinned value. 0.149.0 also added `max`/`ultra` levels, so the accepted set should be read from the binary rather than restated."
probe = "printf 'say ok' | codex exec --ephemeral --sandbox read-only -c model_reasoning_effort=\"xhigh\" - ; echo rc=$?   # control arm: repeat with -c model_reasoning_effort=\"nonsense-level\" and check whether it errors (validated) or is accepted (silently dropped)"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-codex_lane-3"
unit = "mod-codex_lane"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`record_exit` writes the settled signal itself — `EXIT: <code>` appended to `lane.log` by the LAUNCHER, never by codex — and `launch_lane` settles on the failure path too (catching `BaseException`, recording `LAUNCHER_FAILED_EXIT = 70`)."
evidence = "python/src/dotfiles_setup/codex_lane.py:397-420 (`record_exit`, the only writer of lane.log), :137-142 (`LAUNCHER_FAILED_EXIT = 70`), :535-545 (`except BaseException as exc: record_exit(...); raise`), :47-54 (invariant 1: never tee codex stdout into lane.log)"
native_alternative = "Three in-codex completion signals: the `notify` external-program config (event `agent-turn-complete`); codex lifecycle hooks from `hooks.json` or inline `[hooks]` tables (async commands + MCP handlers since 0.148.0, `Interrupt` hooks since 0.150.0); and `codex exec --json` JSONL with `turn.completed` / `turn.failed` events."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/config-file__config-advanced.md:617-623 (`notify` … currently only `agent-turn-complete`), :111-141 (hooks.json / inline `[hooks]`, layered config, project trust); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/non-interactive-mode.md:78-88 (`--json` … `turn.completed`, `turn.failed`); .agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md (0.148.0 async hooks + MCP, 0.150.0 Interrupt hooks)"
rationale = "Justification, not a default: every native signal is emitted BY the codex process, and the invariant this code exists for is the case where that process produces nothing — a launcher that dies before the spawn, a `codex` binary missing from PATH under launchd, a SIGKILL. Those leave the lane `in_progress` with no settled signal, which `codex_verdict.reap` reads as NOT_SETTLED — a no-op edge, forever, watched by nobody (codex_lane.py:502-518). A `notify` program or a hook cannot fire in any of them, and both are CONFIG-layer registrations (`~/.codex/config.toml`), so they would fire for every codex invocation on the host, not only for lanes. `--json` is worse than useless here: it would put model output on the stream the liveness gate substring-matches, which is exactly invariant 1 (a model typing `EXIT: 0` settles its own lane). The only thing native could add is a richer in-band record, and the module deliberately leaves codex's stdout to launchd."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_lane-4"
unit = "mod-codex_lane"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The module's own lane protocol: `lane_run_dir` / `prepare_lane` (mkdir + `fcntl.flock` on `.reap.lock` + clearing `_STALE_ARTIFACTS`), `write_lane_record` (owner/status/rework_count for the reaper's CAS), the rework-budget increment via `previous_round_was_rework`, and `warn_if_node_unknown`."
evidence = "python/src/dotfiles_setup/codex_lane.py:167-180 (lane_run_dir), :304-319 (prepare_lane: flock, increment-then-clear order), :322-349 (write_lane_record), :183-217 (previous_round_was_rework), :560-591 (warn_if_node_unknown); python/verification/suites.toml:2181-2192 (workflow.codex-lane-producer contract)"
native_alternative = "Checked and absent. Codex's own task machinery (`codex agents` interactive dashboard, `codex queue`, `@` task mentions — 0.149.0/0.150.0) manages codex's own threads, not a per-node run directory with an owner/status record. Claude Code 2.1.258-2.1.266 shipped no job/lane primitive (nearest new items are `bashOutputMaxChars`/`taskOutputMaxChars` and a 1 GB tool-result cap). mise, hk, uv and gh offer nothing shaped like a compare-and-swap over a claimed unit of work."
alternative_evidence = ".agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md (0.149.0 `codex agents` dashboard, `codex queue`; 0.150.0 `@` mentions — all TUI/session-scoped); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 capabilities, none job/lane related); .agent/kb/raw/claude-code-llms-index.digest.md (offline claude-code doc tree current through whats-new 2026-w34, zero new capability pages)"
rationale = "Justification: the artifacts here exist to satisfy a CONSUMER inside this repo (`codex_verdict.reap` + `dag_tick.reap_codex_lanes`) whose contract — owner+status CAS under an exclusive flock, `verdict.json` -> `verdict.processed.json` rename for idempotency, a rework counter bounded by `--max-rework` — is not a shape any external tool models. The one native-ish alternative (codex's session/task registry) is keyed by codex thread id, is TUI-oriented, and carries no field the CAS re-verifies. Two of the module's invariants (clear the previous round's settled signals; read the consumed verdict BEFORE clearing) are properties of THIS protocol and have no external owner. Nothing to retire; nothing to wrap."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_lane-5"
unit = "mod-codex_lane"
kind = "module"
disposition = "keep"
confidence = 0.95
custom_thing = "The module's central written justification for not wrapping the fable-orchestrator plugin's `run-lane.sh` — three of the four artifacts the consumer needs are absent from it (mktemp'd outputs so no stable run dir, `--output-last-message` without `--output-schema`, no lane record). Pinned to a read of that script 'as of 2026-08-06'."
evidence = "python/src/dotfiles_setup/codex_lane.py:12-31 (the four-clause justification and its 2026-08-06 read date); mise.toml:661-665 and python/verification/suites.toml:2183 both point AT that docstring rather than restating it"
native_alternative = "The installed plugin's own lane runner, fable-orchestrator 1.21.0: `~/.claude/plugins/marketplaces/fable-orchestrator/scripts/run-lane.sh`."
alternative_evidence = "~/.claude/plugins/marketplaces/fable-orchestrator/scripts/run-lane.sh:84-85 (`FINAL=$(mktemp …)` / `LOG=$(mktemp …)`), :91-94 (codex lane: `--output-last-message \"$FINAL\"`, no `--output-schema`; `echo \"EXIT: $?\" >> \"$LOG\"`), :112-115 (codex-review lane: `--json --output-last-message`, still no `--output-schema`), and no `lane.json`/owner/status write anywhere in its 224 lines; installed version confirmed 1.21.0 via ~/.claude/plugins/cache/fable-orchestrator/fable-orchestrator/1.21.0/scripts/run-lane.sh and .agent/kb/raw/fable-orchestrator-changelog.digest.md (1.21.0 is the TOP changelog entry, so the plugin is not behind)"
rationale = "Re-verified today against the CURRENTLY INSTALLED plugin (1.21.0, i.e. newer than the docstring's 2026-08-06 read) rather than trusting the docstring: all three clauses still hold — mktemp'd output paths, no `--output-schema` on either codex lane, no lane record. So the `use-tool-builtins.md` justification is live, not rotted, and the custom producer stays. Two things a future audit should re-check rather than inherit: the plugin's codex-review lane now passes `--json` and `-c sandbox_mode=\"read-only\"`, so it is drifting TOWARD machine-readable output — the day it adds `--output-schema` plus a stable run dir, clause 1 and 2 die and this becomes a wrap-the-plugin refactor. The docstring's deliberate omission of line numbers remains correct (the file is replaced wholesale on plugin update); the line numbers in this finding are audit evidence with a version stamp, not a new pin."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_lane-6"
unit = "mod-codex_lane"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`LANE_ENV_OVERRIDES = {\"PLANNING_DISABLED\": \"1\"}` merged into the spawn's `env=` so a codex lane never inherits the coordinator's planning-with-files state."
evidence = "python/src/dotfiles_setup/codex_lane.py:121-136 (the constant and its measurement), :463-470 (`env={**os.environ, **LANE_ENV_OVERRIDES}` at the call site); python/verification/suites.toml:1470-1494 (workflow.codex-lane-planning-isolation asserts it AT the call site)"
native_alternative = "Already native: `PLANNING_DISABLED` is planning-with-files' own documented per-invocation opt-out (guarded at the plugin's first line in `hooks/claude-hook.sh` and `scripts/inject-plan.sh`). Codex's adjacent isolation knobs — `--ignore-user-config`, `--ignore-rules`, hook trust (`--dangerously-bypass-hook-trust`), and 0.152.0 disabling codex's own planning tool by default (`tools.update_plan.enabled`) — are a DIFFERENT layer."
alternative_evidence = ".agent/kb/raw/planning-with-files-changelog.digest.md (pinned 3.17.2 IS the top released entry; Unreleased is empty — no newer opt-out mechanism shipped); .agent/kb/raw/codex-exec-help-0.152.1.md:87-101 (`--ephemeral`, `--ignore-user-config`, `--ignore-rules`) and :51-57 (`--dangerously-bypass-hook-trust`); .agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md (0.152.0: planning tool disabled by default via `tools.update_plan.enabled`)"
rationale = "Justification: the custom part here is only WHERE the flag is set (on the spawn, so no brief has to remember it — agent-report-persistence.md §1b), and the flag itself is already the vendor's own opt-out, which is exactly what use-tool-builtins.md asks for. The codex-side knobs do not substitute: `--ignore-user-config` would drop the whole `$CODEX_HOME/config.toml` layer (model, provider, effort) to suppress a Claude-Code plugin's env-triggered hook chain that lives outside codex config entirely, and 0.152.0's `tools.update_plan` governs codex's OWN plan tool, not planning-with-files. Do not conflate the two when refreshing this comment."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_lane-7"
unit = "mod-codex_lane"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "The flag set and its written verification stamp — `--ephemeral`, `--sandbox read-only`, `--output-schema`, `-o`, trailing `-` for stdin — documented as 'verified against the real codex-cli 0.146.0 binary'."
evidence = "python/src/dotfiles_setup/codex_lane.py:352-360 (docstring: 'Verified against the real ``codex-cli 0.146.0`` binary's ``exec --help``'), :113-119 (`READ_ONLY_SANDBOX` described as 'One of the three values `codex exec --sandbox` accepts on 0.146.0')"
native_alternative = "The host binary is codex-cli 0.152.1 (six minor releases on). Every flag the module passes is still present and still means the same thing: `-s, --sandbox <SANDBOX_MODE> [read-only, workspace-write, danger-full-access]`, `--ephemeral`, `--output-schema <FILE>`, `-o/--output-last-message <FILE>`, and `[PROMPT] … (or if '-' is used) instructions are read from stdin`."
alternative_evidence = ".agent/kb/raw/codex-exec-help-0.152.1.md:5 (`codex-cli 0.152.1`), :19-23 (PROMPT positional: `-` reads stdin), :59 (`-s, --sandbox`, three values), :87 (`--ephemeral`), :96 (`--output-schema <FILE>`), :108 (`-o, --output-last-message <FILE>`); cross-checked against /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/codex-docs/docs/codex_cli_surface.json (codex_cli_version 0.153.4, same five options on `codex exec`) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/non-interactive-mode.md:92-120 (`--output-schema` + `-o` as the documented structured-output pair)"
rationale = "Nothing to retire — the module already uses the native structured-output mechanism (`--output-schema`, which is the whole reason #580 rejected prompt-enforcement), and the live host binary confirms none of the five flags moved between 0.146.0 and 0.152.1. The only actionable part is documentary: the two version stamps say 0.146.0 while the pin is 0.152.1, so a reader cannot tell 'still true' from 'never re-checked'. Refresh both stamps to 0.152.1 citing this probe. One capability the module could newly reach if it ever drops `--ephemeral`: `codex exec resume`/`fork` (0.148.0) would let a rework round continue round 1's thread instead of re-reading the diff cold — deliberately out of scope today, since `--ephemeral` is what keeps one transcript per round off disk."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_verdict-1"
unit = "mod-codex_verdict"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "A hand-written JSON Schema dict (VERDICT_SCHEMA) plus a hand-rolled validator that re-implements additionalProperties/required/const/enum/type checking in Python, plus a writer that serialises the dict for `codex exec --output-schema`."
evidence = "python/src/dotfiles_setup/codex_verdict.py:183-192 (VERDICT_SCHEMA literal dict with \"additionalProperties\": False, \"required\", \"const\": SCHEMA_VERSION); :245-269 (_validate_fields: extra-key set diff, missing-key set diff, version compare, Verdict(...) enum coercion, isinstance(rationale, str)); :213-223 (write_schema json.dumps of the dict)."
native_alternative = "msgspec (already a hard dependency and the project's mandated model system, reachable only via dotfiles_setup.codec): declare the payload as `class VerdictPayload(msgspec.Struct, forbid_unknown_fields=True)` with fields `schema_version: Literal[1]`, `verdict: Verdict` (an enum.Enum), `rationale: str`; validate with `codec.decode(raw, VerdictPayload)`; DERIVE the --output-schema file with `msgspec.json.schema(VerdictPayload)` instead of hand-writing the dict."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/structs.rst:726-732 (`forbid_unknown_fields=True` rejects unknown fields); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/src/msgspec/_json_schema.py:427 (`schema[\"additionalProperties\"] = False` emitted when forbid_unknown_fields), :382 and :387 (Literal and Enum both emit `enum`), :419-424 (`properties`/`required` emitted from the struct fields); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/jsonschema.rst:8 (`msgspec.json.schema` generates a complete JSON Schema for a type). Repo mandate: python/AGENTS.md:85-92 (\"msgspec is the project's model system... never call it directly - use dotfiles_setup.codec\"), python/pyproject.toml:41 (msgspec>=0.21.1 dependency), :106-136 (ruff TID251 ban list), python/src/dotfiles_setup/codec.py:362 (`def decode[T](data: bytes, target: type[T], ...)`)."
rationale = "Two of the three pieces are pure re-implementation of what msgspec does natively, and the repo already declares msgspec the single model system - this module is one of only a handful still hand-parsing with stdlib json (only session_ledger.py and graphify.py import codec today). Making the Struct the single source of truth also removes the remaining drift risk this file names: the schema dict and the validator are today two hand-kept statements of one contract (a field added to `properties` without a matching branch in `_validate_fields` validates by accident). The wrapper stays: `parse_verdict` must keep returning (None, why) rather than raising, because the caller escalates with a reason (:272-288), and the empty-file case (:227-229) must stay a first-class branch since codex writes an empty -o file when a run produced no last message. Not 'retire' because of two real caveats that need settling first (see probe): `msgspec.json.schema` hoists struct schemas into `$defs` and returns a top-level `$ref` (_json_schema.py:55-60), and a `Literal[1]` renders as `enum: [1]` rather than the `const` the current file emits and the verification contract pins (python/verification/suites.toml:2178 pins the literal token '\"const\": SCHEMA_VERSION')."
probe = "uv run --project python python -c \"import msgspec,json,enum,typing; class V(enum.Enum): A='approve'\" ; then: uv run --project python python -c \"import msgspec, json; from typing import Literal;\\nclass P(msgspec.Struct, forbid_unknown_fields=True):\\n    schema_version: Literal[1]\\n    verdict: str\\n    rationale: str\\nprint(json.dumps(msgspec.json.schema(P), indent=2))\" and feed the emitted file to a real run: codex exec --ephemeral --sandbox read-only --output-schema /tmp/p.json -o /tmp/v.json - <<< 'reply with an approve verdict' ; echo rc=$? (control arm: repeat with a deliberately invalid schema and require a non-zero rc, so the probe can also fail)"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-codex_verdict-2"
unit = "mod-codex_verdict"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`_read_lane` reads lane.json with stdlib json and hand-checks the decoded value is a dict, then the CAS reads untyped `lane.get(\"owner\")` / `lane.get(\"status\")` off a `dict[str, object]`, and `_mark_reaped` writes it back with json.dumps of a spread dict."
evidence = "python/src/dotfiles_setup/codex_verdict.py:374-384 (_read_lane: read_text -> json.loads -> `return data if isinstance(data, dict) else None`); :475,:492 (`lane.get(\"owner\")`, `lane.get(\"status\")` against an untyped mapping); :601 (`json.dumps({**lane, \"status\": _REAPED})`)."
native_alternative = "The same msgspec/codec path: a `LaneRecord` Struct (owner: str, status: str, ...) decoded with `codec.decode(raw, LaneRecord)` and re-encoded with `codec.encode(...)`. msgspec raises `msgspec.ValidationError` for a non-object or a wrong-typed field, which is exactly the `None -> LANE_UNREADABLE` branch the module already wants, and it types the two CAS fields so a producer/consumer key rename is a type error rather than a silent `None != expected_owner` -> OWNER_MISMATCH -> Edge.NONE (the documented #613 'every lane goes quiet' failure mode)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/structs.rst:726-732; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/src/msgspec/_json_schema.py:419-427; python/src/dotfiles_setup/codec.py:339 and :362 (encode/decode entry points); python/AGENTS.md:85-92 (mandate)."
rationale = "The lane record is the CAS's whole input and it is currently the least typed thing in the module - a `.get()` on an untyped dict cannot fail loudly, and the file itself (:92-97) records that a drifted constant makes every lane STATUS_MISMATCH -> Edge.NONE and silent. Typing it moves that class of drift from 'silently quiet' to a decode-time error the LANE_UNREADABLE branch already escalates. Refactor rather than retire because the surrounding behaviour (tolerate-and-escalate rather than raise, best-effort write-back at :600-603) is deliberate and has no native equivalent; only the parse/serialise half is superseded. The producer (codex_lane._write_lane, python/src/dotfiles_setup/codex_lane.py:337-343) must adopt the same Struct in the same change, or the two halves drift in the direction this module warns about."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-codex_verdict-3"
unit = "mod-codex_verdict"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "`lane_is_settled` - a liveness gate implemented as a substring test for the literal `EXIT: ` in lane.log (or the presence of an exit.marker file), used to decide whether the verdict file is safe to read."
evidence = "python/src/dotfiles_setup/codex_verdict.py:336-371 (`_EXIT_LINE_PREFIX in (run_dir / LANE_LOG_FILENAME).read_text()`), :87-89 (the two filename constants and the prefix), and the docstring's own stated limit at :361-364 (\"`EXIT: 0` proves the CLI returned, never that it did the work\")."
native_alternative = "`codex exec --json` turns stdout into a JSON Lines event stream whose terminal events are `turn.completed`, `turn.failed` and `error` - a provider-emitted settled signal that additionally distinguishes a completed turn from a failed one, which the `EXIT:` echo cannot. Codex 0.148+ also exposes `Stop` / `SessionEnd` lifecycle hooks that can write an authoritative marker when the thread ends."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/non-interactive-mode.md:71-85 (`codex exec --json`; \"Event types include `thread.started`, `turn.started`, `turn.completed`, `turn.failed`, `item.*`, and `error`\" plus the sample JSONL); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/hooks.md:25-27 (`Stop` during a turn, `SessionEnd` when the main thread ends). Producer side, this repo's own: python/src/dotfiles_setup/codex_lane.py:380-393 (`_build_argv` passes --ephemeral/--sandbox/--output-schema/-o and NO --json), :397-421 (`record_exit` hand-writes the `EXIT: <code>` line)."
rationale = "The gate is a hand-rolled settled signal over a log line the launcher echoes, and codex now emits a machine-readable terminal event of its own. It matters beyond tidiness: the module documents that a lane which died on an internal cancellation still exits 0, so `EXIT: 0` cannot discriminate success from a dead turn - `turn.failed`/`error` can, and would let a dead lane escalate on the first tick instead of waiting for the file_missing path. This repo owns the producer, so adopting it is in scope: `_build_argv` simply does not pass `--json` today. Held at 'refactor' rather than higher confidence because the module must ALSO keep serving the fable-orchestrator plugin's run-lane.sh (#575 R4 assigns supervision there, and that script emits only `EXIT:`), so the realistic shape is accepting the JSONL terminal event as a THIRD settled form alongside the two existing ones, not replacing them. The substring-safety property must survive too (:410-412 of codex_lane records that codex stdout must never be teed into lane.log, since a model typing `EXIT: 0` would settle its own lane) - parsing discrete JSONL records is strictly better on that axis than a substring test."
probe = "printf 'say hi\\n' | codex exec --ephemeral --sandbox read-only --json - > /tmp/lane.jsonl 2>/tmp/lane.err; echo rc=$?; jq -r 'select(.type|test(\"^turn\\\\.|^error\")) | .type' /tmp/lane.jsonl  # then the FAILING arm: repeat with --model bogus-model-xyz and confirm the stream ends in turn.failed/error (or that no terminal event is written), so the probe can produce both answers"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-codex_verdict-4"
unit = "mod-codex_verdict"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The reaper proper: an exclusive fcntl.flock around a compare-and-swap that re-verifies lane owner AND status inside the lock, a verdict.json -> verdict.processed.json rename for idempotency, a typed ReapOutcome enum with an exhaustive OUTCOME_EDGES map, and the two deliberately inverted OMC defaults (file_missing / parse_failed escalate instead of warning)."
evidence = "python/src/dotfiles_setup/codex_verdict.py:400-447 (reap: liveness gate then `fcntl.flock(handle.fileno(), fcntl.LOCK_EX)` on an \"a\"-mode handle), :450-499 (_cas_check, ownership -> idempotency -> status ordering), :387-397 (_consume rename), :127-170 (ReapOutcome + the exhaustive OUTCOME_EDGES map), :291-333 (edge_for / demands_rework, the rework-budget bound)."
native_alternative = "None found. The closest natives each cover a strictly smaller problem: `codex exec --output-schema` enforces the shape of a FINAL RESPONSE at the provider but says nothing about a file on disk (and codex explicitly writes an empty -o file when a run produced no last agent message); codex 0.149's interactive `codex agents` dashboard and `codex queue` are operator surfaces for starting/stopping/messaging tasks, not a programmatic exactly-once consumer; codex hooks fire in-process during a session and cannot arbitrate between two reaper ticks in different processes; Claude Code subagents/agent teams return results into a conversation, not into a lockable on-disk record, and cannot host a codex-CLI lane at all. Python's own `fcntl.flock` IS the native primitive here, and the module already uses it directly rather than reimplementing locking."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/non-interactive-mode.md:92-117 (--output-schema constrains the final response only; -o writes the final message to a file); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/hooks.md:25-27 (hook events are session-lifecycle, no cross-process consume/CAS); .agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md (0.149.0 `codex agents` dashboard, `codex queue` - operator/session surfaces, no verdict-consumption primitive; nothing in 0.148-0.152 touches -o/--output-schema semantics)."
rationale = "Required justification: this is exactly-once consumption of a file written by an untrusted, sometimes-aborting producer, arbitrated between concurrent DAG ticks - a concurrency contract, not a serialisation or a CLI-flag problem, and no tool in the pinned set offers one. The module has already adopted every native that does exist (it passes --output-schema/-o rather than prompt-begging for JSON, and it uses stdlib fcntl rather than a hand-rolled lockfile protocol), and the parts that remain custom are precisely the parts the provider cannot enforce: the module's own measured evidence (:26-48) is that codex can write an empty file or no file at all, so 'the provider validated it' is necessary but never sufficient. The two inverted defaults are an operational policy choice for unattended running, which no upstream default can make for us. The one genuine duplication is internal, not native: the flock-on-append-handle idiom is a third copy of the same pattern (dag_tick.try_acquire_lock and codex_lane both carry it, cited in the docstring at :437-439 and codex_lane.py:306-309) - worth hoisting into one helper, but that is in-repo dedup, not a native supersession."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-command_audit-1"
unit = "mod-command_audit"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "encode_cwd()/transcripts_base()/project_dir() re-derive the Claude Code transcript directory by hand: the projects root is guessed from CLAUDE_CONFIG_DIR-or-$HOME/.claude, and the project directory name is computed as re.sub(r\"[/.]\", \"-\", str(cwd)) — only two character classes, no length handling, and no awareness of a name override."
evidence = "python/src/dotfiles_setup/command_audit.py:213-231 (transcripts_base, encode_cwd `re.sub(r\"[/.]\", \"-\", str(cwd))`, project_dir); the only env var read is CLAUDE_CONFIG_DIR at command_audit.py:219; command_audit_main calls transcripts_base() with no arguments at command_audit.py:792, and the SessionEnd hook passes only --output (.claude/settings.json:113), so nothing feeds it the real path."
native_alternative = "Three native mechanisms, any one of which removes the guess: (a) every hook's stdin JSON carries `transcript_path` and `cwd` — sessions.md names this the supported way to get at session data from a script, and a SessionEnd hook receives it; (b) the derivation itself is documented and differs from the module's: `<project>` is the working directory path with EVERY non-alphanumeric character replaced by `-`, truncated to 200 characters with a hash of the full path appended past that; (c) `CLAUDE_CODE_PROJECT_DIR_NAME` (v2.1.234+) replaces the derived name outright, at which point no derivation can be right."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sessions.md:211 (\"React to session events: read the `transcript_path` field that hooks ... receive as input\"); sessions.md:222 (encoding rule + 200-char truncate-and-hash); sessions.md:239 (`CLAUDE_CODE_PROJECT_DIR_NAME`, requires v2.1.234+; host runs 2.1.266); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:730 (`transcript_path` common input field)."
rationale = "The scan legitimately needs the DIRECTORY (it enumerates the last 50 sessions, not just the current one), so `transcript_path` cannot retire the module — but `Path(hook_input[\"transcript_path\"]).parent` is exactly that directory and is authoritative, whereas the current regex is a lossy re-implementation of a documented rule. It happens to be correct for THIS repo only because /Users/rmanaloto/dev/github/ray-manaloto/dotfiles contains no non-alphanumeric character other than `/`. It is already wrong for a cwd containing `_`, a space or `+` (a real cwd with an underscore is recorded in this machine's own transcripts: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/python/src/dotfiles_setup), and for any path over 200 encoded characters it produces a name that cannot exist. Every one of those failures is SILENT: project_transcripts() returns [] and command_audit_main prints 'no transcripts' and exits 0 (command_audit.py:794-799), so an empty audit is indistinguishable from a clean one. Refactor: have the SessionEnd hook pass the transcript directory (read from stdin `transcript_path`), and keep the derivation only as a fallback aligned to the documented rule."
probe = "cd /private/tmp && mkdir -p ca_probe_dir && cd ca_probe_dir && claude -p 'echo probe' >/dev/null 2>&1; ls -d ~/.claude/projects/*ca?probe?dir*   # native rule predicts '-private-tmp-ca-probe-dir'; encode_cwd predicts '-private-tmp-ca_probe_dir'. Control arm: the same command in a dir with no non-alphanumerics must produce a directory both encoders agree on."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-command_audit-2"
unit = "mod-command_audit"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "_executed_ids() infers 'the command actually RAN' vs 'the permission layer refused it' from the SHAPE of the undocumented per-line `toolUseResult` field (a dict containing `stdout` means executed; a bare str means refused), then classify() turns that inference into the report's only alarm (bypass) and into the guard-denial bucket (blocked)."
evidence = "python/src/dotfiles_setup/command_audit.py:314-345 (`if not (isinstance(result, dict) and \"stdout\" in result)`), consumed at command_audit.py:493-497 (`return \"bypass\" if bc.executed else \"blocked\"`); the docstring at command_audit.py:17-19 concedes the schema is \"community-reverse-engineered, not an officially versioned contract\"."
native_alternative = "Claude Code emits the executed/denied distinction natively, twice over. (1) Hooks: `PostToolUse` \"Runs immediately after a tool completes successfully\", with `PostToolUseFailure` for the failing case; these fire inside subagents too, carrying `agent_id`/`agent_type`, so an async `PostToolUse` matcher on `Bash` appending `tool_use_id` + `tool_input.command` to a local log is ground truth with no format parsing. (2) OpenTelemetry: `claude_code.tool_result` whose `decision_type` is \"Always accept, since this event is only emitted after the tool runs. Rejected calls don't produce a tool result\", paired with `claude_code.tool_decision` carrying `decision` (accept/reject) AND `source` (config / hook / user_permanent / user_temporary / user_abort / user_reject)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1919 (PostToolUse \"Runs immediately after a tool completes successfully\"); hooks.md:2024 (PostToolUseFailure); hooks.md:267 (\"When a subagent calls a tool, tool events such as PreToolUse and PostToolUse fire the same configured hooks as in the main conversation\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/monitoring-usage.md:665 (`decision_type` on tool_result); monitoring-usage.md:793-805 (`claude_code.tool_decision`, `decision` accept/reject); monitoring-usage.md:576 (`source`: config | hook | user_permanent | user_temporary | user_abort | user_reject)."
rationale = "Native events supersede the structural guess, and they close a discrimination the module cannot make today: classify() calls EVERY rule-matching non-executed command 'blocked' and credits the PreToolUse guard for it, but a user Esc-abort, a settings deny rule and a guard deny are byte-identical in the transcript — `tool_decision.source` separates them (`hook` vs `user_abort`/`user_reject`/`config`), which is precisely what the 'audit these for false positives' bucket is for. Not a retire: a hook or an OTel exporter records only from the moment it is installed, whereas the whole value of this module is a RETROACTIVE sweep of history the guard predates, and the module's design explicitly rejects a collector (command_audit.py:12 \"Capture is 100% native — no logging hook, no OTel collector\"). The refactor is to add the async PostToolUse Bash log as the authoritative executed-set going forward and fall back to the `toolUseResult` heuristic only for lines older than the log, so a schema change degrades the history rather than silently zeroing the alarm."
probe = "Add a temporary `.claude/settings.local.json` PostToolUse hook, matcher \"Bash\", async:true, appending $(jq -r .tool_use_id) to /tmp/ptu.log; run one allowed Bash call and one the guard denies; assert the allowed id is in the log and the denied one is not, then diff that set against `_executed_ids` over the same session's transcript."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-command_audit-3"
unit = "mod-command_audit"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The module's whole premise: parse ~/.claude/projects/**/*.jsonl line-by-line as a stable data source — _json_lines/_content_blocks/_bash_blocks/iter_bash_commands read `type`, `message.content[].type == \"tool_use\"`, `name == \"Bash\"`, `input.command`, `sessionId`, `timestamp` and `toolUseResult` with no version guard and no schema canary."
evidence = "python/src/dotfiles_setup/command_audit.py:274-305 and 348-369; project_transcripts() rglobs nested subagent transcripts at command_audit.py:263-271; the report is wired to run every session by the SessionEnd hook at .claude/settings.json:110-116 and by mise.toml:701-711."
native_alternative = "The vendor documents this file format as internal and version-unstable and names the supported substitutes: \"The entry format is internal to Claude Code and changes between versions, so scripts that parse these files directly can break on any release. To build on session data, use `/export` or the script interfaces instead.\" The four listed script interfaces are `claude -p --output-format json|stream-json`, `claude -p --resume <session-id>`, the hook/statusline `transcript_path` field, and the Agent SDK."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sessions.md:224 (the warning) and sessions.md:205-213 (the four supported script interfaces)."
rationale = "Justification, not a default: none of the four supported interfaces answers this module's question. `--output-format json` and `--resume` describe ONE run and would cost a model call per session; `transcript_path` hands back the same internal JSONL this module already parses (so it relocates the parse, it does not replace it — see finding 1); the Agent SDK observes a LIVE session, not history; and `/export` renders prose for a human. There is no native 'enumerate every Bash tool call across the last N sessions of this project' surface at 2.1.266. Two mitigations follow from the citation rather than from replacing the module: (a) the parse is already defensive per line, but nothing detects a WHOLESALE schema change — a renamed key yields 0 commands and a clean-looking report, the same silent-zero shape as finding 1, so the report should assert a non-zero floor or a known-key canary and fail loudly; (b) one part of the reverse engineering is now DOCUMENTED and the docstring is stale about it — nested subagent transcripts under `<session-id>/subagents/` are described in hooks.md:2325 (`agent_transcript_path` \"is the subagent's own transcript stored in a nested subagents/ folder\"), which confirms project_transcripts()'s rglob rather than leaving it as measurement-only folklore."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-command_audit-4"
unit = "mod-command_audit"
kind = "module"
disposition = "refactor"
confidence = 0.90
custom_thing = "The SessionEnd hook that makes the loop recurring declares `\"timeout\": 120` for `mise -C ... run command-audit -- --output ...`, and both the module docstring and the workflow.command-audit-wiring contract treat SessionEnd as an unbounded place to do a full transcript sweep."
evidence = ".claude/settings.json:108-117 (SessionEnd entry, `\"timeout\": 120`); python/src/dotfiles_setup/command_audit.py:54-61 (docstring justifying SessionEnd over Stop); python/verification/suites.toml:1885-1903 (workflow.command-audit-wiring pins the SessionEnd wiring). Measured on this host 2026-09-09: the exact hook command completes rc=0 in 4478 ms over 458 transcript files (112 session roots) under the project directory."
native_alternative = "SessionEnd has a hard, documented budget that a per-hook `timeout` cannot exceed: \"SessionEnd hooks have a default timeout of 1.5 seconds... The overall budget is automatically raised to the highest per-hook timeout configured in settings files, up to 60 seconds... To override the budget explicitly, set the `CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS` environment variable in milliseconds.\" The same cap is restated in the handler-field table. The native lever for anything above 60 s is that env var (settable from the settings `env` block), not a larger `timeout`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:3271 (SessionEnd budget, 60 s ceiling, CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS); hooks.md:428 (\"SessionEnd hooks share a 1.5-second budget; if your settings set a longer per-hook timeout, Claude Code raises the budget to match, up to 60 seconds\"); hooks.md:3269 (SessionEnd hooks have no decision control and their JSON output is discarded, so a truncated run reports nothing)."
rationale = "`timeout: 120` states a bound the harness will not grant — it is silently capped at 60 s — so the config documents a guarantee that does not exist. Today the run is 4.5 s and nothing is lost, but the corpus is what grows (the module's own docstring records 2,176 files in this project dir in Aug 2026 against 458 today after the 30-day `cleanupPeriodDays` retention), and the failure is invisible when it arrives: SessionEnd hooks have no decision control and their output is discarded, so a killed sweep leaves the previous .agent/command-audit.md in place and the next reader sees a stale report as a fresh one. Refactor: set the value to a real one (<=60) and, if a longer sweep is ever wanted, use CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS in the settings `env` block rather than a larger per-hook timeout; also have write_report stamp the scan's wall-clock and file count so a truncated or stale report is legible. Note `async: true` is NOT the escape here — an async hook's work is not guaranteed to survive teardown (hooks.md:3600-3606)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-command_audit-5"
unit = "mod-command_audit"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "_FALSE_SIGNAL_SHAPES: four hand-written regexes over raw Bash command text flagging probes whose ANSWER cannot be trusted — `$?` read after a pipeline, `| grep -q` under pipefail, a depth-bounded `find`, and `docker exec ... bash -lc '...'` nested quoting — plus false_signals()/false_signal_advice() and the report section that renders them."
evidence = "python/src/dotfiles_setup/command_audit.py:431-477 (the four shapes and their advice strings) and command_audit.py:741-760 (the 'False signals' report section)."
native_alternative = "shellcheck 0.11.0 (pinned in this repo) is the obvious native shell-shape linter, and it can be run over an extracted command by writing it to a file. Measured control-armed on this host 2026-09-09: `ls | tail -3` followed by `rc=$?` under `set -o pipefail` produced only SC2012 (use find instead of ls) and NOT a $?-after-pipeline diagnostic; `ls | grep -q foo` plus `find . -maxdepth 2 -name x` produced only SC2010 (don't use ls | grep) and nothing about SIGPIPE-under-pipefail or a bounded search. Control arm on the same command shape: an unquoted `echo $UNQUOTED` fired SC2086, so the probe discriminates."
alternative_evidence = "Live probe, /private/tmp/claude-501/.../scratchpad/p1.sh p2.sh p3.sh with `shellcheck -f gcc` (shellcheck 0.11.0, the repo pin from .config/mise/conf.d/shared.toml): 0 of 4 shapes covered, control arm SC2086 fired. No doc citation exists for the negative because the capability is absent."
rationale = "Justification: shellcheck — the only candidate native linter, already pinned and already wired into hk — covers ZERO of the four shapes, measured with a control arm rather than assumed. Three of them are not lint rules at all but epistemic rules about what a PROBE's output means (a bounded find turning 'unreachable' into 'absent'; nested quoting through docker exec mangling a payload; a grep -q match returning 141), and they are this repo's own doctrine from .claude/rules/probes-need-a-control-arm.md, which no third-party tool encodes. ast-grep does not help either: these are one-line ad-hoc shell strings from a transcript, not tracked source, so nothing in the hk/ast-grep tracked-file lane can ever see them — which is exactly the gap this section exists to fill (hk.pkl's no_grep_q_under_pipefail already covers the tracked-file half)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-command_audit-6"
unit = "mod-command_audit"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "fail_open_summary() + _fail_open_section(): parse a bespoke tab-separated log (~/.local/state/dotfiles/guard-fail-open.log, timestamp/reason/cwd) that scripts/pretooluse-guard.sh writes whenever the PreToolUse guard could not run at all, and report the count so an unenforced call is distinguishable from an allowed one."
evidence = "python/src/dotfiles_setup/command_audit.py:597-634 (FAIL_OPEN_LOG, _FAIL_OPEN_FIELDS, fail_open_summary) and command_audit.py:637-663 (_fail_open_section, citing issue #343's 125 unchecked commands)."
native_alternative = "Nothing native records that a hook process failed to start. The closest native surfaces are the exit-code/stderr path (a non-zero hook exit shows stderr to the user for some events, and SessionEnd 'Shows stderr to user only'), `--debug` warnings, and the OTel `claude_code.tool_decision` event's `source` attribute — which names where a decision CAME from (config / hook / user_*) but is emitted only when a decision was actually made, so a guard that never ran produces no event distinguishable from one that allowed the call."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:873 (SessionEnd: \"Shows stderr to user only\"); hooks.md:1025 (Setup, ..., SessionEnd, ... \"No decision control. Used for side effects like logging or cleanup\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/monitoring-usage.md:576 and :793-805 (tool_decision emits only on an actual accept/reject decision)."
rationale = "Justification: the fact being recorded is the ABSENCE of a decision, and every native observability surface is keyed to a decision having been made — tool_decision fires on accept/reject, PostToolUse fires on success, PermissionDenied fires only in auto mode and explicitly \"doesn't run when a PreToolUse hook blocks a call\" (hooks.md:2145). A wrapper that exits 0 because its interpreter was unreachable is, from the harness's point of view, an allow; nothing at 2.1.266 emits a 'hook could not run' signal a script can count. The custom log is therefore the only possible source, and it is per-user state by design because the failure mode is precisely that the repo's toolchain is unresolvable. Worth noting the residual: fail_open_summary reads a log with no upper bound and no rotation, and a partial line is dropped rather than counted (command_audit.py:627-629), so an under-count is possible in the one direction that matters."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-config-1"
unit = "mod-config"
kind = "module"
disposition = "retire"
confidence = 0.85
custom_thing = "MiseConfig.install_path: Path = Path(\"/usr/local/bin/mise\") — a typed setting bound to the ambient MISE_INSTALL_PATH env var (env_prefix=\"MISE_\") that no production code reads."
evidence = "python/src/dotfiles_setup/config.py:21-24 (env_prefix=\"MISE_\", install_path default). Consumer grep for `mise\\.install_path` across python/src + tests returns 0 hits; CONTROL ARM with the identical command shape for the sibling field `mise\\.shell` returns 1 real consumer (python/src/dotfiles_setup/audit.py:324), so the 0 is the world, not a broken probe. The only reader is its own test, tests/test_config.py:54-57."
native_alternative = "mise's own MISE_INSTALL_PATH contract (installer default $HOME/.local/bin/mise) plus `shutil.which(\"mise\")`; the repo already consumes mise's variable directly where it actually matters, at python/src/dotfiles_setup/image_lock.py:242 (\"MISE_INSTALL_PATH\": str(install_path))."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/packaging/standalone/install.envsubst:282 — `install_path=\"${MISE_INSTALL_PATH:-$HOME/.local/bin/mise}\"`; same contract at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/packaging/mise.run/shell.envsubst:43. Repo-side native use: python/src/dotfiles_setup/image_lock.py:238-256."
rationale = "Dead typed config with a value that contradicts mise's own default (/usr/local/bin/mise vs $HOME/.local/bin/mise), kept alive only by a test that asserts the constant back to itself — a tautological test over an unread field. mise already owns the variable and the one place that needs it sets it directly. Deleting the field and its test removes a stale fact that a future reader would trust."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-config-2"
unit = "mod-config"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "The whole ContainerConfig block (image=\"dotfiles-dev-local\", base_image, ssh_port) exists to feed DockerManager's build/up/down/test wrapper around container lifecycle."
evidence = "python/src/dotfiles_setup/config.py:27-35; consumed at python/src/dotfiles_setup/docker.py:302-303 (self.image_name / self.base_image) and docker.py:408 + audit.py:535 (ssh_port). No mise task invokes `dotfiles-setup docker {build,up,down,test}` — the CLI still registers `docker test` at python/src/dotfiles_setup/main.py:837,1873-1874. CONTROL ARM, same grep shape: the docker subcommands that ARE wired do appear — mise.toml:939 (`docker sync`), mise.toml:949 (`docker verify-latest`), mise.toml:1014 (`docker verify-ssh-inbound`), .devcontainer/devcontainer.json:235 (`docker initialize-host`)."
native_alternative = "@devcontainers/cli 0.89.0 (pinned in mise.toml) driven directly by the mise tasks `up` / `down` / `dev-rebuild`, which is the documented replacement for this wrapper."
alternative_evidence = "AGENTS.md:35-37 — \"The devloop is `mise run up` -> work inside the container -> `mise run down`. The legacy `dotfiles-setup docker {up,down}` wrapper has been replaced by the official `@devcontainers/cli` (pinned in `mise.toml`).\" The live path: mise.toml:241 (task description \"Bring the devcontainer up via the official @devcontainers/cli\") and mise.toml:358 (`devcontainer up --workspace-folder . --id-label ...`), which never touches DockerManager."
rationale = "The native mechanism (the official devcontainer CLI under mise tasks) has already replaced the surface these settings configure, so they are configuration for a dead lane — and a live-looking one, since `docker test` is still a registered subcommand. The unit itself cannot be retired outright: deciding the fate of DockerManager.build/up/down/test is a docker.py/main.py change, so config should shed these fields as part of that, not before it. Note the collateral risk that keeps this from being a pure delete-later: docker.py:339 still runs a classic `docker pull` of the ~38GB base, which the project's own guidance (.claude/rules/verify-before-advancing.md, \"never classic docker pull, which wedges on the ~38GB image\") forbids."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-config-3"
unit = "mod-config"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "ContainerConfig.ssh_port: int = 4444, read from DOTFILES_SSH_PORT — a second, independent declaration of the devcontainer's SSH port."
evidence = "python/src/dotfiles_setup/config.py:30,35 (env_prefix DOTFILES_, ssh_port default 4444). Re-exported into the container at python/src/dotfiles_setup/docker.py:411 (`export DOTFILES_SSH_PORT={ssh_port}`) and used for an ssh round-trip at python/src/dotfiles_setup/audit.py:535-555. Nothing in mise.toml, .devcontainer/devcontainer.json or the shared fragment ever sets DOTFILES_SSH_PORT (grep across *.toml/*.json/*.md/*.py: only config.py, docker.py:411 and tests/test_config.py:41,153-154); CONTROL ARM, same grep shape: DEVCONTAINER_SSH_PORT returns the real wiring at mise.toml:286,352,1006 and devcontainer_names.py:107."
native_alternative = "The #677 derived-port resolver already exposed as a mise task: `mise run ssh-port` -> `dotfiles-setup devcontainer name port` (python/src/dotfiles_setup/devcontainer_names.py:107,178-186), which R1's own gate is required to ask rather than restate a number."
alternative_evidence = "mise.toml:329-334 ([tasks.ssh-port] \"Derived from the workspace path and the arch ... Pin one per-clone with DEVCONTAINER_SSH_PORT\"); mise.toml:1010-1014 — \"Resolved, not defaulted (#677): the port is derived from workspace+arch unless DEVCONTAINER_SSH_PORT pins one, so R1 must ask the same resolver `mise run up` asked rather than restating a number.\" AGENTS.md:171 records the port as derived and \"Unset by default (#677)\", with the container's sshd hardcoded at 2222."
rationale = "This is the exact split-brain the #677 resolver was built to end: one port chosen by a Pydantic default that nothing sets, another derived per workspace+arch. Worse, the consumer is a probe that appears to have only one face — audit.py's round trip runs INSIDE the container (docker.py:405-420 execs it there) against localhost:4444, while the container's sshd listens on 2222, so the check can only report failure. config should drop the field and the two consumers should call devcontainer_names.ssh_port() (or take the port as a parameter), matching what verify-ssh-inbound already does."
probe = "container=$(uv run --project python dotfiles-setup devcontainer name container) && docker exec \"$container\" sh -c 'ss -ltn | grep -E \":(2222|4444)\\b\" || echo NO-LISTENER' && docker exec \"$container\" sh -c 'ssh -p 4444 -o BatchMode=yes -o ConnectTimeout=5 localhost true; echo rc=$?'"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-config-4"
unit = "mod-config"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "ContainerConfig.base_image = \"ghcr.io/ray-manaloto/dotfiles-devcontainer:dev\" — a Python-side copy of the base-image reference, reachable only as DOTFILES_BASE_IMAGE because of the env_prefix."
evidence = "python/src/dotfiles_setup/config.py:30,33; a further unused duplicate of the same literal at python/src/dotfiles_setup/docker.py:285 (DEFAULT_BASE_IMAGE, 0 other references). The documented override for this value is the UNPREFIXED name: mise.local.toml.example:27-28 (\"BASE_IMAGE — pin to a specific tag/SHA for reproducible rebuilds. Default ghcr.io/ray-manaloto/dotfiles-devcontainer:dev.\") and mise.toml:286,352,381, so `BASE_IMAGE=... mise run ...` cannot reach ContainerConfig at all. Tracked copies of the literal outside docs: .devcontainer/Dockerfile.host-user:5, mise.toml:286/352/381, config.py:33, docker.py:285, scripts/benchmark-docker.sh:13."
native_alternative = "mise's own [env] Tera templating with a default — `BASE_IMAGE = \"{{ env.BASE_IMAGE | default(value='...') }}\"` — as the single declaration, consumed by the Dockerfile ARG; plus the repo's existing single-parameter enforcement pattern, the `no_platform_literals` hk step, which holds DOTFILES_PLATFORM's one default in mise.toml [env] and rejects the literal appearing anywhere else."
alternative_evidence = "mise env templating: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/environments/index.md:628-630 (\"## Templates — Environment variable values can be templates\") with the default-filter form at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/mise-cookbook/nodejs.md:63. Repo-side enforcement precedent: hk.pkl:268 ([\"no_platform_literals\"]), described in python/verification/suites.toml:143 as rejecting \"a literal re-appearing anywhere else\" and holding the mise default equal to docker-bake.hcl's mirror."
rationale = "The project already solved this exact class once (#673, one platform parameter + a gate that forbids literals); the base-image ref never got the same treatment, so config.py holds one of six tracked copies and reads it under a name (DOTFILES_BASE_IMAGE) that no task, no example file and no bake target uses — a per-clone `BASE_IMAGE` pin silently does not apply to the Python path. Either read the unprefixed BASE_IMAGE mise already exports (validation_alias) or drop the field with the legacy DockerManager lane (mod-config-2), and extend the literals gate to the image ref."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-config-5"
unit = "mod-config"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "The settings stack itself: pydantic-settings BaseSettings + SettingsConfigDict(env_prefix=...) + pydantic Field(default_factory=...) as the mechanism for prefixed, nested, typed env config."
evidence = "python/src/dotfiles_setup/config.py:14-15 (imports), :18-35 and :74-89 (the three BaseSettings models); declared dependencies at python/pyproject.toml:10-11 (pydantic>=2.13.4, pydantic-settings>=2.15.0). Import cost is already pinned as a known defect surface: tests/test_hook_guard.py:753-756 asserts \"dotfiles_setup.config\", \"pydantic_settings\" and \"pydantic\" are all resident in the PreToolUse guard's fresh-interpreter import surface."
native_alternative = "msgspec — already pinned (python/pyproject.toml:41) and already declared the project's one model system, routed through dotfiles_setup.codec; the migration is specified and tracked as issue #683 (\"load settings onto the generated model type, dropping the old validation stack\", blocked by #675, which has landed)."
alternative_evidence = "python/AGENTS.md:85-88 — \"## Serialization: route every call through `codec` (#675) — `msgspec` is the project's model system (#669). Never call it directly\"; python/AGENTS.md:107 lists pydantic as \"(config; leaving the tree in #683)\". Issue #683 body (gh issue view 683, state OPEN) carries the ACs: prefixes/nesting/defaults/coercion preserved, a malformed value must NAME the field, both previous dependencies gone, consumers unchanged."
rationale = "Not a retire: #683's own body records that the research was done and no maintained settings library exists for msgspec, so the replacement is custom code and needs the written justification .claude/rules/use-tool-builtins.md demands — pydantic-settings is currently the *existing tool*, which is the rule-compliant choice until #683 actually lands. What makes it a refactor rather than a keep is that this module is now the sole reason two model systems are in one tree, and it is the measured cause of pydantic loading inside every PreToolUse guard invocation (tests/test_hook_guard.py:756). Confidence is capped at 0.6 because the error-message AC (a malformed value must name the offending field) is the part msgspec does not give for free."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-config-6"
unit = "mod-config"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "host_state_dir() — a three-tier resolver (explicit config value -> DOTFILES_HOST_STATE_DIR -> DEVCONTAINER=true ? /tmp/dotfiles-host-state : ~/.local/state/dotfiles) plus the two path constants."
evidence = "python/src/dotfiles_setup/config.py:43-71; consumers at python/src/dotfiles_setup/devcontainer_names.py:51,1116 (composes the doppler env-file path) and python/src/dotfiles_setup/docker.py:96,227,239. The docstring at config.py:48-57 records why it lives in this leaf module (#893: devcontainer_names needs it, and docker already imports devcontainer_names, so keeping it in docker forced a function-level import to dodge a cycle)."
native_alternative = "None. The one thing that could have been sniffed — am I inside the container — is already taken from a native fact the devcontainer spec publishes rather than inferred: containerEnv sets DEVCONTAINER=true."
alternative_evidence = ".devcontainer/devcontainer.json:131,209-212 — \"containerEnv\": { ... \"DEVCONTAINER\": \"true\" } with the in-file comment naming it \"the explicit signal\". No mise/hk/uv/devcontainer-CLI feature maps a host directory to its in-container bind-mount path: the digests for the tools that could plausibly own it are empty of anything relevant (.agent/kb/raw/mise-releases-since-pin.digest.md — zero releases newer than the host's 2026.9.3; .agent/kb/raw/hk-releases-since-pin.digest.md — v1.58.1 is documentation-only; .agent/kb/raw/uv-releases-since-pin.digest.md — publish/pylock changes only)."
rationale = "Justified, not defaulted: the value is a repo-specific pairing between a host path and the container mount that R1's sshd reads authorized_keys from, which is knowledge no tool has — mise [env] could export it for task-launched processes, but the function is called from library paths that do not run under a mise task (devcontainer.json's initializeCommand, and devcontainer_names when composing the secrets env-file path), so a mise-only declaration would be absent exactly where it is needed. Its container-vs-host discriminator already reads the devcontainer spec's own containerEnv signal instead of sniffing /.dockerenv, which is the native-first shape this repo asks for."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-container-1"
unit = "mod-container"
kind = "module"
disposition = "refactor"
confidence = 0.90
custom_thing = "_run_smoke() executes the tier-1-3 smoke with RAW `docker exec --workdir <bind-mount dest> <container-id> scripts/devcontainer-smoke.sh`, hand-resolving the container id and the workdir itself."
evidence = "python/src/dotfiles_setup/container.py:108-119 (the `[\"docker\", \"exec\", \"--workdir\", workspace_dest, container_id, \"scripts/devcontainer-smoke.sh\"]` argv); container.py:95-105 (_bind_mount_dest exists largely to supply that --workdir); reached from python/src/dotfiles_setup/sync.py:804 (`mise run sync` / `mise run land` verification) and python/src/dotfiles_setup/main.py:1881-1882 (`mise run verify-container-latest`)."
native_alternative = "`devcontainer exec --workspace-folder <ws> --id-label <k=v> --id-label <k=v> scripts/devcontainer-smoke.sh` from the pinned npm:@devcontainers/cli 0.89.0: it looks the container up by id-label, runs in the configured remoteWorkspaceFolder as remoteUser, and applies userEnvProbe (default loginInteractiveShell) + remoteEnv. The repo already wraps exactly this in-tree as `image_lock.devcontainer_exec_prefix()`, and the standalone smoke task already uses it."
alternative_evidence = ".agent/kb/raw/devcontainer-cli-0.89.0-help.md:26-28 (exec subcommand), :44 (--workspace-folder), :48 (--id-label lookup), :55 (--default-user-env-probe, default loginInteractiveShell), :56 (--remote-env) — captured live from the pinned 0.89.0 binary; python/src/dotfiles_setup/image_lock.py:322-338 (`devcontainer_exec_prefix`, whose docstring cites do-not.md #3 for exactly this reason); mise.toml:405-411 (`mise run smoke` runs the same script through `devcontainer exec --workspace-folder . $DEVCONTAINER_ID_FLAGS`); .claude/rules/do-not.md:16-18 (raw docker `exec` is a listed project invariant violation; `docker ps/inspect` inspection stays allowed)."
rationale = "Two routes run the SAME smoke script against the SAME container by different mechanisms, and they are documented to have disagreed: .claude/rules/persistence-gate-retry.md:51 and :53-61 record two 2026-09-03 cases where `mise run land` (which reaches container.py's `docker exec` via sync.py:804) reported `FAIL smoke-tiers-1-3` while standalone `mise run smoke` (devcontainer exec, mise.toml:411) returned rc=0 on the same container — the rule's own discriminating probe is 'run the devcontainer-exec route instead'. `docker exec` skips remoteUser, remoteEnv and userEnvProbe, so PATH/env inside the smoke differ from the environment the devloop actually validates in; that is a false-fail (and potentially false-pass) generator, not a style nit. Swap the argv for `image_lock.devcontainer_exec_prefix(workspace)` + the script name; `_bind_mount_dest` then survives only as the assertion (finding mod-container-4), not as a workdir supplier. Keep `verify_latest`'s signature — sync.py:804 and tests/test_sync.py depend on it."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && eval \"$(uv run --project python dotfiles-setup devcontainer env)\" && devcontainer exec --workspace-folder . $DEVCONTAINER_ID_FLAGS scripts/devcontainer-smoke.sh; echo \"dc_exec_rc=$?\"  # then the control arm on the same live container: docker exec --workdir \"$(docker inspect \"$(docker ps -q --filter label=$DEVCONTAINER_WORKSPACE_LABEL)\" --format '{{json .Mounts}}' | jq -r 'map(select(.Type==\"bind\"))[0].Destination')\" <id> scripts/devcontainer-smoke.sh; echo \"docker_exec_rc=$?\" — the two rcs must agree; a disagreement IS the defect."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-container-2"
unit = "mod-container"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "A hard-coded in-process 1800s wall clock on the smoke subprocess (`_SMOKE_TIMEOUT_S`), passed to subprocess.run — with NO `except subprocess.TimeoutExpired` anywhere in container.py or its main.py dispatch, so expiry raises a traceback instead of producing the FAIL Check the docstring promises ('bound it so a hang surfaces instead of blocking'), and `[tasks.verify-container-latest]` carries no outer bound at all."
evidence = "python/src/dotfiles_setup/container.py:47-49 (constant + rationale comment), :108-119 (`timeout=_SMOKE_TIMEOUT_S`), :61-66 (`_run` re-raises); `git grep -n TimeoutExpired -- python/src` lists audit.py, dag_project.py, doctor.py, hook_selfcheck.py, lint.py, path_drift.py, pr.py, reap.py — and NOT container.py (control arm: the same grep shape returns 10 hits elsewhere, so the 0-hit for container.py is real); mise.toml:941-949 (`[tasks.verify-container-latest]` declares no `timeout`)."
native_alternative = "mise's native per-task `timeout` key (and `mise run --timeout` / the `task.timeout` setting) — 'The task fails if it does not complete within the configured duration', shorter-of-the-two semantics when both a global and per-task bound are set."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060-1077; live control arm on the installed binary (mise 2026.9.3, the newest published release per .agent/kb/raw/mise-releases-since-pin.digest.md): `mise run --help` line 162 → `--timeout <TIMEOUT>  Timeout for the task to complete`; already proven in this repo at mise.toml:207-213 (`[tasks.lint] timeout = \"700s\"`, with the documented decision that the native bound is an outer backstop and the Python wrapper stays because it adds diagnostics the native kill cannot)."
rationale = "Native covers the OUTER bound only. Follow the `[tasks.lint]` precedent verbatim: add `timeout = \"35m\"` (above the in-process 1800s) to `[tasks.verify-container-latest]` so a wedge inside the Python process itself still fails loud, and keep the in-process bound because only it can render a per-check `FAIL smoke-tiers-1-3` line with the `dev-rebuild` hint. The retire condition for the in-process bound is full equivalence, which does not hold. Independently of the native adoption, the missing `except subprocess.TimeoutExpired` should be closed — today the constant buys a traceback, not the surfaced hang it documents."
probe = "uv run --project python python -c \"import dotfiles_setup.container as c, subprocess, pathlib; c._run=lambda cmd,**k: (_ for _ in ()).throw(subprocess.TimeoutExpired(cmd,1.0)) if cmd[:2]==['docker','exec'] else subprocess.CompletedProcess(cmd,0,'x',''); print(c.verify_latest(pathlib.Path('.')))\"  # today: TimeoutExpired escapes; after the fix: a FAIL Check"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-container-3"
unit = "mod-container"
kind = "module"
disposition = "keep"
confidence = 0.78
custom_thing = "Container discovery + identity-uniqueness: `docker ps -q --filter label=dotfiles.workspace=<hash> --filter label=dotfiles.arch=<arch>` and the hard failure when the result count is not exactly 1 ('N running containers share this workspace+arch identity; run `mise run down`')."
evidence = "python/src/dotfiles_setup/container.py:79-92 (`_running_container_ids`), :139-160 (the not-running and not-unique short circuits); labels come from python/src/dotfiles_setup/devcontainer_names.py:244-256 (`workspace_label`/`arch_label`)."
native_alternative = "`devcontainer exec --id-label` performs the same label-based lookup natively ('If no --container-id is given the id labels will be used to look up the container'), and infers one from --workspace-folder when none is passed — but it exposes no 'exactly one match' assertion, no machine-readable listing, and no way to report the ambiguity; `docker ps` for inspection is explicitly permitted by the project invariant that bans raw docker for lifecycle."
alternative_evidence = ".agent/kb/raw/devcontainer-cli-0.89.0-help.md:48 (--id-label lookup semantics) and :15 (the CLI's full subcommand list — no `ps`/`list`/`status` verb exists in 0.89.0); .claude/rules/do-not.md:16-19 ('Raw `docker ps/logs/info` for inspection are fine')."
rationale = "Justification, not a default: the #677 failure this guards against — two containers (amd64 + arm64) alive for one clone, where a folder-inferred lookup silently smokes the wrong architecture (mise.toml:405-409 documents that exact trap) — is precisely what `devcontainer exec`'s single-container resolution CANNOT report, because it resolves and proceeds rather than counting. @devcontainers/cli 0.89.0 ships no enumeration subcommand at all, so there is nothing native to delegate the count to. The label vocabulary is already centralized in devcontainer_names.resolve_names(), so there is no duplication to retire either. Note this check must stay even after mod-container-1's refactor, which would otherwise hand container selection to the CLI and lose the ambiguity signal."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-container-4"
unit = "mod-container"
kind = "module"
disposition = "keep"
confidence = 0.82
custom_thing = "Source-liveness assertion: `docker inspect <id> --format '{{json .Mounts}}'` parsed in Python to prove some mount is `Type == bind` with `Source == <this host workspace>`, i.e. the container is running the host working tree at branch HEAD rather than a snapshot."
evidence = "python/src/dotfiles_setup/container.py:95-105 (`_bind_mount_dest`), :170-181 (the `workspace-bind-mount` Check and its 'source not live' failure detail)."
native_alternative = "None. `devcontainer read-configuration` reports the DECLARED merged devcontainer.json (workspaceFolder/mounts as configured), not what the running container actually mounts; `devcontainer outdated` reports current-vs-available versions of features/templates from the workspace config and accepts no --container-id/--id-label at all, so it cannot speak about a running container. Only docker's own inspect (allowed as inspection) exposes the realized mount table."
alternative_evidence = ".agent/kb/raw/devcontainer-cli-0.89.0-help.md:58-73 (`devcontainer outdated --help`: options are --workspace-folder/--config/--output-format only — no container targeting), :77-95 (`read-configuration --help` — a config reader), :15 (no subcommand in 0.89.0 inspects a running container's mounts); .claude/rules/do-not.md:16-19 (docker inspection permitted)."
rationale = "Justification: the assertion is about the RUNNING container's realized state, and every native devcontainer surface at the pinned 0.89.0 reads the workspace CONFIG instead — a container started before a devcontainer.json edit satisfies read-configuration and still mounts the wrong tree, which is the exact false-positive .claude/rules/verify-before-advancing.md exists to prevent. The only native trimming available is cosmetic (a `docker inspect --format` Go template could filter Source-side in the engine instead of json.loads), which would cost testability for nothing. After mod-container-1 lands this function's return value stops being an exec workdir and becomes purely this assertion — narrow it to a bool then, do not delete it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-container-5"
unit = "mod-container"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The composite freshness gate itself: three ordered named Checks (container-running / workspace-bind-mount / smoke-tiers-1-3) with PASS/FAIL rendering, an aggregate exit code, and remediation text pointing at `mise run up` / `mise run dev-rebuild` — base currency delegated to the smoke's tier-1 in-image config-hash identity check."
evidence = "python/src/dotfiles_setup/container.py:1-29 (the gate's contract), :52-59 (`Check`), :129-197 (`verify_latest`), :200-219 (`verify_latest_main` rendering + rc); consumed by mise.toml:941-949 and python/src/dotfiles_setup/sync.py:53 & :804."
native_alternative = "No native equivalent exists in @devcontainers/cli 0.89.0, mise 2026.9.3, hk 1.57.0, docker 29.7.2 or gh. `devcontainer outdated` is the nearest-named candidate and is config-scoped (features/templates), not running-container-scoped; `devcontainer up` would report containerId/remoteWorkspaceFolder but is a mutating lifecycle call, unusable as a read-only gate."
alternative_evidence = ".agent/kb/raw/devcontainer-cli-0.89.0-help.md:5-15 (complete subcommand list for 0.89.0: up/set-up/build/run-user-commands/read-configuration/outdated/features/templates/exec — no status, no verify, no doctor), :58-73 (outdated's config-only option surface); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060-1077 (mise tasks offer bounding, not container assertions)."
rationale = "Justification: the gate encodes a policy — '.claude/rules/verify-before-advancing.md' § 'Validate against the latest branch code' — that binds three independent facts (a container exists for THIS workspace+arch, its source is the live host tree, and its base is not older than mise-system.toml) into one rc. No shipped tool models 'is my running dev container a valid validation environment for this branch'; each vendor surface answers at most one third of it, and only about declared config. What SHOULD change is the internals, not the gate: findings 1 and 2 route its exec through the native CLI and its outer bound through the native mise timeout, leaving this composition — which is genuinely repo-specific — as the only custom part. Secondary note (not a native-alternative finding): `git grep` finds no `[[suite]]` in python/verification/suites.toml naming `verify-container-latest` or `container.py`, so the wiring between the mise task, the CLI subcommand and this module is asserted only by tests/test_container.py, not by a contract."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_project-1"
unit = "mod-dag_project"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "`ensure_label()` makes `gh label create` idempotent by PARSING the CLI's failure text: it runs `gh label create dag:needs-human -R <repo> --color <c> --description <d>`, and when that exits non-zero it re-reads success out of the message with `if \"already exists\" in (result.stderr + result.stdout).lower()`. The steady state (label present) is therefore a non-zero exit that the module has to interpret."
evidence = "python/src/dotfiles_setup/dag_project.py:640-673 (function `ensure_label`), with the substring parse at python/src/dotfiles_setup/dag_project.py:663 and the docstring at :641-645 stating \"`gh label create` exits non-zero when the label already exists, which is the steady state\". Label constants at python/src/dotfiles_setup/dag_project.py:119-122."
native_alternative = "`gh label create --force` — \"Update the label color and description if label already exists\", i.e. a native upsert that exits 0 in the steady state and also reconciles colour/description drift, which is exactly what an `ensure_label` wants. Removes the need to read any upstream error string."
alternative_evidence = "Live probe on the host (gh 2.100.0, 2026-09-03), saved verbatim at .agent/kb/raw/mod-dag_project-native-probes.md: `gh label create --help` prints `-f, --force  Update the label color and description if label already exists` and the long help line \"Create a new label on GitHub, or update an existing one with `--force`.\" Corroborated in the offline source at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/label/create.go:87 (flag registration) and :115 (the error that is raised WITHOUT --force, which is the string the module currently matches)."
rationale = "This is the exact shape `.claude/rules/probes-need-a-control-arm.md` rule 9 names: the check binds something we do not own (an upstream CLI's error wording). If cli/cli rewords `label with name %q already exists`, `ensure_label` silently starts reporting a warning and returning False on every tick for a label that is present — a symptom sniff that degrades to a permanent false alarm. `--force` binds the BEHAVIOUR instead: the call becomes rc=0 whether or not the label exists, the `if \"already exists\"` branch and its `+ result.stdout` fallback disappear, and the module's own `_LABEL_COLOR`/`_LABEL_DESCRIPTION` constants become authoritative rather than write-once. Scope note for the implementer: `python/verification/suites.toml:1577` pins the token `def ensure_label(` for this module, so the function must keep its name; only its body changes, and the existing `warning`-on-other-failure arm stays. Not a `retire` because the function still owns the decision of WHICH label and whether a real failure is fatal."
probe = "gh label create dag:needs-human-probe -R ray-manaloto/dotfiles --color d93f0b --description probe --force; echo rc=$?; gh label create dag:needs-human-probe -R ray-manaloto/dotfiles --color d93f0b --description probe --force; echo rc=$?; gh label delete dag:needs-human-probe -R ray-manaloto/dotfiles --yes   # both rc must be 0 (idempotent); the control arm is the same pair WITHOUT --force, whose second call must be non-zero"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_project-2"
unit = "mod-dag_project"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`collect_escalations()` / `escalation_from_state()` enumerate the harness's PRIVATE on-disk job layout directly — `sorted(p for p in jobs_dir.iterdir() if p.is_dir())` under `~/.claude/jobs`, then `json.loads((job_dir / \"state.json\").read_text())` plus `state_path.stat().st_mtime` — rather than going through a documented CLI surface."
evidence = "python/src/dotfiles_setup/dag_project.py:316-352 (`collect_escalations`), the state file read at python/src/dotfiles_setup/dag_project.py:332-335, and the jobs root at python/src/dotfiles_setup/dag_tick.py:233 (`JOBS_DIR = Path.home() / \".claude\" / \"jobs\"`), resolved at python/src/dotfiles_setup/dag_project.py:823. Fields consumed: `needs`, `suggestedReply`, `state`, `tempo`, `sessionId`, `cliVersion`, `updatedAt` (python/src/dotfiles_setup/dag_project.py:294-306)."
native_alternative = "`claude agents --json --cwd <path> --all` — the documented, supported session census, which the sibling watchdog already uses. Its per-entry field table is `cwd`, `kind`, `startedAt`, `id`, `state` (`working`/`blocked`/`done`/`failed`/`stopped`), `pid`, `status`, `waitingFor` (`input needed` for a question from Claude), `sessionId`, `name`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-view.md:647 (the `--json`/`--all`/`--cwd` contract) and the complete field table at agent-view.md:653-659. The sibling caller that does use it: python/src/dotfiles_setup/dag_tick.py:1048 (\"`claude agents --json --cwd <cwd> --all`, defensively parsed\") and dag_tick.py:54."
rationale = "The documented census cannot supply the module's product. Every field the projected comment carries as CARGO is absent from the `claude agents --json` table at agent-view.md:653-659: there is no `needs` (the verbatim question), no `suggestedReply`, no `queuedPrompt` (the third term of the NEEDS_HUMAN conjunction, without which an already-answered node would be re-escalated forever), no `tempo` (the stall axis), no `updatedAt` (which python/src/dotfiles_setup/dag_project.py:404-415 establishes is authoritative over the mtime), and no `cliVersion`. `waitingFor: input needed` is a CATEGORY, not the question text, so a projection built on it would post an escalation comment with nothing in it — the failure #602 exists to end, one layer up. A hybrid (classify from `--json`, fetch the payload from `state.json`) is worse than either: it would put two disagreeing readers on one node, the exact defect class python/verification/suites.toml:1565 makes this module's most load-bearing clause. Keeping the direct read is therefore justified, not merely convenient; the real mitigation is that the layout is read through `dag_tick.node_from_state`, one shared parser, so a harness layout change breaks in one place. Re-check this finding at any Claude Code release that adds a payload field to `claude agents --json`."
probe = "claude agents --json --cwd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles --all | jq -r '[.[] | keys] | add | unique | join(\" \")'   # control arm: the same jq over a hand-made fixture containing a \"needs\" key must print needs, proving the extractor would see one if it were there"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_project-3"
unit = "mod-dag_project"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "The module's whole reason for being: detecting an out-of-band background session that is `blocked` with a non-empty `needs` and no `queuedPrompt`, and surfacing it to a human — with NO terminal, NO agent view, and no Claude session running, from a 300s launchd agent."
evidence = "python/src/dotfiles_setup/dag_project.py:272-306 (`escalation_from_state`, delegating the predicate to `dag_tick.is_needs_human`) and the module docstring at python/src/dotfiles_setup/dag_project.py:2-11, which records the motivating measurement: \"two nodes sat `blocked ∧ needs` since 2026-07-13 and 2026-07-22, visible in a launchd log and nowhere else\". Schedule: mise.toml:1463-1470 (`[bootstrap.macos.launchd.agents.dotfiles-dag-project]`, `start_interval = 300`)."
native_alternative = "The `Notification` hook's `agent_needs_input` type (added v2.1.198), delivered through `preferredNotifChannel` — the harness's own \"a background session needs you\" signal, which could in principle drive an external notifier without any custom census."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:313 (the `Notification` matcher's type list, including `agent_needs_input`), hooks.md:2206-2207 (the per-type table), and agent-view.md:130 (\"While agent view is open, Claude Code also sends a notification ... and fire[s] the `Notification` hook with the `agent_needs_input` or `agent_completed` type\")."
rationale = "The native signal is gated on precisely the condition this module exists to survive. hooks.md:2206 says `agent_needs_input` fires when a background session starts waiting \"while agent view is open in a terminal\", and agent-view.md:130 repeats the gate; hooks.md:2207 states the same for `agent_completed`. The two live escalations that motivated #602 sat blocked for three and four weeks with no agent view open — under the native mechanism they would have emitted nothing at all, and even had one fired, a terminal bell is an EDGE event with no durable record, whereas the module's product is an append-only, deduped, byte-faithful tracker comment that is still there weeks later. It is also strictly one-directional and reads nothing the hook could hand it (the `needs` payload is not in the Notification hook input either). Nothing native covers this; keeping it is the justified answer, not the default one."
probe = "No probe needed — the gate is stated in the vendor docs at hooks.md:2206-2207. If it must be armed: with agent view CLOSED, put a background session into `blocked` and confirm a `Notification` hook logging every invocation records zero `agent_needs_input` events, with the control arm being the same session while `claude agents` IS open, which must record one."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_project-4"
unit = "mod-dag_project"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "A hand-built idempotency layer over GitHub issue comments: an invisible `<!-- dag:needs-human node=<id> digest=<hex> schema=1 -->` marker embedded in every posted body, a regex that mines markers back out of every existing comment, and a pre-write snapshot of the whole comment list per target issue used as the dedupe set."
evidence = "python/src/dotfiles_setup/dag_project.py:129 (`_MARKER_RE`), :176-192 (`marker()`), :151-155 (`payload_digest`, keyed on the payload not the node), :576-609 (`existing_markers`, `gh api /repos/<repo>/issues/<n>/comments --paginate` then `_MARKER_RE.findall` over each body), and the snapshot/dedupe wiring at :709-762 (`project_escalation`, `key = f\"node={...} digest={...}\"`, `markers.add(key)`)."
native_alternative = "`gh issue comment <n> --edit-last --create-if-none --body <b>` — gh's own upsert-a-comment idempotency: edit the current user's last comment on the issue, creating one only if none exists."
alternative_evidence = "Live probe on the host (gh 2.100.0), saved at .agent/kb/raw/mod-dag_project-native-probes.md: `gh issue comment --help` prints `--edit-last  Edit the last comment of the current user` and `--create-if-none  Create a new comment if no comments are found. Can be used only with --edit-last`. Source: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/pr/comment/comment.go:79,82, with the issue-side acceptance cases at gh/acceptance/testdata/issue/issue-comment-edit-last-with-comments.txtar:26 and issue-comment-edit-last-without-comments-creates.txtar:19."
rationale = "`--edit-last` gives ONE slot per (user, issue); this module needs one slot per (node, question) on an issue shared by every unbound node. On the standing escalation issue (`DEFAULT_ESCALATION_ISSUE = 623`, python/src/dotfiles_setup/dag_project.py:96) a second escalated node running `--edit-last` would OVERWRITE the first node's escalation — destroying the record that is the module's entire deliverable, and doing it silently because the write succeeds. The key must also be composite in both directions, which `--edit-last` cannot express at all: python/src/dotfiles_setup/dag_project.py:151-158 records that keying on the node alone silences a node that re-escalates with a NEW question, and on the digest alone collides across nodes asking the SAME one. The marker also cannot be replaced by the label, for the reason given at python/src/dotfiles_setup/dag_project.py:184-190 (the label lives on the issue, so the first node would suppress every later one). Note that the parts of this layer that ARE native are already used: `gh api --paginate` for the comment list (python/src/dotfiles_setup/dag_project.py:598) rather than a hand-rolled page loop."
probe = "No probe needed — the semantics are in gh's own flag text (\"the last comment of the current user\"), which cannot express a per-node key. To arm it: post two marker comments as the same user on one scratch issue, run `gh issue comment <n> --edit-last --body X`, and confirm only the SECOND is rewritten while the first is untouched but unreachable."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_project-5"
unit = "mod-dag_project"
kind = "module"
disposition = "refactor"
confidence = 0.45
custom_thing = "Hand-rolled mutual exclusion of the two CLI modes: `--dry-run` and `--write` are registered as two independent flags, and `run_project` enforces exactly-one-of itself with `if args.dry_run == args.write: logger.error(...); return 2`."
evidence = "python/src/dotfiles_setup/dag_project.py:816-822 (the check and its bespoke refusal message) with the docstring at :809-814; the two independent registrations at python/src/dotfiles_setup/main.py:1451-1465, whose `--write` help text already claims \"Mutually exclusive with --dry-run\" without anything in argparse enforcing it."
native_alternative = "`argparse.ArgumentParser.add_mutually_exclusive_group(required=True)` — stdlib enforcement of exactly-one-of at parse time, which emits a usage error and exits 2, the same return code the custom check produces."
alternative_evidence = "/Users/rmanaloto/.local/share/uv/python/cpython-3.14-macos-aarch64-none/lib/python3.14/argparse.py:1569 — `def add_mutually_exclusive_group(self, **kwargs)` on `_ActionsContainer` (and the `_ArgumentGroup` override at :1815); the host interpreter is the uv-managed CPython 3.14 this repo pins (`python = 3.14.7`)."
rationale = "Native covers PART of it, and only part — which is why this is a refactor rather than a retire. The group would move the constraint to where the flags are declared, making main.py's \"Mutually exclusive with --dry-run\" help text true by construction instead of by convention, and it would stop the two-flag invariant living a file away from the two `add_argument` calls. What it does NOT cover: argparse would replace the deliberate operator-facing refusal text (python/src/dotfiles_setup/dag_project.py:817-821, which explains WHY writing is never a default) with a stock usage error, and a mutually-exclusive group is enforced only by `parse_args` — `run_project` is also called directly by tests and could be called by a future scheduler phase, so the in-function guard is a real second layer, not redundancy. Recommended shape: adopt the group AND keep the in-function check. Scope note: python/verification/suites.toml:1577 pins the literal token `if args.dry_run == args.write:` for this module, so the in-function check must survive any refactor regardless."
probe = "uv run --project python dotfiles-setup dag-project --dry-run --write; echo rc=$?   # must stay 2 after the change, with the control arm being a bare `dotfiles-setup dag-project` which must also stay 2"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_project-6"
unit = "mod-dag_project"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The recurring 300-second execution of the projector, declared as a macOS LaunchAgent in the repo's mise config (`program = ~/.local/bin/mise`, `args = [\"run\", \"dag-project\", \"--\", \"--write\"]`, `start_interval = 300`, plus an explicit literal `PATH` so `gh` resolves from the mise shims under launchd)."
evidence = "mise.toml:1463-1478, with the rationale for 300s over the watchdog's 60s at mise.toml:1444-1449 and the inert-until-applied invariant at mise.toml:1459-1461. The verb it drives is mise.toml:636-652 (`[tasks.dag-project]`)."
native_alternative = "Claude Code's own schedulers: `/loop` + the `CronCreate`/`CronList`/`CronDelete` tools for in-session recurrence, cloud Routines for machine-independent recurrence, and Desktop scheduled tasks for scheduled runs that can touch local files."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/scheduled-tasks.md:11 (\"Tasks are session-scoped: they live in the current conversation and stop when you start a new one\"; the same line points at Routines / Desktop tasks / GitHub Actions for scheduling that survives a session), scheduled-tasks.md:23 (the comparison table's \"Access to local files\" row: cloud = No, fresh clone) and scheduled-tasks.md:30 and :158 (the `CronCreate` tool row)."
rationale = "The scheduling is ALREADY the native mechanism for its tier — mise's own `[bootstrap.macos.launchd.agents]` block, not a hand-written plist or a shell cron wrapper — so there is no custom scheduler here to retire. The Claude-Code-native schedulers each fail a hard requirement of this job: `/loop` and `CronCreate` are session-scoped (scheduled-tasks.md:11), and a projector whose purpose is to notice sessions nobody is watching cannot itself depend on a session being open; cloud Routines have no access to local files (scheduled-tasks.md:23) and so cannot read `~/.claude/jobs` on this Mac at all; and a Desktop scheduled task would spend model tokens on every one of the ~288 daily fires whose overwhelmingly common outcome is `0 escalations` and, by design, zero API calls (python/src/dotfiles_setup/dag_project.py:764-771). A deterministic Python run under launchd is the correct tier. Re-open this only if a future release gives Routines local-filesystem access."
probe = "No probe needed — the session-scoping and no-local-files limits are stated in the vendor doc lines cited above."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_tick-1"
unit = "mod-dag_tick"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "DEAD -> RESPAWN recovery: classify() maps a non-terminal node with no live process to NodeClass.DEAD, plan() turns that into ActionKind.RESPAWN, and execute_respawn() shells out to `claude respawn <id>` with a fresh escalation + pid re-check."
evidence = "python/src/dotfiles_setup/dag_tick.py:706 (plan(); DEAD arm at :739-755), python/src/dotfiles_setup/dag_tick.py:1187 (execute_respawn), python/src/dotfiles_setup/dag_tick.py:373 (is_terminal, the state+tempo+queuedPrompt predicate the respawn suppression is built on)"
native_alternative = "The Claude Code supervisor already restarts a background session whose process exits unexpectedly, and its three documented safeguards are near-identical to this module's own predicate: a session whose on-disk state is done/failed/stopped is not restarted UNLESS a reply is still waiting to be delivered (i.e. state-terminal + queuedPrompt, exactly is_terminal()); a session the user killed after `<-`/`/background` is marked stopped instead; a dispatched session is still restarted so its work completes."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-view.md:697 (\"The supervisor also restarts a session whose process exits unexpectedly, with three safeguards...\"), :699, :700, :701; residual gap cited at :818 (\"On Linux and WSL, the supervisor checks each host process every few seconds\" — a platform qualifier that excludes this macOS host) and :848 (a stalled supervisor is only replaced by the next `claude agents`)"
rationale = "The mainline of this module's primary recovery path is native and has been for some time: while the supervisor is alive it restarts dead workers itself, with a suppression predicate that duplicates is_terminal() term for term. The module's own docstring (:76-79) states the residual it exists for — the harness's in-life watchdog runs only while the supervisor process is itself alive — and the docs support a second residual (the per-few-seconds host-process poll is documented for Linux/WSL only, not macOS). Neither residual is 'the whole DEAD path', so the honest shape is to narrow this unit to supervisor-death recovery and cite agent-view.md:697-701 for the rest, rather than keep a full parallel respawn engine. Not 'retire': nothing native recovers a session when the supervisor itself is gone, and nothing native honours this module's --max-age backstop or its NEEDS_HUMAN suppression."
probe = "claude daemon status; then, with the supervisor confirmed running, kill -9 the roster pid of a disposable `claude --bg` node on this macOS host and poll `claude agents --json --all` for ~120s to see whether the supervisor restarts it unaided (control arm: repeat with `claude daemon stop --any --keep-workers` first, where nothing should restart it)."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-dag_tick-2"
unit = "mod-dag_tick"
kind = "module"
disposition = "unknown"
confidence = 0.45
custom_thing = "A ~160-line hand-rolled liveness+identity stack over the harness's internal daemon roster: read_roster() parses ~/.claude/daemon/roster.json's undocumented per-worker `pid`/`procStart` fields, pid_is_alive() does kill(pid,0), read_proc_start() shells `LC_ALL=C TZ=UTC ps -o lstart= -p <pid>`, and background_pid_alive() reimplements the harness's own GC predicate (liveness AND exact procStart string equality)."
evidence = "python/src/dotfiles_setup/dag_tick.py:792 (read_roster), :825 (pid_is_alive), :854 (read_proc_start), :885 (background_pid_alive)"
native_alternative = "`claude agents --json` documents `pid` and `status` as fields present *only* 'While the process is alive' — i.e. their ABSENCE on a background row is a first-party liveness negative, from the same read this module already performs (read_census at :1047), with no second file and no `ps` subprocess."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-view.md:656 (\"| `pid`, `status` | While the process is alive | Process ID and current status |\"), :647 (the census includes background sessions 'even when their process has exited'), :738 (roster.json is documented only as a reconnect list, not as a liveness API)"
rationale = "The documented FIELD-PRESENCE semantics of the census may make the whole roster/ps/procStart stack unnecessary, which would delete the module's largest and most fragile surface (it depends on undocumented JSON keys and on a `ps` output encoding). It is genuinely unsettled, not decidable from docs: #593 measured that the census's pid VALUE names a different process (`claude bg-spare`) than the roster's (`claude bg-pty-host`, its parent) — see dag_tick.py:61-68 — so a naive swap is known-wrong, but that measurement says nothing about whether the field's PRESENCE tracks worker liveness. It also cannot be assumed that presence is reliable within one 60s tick. A live probe on this host settles it; nothing else will."
probe = "Start a disposable node with `claude --bg`, then run: `claude agents --json --all --cwd \"$PWD\" | jq -c '.[] | select(.kind==\"background\") | {id, state, has_pid: has(\"pid\"), status}'` alongside `jq -c '.workers | to_entries[] | {k:.key, pid:.value.pid}' ~/.claude/daemon/roster.json`, before AND after `kill -9` of the roster pid (both arms required: a row that keeps `pid` after the kill refutes the alternative)."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_tick-3"
unit = "mod-dag_tick"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "gate_preflight() reads the fleet gate by minting a bogus uuid, running `claude logs <bogus-id>`, and classifying the stderr TEXT ('No job matching' -> on, 'is disabled by' -> off) because both faces exit rc=1."
evidence = "python/src/dotfiles_setup/dag_tick.py:1017 (gate_preflight), :776 (gate_status, the stderr string matcher)"
native_alternative = "`claude daemon status` is the documented command for inspecting the supervisor/background-agent subsystem from a shell — 'To inspect this state without reading the files directly, run `claude daemon status`. It reports whether the supervisor is reachable, its process ID and version, the socket directory, and how many background sessions are live.' The gate itself is the documented `disableAgentView` setting / `CLAUDE_CODE_DISABLE_AGENT_VIEW` env var, which turns off `claude agents`, `--bg`, `/background` and the on-demand supervisor."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-view.md:744 and :642 (claude daemon status); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:4599 and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/env-vars.md:231 (disableAgentView / CLAUDE_CODE_DISABLE_AGENT_VIEW)"
rationale = "Binding a gate to an English stderr substring is precisely the 'sniff for a symptom' shape .claude/rules/probes-need-a-control-arm.md rule 9 warns about: a reworded harness message silently flips the gate to 'unknown'. A first-party status command would be a capability assertion instead. But the offline corpus documents neither an exit-code contract nor a `--json` form for `claude daemon status`, and does not say what it prints when the gate is off (agent view disabled means no supervisor at all), so this cannot be decided from docs. Note the module already fails OPEN on 'unknown' (dag_tick.py:44-53), so the current risk is bounded — this is a robustness/plainness improvement, not a live defect."
probe = "claude daemon status; echo \"rc=$?\"  # then the control arm, which is what makes it a probe: CLAUDE_CODE_DISABLE_AGENT_VIEW=1 claude daemon status; echo \"rc=$?\"  — plus `claude daemon --help` to check for a --json form and for an install-as-service subcommand."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_tick-4"
unit = "mod-dag_tick"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "NEEDS_HUMAN / REPLY_QUEUED escalation detection by polling each node's ~/.claude/jobs/<id>/state.json every 60s for `state == \"blocked\"` + a `needs` payload + queuedPrompt, and emitting an operator-facing log line."
evidence = "python/src/dotfiles_setup/dag_tick.py:434 (is_needs_human), :470 (is_reply_queued), :1094 (classify_background_rows, the note emission at :1148-1168), :974 (load_state_json)"
native_alternative = "The `Notification` hook fires with `notification_type: \"agent_needs_input\"` when a background session starts waiting on input, and `agent_completed` when one finishes or fails (v2.1.198+) — an event-driven path that would replace a 60s poll."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2206 (\"A background session starts waiting on your input **while agent view is open in a terminal**\"), :2207 (\"Fires only while agent view is open in a terminal\"), :313 (the notification_type enum), :2212 (v2.1.198+); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-view.md:130 (same condition stated from the agent-view side)"
rationale = "The native event exists and matches the semantics, but it is explicitly conditioned on agent view being OPEN IN A TERMINAL (hooks.md:2206-2207, stated twice and independently at agent-view.md:130). This watchdog's entire premise is the unattended case — a launchd tick with no terminal and no agent view — so the hook would fire exactly when the watchdog is least needed and be silent exactly when it is most needed. The same condition rules out the census's `waitingFor` field, which is present only when `status` is `waiting` and `status` itself only 'While the process is alive' (agent-view.md:656-657), i.e. never for the escalated-and-then-crashed node this classification exists to protect. No native mechanism covers a headless, out-of-band escalation census, so the custom predicate stays."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_tick-5"
unit = "mod-dag_tick"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "try_acquire_lock(): a hand-rolled single-instance guard using fcntl.flock(LOCK_EX|LOCK_NB) on ~/.local/state/dotfiles/dag-tick.lock, so a second tick exits 0 silently rather than racing a respawn/stop."
evidence = "python/src/dotfiles_setup/dag_tick.py:997 (try_acquire_lock), :229-231 (LOCK_PATH and the overlap rationale), :1017+ execute_tick's lock-first ordering at the end of the module"
native_alternative = "mise task-level mutual exclusion — the closest native primitives are `raw` (a global per-COMMAND exclusive lock that forces jobs=1) and `interactive` (an exclusive stdio lock)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:387-390 — the raw lock is taken 'per command rather than per task, so two raw tasks can still take turns between their individual commands', and mise's own docs then say: 'If you need a whole task to run without interruption, search/file a ticket for a property like `single = true`.' i.e. mise has NO single-instance task property today (pinned mise is already the latest release: .agent/kb/raw/mise-releases-since-pin.digest.md, zero releases newer than the host's 2026.9.3)."
rationale = "Required justification: the native candidate is documented as NOT providing this. mise's exclusive lock is per-command and global (it would serialise every other mise invocation on the host, which for a 60s agent is a denial-of-service on the operator's own shell), and mise's docs explicitly state the whole-task property does not exist yet. The mise launchd surface maps only the plist keys (StartInterval, ThrottleInterval) and documents no non-overlap guarantee, and the lock must also cover the manual `mise run dag-tick -- --dry-run` path that launchd cannot see at all. 12 lines of fcntl is the minimum correct thing here."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_tick-6"
unit = "mod-dag_tick"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "Reading each node's ledger fields — `tempo`, `queuedPrompt`, `needs` — plus the state.json mtime used as the WEDGED staleness proxy, straight off ~/.claude/jobs/<id>/state.json."
evidence = "python/src/dotfiles_setup/dag_tick.py:974 (load_state_json returns parsed object + mtime), :953 (node_from_state reads state/tempo/queuedPrompt/needs), :415 (is_stalled, the mtime-based stall predicate)"
native_alternative = "`claude agents --json` is the first-party census and already supplies `state` (working/blocked/done/failed/stopped) per background session — the module consumes it via read_census() for the row set and `kind`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-view.md:655 (`state` field), :653 (`cwd`, `kind`, `startedAt`), :656-657 (`pid`/`status`/`waitingFor`) — the documented field table lists NO `tempo`, `queuedPrompt`, `needs`, or last-progress timestamp"
rationale = "Required justification: the native census is already adopted for everything it exposes (read_census at :1047 is the row source), and the documented field table simply does not carry the three axes the classifier turns on. `tempo` gates is_terminal (a done+active node is NOT terminal — the watchdog's primary recovery case), `queuedPrompt` is what splits NEEDS_HUMAN from REPLY_QUEUED, and `needs` is the escalation payload itself; none appear in agent-view.md:653-657. There is also no native last-progress timestamp, so the WEDGED staleness proxy has to come from the file's mtime. The only native improvement available is cross-checking the census `state` against the file's `state`, which is a consistency check, not a replacement."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dag_tick-7"
unit = "mod-dag_tick"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "Scheduling and process supervision of the tick itself: a declarative `[bootstrap.macos.launchd.agents.dotfiles-dag-tick]` block with program/args/start_interval/working_directory/std*_path/environment, applied only by an explicit human `mise bootstrap macos launchd-agents apply`."
evidence = "mise.toml:1419-1435 (the LaunchAgent block), mise.toml:622-634 ([tasks.dag-tick], a thin `uv run --project python dotfiles-setup dag-tick` caller)"
native_alternative = "mise's own bootstrap launchd surface — `[bootstrap.macos.launchd.agents.<name>]` written to ~/Library/LaunchAgents/dev.mise.<name>.plist and loaded with `launchctl bootstrap`, with `status`/`status --json`/`status --missing` for drift and `apply --dry-run`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/bootstrap/launchd.md:1-34 (the key mapping incl. `start_interval` -> `StartInterval` at :34) and its Commands section (`mise bootstrap macos launchd-agents status --json`, `apply --dry-run`)"
rationale = "Required justification: this half is ALREADY the native mechanism and needs no change — the scheduling, the plist generation, the launchctl load and the drift check are all mise built-ins, and the repo holds only the declaration. The one thing to leave alone deliberately: mise's launchd surface offers `kickstart` and `keep_alive`, and this agent uses neither, consistent with .claude/rules/do-not.md's 'nothing applies a LaunchAgent implicitly' invariant. Recorded so a later audit does not 'modernize' a block that is already the tool built-in."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dependency_ownership-1"
unit = "mod-dependency_ownership"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "Hand-rolled discovery of mise-declared Python packages: `_mise_python_packages` reads exactly one file (`repo_root/mise.toml`) with `tomllib` and scans `[tools]` keys for the `pipx:`/`pip:`/`uvx:` prefixes, reimplementing mise's own config resolution."
evidence = "python/src/dotfiles_setup/dependency_ownership.py:16 (`_PYTHON_TOOL_PREFIXES`), :50-60 (`_mise_python_packages` iterating a raw `[tools]` dict), :82 (`tomllib.loads((repo_root / \"mise.toml\").read_text())` — the ONLY config file read); hk.pkl:443-446 (the `dependency_ownership` step, whose glob is `mise.toml, mise.lock, python/pyproject.toml, python/uv.lock` — `.config/mise/conf.d/shared.toml` is not even a trigger)"
native_alternative = "`mise ls --current --json` — mise's own merged view of the ACTIVE tool set across the whole config hierarchy (project `mise.toml` + `.config/mise/conf.d/*.toml` + global), with a per-tool `source.path` attributing each declaration to the file it came from. `--all-sources` additionally shows every config that declares a tool."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/ls.md:24 (`-J --json` Output in JSON format), :27 (`--all-sources` Display all tracked config sources for tools), :58 (the `source: {type, path}` object in the documented `mise ls --json` output), :67 (`mise ls --all-sources` example). Live probe saved at .agent/kb/raw/mod-dependency_ownership-mise-ls-probe.md: `mise ls --current --json` rc=0, 150 tool keys, and it reports `pipx:check-jsonschema source=<repo>/.config/mise/conf.d/shared.toml` plus `pipx:graphifyy source=~/.config/mise/config.toml`."
rationale = "The single-file tomllib read is a blind spot mise's own resolver does not have. `.config/mise/conf.d/shared.toml` is repo-tracked, merged by both host and image (AGENTS.md 'Key Files'), and already declares a python-backend tool (`pipx:check-jsonschema`) — a duplicate added there today is invisible to the audit, and the hk glob would not even re-run the step. The probe also shows `pipx:graphifyy` active from the user-global config while python/pyproject.toml:9 declares `graphifyy[all]==0.9.53`, i.e. the exact condition this module exists to detect is currently live one config layer up and the audit reports ok (tests/test_dependency_ownership.py:69 even asserts `'graphifyy' not in result.mise_python_packages`). Refactor rather than retire: mise supplies the merged tool inventory, but the intersection with pyproject is still ours (finding -4). Note `mise ls` shells out and hits the tool registry, so the refactor must keep the gate offline-safe (`--current` + `--json`, no network install) or feed the file list from `mise config ls` instead."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && mise ls --current --json | jq -r 'to_entries[] | select(.key|test(\"pipx:|uvx:|^pip:\")) | \"\\(.key) \\(.value[0].source.path)\"'   # then decide whether user-global declarations are in or out of the invariant's scope"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-dependency_ownership-2"
unit = "mod-dependency_ownership"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "String-equality assertion that mise's uv deps provider is configured for locked materialization: `uv_provider.get(\"run\") == \"uv sync --locked\"` (plus `auto is True` and `dir == expected_dir`), failing the gate on any other spelling."
evidence = "python/src/dotfiles_setup/dependency_ownership.py:85-91 (`uv_locked = ... uv_provider.get(\"run\") == \"uv sync --locked\"`), :108-109 (the error text `mise [deps.uv] must auto-run uv sync --locked`); mise.toml:5-8 (the `[deps.uv]` block being asserted); tests/test_dependency_ownership.py:53-61 (the arm that flips `uv sync --locked` to `uv sync`)"
native_alternative = "uv's own `UV_LOCKED` environment variable (equivalent to `--locked`, supported since uv 0.4.x), exported once from mise `[env]` so EVERY `uv sync` mise's deps provider runs is locked by construction — enforcing the behaviour instead of asserting one literal string. mise's built-in uv provider's default command is plain `uv sync`, which is what the override exists to prevent."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/changelogs/0.4.x.md:1050 ('Add support for `UV_FROZEN` and `UV_LOCKED`'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/clippy.toml:16,18 (`UV_FROZEN`, `UV_LOCKED` in uv's own allowed-env-var list); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/dev-tools/deps.md:66 (built-in provider table: `uv` -> sources `pyproject.toml, uv.lock`, command `uv sync` — no `--locked`)"
rationale = "The invariant worth protecting is 'materialization never re-resolves', not 'the run string is byte-exactly `uv sync --locked`'. The current check is a symptom assertion in the sense of probes-need-a-control-arm.md rule 9: a legitimate `uv sync --locked --no-dev` or `uv sync --frozen` fails it, while `UV_LOCKED=1` in mise `[env]` would make even the provider's DEFAULT `uv sync` locked. Not a retire: mise has no setting that pins a provider's command, so a gate is still wanted — but it should assert the enforcement (env var present, or run string starting `uv sync` with a locked/frozen mode) rather than one literal. Confidence held at 0.55 because I did not probe that mise's deps-provider subprocess actually inherits mise `[env]` exports."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && MISE_EXPERIMENTAL=1 mise deps install --dry-run 2>&1 | head -20   # and, with UV_LOCKED=1 exported and `run` reverted to `uv sync`, confirm the provider still refuses a stale lock"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-dependency_ownership-3"
unit = "mod-dependency_ownership"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "Hand-rolled PEP 508 requirement-name extraction and PEP 503 name canonicalization: a `^\\s*([A-Za-z0-9][A-Za-z0-9._-]*)` regex plus `re.sub(r\"[-_.]+\", \"-\", ...).lower()`, and a separate ad-hoc `key.split(\":\",1)[1].split(\"[\",1)[0]` for the mise side."
evidence = "python/src/dotfiles_setup/dependency_ownership.py:15 (`_PACKAGE_NAME`), :20-24 (`_canonical_name`), :57 (the `split(\"[\")` extras strip)"
native_alternative = "`packaging.requirements.Requirement(spec).name` for PEP 508 parsing and `packaging.utils.canonicalize_name()` for PEP 503 normalization — the PyPA reference implementation, already resolved in this project's lock and installed in its venv."
alternative_evidence = "python/uv.lock:1243 (`name = \"packaging\"` — already in the resolution, pulled in transitively at 774/996/1186/1452); live probe saved at .agent/kb/raw/mod-dependency_ownership-packaging-probe.md: packaging 26.3 installed, `canonicalize_name` at python/.venv/lib/python3.14/site-packages/packaging/utils.py:82, `Requirement` at .../packaging/requirements.py:35, and `Requirement('graphifyy[all]==0.9.53').name` -> `graphifyy`"
rationale = "use-tool-builtins.md's hard gate: the capability is 'normalize a distribution name', a solved, specified problem with a maintained PyPA implementation already in the lock — adopting it costs one line in `[project.dependencies]` and no new resolution. The custom regex silently returns `\"\"` for shapes it cannot parse (dependency_ownership.py:22-23), so a malformed or URL-form requirement is dropped from the audited set rather than raising — a false-negative shape, which is exactly what this module is supposed to prevent. Not a retire: the mise-key side (`pipx:name[extras]`) is a mise spelling, not a PEP 508 requirement, so that split stays ours (or comes from finding -1's `mise ls --json` keys)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-dependency_ownership-4"
unit = "mod-dependency_ownership"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The audit's actual invariant: a set intersection between the packages declared in `python/pyproject.toml` (`[project.dependencies]` + `[dependency-groups]`) and the Python-backend tools declared to mise, reported as `duplicate_packages` and failing the hk gate."
evidence = "python/src/dotfiles_setup/dependency_ownership.py:78-97 (`audit_dependency_ownership`, `duplicates = tuple(sorted(set(python_packages) & set(mise_packages)))`), :100-110 (`dependency_ownership_main`); python/src/dotfiles_setup/main.py:1064 + :2389-2390 (the `dependency-ownership` subcommand); hk.pkl:443-446 (the gate); tests/test_dependency_ownership.py:44-51"
native_alternative = "None found. mise's health command has no cross-manager duplicate-declaration check (`mise doctor` is install-health only); Renovate has no cross-manager dedupe concept — it would bump both copies independently forever; uv sees only its own project; hk ships no builtin for it (docs/hk-builtins-audit.md:70 lists `dependency_ownership` as an hk.pkl-local step)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor.md:1-26 (the whole documented surface of `mise doctor`: `-J --json` and a `path` subcommand; the only example is 'plugin node is not installed'). Control-armed grep over mise's doctor implementation: `grep -rni duplicate /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/doctor/` -> 1 hit, mod.rs:1341, an unrelated code comment ('duplicate that as a second, wronger message'); the same command shape with the known-present term `plugin` -> 49 hits in the same tree, so the near-zero result is the world, not a broken probe. .agent/kb/raw/renovate-config-options.digest.md -> 0 hits for 'duplicate'."
rationale = "Required justification: every tool in the stack is scoped to a single manifest. The invariant here is inherently cross-manifest — 'this name must be owned by pyproject.toml AND NOT by mise's tool table' — and no package manager, linter, or Renovate preset computes that intersection, because none of them knows the other manager exists. That is precisely the case use-tool-builtins.md leaves to custom code. The custom code that should survive is the intersection and the failure message; its two INPUTS should come from native sources instead (findings -1 and -3), which is where the real modernization lives."
probe = ""
correction = "**⚠ CITATION CORRECTED (round 5) — RANGE END PAST EOF.** `sources/mise/docs/cli/doctor.md:1-26` overshoots by two lines: the file is **24** lines (`wc -l` -> 24). Read it as **`:1-24`**, which is still “the whole documented surface of `mise doctor`” — the claim the range was making. The row's control-armed grep over mise's doctor implementation is unaffected."
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-devcontainer_names-1"
unit = "mod-devcontainer_names"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "Overlay-image identity resolution for `mise run prune`: ~180 lines that reverse-engineer @devcontainers/cli's tag shapes and re-derive container provenance instead of reading a label — `_is_orphan_tag` (re-derives BOTH `vsc-dotfiles-<hash>` and the CLI's own `vsc-<basename>-<full-sha256>` tag), `_docker_container_images` (a Go-template `index .Config.Labels` read for trust), `_trusted_by_container`, `_has_registry_slash` (a `/`-in-ref heuristic standing in for provenance), `_docker_image_refs`, `teardown_image_refs`."
evidence = "python/src/dotfiles_setup/devcontainer_names.py:663-670 (`_OVERLAY_TAG_PREFIX` hardcoded to mirror devcontainer.json), :793-818 (`_is_orphan_tag` derives two tag shapes), :821-834 (`_has_registry_slash` — the docstring itself concedes the general claim is false and only the narrow one holds), :837-877, :923-991; consumed by mise.toml:1101 (`devcontainer teardown-images --container-ids`); contract python/verification/suites.toml:2429 states the ordering invariant is carried \"by nothing mechanical\"."
native_alternative = "Label the overlay image at build time and query it by label: `docker build --label dotfiles.workspace=<hash> --label dotfiles.arch=<arch>` added to devcontainer.json's `build.options` (which is already proven to reach the builder — `--tag` and `--platform` ride it today), then `docker image ls -aq --filter label=dotfiles.workspace=<hash>`. This is the exact image-side twin of the container-side `--id-label` the module already adopted."
alternative_evidence = ".agent/kb/raw/docker-cli-label-filter-probe.md (host docker-cli 29.7.2, probed 2026-09-09): `docker build --help` line 39 `--label stringArray  Set metadata for an image`; `docker image ls --help` `-f, --filter`. Control-armed: `docker ps -aq --filter label=dotfiles.workspace` -> 1, same shape with a fresh nonce key `qq7x2v.absent.key` -> 0, and `docker image ls -aq --filter label=org.opencontainers.image.source` -> 5 while `--filter label=dotfiles.workspace` -> 0 (i.e. the image filter works and nothing labels the overlays today). Passthrough proof: .devcontainer/devcontainer.json:103 already ships `\"options\": [\"--tag=vsc-dotfiles-...\", \"--platform=...\"]`."
rationale = "The whole tag-shape/trust apparatus exists only because the overlay images carry no identity of ours — the module says so itself (`_is_orphan_tag`'s two derived shapes, `_has_registry_slash`'s admittedly-false general claim, and the accepted E1 gap where an untagged image behind an untrusted container is unreachable). One `--label` pair at the same site that already sets `--tag` gives docker the identity natively and makes the untagged-overlay gap disappear. Not `retire`: the container-arm resolution and the `docker rmi`-by-ref (not id) behaviour still need Python, images built before the label lands stay label-less, and the tag arm must survive one transition period — hence refactor, not delete."
probe = "Add `--label dotfiles.workspace=${localEnv:DEVCONTAINER_WORKSPACE_HASH}` to .devcontainer/devcontainer.json:103 `build.options`, run `mise run dev-rebuild`, then `docker image ls -aq --filter label=dotfiles.workspace=$(mise run --quiet ssh-port >/dev/null; uv run --project python dotfiles-setup devcontainer name hash)` and confirm it returns the overlay ids `dotfiles-setup devcontainer teardown-images` returns."
correction = "**⚠ THE STRONGER EVIDENCE FOR THIS ROW WAS ON DISK AND UNCITED THROUGH ROUNDS 1-5 (added round 6), AND IT CARRIES A CAVEAT THAT WOULD SINK A NAIVE IMPLEMENTATION.** The `alternative_evidence` above proves the passthrough by INFERENCE FROM OBSERVED USAGE (\".devcontainer/devcontainer.json:103 already ships `\\\"options\\\": [\\\"--tag=...\\\", \\\"--platform=...\\\"]`\"). A source-level proof of the same fact, read from the EXACT pinned install, was already saved: `.agent/kb/raw/devcontainers-cli-089-build-options-label-probe.md` (2026-09-09 11:24), reading `~/.local/share/mise/installs/npm-devcontainers-cli/0.89.0/node_modules/@devcontainers/cli/dist/spec-node/devContainersSpecCLI.js`. At **line 470** of that bundle: `let D=(N=s.build)==null?void 0:N.options;D!=null&&D.length&&l.push(...D),l.push(...c),l.push(await QI(xG(n,s),n));` — `s` is the resolved devcontainer.json config and `l` is the exact arg array handed to `docker build`/`docker buildx build`, on the single-Dockerfile path (`IW`, called from `Sp`) this repo uses. So `build.options` is a genuine raw docker-CLI-flag passthrough, not a fixed allowlist, and `--label=dotfiles.workspace=<hash>` would reach `docker build` identically (probe :5-18). **THE CAVEAT, which appears nowhere else in this report:** the CLI ALSO has a first-class `additionalLabels` → `--label` mechanism (`...e.additionalLabels.length>0?e.additionalLabels.map(f=>[\"--label\",f]).flat():[]...` in `FQ`), but it is populated ONLY by the standalone `devcontainer build` subcommand's own `--label` flag — **every other call site, `devcontainer up` included, hardcodes `additionalLabels:[]`** (probe :20-36). Since `mise.toml [tasks.up]` drives `devcontainer up`, the native label path is UNREACHABLE from this repo's invocation and `build.options` is the only route. An implementer who reads \"the CLI has a --label flag, use that instead\" will wire a mechanism that silently does nothing. The live host arms in the older `docker-cli-label-filter-probe.md` remain valid and are unaffected."
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-devcontainer_names-2"
unit = "mod-devcontainer_names"
kind = "module"
disposition = "retire"
confidence = 0.55
custom_thing = "The one-shot pre-#677 home-volume migration: `plan_home_volume_migration`, `migrate_home_volume_main`, `_probe_volume`, `_volume_is_mounted`, `migration_platform_refusal`, `render_plan`, `HomeVolumeMigration`, `MIGRATION_MARKER`, `REFUSED_ACTIONS`, `_MIGRATION_IMAGE`, `_PROBE_SCRIPT`, and the `legacy-volume` name field — plus `mise run migrate-home-volume` and ~160 lines of tests."
evidence = "python/src/dotfiles_setup/devcontainer_names.py:369-401, :404-522, :525-529, :532-575, :1014-1027, :1030-1104; mise.toml:336-342 (`[tasks.migrate-home-volume]`); tests/test_devcontainer_names.py:1014-1100+ (six migration tests)."
native_alternative = "None — docker has no volume-copy or volume-rename verb, so the busybox `cp -a` recipe is the canonical mechanism. The reason to remove is COMPLETION, not native coverage: the migration source no longer exists."
alternative_evidence = ".agent/kb/raw/docker-cli-label-filter-probe.md (probed 2026-09-09, this host): every one of the 10 `dotfiles-*` volumes is arch-scoped (`...-amd64-home`); ZERO carry the pre-#677 `<stem>-home` shape that `legacy_home_volume` names (python/src/dotfiles_setup/devcontainer_names.py:239-242). Control arm: the same `docker volume ls --format '{{.Name}}'` shape returned 10 rows out of 44 total volumes, so the query discriminates rather than being blind."
rationale = "This is a migration, not a capability — its trigger condition (`names.legacy_home_volume in existing_volumes`) is now false on the only host that has ever run it, so every invocation returns `nothing-to-migrate`. Retiring it deletes ~200 lines of module code, a mise task, six tests and a docker-image dependency (`busybox:stable`). Confidence is held at 0.55 deliberately: the probe speaks only for THIS host at THIS moment (probes-need-a-control-arm rule 3 — the sneakiest bound is when you ran it), and any other clone or machine that never ran `up` since #677 could still hold a legacy volume. Retire only after the cross-host probe below, and land the removal with a note in the #677 receipt so the recipe stays recoverable from git history."
probe = "On every host/clone that predates #677: `docker volume ls --format '{{.Name}}' | grep -E '^dotfiles-.*[^0-9a-z]home$' | grep -vE -- '-(amd64|arm64)-home$'` — an empty result on all of them (with a control arm: the same command minus the second grep must return the arch-scoped volumes) clears the retirement."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-devcontainer_names-3"
unit = "mod-devcontainer_names"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`workspace_hash()` — an 8-char SHA-256 prefix of the resolved absolute workspace path, hand-rolled in Python and pinned byte-for-byte against the retired `scripts/workspace-hash.sh`."
evidence = "python/src/dotfiles_setup/devcontainer_names.py:157-175; goldens at tests/test_devcontainer_names.py:83-99."
native_alternative = "mise's template engine ships this exact primitive: the `hash` filter, sha256 by default with a `len` truncation — `{{ config_root | hash(len=8) }}`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/templates.md:446 (`str | hash([algorithm], [len]) -> String`), :447 (`algorithm: \"sha256\" | \"blake3\"`, default sha256), :452 (`{{ \"foo\" | hash(len=8) }}` — SHA256 truncated to 8 characters)."
rationale = "Justification, not a default: the digest has six Python consumers that a mise template cannot serve — container.py:40, docker.py:27, image_lock.py:51, lock_shared.py:137, sync.py:79 and lint.py:55 all import from this module, and mise filters are only evaluable inside mise config templates. Two further blockers: mise's `config_root` is the config directory and is not documented as symlink-resolved, while `workspace_hash` deliberately hashes the PHYSICAL path (:166-172) so two logical routes to one directory are one workspace; and the digest is load-bearing for already-deployed volume names — a drifting digest renames every home and reads as an empty home, not an error. Worth recording so nobody 'modernises' the derivation into a template: the native filter agrees on algorithm and truncation, which is exactly what makes the swap look safe and be wrong."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-devcontainer_names-4"
unit = "mod-devcontainer_names"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "`ssh_port()` — deriving the host-side SSH port from `sha256(workspace \\0 arch) % 10000 + 20000`, with a hand-written override validator (`isdigit()` + 1024-65535 range) instead of letting docker pick a port."
evidence = "python/src/dotfiles_setup/devcontainer_names.py:113-123 (the derived window), :178-211 (derivation + override validation); consumed by mise.toml:329-334 (`[tasks.ssh-port]`) and the `ssh-keygen -R \"[localhost]:${DEVCONTAINER_SSH_PORT}\"` line in `[tasks.up]`."
native_alternative = "Docker's native ephemeral publish — `-p 0:2222` asks the kernel for a free host port at bind time, and `docker port <container> 2222` reads the assignment back, so no derivation and no collision window are needed."
alternative_evidence = ".agent/kb/raw/docker-cli-label-filter-probe.md: `docker port --help` -> `docker port CONTAINER [PRIVATE_PORT[/PROTO]] — List port mappings or a specific mapping for the container`; `docker run --help` line 136 `-p, --publish list  Publish a container's port(s) to the host` (host docker-cli 29.7.2, probed 2026-09-09)."
rationale = "Justification: the port must exist BEFORE the container does. devcontainer.json substitutes `${localEnv:DEVCONTAINER_SSH_PORT}` at `up` time, and `[tasks.up]` clears the stale host key with `ssh-keygen -R \"[localhost]:$PORT\"` before the container is created — `docker port` cannot answer either question because there is nothing to inspect yet. The derived port is also STABLE across recreate, which is what makes the documented R1 UX (`ssh ${USER}@localhost -p $(mise run ssh-port)`, a `~/.ssh/config` Host entry, one known-hosts line) work at all; an ephemeral port moves on every rebuild and turns each recreate into a fresh host-key mismatch. The one thing the native form buys — guaranteed no collision — is already bought differently, by placing the window at 20000-29999, below the macOS ephemeral range (:113-118)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-devcontainer_names-5"
unit = "mod-devcontainer_names"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "`names_env()` + `devcontainer_env_main()` — a shell-export bridge (`export KEY=<shlex.quote>`) consumed as `_dc_env=\"$(… devcontainer env)\"; eval \"${_dc_env}\"` at the head of roughly seven mise task bodies."
evidence = "python/src/dotfiles_setup/devcontainer_names.py:324-340, :1107-1119; call sites mise.toml:303, :355, :408, :419, :474, :1026 and mise.toml:327 (`[tasks.names]`)."
native_alternative = "mise's own env-injection directive `env._.source`, which sources a script and pulls its exported variables into the mise environment (single source, array of sources, relative/absolute paths, `redact` and `tools` options)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/environments/index.md:481-487 (`env._.source` directive and example), :489-498 (it **must** be a script sourced by bash; \"The shebang will be **ignored**\"), :510-516 (supported forms); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/troubleshooting.md:46 (\"Expensive `_.source` scripts in `mise.toml` — these re-run on every prompt\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/security.md:56 (`_.source` is treated as code execution and is ignored in safe mode)."
rationale = "Justification: `_.source` is documented as bash-only with the shebang ignored (index.md:489-498), so adopting it would require re-introducing a `scripts/*.sh` shim purely to call `uv run … devcontainer env` — a new file gated by `bash_logic_budget` and squarely against [[zero-bash-logic]]. It is also `[env]`-scoped rather than task-scoped, so a resolver that spawns `uv`, `platform_target` and a path resolution would run on every shell prompt (troubleshooting.md:46) rather than once per devloop task, and it silently vanishes under mise safe mode (security.md:56) — the failure shape `[tasks.up]`'s comment already warns about, where empty `${localEnv:...}` substitutions produce an unnamed container instead of an error. The current `_dc_env=\"$(…)\"` -then-`eval` form is deliberate for exactly that reason (mise.toml:296-302: assignment propagates the resolver's failure, bare `eval` would not)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-devcontainer_names-6"
unit = "mod-devcontainer_names"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`DevcontainerNames.id_labels` / `.id_flags` — composing `--id-label dotfiles.workspace=<hash> --id-label dotfiles.arch=<arch>` as one whitespace-free string meant to be splatted unquoted into `devcontainer exec`."
evidence = "python/src/dotfiles_setup/devcontainer_names.py:141-154 (why both labels are required; `--id-label` REPLACES the inferred set), :254-275; call sites mise.toml:401-411, :421, :501-551."
native_alternative = "This IS the native mechanism — @devcontainers/cli's `--id-label`, which the module exists solely to parameterise. The custom part is only the arch/workspace value derivation, which no CLI flag supplies."
alternative_evidence = "python/src/dotfiles_setup/devcontainer_names.py:143-148 cites the CLI's own resolution path (`devContainersSpecCLI.js`, function `bg`: `if (idLabels) return {...}`, else `[devcontainer.local_folder=<folder>]`), i.e. the native lookup is per-FOLDER and cannot discriminate architecture; mise.toml:399-406 records the same finding at the call site. Corroborated on this host: `docker ps -aq --filter label=dotfiles.workspace` -> 1 container (.agent/kb/raw/docker-cli-label-filter-probe.md), so the labels really are attached by the CLI."
rationale = "Justification: the unit already wraps the native feature rather than replacing it — there is nothing to retire and nothing further to adopt. The CLI's native inference is exactly what is insufficient (per-folder, arch-blind), and no flag derives a per-clone+arch value, so the derivation has to live somewhere. One residual smell worth naming without acting on it here: `id_flags` is a pre-joined string used UNQUOTED (`devcontainer exec … $DEVCONTAINER_ID_FLAGS`), safe only by the whitespace-free construction the tests pin at tests/test_devcontainer_names.py:279 — a Python caller should prefer `id_labels` and never re-split the string."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-devcontainer_names-7"
unit = "mod-devcontainer_names"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "Volume scoping by NAME PREFIX rather than by label: `legacy_home_volume` is exposed as the `legacy-volume` name field mainly so two mise task bodies can strip `-home` off it in bash to recover a `dotfiles-<basename>-<user>-<hash>` stem, then `grep -E \"^${_stem}\"` the volume list."
evidence = "python/src/dotfiles_setup/devcontainer_names.py:239-242 (`legacy_home_volume`), :130-139 (`legacy-volume` in `NAME_FIELDS`); mise.toml:428-429 and mise.toml:1081-1082 (`_stem=\"$(… devcontainer name legacy-volume)\"; _stem=\"${_stem%-home}\"`), mise.toml:433-435 (`docker volume ls --format '{{.Name}}' | grep -E \"^${_stem}\"`)."
native_alternative = "Docker's native volume labelling: `docker volume create --label dotfiles.workspace=<hash> --label dotfiles.arch=<arch> <name>` in `[tasks.up]` before `devcontainer up` (docker reuses an existing named volume, so pre-creating is idempotent), then `docker volume ls -q --filter label=dotfiles.workspace=<hash>` — the same identity mechanism `--id-label` gives containers."
alternative_evidence = ".agent/kb/raw/docker-cli-label-filter-probe.md (host docker-cli 29.7.2, probed 2026-09-09): `docker volume create --help` line 7 `--label list  Set metadata for a volume`; `docker volume ls --filter label=qq7x2v.absent` is ACCEPTED (headers, empty result) while the control arm `docker volume ls --filter zzqnonce=1` errors `invalid filter 'zzqnonce'` — so `label` is a real volume filter key, armed in both directions."
rationale = "A name-prefix grep is the same class of defect #803 closed for images: `^dotfiles-<basename>-<user>-<hash>` is a string convention, not an identity, and the stem is reconstructed by SHELL SURGERY on a value this module hands out for an unrelated purpose (`legacy-volume` documents itself as \"the migration source\", :241). It also couples the prune/ps scope to the migration code that finding -2 proposes to delete — remove `legacy_home_volume` and both task bodies break in a way no test in this repo would catch. The refactor is to add `--label` at volume-create time and expose a `stem` (or `volume-label`) field from `DevcontainerNames` rather than have bash derive one; the label path additionally survives any future rename of the name scheme."
probe = "In `[tasks.up]`, before `devcontainer up`: `docker volume create --label dotfiles.workspace=\"$DEVCONTAINER_WORKSPACE_HASH\" --label dotfiles.arch=\"$DEVCONTAINER_ARCH\" \"$DEVCONTAINER_HOME_VOLUME\"`, then confirm `docker volume ls -q --filter label=dotfiles.workspace=$(uv run --project python dotfiles-setup devcontainer name hash)` returns exactly the set the current `grep -E \"^${_stem}\"` returns, and that re-running `up` on an existing volume neither errors nor recreates it."
correction = "**⚠ A SECOND ON-DISK PROBE CARRIES THIS ROW'S LIVE ARMS AND WAS UNCITED THROUGH ROUNDS 1-5 (added round 6).** The row cites `.agent/kb/raw/docker-cli-label-filter-probe.md`, whose volume arm proves only that `label` is an ACCEPTED filter key (`--filter label=qq7x2v.absent` returns headers + empty, while `--filter zzqnonce=1` errors `invalid filter 'zzqnonce'`). A stronger, end-to-end arm was already saved in `.agent/kb/raw/devcontainers-cli-089-build-options-label-probe.md:49-54`: `docker volume create --label dotfiles.currencyprobe=qq7x2vvolabsent testprobe-vol-qq7x2v` → `docker volume ls -q --filter label=dotfiles.currencyprobe=qq7x2vvolabsent` returns `testprobe-vol-qq7x2v`, while the absent-nonce arm `--filter label=dotfiles.currencyprobe=totallydifferentnoncevol999` returns nothing — i.e. the create-then-filter round trip this row's `probe` field proposes was actually EXECUTED on this host, both arms, and it works at docker-cli 29.7.2. The same file records `docker volume --help` at 29.7.2 listing only `create, inspect, ls, prune, rm` — **no `rename`/`update` verb** — which is the constraint that decides the migration shape: an existing unlabelled volume cannot be labelled in place, so the label can only be attached at create time (as this row proposes) and pre-existing volumes stay unlabelled until recreated. That is the same one-transition-period caveat `mod-devcontainer_names-1` states for images, and it belongs on this row too. ⚠ Note also that the row's own `native_alternative` asserts \"docker reuses an existing named volume, so pre-creating is idempotent\" — that clause is NOT covered by either probe and is the one part of this row still standing on assertion; the row's `probe` field already names the re-run that would settle it."
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-doc_refs-1"
unit = "mod-doc_refs"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Backtick code-span path-reference existence checker: `_SPAN_RE` extracts every `...` span from the scoped agent docs, `_is_path_candidate` filters it to things that look like a repo path, and `find_unresolved_refs` resolves it (exact path, tracked basename, tracked suffix) or fails, with a justified allowlist."
evidence = "python/src/dotfiles_setup/doc_refs.py:32 (`_SPAN_RE = re.compile(r\"`([^`\\n]+)`\")`), :231 `_is_path_candidate`, :454 `find_unresolved_refs`; wired as hk step hk.pkl:514-517 (`check = \"uv run --project python dotfiles-setup check-doc-refs\"`) and CLI python/src/dotfiles_setup/main.py:2190 `handle_check_doc_refs`."
native_alternative = "The three markdown/agent linters that come closest all validate LINK SYNTAX, not code spans: rumdl MD057 (pinned 0.2.63) validates only `[text](path.md)` inline links and `[ref]: path.md` definitions and explicitly skips wikilinks; agnix REF-002 (pinned 0.52.1) validates markdown links only, via `extract_markdown_links`. The one tool that does check backtick spans, agentlinter `consistency/stale-file-reference`, is NOT pinned in this repo and resolves refs with `fs.existsSync` against the workspace/root dir only."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/rumdl/docs/link-validation.md:41-44 (\"What gets validated: Inline links / Reference-style links\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/rumdl/docs/md057.md:10 (\"Wikilinks and wiki embeds ... are not checked\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/imports.rs:8 and :726 (`let links = extract_markdown_links(content);`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agentlinter/packages/cli/src/engine/rules/freshness.ts:12 (backtick `file.ext` pattern) and :192 (`id: \"consistency/stale-file-reference\"`), resolving at :235-246 with `fs.existsSync`."
rationale = "No PINNED tool in this repo reads a backtick code span as a file citation — the module's own docstring claim (doc_refs.py:5-7) is still true at rumdl 0.2.63 and agnix 0.52.1, verified in their source/docs rather than assumed. The only tool that does (agentlinter 2.3.0, unpinned) would be a strictly worse swap here: it resolves by filesystem existence with no git-tracked model (the exact local/CI divergence finding 2 exists to catch), has no basename/suffix resolution — so this repo's dominant `ci.yml`-style bare citation (~60 of ~100 refs, doc_refs.py:14-15) would be reported broken — and offers no per-entry justified allowlist like `_ALLOWED_ABSENT`/`_ALLOWED_PREFIXES` (doc_refs.py:90-186). Adopting it means a new npm dependency that reports more false positives and fewer true ones."
probe = "npx agentlinter@2.3.0 --rule consistency/stale-file-reference . 2>&1 | head -40  # expect a wall of false positives on bare `ci.yml`-style refs; control arm: plant a real stale ref and confirm it is also reported"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doc_refs-2"
unit = "mod-doc_refs"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "`find_local_only_refs` — the closed-world half: a ref that exists on disk but is neither a tracked file nor a directory containing one is flagged, so a gitignored artifact cannot make a doc ref resolve locally and fail in a fresh CI checkout (the PR #359 failure)."
evidence = "python/src/dotfiles_setup/doc_refs.py:260-298 (`find_local_only_refs`, tracked-set membership + `any(p.startswith(bare + \"/\"))`), pinned by tests/test_doc_refs.py:81 `test_no_doc_ref_resolves_only_on_this_machine`."
native_alternative = "rumdl 0.2.61 added `--stdin-batch` (NUL-framed multi-document linting) with `--stdin-batch-closed-world` for \"supplied-document-only link resolution\" — i.e. resolve links against ONLY the documents you feed it (e.g. `git ls-files`), which is the same closed-world idea. It applies to MD057 link resolution, not to code spans."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/rumdl/CHANGELOG.md:20 (0.2.61: \"add `--stdin-batch` for NUL-framed multi-document linting and `--stdin-batch-closed-world` for supplied-document-only link resolution\"); scope limited to links per /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/rumdl/docs/link-validation.md:41-44."
rationale = "The native closed-world mechanism exists but only for markdown link syntax, and only if the ref set is fed as documents on stdin — it cannot see the backtick citations this repo actually writes, and the closed world it builds is over the SUPPLIED DOCUMENTS, not over the tracked-file universe that a citation like `.github/workflows/ci.yml` must resolve against. Retiring the custom check would reopen exactly the divergence it was written for. Worth revisiting only if the repo ever converts backtick citations to real markdown links (hk.pkl:361-363 records that rumdl adoption is deferred behind a 591-finding burn-down, so that is not near-term)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doc_refs-3"
unit = "mod-doc_refs"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`declared_mise_tasks` hand-parses `mise.toml` + `.config/mise/conf.d/*.toml` with `tomllib`, collecting task names and string/list `alias` values, deliberately avoiding `mise tasks` because it merges the invoking user's global config (this Mac contributes four `update:*` tasks)."
evidence = "python/src/dotfiles_setup/doc_refs.py:344-371 (`declared_mise_tasks`, docstring \"Deliberately not ``mise tasks``\"), consumed by :398 `find_unresolved_task_refs`; contract python/verification/suites.toml:2042-2044 pins `def declared_mise_tasks(`."
native_alternative = "`mise tasks ls --local --json` — `--local` is documented as \"Only show non-global tasks\", which is precisely the global-config bleed the docstring cites as the reason for hand-parsing; `--json` emits per-task `aliases`, `source` (the config file the task came from), `config_sources` and `hide`, so alias handling and tracked-vs-untracked (`mise.local.toml`) filtering are both available natively, along with file tasks (`mise-tasks/`, `.mise/tasks/`) that a `[tasks]`-only TOML parse cannot see."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/tasks/ls.md:18-19 (`-J --json`, `-l --local` \"Only show non-global tasks\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/tasks/ls.rs:288-297 (JSON emits `\"aliases\"`, `\"source\"`, `\"config_sources\"`, `\"hide\"`). mise gap digest confirms the host is already on the newest release, so no newer flag exists: .agent/kb/raw/mise-releases-since-pin.digest.md."
rationale = "The stated justification for the custom parser (\"`mise tasks` merges the user's global config\") is answered by a flag the module does not mention — `--local` — and the JSON payload already carries `aliases` and `source`, so the hand-rolled alias branch (doc_refs.py:365-370) and the config-file enumeration are re-implementations. Filtering `--local --json` output by a `source` that `git ls-files` tracks would keep the tracked-only binding AND pick up file tasks and any config shape mise supports, closing a latent false-positive hole (a doc citing a `mise-tasks/` file task is reported broken today; the repo has no such directory yet, so the hole is latent, not live). Not 'retire': the subprocess costs offline-ness, which doc_refs.py:351-353 gives as a second reason, so the honest move is to wrap the native command (or keep the parse and cite `--local` as the reason it is still equivalent), not to delete the resolver."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && mise tasks ls --json --hidden | python3 -c 'import json,sys; d=json.load(sys.stdin); print(sorted(t[\"name\"] for t in d if \"update\" in t[\"name\"]))' && mise tasks ls --local --json --hidden | python3 -c 'import json,sys; d=json.load(sys.stdin); print(sorted(t[\"name\"] for t in d if \"update\" in t[\"name\"])); print(sorted({t[\"source\"] for t in d}))'   # control arm: the four global update:* tasks must appear in the first list and be ABSENT from the second"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-doc_refs-4"
unit = "mod-doc_refs"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`find_unresolved_skill_refs` — resolves two prose citation forms, `` `name` skill `` and `[[name]]` wikilinks, against `.claude/skills/*/SKILL.md` plus `.claude/rules/*.md` stems, with exclusions for backticked wikilinks (syntax mentions) and `plugin:skill` names."
evidence = "python/src/dotfiles_setup/doc_refs.py:315-316 (`_SKILL_RE`, `_WIKILINK_RE`), :374 `declared_skills`, :382 `_declared_rules`, :425-451 `find_unresolved_skill_refs`; contract python/verification/suites.toml:2055-2075."
native_alternative = "agnix CC-AG-005 \"Referenced Skill Not Found\" is the nearest native rule, but it reads the `skills:` ARRAY in agent-file frontmatter, not prose citations. For wikilinks there is no native checker at all: rumdl MD057 documents that `[[page]]` is skipped by design, and agnix has no wikilink handling anywhere in its crates. Claude Code's own newest surface (`/skill-doctor` at 2.1.261, `claude plugin validate --json` at 2.1.259) reports unused/costly loaded skills and validates plugin manifests — neither resolves a skill named in a doc."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/agent.rs:578-590 (`// CC-AG-005: Referenced skill not found` -> `if let Some(skills) = &schema.skills`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/rumdl/docs/md057.md:10; control arm for the agnix wikilink claim: `grep -rli wikilink agnix/crates --include=*.rs` -> 0 files while `grep -rli \"markdown link\"` over the same tree -> 3 files (parsers/markdown.rs, schemas/agents_md.rs, rules/imports.rs), so the 0 is real, not a broken probe; .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (`/skill-doctor`, `claude plugin validate --json` bullets)."
rationale = "Both citation forms this repo actually uses are structurally outside every pinned linter's scope: agnix only reads a frontmatter array in agent files (this repo's citations are prose in rules/AGENTS.md), and wikilinks are explicitly out of scope for rumdl and entirely absent from agnix. The rules-plus-skills resolution set (`[[zero-skip-policy]]` is a RULE, doc_refs.py:428-431) is repo-specific and has no native analogue at all — a skills-only native checker would report every wikilink in `.claude/rules/` as broken."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doc_refs-5"
unit = "mod-doc_refs"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "The whole check-doc-refs gate (paths + mise tasks + skills) runs as a bespoke hk step, while three agent-doc linters are PINNED in mise.toml and invoked by nothing — so part of this capability may already be paid for and unused."
evidence = "mise.toml:30 (`\"npm:agents-lint\" = \"0.5.0\"`), :38 (`\"npm:claude-code-lint\" = \"0.8.0\"`), :39 (`\"npm:@contextlint/cli\" = \"1.1.1\"`); grep for those three names across mise.toml tasks, hk.pkl and .github/workflows/*.yml returns only the pin lines — no invocation (control arm: the same grep shape over `agnix` returns hk.pkl:491-498 and mise.toml:1259-1260, so the probe discriminates)."
native_alternative = "Unknown — `agents-lint`, `claude-code-lint` and `@contextlint/cli` have no offline corpus under knowledge-base/sources (the `cclint` tree there is `@felixgeelhaar/cclint` 0.16.0, a different package, whose only reference rule is ImportResolutionRule for `@path` imports)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/cclint/package.json:2-3 (`\"name\": \"@felixgeelhaar/cclint\", \"version\": \"0.16.0\"`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/cclint/src/rules/ImportResolutionRule.ts:13-17 (validates `@path/to/file` imports only). No path:line exists for the three pinned packages — that is the gap."
rationale = "Cannot decide retire/refactor for the gate's coverage overlap without knowing what the three pinned-but-unwired linters check. Per the citation rule this stays 'unknown' with the settling probe rather than a guess. Note the finding is real regardless of the answer: three pinned tools with zero invocations are either a wiring gap or dead pins."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && mise exec -- agents-lint --help 2>&1 | head -30 && mise exec -- claude-code-lint --help 2>&1 | head -30 && mise exec -- contextlint --help 2>&1 | head -30 && mise exec -- agents-lint . 2>&1 | tail -40   # look for any rule that resolves backtick file refs, `mise run <task>` names, or [[wikilinks]]; control arm: plant a bogus `.claude/rules/zzq-not-real.md` citation in a scratch copy and confirm the linter reports it"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-1"
unit = "mod-docker"
kind = "module"
disposition = "retire"
confidence = 0.88
custom_thing = "DevContainerManager.build() pulls the ~38GB published base with a CLASSIC `docker pull --platform <p> ghcr.io/ray-manaloto/dotfiles-devcontainer:dev` subprocess, then shells `devcontainer build --image-name <name> --platform <p>`."
evidence = "python/src/dotfiles_setup/docker.py:333-355 (`subprocess.run([docker, \"pull\", \"--platform\", platform, self.base_image], check=True, ...)` at :338-343); reachable only via `dotfiles-setup docker build` (python/src/dotfiles_setup/main.py:835 registers it, :1869-1870 dispatches). No live caller: `git grep -nE \"dotfiles-setup docker (build|up|test|down)\" -- mise.toml '.devcontainer/*' '.claude/**' 'python/verification/*' 'scripts/*' '.github/**'` returns 0 wiring hits (control arm: the same command shape for `initialize-host|sync` returns .devcontainer/devcontainer.json:235, mise.toml:939, python/verification/suites.toml:188/583/1145)."
native_alternative = "`mise run sync` -> `dotfiles-setup docker sync` (sync.py), which refreshes the local `:dev` tag with a BuildKit `docker buildx build --output type=docker` re-export after a `buildx imagetools inspect` digest compare — the project's canonical, guard-redirected replacement for classic pull. `mise run up` / `mise run dev-rebuild` delegate the base fetch to `devcontainer up`'s own buildx `FROM ${BASE_IMAGE}` resolution instead."
alternative_evidence = ".claude/rules/mise-tasks-only.md:15 (table row: `docker pull …dotfiles-devcontainer…` -> `mise run sync` (buildkit, digest-aware, verifying; classic pull wedges on ~38GB)); .claude/rules/verify-before-advancing.md:65 and :78 (\"buildkit, never classic `docker pull` which wedges on the large blob\"); mise.toml:448-451 (\"Standalone `docker pull` was removed — it bypassed the lifecycle and masked silent failures (PR #86 review)\"); python/src/dotfiles_setup/sync.py:663-669 (the buildx `--output type=docker` call); mise.toml:928-939 (`[tasks.sync]` -> `dotfiles-setup docker sync`)."
rationale = "This is a live copy of a pattern the repo banned twice over and re-routed to `mise run sync`, sitting in a code path with zero callers. It is worse than dead: the PreToolUse hook_guard only sees Bash strings, so a `docker pull` issued from a Python subprocess bypasses the redirect entirely — the guard cannot catch it. Delete `build()` together with the `dotfiles-setup docker build` verb (main.py:835, :1869-1870); nothing in mise.toml, hk*.pkl, .claude/settings.json or suites.toml references it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-2"
unit = "mod-docker"
kind = "module"
disposition = "retire"
confidence = 0.86
custom_thing = "DevContainerManager.up() calls `devcontainer up --workspace-folder <root> --remove-existing-container` with NO `--id-label` pair, and unconditionally rebuilds via self.build() first."
evidence = "python/src/dotfiles_setup/docker.py:357-368; verb registered at python/src/dotfiles_setup/main.py:836 and dispatched at :1871-1872. No wiring caller (same grep + control arm as mod-docker-1)."
native_alternative = "`mise run up` (and `mise run dev-rebuild`), which resolve `DEVCONTAINER_WORKSPACE_LABEL` / `DEVCONTAINER_ARCH_LABEL` from `dotfiles-setup devcontainer env` and pass both to the SAME native `devcontainer up` verb — the @devcontainers/cli 0.89.0 mechanism that makes per-architecture container identity real."
alternative_evidence = "mise.toml:240 `[tasks.up]`; mise.toml:311-318 (\"--id-label is what makes the arch scoping REAL (#677). @devcontainers/cli 0.88.0 looks an existing container up by id labels, and with none supplied it infers them from the WORKSPACE FOLDER alone … so without these an arm64 `up` in a directory that already has an amd64 container finds and reuses it\"); probed `mise exec -- devcontainer up --help` on the pinned npm:@devcontainers/cli 0.89.0 confirms `--id-label` is still the native scoping flag (control arm: the same help output lists `--secrets-file`, `--remote-env`, so the flag inventory is real)."
rationale = "Not merely superseded — actively wrong at today's pins. Without the two `--id-label` flags this verb reuses (and with `--remove-existing-container`, DESTROYS) the other architecture's container in the same folder, which is precisely the #677 defect mise.toml:311-318 documents. The native `devcontainer up` is already the mechanism; `mise run up` is the correct thin caller. Delete `up()` and the `dotfiles-setup docker up` verb."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-3"
unit = "mod-docker"
kind = "module"
disposition = "retire"
confidence = 0.85
custom_thing = "DevContainerManager.test() shells a hand-composed `bash -lc` string through `devcontainer exec` running `dotfiles-setup audit --all`, `pytest tests/test_bootstrap.py` and `bats tests/infra/*.bats`, with the workspace path hardcoded as `/workspaces/dotfiles`."
evidence = "python/src/dotfiles_setup/docker.py:405-427 (hardcoded `cd /workspaces/dotfiles/python` at :412, `cd /workspaces/dotfiles` at :413); verb at python/src/dotfiles_setup/main.py:837, dispatch at :1873-1874. No wiring caller (same grep + control arm as mod-docker-1)."
native_alternative = "`mise run smoke` — `devcontainer exec --workspace-folder . $DEVCONTAINER_ID_FLAGS scripts/devcontainer-smoke.sh` (tiers 1-3, including the tier-1 image-identity check), and `mise run verify-container-latest` -> `dotfiles-setup docker verify-latest` for the latest-code + current-base gate."
alternative_evidence = "mise.toml:399-411 (`[tasks.smoke]`, the `devcontainer exec … $DEVCONTAINER_ID_FLAGS scripts/devcontainer-smoke.sh` line at :411); mise.toml:941-949 (`[tasks.verify-container-latest]` -> `uv run --project python dotfiles-setup docker verify-latest`); .claude/rules/verify-before-advancing.md 'check matrix' devcontainer row names `mise run verify-container-latest` / `mise run verify-local`, never `dotfiles-setup docker test`."
rationale = "The in-container verification surface moved to scripts/devcontainer-smoke.sh + the tiered `sync`/`verify-latest` python path, both of which are id-label scoped and cited by the rules. This copy is unreferenced AND doubly stale: it resolves the container by folder alone (smokes the wrong architecture once both are up, #677) and hardcodes `/workspaces/dotfiles`, so it breaks in any clone whose basename is not `dotfiles` — while devcontainer.json:workspaceFolder is `/workspaces/${localWorkspaceFolderBasename}`. Delete `test()` and the `dotfiles-setup docker test` verb."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-4"
unit = "mod-docker"
kind = "module"
disposition = "retire"
confidence = 0.80
custom_thing = "DevContainerManager.down() re-implements teardown: resolve ids via `teardown_container_ids(resolve_names(workspace=abs_root))`, then loop `docker stop <id>` + `docker rm -f <id>`."
evidence = "python/src/dotfiles_setup/docker.py:370-403; verb at python/src/dotfiles_setup/main.py:838, dispatch at :1875-1876. Prior cold review already recorded it as near-dead: docs/research/kb/reports/agents/cold-review-800-r2-20260828.md:50 (\"`mise run down` is an alias for `[tasks.stop]` … `DevContainerManager.down()` is reached only by `dotfiles-setup docker down`\")."
native_alternative = "`mise run stop` (alias `down`), which calls the SAME python resolver as its only logic — `container_ids=\"$(uv run --project python dotfiles-setup devcontainer teardown)\"` — then `docker rm -f ${container_ids}`. There is NO native `devcontainer down` verb to adopt instead: probed `mise exec -- devcontainer --help` at the pinned 0.89.0 lists up / set-up / build / run-user-commands / read-configuration / outdated / upgrade / features / templates / exec and nothing else (control arm: `up` — a verb known to exist — IS in that list, so the absence of `down` is a real absence, not a broken probe)."
alternative_evidence = "mise.toml:1174-1198 (`[tasks.stop]`, `alias = \"down\"`; the resolver call at :1191, `docker rm -f` at :1194; comment at :1175 'no devcontainer down verb'); python/src/dotfiles_setup/devcontainer_names.py `teardown_container_ids` (imported at python/src/dotfiles_setup/docker.py:27-32)."
rationale = "The docker-CLI fallback itself is justified (the native CLI still has no `down` at 0.89.0 — probed with a control arm), but it already exists once, in `[tasks.stop]` over `dotfiles-setup devcontainer teardown`. This is a second implementation of the same decision, differing only in its stop-then-rm loop, and its sole entry point is a CLI verb no task, hook, workflow or contract invokes. Retire the verb + method and keep the single `devcontainer teardown` path; the module's teardown tests (tests/test_docker.py:155-233) should move to the `devcontainer_names` teardown surface rather than be deleted."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-5"
unit = "mod-docker"
kind = "module"
disposition = "retire"
confidence = 0.90
custom_thing = "Class constants `DEFAULT_IMAGE_NAME = \"dotfiles-dev-local\"` and `DEFAULT_BASE_IMAGE = \"ghcr.io/ray-manaloto/dotfiles-devcontainer:dev\"` on DevContainerManager."
evidence = "python/src/dotfiles_setup/docker.py:284-285. `git grep -n \"DEFAULT_IMAGE_NAME\\|DEFAULT_BASE_IMAGE\" -- python tests hk.pkl python/verification` returns exactly those two definition lines and no reader (control arm: `git grep -c DevContainerManager -- python tests` returns 3 files, so the grep shape does find real symbols). `__init__` reads `self.config.container.image` / `.base_image` instead (docker.py:302-303)."
native_alternative = "The base-image default is already a single declared parameter elsewhere: mise's task-scoped templated env `BASE_IMAGE = \"{{ env.BASE_IMAGE | default(value='ghcr.io/ray-manaloto/dotfiles-devcontainer:dev') }}\"`, consumed by devcontainer.json `build.args.BASE_IMAGE`, with `DotfilesConfig.container.base_image` as the python-side reader."
alternative_evidence = "mise.toml:287 (`[tasks.up]` env line defining BASE_IMAGE with its default) and the same line in `[tasks.dev-rebuild]` (mise.toml:349); .devcontainer/devcontainer.json build.args `\"BASE_IMAGE\": \"${localEnv:BASE_IMAGE}\"`; python/src/dotfiles_setup/docker.py:301-303 (the config-backed read that supersedes the constants)."
rationale = "Dead symbols that also duplicate a registry-ref literal whose one declaration point is the mise task env — exactly the drift shape `.claude/rules/use-tool-builtins.md` and the `no_platform_literals` doctrine exist to prevent, except nothing gates a base-image literal. Zero readers, so deletion is free and removes a second place a `:dev` -> other-tag change would have to be made."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-6"
unit = "mod-docker"
kind = "module"
disposition = "keep"
confidence = 0.82
custom_thing = "download_doppler_env(): run `doppler secrets download --format docker --no-file --project <p> --config <c>` under a 120s timeout, reject rc!=0 and empty stdout with SecretsDownloadError, then write the payload to a per-workspace+arch path via os.open(0o600) + temp-file rename."
evidence = "python/src/dotfiles_setup/docker.py:101-153 (subprocess at :115-132, guards at :133-138, born-0600 write-then-rename at :143-152); path derivation at docker.py:64-81 via `doppler_env_filename(names)`; wired as the devcontainer `initializeCommand` (.devcontainer/devcontainer.json:235) through `stage_host_secrets` (docker.py:84-98) and `initialize_host` (docker.py:429-438). Tests: tests/test_docker.py:245-334."
native_alternative = "Considered and rejected: (a) doppler's own file-writing path `doppler secrets download <filepath>` — which IS natively atomic and 0400 — and (b) fnox's first-class `doppler` provider (`[providers] doppler = { type = \"doppler\", project = …, config = … }`), the mechanism devcontainer.json's own comment names as the #83 follow-up."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/doppler/pkg/cmd/secrets.go:554-568 — the file path ENCRYPTS before writing (`crypto.Encrypt(passphrase, body, \"base64\")` then `utils.WriteFile(...)`), so a natively-written file is a base64 ciphertext, not a docker env-file; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/doppler/pkg/utils/io.go:37-50 (`// WriteFile atomically writes data to a file` — temp + os.Rename) confirms the atomicity we re-implement exists ONLY on the encrypted path; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/docs/providers/doppler.md:16-21 shows the fnox provider is per-DECLARED-secret (`[secrets] DATABASE_URL = { provider = \"doppler\", value = \"DATABASE_URL\" }`), not a bulk export of a config."
rationale = "Required justification: the one native mechanism that would give us the atomic write (`doppler secrets download <path>`) encrypts its output, so it cannot feed `runArgs --env-file`; `--no-file` to stdout is the only usable native shape, and that leaves the caller owning the write. fnox's doppler provider enumerates secrets one declared name at a time and so cannot express \"inject this whole config\", which is what the container needs. The remaining custom surface — per-workspace+arch filename (#893), 0o600-at-creation, temp+rename, and the empty-download guard that reproduces the old `&& [ -s … ] &&` — has no native equivalent at today's pins (doppler 3.76.5, @devcontainers/cli 0.89.0)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-7"
unit = "mod-docker"
kind = "module"
disposition = "unknown"
confidence = 0.45
custom_thing = "The transport chosen for those secrets: a docker-format KEY=VALUE env file handed to `runArgs --env-file`, whose path this module mints (doppler_env_file) and devcontainer.json interpolates as ${localEnv:DEVCONTAINER_ENV_FILE}."
evidence = "python/src/dotfiles_setup/docker.py:64-81 and :140 (the path), :118-120 (`--format docker`); .devcontainer/devcontainer.json runArgs `\"--env-file\", \"${localEnv:DEVCONTAINER_ENV_FILE}\"`."
native_alternative = "@devcontainers/cli 0.89.0 ships `devcontainer up --secrets-file <path>` — \"Path to a json file containing secret environment variables as key-value pairs\" — a first-class secrets channel the repo does not use."
alternative_evidence = "Probed `mise exec -- devcontainer up --help` against the pinned npm:@devcontainers/cli 0.89.0: `--secrets-file  Path to a json file containing secret environment variables as key-value pairs.  [string]` (control arm: the same output lists `--remote-env` and `--dotfiles-repository`, known-present flags, so the inventory is real); the option is declared in the shipped bundle at /Users/rmanaloto/.local/share/mise/installs/npm-devcontainers-cli/0.89.0/node_modules/@devcontainers/cli/dist/spec-node/devContainersSpecCLI.js:668 (`\"secrets-file\":{type:\"string\",description:\"Path to a json file containing secret environment variables as key-value pairs.\"}`)."
rationale = "The flag plainly exists and would replace the JSON-vs-env-file half plus give log masking, but I could not establish from the bundled source whether `--secrets-file` sets CONTAINER-WIDE env (what `--env-file` at creation does, and what every interactive shell and `devcontainer exec` in this repo relies on) or only injects into lifecycle-command environments. That distinction decides retire-vs-keep, and asserting it from the flag's one-line description would be exactly the un-armed inference this audit forbids. Probe first; if it is lifecycle-only, this becomes a firm 'keep' for the `--env-file` route."
probe = "printf '{\"AUDIT_PROBE_KEY\":\"present\"}' > /tmp/dc-secrets-probe.json && mise exec -- devcontainer up --workspace-folder . --id-label \"$(uv run --project python dotfiles-setup devcontainer name workspace-label)\" --secrets-file /tmp/dc-secrets-probe.json >/tmp/dc-up.log 2>&1; echo \"rc=$?\" >> /tmp/dc-up.log; mise exec -- devcontainer exec --workspace-folder . sh -lc '[ -n \"$AUDIT_PROBE_KEY\" ] && echo SET || echo ABSENT'   # SET => container-wide (retire the --env-file route); ABSENT => lifecycle-only (keep). Control arm: rerun the exec asserting a var the current --env-file route DOES provide, which must print SET in both runs. Then rm -f /tmp/dc-secrets-probe.json."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-8"
unit = "mod-docker"
kind = "module"
disposition = "keep"
confidence = 0.87
custom_thing = "Host SSH-agent key staging for R1: _agent_public_keys (`ssh-add -L` under a chosen env), _ssh_agent_environment (macOS fallback that reads SSH_AUTH_SOCK out of `/bin/launchctl getenv` and validates it is a single absolute path), _write_host_authorized_keys (dedupe + 0600 write), initialize_host_ssh_runtime, host_authorized_keys."
evidence = "python/src/dotfiles_setup/docker.py:156-246; consumed by devcontainer.json's `initializeCommand` (.devcontainer/devcontainer.json:235) and by `postCreateCommand`'s `install -m 600 /tmp/dotfiles-host-state/authorized_keys ~/.ssh/authorized_keys`; hardening tests at tests/test_docker.py:23-108."
native_alternative = "Surveyed and none covers it: the devcontainers `sshd` feature installs openssh-server but silently drops its `port`/`username`/`startNow` options and stages no keys; @devcontainers/cli 0.89.0's nearest host-side hooks are `--dotfiles-repository` / `--dotfiles-install-command` (clone-and-run a dotfiles repo, not agent-key extraction) and `initializeCommand`, which is the lifecycle slot this code already occupies natively. Docker Desktop's `/run/host-services/ssh-auth.sock` is native and IS used — but only for R2 outbound, not for R1 inbound authorized_keys."
alternative_evidence = "Probed `mise exec -- devcontainer up --help` (pinned 0.89.0): the only dotfiles/host hooks are `--dotfiles-repository`, `--dotfiles-install-command`, `--dotfiles-target-path`; no agent/authorized-keys option (control arm as in mod-docker-7). .devcontainer/devcontainer.json:26-38 documents the sshd feature's dropped options and that `postCreateCommand` copies keys \"pre-written by `dotfiles-setup docker initialize-host` on the host from `ssh-add -L`\"; AGENTS.md 'Devcontainer success criteria' R1/R2 table."
rationale = "Required justification: no tool in the stack extracts the host agent's public keys and stages them for a container's sshd. The macOS-specific half is genuinely un-substitutable — a GUI-launched or hook-spawned process does not inherit the launchd SSH_AUTH_SOCK (the same class as do-not.md #1), so the `/bin/launchctl getenv` recovery, and its hostile-output validation (single line, absolute path), exist because nothing else can produce a working agent env there. The module is already mounted in the native `initializeCommand` slot, which is the correct adoption of the devcontainer lifecycle; only the key logic is ours, per zero-bash-logic."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-docker-9"
unit = "mod-docker"
kind = "module"
disposition = "keep"
confidence = 0.84
custom_thing = "verify_host_ssh_inbound(port, user): a hardened `ssh -o BatchMode=yes -o StrictHostKeyChecking=accept-new -o UserKnownHostsFile=/dev/null -o ConnectTimeout=10 -p <port> <user>@localhost 'hostname && whoami'` probe that fails unless the LAST stdout line is exactly the expected user, run under the same resolved agent env that staged the key."
evidence = "python/src/dotfiles_setup/docker.py:249-278; exposed as `dotfiles-setup docker verify-ssh-inbound` (main.py:843-848, dispatch :1879-1880) and wired live at mise.toml:1014 inside `[tasks.verify-ssh-inbound]`; identity-check tests at tests/test_docker.py:110-153."
native_alternative = "None found. Surveyed: @devcontainers/cli 0.89.0 has no health/verify verb (probed verb list: up, set-up, build, run-user-commands, read-configuration, outdated, upgrade, features, templates, exec); `mise doctor` reports mise's own state, not container reachability; the sshd feature ships no self-test; gh/renovate/graphify are out of domain."
alternative_evidence = "Probed `mise exec -- devcontainer --help` at the pinned 0.89.0 — the full verb list above, with `up` present as the control arm proving the probe discriminates; AGENTS.md 'Devcontainer success criteria' R1 row names `mise run verify-ssh-inbound` as the gate, i.e. this custom probe IS the declared mechanism."
rationale = "Required justification: R1 is a project-specific invariant (a passwordless host->container login landing as the HOST user on a per-workspace derived port); no upstream tool asserts it. The load-bearing part is the identity assertion — `lines[-1] != user` — which is what makes this a gate rather than a connectivity ping, and which nothing native offers. It also deliberately reuses `_ssh_agent_environment()` so the probe authenticates through the same agent that staged the key, closing the control-arm hole where a probe could pass on a different agent than the one under test."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-1"
unit = "mod-doctor"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "parse_mcp_list's row regex accepts only the glyphs [✔✘⏸!], so a server whose status Claude Code renders with ⊘ (Disabled for this project) never matches and its row is silently DROPPED — the exact failure shape the regex's own comment says a fixture caught once already."
evidence = "python/src/dotfiles_setup/doctor.py:944 (_MCP_LIST_RE glyph class), :941 comment 'a dropped row is a server reported healthy', :1033 (_CONSENT_STATES lists only 'Pending approval'/'Needs authentication'), :986 check_mcp_health"
native_alternative = "Claude Code v2.1.238+ prints '⊘ Disabled for this project (re-enable via /mcp)' as a first-class `claude mcp list` status for any server named in `disabledMcpServers`, alongside the '✘ Rejected (see disabledMcpjsonServers)' and 'not configured' (empty url) states — all statuses that postdate this parser."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/mcp.md:242 (⊘ Disabled, both `claude mcp list` and `claude mcp get`, behaviour changed at v2.1.238); mcp.md:241 (✘ Rejected, `claude mcp get` only); mcp.md:281 ('not configured' for an empty url)"
rationale = "The delegation to `claude mcp list` is the right native choice and should stay, but the parser has drifted behind the command's status vocabulary. Add ⊘ to the glyph class and treat 'Disabled for this project' the way _CONSENT_STATES already treats a consent state — reported as deliberate, not as a defect — so a disabled server is visible rather than invisible. Host is 2.1.266, well past the 2.1.238 that introduced the glyph."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-2"
unit = "mod-doctor"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "check_mcp_duplicate re-derives, from a four-source static parse, that one server name is registered more than once (the ~/.claude.json user-global entry shadowing the project .mcp.json / plugin one)."
evidence = "python/src/dotfiles_setup/doctor.py:766 check_mcp_duplicate; :334 claude_json_servers docstring naming the shadowing defect as the motivating case; :356 collect_servers"
native_alternative = "Claude Code now emits a 'Same name in more than one scope' configuration warning in `claude mcp list` output and in `/mcp`, quoting each scope's endpoint with ${VAR} references left unexpanded, and tells you the fix (`claude mcp remove <name> --scope <scope>`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/mcp.md:288 (under '#### Configuration warnings'); scope table at mcp.md:503-509"
rationale = "The module already shells out to `claude mcp list` in the live arm (doctor.py:1008), so the native warning is one parse away and covers the canonical local/project/user shadow. Two residues keep a custom part alive rather than a full retire: the native warning fires only when the endpoints DIFFER, and a plugin-contributed server is not one of the three documented scopes. Fold the static check down to those two residues and read the rest off the harness's own warning."
probe = "claude mcp list 2>&1 | grep -n -i 'more than one scope' # after temporarily registering one name at both user and project scope with different endpoints; control arm: the same grep with no duplicate registered must return 0"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-3"
unit = "mod-doctor"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "check_listing_budget flags any skill/agent listing entry over a HARD-CODED 1,536-char cap and totals standing listing characters against doctor.toml's max_chars = 34000."
evidence = "python/src/dotfiles_setup/doctor.py:1055 check_listing_budget; :71 imports SKILL_DESCRIPTION_MAX as a constant; :1206 registration in CHECKS; doctor.toml [listing] max_chars = 34000"
native_alternative = "Three native mechanisms now exist: `skillListingMaxDescChars` makes the per-entry cap a SETTING (so 1,536 is a default, not an invariant); `/doctor` prints an estimate of the listing's context cost and its biggest contributors and Claude Code writes a debug-log warning when the listing overflows its budget; the `/context` Skills row reports post-budget listing size; and `/skill-doctor` (shipped 2.1.261, host is 2.1.266) shows which loaded skills go unused and what they cost."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:2739 (skillListingMaxDescChars) and :2723 (skillListingBudgetFraction); skills.md:1054 (Run /doctor for an estimate ... debug-log warning on overflow); skills.md:1056 (/context Skills row is post-budget); commands.md:83 (/doctor 'Finds unused skills, MCP servers, and plugins versus their context cost'); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (/skill-doctor, 2.1.261)"
rationale = "The 'what does the listing cost' half is now native and better-informed (it knows the post-budget size and which skills go unused; the custom check can only sum source characters). Two things stay custom and justify keeping a reduced check: `.claude/agents/*.md` entries, which none of the native surfaces measure, and the reviewed doctor.toml ceiling as a SessionStart tripwire rather than an on-demand command. The concrete defect to fix now is the hard-coded cap: read `skillListingMaxDescChars` from the resolved settings and fall back to 1,536, or the check reports truncation that is not happening (cap raised) or misses truncation that is (cap lowered)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-4"
unit = "mod-doctor"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "check_mcp_env_opt_in walks every ${VAR} an MCP config interpolates and reports one this process cannot see, using the doctor's own environment as the oracle and fnox only to explain the absence."
evidence = "python/src/dotfiles_setup/doctor.py:456 check_mcp_env_opt_in; :107 _INTERPOLATION_RE matches both ${VAR} and ${VAR:-default}; :500 _explain_absence"
native_alternative = "Claude Code now warns natively about a missing environment variable: 'if a ${VAR} reference names a variable that isn't set AND HAS NO :-default, Claude Code warns in `claude mcp list` output and in /mcp, naming the variable, and still loads the server with the ${VAR} text unexpanded.'"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/mcp.md:290 and the same statement at mcp.md:635"
rationale = "The native warning is explicitly scoped to the no-default form. The defect this check was built for (#418) is the OPPOSITE form — `${CONTEXT7_API_KEY:-}` — where the default makes an absent variable a legal empty string, the server starts, reports connected, and serves an anonymous tier. Claude Code documents that shape as the recommended FIX for its own warning ('add a ${VAR:-default} fallback'), so adopting the native warning would retire the check for exactly the input it exists to catch. Keep it, and keep the ':-' branch of _INTERPOLATION_RE as the load-bearing part."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-5"
unit = "mod-doctor"
kind = "module"
disposition = "refactor"
confidence = 0.65
custom_thing = "collect_servers builds the MCP inventory from exactly four static sources (.mcp.json, each enabled plugin's .mcp.json resolved via marketplace.json, and ~/.claude.json's user-global and per-project blocks); every non-live check reasons off that tuple."
evidence = "python/src/dotfiles_setup/doctor.py:356 collect_servers docstring 'Every MCP server Claude Code loads here, from all four sources'; :296 plugin_mcp_path; :334 claude_json_servers"
native_alternative = "The set is no longer four: a managed deployment supplies servers through `managed-mcp.json` (and when it is present `claude mcp list` shows ONLY those), `managedMcpServers` arrived as a managed setting at 2.1.259, and `disabledMcpServers` / `disabledMcpjsonServers` mean a statically-parsed entry may be inert. `claude mcp list` — which this module already invokes — is the harness's own resolved view across every one of those."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/managed-mcp.md:106 ('`claude mcp list` shows only the servers in `managed-mcp.json`'); mcp.md:310 (disabledMcpServers opt-out covering user, plugin, connector and built-in servers); mcp.md:315 (the two unrelated *Mcpjson* lists); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (managedMcpServers, 2.1.259)"
rationale = "The static parse must stay — repo_owned, check_mcp_pin and check_mcp_scope all need the DECLARATION, which `claude mcp list` does not expose. What should change is the claim: the docstring's 'all four sources' is now false, and a server the operator disabled still contributes findings from pin/scope/guard-coverage checks. Reconcile the static tuple against the already-collected `claude mcp list` rows (mark unknown-to-static rows, and mark statically-known rows the harness reports as ⊘ Disabled) instead of asserting completeness."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-6"
unit = "mod-doctor"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "read_fnox hand-parses exactly one path, ~/.config/fnox/config.toml, for its env fields and sync coverage; check_fnox_baseline and check_exec_only_not_leaked both reason off that single file."
evidence = "python/src/dotfiles_setup/doctor.py:255 read_fnox; :400 collect() hard-codes home/'.config'/'fnox'/'config.toml'; :583 check_fnox_baseline; :625 check_exec_only_not_leaked"
native_alternative = "fnox ships `fnox config-files` — 'List all config files that would be loaded' — plus `fnox doctor` ('Show diagnostic information about the current fnox state') and `fnox check` ('Check if all required secrets are defined and configured', with `-a/--all`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/docs/cli/config-files.md (whole file); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/fnox.usage.kdl:49 (config-files), :83 (doctor), :35 (check)"
rationale = "fnox loads more than the one user config — fnox's own repo ships a project-root fnox.toml — so a project-level declaration is invisible to this parse today, and an env-mode override there would defeat both fnox checks silently. Enumerate the real set with `fnox config-files` and parse each, keeping the never-read-a-value discipline (do NOT reach for `fnox get`). The baseline comparison itself stays custom: neither `fnox check` nor `fnox doctor` knows about a REVIEWED name set, which is the bootstrap-config tripwire's whole point."
probe = "fnox config-files   # count the paths; control arm: the count must exceed 1 when a project fnox.toml exists in cwd and equal 1 when it does not"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-7"
unit = "mod-doctor"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "check_path_drift is registered in CHECKS and asks whether the ambient shell resolves the tools mise currently pins, reporting BLIND when the SessionStart hook did not capture DOTFILES_AMBIENT_PATH."
evidence = "python/src/dotfiles_setup/doctor.py:1159 check_path_drift; :1207 registration; :88 imports check_path_drift as shell_path_drift"
native_alternative = "`mise doctor` (with `-J --json`) ships two PATH analyses of its own — check_path_ordering ('mise tool paths are not first in PATH') and check_shim_shadowing ('mise shims are shadowed by executables earlier in PATH') — plus a `mise doctor path` subcommand. Claude Code's `/doctor` separately checks 'duplicate or leftover installs, PATH problems' for its own installation."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/doctor/mod.rs:934 (check_path_ordering), :1005 (check_shim_shadowing), :995 and :1047 (the two warning strings); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor.md:4 (`mise doctor [-J --json]`) and :17 (`mise doctor path`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/commands.md:83 (/doctor PATH problems, scoped to the Claude Code install)"
rationale = "Both native checks are about PATH ORDER and shim shadowing; neither compares the VERSION embedded in a PATH entry against the active pin, which is the drift this check exists for (a shell resolving hk 1.52.0 while the pin is 1.54.0 is perfectly ordered and unshadowed). Claude Code's /doctor PATH check is about its own duplicate installs. Keeping it also preserves the capture/blindness seam, which no external tool can supply because `mise doctor` invoked through mise sees a repaired PATH just as `mise run <task>` does."
probe = "DOTFILES_AMBIENT_PATH=\"$PATH\" mise doctor -J | jq -r '..|strings|select(test(\"PATH\"))'   # against a shell with a stale installs/<tool>/<old-version>/bin prepended; control arm: the same command on a clean shell must report nothing"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-8"
unit = "mod-doctor"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "check_mcp_guard_coverage requires every declared mutating MCP tool to carry a reviewed permission decision in the TRACKED .claude/settings.json or a PreToolUse matcher, and words a settings.local.json-only allow differently; check_mcp_pin rejects an unpinned npx/bunx spec for a repo-owned server."
evidence = "python/src/dotfiles_setup/doctor.py:725 check_mcp_guard_coverage; :720 _covered_by_rules; :709 _matched_by_hook; :681 check_mcp_pin; :112 _PACKAGE_RUNNERS"
native_alternative = "Nothing in Claude Code enumerates MCP tools lacking a permission decision: `permissions` rules and the /permissions UI are declarative-only, and the one tool-level restriction mechanism (`toolPolicy` inside `managedMcpServers`) is documented for Claude Desktop 3P deployments, not Claude Code. Renovate has no MCP manager — the config-options page names no mcp manager and the pep621/npm managers do not read .mcp.json."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/desktop.md:735 (managedMcpServers toolPolicy, 'third-party (3P) Desktop deployments only'); mcp.md:503-509 (scope table — no pin or tool-decision facility); .agent/kb/raw/renovate-config-options.digest.md (no mcp manager; the manager list is pep621/npm/etc.)"
rationale = "Required justification: both checks assert a REPO POLICY (a reviewed decision must live in the tracked settings file; a server's tool set must not be able to change between sessions), and no native mechanism has an opinion about either. The pin check is also load-bearing for the guard-coverage check — an unpinned spec makes the declared tool list unfalsifiable — so retiring one would quietly weaken the other."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-doctor-9"
unit = "mod-doctor"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The unit's residual core: the doctor.toml-vs-host baseline comparison itself (check_fnox_baseline, check_exec_only_not_leaked, check_mcp_scope, check_graphify_skill_surface), the fail-open contract (run_checks containing a crashed check, _record_crash appending to ERROR_LOG, doctor_main exiting 0 unless --strict), and check_pin_currency_wired asserting the delegated currency engine is both WIRED and importable."
evidence = "python/src/dotfiles_setup/doctor.py:1197 CHECKS tuple; :1229 run_checks; :1218 _record_crash; :1285 doctor_main; :785 check_pin_currency_wired; :1091 check_graphify_skill_surface; :537 check_mcp_scope"
native_alternative = "`claude doctor` prints read-only installation and settings diagnostics including settings-file validation errors, and the in-session `/doctor` skill checks installation health and unparseable settings files, flags slow hooks, and can apply fixes. `claude plugin validate --json` (2.1.259) validates plugin/skill manifests machine-readably."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/cli-reference.md:34 (`claude doctor`); commands.md:83 (/doctor full check list); skills.md:1040 (`claude plugin validate .claude/skills` for unparseable frontmatter); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (--json on claude plugin validate, 2.1.259)"
rationale = "Required justification: every native doctor validates SHAPE (does this file parse, is this install healthy, is a hook slow). None of them can hold a project-specific REVIEWED BASELINE — that the fnox env mode is `true` with these 56 names, that .agents/skills/graphify/SKILL.md still carries 'DELIBERATE STUB', that the root AGENTS.md does not carry the vendor installer's append, that a scope declaration matches the roots the harness will actually send. Nor can they assert that a specific SessionStart hook's delegate (kb_setup.currency) is importable rather than merely written, which is precisely the gap workflow.tool-currency-wiring's text grep cannot see. The fail-open + record-the-crash contract is also unavailable natively: `claude doctor` is a separate command, not a per-session non-blocking report."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-env_blob_scan-1"
unit = "mod-env_blob_scan"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "`_VALUE_PATTERNS` — five hand-written high-confidence secret VALUE regexes (aws-access-key-id `AKIA…`, google-client-secret `GOCSPX-…`, `github-pat` `gh[pousr]_…`, `github-fine-grained-pat` `github_pat_…`, `private-key-header`) re-implemented in Python. The module docstring states the reason explicitly: gitleaks' path allowlist covers `docs/research/kb/` and `docs/research/runs/`, which git TRACKS, so a dump landing there is committed with the scanner deliberately looking away — 'This check has no such exemption.'"
evidence = "python/src/dotfiles_setup/env_blob_scan.py:106-115 (the pattern table); python/src/dotfiles_setup/env_blob_scan.py:35-39 (the stated justification: gitleaks allowlists tracked research paths); .gitleaks.toml:11-24 (one GLOBAL `[[allowlists]]` with `paths = ['''docs/research/kb/''', '''docs/research/runs/''', …]` and no rule scoping)"
native_alternative = "gitleaks `[[allowlists]] targetRules = [...]` (added v8.25.0). A global allowlist with no `targetRules` disables EVERY rule on those paths; adding `targetRules` scopes an allowlist to the named rules that actually false-positive, leaving the high-confidence default rules live on the same paths. Three of the module's five patterns are verbatim re-implementations of gitleaks default rule ids: `github-pat`, `github-fine-grained-pat`, `private-key`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/README.md:409-414 ('⚠️ In v8.25.0, `[[allowlists]]` have a new field called |targetRules|. … Common allowlists can be defined once and assigned to multiple rules using |targetRules|'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/config/config.go:82 (`TargetRules []string`) and :233-235 ('Allowlists with |targetRules| aren't added to the global list'); default rule ids at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/cmd/generate/config/rules/github.go:22,41 and privatekey.go:12"
rationale = "The value-scan is not a capability gitleaks lacks — it is a workaround for a MISCONFIGURED allowlist. Scoping `.gitleaks.toml`'s path allowlist with `targetRules` (naming only the rules whose false positives motivated each path entry — the localhost curl example, the `trig_01ABC…` placeholder, the markdown-link `generic-api-key` hit documented in .gitleaks.toml:16-20) restores `github-pat`, `github-fine-grained-pat`, `private-key` and the AWS rule on `docs/research/kb/` and `docs/research/runs/`, which is exactly what `_VALUE_PATTERNS` was written to recover. Refactor rather than retire: the config change must be measured before deleting the Python patterns (see probe), and the AWS/Google rule ids were not verified in the offline corpus."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && printf 'k = \"AKIA%s\"\\n' '3QZ7WTVB2NKLDXPR' > docs/research/kb/.armprobe.md && gitleaks dir --no-banner --redact . ; echo \"rc=$? (control arm: same file at repo root must report the finding)\" ; rm docs/research/kb/.armprobe.md   # then re-run with targetRules added to .gitleaks.toml's allowlist and confirm the finding appears"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-env_blob_scan-2"
unit = "mod-env_blob_scan"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "`decode_blob()` + the bare-run scan: take any base64 run >= 120 chars, base64/base64url-decode it, then zlib- or gzip-DECOMPRESS it, and flag the line if the decompressed text names >= 2 secret-bearing environment variables (`*SECRET*`, `*TOKEN*`, `*API_KEY*`, …). This is the `__MISE_DIFF` shape: mise zlib-compresses the whole env delta, so the credentials are present but unreadable to a regex scanner."
evidence = "python/src/dotfiles_setup/env_blob_scan.py:132-151 (`decode_blob`, base64 then zlib/gzip), :95-104 (`_SECRET_NAME`, `MIN_DECODED_NAMES = 2`), :187-198 (the bare-run branch); python/src/dotfiles_setup/env_blob_scan.py:17-27 (measured both arms: gitleaks 8.30.1 = 2 leaks plaintext / 0 blob; betterleaks = 1 / 0); hk.pkl:300-309 (`no_env_dump`, deliberately glob-less)"
native_alternative = "None. gitleaks' `--max-decode-depth` is the closest native feature and cannot reach this content: its base64 decoder DISCARDS any decode whose output is not printable ASCII (a zlib stream is binary), and the supported encoding set is percent / hex / base64 only — there is no decompression step in the decode path. gitleaks' `--max-archive-depth` decompression exists but keys off `archives.Identify(ctx, s.Path, nil)` on a whole FILE, so it cannot see a compressed run embedded in a line of text."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/detect/codec/base64.go:29,34 (`if err == nil && isPrintableASCII(decodedValue)` on both the StdEncoding and RawURLEncoding branches; otherwise `return \"\"`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/README.md:549-553 ('Currently supported encodings: percent … hex … base64'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/detect/codec/encodings.go:22-42 (the complete encodings table — percent, unicode, hex, base64); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/sources/file.go:53 (`archives.Identify(ctx, s.Path, nil)` — file-level, not inline)"
rationale = "REQUIRED JUSTIFICATION: no scanner in this repo's toolchain decompresses. `grep -rn 'compress/zlib|compress/gzip' <gitleaks tree> --include=*.go` returns 0 hits; control arm `grep -rn 'encoding/base64'` on the same tree with the same command shape returns 3 hits (detect/codec/base64.go:4, cmd/generate/config/rules/jwt.go:4, sentry.go:4), so the zero is a real negative, not a blind probe. Even with `--max-decode-depth` enabled, base64.go's `isPrintableASCII` guard drops the blob before any rule sees it. The module's own measurement (0 findings for both scanners on the compressed form, non-zero on the plaintext) is the matching two-arm evidence on the runtime side. This capability stays custom."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-env_blob_scan-3"
unit = "mod-env_blob_scan"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "Implicit coverage assumption in the gate's design: `no_env_dump` only reports base64 that DECOMPRESSES, and gitleaks is assumed to own 'the general case'. A plain, UNcompressed base64-encoded secret (the ordinary `base64 <<< 'AWS_SECRET_ACCESS_KEY=…'` shape) is therefore covered by neither — the custom module skips it (`decode_blob` returns None without a compression header) and gitleaks is wired with decoding OFF."
evidence = "python/src/dotfiles_setup/env_blob_scan.py:146-151 (a decode is only returned if zlib or gzip succeeds; otherwise `None`); python/src/dotfiles_setup/env_blob_scan.py:106-108 ('gitleaks and betterleaks own the general case'); hk-common.pkl:134-136 (`[\"gitleaks\"] = (Builtins.gitleaks) { batch = true }` — the only override is `batch`, so the builtin argv is taken verbatim)"
native_alternative = "gitleaks `--max-decode-depth <n>` — recursive base64/hex/percent/unicode decoding, disabled by default (`0`). Wiring it is a one-line hk override of the builtin's `check` CommandSpec (hk supports overriding a builtin step's fields, which hk-common.pkl already does for `batch`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/README.md:528-538 ('The flag `--max-decode-depth` enables this feature (the default value \"0\" means the feature is disabled by default) … Overall, decoding only minimally increases scan times'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/cmd/root.go:91 (`rootCmd.PersistentFlags().Int(\"max-decode-depth\", 5, …)`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/gitleaks.pkl:17 (`argv = List(\"gitleaks\", \"dir\", \"--redact\", \"--verbose\", \"--no-banner\", \".\")` — no decode flag); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:162,166 (argv override form)"
rationale = "This does not retire any custom code — it closes a coverage hole the custom module explicitly delegates away. Override the gitleaks builtin's argv in hk-common.pkl to add `--max-decode-depth 3` (and consider `--max-archive-depth`), then re-state the module docstring's coverage claim: after the change gitleaks owns plain-encoded secrets and `env_blob_scan` owns only the compression step, which is a cleaner seam than the one documented today. Cost is stated by the vendor as minimal."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && python3 -c \"import base64;print(base64.b64encode(b'AWS_SECRET_ACCESS_KEY=wJalr9XUtnFEMI0K7MDENG2bPxRfiCYnotreal').decode())\" > /tmp/enc.txt && gitleaks dir --no-banner --redact /tmp; echo \"rc=$? (expect 0 findings)\"; gitleaks dir --no-banner --redact --max-decode-depth 3 /tmp; echo \"rc=$? (expect >=1 finding — this is the control arm proving the flag is what changed it)\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-env_blob_scan-4"
unit = "mod-env_blob_scan"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`_ENV_ASSIGNMENT` + `ENV_DIFF_NAME` — check #1: a `__MISE_DIFF=<opaque run >= 40 chars>` assignment is a finding on presence alone, with no decode required, plus the generic `NAME=<opaque run>` form promoted to a finding when the run decompresses to >= 2 secret names."
evidence = "python/src/dotfiles_setup/env_blob_scan.py:78-93 (`MIN_NAMED_BLOB_CHARS`, `_ENV_ASSIGNMENT`, `ENV_DIFF_NAME = \"__MISE_DIFF\"`), :167-185 (the assignment branch)"
native_alternative = "The `__MISE_DIFF` half is a plain regex over plaintext with no decode, which is exactly a gitleaks custom `[[rules]]` entry (`id`, `description`, `regex` — Go RE2, no lookaheads needed for `__MISE_DIFF\\s*[=:]\\s*['\\\"]?[A-Za-z0-9+/_-]{40,}`). Adding it to `.gitleaks.toml` puts the check in the scanner the repo already runs on every commit and in CI, alongside the `targetRules` fix in finding 1."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/README.md:303-315 (custom `[[rules]]`: `id`, `description`, `regex` — 'Golang regular expression used to detect secrets'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/README.md:373-380 (per-rule `[[rules.allowlists]]` with `regexTarget = \"line\"`)"
rationale = "Partial: the `__MISE_DIFF`-presence branch is natively expressible and belongs in `.gitleaks.toml`, but the SECOND half of the same code path — a generic `NAME=<run>` promoted only when the run decompresses — depends on `decode_blob` and stays custom per finding 2. Two obstacles keep this a refactor rather than a retire, and both need measuring first: (a) gitleaks would still be silenced on the tracked research trees unless finding 1's `targetRules` change lands with it, and (b) the hk builtin runs `gitleaks dir .` over the working tree while this module enumerates `git ls-files`, so the scanned sets are not identical. REDACTION 2026-09-09: the synthetic __MISE_DIFF example in `probe` was elided to a description because hk's no_env_dump scanner (deliberately glob-less) flagged the committed TOML; the example was zlib-header + 61 filler chars, not a real dump."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && cp .gitleaks.toml /tmp/gl.toml && cat >> /tmp/gl.toml <<'EOF'\n[[rules]]\nid = \"mise-env-diff\"\ndescription = \"mise compressed environment delta\"\nregex = '''__MISE_DIFF\\s*[=:]\\s*[\"']?[A-Za-z0-9+/_-]{40,}'''\nEOF\nprintf '__MISE_DIFF=eJx%s\\n' \"$(head -c 60 /dev/zero | tr '\\0' 'A')\" > /tmp/gl-probe/x.txt 2>/dev/null || (mkdir -p /tmp/gl-probe && printf '__MISE_DIFF=eJx<61 filler A chars, elided so the committed report is not itself an env-dump-shaped blob>\\n' > /tmp/gl-probe/x.txt); gitleaks dir -c /tmp/gl.toml --no-banner --redact /tmp/gl-probe; echo \"rc=$? (expect 1 finding); control arm: same command with the stock .gitleaks.toml must report 0\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-env_blob_scan-5"
unit = "mod-env_blob_scan"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`tracked_files()` — the gate enumerates `git ls-files -z` and scans EVERY tracked file on every run, ignoring hk's file list; the hk step is deliberately glob-less and passes no `{{files}}`."
evidence = "python/src/dotfiles_setup/env_blob_scan.py:202-213 (`git ls-files -z`), :216-231 (`find_violations` defaults to the whole tracked set); hk.pkl:300-309 ('Deliberately NOT scoped by glob: an `env` dump can land in any tracked file, and the paths most likely to receive one … are exactly the ones `.gitleaks.toml` allowlists away')"
native_alternative = "hk's `{{files}}` / `{{workspace_files}}` templating would hand the step only the changed/staged files (hk expands an exact standalone `{{files}}` entry into one argument per file), and `main.py` already accepts a `--paths` argument that would receive them."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:166 ('Exact, standalone `{{files}}` and `{{workspace_files}}` entries are special: hk expands them into one argument per file'); python/src/dotfiles_setup/main.py:492-500 (the `env-blob-scan` subparser and its `paths` argument)"
rationale = "REQUIRED JUSTIFICATION: adopting hk's `{{files}}` here would be a regression, not a modernization. The whole-tracked-tree scan is the gate's stated design (hk.pkl:300-306) and it is the only layer that re-checks files already committed — a dump that landed while the gate was absent, or via a path the pre-commit hook was skipped on, is caught on the next full run and would be invisible to a changed-files-only step. hk offers no native 'scan the whole tracked tree' primitive that also reads `git ls-files` (the sibling gitleaks builtin works around the same problem by hard-coding `.` and ignoring hk's list — see hk/pkl/builtins/gitleaks.pkl:13-17), so the enumeration stays custom. The `--paths` argument already exists for callers that want the narrow form."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-eval_cases-1"
unit = "mod-eval_cases"
kind = "module"
disposition = "refactor"
confidence = 0.95
custom_thing = "DOCTOR_SCRIPT hard-codes the fable-orchestrator plugin cache path INCLUDING its version segment (`~/.claude/plugins/cache/fable-orchestrator/fable-orchestrator/1.14.0/scripts/doctor.sh`), and `tier1.lane-health` drives that literal path."
evidence = "python/src/dotfiles_setup/eval_cases.py:54-58 (DOCTOR_SCRIPT literal `1.14.0`); consumed at python/src/dotfiles_setup/eval_cases.py:317 and :378 (`evals.doctor_health(doctor)`). The host runs fable-orchestrator 1.21.0 (task-supplied plugin versions). Live probe, control-armed: `ls ~/.claude/plugins/cache/fable-orchestrator/fable-orchestrator/` -> only `1.21.0`; `ls .../1.14.0/scripts/doctor.sh` -> No such file or directory; control arm same command shape `find ~/.claude/plugins/cache/fable-orchestrator -name doctor.sh` -> `/Users/rmanaloto/.claude/plugins/cache/fable-orchestrator/fable-orchestrator/1.21.0/scripts/doctor.sh` (so the probe can find a doctor.sh; the 1.14.0 absence is real, not a blind probe)."
native_alternative = "Claude Code resolves a plugin's installation directory itself: `${CLAUDE_PLUGIN_ROOT}` is the documented substitution for 'absolute path to the plugin's installation directory', and the docs state outright that it CHANGES when the plugin updates and that the previous version's directory is ephemeral. For an out-of-session resolution (which is what a python eval case needs), `claude plugin list --json` prints the installed plugins machine-readably, and `claude plugin details` prints a plugin's component inventory."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:693 (`${CLAUDE_PLUGIN_ROOT}` = absolute path to the plugin's installation directory); plugins-reference.md:726 ('`${CLAUDE_PLUGIN_ROOT}` changes when the plugin updates. The previous version's directory remains on disk for a grace period after an update, but treat it as ephemeral'); plugins-reference.md:1139-1147 (`claude plugin list [options]`, `--json` = Output as JSON); plugins-reference.md:1156+ (`plugin details`)."
rationale = "The docs name this exact failure mode: a version-segmented plugin path is ephemeral by design, so pinning one in source guarantees eventual drift — and it has already drifted (1.14.0 pinned, 1.21.0 installed). Because the case is `live=True` and the module comments the absent-script branch as a 'LOUD skip', the drift does not turn a gate red; it turns the only lane-auth probe into a permanent SKIP, which is precisely the 'inert declaration wearing a green badge' this module's own docstring (eval_cases.py:234-240) warns about. Resolve the path at run time from `claude plugin list --json` (or glob the single child of the plugin's cache dir) instead of pinning a version literal."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-eval_cases-2"
unit = "mod-eval_cases"
kind = "module"
disposition = "keep"
confidence = 0.88
custom_thing = "The whole per-repo eval-case corpus (`cases()` returning tier-1 reachability cases + the tier-2 guard table) run as a bespoke harness through `dotfiles-setup eval` / `mise run eval`, wired as a ship gate."
evidence = "python/src/dotfiles_setup/eval_cases.py:315-394 (`cases()`); mise.toml:1229-1244 (`[tasks.eval]` -> `uv run --project python dotfiles-setup eval`); python/src/dotfiles_setup/main.py:55,2234 (`handle_eval` -> `evals.run(eval_cases_for(project_root))`); python/src/dotfiles_setup/pr.py:352 (`Gate(\"eval\", (\"mise\", \"run\", \"eval\"))`); contract python/verification/suites.toml:1332-1347 (`eval.tier1-runner-wiring`)."
native_alternative = "None at the host's version. A prior in-repo report asserted a first-party `claude plugin eval` with graders/ablation/thresholds; the corpus refutes it — the enumerated `claude plugin` subcommands are init, install, uninstall, prune, enable, disable, update, list, details, validate, marketplace, and no `eval`. The only first-party eval surface is the skill-creator plugin's skill-quality loop (`evals/evals.json`, subagent-isolated runs, LLM grading, with/without-skill benchmark), which grades SKILL OUTPUT QUALITY, not CLI reachability or a pure decision function."
alternative_evidence = "Refuted claim: docs/research/kb/reports/agents/2026-08-31-laneC-replaceable.md:42. Corpus enumeration: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:930,987,1023,1053,1074,1093,1113,1139 plus cli-reference.md:40 — no `eval` subcommand. Adjacent native surface: .../claude-code/skills.md:804-819 (`### Run evals with skill-creator`, 'stores prompts, input files, and expected behavior in `evals/evals.json`'). Control arm for the 0-result grep: same command shape `grep -rln '\\bhooks\\b'` over the same corpus dir returns 114 files, so the grep discriminates."
rationale = "Justification, not a default: the questions these cases ask — does a named lane CLI resolve on PATH, does a SHA-pinned dependency actually contain the module the pin promises, does the pinned CLI dispatch a subcommand, does the local graph return a non-empty answer — are deterministic host-environment assertions with no LLM in the loop. skill-creator's eval loop is an LLM-graded A/B over skill prompts and cannot express any of them, and nothing in `claude plugin` runs a user-supplied case table. Keeping the harness is also cheap: the runner is the shared `kb_setup.evals`, so only the cases are per-repo."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-eval_cases-3"
unit = "mod-eval_cases"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "GUARD_FIXTURES — a 40-row must-DENY/must-ALLOW corpus driven through the real `hook_guard.decide` as `tier2.guard-fixtures`, asserting the PreToolUse guard's DECISIONS (not its wiring)."
evidence = "python/src/dotfiles_setup/eval_cases.py:95-215 (the table) and :386-393 (`evals.guard_table_case(... GUARD_FIXTURES, hook_guard.decide)`); structural tests at tests/test_eval_cases.py:192-253; contract python/verification/suites.toml:1360-1364."
native_alternative = "Claude Code's native hook-introspection surfaces are observational only: `/hooks` opens a read-only browser of configured hooks (event, matcher, source settings file, handler detail), and `claude --debug` logs each event, which matchers were checked, and the hook's exit code/output as tool calls actually happen. There is no native fixture/replay mechanism that feeds a synthetic command to a PreToolUse hook and asserts its decision."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:692 ('Type `/hooks` in Claude Code to open a read-only browser for your configured hooks … Use it to verify configuration'); .../debug-your-config.md:60-70 ('Run `/hooks` to list every hook registered…'; 'the next step is to watch hook evaluation live. Start a session with `claude --debug` and trigger the tool call'); .../debug-your-config.md:111 (the docs' own remedy for an under-matching rule is 'use a PreToolUse hook … for a hard guarantee', i.e. the harness is the user's to test)."
rationale = "Both native surfaces require an interactive session and a real tool call, and both report what a hook DID rather than asserting what it MUST decide — they cannot run in a ship gate and cannot carry a must-ALLOW half. The must-ALLOW half is the load-bearing one here (2 of this guard's 3 all-time denials were false positives, eval_cases.py:86-91), and no native mechanism grades false positives at all. The one genuine overlap is with `tests/test_hook_guard.py`, which already parametrizes ~35 pytest cases over the same `decide` — but that is duplication with pytest, not supersession by a native harness feature, and the eval table adds the ship-time (non-pytest) run via pr.py:352."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-eval_cases-4"
unit = "mod-eval_cases"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "DECLARED_LANES + FALLBACK_TOKENS reachability check (`tier1.lanes-declared-or-degraded`): every lane CLI named in doctrine either resolves on PATH or its degradation path is written down in `.claude/CLAUDE.md`."
evidence = "python/src/dotfiles_setup/eval_cases.py:42-52 (DECLARED_LANES, FALLBACK_TOKENS) and :321-339 (the case, with `fallback_doc = repo_root/.claude/CLAUDE.md`)."
native_alternative = "mise can answer only the mise-managed half: `mise ls -m/--missing` displays missing tool versions for tools mise declares. The lane-auth half is answered by the fable-orchestrator plugin's own doctor (`/fable-orchestrator:doctor`: 'Validate the CLI lanes — presence, auth, and model access via a few tiny live calls per installed CLI'), which is already the subject of tier1.lane-health."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/ls.md:26 ('**`-m --missing`** — Display missing tool versions'). Plugin doctor: the fable-orchestrator plugin's `doctor` command description, and its script at ~/.claude/plugins/cache/fable-orchestrator/fable-orchestrator/1.21.0/scripts/doctor.sh (probed present)."
rationale = "`mise ls --missing` is structurally unable to answer this case: the interesting lane is `grok`, which is DELIBERATELY not declared in mise.toml (.claude/CLAUDE.md's 'There is no grok here' section), so mise would report nothing missing while the doctrine still names it — the exact silent-pass shape #354 cost. And the second half of the assertion is a documentation reconcile (does the doc state the degradation path?), which no tool models. The plugin doctor covers auth for INSTALLED lanes only and is already a separate live case. Keeping this case is justified; the only defensible narrowing would be to source DECLARED_LANES from the doctrine doc rather than a literal tuple, which is a maintenance change, not a native adoption."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-eval_cases-5"
unit = "mod-eval_cases"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`_kb_setup_importable` / SHARED_MODULES — importing `kb_setup.evals` and `kb_setup.md_budget` at run time to prove the SHA-pinned git dependency actually contains the modules the pin promises (the #391 shape: a pin naming a commit that predates the module), plus `_launcher_dispatches`, which drives the pinned `kb-setup` CLI and greps its handler's own refusal string because rc=2 is returned in both directions."
evidence = "python/src/dotfiles_setup/eval_cases.py:397-428 (SHARED_MODULES, `_importable`, `_kb_setup_importable`, `_kb_setup_import_control`) and :249-290 (`_launcher_dispatches`, LAUNCHER_REACHED_MARKER, `_launcher_dispatch_control`)."
native_alternative = "uv's inspection surface is the nearest native candidate: `uv pip check` reports installed-environment dependency diagnostics, and uv 0.12.10/0.12.11 add lock/export diagnostics — none of which assert that a given importable module or CLI subcommand exists at a pinned git revision."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/docs/pip/inspection.md:41 (`uv pip check`); .../uv/docs/pip/compatibility.md:264-274 (enumerates the diagnostics `uv pip check` surfaces — all dependency-compatibility, none module-presence). Gap digest .agent/kb/raw/uv-releases-since-pin.digest.md (0.12.10/0.12.11: publish-token revocation, `missing-exclude-newer-package-lock`, `uv tree --invert` cycles, pylock.toml hashes — no reachability check)."
rationale = "A resolver check answers 'do the declared versions install and agree'; these cases answer 'does the artifact the pin points at contain the entry point we call'. #391 is the proof the two differ — the pin resolved and installed cleanly while `mise run cc` had never once worked. `uv pip check` would have stayed green throughout. The launcher probe additionally encodes a discrimination fact no tool knows (rc=2 in both directions, so only the handler's own marker string separates 'dispatched' from 'unknown command'), which is exactly the custom knowledge a native check cannot carry."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-eval_cases-6"
unit = "mod-eval_cases"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`tier1.graph-answers` + `_broken_graph_canary` + `_graphify_installed` — asking the local graph a real question and requiring a non-empty answer, with a corrupt-graph control arm and a host-only precondition."
evidence = "python/src/dotfiles_setup/eval_cases.py:60-62 (CANARY_QUESTION), :218-231 (`_broken_graph_canary`), :293-312 (`_graphify_installed`), :361-371 (the case)."
native_alternative = "graphify ships `graphify hook status` (confirms the assistant-side hooks are active) and `graphify query`/`path`/`explain` as the query surface, but no self-check that grades whether a built graph actually ANSWERS. The 0.9.56 README fetch adds nothing here."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/README.md:642 ('Confirm the hooks are active with `graphify hook status`') and :713 (`graphify hook status` in the full command reference); README.md:98,472-473 (`graphify query \"<question>\"` as the query surface). Gap digest .agent/kb/raw/graphify-readme-0.9.56.digest.md — 'Finding: zero new capabilities' vs the corpus README, so nothing newer supersedes this."
rationale = "`graphify hook status` grades the WIRING (are the assistant hooks installed), which is the tier-0 question this case explicitly refuses to settle for; the case's whole point (eval_cases.py:363-367) is that `rc=0` with empty output is 'a graph that reads as healthy and knows nothing'. No graphify command expresses a non-empty-answer assertion, and the two custom parts that make the case honest — a present-but-meaningless `graph.json` as the control fixture, and a host-only precondition so the in-container run SKIPs rather than fails — are repo-specific facts no upstream tool can supply."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-gcc_sha-1"
unit = "mod-gcc_sha"
kind = "module"
disposition = "keep"
confidence = 0.88
custom_thing = "The core capability: parse `ARG GCC_LATEST_DEB` + `ARG GCC_LATEST_DEB_SHA256` out of .devcontainer/Dockerfile, download the pinned dated .deb from kayari.org, sha256 it, and rewrite ONLY the digest with a strict subn count (write only on drift)."
evidence = "python/src/dotfiles_setup/gcc_sha.py:95-110 (parse_pins), :162-175 (compute_sha), :178-192 (replace_sha, `count != 1` fails loud), :195-225 (repair, byte-identical when unchanged); wired as `dotfiles-setup gcc-sha` at python/src/dotfiles_setup/main.py:1751-1763 and :2401, called once by .github/workflows/gcc-sha-repair.yml:80."
native_alternative = "None. Renovate is the only tool in the stack that touches this pin and it is structurally unable to produce the value: its datasources/managers yield VERSION strings (and registry-published digests), never a locally-computed hash of a fetched artifact — which is exactly why the companion ARG is declared Renovate-unmanaged in renovate.json. mise's lockfile checksums cover mise-managed tool assets only (no .deb URL surface); hk/uv/gh/graphify/ast-grep have no artifact-hash-and-rewrite capability; and the closest native integrity primitive (BuildKit `ADD --checksum=`, coreutils `sha256sum -c`) CONSUMES a sha, it does not compute a new one."
alternative_evidence = "renovate.json:127 — \"The companion GCC_LATEST_DEB_SHA256 is NOT managed here (Renovate can't hash an artifact)\"; renovate.json:132-135 (the custom manager extracts only the `currentValue` from the filename via `custom.gcc-latest`). Renovate's own docs page saved at .agent/kb/raw/renovate-config-options.md exposes no option that hashes a fetched file — the only escape hatch is running an external command (postUpgradeTasks, .agent/kb/raw/renovate-config-options.md:17713), i.e. running THIS module."
rationale = "Justification (not a default): upstream kayari.org publishes no checksum or signature, so the sha is a locally-computed integrity pin that only a fetch-and-hash step can produce. Every native mechanism in the stack either supplies a version (Renovate datasource) or verifies a supplied sha (sha256sum -c at .devcontainer/Dockerfile:611, contracted by python/verification/suites.toml:351-359) — nothing closes the loop between the two. The module is also already the minimal shape the repo's rules ask for: subprocess curl as the blessed fetcher, an injectable seam for tests, and a strict rewrite count."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-gcc_sha-2"
unit = "mod-gcc_sha"
kind = "module"
disposition = "refactor"
confidence = 0.82
custom_thing = "Hand-rolled streaming hash: a 1 MiB `_DOWNLOAD_CHUNK` constant plus `for chunk in iter(lambda: stdout.read(_DOWNLOAD_CHUNK), b\"\"): digest.update(chunk)` over curl's stdout pipe, with a manual `stdout is None` narrowing branch."
evidence = "python/src/dotfiles_setup/gcc_sha.py:46 (_DOWNLOAD_CHUNK), :127 (hashlib.sha256()), :144-151 (the None-narrowing branch and the iter/read/update loop)."
native_alternative = "`hashlib.file_digest(fileobj, \"sha256\")` — Python 3.11+ stdlib, which does the buffered read-into-hash loop itself (default 256 KiB buffer, zero-copy `readinto`). It accepts `proc.stdout` (a BufferedReader) directly, so the chunk constant, the lambda/iter loop and the explicit-None dance collapse to one call."
alternative_evidence = "Live probe, both arms, this session (saved: .agent/kb/raw/mod-gcc_sha-native-probes.md:17-19). Positive arm over a real subprocess PIPE: `hashlib.file_digest(p.stdout, \"sha256\").hexdigest()` == `hashlib.sha256(b\"hello\").hexdigest()` == 2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824, rc=0, under the repo's pinned interpreter (python 3.14.0 via `uv run --project python`). Negative control arm: an object exposing `read()` but no `readinto` raises ValueError \"is not a file-like object in binary reading mode\" — so the probe discriminates and is not merely accepting anything."
rationale = "Native stdlib fully covers this sub-capability at the pinned Python (3.14.7 in mise.toml; 3.14.0 in the resolved uv env). The repo has zero existing `file_digest` callers (grep over python/ + tests/ returns only p2996_hash's own hand-rolled `_file_digest` helper at python/src/dotfiles_setup/p2996_hash.py:328), so this is a genuine unadopted built-in, not a duplicate. Keep the surrounding curl subprocess and the returncode check — only the byte loop is superseded. Not a 'retire' because the module's other capabilities stand."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-gcc_sha-3"
unit = "mod-gcc_sha"
kind = "module"
disposition = "unknown"
confidence = 0.60
custom_thing = "The CI seam this module exists to serve: a dedicated workflow that mints a GitHub App token, checks out the renovate/** branch, runs `dotfiles-setup gcc-sha`, then commits and pushes the repaired Dockerfile back so the App-token push re-fires the PR's `pull_request` CI (GitHub's own recursion guard makes GITHUB_TOKEN pushes inert)."
evidence = ".github/workflows/gcc-sha-repair.yml:8-14 (the push-trigger + App-token re-fire rationale), :54-66 (mint step), :80 (the module invocation), :81-97 (git diff / commit / tokenized push); the module's docstring names this workflow as its reason for existing at python/src/dotfiles_setup/gcc_sha.py:11-12."
native_alternative = "Renovate `postUpgradeTasks` — `commands` run after a dependency is updated but BEFORE Renovate creates the commit, and `fileFilters` globs decide which modified files Renovate includes in that same commit (`executionMode: \"branch\"` runs it once per branch). Adopting it would delete the whole workflow: no second workflow file, no App-token mint, no push, and no re-fire dance, because the repaired sha lands in Renovate's ORIGINAL commit. The python module would remain — it becomes the allowed command."
alternative_evidence = ".agent/kb/raw/renovate-config-options.md:17713 (`postUpgradeTasks`, \"executed before a commit is made by Renovate\"), :17769 (`postUpgradeTasks.commands`), :17869 (`postUpgradeTasks.executionMode`, allowedValues [update, branch]), :17908 (`postUpgradeTasks.fileFilters` — \"Files that match the glob pattern will be committed after running a post-upgrade task\"). BLOCKER on the same page: :17755 \"In Mend-hosted Renovate apps, commands remain blocked by default, but depending on your plan, you have more control over this\", and :17757 each command must match global-only `allowedCommands`."
rationale = "The native mechanism exists and would fully subsume the workflow seam, but its availability depends on a fact this read-only audit cannot see: whether this repo's Mend-hosted Renovate plan permits postUpgradeTasks commands (this repo consumes the hosted app — PRs come from `renovate[bot]`, cf. .github/workflows/refresh.yml:271-273 — and self-hosts Renovate only for the local dry-run task at mise.toml:844-877). `allowedCommands` is global-only, so a repo-level renovate.json entry alone cannot enable it. Until that is settled, keep gcc-sha-repair.yml."
probe = "gh api repos/ray-manaloto/dotfiles/installation 2>/dev/null | jq '.app_slug'; then, in a throwaway branch, add {\"postUpgradeTasks\":{\"commands\":[\"uv run --project python dotfiles-setup gcc-sha\"],\"fileFilters\":[\".devcontainer/Dockerfile\"],\"executionMode\":\"branch\"}} to renovate.json and read the next Renovate dependency-dashboard/PR log for either the repaired sha in Renovate's own commit or the 'Post-upgrade task did not match any on allowed list' warning (mise run renovate-dryrun --json also reports config validity locally)."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-gcc_sha-4"
unit = "mod-gcc_sha"
kind = "module"
disposition = "retire"
confidence = 0.55
custom_thing = "A `--check` mode: `gcc_sha_main(..., check=True)` downloads the ~480 MB .deb, compares without writing, logs a drift warning and returns rc=1 so 'a caller can gate on it'."
evidence = "python/src/dotfiles_setup/gcc_sha.py:228-240 (gcc_sha_main / check branch), :215-224 (the write=False warning path); flag declared at python/src/dotfiles_setup/main.py:1757-1763 and threaded at :2401."
native_alternative = "The build's own coreutils gate already is the drift check: `echo \"${GCC_LATEST_DEB_SHA256}  ${GCC_LATEST_DEB}\" | sha256sum -c -` runs in the same RUN that downloads the .deb and fails the image build loudly on any mismatch — contracted so it cannot be removed. The repair path additionally reports drift structurally via `RepairResult.changed` in its JSON line, so nothing needs a separate non-writing mode."
alternative_evidence = ".devcontainer/Dockerfile:611 (`sha256sum -c -` in the gcc-latest RUN) and :579 (the pinned ARG); the contract binding both tokens is python/verification/suites.toml:351-359 (`build.gcc-latest-deb-checksum`)."
rationale = "Dead surface: no caller anywhere. Arm — grepping the whole tree for `gcc-sha --check` / `gcc_sha_main(..., check=True)` returns only tests/test_gcc_sha.py:164; no mise task, hk step, .claude/settings.json hook or workflow uses it (.github/workflows/gcc-sha-repair.yml:80 runs the bare subcommand). Control arm for the grep shape — the same search for a flag that IS wired (`apt-pins`) hits mise.toml:806-813, so the probe can find callers when they exist. And it can never become a local/pre-commit gate: it downloads ~480 MB every run. Confidence is mid, not high, because retiring it is a judgement call about a deliberate escape hatch — the alternative disposition is to wire it into a scheduled workflow (image-analysis.yml) rather than delete it; either way the current state (declared, documented, uncalled) should not persist."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-gcc_sha-5"
unit = "mod-gcc_sha"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "Hand-tuned transient-failure policy expressed only as timeouts: `--connect-timeout 30`, `--speed-limit 1024`, `--speed-time 120`, and an explicit design note that there is deliberately no total timeout. A transient 5xx, TLS reset or connection-refused mid-transfer therefore aborts the repair with `RuntimeError` and the Renovate PR stays red until the workflow is re-run by hand."
evidence = "python/src/dotfiles_setup/gcc_sha.py:47-52 (the no-total-timeout rationale and the three constants), :128-138 (the curl argv), :153-158 (non-zero rc raises)."
native_alternative = "curl's own retry family — `--retry <num>`, `--retry-all-errors`, `--retry-connrefused`, `--retry-delay`, `--retry-max-time` — which is the tool-builtin answer to transient fetch failure and is already available in the installed curl."
alternative_evidence = ".agent/kb/raw/mod-gcc_sha-native-probes.md:11-15 (verbatim `curl --help all` rows for --retry, --retry-all-errors, --retry-connrefused, --retry-delay, --retry-max-time) against curl 8.7.1 (:4)."
rationale = "The built-in exists and the module hand-rolls only half the policy, BUT adopting it naively would be a correctness bug, not an improvement: curl retries by restarting the transfer, and with the body streamed to stdout and hashed incrementally a retry after partial output would append duplicate bytes to the digest — producing a WRONG sha that this module would then commit into the Dockerfile as an integrity pin. So the honest state is 'needs a probe', not 'refactor': either prove curl emits nothing to stdout before a retry decision, or move the download to a temp file (then `hashlib.file_digest` per finding 2) where retry is unambiguously safe."
probe = "printf '' > /tmp/gcc-sha-retry.log; python - <<'PY'\nimport subprocess, hashlib\n# serve 200-then-truncate vs a 503-then-200 from a local http.server stub; assert:\n#   sha256(stdout bytes of `curl -fsSL --retry 2 --retry-all-errors <url>`) == sha256(file content)\n# and count bytes written to stdout across the retry to prove no duplication.\nPY\n# minimal live arm without a stub: curl -fsSL --retry 2 --retry-all-errors https://kayari.org/gcc-latest/<pinned .deb> | wc -c  # must equal Content-Length from `curl -sSI <same url>`"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr-1"
unit = "mod-ghcr"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "_parse_scopes() regex-scrapes the human-readable line `Token scopes: 'repo', 'read:org', ...` out of `gh auth status` stdout+stderr to learn the token's OAuth scopes."
evidence = "python/src/dotfiles_setup/ghcr.py:56-62 (`re.search(r\"Token scopes:\\s*(.+)\", auth_output)`) fed from ghcr.py:124-131 (`_run([\"gh\", \"auth\", \"status\"])`, concatenating stdout and stderr)."
native_alternative = "gh exposes the authoritative machine-readable source directly: the `X-Oauth-Scopes` response header, which is exactly what gh itself reads to decide scope questions (`GetScopes`). Reachable from the CLI as `gh api -i / ` (`--include`/`-i` prints the status line and response headers)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/auth/shared/oauth_scopes.go:35-66 (`GetScopes` ... `res.Header.Get(\"X-Oauth-Scopes\")`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/api/api.go:294 (`cmd.Flags().BoolVarP(&opts.ShowResponseHeaders, \"include\", \"i\", false, \"Include HTTP response status line and headers in the output\")`)."
rationale = "The custom side binds a DISPLAY string that gh formats for humans (quoted, comma-joined, and colour-bolded: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/auth/status/status.go:74 writes `cs.Bold(displayScopes(e.Scopes))`, and displayScopes at status.go:347-356 adds the quotes the regex then strips at ghcr.py:61). A wording or formatting change upstream silently turns the probe into `set()` -> 'missing every scope'. `gh auth status` has no `--json` (its only flags are --hostname/--show-token/--active, status.go:182-184), so the fix is not a flag but a different route: read the header. Refactor, not retire — the scope-set comparison logic still has to live somewhere."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-ghcr-2"
unit = "mod-ghcr"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "The scope gate hard-fails whenever the four required scopes are not literally listed, including for tokens that carry NO OAuth scopes at all (fine-grained PAT / GitHub App / Actions token)."
evidence = "python/src/dotfiles_setup/ghcr.py:132-137 — `required_scopes = {\"repo\", \"read:org\", \"workflow\", \"write:packages\"}` then `missing_scopes = sorted(required_scopes - scopes)` raises `GhcrCheckError`. This runs on every `git push` via the hk pre-push step (hk.pkl:774-777)."
native_alternative = "gh's own scope policy: an empty `X-Oauth-Scopes` header means 'assume it is an integration token and give up on detecting its capabilities' (do not fail), and gh only expects scopes for `ghp_`/`gho_` tokens at all."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/auth/shared/oauth_scopes.go:81-86 (`HeaderHasMinimumScopes` ... `if scopesHeader == \"\" { // if the token reports no scopes, assume that it's an integration token ... return nil }`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/auth/status/status.go:347-350 (`displayScopes` returns the literal string `\"none\"` for an empty scope set) and status.go:358-360 (`expectScopes` = token has prefix `ghp_` or `gho_`)."
rationale = "Because gh prints `Token scopes: none` for a scope-less token, _parse_scopes returns {'none'} and the gate reports all four scopes missing — a pre-push hard block on a token that may be fully capable. Adopting gh's documented rule (empty/absent scope header => skip the scope check, and only assert scopes for classic OAuth tokens) removes a false-fail class the custom code cannot currently express. This is the same over-privilege false-positive class the module already patched once for admin:org (ghcr.py:65-71, '#268 follow-up'), reappearing on the other end of the token-type axis."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr-3"
unit = "mod-ghcr"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "_SCOPE_IMPLICATIONS + _expand_scopes(): a hand-maintained OAuth scope-implication table (admin:org -> write:org -> read:org, write:packages -> read:packages, repo -> repo:status/repo_deployment/public_repo) iterated to a fixed point."
evidence = "python/src/dotfiles_setup/ghcr.py:72-96, with the incident rationale in the comment at ghcr.py:65-71 and unit coverage at tests/test_ghcr.py:31-45 (including a control arm: `_expand_scopes({'workflow'}) == {'workflow'}`)."
native_alternative = "gh implements the same idea internally in `HeaderHasMinimumScopes`, but only for `repo` and `read:org`/`write:org`/`admin:org`, and it is a Go function with no CLI surface — `gh auth status` emits no machine-readable scope verdict and has no `--json` flag."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/auth/shared/oauth_scopes.go:88-109 (search map covers only repo/read:org/admin:org; no write:packages, no workflow); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/auth/status/status.go:182-184 (the complete flag list for `gh auth status`: --hostname, --show-token, --active)."
rationale = "Required justification: no CLI, API or linter in the pinned toolchain answers 'does this token's granted scope set imply write:packages/workflow?'. gh's closure is narrower than what GHCR publishing needs and is unreachable from outside the Go binary; the REST API returns the granted scopes only, never their closure. The table is small, documented against the GitHub scope docs (ghcr.py:71) and control-armed in tests, so it stays — but it should be fed by the header from mod-ghcr-1 rather than by scraped display text."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr-4"
unit = "mod-ghcr"
kind = "module"
disposition = "retire"
confidence = 0.85
custom_thing = "_require_workflow_permissions(): reads .github/workflows/ci.yml and fails unless the substring `packages: write` appears anywhere in it."
evidence = "python/src/dotfiles_setup/ghcr.py:99-109, called at ghcr.py:179 (`_require_workflow_permissions(repo_root / \".github\" / \"workflows\" / \"ci.yml\")`)."
native_alternative = "The repo's own structured-verification engine already asserts this, and asserts it more strictly: suite `ci.explicit-packages-write`, handler `require_tokens` with `per_path_tokens`, binding the exact block `\\npermissions:\\n  contents: read\\n  packages: write\\n` in .github/workflows/ci.yml. It runs offline in `mise run verify` / CI `contract-preflight`, with no gh, no auth and no network."
alternative_evidence = "python/verification/suites.toml:668-678 (`name = \"ci.explicit-packages-write\"` ... `per_path_tokens = { \".github/workflows/ci.yml\" = [\"\\npermissions:\\n  contents: read\\n  packages: write\\n\"] }`); the engine's default-strict path semantics are documented in python/AGENTS.md ('Verification contracts', `paths_required` defaults to true, `per_path_tokens` over bare `tokens`)."
rationale = "Duplicate, and the surviving copy is the better one: the suite pins the whole permissions block at a known position while the python check is a bare substring that a `packages: write` anywhere (a comment, an unrelated job) satisfies — exactly the stand-in weakness python/AGENTS.md warns about. Deleting ghcr.py:99-109 and its call at ghcr.py:179 removes a static YAML assertion from a network+auth-gated code path with no loss of coverage. Note the deletion also frees tests/test_ghcr.py:52-54 and :84-86 from having to fabricate a ci.yml under tmp_path."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr-5"
unit = "mod-ghcr"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "The whole check is wired as an unconditional hk pre-push step that shells out to the module and needs a logged-in gh plus five network round-trips on every push; CI jobs that push must be excluded from hooks by separate ADR-0001 machinery because this step cannot pass on a runner."
evidence = "hk.pkl:773-777 (`[\"pre-push\"] { steps { [\"ghcr_publish_prereqs\"] { exclusive = true; check = \"uv run --project python dotfiles-setup ghcr-check\" } } }`); hk.pkl:381 ('ghcr_publish_prereqs needs a logged-in gh'); the same caveat restated in python/src/dotfiles_setup/workflow_hooks.py:8, .github/workflows/refresh.yml:80 and .github/workflows/gcc-sha-repair.yml:49."
native_alternative = "hk 1.57 has two step-level gating mechanisms this step does not use: `profiles: List<String>?` (opt-in/opt-out step selection, driven by `HK_PROFILE`/`hk.profile` git config, with a `missing-profiles` warning-suppression tag) and `step_condition: String?` (the step runs only if the condition evaluates true, e.g. `exec('...')`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:668-682 (step `profiles`, with `List(\"slow\")` / `List(\"!slow\")` examples) and Config.pkl:850-861 (`step_condition` — 'the step will only run if this condition evaluates to true', example `exec('test -f check.js')`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/environment_variables.md:248-257 (`HK_PROFILE`, `!` to disable, `HK_PROFILES` alias); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:137-145 (`hide_warnings = List(\"missing-profiles\")`). Gap digest .agent/kb/raw/hk-releases-since-pin.digest.md confirms 1.58.1 is documentation-only, so both features are present at the pinned 1.57.0."
rationale = "The step's environmental precondition (a logged-in gh, network) is currently expressed nowhere in hk and is compensated for by hook-skipping machinery in three workflow files plus a python enforcer. `step_condition` lets the precondition live on the step itself (self-skip when gh cannot authenticate) and `profiles` makes the whole network gate opt-in for the push that actually matters, rather than taxing every push with five API calls under `exclusive = true`. Refactor rather than retire: the check itself is still wanted, only its trigger is hand-rolled around hk instead of expressed in it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-ghcr-6"
unit = "mod-ghcr"
kind = "module"
disposition = "unknown"
confidence = 0.60
custom_thing = "Package existence/ownership is probed against the ORGANISATION packages endpoint, `/orgs/{owner}/packages/container/{package_name}` (and its `/versions?per_page=20` sibling), with `owner` defaulting to `ray-manaloto`."
evidence = "python/src/dotfiles_setup/ghcr.py:166-169 and ghcr.py:181-188; default owner at python/src/dotfiles_setup/main.py:1807-1812 (`--owner`, `default=\"ray-manaloto\"`). Cross-check: the sibling cleanup workflow probes the SAME package through the USER endpoint — .github/workflows/ghcr-cleanup.yml:69 (`\"/users/${OWNER}/packages/container/${PACKAGE}/versions?per_page=100\"`) and :100."
native_alternative = "GitHub's account-type-agnostic route for a package owned by the authenticated viewer: `gh api /user/packages/container/<package_name>` (and `/user/packages/container/<name>/versions`), which needs no `/orgs/` vs `/users/` decision at all."
alternative_evidence = "No corpus citation available offline — the GitHub Packages REST reference is not in the offline trees, and no digest under .agent/kb/raw/ covers it; this is why the disposition is 'unknown' rather than 'refactor'."
rationale = "Two probes of one fact disagree, so one of them is wrong: ghcr.py says the owner is an org, ghcr-cleanup.yml says it is a user. If `ray-manaloto` is a personal account, `/orgs/ray-manaloto/...` 404s and ghcr.py:166 raises before ever reaching the version listing — i.e. the pre-push gate would be failing (or is being skipped) rather than validating. If it is an org, the cleanup workflow's `/users/` calls are the broken pair. Settle which, then collapse both call sites onto one endpoint. Not resolvable read-only/offline."
probe = "gh api /users/ray-manaloto --jq .type; gh api /orgs/ray-manaloto/packages/container/dotfiles-devcontainer --jq .name; gh api /users/ray-manaloto/packages/container/dotfiles-devcontainer --jq .name; gh api /user/packages/container/dotfiles-devcontainer --jq .name   # control arm: one of the three package routes must return the name and the other(s) 404 — if all three fail, the probe (auth/scopes) is broken, not the endpoint choice"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr-7"
unit = "mod-ghcr"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The composite orchestration itself: gh presence (`which(\"gh\")`), auth liveness, repo identity via `gh repo view --json`, Actions-enabled and default-workflow-permissions via `gh api repos/.../actions/permissions[/workflow]`, package identity/owner/visibility, and a single typed JSON verdict emitted by the CLI handler."
evidence = "python/src/dotfiles_setup/ghcr.py:112-212 (the `validate_ghcr_prereqs` body and its returned dict, including the `note` at ghcr.py:206-211 recording the one setting gh cannot observe); CLI surface at python/src/dotfiles_setup/main.py:1803-1817 and handler main.py:2265-2273; thin subprocess seams `_run`/`_run_gh_json` at ghcr.py:20-53 that exist so tests can monkeypatch them (tests/test_ghcr.py:67, :128-129)."
native_alternative = "None. `gh api` supplies each individual fact and `--jq` can shape one response, but nothing in gh, the gh extension surface, mise, hk, uv, renovate, graphify, or the pinned workflow linters (actionlint, ghalint, zizmor at hk.pkl:373-376) composes 'is this clone able to publish to this GHCR package' into one pass/fail with a machine-readable verdict."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/api/api.go:294 and the auth-status flag set at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/auth/status/status.go:182-184 show gh's surface is per-fact only; the workflow linters wired at hk.pkl:373-376 audit workflow YAML (permissions hygiene), never live registry/repo state."
rationale = "Required justification: the value here is the COMPOSITION and its verdict shape, which no single native command produces — plus the honest negative recorded in the `note` field (the package's Actions repository allowlist is not cleanly exposed through the gh API), which is knowledge no tool encodes. Keep the orchestration and the `_run`/`_run_gh_json` seams; the pieces that should change are the ones named in findings 1, 2, 4 and 5, all of which leave this skeleton intact. Caveat for whoever acts on this: every existing test drives the module through monkeypatched seams (tests/test_ghcr.py:67, :128-129), so per .claude/rules/real-integration-evidence.md there is currently no real invocation proving the composition works end to end."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr_cleanup-1"
unit = "mod-ghcr_cleanup"
kind = "module"
disposition = "keep"
confidence = 0.82
custom_thing = "Per-tag-family newest-N retention windows: three independent counters (base-/p2996-/dev-) over one GHCR package, with `dev`/`latest` unconditionally protected and any unrecognized tag keeping the version."
evidence = "python/src/dotfiles_setup/ghcr_cleanup.py:30 (PRUNABLE_FAMILIES), :31 (_HASH_TAG_RE), :33 (PROTECTED_TAGS), :65-67 (per-family counters), :76-82 (protected / non-hash-family keeps), :84-94 (per-family window). Wired at python/src/dotfiles_setup/main.py:1780 (subparser) and :2239 (handle_ghcr_cleanup); consumed by .github/workflows/ghcr-cleanup.yml:73-82."
native_alternative = "actions/delete-package-versions (GitHub-owned action) — `min-versions-to-keep` + `ignore-versions` regex."
alternative_evidence = ".agent/kb/raw/actions-delete-package-versions-readme.md (fetched from https://raw.githubusercontent.com/actions/delete-package-versions/main/README.md, 2026-09-09): `min-versions-to-keep` is \"the number of latest versions to keep\" GLOBAL per package and \"cannot be scoped to a subset of tags\"; `ignore-versions` \"Takes regex for the version name as input\" and is exclusion-only, with no positive tag-pattern selection."
rationale = "GitHub's own action has exactly one keep-N per package and no positive tag selector, so it cannot express three simultaneous, independent newest-3 windows inside a single package (dotfiles-devcontainer). Running it three times is impossible because there is no way to say 'only consider base-* versions' — `ignore-versions` can only subtract, and subtracting the other two families still leaves ONE global counter over the remainder plus the `:sha`/`:pr-` tags the module deliberately holds out of scope (ghcr_cleanup.py:80-82). The custom planner is therefore not a reinvention of this action; it computes something the action has no input for."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr_cleanup-2"
unit = "mod-ghcr_cleanup"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "The same per-family retention planning, evaluated against the most expressive third-party retention action rather than GitHub's own."
evidence = "python/src/dotfiles_setup/ghcr_cleanup.py:50-95 (plan_cleanup as a pure function over the package-versions JSON); the delete loop it feeds at .github/workflows/ghcr-cleanup.yml:91-103."
native_alternative = "snok/container-retention-policy@v3.1.0 — `image-tags` wildcard/negation filter + `keep-n-most-recent` + `tag-selection` + `dry-run`, invoked once per hash family."
alternative_evidence = ".agent/kb/raw/snok-container-retention-policy-readme.md (fetched from https://raw.githubusercontent.com/snok/container-retention-policy/main/README.md, 2026-09-09, action version v3.1.0): `image-tags` supports wildcards and negation \"the same way\" as `image-names`; `keep-n-most-recent` \"retains the N newest tagged images per package (per image-names entry)\"; `tag-selection` is `both|tagged|untagged`; `dry-run` prints deletable versions. Blocking limits in the same file: \"Filtering operators only work with personal and GitHub app tokens, not temporal tokens\", and keep-n-most-recent is per PACKAGE, not per tag pattern."
rationale = "This action is the closest native fit and still does not cover the unit. Three blockers, each independently disqualifying: (1) the workflow authenticates with `secrets.GITHUB_TOKEN` (.github/workflows/ghcr-cleanup.yml:65,89), a temporal token, and the README states filtering operators — which is exactly the `image-tags: 'base-*'` selector the per-family scheme depends on — do not work with temporal tokens; adopting it means provisioning and rotating a PAT or GitHub App token with packages:delete, a strictly larger credential blast radius than the current read-mostly GITHUB_TOKEN. (2) `keep-n-most-recent` is per package, so three families need three invocations, and each invocation is blind to the others: a version tagged with two families (ghcr_cleanup.py:79-92, asserted by tests/test_ghcr_cleanup.py::test_multi_tag_version_kept_while_any_family_window_open) would be deleted by the pass whose window has closed even though the other family's window is still open. (3) `cut-off` is a required input, so the action cannot express a pure count-based policy. Keeping the ~45-line pure planner is cheaper and safer than the token escalation plus a three-pass approximation that is wrong on the multi-tag case."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr_cleanup-3"
unit = "mod-ghcr_cleanup"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "Blanket 'NEVER touch untagged versions' rule, justified as 'attestation/child manifest of a kept index'."
evidence = "python/src/dotfiles_setup/ghcr_cleanup.py:12-14 (module docstring rationale), :70-75 (every untagged version is kept with that reason string); tests/test_ghcr_cleanup.py::test_untagged_versions_are_never_deleted."
native_alternative = "snok/container-retention-policy@v3.1.0's precise referrer handling: automatic multi-arch child-image protection plus `skip-shas`, with `tag-selection: tagged|untagged|both` as the coarse switch."
alternative_evidence = ".agent/kb/raw/snok-container-retention-policy-readme.md: \"skip-shas: Manually protects specific digests. Multi-arch child images are automatically protected as of v3.1.0.\" and `tag-selection` default `both`, options `tagged`/`untagged`."
rationale = "The custom rule uses 'untagged' as a PROXY for 'referenced by a kept index', which is the correct-but-over-broad direction: it can never corrupt a kept image, but it also never reclaims a genuinely orphaned untagged version, so untagged manifests accrete forever and the weekly pass can never shrink them. v3.1.0 resolves the actual parent/child relation instead of the proxy. Refactor, not retire: only the untagged half is superseded, the per-family windows (finding 1/2) are not, and the token blocker in finding 2 applies here too — so the concrete refactor is either to teach plan_cleanup the same child-manifest resolution (the API exposes the manifest referrers) or to leave the proxy in place with the over-keeping documented as deliberate. Today the docstring at :12-14 asserts the referrer relation without ever reading it."
probe = "gh api /users/ray-manaloto/packages/container/dotfiles-devcontainer/versions --paginate --slurp | jq '[.[][] | select((.metadata.container.tags|length)==0)] | length' — count how many untagged versions the blanket rule is currently pinning; then compare against the child manifests actually referenced by the kept indexes (docker buildx imagetools inspect --raw on each kept tag) to size the reclaimable set."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr_cleanup-4"
unit = "mod-ghcr_cleanup"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "Hand-rolled page concatenation feeding the module's `--versions-json` input contract: `gh api --paginate ... --jq '.' | jq -s 'add' > /tmp/versions.json`, whose flat-array shape plan_cleanup consumes directly."
evidence = ".github/workflows/ghcr-cleanup.yml:68-70; consumed at python/src/dotfiles_setup/main.py:2246 (`json.loads(Path(args.versions_json).read_text())`) and python/src/dotfiles_setup/ghcr_cleanup.py:50 (`versions: list[dict]`)."
native_alternative = "`gh api --paginate --slurp` (gh v2.98.0), which writes all pages into one JSON array natively."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/api/api.go:295 (flag registration: \"Use with \\\"--paginate\\\" to return an array of all pages of either JSON arrays or objects\"), :358-367 (jsonArrayWriter wiring). Two constraints from the same source: api.go:258 — \"the `--slurp` option is not supported with `--jq` or `--template`\"; and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/api/pagination_test.go:190-193 — pages `[1,2]` and `[3]` slurp to `[[1,2],[3]]`, i.e. an array OF pages, not a flattened concatenation."
rationale = "`--jq '.'` is an identity filter whose only job is to make gh emit a page stream for `jq -s` to reassemble; `--slurp` does the reassembly in gh. The correct replacement is `gh api --paginate --slurp <path> | jq 'add'` — NOT a bare `--slurp`, because slurp nests (`[[1,2],[3]]`) where the current pipeline flattens, and because `--slurp` is rejected outright alongside `--jq` (api.go:258). Net saving is one process and the identity filter, so this is a tidy-up rather than a defect; the reason to name it is that a future edit dropping the trailing `jq 'add'` would hand plan_cleanup a list of lists, where `version.get(\"id\", 0)` (ghcr_cleanup.py:69) silently returns 0 for every page and the plan reads as 'nothing deletable' at rc=0."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-ghcr_cleanup-5"
unit = "mod-ghcr_cleanup"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "The existence of the whole unit — a self-hosted retention policy for a GHCR package."
evidence = "python/src/dotfiles_setup/ghcr_cleanup.py:2-20 (module purpose) plus the weekly schedule at .github/workflows/ghcr-cleanup.yml:16-18."
native_alternative = "A GitHub-native, declaratively-configured retention policy for GitHub Packages / GHCR container versions (i.e. the platform doing this without any action or code), if one has shipped."
alternative_evidence = "NOT FOUND in the offline corpus. Control arm: `grep -rln 'packages/container' /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/` returns 0 files, while the same command shape over the alternation `delete-package-versions|package_versions|packages/container|retention polic` returns 20+ files (all codex/claude-docs matches on 'retention polic'), so the grep discriminates and the GitHub Packages API/UI documentation is simply absent from the corpus. No citation exists either way at this host's snapshot."
rationale = "Rule 4 of this audit: no citation for the native side means 'unknown', not 'keep'. GitHub has historically offered no user-configurable retention policy for container package versions (which is why both third-party actions above exist), but that is an inherited belief, not something the corpus can confirm at 2026-09-09, and this is precisely the class of claim that ages. If GitHub has since shipped a declarative package retention policy, the entire unit — module, CLI subcommand, workflow and tests — retires in one change."
probe = "curl -sS 'https://docs.github.com/en/packages/learn-github-packages/deleting-and-restoring-a-package' -o .agent/kb/raw/github-packages-deleting-restoring.html && curl -sS 'https://docs.github.com/en/rest/packages/packages?apiVersion=2022-11-28' -o .agent/kb/raw/github-rest-packages.html && grep -Eio 'retention polic[a-z]*|automatically delete|version limit' .agent/kb/raw/github-packages-deleting-restoring.html | sort -u  # control arm: the same grep must also hit a term known to be on that page, e.g. 'delete a package version'"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graph_bakeoff-1"
unit = "mod-graph_bakeoff"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "parse_out_tokens() scrapes the output-token count out of graphify's human stdout line ('[graphify extract] tokens: 5,697 in / 7,479 out'), by splitting on 'tokens:' then '/', and returns 0 on any parse miss ('a failed run has no summary, and 0 is honest') — so a wording change upstream silently degrades every out_tokens cell in the report and manifest to 0."
evidence = "python/src/dotfiles_setup/graph_bakeoff.py:189-203 (parse_out_tokens); consumed at python/src/dotfiles_setup/graph_bakeoff.py:463 (RunResult.out_tokens) and recorded at :427; rendered at :711-715. No reader of graphify's machine-readable analysis file exists anywhere in this repo: grep '.graphify_analysis' over python/ and tests/ -> 0 hits (control arm: grep -rln 'graphify-out' python/src/dotfiles_setup/ -> branch_guard.py, eval_cases.py, graphify.py, so the shape works)."
native_alternative = "graphify extract already writes the same two numbers as JSON, atomically, next to graph.json: `.graphify_analysis.json` with `{\"tokens\": {\"input\": ..., \"output\": ...}}`. At the harness's own layout that is run_dir/out/graphify-out/.graphify_analysis.json — the sibling of the graph.json load_graph() already reads."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py:4599-4610 (analysis dict with the 'tokens' {'input','output'} block, written via write_json_atomic); path defined at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py:4209 ('analysis_path = graphify_out / \".graphify_analysis.json\"'); the stdout line the harness parses is printed from the SAME merged dict at cli.py:4634-4640, so the JSON is the identical figure, not an approximation. Corpus snapshot is graphify 0.9.53 (pyproject.toml:7), which is this repo's pin (python/pyproject.toml graphifyy 0.9.53)."
rationale = "The number the harness wants is already serialized by the tool; parsing prose for it is the hand-rolled path the repo's use-tool-builtins rule exists to prevent, and its failure mode is silent (0, indistinguishable from a failed run). Refactor rather than retire: keep parse_out_tokens as the fallback for a run that produced no analysis file, but read run_dir/out/graphify-out/.graphify_analysis.json first via the existing load_graph()-shaped helper. Also worth fixing while there: 0 currently conflates 'no summary line' with 'genuinely zero tokens'."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graph_bakeoff-2"
unit = "mod-graph_bakeoff"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "nim_provider_config() returns a hand-authored ~/.graphify/providers.json entry (base_url, env_key, default_model, pricing, temperature, max_tokens) for the NIM backend, with a docstring explaining the global-vs-project path rule and the GRAPHIFY_ALLOW_LOCAL_PROVIDERS gate — i.e. the module reimplements, in prose plus a dict literal, the registration of a custom graphify provider."
evidence = "python/src/dotfiles_setup/graph_bakeoff.py:597-625 (nim_provider_config, incl. the 'Not written automatically' note and the llm.py:264 citation); referenced as the NVIDIA_API_KEY consumer in docs/secrets-doppler-fnox-keychain.md:126."
native_alternative = "graphify ships a first-class `graphify provider add|list|show|remove` CLI that writes exactly this file at exactly the global path, with `--base-url`, `--default-model`, `--env-key`, `--pricing-input`, `--pricing-output`; it refuses to shadow a built-in backend, validates the URL through provider_base_url_ok(), and prints the follow-on command ('Use with: graphify extract . --backend nim')."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py:1065-1180 (the whole `provider` dispatch; add at :1100-1160, built-in-name refusal at :1107-1109, provider_base_url_ok at :1139-1141, the written payload at :1150-1156, list/show/remove at :1071-1098 and :1162-1177). The global path and the local-file gate the docstring describes are at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/llm.py:240-241 and :281-292. The command was surfaced in `graphify --help` in 0.9.52 (knowledge-base/sources/graphify/CHANGELOG.md, 0.9.52 entry: '`graphify --help` now lists the `prs` and `provider` commands')."
rationale = "Nothing calls nim_provider_config() — it exists to document a shape a human then writes by hand, and the CLI both writes that shape and adds a safety check (unsafe base_url refusal) the dict literal cannot. Replace the function with the one-line documented invocation `graphify provider add nim --base-url https://integrate.api.nvidia.com/v1 --default-model meta/llama-3.3-70b-instruct --env-key NVIDIA_API_KEY --pricing-input 0 --pricing-output 0`, keeping the NVIDIA ToS §3.3(iv) caveat in prose. One real delta to state, not paper over: the CLI writes base_url/default_model/env_key/pricing/temperature only — it does NOT set the `max_tokens: 16384` this dict carries (cli.py:1150-1156), so if that ceiling is load-bearing the file needs one hand edit after `provider add`. Related staleness: the docstring cites `llm.py:264 _load_custom_providers`, which at the pinned 0.9.53 is llm.py:281."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graph_bakeoff-3"
unit = "mod-graph_bakeoff"
kind = "module"
disposition = "refactor"
confidence = 0.30
custom_thing = "A second, independent multi-arm scoring harness in the same repo pair: Arm, Score (recall + decoy resistance), per-arm markdown tables, and arm-vs-reference delta rendering."
evidence = "python/src/dotfiles_setup/graph_bakeoff.py:109-124 (Arm), :316-372 (Score/score_graph), :701-767 (_render_metrics/_render_significance/_render_scores)."
native_alternative = "kb_setup.evals — the SHARED cross-repo engine this repo already pins as a uv git dep (kb-setup @ c70f0f8) and already routes currency mechanics through — carries the same vocabulary: Arm (one named configuration, 'two or more arms are run over the SAME queries and reported side by side'), RetrievalRow.recall, score_retrieval, _ArmResult/_arm_lines/_delta_line, an arm-list shape guard that rejects duplicate arm names, and a floor guard that refuses a threshold which 'can only pass'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/python/src/kb_setup/evals.py:744-770 (Arm), :712-732 (RetrievalRow.recall), :803 (score_retrieval), :899-930 (_ArmResult), :928-945 (_arms_shape duplicate-name guard), :975-987 (_arm_lines), :989-1000 (_delta_line), :1037-1064 (_floor_problem / _floor_verdict)."
rationale = "Partial, and I am deliberately not overstating it: kb_setup.evals.Arm is retrieval-typed (it holds a Retrieve callable and a corpus-membership oracle, and its rows are GoldenQuery/Phrasing NATURAL-vs-ECHO pairs), it scores per-query recall rather than link recall + forbidden pairs, and it has no repeats, median/spread, null arm or noise floor. So this is not a drop-in adoption — it is either (a) lift the two genuinely generic guards (duplicate-arm-name detection, and the 'a check that can only pass' floor validation) into graph_bakeoff, or (b) generalize the shared engine's arm/report layer so both callers use one implementation, which is the doctrine the tool-currency rule states for cross-repo mechanics. Worth a decision, not an automatic change."
probe = "diff the two arm/report layers side by side and decide adoption vs. lift: sed -n '744,1000p' /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/python/src/kb_setup/evals.py > /tmp/evals-arms.txt; sed -n '109,124p;316,372p;701,767p' /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/python/src/dotfiles_setup/graph_bakeoff.py > /tmp/bakeoff-arms.txt; diff -u /tmp/evals-arms.txt /tmp/bakeoff-arms.txt | head -200"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graph_bakeoff-4"
unit = "mod-graph_bakeoff"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The harness proper: per-(corpus, arm, repeat) fresh run directories for cache isolation (prepare_run_dir), the provenance manifest recording backend/model/argv/fixed flags/corpus SHA256/tool versions beside every graph (write_manifest, corpus_digest, gather_versions), the null arm + noise floor + significance adjudication (make_null_arm, noise_floor, is_significant), answer-key link scoring with forbidden-link decoys (score_graph, match_entity, resolve_nodes), and the sequential matrix runner (execute_matrix)."
evidence = "python/src/dotfiles_setup/graph_bakeoff.py:172-186, :397-432, :160-169, :549-568, :496-538, :239-372, :628-664; wired as a thin caller at mise.toml:781-804 ([tasks.bakeoff]) and python/src/dotfiles_setup/main.py:1005-1032, :1992-2003; gold input fixture at tests/fixtures/graphify-gold/ (ANSWER_KEY.json + 6 docs); tests at tests/test_graph_bakeoff.py (540 lines)."
native_alternative = "None found. The three candidates all fail on inspection: (1) graphify's shipped `benchmark.py` is a token-reduction benchmark that BFS-walks an already-built graph — it measures context savings, not extraction quality across backends; (2) the harness behind graphify's published BENCHMARKS.md (memory/runner.py, crosstool/run.py) is not distributed in the package and measures retrieval/QA against memory systems (mem0, supermemory) on LOCOMO/LongMemEval, not (backend, model) extraction on a private corpus; (3) graphify's own on-disk manifest is change-detection only (mtimes + ast/semantic content hashes) and records no backend or model."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/benchmark.py:1 ('Token-reduction benchmark - measures how much context graphify saves vs naive full-corpus approach') and :37-72 (_query_subgraph_tokens BFS); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/BENCHMARKS.md:36-52 and :176-186 (the harness and its repro commands `python memory/runner.py` / `python crosstool/run.py` — neither directory exists in the distributed tree: ls of knowledge-base/sources/graphify shows only graphify/, docs/, scripts/, tools/, tests/); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/detect.py:2112-2142 (save_manifest: 'current file mtimes + content hashes for change detection'). The cache-isolation premise still holds at the pinned 0.9.53: grep 'model' knowledge-base/sources/graphify/graphify/cache.py -> 2 hits, both prose (cache.py:1412, :1649), while the control arm grep -c 'prompt' on the same file -> 81, so the cache key remains prompt-fingerprinted and model-blind exactly as the module docstring claims."
rationale = "Required justification: no tool in the host's version set does multi-model extraction comparison with provenance. graphify neither stamps model identity into its artifacts nor keys its semantic cache on it, so the fresh-directory isolation and the manifest are load-bearing compensations for a gap in the tool, not reinventions of it; the null arm / noise floor / significance rule and the answer-key decoy scoring are experimental-design decisions no CLI provides. Two sub-parts that look reinvented but are correct as written and should NOT be 'modernized': gather_versions() shells out to `graphify --version` rather than using importlib.metadata.version('graphifyy') as graphify.py:81 does, and that is right — the harness runs the PATH binary as a subprocess, so the PATH binary's version is the honest attribution (importlib would record the in-process package, the exact two-install drift graphify-first.md warns about); and execute_matrix is sequential on purpose, so a parallel-runner 'improvement' would make the seconds column measure contention. One gap noted without proposing a native fix: `bakeoff` has zero occurrences in python/verification/suites.toml (control arm: 'graphify' -> hits at suites.toml:1833,1854), so nothing contractually binds the mise task to the module."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify-1"
unit = "mod-graphify"
kind = "module"
disposition = "refactor"
confidence = 0.82
custom_thing = "graphify_health hard-codes the expected graphify version as a string literal to detect runtime drift, creating a third copy of the pin (pyproject dependency, the .graphify_version skill stamp, and this literal) that is held together only by a source-TEXT assertion in the test suite."
evidence = "python/src/dotfiles_setup/graphify.py:235-236 (`if runtime != \"0.9.53\": return HealthResult(GraphifyStatus.VERSION_DRIFT, runtime, \"expected 0.9.53\")`); the pin itself is python/pyproject.toml:9 (`\"graphifyy[all]==0.9.53\"`) and python/pyproject.toml:57; the binding test asserts the literal source line, not the behaviour, at tests/test_graphify.py:63 and tests/test_graphify.py:74 (`assert f'if runtime != \"{version}\":' in health_source`)."
native_alternative = "importlib.metadata.requires(\"dotfiles-setup\") returns the declared specifier verbatim, so the expected version can be derived from package metadata at runtime instead of restated — the module already imports importlib.metadata for the installed-version half. The pin-vs-installed drift signal is additionally owned natively by the shared kb_setup.currency engine this repo already declares graphify to (currency.toml [tool.graphify], `mise run tool-currency-check`)."
alternative_evidence = "Live probe (read-only, this repo's env): `uv run --project python python -c \"from importlib.metadata import requires, version; print([x for x in requires('dotfiles-setup') if x.startswith('graphifyy')], version('graphifyy'))\"` -> `requires -> ['graphifyy[all]==0.9.53']` / `installed -> 0.9.53`. Both arms non-trivial: the requires() call returned a populated list AND the installed version resolved. Currency ownership: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/currency.toml:29-37 ([tool.graphify], python_package = \"graphifyy\", python_project_dir = \"python\")."
rationale = "The native mechanism (importlib.metadata.requires) makes the literal, and therefore the source-text test that binds it, unnecessary — one source of truth instead of three. The current shape is a known-fragile arrangement: the test comment at tests/test_graphify.py:67-73 documents exactly the failure it guards (a bump lands on the pin and the stamp while health keeps demanding the old version, so every session reports VERSION_DRIFT and nothing fails), which is a fault the native derivation cannot have. Refactor rather than retire: the drift CHECK is still wanted (nothing native reports it inside graphify_health), only its hard-coded operand is superseded."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify-2"
unit = "mod-graphify"
kind = "module"
disposition = "retire"
confidence = 0.68
custom_thing = "Build-receipt verification: _receipt_matches / _receipt_problem re-derive sha256, byte length, node/edge/hyperedge counts and runtime version from graphify-out/build-receipt.json to detect an incomplete or tampered build, plus the GraphifyStatus.STALE state it feeds."
evidence = "python/src/dotfiles_setup/graphify.py:116-201 (_receipt_matches, _receipt_problem) and python/src/dotfiles_setup/graphify.py:237-238 (the call site in graphify_health). The docstring itself states the branch is dead here: graphify.py:180-185 — \"**This branch is currently unreachable in practice**: the KB's own writer (``kb_setup/graph.py``) raises on a links-keyed graph ... and the KB's own ``graphify-out/`` has no receipt on disk either.\" currency.toml:52-56 confirms this repo never builds a committed corpus, so nothing here writes a receipt."
native_alternative = "graphify itself now refuses to persist an incomplete build: the incomplete-build shrink guard (0.9.51) stays armed when a chunk came back hollow, unparseable, or omitting files, so a run that silently lost content cannot overwrite the existing graph with a smaller one; 0.9.53 extends it so an unverified per-source semantic shrink re-arms the guard and re-queues the source. graphify also enforces a graph size cap on every query before loading (_enforce_graph_size_cap_or_exit)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/CHANGELOG.md:37 (0.9.51 \"the incomplete-build shrink guard now stays armed when a chunk came back hollow, unparseable, or omitting files, so a run that silently lost content can no longer overwrite the existing graph with a smaller one\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/CHANGELOG.md:18 (0.9.53 \"a semantic node is no longer silently dropped when a run did not actually re-extract the semantic tier — an unverified per-source shrink arms the shrink guard and re-queues the source next run\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py:1250 (`_enforce_graph_size_cap_or_exit(gp)`)."
rationale = "The code is provably dead in this repo (no writer, and the only upstream writer raises on the links-keyed graphs this repo produces), and the failure class it was built for — a truncated/incomplete rebuild silently replacing a good graph — is now prevented at the source by graphify's own shrink guard at the pinned 0.9.53, so the receipt is not the only line of defence any more. Retiring it also removes graphify.py's sole dependency on kb_setup.graph.GraphifyBuildReceipt (graphify.py:23), narrowing a cross-repo coupling. Confidence held below 0.8 because the shrink guard proves build completeness, not byte-identity of the graph on disk after the build — but nothing in this repo ever verified that either, since the receipt path never executes."
probe = ""
correction = "**⚠ CITATION CORRECTED TWICE — ROUND 2'S CORRECTION IS ITSELF NOW STALE (round 5).** Round 2 moved the 0.9.51 shrink-guard anchor from `CHANGELOG.md:37` to `:40` and the 0.9.53 anchor from `:18` to `:16`. Both of those are now wrong as well, because **the corpus file was rewritten during this audit**: `sources/graphify/CHANGELOG.md` has an mtime of 2026-09-09 15:40 local — after round 3's render (15:24) and before round 4's (15:52) — and now runs to 1996 lines with `## 0.9.56 (2026-09-07)` and `## 0.9.57 (unreleased)` sections that did not exist when the finder ran. Re-probed against the bytes on disk right now: `grep -n 'incomplete-build shrink guard' CHANGELOG.md` -> **`89:`** (inside `## 0.9.51 (2026-08-28)`, which is at `:87`), and the 0.9.53 semantic-node bullet is at **`:65`** (inside `## 0.9.53 (2026-08-30)`, at `:59`). Control arm: `grep -n '^## 0\\.9\\.5' CHANGELOG.md` returns 9 real section headings, and a bogus token returns 0, so the probe discriminates. **The quoted sentences are still present verbatim, so the claim stands on substance at every round; only the pointers moved.** Read this row as the report's own worked example of why an anchor into an unversioned, re-fetched corpus decays — see Method → “The corpus moved under the audit”."
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify-3"
unit = "mod-graphify"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "A hard-coded 65,536-byte output cap on a successful query, raising GraphifyIncompleteError with the reason \"exceeds ...-byte agent transport cap; narrow the question, context, or budget\" — a repo-local model of how much output an agent can receive."
evidence = "python/src/dotfiles_setup/graphify.py:35 (`_MAX_AGENT_OUTPUT_BYTES = 65_536`) and python/src/dotfiles_setup/graphify.py:352-358 (the encode-and-compare that turns an rc=0 graphify answer into a hard failure)."
native_alternative = "Claude Code now owns the transport budget natively and makes it configurable rather than fixed: the `bashOutputMaxChars` and `taskOutputMaxChars` settings raise how much command/background-task output Claude receives inline before it is saved to a file (up to 128K characters, 2.1.261), and 2.1.265 saves oversized tool results to disk under a 1 GB cap with the in-conversation preview noting that the saved file was truncated."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — capability bullet \"`bashOutputMaxChars` and `taskOutputMaxChars` settings raise how much command/background-task output Claude receives inline before it is saved to a file (up to 128K characters)\", version 2.1.261; and the bullet \"1 GB cap on tool results saved to disk, with the in-conversation preview noting when a saved file was truncated\", version 2.1.265."
rationale = "The custom constant now both duplicates and understates the harness's own limit: at the host's 2.1.266 the inline budget is a setting reaching 128K characters, and output above it is saved to a file rather than lost — so a 64 KiB answer that the harness would have handled is instead converted into a raised exception and an rc=3, discarding a query graphify already paid for. The gate should either read the configured budget or be dropped in favour of the harness's save-to-disk path. Not 'retire' outright and confidence held mid-range: the module is also invoked outside Claude Code (mise tasks from a plain shell, CI), where no harness budget applies, so some cap or an explicit opt-out may still be wanted; and the digest is a changelog bullet, not the settings reference page, so the exact setting semantics are second-hand."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-graphify-4"
unit = "mod-graphify"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Three subprocess wrappers that construct argv for and shell out to graphify's own commands — build_query_args/query, build_affected_args/affected, build_prs_args/prs — parsing stdout as text."
evidence = "python/src/dotfiles_setup/graphify.py:287-370 (query), python/src/dotfiles_setup/graphify.py:373-430 (affected), python/src/dotfiles_setup/graphify.py:457-512 (prs); the single subprocess boundary is python/src/dotfiles_setup/graphify.py:270-284 (_run)."
native_alternative = "graphify ships its own MCP server (`--mcp`) exposing the same operations as typed tools — `list_prs`, `triage_prs`, `get_pr_impact`, `get_node`, `get_neighbors` — which would give structured results instead of stdout scraping and remove the argv-construction layer entirely."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/CHANGELOG.md:31 (0.9.52: \"the MCP `prs` tools (`list_prs`, `triage_prs`, `get_pr_impact`) now surface a genuine failure as an MCP error (`isError`)\") and CHANGELOG.md:32 (\"the MCP `get_node` tool now resolves a node through the same tiered resolver as `get_neighbors`\"); the `--mcp` flag is listed in the corpus README per .agent/kb/raw/graphify-readme-0.9.56.digest.md."
rationale = "Justification, not a default. Two independent reasons the native path is refused here. (1) Project policy: this is lane 2 — our own lookups — where `.claude/rules/do-not.md` #11 and `.claude/rules/research-doc-sources.md` § \"MCP: two lanes\" require the tool's CLI first and treat native MCP registration as a documented last resort; a registered graphify server would buy a call the existing CLI already makes. (2) The wrappers are not a thin re-expression of the CLI: query() gates on graph health BEFORE spawning, re-reads health AFTER and compares graph_sha256 so an answer computed against a graph that changed mid-run is rejected (graphify.py:325-349), and it treats any stderr as incomplete evidence — the `.claude/rules/graphify-first.md` fall-back-to-source contract. graphify's MCP surface has no equivalent of that stability gate, so adopting it would lose the property the wrapper exists for."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify-5"
unit = "mod-graphify"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "hook_guard_main + rewrite_hook_nudge: a repo-local re-implementation of graphify's Claude Code PreToolUse guard that execs `graphify hook-guard <kind>` and string-substitutes its advisory nudge copy so it names this repo's mise tasks instead of the bare `graphify` binary."
evidence = "python/src/dotfiles_setup/graphify.py:564-604 (rewrite_hook_nudge, hook_guard_main); wired by hand at .claude/settings.json:63-82 (matchers \"Bash|Grep\" and \"Read|Glob\" calling scripts/graphify-hook-guard.sh) through scripts/graphify-hook-guard.sh:28."
native_alternative = "graphify's own installer writes exactly these two PreToolUse matchers (`_claude_pretooluse_hooks`, matchers \"Bash|Grep\" and \"Read|Glob\", both invoking `graphify hook-guard`), and its strict/soft behaviour is already env-configurable at runtime via GRAPHIFY_HOOK_STRICT / GRAPHIFY_HOOK_STRICT_TTL without a reinstall."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/install.py:301-327 (_claude_pretooluse_hooks; matchers and commands); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py:676-679 and cli.py:700-704 (GRAPHIFY_HOOK_STRICT / GRAPHIFY_HOOK_STRICT_TTL overrides). Nudge copy is hard-coded: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py:18, cli.py:28 and cli.py:42 define _SEARCH_NUDGE, _READ_NUDGE and _READ_NUDGE_STALE as literal `json.dumps({...})` payloads."
rationale = "Justification, not a default. The wrapper's whole value is the one thing upstream does not expose: the nudge text is three literal json.dumps payloads at cli.py:18/28/42 with no flag, env var or file that changes the wording, so `mise run graphify-query` cannot be named without post-processing. Adopting the native installer would make it worse rather than better — a project-scoped `graphify install` emits a BARE `graphify` hook command resolved at run time (CHANGELOG.md 0.9.52, \"emits a bare `graphify` hook command resolved at run time instead of pinning the absolute interpreter path\"), which is precisely the bare-PATH invocation `.claude/rules/graphify-first.md` forbids here because two graphify installs exist on this machine, and `.claude/rules/do-not.md` #8 bans running graphify's installer against this repo at all. Only the strict-mode toggle is genuinely native, and scripts/graphify-hook-guard.sh:16-18 already documents that it is an env var to be set in settings.json rather than code."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify-6"
unit = "mod-graphify"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "GraphifyStatus.STALE as this module models it — a state reachable only through build-receipt mismatch, so graphify_health has no source-versus-graph freshness signal at all: a graph built before the working tree changed is reported FRESH and its answers are consumed as evidence."
evidence = "python/src/dotfiles_setup/graphify.py:52 (the STALE enum member) with its only producer at python/src/dotfiles_setup/graphify.py:200 (inside the receipt branch that graphify.py:180-185 documents as unreachable here); graphify_health's decision chain, python/src/dotfiles_setup/graphify.py:224-243, checks existence, JSON shape, collection types and runtime version — never a source mtime."
native_alternative = "graphify already computes per-file staleness in its PreToolUse guard and emits a distinct payload for it (_READ_NUDGE_STALE: \"graphify-out/graph.json exists but may be STALE for this file (the file changed after the last build)\"), and mise natively closes the loop that would keep it fresh — a `[[watch_files]]` hook with `patterns` + `run`/task, or `mise watch <task>`, either of which can re-run the existing graphify-update task on source change."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py:42-53 (_READ_NUDGE_STALE payload) with its emit site at cli.py:929; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/hooks.md:132-141 (\"## Watch files hook\" — `[[watch_files]] patterns = [...] run = ...`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tips-and-tricks.md:250-255 (`mise watch` reruns tasks when files change)."
rationale = "The health gate exists to satisfy `.claude/rules/graphify-first.md`'s rule that a stale graph must be declared unavailable and fall back to source — but the only staleness it can actually detect is the one that never happens here, while the staleness that DOES happen (the guard fired _READ_NUDGE_STALE against this very module during this audit) is invisible to it. Upstream already computes the signal, so the fix is to consume graphify's own staleness notion rather than invent a second one, and to let mise's native watch_files/watch keep the graph current instead of relying on a remembered manual `mise run graphify-update` (graphify's own `--watch` is separately banned by do-not.md #8, which is exactly why the mise-side mechanism matters). Confidence held at 0.6 because the exact upstream staleness predicate is per-file and internal to the guard, so wiring it into a whole-graph health verdict needs a design decision, not a drop-in."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify-7"
unit = "mod-graphify"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "The module wraps four of graphify's commands (query, affected, prs, update) and no others, while rewrite_hook_nudge rewrites only the `graphify query` and `graphify update` spellings out of the nudge it post-processes."
evidence = "python/src/dotfiles_setup/graphify.py:576-578 (the two-substitution rewrite) against the nudge text it receives, which also instructs the agent to run `graphify explain \"<concept>\"` and `graphify path \"<A>\" \"<B>\"`; the corresponding task surface is mise.toml:724-779 (graphify-query / graphify-health / graphify-update / graphify-affected / graphify-prs) — no explain, no path."
native_alternative = "graphify ships `explain` and `path` as first-class deterministic commands alongside `query`, with the same `--graph` addressing the module already builds argv for."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py:1534 (`elif cmd == \"path\":`) and cli.py:1701 (`elif cmd == \"explain\":`); both are listed in the corpus README's command surface per .agent/kb/raw/graphify-readme-0.9.56.digest.md (\"graphify query/path/explain\")."
rationale = "Two native capabilities go unadopted, and the omission is not neutral: the rewritten nudge this repo injects into every Read/Glob call still tells the agent to run bare `graphify explain` / `graphify path`, which `.claude/rules/graphify-first.md` forbids and for which no mise task exists — so the guard actively points at commands the repo has no sanctioned way to run. Either wrap them (same _run boundary, same health gate, two more build_*_args functions and mise tasks) or extend rewrite_hook_nudge to strip them. Confidence 0.6 because whether explain/path earn a task here is a judgement about usage, whereas the nudge inconsistency is settled."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify-8"
unit = "mod-graphify"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "graphify_health / HealthResult / GraphifyStatus — the typed, read-only health verdict (missing / corrupt / version_drift / stale / incomplete / fresh) with a JSON rendering mode and a distinct rc=3, which every read path in the module gates on before spawning graphify."
evidence = "python/src/dotfiles_setup/graphify.py:46-78 (GraphifyStatus, HealthResult), graphify.py:224-267 (graphify_health, graphify_health_main), consumed at graphify.py:325-328 and graphify.py:416-419; surfaced as mise.toml:733-735 ([tasks.graphify-health]) and required by `.claude/rules/graphify-first.md`."
native_alternative = "None. graphify exposes no health or doctor command. Its nearest neighbours each answer a different question: `diagnose multigraph` reports parallel-edge/multigraph structure, `check-update` only reports whether a `needs_update` flag file was written for non-code changes, and `cache-check` reports semantic-cache hit/miss for a file list."
alternative_evidence = "Enumerated every top-level command branch in /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/cli.py (provider:1065, prs:1182, hook:1185, query:1202, affected:1323, save-result:1446, reflect:1477, path:1534, explain:1701, diagnose:1835, add:1929, watch:1964, update:2428, hook-check:2488, hook-guard:2493, check-update:2505, tree:2513, merge-driver:2576, merge-graphs:2631, clone:2777, export:2801, benchmark:3123, global:3140, extract:3196, cache-check:4651, merge-chunks:4711, merge-semantic:4792) — no health/doctor branch. Control arm for the enumeration: the same command shape DID surface the near-misses it should (`diagnose` at cli.py:1835-1845 rejects any subcommand but `multigraph`; `check-update` at cli.py:2505-2512 dispatches to graphify/watch.py:2108-2120, whose body only prints when `graphify-out/needs_update` exists and always returns True). graphify hook status (cli.py:1197) reports git-hook installation, not graph health."
rationale = "Justification, not a default. Nothing upstream answers \"may I consume this graph's answers as evidence\", and the verdict is load-bearing for a repo rule rather than cosmetic: `.claude/rules/graphify-first.md` requires that missing/stale/corrupt/version-drift states be declared unavailable and fall back to source, never silently rendered as an empty or complete answer, and query()/affected() enforce that by raising before any subprocess runs. The three near-miss native commands each answer a narrower question and none of them gates evidence consumption. The parts of this unit that ARE superseded are reported separately as mod-graphify-1 (the hard-coded version operand), mod-graphify-2 (the receipt branch) and mod-graphify-6 (the missing source-staleness signal); the gate itself stays."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify_skill-1"
unit = "mod-graphify_skill"
kind = "module"
disposition = "refactor"
confidence = 0.92
custom_thing = "resolve_placement() derives the project-scoped destination by re-joining graphify's table entry itself: `skill_dst = (project_dir / cfg[\"skill_dst\"]).resolve()`, and its docstring asserts that every project-scoped branch of graphify's own resolver -- naming opencode, hermes, devin, amp, agents, antigravity -- 'resolves to project_dir / cfg[\"skill_dst\"] -- the same join used here'."
evidence = "python/src/dotfiles_setup/graphify_skill.py:185 (the join); python/src/dotfiles_setup/graphify_skill.py:145-154 (the parity claim); python/src/dotfiles_setup/graphify_skill.py:118 (already imports the private _PLATFORM_CONFIG under the pyproject SLF001 allowance, python/pyproject.toml:104)"
native_alternative = "graphify.install._platform_skill_destination(platform_name, project=True, project_dir=...) -- the vendor's OWN project-scope resolver, which special-cases six platforms AHEAD of the generic _PLATFORM_CONFIG lookup. Two of those special cases do NOT equal project_dir / cfg['skill_dst']: opencode resolves to .opencode/skills/graphify/SKILL.md while its cfg says .config/opencode/...; devin resolves to .devin/skills/graphify/SKILL.md while its cfg says .config/devin/... . Adopting the vendor resolver (read-only, same class of private access already allowed here) removes the divergence by construction."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/install.py:67-70 (opencode project branch -> .opencode/...); :350-354 (_PLATFORM_CONFIG['opencode'].skill_dst = .config/opencode/...); :81-84 (devin project branch -> .devin/...); :470-476 (_PLATFORM_CONFIG['devin'].skill_dst = .config/devin/..., with the vendor's own comment on :138-139 'Project scope: .devin/skills/graphify/SKILL.md (overridden in _platform_skill_destination)'); :105-107 (the generic fallback the custom join actually mirrors). Corpus pyproject version = 0.9.53, the repo's pin."
rationale = "The module's stated invariant -- 'this repo's placement table cannot silently drift from what the installed graphify version itself declares' (graphify_skill.py:18-22) -- is already false for two platforms at the pinned version. `mise run graphify-skill-install -- opencode` would create .config/opencode/skills/graphify/ INSIDE the repo, a path no opencode install reads, and devin likewise. The containment guard (UnsafePlacementError) would not catch it: both wrong paths are inside project_dir. Fix by calling _platform_skill_destination(platform, project=True, project_dir=project_dir) instead of re-joining cfg['skill_dst'], keeping the existing containment check on the returned path. Note the repo's only exercised platforms today are claude/codex/agents, where the join happens to agree -- which is exactly why the drift is silent."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify_skill-2"
unit = "mod-graphify_skill"
kind = "module"
disposition = "refactor"
confidence = 0.84
custom_thing = "install_skill() reimplements the whole packaged-skill copy: mkdir the parent, rmtree+copytree the references/ sidecar, byte-diff then .bak the existing SKILL.md, temp-file + atomic rename SKILL.md, write .graphify_version from importlib.metadata.version('graphifyy')."
evidence = "python/src/dotfiles_setup/graphify_skill.py:245-273; the references half at :253-257; the .bak at :259-264; the temp+rename at :266-268; the stamp at :270-272 via _installed_version()/:209-213"
native_alternative = "graphify.install._copy_skill_file(platform_name, project=True, project_dir=...) does exactly this and, under project=True, writes ONLY under skill_dst.parent -- the ~/.claude write, the CLAUDE.md registration and the AGENTS.md append all live in the CALLERS (install() and _agents_install()), not in _copy_skill_file. It additionally carries two behaviours this reimplementation lacks: (a) it clears an orphan references/ when the platform has no skill_refs, (b) it installs references/ atomically via a staged references.tmp + os.replace and chmods owner-write so a read-only packaged bundle (site-packages, Nix store, image layer) does not wedge the next install's rmtree."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/install.py:172-238 (_copy_skill_file, whole body); :207-211 (orphan references/ clearing on monolith platforms); :144-176 (_install_skill_references staged-rename + chmod S_IWUSR for read-only bundles); :236 (stamp from graphify.__version__, not importlib.metadata); and the writes this repo bans are provably OUTSIDE it: :640-665 (CLAUDE.md registration in install()) and :1508-1542 (_agents_install appending AGENTS.md)."
rationale = "The ban in do-not.md #8 is on graphify's install ENTRY POINTS, not on the copy primitive; _copy_skill_file under project=True is confined to skill_dst.parent by the vendor's own code, so wrapping it (behind the existing UnsafePlacementError containment check) keeps the confinement guarantee while deleting ~30 lines of duplicated copy logic and picking up two behaviours the copy currently misses -- most concretely, a platform that loses its skill_refs bundle leaves a stale references/ dir here forever, and a read-only packaged references/ makes the second install fail on shutil.rmtree(refs_dst) (graphify_skill.py:256), the exact case install.py:155-160 documents. Not 'retire': _copy_skill_file prints to stdout and sys.exit(1)s on a missing packaged file instead of raising, so the CLI layer's error handling (graphify_skill.py:288-296) still has to wrap it, and the containment check must stay because the vendor's resolver takes a caller-supplied project_dir with no containment of its own."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-graphify_skill-3"
unit = "mod-graphify_skill"
kind = "module"
disposition = "refactor"
confidence = 0.68
custom_thing = "known_platforms() = tuple(sorted(_PLATFORM_CONFIG)), used both as the CLI's accepted-platform set and as the error message's 'Known:' list."
evidence = "python/src/dotfiles_setup/graphify_skill.py:121-130; consumed at :288-293 (KeyError -> 'not a platform graphify's own installer knows about. Known: ...')"
native_alternative = "graphify.install._canonical_platform(name) resolves the vendor's CLI aliases before dispatch (_PLATFORM_ALIASES = {'skills': 'agents'}), and graphify's own usage line advertises the accepted set as _PLATFORM_CONFIG plus gemini and cursor."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/install.py:479-486 (_PLATFORM_ALIASES + _canonical_platform); :603-604 (install() canonicalises first); :697-699 (_print_install_usage: platforms = [*_PLATFORM_CONFIG, 'gemini', 'cursor'])"
rationale = "The claim 'every platform name graphify's own installer knows about' is narrower than the vendor's own accepted set: `mise run graphify-skill-install -- skills` fails with 'not a platform graphify's own installer knows about' even though `graphify install --platform skills` works (alias for agents). Routing the argument through _canonical_platform() before the _PLATFORM_CONFIG lookup is a two-line adoption of the vendor's own resolution. gemini/cursor stay legitimately unsupported (no _PLATFORM_CONFIG entry / separate code path) and the module already documents gemini's absence at :126-129 -- but the error text should say so rather than implying the installer does not know them."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-graphify_skill-4"
unit = "mod-graphify_skill"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The unit as a whole: a repo-owned, project-confined skill installer (UnsafePlacementError containment on skill_dst.parent, install_skill's three-artifact write, the `dotfiles-setup graphify skill-install` subcommand and the `mise run graphify-skill-install` task) standing in for the banned `graphify install`."
evidence = "python/src/dotfiles_setup/graphify_skill.py:61-78 (UnsafePlacementError) and :193-200 (the parent-containment check); mise.toml:749-758 (thin task caller); python/src/dotfiles_setup/main.py:1035 and :2005-2011 (CLI wiring); hk.pkl:669-673 (graphify_skill_surface commit gate) and doctor.toml:162-193 (session-time twin)"
native_alternative = "None at the host's pins. graphify's own entry point `graphify install --project` still appends the project .claude/CLAUDE.md for claude_md platforms and _agents_install appends AGENTS.md for always-on ones, which the repo's size/stub gates reject; and Claude Code has no native mechanism to materialise a skill out of an installed python package -- its two supported sources are a personal/project skills directory and a plugin from a marketplace, and graphify ships no Claude Code plugin/marketplace."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/install.py:640-665 (CLAUDE.md registration inside install(), fires with project=True) and :1508-1542 (_agents_install writes the AGENTS.md section); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins.md:15-20 (the only two ways to add custom skills: a skills directory, or a plugin) and :80 (plugin skills live in the plugin's own skills/ dir). Control-armed grep of the corpus graphify README for a Claude Code marketplace: 'marketplace' -> 0 hits, positive arm 'hook install' -> 7 hits on the same file with the same command shape (/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/README.md)."
rationale = "Justification, not a default: the native installer's confinement gap is real and unconditional at project scope (install.py:640-665 runs whenever cfg['claude_md'] is true, --project or not), and no harness feature substitutes -- Claude Code installs skills from a directory or a marketplace plugin, and graphify publishes neither. What must stay repo-owned is therefore the CONFINEMENT and the CLI/task/gate surface; findings 1-3 say the resolution and copy MECHANICS inside it should be delegated to graphify's own _platform_skill_destination/_copy_skill_file rather than mirrored, which also retires the drift the module's docstring claims is impossible."
probe = "uv run --project python python -c \"from graphify import install as g; from pathlib import Path; print([(p, g._platform_skill_destination(p, project=True, project_dir=Path('/tmp/x')), Path('/tmp/x')/g._PLATFORM_CONFIG[p]['skill_dst']) for p in ('opencode','devin','claude','codex','agents')])\"  # confirms findings 1's divergence against the INSTALLED 0.9.53 wheel, not only the corpus copy"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-handoff_check-1"
unit = "mod-handoff_check"
kind = "module"
disposition = "refactor"
confidence = 0.95
custom_thing = "Hand-rolled parse of mise's HUMAN-READABLE task table to build the set of known task names: subprocess.run(['mise','tasks','ls']) then line.split() and take fields[0] as the name."
evidence = "python/src/dotfiles_setup/handoff_check.py:107-130 (argv is literally [\"mise\",\"tasks\",\"ls\"]; names.add(fields[0])); consumed by python/src/dotfiles_setup/handoff_check.py:133-152; the argv shape is frozen by the test double at tests/test_handoff_check.py:35 (assert cmd == [\"mise\",\"tasks\",\"ls\"])."
native_alternative = "mise's own structured task listing: `mise tasks ls -J/--json` (carries name, aliases, hide, description, source per task) or `mise tasks ls --name-only` (one name per line), plus `--hidden` and `--no-header`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/tasks/ls.md:19 (`-J --json` Output in JSON format), :22 (`--hidden` Show hidden tasks), :23 (`--name-only` Only show task names, one per line), :24 (`--no-header`). Live probe on host mise 2026.9.3: `mise tasks ls --json | python3 -c ...` -> [('sh', ['shell']), ('stop', ['down'])], hidden: []."
rationale = "The column parse is not merely stylistic — it drops mise's alias column, and that produces a LIVE false positive today. Control-armed probe (same command shape, both arms): handoff_check.check(repo, 'ran mise run down and mise run shell') -> two UNKNOWN_TASK findings, while handoff_check.check(repo, 'ran mise run lint') -> [] (so the probe discriminates). `down` and `shell` are real, runnable aliases declared at mise.toml:416 (alias = \"shell\" on [tasks.sh]) and mise.toml:1176 (alias = \"down\" on [tasks.stop]), and `mise run up / down` is the form AGENTS.md's Quick Start itself teaches — so a handoff quoting the project's own documented command is reported as a stale citation. A second latent hazard: default `mise tasks ls` omits hidden tasks (docs/cli/tasks/ls.md:22), so the day any task gains `hide = true` the same false positive returns silently (currently 0 hidden tasks, so this arm is unarmed by the world, not by the probe). Refactor `_mise_task_names` to `mise tasks ls --json` and build the known set from `name` UNION `aliases`, skipping nothing on `hide`; the CLI already ships the structured surface, so the fix deletes parsing code rather than adding it. Not 'retire': mise has no notion of validating a task NAME quoted inside prose, so the surrounding checker still has a job. (`mise tasks validate` is a different capability — it checks task DEFINITIONS for cycles/missing deps/bad timeouts, docs/cli/tasks/validate.md:36-46 — and cannot see a markdown citation.)"
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-handoff_check-2"
unit = "mod-handoff_check"
kind = "module"
disposition = "keep"
confidence = 0.87
custom_thing = "Regex extraction of bare-text repo-relative `file:line` / `file:LO-HI` citations from a markdown handoff, then containment-checked existence plus a LINE-RANGE bound check against the file's real line count."
evidence = "python/src/dotfiles_setup/handoff_check.py:22-27 (_PATH_CITATION_RE), :70-101 (_path_findings: is_relative_to containment, read_text().splitlines() line-count bound, MISSING_PATH / BAD_LINE_RANGE verdicts)."
native_alternative = "The two nearest native link checkers already available to this repo: hk's `lychee` builtin step (a link checker hk can run as a pre-commit step) and agnix's REF-002 'Broken markdown links' rule (agnix 0.52.1 is pinned and already run by `mise run lint-docs`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/lychee.pkl:6 (\"Fast, async, stream-based link checker\"), :12 (argv = List(\"lychee\",\"--no-progress\",\"{{files}}\")); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/imports.rs:7 (REF-002 Broken markdown links), :231-249 (\"Only check agent config files, not generic markdown. Generic markdown files (plans, research notes, etc.) commonly have broken relative links\" — is_agent_config allowlist is CLAUDE.md/AGENTS.md/SKILL.md/GEMINI.md/*.instructions.md/copilot-instructions.md), :726 + parsers/markdown.rs:133-136 (link extraction regex is `(!?)\\[([^\\[\\]]*)\\]\\(([^()]*)\\)` — markdown links only)."
rationale = "Justification, not a default. Three independent mismatches, each cited: (a) SHAPE — every native checker extracts markdown LINKS (`[text](url)`) or bare URLs; a handoff cites `python/src/dotfiles_setup/lint.py:42-58` as prose, which parsers/markdown.rs:133-136 cannot see at all. (b) CORPUS — agnix deliberately refuses generic markdown and names 'plans' as the excluded class (imports.rs:232-234), and `.agent/plans/session-*.md` is gitignored, so hk (a git-hook system operating on staged/tracked files) can never hand it to lychee either. (c) CAPABILITY — no link checker validates a LINE RANGE against the target file's length, which is the half that actually rots (a cited file usually survives; the line number does not). So nothing native covers it and the custom code stays. The only thing worth adopting from the native side is nothing here — leave _path_findings as is."
probe = "uv run --project python python -c \"import sys; sys.path.insert(0,'python/src'); from pathlib import Path; from dotfiles_setup import handoff_check as h; print(h.check(Path('.'), 'see python/src/dotfiles_setup/lint.py:99999 and docs/nope.md:1'))\"  # both arms: expect BAD_LINE_RANGE + MISSING_PATH, and [] for a true citation"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-handoff_check-3"
unit = "mod-handoff_check"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "Newest-handoff discovery: glob `.agent/plans/session-*.md`, parse `session-YYYY-MM-DD[-]?<letter>.md` and order by (ISO date, letter-suffix rank) to pick the file to check when no path is given."
evidence = "python/src/dotfiles_setup/handoff_check.py:19-21 (_HANDOFF_RE), :51-67 (newest_handoff); the no-arg default path is python/src/dotfiles_setup/handoff_check.py:183-190; ordering is pinned by tests/test_handoff_check.py:239."
native_alternative = "planning-with-files' automatic recovery / `session-catchup.py`, and Claude Code's own session resume surface, are the only shipped mechanisms for 'find where the last session left off'."
alternative_evidence = "/Users/rmanaloto/.claude/plugins/cache/planning-with-files/planning-with-files/3.14.0/skills/planning-with-files/SKILL.md:42 (recovery reads `task_plan.md`, `progress.md`, `findings.md`), :45 (\"Automatic recovery stops there. Bare `session-catchup.py` and lifecycle hooks do not inspect agent session stores.\"), :227 (session-catchup.py aggregates session RECORDS, not repo files); .agent/kb/raw/planning-with-files-changelog.digest.md (no new capability since the 3.17.2 pin, so nothing newer closes this)."
rationale = "Justification: planning-with-files owns a fixed, different file set (`task_plan.md` / `findings.md` / `progress.md` in the project root) and its catchup script reads harness session records; neither reads `.agent/plans/session-<date><letter>.md`, which is this repo's own convention fixed by .claude/rules/agent-artifact-conventions.md. Claude Code's `--resume`/`/rewind` restore a TRANSCRIPT, not a repo-relative handoff document, and cannot rank two same-day handoffs by letter suffix. There is no native selector for a project-defined filename convention, so the 17-line selector stays. It is also the cheapest half of the module to keep correct — its ordering is already regression-pinned by a test."
probe = ""
correction = "**⚠ RE-ANCHORED AT THE PINNED VERSION, AND THE PREMISE RE-DERIVED (round 5).** Every native anchor in this row pointed at `planning-with-files/**3.14.0**/skills/planning-with-files/SKILL.md`, a cached version that is **not the one this session loads** — the pin is **3.17.2** (both trees exist under `~/.claude/plugins/cache/planning-with-files/planning-with-files/`, alongside 3.12.0/3.12.1/3.13.0/3.16.0/3.17.1), and `rule-notepad-enforcement-3` in this same report cites the 3.17.2 tree, so the audit was internally inconsistent about which version was authoritative. Re-probed at **3.17.2**: `:42` is now `Use the installed `scripts/resolve-plan-dir.sh` … with the task's `PLAN_ID` and `PWF_PLAN_ROOT`. Read `task_plan.md`, `progress.md`, and `findings.md` from that one selected directory`; the quoted “Automatic recovery stops there. Bare `session-catchup.py` and lifecycle hooks do not inspect agent session stores.” has moved from `:45` to **`:48`**; and `:227` is now the `scripts/resolve-plan-dir.sh` description rather than `session-catchup.py`'s — the session-record claim is carried at **`:3`** (frontmatter `description`) and **`:457`** (“Explicit `--metadata` reads same-project local session records and emits aggregate counts only”). **The premise named in the rationale DID change and the rationale's wording is now wrong:** 3.17.2 no longer owns “a fixed file set in the project root” — it resolves a per-plan `.planning/<id>/` directory (`:43`, `:76`, `:225`, `:227`, `:324`) and treats the project root as a *legacy* fallback, and `:324` even ranks candidate plans (`${PLAN_ID}` -> `.planning/.active_plan` -> newest `.planning/<dir>/` by mtime -> legacy `./task_plan.md`), which is the closest native analogue to `newest_handoff` that exists. **The `keep` disposition nonetheless survives re-derivation, on a narrower argument:** at 3.17.2 `grep -c '\\.agent/plans' SKILL.md` -> **0** (control arm `grep -c 'session-catchup' SKILL.md` -> **8**, so the file is readable and the probe is not blind), so planning-with-files still never reads `.agent/plans/session-<date><letter>.md`; and its ranking key is directory mtime over `.planning/<id>/`, not an ISO date plus a letter-suffix rank over a filename — a different namespace and a different ordering key. Rewrite the rationale to say that, not “project root”."
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-handoff_check-4"
unit = "mod-handoff_check"
kind = "module"
disposition = "refactor"
confidence = 0.62
custom_thing = "Delivery of the check is prose-only: the checker runs solely when an agent following the session-resume skill remembers to type `mise run handoff-check` (it is wired into no hk step, no suites.toml contract, and no settings.json hook)."
evidence = ".claude/skills/session-resume/SKILL.md:55 (```mise run handoff-check```) is the only invocation; mise.toml:923-926 is the thin caller; grep for 'handoff' across hk.pkl, hk-common.pkl, hk-image.pkl, .claude/settings.json and python/verification/suites.toml returns no wiring (control arm: the same grep shape returns mise.toml:923 and tests/test_handoff_check.py rows, so the 0-result on the gate files is real)."
native_alternative = "Claude Code's `SessionStart` hook with the `clear` source matcher — the exact moment the session-resume flow exists to serve — returning its output as session context, alongside the `startup|resume` hook this repo already runs."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:37 (SessionStart: 'When a session begins or resumes'), :310 (SessionStart matcher values: `startup`, `resume`, `clear`, `compact`, `fork`); the repo's existing SessionStart entry in .claude/settings.json matches only \"startup|resume\", so a `/clear` — the documented trigger for session-resume — fires nothing."
rationale = "The module is fine; its trigger is the weak layer, and .claude/rules/mise-tasks-only.md's own doctrine says markdown alone is 'relying on the LLM, never the only layer'. Claude Code natively supports the `clear` SessionStart source (hooks.md:310), which is precisely the post-/clear moment the session-resume skill documents — so the harness can run the checker deterministically instead of an agent remembering to. Scoped as refactor, not retire, because the hook is a delivery mechanism for this module, not a replacement for it. Kept at moderate confidence because it is a wiring judgement (a SessionStart hook adds startup latency and would emit findings for handoffs the user may not care about), and because a `clear`-matched hook's behaviour on this host has not been probed in this session."
probe = "Add a temporary SessionStart matcher \"clear\" entry running `mise -C \"$CLAUDE_PROJECT_DIR\" run handoff-check`, then /clear and confirm the output arrives as session context (and time it) — hooks.md:310 documents the matcher; nothing in this session proves the host honours it end-to-end."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-heredoc-1"
unit = "mod-heredoc"
kind = "module"
disposition = "unknown"
confidence = 0.60
custom_thing = "HEREDOC_PATTERN + blank_heredoc: a hand-written regex that locates a heredoc redirect, its body and its delimiter line, and blanks the body length-preservingly with a NUL filler. Consumed by hook_guard._inert_masked (PreToolUse guard) and workflow_hooks.git_write_subcommands (ADR-0001 check)."
evidence = "python/src/dotfiles_setup/heredoc.py:40-48 (HEREDOC_PATTERN), python/src/dotfiles_setup/heredoc.py:68-72 (blank_heredoc); consumers python/src/dotfiles_setup/hook_guard.py:45 and :685/:706, python/src/dotfiles_setup/workflow_hooks.py:54 and :504; pinned by python/verification/suites.toml:1272 and :1813. Recorded justification for hand-writing it: python/src/dotfiles_setup/hook_guard.py:583-604 (shlex cannot model it; bashlex is GPLv3/broken on <<'EOF'; tree-sitter-bash 'costs two deps and ~20-40ms of import')."
native_alternative = "ast-grep 0.45.3 — ALREADY a repo-pinned tool (github:ast-grep/ast-grep) — parses Bash via tree-sitter-bash 0.25.0 and exposes a real `heredoc_body` node with exact byte offsets, which is precisely the 'documented upgrade path' the banner comment named but evaluated only as a raw tree-sitter dependency, not as a tool this repo already ships. An in-process binding (`ast-grep-py`) also exists."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/lib.rs:242 (`impl_lang!(Bash, language_bash)`), .../crates/language/src/parsers.rs:25-26 (`conditional_lang!(tree_sitter_bash, \"tree-sitter-bash\")`), .../crates/language/Cargo.toml:20 (tree-sitter-bash 0.25.0) and :50 (it is inside the default `builtin-parser` feature, so the shipped binary carries it), .../crates/pyo3/Cargo.toml:2 (`name = \"ast-grep-py\"`). LIVE PROBE on the installed 0.45.3 binary (mise which -> ~/.local/share/mise/installs/github-ast-grep-ast-grep/0.45.3/ast-grep): `ast-grep scan --inline-rules 'id: hb\\nlanguage: Bash\\nrule:\\n  kind: heredoc_body' --json=compact /tmp/hd.sh` returned the body text with byteOffset {start:38,end:75} for `gh issue create --body-file - <<'EOF' ... EOF`. Control arm: the same command with a fabricated kind (`zzqq_no_such_node_kind`) is REJECTED ('Cannot parse rule INLINE_RULES'), so the probe discriminates a real node kind from an invented one."
rationale = "A native parse genuinely exists in a tool the repo already pins, so the written justification at hook_guard.py:583-604 is stale on its facts (it never considered ast-grep). But 'retire' is NOT supportable on what is measured today, for three reasons. (a) NO CORRECTNESS GAIN measured: on every probe the two agreed on body boundaries, including a case where BOTH diverge from real bash — for `<<EOF` with a space-indented `    EOF` line, ast-grep returned body 'line1\\n    ' and redact_heredoc_bodies redacted exactly the same span, so both terminate the body early and both let the following `git push` be read as a command (workflow_hooks.git_write_subcommands returned {'push'}). Swapping parsers would not close that. (b) HOT PATH: hook_guard runs per Bash tool call and must fail open; the ast-grep CLI is a file/stdin scanner, so adoption means either spawning a subprocess per call or writing the untrusted command to disk from inside a security guard — both are new failure surfaces, and the in-process alternative (ast-grep-py) is a native wheel not currently in python/pyproject.toml. (c) SINGLE IMPLEMENTATION: the module exists specifically so the two consumers share one pattern (heredoc.py:11-16); adopting ast-grep only in the batch consumer would re-fork it. Settle with the probe rather than by argument."
probe = "uv run --project python --with ast-grep-py python -c \"import time; t0=time.perf_counter(); from ast_grep_py import SgRoot; t1=time.perf_counter(); src=open('/tmp/hd.sh').read(); r=SgRoot(src,'bash'); n=[(m.range().start.index, m.range().end.index) for m in r.root().find_all(kind='heredoc_body')]; t2=time.perf_counter(); print('import_ms',(t1-t0)*1000,'parse_ms',(t2-t1)*1000, n)\" # then replay the guard's real transcript corpus (mise run command-audit inputs) through BOTH HEREDOC_PATTERN and ast_grep_py and diff the redacted strings; adopt only if import+parse stays under the guard's budget AND the diff shows ast-grep strictly closer to bash on at least one real command."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-heredoc-2"
unit = "mod-heredoc"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "owning_command + SHELL_INTERPRETERS + _PREFIX_WORDS + _ASSIGNMENT: a hand-rolled command-position scan that decides WHICH command owns a heredoc redirect, so a body a shell actually executes (`bash <<'SH' ... SH`) is left intact while a body that is stdin data is blanked (redact_heredoc_bodies)."
evidence = "python/src/dotfiles_setup/heredoc.py:52-65 (SHELL_INTERPRETERS, _COMMAND_BREAK, _ASSIGNMENT, _PREFIX_WORDS), :75-89 (owning_command), :92-107 (redact_heredoc_bodies). Recall arm in tests/test_workflow_hooks.py:917-935."
native_alternative = "None. tree-sitter-bash (via the pinned ast-grep) gives the syntactic `command` node but NOT which word in it is the interpreter; shellcheck 0.11.0 emits diagnostics only, with no parse-tree output format; Claude Code's PreToolUse payload still carries only the raw command string, with no parsed form and no custom-reason field on native permission deny rules."
alternative_evidence = "LIVE PROBE: for `env bash <<'SH'\\ngit push\\nSH`, `ast-grep scan --inline-rules 'kind: command' --lang Bash` returns the single node text 'env bash' (and `kind: redirected_statement` returns the whole construct) — the parser does not say that `bash`, not `env`, executes the body. `shellcheck --help` enumerates its ONLY output formats as checkstyle, diff, gcc, json, json1, quiet, tty — none is an AST/parse tree (enumeration, not an expectation-shaped grep). Claude Code corpus: grep for 'heredoc|parsed command|tokeniz' across /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/{hooks.md,settings.md,permissions*.md} hits only hooks.md:468 and hooks.md:470, which describe how the HOOK ITSELF is spawned (exec vs shell form), not the payload; control arm on the same corpus: 'PreToolUse' returns 70 hits, so the grep shape discriminates. No `reason` field appears in permissions.md/permission-modes.md/agent-sdk__permissions.md."
rationale = "'Which command consumes this redirect, and does it execute the body?' is a SEMANTIC question about argv-tail-exec wrappers, not a syntactic one, so no parser answers it — the live probe above shows the best available parser collapsing `env bash` into one command node. That is the required justification for keeping custom code under .claude/rules/use-tool-builtins.md rule 3. TWO CAVEATS worth carrying forward as defects, not as dispositions: (1) _PREFIX_WORDS (heredoc.py:63-65) omits the argv-tail-exec wrappers `env`, `timeout`, `stdbuf`, `nice`, `ionice` — measured: workflow_hooks.git_write_subcommands(\"env bash <<'SH'\\ngit push\\nSH\\n\") and the `timeout 30 bash <<'SH'` form both return set(), i.e. the shell-executed body IS redacted and the git write is MISSED, which is exactly the under-detect direction heredoc.py:95-99 says must never be traded for; the `bash`/`xargs -I{} bash` control arms return {'push'}, so the probe discriminates. (2) the recorded justification for the whole pair (hook_guard.py:583-604) should name ast-grep explicitly now that it is pinned, or the next reviewer cannot tell 'we checked' from 'we never looked'."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-heredoc-3"
unit = "mod-heredoc"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "Multi-redirect handling: HEREDOC_PATTERN consumes one redirect + one body per match, so a line stacking two heredocs leaves the second body unredacted and readable as commands."
evidence = "python/src/dotfiles_setup/heredoc.py:40-48. LIVE PROBE on `cmd <<A <<B\\nbody one\\nA\\nbody two\\nB\\n`: redact_heredoc_bodies blanked only the first body — the output still contains the literal 'body two\\nB' (repo-relative: reproduced via `uv run --project python python -c 'from dotfiles_setup.heredoc import redact_heredoc_bodies; ...'`)."
native_alternative = "ast-grep 0.45.3 Bash / tree-sitter-bash `heredoc_body` — but it does NOT close this case either: the same file yielded only three bodies ('body one\\n', the `<<-EOF` indented body, and the `bash <<'SH'` body); the stacked second body was likewise not reported as a heredoc_body."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/Cargo.toml:20 (tree-sitter-bash 0.25.0) plus the live `ast-grep scan --inline-rules 'kind: heredoc_body' --json=compact /tmp/hd2.sh` run, whose three reported byte ranges {12,21}, {46,56}, {72,81} omit the stacked second body. Control arm as in mod-heredoc-1 (a fabricated kind is rejected), so the empty result for that region is a real absence in the parse, not a broken query."
rationale = "Named separately so it is not silently folded into mod-heredoc-1's 'no correctness gain' claim: this IS a real limit of the custom regex, and it is the reason to state precisely that adopting the native parser would not fix it — the pinned native parser reproduces the same omission on the same input. Nothing native supersedes this capability, so the custom code stays; the residual risk is bounded because the miss is in the OVER-detect direction for both consumers (unredacted text is read as commands, producing a loud false positive rather than a silent miss), which is the direction heredoc.py:95-99 explicitly prefers. If the stacked-heredoc shape ever appears in a real transcript or workflow run: block, and file it against the regex rather than against the parser choice."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hk_builtins_audit-1"
unit = "mod-hk_builtins_audit"
kind = "module"
disposition = "refactor"
confidence = 0.90
custom_thing = "defined_steps() derives the step inventory with a line-anchored text regex over the three .pkl files, r'^\\s*[\"([a-zA-Z0-9_-]+)\"]', which cannot tell a HOOK key from a STEP key. Five hook names are consequently published in the generated doc's 'Custom steps' table as if they were steps 'carrying their own check/fix commands', and inflate the headline count (40 -> 35 real custom steps)."
evidence = "python/src/dotfiles_setup/hk_builtins_audit.py:151 (the pattern), :180 (custom = steps not in wired), :211-219 (renders the 'Custom steps' table). Miscounted rows in the committed output: docs/hk-builtins-audit.md:53 '## Custom steps (40)' with rows `check`, `fix`, `pre-commit`, `pre-push`, `commit-msg`. Those five keys are hooks, not steps: hk.pkl:706 `hooks {` opens the block and hk.pkl:710 `[\"pre-commit\"]`, :748 `[\"check\"]`, :753 `[\"fix\"]`, :763 `[\"commit-msg\"]`, :772 `[\"pre-push\"]` are its keys (plus hk-image.pkl:29 `[\"pre-commit\"]`). Control arm for the grep shape: the same anchored pattern DOES return the genuine step keys in the same files (hk.pkl:778 `[\"test\"]`, :775 `[\"ghcr_publish_prereqs\"]`), so the 0/1 discrimination is real and the defect is conflation, not a blind probe."
native_alternative = "Evaluate the config instead of pattern-matching its text: `pkl eval --format json <config>.pkl` (pkl is already pinned at 0.32.1 in .config/mise/conf.d/shared.toml:42) renders the module value, in which `hooks { ... }` and each hook's `steps { ... }` are separate structural levels, so a hook key can never be read as a step. hk's own resolved view, `hk check --plan --json` (`-P --plan` + `-J --json`), gives the same distinction for a single hook."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/pkl/docs/modules/pkl-cli/pages/index.adoc:898 ('use `--format json`, ...') and :933 (worked `pkl eval --format=json ...` invocation). Hook-vs-step nesting in the schema: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:320 `class Step`. Plan JSON flags: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/check.md:21-22 ('-J --json' / '-P --plan'); its shape (steps[].name only, one hook at a time) at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/plan.rs:5-33."
rationale = "The module exists precisely to stop a document miscategorising hk objects, and its own step table miscategorises five hooks as custom steps — the same class of false claim, at 12.5% of that table. pkl evaluation is the native, structural source and is already a pinned tool in this repo; `hk check --plan --json` is the tool's own resolved view. Refactor rather than retire because neither native route provides the per-config attribution ('Declared in hk.pkl / hk-common.pkl / hk-image.pkl') the doc's tables carry — hk-common.pkl is an imported fragment hk never plans on its own — so the module stays and swaps its parser for `pkl eval --format json` per config file, keeping the file->step mapping."
probe = "for f in hk.pkl hk-common.pkl hk-image.pkl; do pkl eval --format json \"$f\" > /tmp/$f.json; done; jq -r '.hooks | keys[]' /tmp/hk.pkl.json; jq -r '[.hooks[].steps | keys[]] | unique[]' /tmp/hk.pkl.json"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hk_builtins_audit-2"
unit = "mod-hk_builtins_audit"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "wired_builtins() finds builtin adoption with the source-text regex `=\\s*\\(?Builtins\\.([a-zA-Z0-9_]+)` over the three .pkl files — deliberately assignment-anchored so a comment naming `Builtins.x` is not counted as wiring."
evidence = "python/src/dotfiles_setup/hk_builtins_audit.py:59 (_ASSIGNED_BUILTIN), :128-141 (wired_builtins), :54-58 (the module's own record of the loose-pattern false positive on `Builtins.gitleaks` in a comment). Consumed at :178-207 to render the 'Wired builtins (28)' table, docs/hk-builtins-audit.md:20-51."
native_alternative = "None. The candidate — evaluating the config (`pkl eval --format json`, or hk's own pklr backend) — cannot answer this question: a step derived from `Builtins.x` evaluates to a plain `Config.Step` with no provenance field, and `@Builtins.meta` is an annotation on the builtin module, not part of the rendered value. `hk config dump` is explicitly runtime SETTINGS only (jobs, profiles, exclude, skip_steps, ...), not the step graph, and `hk builtins` prints bare names with no adoption information."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:320 `class Step` — a grep for 'builtin' across that file returns only doc-comment examples (:632, :667, :677) and one unrelated Tera mention (:823), never a field, and the grep is control-armed by those four hits so it is not a blind probe. /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/config/dump.md:8 ('Print effective runtime settings') and config/get.md:10 (the exhaustive key list). /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/cli/builtins.rs:11-15 — `hk builtins` is a bare `println!` loop over BUILTINS, no JSON, no metadata."
rationale = "Required justification: the fact being audited — 'this step was derived from a builtin' — exists only in the config's SOURCE TEXT. hk drops it during evaluation (Config.Step carries no builtin field) and exposes it through no CLI at the 1.57.0 pin, and the 1.58.1 gap digest reports zero new hk capabilities (.agent/kb/raw/hk-releases-since-pin.digest.md, 'None ... documentation-only release'). A text regex is therefore the only route, and this one is already anchored against the one false-positive shape the module measured. Note this is the opposite conclusion from finding -1: step NAMES are structural and should come from evaluation; builtin PROVENANCE is not, and must not."
probe = "pkl eval --format json hk.pkl | jq -e '[..|objects|select(has(\"check\"))] | map(keys) | flatten | unique | map(select(test(\"builtin\";\"i\")))' # expect [] — proves no provenance survives evaluation"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hk_builtins_audit-3"
unit = "mod-hk_builtins_audit"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "The regeneration path is manual: the `hk_audit` hk step declares only `check`, and both the failure message and the generated banner instruct a human to run `mise run hk-audit` by hand."
evidence = "hk.pkl:345-347 — `[\"hk_audit\"] { check = \"uv run --project python dotfiles-setup hk-builtins-audit --check\" }`, with no `fix`. python/src/dotfiles_setup/hk_builtins_audit.py:273-277 ('Regenerate with `mise run hk-audit`') and :62-68 (the same instruction in GENERATED_BANNER). The step is in `allSteps`, which hk.pkl:749-758 spreads into BOTH the `check` and the `fix` hooks, so a `fix` command here would already be reachable."
native_alternative = "hk's Step check/fix pair: declare `fix = \"uv run --project python dotfiles-setup hk-builtins-audit\"` alongside the existing `check`. `mise run fmt` is literally `hk fix` (mise.toml:1206), so the doc would regenerate itself in the repo's existing auto-fix lane while `mise run lint` (`hk check --all`) keeps failing loud — hk runs the check first by default (`check_first: Boolean = true`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:536 `fix: (String | Script | Command | CommandSpec)?`, :476 `check:`, :539 `check_first: Boolean = true`. mise.toml:1204-1206 `[tasks.fmt] run = \"hk fix\"`. hk.pkl:754-758 `[\"fix\"] { fix = true; steps { ...allSteps } }`."
rationale = "The generator already has an idempotent write mode (hk_builtins_audit_main with check=False) and the hk step already sits in the fix hook — the only thing missing is one `fix =` line, which is the tool's native mechanism for exactly this 'check reports it, fix repairs it' shape. Today a stale doc is a hard stop that a human must clear with a remembered command; the same class of generated artifact elsewhere in this repo should not need a memorised recipe (.claude/rules/mise-tasks-only.md). Refactor, not retire: the module is what does the generating; only its wiring adopts the native pair."
probe = "uv run --project python dotfiles-setup hk-builtins-audit && git diff --exit-code docs/hk-builtins-audit.md  # confirms the write mode is idempotent before wiring it as `fix`"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hk_builtins_audit-4"
unit = "mod-hk_builtins_audit"
kind = "module"
disposition = "unknown"
confidence = 0.60
custom_thing = "Ten of the 24 authored NOT_ADOPTED reasons are hand-maintained applicability judgements of the form 'No <language> source in project' (biome, eslint, golangci_lint, go_fmt, rubocop, rustfmt, stylelint, terraform, tf_lint, xmllint) — restated by a human and validated only for 'is still a builtin' / 'is not now wired', never for 'is still true'."
evidence = "python/src/dotfiles_setup/hk_builtins_audit.py:75-106 (the NOT_ADOPTED map) and :159-172 (stale_entries, which checks membership and wiring only — nothing re-tests the stated reason). Rendered as fact at docs/hk-builtins-audit.md:'## Deliberately not adopted (24)'."
native_alternative = "hk already computes this fact: every builtin carries `@Builtins.meta { project_indicators { ... } }`, and hk's detector walks them against the project root to decide which builtins are relevant, returning a machine-generated `reason` string per detection. If that detection is reachable read-only, the 'no X source in project' half of the map becomes derived rather than authored."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/cli/init/detector.rs:12-35 (`detect_builtins`, `Detection { builtin, reason }`), :38-60 (`matches_indicator` — file existence plus optional content grep). Example metadata: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ruff.pkl:5-12 (`@Builtins.meta { project_indicators { new { file = \"pyproject.toml\" } ... } }`). The blocker: its only consumer is `hk init`, documented as '- **Effect:** modifies state' at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/init.md:6, whose entire flag set is `-f --force`, `-i --interactive`, `--mise` (init.md:11-17) — no dry-run, no list-only, no JSON."
rationale = "The capability provably exists inside hk 1.57.0 and produces exactly the sentence these ten entries hand-write, but the corpus shows no read-only surface for it, and a gate must never invoke a state-modifying command against this repo. Whether `hk init` prints its detections before writing (and can be run against a throwaway copy of the tree to harvest them) is a behaviour question the docs do not answer — hence 'unknown' rather than a guessed 'refactor'. Settle it with the probe; if detections are only obtainable by writing an hk.pkl, this stays authored and becomes a 'keep' with that as the justification."
probe = "T=$(mktemp -d); tar -cf - --exclude .git -C /Users/rmanaloto/dev/github/ray-manaloto/dotfiles pyproject.toml python .config mise.toml 2>/dev/null | tar -xf - -C \"$T\"; cd \"$T\" && hk init --force > /tmp/hk-init.out 2>&1; echo \"rc=$?\"; cat /tmp/hk-init.out; ls \"$T\"  # does it PRINT the detections + reasons, and does it touch anything outside hk.pkl?"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hk_builtins_audit-5"
unit = "mod-hk_builtins_audit"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The unit as a whole: a generate-and-compare drift gate for a tracked markdown artifact — render() builds the document from live tool output plus config, hk_builtins_audit_main(--check) re-renders in memory and byte-compares the committed file, and stale_entries() fails the run when the one authored map stops being true."
evidence = "python/src/dotfiles_setup/hk_builtins_audit.py:250-278 (the check/write main), :175-247 (render), :159-172 (stale_entries). Wired at hk.pkl:345-347 (`hk_audit` step), mise.toml:1248-1249 (`[tasks.hk-audit]`), python/src/dotfiles_setup/main.py:505-506 and :2509-2510 (CLI subcommand), contract python/verification/suites.toml:1736 (`workflow.hk-builtins-audit`), tests/test_hk_builtins_audit.py:12-15 and :57-69 (both arms of the gate)."
native_alternative = "None found. hk's own introspection stops short of it: `hk validate` only validates the config file (/Users/.../hk/docs/cli/validate.md:8), `hk config dump` prints runtime settings not the step graph (config/dump.md:8), `hk builtins` prints bare names (src/cli/builtins.rs:11-15), and `hk agent instructions|hooks|mcp` emit agent integration snippets for a target editor, not a config audit (docs/cli/agent.md:8, agent/instructions.md:8). No repo linter covers generated-artifact drift either — agnix/claude-code-lint/cclint/agents-lint validate agent markdown structure, ast-grep has no pkl grammar, and mise task `sources`/`outputs` staleness cannot model `hk builtins` output, which is not a file."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/validate.md:8; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/config/dump.md:8 and config/get.md:10; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/cli/builtins.rs:11-15; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/agent.md:8 and agent/instructions.md:8; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/index.md (full subcommand surface — no audit/report verb). Currency: .agent/kb/raw/hk-releases-since-pin.digest.md states v1.58.1 is 'documentation-only ... no changes to hk's runtime behavior', so nothing newer than the 1.57.0 pin adds one."
rationale = "Required justification: no tool in the stack answers 'which of hk's builtins does THIS repo wire, where, and which did it decline on purpose' — hk exposes the two halves (the builtin list, the config) but never joins them, and the join is what makes the doc falsifiable. The gate's value is measured, not asserted: the hand-written predecessor claimed betterleaks ran as a second secret scanner while it was wired nowhere (hk.pkl:313-317 records the 0-vs-1 measurement against a control). Keep the unit; findings -1, -3 and -4 are the three seams inside it that should adopt native mechanisms."
probe = "uv run --project python pytest tests/test_hk_builtins_audit.py -x -q"
correction = "**⚠ CITATION CORRECTED (round 5) — ELIDED PATH, UNRESOLVABLE AS WRITTEN.** `/Users/.../hk/docs/cli/validate.md:8` resolves to `/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/validate.md:8` (`Validate the config file`). Substance unaffected."
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_guard-1"
unit = "mod-hook_guard"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "The two hook-suppression HARD BANS (`git --no-verify` / `git commit -n`, and a `HK_SKIP_HOOKS=`/`HK_SKIP_STEPS=` command prefix) are enforced only by the PreToolUse guard, which fails open on its own errors and on a missing interpreter."
evidence = "python/src/dotfiles_setup/hook_guard.py:493 (Rule \"git --no-verify\"), :517 (Rule \"HK_SKIP_HOOKS prefix\"), :207-212 (comment: these rules are \"the ONLY layer that can see a bypass\"); scripts/pretooluse-guard.sh:40 (`decision=\"$(uv run ... hook pretooluse)\" || fail_open`) and :36-38 (interpreter-absent fail_open); .claude/rules/mise-tasks-only.md:70 (\"Hard bans that must never fail open belong in settings.json permission deny rules, not the hook\")."
native_alternative = "Claude Code `permissions.deny` Bash rules. A deny rule is evaluated by the permission engine regardless of what a PreToolUse hook returns (so it cannot fail open), matches past any leading VAR= assignment, splits compound commands on `&& || ; | |& & newline`, and strips the built-in wrapper set (timeout/time/nice/nohup/stdbuf/command/builtin/noglob/bare xargs). `:*` is an equivalent trailing wildcard."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:495 (\"Hook decisions don't bypass permission rules ... a matching deny rule blocks the call\"), :235 (\"A deny or ask rule matches past any leading assignment\"), :222 (recognized separators; \"A rule must match each subcommand independently\"), :233 (built-in wrapper stripping), :189 (`:*` == trailing ` *`), :186 (a trailing `*` also matches the bare command only when it is the rule's only wildcard). In-repo precedent already using this mechanism: .claude/settings.json:24-25 (`Bash(chezmoi apply:*)`, `Bash(chezmoi update:*)` — the guard's chezmoi rule at hook_guard.py:258 is already natively backstopped) and .claude/settings.json:30-38 (the plan-attest deny set, adopted for exactly this fail-open reason)."
rationale = "Native deny covers ENFORCEMENT of these two bans strictly better than the guard (no fail-open path, engine-side subcommand splitting and wrapper stripping already implemented). It does not cover the guard's product: permissions.md documents no custom-reason field for a deny rule, so the redirect text (\"fix what the hook reports ... ship with mise run ship\") can only come from the hook. It is also COARSER in the one direction this guard has measured defects: a deny rule matches the whole command text, so `git commit -m \"docs: --no-verify is now guarded\"` would match, which is precisely the false-positive class hook_guard.py:_quoted_blind_masked (:697-712) exists to prevent. So: add the deny rules as the non-fail-open layer and keep the guard rules as the message layer — do not delete either."
probe = "Add `\"Bash(git commit *--no-verify*)\"` and `\"Bash(HK_SKIP_HOOKS=*)\"` to a throwaway `.claude/settings.local.json` deny list, restart a session, then run both arms: (a) `git commit --no-verify -m x` in a scratch repo must be blocked by the permission engine; (b) `git commit -m \"docs: --no-verify is now guarded\"` must NOT be blocked. If (b) is blocked, the native rule cannot replace the quoted_blind guard rule and must be scoped or dropped."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_guard-2"
unit = "mod-hook_guard"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "Hand-rolled command-position anchoring and wrapper unwrapping: `_WRAPPER` (env / VAR= / exec / nohup / time / timeout N / xargs, stackable) and `_CMD` (`(?:^|[;&|\\n]\\s*)` + `_WRAPPER`), applied to every rule, plus an unconditional `uv run` interpreter spawn on every Bash/Edit/Write/AskUserQuestion call."
evidence = "python/src/dotfiles_setup/hook_guard.py:117-121 (`_WRAPPER`, `_CMD`), :100-115 (the anchoring rationale); scripts/pretooluse-guard.sh:40 (the spawn, unconditional); .claude/settings.json:55-60 (one handler, matcher `Bash|AskUserQuestion|Edit|Write|NotebookEdit`, no `if` field)."
native_alternative = "The hook handler `if` field (permission-rule syntax). It matches tool name and arguments together, checks EACH subcommand of a compound command, inspects commands inside `$()` and backticks, strips leading `VAR=value` assignments, and — decisively for cost — skips spawning the handler process entirely when nothing matches."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:427 (`if` field reference), :353 (permission-rule syntax against tool name + arguments), :209 (\"the `if` check would fail and `block-rm.sh` would never run, avoiding the process spawn overhead\"), :440-446 (the Bash `if`-matching table: assignment stripping, per-subcommand checking, `$()`/backtick inspection, run-anyway on `$VAR`), :434 (\"The `if` field holds exactly one permission rule. There is no `&&`, `||`, or list syntax\"), :448 (\"Because the `if` filter is best-effort, use the permission system rather than a hook to enforce a hard allow or deny\")."
rationale = "Native covers PART of this: the shell-shape work `_CMD`/`_WRAPPER` reimplement is done by the engine, and `if` would remove the `uv run` spawn from the overwhelming majority of tool calls (today every Bash, Edit, Write and AskUserQuestion call pays it). It cannot replace the matcher: `if` is documented best-effort and explicitly not an enforcement mechanism (hooks.md:448); it holds exactly one rule per handler with no combining (hooks.md:434), so the 17-rule table becomes ~12 settings handlers; and `hook_guard.match()` must remain in Python regardless, because `dotfiles_setup.command_audit` replays it offline over months of transcripts (hook_guard.py:721-740 docstring). So the shape is: keep `match()`, add `if` filters as a spawn-cost prefilter — recall is preserved because the engine runs the hook whenever it cannot determine the commands (hooks.md:446, :448)."
probe = "Add a second PreToolUse handler in a scratch settings file with `\"if\": \"Bash(gh pr merge *)\"` pointing at a script that appends a line to a log, then run (a) `gh pr merge 1 --auto` and (b) `echo 'gh pr merge'` and (c) `rg 'gh pr merge' docs/ | head`. Confirm the handler spawns on (a) only — that establishes whether the native filter reproduces `_CMD`'s command-position anchoring (including its quoting behaviour, which hooks.md does not state) before any rule is moved."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-hook_guard-3"
unit = "mod-hook_guard"
kind = "module"
disposition = "refactor"
confidence = 0.45
custom_thing = "`_PRINTS_SECRET` / `_CREDENTIAL_NAME` / `_CREDENTIAL_LITERALS`: a NAME-shaped regex that denies `echo`/`printf`/`print` of a credential-named variable before the command runs. The module itself records that a shape regex cannot cover opaque or date-named credentials (`CT0`, `NVIDIA_20260705`) and that the literal list must be hand-synced with doctor.toml's `env_true` set; #474 is still open for the shapes it cannot see."
evidence = "python/src/dotfiles_setup/hook_guard.py:172 (`_CREDENTIAL_LITERALS`), :160-171 (the \"⚠️ A SHAPE regex cannot cover an opaque or date-named credential\" comment), :204-206 (`_PRINTS_SECRET`), :531 (the rule); .claude/rules/secrets-out-of-the-shell-env.md rule 7 (\"one shape is now gated, every other shape is carried by this rule alone\")."
native_alternative = "`PostToolUse` decision control: `hookSpecificOutput.updatedToolOutput` replaces the tool's result, and the docs name redaction as the intended use of the PreToolUse/PostToolUse pair. A VALUE-based redaction pass over Bash output closes the residual the NAME-based pre-execution rule structurally cannot (an unknown-named variable, a `fnox get`, a tool that prints a token of its own accord)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1032 (\"`PostToolUse`: `updatedToolOutput` replaces the tool's result\"), :1034 (\"For redaction or transformation use cases, intercept at `PreToolUse` for outbound tool inputs and `PostToolUse` for inbound tool results\")."
rationale = "Native does not supersede the deny rule — denying before execution is strictly better than redacting after, and PostToolUse cannot stop the command from running. But it is the mechanism that covers the half this rule provably cannot reach, and the repo already runs a PostToolUse handler on Edit|Write|NotebookEdit (.claude/settings.json:80-88) so the wiring pattern exists. Adopting it would let the pre-execution rule stay narrow (fewer false positives on legitimate credential HANDLING) while a value-matched PostToolUse pass catches whatever the name regex misses. Cost to weigh before adopting: the redaction hook would have to hold the credential VALUES to match them, which is itself a new exposure surface, and this repo's own rule forbids a probe printing a value."
probe = "Wire a scratch PostToolUse Bash handler emitting `{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"updatedToolOutput\":\"<redacted>\"}}` for a command whose output contains a KNOWN-FAKE canary string, run it, and confirm the transcript records the replacement rather than the canary. Both arms: a non-matching command must pass through unchanged. Use a fabricated canary — never a real credential."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-hook_guard-4"
unit = "mod-hook_guard"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`_inert_masked` / `_quoted_blind_masked` / `_QUOTED_SPAN` plus the heredoc module: a hand-written shell-quoting and heredoc masker that neuters separators which are DATA before the rule patterns run (#265)."
evidence = "python/src/dotfiles_setup/hook_guard.py:660-690 (`_inert_masked`), :697-712 (`_quoted_blind_masked`), :645-656 (`_QUOTED_SPAN`), :578-600 (the native/library options researched and each refuted: `shlex(punctuation_chars=True)` cannot separate a heredoc body from two newline-separated commands, `bashlex` is GPLv3/unmaintained and raises on `<<'EOF'`, `tree-sitter-bash` costs two deps and ~20-40ms per Bash call)."
native_alternative = "None reachable from a hook process. Claude Code's own shell-aware splitter exists (permissions.md:222 separators, hooks.md:440-446 subcommand/`$()` handling) but is only exposed declaratively through permission rules and the `if` field — the PreToolUse payload hands the hook the raw command string only, and no API returns the engine's parse."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:198-201 (the PreToolUse payload is `{\"tool_name\": \"Bash\", \"tool_input\": {\"command\": \"...\"}}` — the raw string, nothing parsed) and :448 (the `if` filter is best-effort and not an enforcement surface). Control-armed absence check over the corpus: `permissionDecisionReason` -> 31 hits with the same `grep -rn` shape, while any surface exposing a parsed command or the engine's tokenizer -> 0."
rationale = "Justification (required): the harness gives a hook the raw command string and nothing else, so any hook that must reason about command position has to parse the string itself; the three off-the-shelf parsers were probed and each fails on the exact shape this masker exists for (heredoc bodies and quoted separators). tree-sitter-bash remains the documented upgrade path if richer shell constructs ever matter — that is the only route by which this could later become a 'refactor'. Nothing at the host's pinned versions changes this."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_guard-5"
unit = "mod-hook_guard"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "The `Rule` dataclass's `since` landing-date field and the `rules()` / `match()` introspection surface, which exist so `dotfiles_setup.command_audit` can replay guard decisions over months of transcripts and tell a genuine bypass from pre-rule history."
evidence = "python/src/dotfiles_setup/hook_guard.py:49-88 (the `Rule` docstring: 147 of 155 rule-matching commands predated their rule, a 95% false-alarm rate without the cutoff), :721-740 (`match()` split out for command_audit), python/verification/suites.toml:1886 (the command-audit wiring contract binds `class Rule:`, `since: str`, `def match(`)."
native_alternative = "None. Claude Code exposes `transcript_path` to hooks (hooks.md), which is the raw material command_audit already reads itself, but there is no harness-side history, audit or replay API for hook decisions, and no per-rule effective-date concept anywhere in the permission or hook surface."
alternative_evidence = "Control-armed corpus grep (same `grep -rni` shape over /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/*.md): control term `permissionDecisionReason` -> 31 hits (probe discriminates); `hook decision history|audit log of hook|replay hook decisions|history of denied commands` -> 0 hits; `hook.*transcript_path` -> 2 hits, i.e. the harness offers the transcript file and nothing above it. Gap digest .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md lists 14 new capabilities for 2.1.258-2.1.266 and none is a hook-decision audit surface (`/skill-doctor`, 2.1.261, is skills-only)."
rationale = "Justification (required): the self-learning loop this field serves — mine transcripts for one-off commands the guard does not yet cover, and distinguish a bypass from history — has no counterpart in Claude Code, codex-cli, or any linter in the pin set (agnix / claude-code-lint / cclint / agents-lint check configuration files, not runtime command history). The dated table is also what makes the guard auditable at all, and the module documents (hook_guard.py:562-573) why any rule whose verdict depends on live repo state would break that replay. Nothing to adopt."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_guard-6"
unit = "mod-hook_guard"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "The redirect rule table as a whole (17 rules) plus `decide_payload`'s dispatch on `tool_name` into ask_quality / branch_guard / script_guard — i.e. one PreToolUse entrypoint that returns a task-specific EXPLANATION (\"use `mise run automerge -- <PR#>` for a bot-opened PR\") rather than a bare block."
evidence = "python/src/dotfiles_setup/hook_guard.py:249-544 (`_RULES`), :795-816 (`decide_payload` dispatch), :818-840 (`pretooluse_main` emitting `permissionDecision: \"deny\"` + `permissionDecisionReason`); .claude/settings.json:53-61 (single handler for all five tools)."
native_alternative = "Partially: `permissions.deny` blocks (no reason field), an `ask` rule prompts the human (permissions.md:64), and PreToolUse `updatedInput` can REWRITE a tool's arguments instead of denying (hooks.md:1030) — the closest native analogue to a redirect, e.g. rewriting `hk run check --all` to `mise run lint`. None of them delivers a written redirect to the model; only the hook's `permissionDecisionReason`, which is shown to Claude, does."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1030 (\"`PreToolUse`: `updatedInput` directly under `hookSpecificOutput` replaces a tool's arguments before it runs\"); permissions.md:64 (deny/ask/allow precedence) and the whole permissions.md rule surface, which documents no message or reason field on a deny rule; permissions.md:493 (\"PreToolUse hooks run before the permission prompt ... The hook output can deny the tool call, force a prompt, or skip the prompt\")."
rationale = "Justification (required): the unit's value is the REASON, and the reason is a hook-only capability at the host's version — a deny rule removes the call silently and an ask rule interrupts the human, neither of which teaches the agent which mise task to run. `updatedInput` is the one native mechanism that could subsume some redirects, but silently rewriting a command the operator asked for trades a teachable deny for an invisible substitution, and the guard's own doctrine (a redirect that misfires erodes trust) argues against it for a 17-rule table with negative lookaheads (`_GH_REPO_NOT_FOREIGN`) that permission-rule glob syntax cannot express. Keep the table; findings 1-3 are where native mechanisms should be added ALONGSIDE it."
probe = "If `updatedInput` is ever considered for the mechanical one-to-one redirects (`npx <tool>` -> `<tool>`, `hk run check --all` -> `mise run lint`): emit `{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"updatedInput\":{\"command\":\"mise run lint\"}}}` for a `hk run check --all` call in a scratch session and confirm both that the rewritten command runs and that the transcript makes the substitution visible to the operator."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_selfcheck-1"
unit = "mod-hook_selfcheck"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "check_script_syntax(): runs `bash -n` over scripts/pretooluse-guard.sh and scripts/web-setup.sh on every ship/land, to catch a parse error that would brick a wired hook."
evidence = "python/src/dotfiles_setup/hook_selfcheck.py:483-490 (`_run([\"bash\", \"-n\", ...])` over `_HOOK_SCRIPTS`, defined at python/src/dotfiles_setup/hook_selfcheck.py:52-54); gated on every ship via python/src/dotfiles_setup/pr.py:338-341."
native_alternative = "hk's `Builtins.shellcheck` step, already enabled in this repo, which reports shell PARSE errors (SC1046/SC1047/SC1072/SC1073) as errors — the same defect class — over `**/*.sh`; `mise run lint` is itself an always-run ship gate that runs before hook-selfcheck. agnix's CC-HK-008 covers the adjacent half (the wired script path resolving to a file that exists)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/shellcheck.pkl:12-21 (`match_any` = `**/*.sh`, `**/*.bash`, plus shebang types); wired at hk.pkl:195 (`[\"shellcheck\"] = Builtins.shellcheck`); ship gate at python/src/dotfiles_setup/pr.py:325 (`Gate(\"lint\", (\"mise\", \"run\", \"lint\"))`). agnix CC-HK-008: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/hooks/mod.rs:73-112. CONTROL ARM run this session on a 3-line unterminated-`if` script: `shellcheck broken.sh` -> SC1046/SC1047/SC1072/SC1073, rc=1, while `shellcheck fine.sh` -> rc=0 (and `bash -n broken.sh` -> rc=2), so the native tool discriminates on exactly the input this check exists for."
rationale = "The parse-error class is already covered by an always-run gate that runs earlier in the same ship matrix, so this check is a third pass over two files. Refactor rather than retire: shellcheck is not the bash parser (its acceptance of exotic bash constructs can diverge), and the selfcheck's value is being self-contained. The cheap modernization is to drop `_HOOK_SCRIPTS`/`check_script_syntax` and let the shellcheck builtin own syntax, keeping hook-selfcheck focused on WIRING — or, if the belt-and-braces arm is kept, say in the docstring that it is deliberately redundant with hk's shellcheck step so a future reader does not read it as the only coverage."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_selfcheck-2"
unit = "mod-hook_selfcheck"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "check_guard_decisions(): an in-process smoke that `hook_guard.decide('gh pr create --fill')` still denies and `decide('git status --porcelain')` still allows."
evidence = "python/src/dotfiles_setup/hook_selfcheck.py:473-480; run as the `guard-decisions` check in hook_selfcheck_main (python/src/dotfiles_setup/hook_selfcheck.py:547)."
native_alternative = "The pytest suite and the kb_setup.evals tier-2 guard table already assert the identical pair, and both are always-run ship gates ahead of / alongside hook-selfcheck — so this is a third copy of one assertion rather than an independent layer."
alternative_evidence = "tests/test_hook_guard.py:35 (`(\"gh pr create --fill\", \"mise run ship\")` in the deny table, exercised through `hook_guard.decide` at tests/test_hook_guard.py:67) — run by the always-on pytest gate at python/src/dotfiles_setup/pr.py:327-329; python/src/dotfiles_setup/eval_cases.py:117 (`_D(\"gh pr create --fill\", _DENY, ...)` inside `GUARD_FIXTURES`, wired into the graded table at python/src/dotfiles_setup/eval_cases.py:391) — run by `Gate(\"eval\", (\"mise\", \"run\", \"eval\"))` at python/src/dotfiles_setup/pr.py:352. NOTE: this alternative is in-repo, not a vendor feature; no doc citation applies."
rationale = "Not a native supersession but a live duplication the modernization should collapse: pr.py already documents that hook-selfcheck answers 'is the guard WIRED?' while the eval tier-2 fixtures answer 'does it DECIDE correctly?' (python/src/dotfiles_setup/pr.py:348-351), and `check_guard_decisions` is the one piece of hook_selfcheck that answers the SECOND question. Refactor, not retire outright, because deleting it changes the failure message a wiring break produces; the honest move is to delete the check and let its docstring's 'belt-and-braces' claim be carried by the eval gate, or to keep it and cross-reference the fixture corpus so the duplication is deliberate and visible."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-hook_selfcheck-3"
unit = "mod-hook_selfcheck"
kind = "module"
disposition = "refactor"
confidence = 0.65
custom_thing = "Hand-rolled structural parsing/validation of .claude/settings.json: `_event_entries` walks `hooks[event][].hooks[].command`, tolerating non-string matchers/commands, and `check_settings_wiring` re-derives what a well-formed hook entry looks like before it can assert anything project-specific."
evidence = "python/src/dotfiles_setup/hook_selfcheck.py:172-183 (`_event_entries`) and :186-253 (`check_settings_wiring`, incl. the `json.JSONDecodeError` arm at :190-192)."
native_alternative = "Two native layers already cover the SHAPE half: (a) the published Claude Code settings JSON schema at https://json.schemastore.org/claude-code-settings.json, which this repo can validate with its already-pinned `check-jsonschema` 0.38.0 and vendor through the existing schemas/sources.toml machinery; (b) agnix's CC-HK-001..CC-HK-028 hook rule family, which agnix ALREADY runs against `.claude/settings.json` in this repo — event-name validity (CC-HK-001), missing `type` (CC-HK-005), missing `command` (CC-HK-006), script-file-not-found (CC-HK-008), timeout policy (CC-HK-010/011), unknown handler type (CC-HK-016), invalid matcher VALUE (CC-HK-025)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings.md:602 and :616 (the `$schema` line and 'the published JSON schema ... gives you autocomplete and inline validation', with the caveat that the schema can lag the newest CLI); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/hooks/mod.rs:1 (`Hooks validation rules (CC-HK-001 to CC-HK-028)`), :52-72 (CC-HK-006), :73-112 (CC-HK-008), and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/hooks/helpers.rs:294 (CC-HK-001), :113 (CC-HK-005), :151 (CC-HK-011), :1644 (CC-HK-025); agnix is wired over settings.json at hk.pkl:498-504 with `--strict`."
rationale = "The project-specific half (WHICH hooks must be wired, with which matcher tokens) is not expressible in either native layer and must stay — but the module should stop being the place that discovers a malformed settings.json. Adopt the schema (vendor `claude-code-settings.json` into schemas/sources.toml next to mise/ruff/typos and validate in `mise run verify`) so a shape defect fails with a schema message, and let `check_settings_wiring` assume a well-formed file. Two caveats to carry into the change: settings.md:616 warns the schema lags new CLI keys (this repo wires `InstructionsLoaded`, a 2026 event), so the schema must be non-blocking on unknown-key warnings; and agnix's CC-HK-025 already produces known false positives here, scoped out in `.agnix.toml` (hk.pkl:494-495)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_selfcheck-4"
unit = "mod-hook_selfcheck"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "_unanchored_hooks(): every wired hook command in settings.json must mention `CLAUDE_PROJECT_DIR`, because hooks run in the session's cwd and a bare relative path silently fails open off-root (#343, 125 unchecked Bash calls)."
evidence = "python/src/dotfiles_setup/hook_selfcheck.py:256-278, with the anchor constant at :140 and the failure text at :273-277; called from check_settings_wiring at :252."
native_alternative = "None. The docs state the behaviour and recommend the variable but nothing enforces it, and the closest linter rule deliberately does the opposite: agnix CC-HK-008 RESOLVES a relative hook path under the project dir before checking existence, so an unanchored command passes it clean."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:600 ('`${CLAUDE_PROJECT_DIR}` stays put: it still points at the project root where the session started') — a recommendation, not a check; agnix's resolution behaviour at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/hooks/helpers.rs:580-581 (substitutes the var) and :2259 ('Regression guard: relative paths still resolve under project_dir'), i.e. CC-HK-008 cannot see this defect. cclint's hook-configuration rule likewise checks only shape and dangerous patterns (/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/cclint/src/rules/HookConfigurationRule.ts:17-31, :121-183)."
rationale = "Required justification: the #343 failure mode is a property of the COMMAND STRING relative to the session's cwd, and no shipped linter or harness command models it — the one rule that touches hook script paths normalises the defect away. Keep, and treat as a hardening item rather than a modernization one: the live settings.json anchors with `${CLAUDE_PROJECT_DIR:-.}` (.claude/settings.json PreToolUse/SessionStart/SessionEnd/InstructionsLoaded/PostToolUse commands), and the `:-.` fallback re-opens exactly #343 whenever the variable is unset, while the substring check at hook_selfcheck.py:271 passes on it. Tightening the check to reject a `:-.`-style default would be a real gain and is not available natively."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_selfcheck-5"
unit = "mod-hook_selfcheck"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "check_pretooluse_endtoend / check_ask_quality_endtoend / check_offroot_arm: hand-built PreToolUse and AskUserQuestion stdin payloads piped into `bash scripts/pretooluse-guard.sh`, asserting `\"permissionDecision\": \"deny\"` in stdout, with a scrubbed-env foreign-cwd arm."
evidence = "python/src/dotfiles_setup/hook_selfcheck.py:167-169 (`_hook_payload`), :281-325, :328-391, :394-470."
native_alternative = "`claude -p --output-format stream-json --verbose --include-hook-events` makes the HARNESS dispatch the hooks and emit `hook_started`/`hook_response` events, so the matcher evaluation and dispatch — which this module only asserts as a static string in `_SETTINGS_WIRING` — are exercised by the real implementation; `--init-only` (with `--debug-file <path>` to confirm the hooks ran) does the same for Setup and SessionStart hooks."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/cli-reference.md:96 (`--include-hook-events` — 'Include hook lifecycle events in the output stream ... Requires `--output-format stream-json`'), :95 (`--init-only` — 'Run Setup and SessionStart hooks, then exit without starting a conversation'), and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1237 ('start with `claude --debug-file <path> --init-only` ... and check the log for the Setup and SessionStart hook entries')."
rationale = "The hand-driven arms stay — the off-root/env-scrubbed arm needs a foreign cwd and a stripped PATH, which a real session cannot be made to reproduce, and the wrapper drive is fast and offline. What is now available natively and is NOT covered today is the layer above the wrapper: nothing in this module proves the harness's own matcher string actually routes Bash/Edit/Write/NotebookEdit into the guard (the matcher is compared as a string, hook_selfcheck.py:226-231). Adding one `claude -p --output-format stream-json --verbose --include-hook-events` arm would close that, at the cost of an API-spending, network-dependent gate — so it belongs behind an opt-in flag (like `mise run eval -- --live`), not in the always-run ship matrix."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && claude -p --output-format stream-json --verbose --include-hook-events 'Run the bash command: git status --porcelain' > /tmp/hookevents.json 2>&1; echo \"rc=$?\" >> /tmp/hookevents.json; grep -c 'hook_started\\|hook_response' /tmp/hookevents.json   # control arm: the same run with a denied command (`gh pr create --fill`) must show the guard's deny in the hook_response payload; if neither run emits hook events, the flag does not reach PreToolUse at 2.1.266 and this finding drops to 'keep'."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_selfcheck-6"
unit = "mod-hook_selfcheck"
kind = "module"
disposition = "unknown"
confidence = 0.45
custom_thing = "check_plan_attest_deny(): asserts the 10 `permissions.deny` rules for the attestation routes are literally present in settings.json — a presence check whose own docstring states it cannot drive the permission engine."
evidence = "python/src/dotfiles_setup/hook_selfcheck.py:499-538, docstring at :508-524 ('This is a presence check, and that is the honest limit of it. The permission engine belongs to the harness; nothing here can invoke it')."
native_alternative = "Two native routes the stated limit did not have when it was written: (a) a headless session with `--permission-prompts none` (2.1.259) auto-denies anything that would prompt while the permission mode keeps deciding, so `claude -p --permission-prompts none --output-format stream-json --include-hook-events` can drive the REAL engine and record the denial; (b) moving the ban into macOS managed settings (`/Library/Application Support/ClaudeCode/managed-settings.json`) makes it non-overridable by user or project settings, which is stronger than checking that a project rule was not deleted."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md:51 ('`--permission-prompts none` for unattended headless hosts — anything that would prompt is auto-denied while the active permission mode (including auto mode) keeps deciding', version 2.1.259 — note this flag is NOT in the offline cli-reference.md, control arm: `grep -n permission-prompts .../cli-reference.md` returned 0 lines); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/managed-settings.md:40 (macOS path) and settings.md:470 ('nothing you set overrides it, apart from a few security-sensitive exceptions')."
rationale = "Whether the headless route can actually exercise a `permissions.deny` rule end-to-end at 2.1.266 is unproven from the corpus — the digest describes prompt suppression, not deny-rule reporting, and the stream's shape for a rule-denied Bash call is not documented in the offline tree. It also cannot be probed from this session: every model route to attestation is denied by policy, so only the operator can run it. Managed settings are a separate, non-overlapping strengthening (they change WHERE the rule lives, not whether it is verified) and would be a deliberate operator decision, not a refactor an audit can take. Until the probe runs, keep the presence check exactly as written — its docstring already states its limit honestly, which is the right shape for an unverifiable gate."
probe = "OPERATOR-RUN ONLY (all model routes to attestation are permission-denied): cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && claude -p --permission-prompts none --output-format stream-json --verbose 'Run the bash command: mise run plan-attest -- --show' > /tmp/attest-deny.json 2>&1; echo \"rc=$?\" >> /tmp/attest-deny.json; grep -i 'denied\\|permission' /tmp/attest-deny.json   # control arm: the same prompt with `shasum -a 256 task_plan.md` must NOT be denied. If the denial is machine-readable in the stream, hook_selfcheck can gain a real live arm behind an opt-in flag."
correction = "**⚠ CITATION CORRECTED (round 5) — OUT-OF-RANGE LINE.** The `--permission-prompts none` bullet is cited at `…digest.md:51`; the file is **19 lines** long (`wc -l` -> 19). The bullet is at **`:17`** (`grep -n 'permission-prompts none'` -> `17:`). The quoted text is present verbatim at `:17`, including the 2.1.259 attribution, so the claim and its control arm (`grep -n permission-prompts …/cli-reference.md` -> 0 lines, i.e. the flag is genuinely absent from the offline CLI reference) both stand; only the pointer was wrong. The row stays `unknown` — what is unproven is whether the flag gives `hook_selfcheck` a way to exercise the permission engine, not where it is documented."
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-hook_selfcheck-7"
unit = "mod-hook_selfcheck"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The unit as a whole: _SETTINGS_WIRING — a declarative table of the five hook events this PROJECT requires, each with the command substrings and the exact alternation-token matcher membership it must carry, enforced single-entry (no pooling across entries) so one hook cannot launder another's requirement."
evidence = "python/src/dotfiles_setup/hook_selfcheck.py:87-131 (the table) and :193-251 (single-entry, exact-token enforcement; the anti-pooling rationale at :200-217 and the `Edit` ⊂ `NotebookEdit` substring trap at :218-222)."
native_alternative = "None. `/hooks` is an interactive READ-ONLY browser (it shows what is configured, it asserts nothing); `claude doctor` prints settings-file VALIDATION errors, not project requirements; cclint's `hook-configuration` rule validates shape and dangerous patterns only, and its event allowlist is stale — it would emit a false 'Unknown hook event' on this repo's live `InstructionsLoaded` hook."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:692 ('Type `/hooks` in Claude Code to open a read-only browser for your configured hooks'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/cli-reference.md:34 (`claude doctor` — 'install health, settings-file validation errors, and Remote Control eligibility'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/cclint/src/rules/HookConfigurationRule.ts:109-119 (KNOWN_EVENTS = PreToolUse, PostToolUse, UserPromptSubmit, Notification, Stop, SubagentStop, SessionStart, SessionEnd, PreCompact — no InstructionsLoaded) and :157-166 (warns 'Unknown hook event'), against .claude/settings.json which wires an `InstructionsLoaded` hook. Note cclint is `@felixgeelhaar/cclint` 0.16.0, NOT the `npm:claude-code-lint` 0.8.0 pinned at mise.toml:38 — that pin is not wired to any hk step or task (grep over hk*.pkl, mise.toml, *.yml: the pin line is its only occurrence), so no claim is made about its rules."
rationale = "Required justification: every native mechanism found describes or validates what IS configured; none of them can express 'this repository requires these five events, wired to these commands, with these exact matcher tokens'. That requirement is the whole point of the unit — it is the layer that caught #343 (guard silently absent off-root) and the `Edit` ⊂ `NotebookEdit` substring hole that a containment check in tests/test_mise_config_context.py could not see (hook_selfcheck.py:114-130). Keeping it is not a default: the two adjacent capabilities it also carries ARE superseded or duplicated (findings -1 and -2), and its structural/shape half should be delegated (finding -3), leaving _SETTINGS_WIRING plus _unanchored_hooks as the irreducible core."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image-1"
unit = "mod-image"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "Tier-1 'no missing tools' check parses mise's HUMAN text output: `mise_output=$(mise ls 2>&1)` then `missing=$(echo \"$mise_output\" | grep -c \"(missing)\")`, binding the literal string `(missing)` from mise's table renderer."
evidence = "python/src/dotfiles_setup/image.py:429-436 (_TIER1_MISE_LS_TOOLSET), shipped into both the CI no-mount smoke (build_smoke_script) and the devcontainer smoke (build_tier1_script) via python/src/dotfiles_setup/image.py:507-512"
native_alternative = "`mise ls --missing` (`-m`), combinable with `-J/--json`: mise emits exactly the missing tool versions as structured data, so the check becomes 'is the JSON empty' with no dependence on the table renderer's wording."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/ls.md:25 (`-m --missing` — Display missing tool versions) and :24 (`-J --json`)"
rationale = "This is a symptom-sniff on a string mise does not owe us (probes-need-a-control-arm rule 9): if the renderer ever drops the parenthesised `(missing)` marker the grep silently returns 0 and the gate becomes a check that can only pass. `--missing --json` asserts the capability directly. Caveat that makes this refactor rather than retire: `$mise_output` from the same `mise ls` call is REUSED by the zero-warning check at image.py:1046, so one plain `mise ls` invocation must survive (or be replaced per mod-image-3) — the two cannot be swapped independently."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image-2"
unit = "mod-image"
kind = "module"
disposition = "refactor"
confidence = 0.78
custom_thing = "Four 'backend policy checks' in the CI smoke tail are literal `grep -q '<key> = <value>' \"$MISE_CFG\"` text greps over the in-image mise system config file: `npm.package_manager = \"bun\"`, `pipx.uvx = true`, `cargo.binstall = true`, `python.uv_venv_auto = \"source\"`."
evidence = "python/src/dotfiles_setup/image.py:1025-1037"
native_alternative = "`mise settings get <SETTING>` — a read-only subcommand that prints the EFFECTIVE resolved value of a named setting. All four keys are real, documented mise settings."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/settings/get.md:3-5 (`mise settings get [-l --local] <SETTING>`, Effect: read-only, 'Show a current setting'); the four keys exist at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:1940 ([npm.package_manager]), :2116 ([pipx.uvx]), :307 ([cargo.binstall]), :2276 ([python.uv_venv_auto])"
rationale = "The greps assert FILE TEXT, not behaviour: a reformat (spacing, a `[npm]` table section instead of dotted form, a different quote style) fails a correct image, and a later override in another merged config layer passes a broken one. `mise settings get` returns what mise will actually do. It is also strictly non-redundant work today — the tier-1 identity block (image.py:402-426) already hashes that same file byte-for-byte, so the text greps add nothing the hash does not already prove about the file, while adding nothing about the effective setting either."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image-3"
unit = "mod-image"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "Zero-warning policy check: `warn_count=$(echo \"$mise_output\" | grep -ci \"WARN\")` over the captured `mise ls` stdout+stderr, failing the smoke when non-zero."
evidence = "python/src/dotfiles_setup/image.py:1045-1051"
native_alternative = "`mise doctor --json` — the documented health command; it emits a structured `warnings` array (and an `errors` array) and exits 1 when `errors` is non-empty."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor.md:4-13 (`mise doctor [-J --json]`, 'Check mise installation for possible problems', Effect: read-only) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/doctor/mod.rs:246-258 (a `warnings` key is emitted in the JSON payload; a non-empty `errors` returns `request_exit(1)`)"
rationale = "Partial coverage, so not a retire. `mise doctor --json` gives a typed warning set instead of a case-insensitive substring sweep that also matches any tool name or path containing 'warn'. But the two do not cover the same surface: doctor's warnings are health checks, while this grep catches warnings mise emits specifically while resolving THIS toolset, so the honest shape is 'add `mise doctor --json` as the structured gate and keep a narrower resolution-time check', not a straight swap."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image-4"
unit = "mod-image"
kind = "module"
disposition = "retire"
confidence = 0.85
custom_thing = "`_parse_human_size` — a 15-line regex parser converting docker's human size strings (`1.2GB`, `512 MB`, `0B`) back into bytes, with a hand-written 1024-based scale table, called on every layer of `docker history --format '{{json .}}'`."
evidence = "python/src/dotfiles_setup/image.py:1314-1331 (definition) and :1391 (`size_bytes = _parse_human_size(entry.get(\"Size\", \"0B\"))`), fed by the invocation at :1383-1385"
native_alternative = "`docker history -H=false` / `--human=false` — the docker CLI prints raw byte integers instead of human-readable sizes, removing the need to parse and re-scale anything."
alternative_evidence = ".agent/kb/raw/docker-history-human-flag-probe.md (live `docker history --help` on this host, docker-cli 29.7.2): `-H, --human   Print sizes and dates in human readable format (default true)`"
rationale = "The parser exists only to undo a formatting step docker can be told not to perform. It is also lossy in the direction that matters for a size gate: docker rounds to 3 significant figures before printing, so a 3.80 GB layer round-trips to a fabricated byte count, and the function silently returns 0 for any string it fails to match (image.py:1330) — a fat layer that stops parsing becomes a zero-byte layer at the top of a sorted-by-size report. Deleting it and adding `--human=false` to the call at image.py:1383 is a strictly smaller and more accurate implementation."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image-5"
unit = "mod-image"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "`size_report(image_ref, platform=...)` resolves a platform triple and stamps it into the returned payload, but the `docker history` and `docker image inspect` calls it makes carry NO `--platform`, so the layer table describes whatever the local image store resolves."
evidence = "python/src/dotfiles_setup/image.py:1367-1400 — `platform = resolve_platform(platform)` at :1372, `docker image inspect` at :1374-1376, `docker history` at :1383-1385 (neither passes `--platform`), and the platform echoed back into the payload at :1404"
native_alternative = "`docker history --platform <os/arch[/variant]>` — the docker CLI selects the requested platform's history from a multi-platform image rather than the store's default."
alternative_evidence = ".agent/kb/raw/docker-history-human-flag-probe.md (live `docker history --help`, docker-cli 29.7.2): `--platform string   Show history for the given platform. Formatted as \"os[/arch[/variant]]\" (e.g., \"linux/amd64\")`"
rationale = "This module already treats measuring one architecture and labelling it as another as the load-bearing defect class — `_sum_manifest_layer_sizes` raises rather than fall back for exactly that reason (image.py:1275-1276, '#674 AC2 ... refusing to measure a different architecture and report it as'). The compressed-size half honours that; the uncompressed/layer half does not, because the native `--platform` flag the docker CLI already offers is not passed. Adopting it closes the asymmetry without new custom code."
probe = ""
correction = "**⚠ AN ON-DISK, CONTROL-ARMED PROBE FROM THIS AUDIT REFUTES THIS ROW'S NATIVE SIDE, AND ROUNDS 1-5 NEVER CITED IT (added round 6). DO NOT ACT ON THIS ROW WITHOUT RE-RUNNING THAT PROBE.** The `alternative_evidence` above cites only `.agent/kb/raw/docker-history-human-flag-probe.md`, a `--help` listing proving the flag EXISTS. A sibling artifact written during the same finder stage — `.agent/kb/raw/docker-history-platform-flag-fails-local-probe.md` (2026-09-09 11:39) — RAN it, with both arms, and concludes verbatim that `--platform` \"is a real, documented flag but is **not currently usable against a locally-loaded single-platform image** on this docker-cli 29.7.2 / containerd-snapshotter host — it 404s regardless of whether the requested platform matches the image's own declared architecture\", warning that \"adopting `--platform` there as proposed would break local measurement rather than close the asymmetry\" (probe file :58-69). Its arms: the MATCHING platform on an image whose `docker image inspect` reports `amd64/linux` → `Error response from daemon: ... does not provide the specified platform (linux/amd64)` (:19-23, reproduced on a second unrelated image at :29-33); a deliberately WRONG `linux/arm64` on the same image → the identical error (:39-40); the same command WITHOUT `--platform` → succeeds (:50-55). Identical text for match and mismatch is the tell: the flag is not discriminating, it is rejecting any `--platform` request against a non-manifest-list local image. **The `currency` lens is the one that refuted this row (1 of 3), on exactly this probe.** THE TWO LENSES ARE NOT ACTUALLY IN CONFLICT — they probed different objects, and the reconciliation is the actionable part: the `coverage` lens probed the REGISTRY manifest list and found the amd64 manifest carries `\"variant\": \"v2\"`, so only the FULL triple `docker history --platform linux/amd64/v2` works (a bare `linux/amd64` fails there too), while the `currency` lens probed the LOCAL store, where every `--platform` form fails. `size_report`'s `docker history` call runs against whatever is in the local store, including the local-only `_gzip_size_for_image` fallback path. **Net: the refactor is conditional, not confirmed** — it is safe only for refs that resolve to a manifest list AND only with the full `os/arch/variant` triple that `resolve_platform()` already returns (platform_target.py:435-454; `DOTFILES_PLATFORM` default `linux/amd64/v2`, mise.toml:153), and it BREAKS the local-image path. The row's own `probe` field is empty; treat the two raw artifacts above as its probe. Same treatment as `mise-tasks-1-8`, and on stronger grounds, because this refutation is backed by a saved artifact."
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-image-6"
unit = "mod-image"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "`fetch_build_timing` shells `gh api repos/<repo>/actions/runs/<id>/jobs` and deliberately forgoes pagination, with an in-code justification that `--paginate` would emit one unparsable JSON doc per page and that 'the build chain has far fewer than 100 jobs'."
evidence = "python/src/dotfiles_setup/image.py:1623-1637 (docstring states 'No `--paginate`: the object-wrapped list endpoint would emit one JSON doc per page (unparsable)'), consumed by `_parse_build_timing` at :1588-1620"
native_alternative = "`gh run view <run-id> -R <repo> --json jobs` — gh's own jobs fetch follows the REST `Link: next` chain internally and returns ONE JSON document, exposing per-job `name`, `conclusion`, `startedAt` and `completedAt`: exactly the four fields `_parse_build_timing` reads."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/run/shared/shared.go:203-211 (the `jobs` JSON export emits `name`, `conclusion`, `startedAt`, `completedAt`), :89 (`SingleRunFields = append(RunFields, \"jobs\")`), and :510-522 (`GetJobs` loops `for jobsPath != \"\"` following `RESTWithNext`, i.e. full pagination into one slice)"
rationale = "The stated blocker for the native path is real for `gh api --paginate` but not for `gh run view --json jobs`, which solves the multi-doc problem inside gh and removes the unpinned '<100 jobs' assumption the current code rests on. Only the FETCH is superseded — the wall-clock aggregation (min start to max end across partly-parallel jobs) has no native equivalent and stays, so this is a refactor of `fetch_build_timing` alone, leaving `_parse_build_timing` pure and unit-testable as it is. Note the field names change case (`started_at` -> `startedAt`)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-image-7"
unit = "mod-image"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`_normalize_tool_os`, `_normalize_tool_arch` and `_tool_os_supported` — a hand-written python re-implementation of mise's `os = [\"linux/arm64\"]` selector semantics, so the expected tool set can be computed FOR A TARGET ARCHITECTURE from the host. Their docstrings cite `toolset/tool_request.rs` as the mirrored source."
evidence = "python/src/dotfiles_setup/image.py:129-224 (three functions; docstrings at :132, :147 and :174 all name `toolset/tool_request.rs`), consumed by `parse_declared_tools` at :227-249"
native_alternative = "No native cross-architecture resolver exists. mise decides this with `ToolRequest::is_os_supported`, which delegates to `cli::version::os_selector_matches`, which compares against the `OS`/`ARCH` statics built from `std::env::consts` — process constants with no config or env override, so mise cannot be asked 'what would you install on linux/arm64' from an amd64 host (or vice versa)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/toolset/tool_request.rs:561-571 (is_os_supported delegates to `crate::cli::version::os_selector_matches`) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/version.rs:55-89 (`ARCH` from `env::consts::ARCH`, `normalize_os`, `normalize_arch`, `os_selector_matches`)"
rationale = "Required justification: the check's whole value is that the EXPECTED set is derived independently of the mise instance being tested — asking mise for both sides would make the tier-1 tool-set diff tautological, and there is no mise interface that answers the selector question for a non-host architecture anyway (OS/ARCH are compile/process constants, not settings). Two maintenance corrections that follow from the current source: (a) all three docstring citations are STALE — the mirrored functions no longer live in `toolset/tool_request.rs`, they are `cli/version.rs:65-89`, and a mirror pointed at the wrong file drifts silently; (b) mise's `normalize_arch` returns the token UNCHANGED for an unrecognised arch while `_normalize_tool_arch` returns `None` — the outcomes agree today only because a token mise leaves unchanged can never equal `x64`/`arm64`, which is worth pinning in a test rather than left as coincidence."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image-8"
unit = "mod-image"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "Tier-1 image identity: `IDENTITY_IMAGE_PATHS` + `resolve_expected_identity_head` / `resolve_expected_identity_at_base` sha256-hash three repo build inputs and the injected smoke block re-hashes their in-image copies with `sha256sum`, to prove the image was built from the current (or merge-base) config."
evidence = "python/src/dotfiles_setup/image.py:289-323 (the map and both resolvers) and :402-426 (_TIER1_IDENTITY_BLOCK)"
native_alternative = "The repo ALREADY publishes SLSA provenance and SBOM attestations on every image target (`attest = [\"type=provenance,mode=max\", \"type=sbom\"]`) and already reads them in CI with `docker buildx imagetools inspect --format '{{json .Provenance}}'` to assert a build input — so 'which revision/inputs produced this image' may be answerable natively from the published attestation instead of a bespoke three-file hash map."
alternative_evidence = "docker-bake.hcl:167-169 (and :209-211, :244-246) declare `type=provenance,mode=max` + `type=sbom`; .github/workflows/build-publish.yml:755-771 already inspects `{{json .Provenance}}` and greps it for the p2996 digest"
rationale = "Cannot be settled from the offline corpus: BuildKit's own docs are not in the corpus, and whether `mode=max` provenance records per-COPY LOCAL FILE digests (as opposed to only the git revision, build args and base-image materials) decides whether this is a retire, a refactor, or genuinely nothing. Two facts already argue for 'refactor at most': the devcontainer arm needs MERGE-BASE semantics per input, which no attestation carries, and the local thin host-user overlay's provenance describes the overlay, not the `:dev` base being validated. Settle it before acting."
probe = "docker buildx imagetools inspect ghcr.io/ray-manaloto/dotfiles-devcontainer:dev --format '{{json .Provenance}}' > /tmp/prov.json; jq -r '.. | objects | keys[]' /tmp/prov.json | sort -u | head -50; jq -e '.. | objects | select(has(\"materials\") or has(\"resolvedDependencies\"))' /tmp/prov.json | head -40   # does any entry name .devcontainer/mise-system.toml or carry a digest for a local COPY source?"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image-9"
unit = "mod-image"
kind = "module"
disposition = "unknown"
confidence = 0.45
custom_thing = "The smoke harness itself: ~380 lines of shell assertions embedded as python string constants (_TIER1_MISE_PATHS, _TIER1_IDENTITY_BLOCK, _TIER1_MISE_LS_TOOLSET, _TIER1_PYTHON_DEFAULT, _TIER3_DEFAULT_CLANG, _TIER3_COMPILER_BODY) that are emitted by `dotfiles-setup image smoke-script --tier {1,3}` and eval'd inside the container, plus `build_smoke_docker_cmd` which hand-assembles the `docker run --rm --platform ... --entrypoint /bin/bash ... -lc <script>` invocation."
evidence = "python/src/dotfiles_setup/image.py:380-770 (the shell bodies), :1057-1111 (build_smoke_docker_cmd), consumed by scripts/devcontainer-smoke.sh:34 and :84 and by .github/workflows/build-publish.yml:1004"
native_alternative = "Candidate: GoogleContainerTools/container-structure-test — a declarative YAML container-assertion runner (commandTests / fileExistenceTests / fileContentTests / metadataTest) that natively covers the presence, PATH-resolution and command-exit-status classes this script hand-rolls. CONTROL ARM for the adjacent candidate: the pinned @devcontainers/cli 0.89.0 has NO test subcommand (its top-level command list is up / set-up / build / run-user-commands / read-configuration / outdated / features / templates / exec), so the devcontainer CLI is NOT the native home for this."
alternative_evidence = ".agent/kb/raw/devcontainer-cli-0.89.0-help.md:6-15 (the full top-level subcommand list — no `test`) is on-disk evidence for the control arm; container-structure-test itself has NO corpus entry and NO saved URL, which is why this finding is 'unknown' rather than a recommendation."
rationale = "The zero-bash-logic rule tolerates this today only because the shell is GENERATED and the branching lives in python (bash_budget.py:91 records the exemption verbatim: 'cores live in `dotfiles-setup image smoke-script` (#223)'), but 'research an existing tool before custom code' has evidently never been run against this surface, and roughly half the assertions (command presence, file existence, exit-status probes) are the exact shape a structure-test runner declares. The other half — sha256 identity, the mise tool-set set-diff, compile+link+RUN of sanitizer/reflection/OpenMP/flang programs, the p2996 ref-pin parse — almost certainly is NOT expressible declaratively, so the realistic ceiling is a partial adoption, and adopting a new unpinned dependency for half a harness may well lose. Do not act on this without the probe."
probe = "curl -sL https://raw.githubusercontent.com/GoogleContainerTools/container-structure-test/main/README.md -o .agent/kb/raw/container-structure-test-readme.md && grep -nE 'commandTests|fileContentTests|metadataTest|--platform|sha256|expectedError' .agent/kb/raw/container-structure-test-readme.md | head -40"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image-10"
unit = "mod-image"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The metrics half of the module with no native counterpart: `classify_layer_source` (keyword layer->toolchain attribution), `estimate_pull_time_s` (modeled download+zstd-decompress wall time), `compare_payloads`/`metrics_compare` (benchmark deltas), `render_metrics_summary`/`metrics_summary` (GitHub step-summary markdown), `count_installed_tools`, `_gzip_size_for_image` (streaming `docker image save` through zlib), and `decide_analysis_target`/`resolve_analysis_target` (the #231 `:pr-NNN` resolution)."
evidence = "python/src/dotfiles_setup/image.py:1351-1364 (classify_layer_source), :1426-1454 (estimate_pull_time_s), :1494-1530 (compare_payloads/metrics_compare), :1660-1791 (render_metrics_summary/metrics_summary), :1564-1586 (count_installed_tools), :1137-1157 (_gzip_size_for_image), :1817-1900 (decide_analysis_target)"
native_alternative = "None found. The nearest native mechanisms are already adopted rather than reimplemented: registry layer sizes come from `docker buildx imagetools inspect --raw` (image.py:1297-1299) with the zlib stream only as the local-only-image fallback; the PR lookup is `gh api repos/<repo>/commits/<sha>/pulls` (image.py:1905-1913); the summary is written to GitHub's own `$GITHUB_STEP_SUMMARY`. Control arm on the harness side: the Claude Code 2.1.258-2.1.266 gap digest lists 14 new capabilities and NONE touch container images, build metrics or CI job timing."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md:1-62 (all 14 capability bullets: plugin-dir hot reload, tool-result cap, bashOutputMaxChars/taskOutputMaxChars, --append-subagent-system-prompt-file, /skill-doctor, org-policy line, /diff panel, prompt-cache-miss cause, /reload-plugins headless, text /advisor, managedMcpServers, --permission-prompts none, glab MR recognition, `claude plugin validate --json`)"
rationale = "Required justification: these are project-specific MODELS and POLICIES, not generic capabilities. `classify_layer_source`'s buckets are this image's five toolchains; `estimate_pull_time_s` is a declared estimate with this project's bandwidth/zstd assumptions echoed back in the payload; `decide_analysis_target` encodes a GitHub-platform fact (`workflow_run.pull_requests[]` is empty on same-repo PR runs) that no tool exposes as a feature. Nothing in mise, hk, uv, gh, docker, renovate, graphify, the pinned linters, or the Claude Code / codex-cli gap digests offers layer-to-toolchain attribution, a pull-time model, or benchmark-delta rendering. The parts that DO have native answers are already delegating to them."
probe = ""
correction = "**⚠ CITATION CORRECTED (round 5) — OUT-OF-RANGE LINE SPAN AND A MISCOUNT.** The cited span `…digest.md:1-62 (all 14 capability bullets…)` cannot exist: `wc -l` on `.agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md` -> **19**. The bullets are at **`:5-19`** and there are **15** of them, not 14 (`sed -n '5,19p' … | grep -c '^- '` -> 15); `:1-4` is the digest's own provenance header (source file, and the note that fixes-only entries were skipped). Read the anchor as **`:5-19`, 15 bullets**. Control arm: I read the file in full with `cat -n`; every bullet the row's prose names is present verbatim, so the substance of the `keep` is untouched — only the span and the count were wrong. This class matters more than its size: the gap digests are the whole basis of the report's “newer than the offline corpus” lane, so an out-of-range anchor into one is the citation defect most likely to be replayed by a later session."
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_lock-1"
unit = "mod-image_lock"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "A hand-rolled 5-pass convergence loop that re-runs the WHOLE `mise lock` command, tolerating failure on every pass but the last, because 'mise lock resolves through GitHub, and anonymous quota exhausts mid-run'."
evidence = "python/src/dotfiles_setup/image_lock.py:79-81 (DEFAULT_PASSES = 5, with the anonymous-quota rationale), :281-320 (run_lock_passes: loop, warn-and-retry, raise on the last failure), :306-317; wired as a CLI flag at python/src/dotfiles_setup/main.py:728-733 and defaulted into image_lock_main at :439."
native_alternative = "mise's own transient-HTTP retry (`http_retries`, default 3, explicitly retrying HTTP 429 Too Many Requests with jittered ~200ms/1s/4s/15s backoff, per-request and in-process) plus mise's native GitHub token discovery (`MISE_GITHUB_TOKEN`/`GITHUB_TOKEN` env, `github_tokens.toml`, and `github.gh_cli_tokens` which is default TRUE and reads the gh CLI's hosts.yml), which lifts the anonymous 60/hr quota that the loop exists to survive."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:1269-1293 (`[http_retries]` default 3; 'Retries are attempted only on transient errors: HTTP 5xx ..., 429 (Too Many Requests)'; 'Backoff schedule with jitter: ~200ms / ~1s / ~4s / ~15s'); same file :894-908 (`[github.gh_cli_tokens]` default true, 'fallback when no GITHUB_TOKEN or MISE_GITHUB_TOKEN environment variable is set'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/AGENTS.md:346 ('If GitHub API calls 429, run export GITHUB_TOKEN=\"$(gh auth token)\"; export MISE_GITHUB_TOKEN=\"$GITHUB_TOKEN\"'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/CHANGELOG.md:8911 (http_retries setting added upstream, so it predates both the image pin 2026.9.1 and the host 2026.9.3)."
rationale = "The 429 half of the loop's stated cause is already handled natively, per-request and with backoff, one layer down — a whole-command re-run is the coarsest possible retry and hides which request actually failed. The module should (a) assert/propagate a GitHub token before the first pass (it inherits os.environ at :298-304, so a token already present is used, but nothing checks that one exists or says so in the log) and (b) raise MISE_HTTP_RETRIES for the child rather than budgeting 5 full passes. It is a refactor and not a retire because a lock run resolves hundreds of tools and a per-request retry cannot rescue a run whose quota is exhausted for the hour, and because DEFAULT_PASSES=5 is deliberately kept identical to CI's composite (image_lock.py:272-273) — any change has to move both call sites together."
probe = "Inside the amd64 devcontainer, with a token present: `MISE_GITHUB_TOKEN=$(gh auth token) MISE_HTTP_RETRIES=5 uv run --project python dotfiles-setup image-lock --no-container --passes 1` and check the log for 'mise lock converged on pass 1/1'; control arm = the same run with `MISE_GITHUB_TOKEN=` and `github.gh_cli_tokens` disabled, which must still need >1 pass."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_lock-2"
unit = "mod-image_lock"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "A hard refusal to regenerate the image locks on any non-Linux host (and the default routing into the amd64 devcontainer that exists to work around it), premised on 'mise on darwin cannot write the linux conda checksums (jdx/mise#7700), so a regen there silently truncates' — measured 2026-08-08."
evidence = "python/src/dotfiles_setup/image_lock.py:17-20 (gotcha 1), :83-84 (REQUIRED_OS = 'Linux'), :162-190 (host_can_lock refuses on OS then on machine), :195 (the refusal names the upstream defect), :395-410 (image_lock_main routes into the container by default when the host is incapable, and errors when --no-container); mise.toml:1341-1343 documents the same premise."
native_alternative = "mise 2026.8.6 (2026-08-14) shipped '(conda) preserve cross-platform lock data' (jdx/mise#11946), and the conda backend now solves repodata for an arbitrary TARGET platform rather than only the current one (`target_to_conda_platform(target: &PlatformTarget)` feeding `solve_packages(vec![match_spec], platform, opts)`), which is the capability whose absence the refusal encodes."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/CHANGELOG.md:952 ('**(conda)** preserve cross-platform lock data by @jdx in #11946', under the `## [2026.8.6] - 2026-08-14` header at CHANGELOG.md:930); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/backend/conda.rs:134 (`fn target_to_conda_platform(target: &PlatformTarget) -> CondaPlatform`) and :796-811 (deps solved with that target platform, not `CondaPlatform::current()`). The repo now pins mise 2026.9.1 in the image (.devcontainer/Dockerfile:115) and the host runs 2026.9.3 — both strictly after the fix, whereas the refusal was measured on 2026-08-08."
rationale = "The refusal's premise carries a date, and a fix landed upstream six days after that date, inside the version range this repo now pins. If darwin can write faithful linux conda entries at 2026.9.1, then host_can_lock's OS axis, the container routing, and the whole --container/--no-container tri-state become dead weight for the common case (the machine axis would still be needed for anything genuinely arch-bound). But nothing in the corpus states that #11946 closes #7700 — the changelog line is a one-liner and #7700 is not referenced anywhere in the corpus (grep for '7700' returned only unrelated commit hashes; control arm: grepping 'conda' in the same file returns the five real conda entries) — so this must be measured, not assumed. Do NOT relax the gate on the strength of a changelog line: the failure mode is a SILENT truncation with an unchanged tool count (#648)."
probe = "On this macOS host, bypass the gate on a scratch copy: `python -c \"from pathlib import Path; from dotfiles_setup.lock_refresh import stage_system_lock_dir\"`-style staging into /tmp, then `mise-pinned/bin/mise lock --bump --platform linux-x64 --platform linux-arm64 -C <stage>` with the 2026.9.1 binary, and compare `grep -c linux-x64 <stage>/mise.lock` and the conda-section entry count against `git show HEAD:.devcontainer/mise-system.lock`. Control arm: run the identical command inside the amd64 devcontainer and require the two outputs to agree line-for-line after provenance stripping. Only a byte-level agreement retires the OS gate."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_lock-3"
unit = "mod-image_lock"
kind = "module"
disposition = "keep"
confidence = 0.65
custom_thing = "platforms_to_lock/lock_platforms: a bespoke platform-set computation — union of the platforms the committed lock already carries, plus every published architecture, then bounded by the image config's declared `lockfile_platforms` expanded to OS FAMILIES so musl/baseline variants survive — passed to mise as explicit repeated `--platform` flags."
evidence = "python/src/dotfiles_setup/image_lock.py:104-114 (lock_platforms), :117-159 (platforms_to_lock, incl. the family split at :157-158 and the measured variant loss 'hk 6 -> 2, uv 8 -> 2' at :155), :419-430 (the default path); the declaration it reads is mise's own setting at .devcontainer/mise-system.toml:319 (`lockfile_platforms = [\"linux-x64\", \"linux-arm64\"]`), parsed by regex at python/src/dotfiles_setup/platform_target.py:255."
native_alternative = "mise's `lockfile_platforms` setting, which mise treats as AUTHORITATIVE for lockfile operations — `determine_existing_platforms` short-circuits on it before ever reading the lockfile's existing keys — so `mise lock` with no `--platform` already targets exactly the declared set plus the current platform."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/lockfile.rs:2551-2562 ('If lockfile_platforms setting is configured, use it as the authoritative set') and :2519-2529 ('The `lockfile_platforms` setting overrides both paths'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:1574-1596, whose docs also state the limit: line 1584 'Does NOT override explicit `mise lock --platform` flags', and line 1592 'The current platform is always included in addition to any configured platforms'."
rationale = "Justification, not a default: the native setting cannot replace this computation for two reasons that are both documented and both measured here. (1) It names EXACT platforms — `linux-x64`, `linux-arm64` — while the image genuinely needs the variant entries mise writes beneath them (`linux-x64-musl`, `linux-x64-baseline`, `linux-arm64-musl`); the module measured that an exact-name intersection clips those (hk 6 -> 2, uv 8 -> 2, image_lock.py:150-156), and the native no-`--platform` path resolves to the same exact-name set, so adopting it would reproduce the clipping and then be refused by the coverage check. (2) mise's authoritative set always adds `Platform::current()` (lockfile.rs:2558-2560), i.e. the RESOLVING host's platform, which is exactly the macOS contamination #698/2026-08-27 pruned out of these locks. The one native piece is already adopted — the bound is read from mise's own `lockfile_platforms` declaration rather than a second private list — which is the right seam. Worth noting for a future pass: platform_target.py:255 re-parses that setting with a regex instead of asking mise (`mise settings get lockfile_platforms`), which is a smaller, separable adoption in that unit."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_lock-4"
unit = "mod-image_lock"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "fetch_installer + install_pinned_mise: curl https://mise.run, assert the scheme is https, then pipe the bytes to `sh` with MISE_VERSION and MISE_INSTALL_PATH set, to place the image's exact mise version side-by-side in a stage directory."
evidence = "python/src/dotfiles_setup/image_lock.py:74-77 (PINNED_MISE_DIRNAME, MISE_INSTALLER_URL), :203-221 (fetch_installer, https assertion), :224-256 (install_pinned_mise, MISE_VERSION/MISE_INSTALL_PATH env), :438 (call site); the version comes from the Dockerfile ARG at .devcontainer/Dockerfile:115 via lock_refresh.stage_system_lock_dir (python/src/dotfiles_setup/lock_refresh.py:310)."
native_alternative = "This IS mise's documented native install mechanism — the install script's `MISE_VERSION` / `MISE_INSTALL_PATH` variables — and it is the same pair the Dockerfile itself uses. The nearest alternative, `mise self-update <version>`, replaces the RUNNING binary in place and cannot produce a side-by-side pinned copy."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/installing-mise.md:93 ('`MISE_VERSION=v2025.12.0` – install a specific version'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/continuous-integration.md:42-43 and :116 (`RUN curl https://mise.run | MISE_VERSION=v... MISE_INSTALL_PATH=/usr/local/bin/mise sh`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/self_update.rs:235-236 ('Update to a specific version') with :503-513 showing it retargets and replaces the current binary."
rationale = "The native mechanism is already the one in use; the only custom parts are the https-scheme assertion (the bytes are piped to `sh`, so an http:// or file:// override would be a code-execution primitive — image_lock.py:207-213) and the post-condition that the install path actually exists (:253-255, the build-time self-check pattern). Neither is provided by the installer, and the version pin is not optional because mise lock formats are not cross-version compatible (:232-236), so a `mise` already on PATH cannot be substituted."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_lock-5"
unit = "mod-image_lock"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "container_command / devcontainer_exec_prefix: build a `devcontainer exec --workspace-folder <root> --id-label ... -- mise exec -- uv run --project python dotfiles-setup image-lock --no-container` argv to re-invoke this CLI inside the amd64 devcontainer, with recursion terminated by --no-container."
evidence = "python/src/dotfiles_setup/image_lock.py:323-338 (devcontainer_exec_prefix, id-label resolution via devcontainer_names.resolve_names), :341-378 (container_command, incl. the recorded failure of the generalised form), :396-400 (the routing call site); reused by python/src/dotfiles_setup/lock_shared.py:31-36."
native_alternative = "The @devcontainers/cli `exec` subcommand itself (pinned npm:@devcontainers/cli 0.89.0) — which is the mechanism being wrapped. Nothing in mise, hk, uv, gh, Claude Code or codex-cli offers 'run this command on a host of another OS/architecture'; mise's `--platform` targets which platform a lock DESCRIBES, not which host RESOLVES it, and that distinction is the whole reason this routing exists."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:1574-1596 (`lockfile_platforms` and `mise lock --platform` describe target platforms only; nothing about the resolving host) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:80-87 (`--platform`: 'Comma-separated list of platforms to target'); .claude/rules/do-not.md #3 mandates @devcontainers/cli over raw docker for exactly this lifecycle. Control arm for the negative: the same corpus DOES surface a resolving-host-sensitive behaviour (settings.toml:1591 'The current platform is always included'), so the grep can see host-related semantics when they exist."
rationale = "The native tool is already the one invoked; the custom surface is only the argv assembly (id-label resolution, the --no-container recursion terminator, and the `mise exec --` wrapper), and its shape is load-bearing rather than incidental — the docstring at :352-363 records that generalising it for lock_shared broke because `mise exec --` with MISE_IGNORED_CONFIG_PATHS cleared resolves the entire declared toolset and dies attesting host-only tools. A retire would need a native cross-architecture exec that does not exist at these pins. Note it is already factored so lock_shared reuses the prefix rather than copying it, which is the correct wrap of the native mechanism."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_lock-6"
unit = "mod-image_lock"
kind = "module"
disposition = "refactor"
confidence = 0.40
custom_thing = "image_lock_main reports success as a platform COUNT ('regenerated with mise v%s across %d platform(s), coverage verified against HEAD') and never says which tool versions moved; the only place that answers 'did anything actually advance?' is CI's separate pytest drift-check job."
evidence = "python/src/dotfiles_setup/image_lock.py:447-453 (the success log), :275 (`mise lock --bump` is run without --json); the out-of-band answer lives in python/verification/suites.toml:848 (refresh.yml's `id: drift-check` re-running five tests/test_lock_coverage.py cases) alongside the regen call at suites.toml:849 (`run: mise run lock-image -- --no-container`)."
native_alternative = "`mise lock --json`, which prints an array of {name, backend, lockfile, old_versions, new_versions} describing exactly the version-level changes a `--bump` produced, and is documented to print `[]` when only checksums/URLs were refreshed — i.e. the native, machine-readable form of 'did this regen advance anything?'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:100-112 ('Output version changes as JSON ... Prints an array of objects describing lockfile version changes: name, backend, lockfile, old_versions, new_versions ... plain `mise lock --json` typically prints `[]` while still updating the lockfile'); the sibling `--dry-run` at lock.rs:76-79 for the detect-without-writing case."
rationale = "Not a superseded capability so much as a native one left unclaimed: --bump's whole point (image_lock.py:264-273, five refresh-bot commits with zero version advances) is that a regen must be judged by whether versions moved, and mise will report that directly. Adding --json to the final converging pass would let the success line name the tools that advanced instead of a platform count, and would give the refresh bot's PR body real content without a second pytest run. Low confidence because the passes are also allowed to fail mid-run (only the last pass's output would be meaningful) and because CI's argv parity with .github/actions/lock-refresh/action.yml is deliberately enforced (image_lock.py:272-273, tests/test_image_lock.py:249) — so the flag would have to land in both call sites or be added outside the shared lock_command()."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_manifest-1"
unit = "mod-image_manifest"
kind = "module"
disposition = "keep"
confidence = 0.88
custom_thing = "resolve_arch_tag() hand-resolves a registry ref to the ONE real platform+digest behind it: it parses the raw OCI document, filters `unknown/unknown` attestation entries, reads through an index to its single real manifest entry, and falls back to `{{json .Image}}` + `{{.Manifest.Digest}}` for a bare manifest."
evidence = "python/src/dotfiles_setup/image_manifest.py:190-233 (resolve_arch_tag), :156-172 (_is_real / real_platform_entries), :133-139 (docker_inspector's three `imagetools inspect` reads)"
native_alternative = "docker buildx imagetools inspect — the tool already in the runner and already wrapped here. Checked for a native per-platform resolution flag that would make the hand-parsing unnecessary."
alternative_evidence = ".agent/kb/raw/docker-buildx-imagetools-inspect-help-0.37.0.md:1-20 — live probe on buildx v0.37.0 (host, 2026-09-09): `docker buildx imagetools inspect` accepts ONLY `--builder`, `-D/--debug`, `--format` and `--raw`. There is no `--platform` and no per-platform digest selector. Control arm: the same help output does list `--format` and `--raw`, the two flags image_manifest.py:136-138 uses, so the probe can print flags — the absence of `--platform` is real, not a blind probe."
rationale = "The native tool cannot answer the question the module asks. `--raw` returns the document and `--format` runs a Go template over it, but neither selects a platform, so 'which digest does this ref serve for linux/amd64, ignoring attestations' has to be computed by the caller. The module is already the thinnest possible wrapper over the most native surface available (it uses `--raw` and buildx's own `{{json .Image}}` rather than re-implementing manifest fetching), and it deliberately does NOT assert the buildx `--prefer-index` default it reads through (image_manifest.py:40-45, :99-101), which is exactly the `use-tool-builtins.md` posture of depending on the capability and not on a vendor default."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_manifest-2"
unit = "mod-image_manifest"
kind = "module"
disposition = "refactor"
confidence = 0.82
custom_thing = "_inspect() shells out to `docker buildx imagetools inspect` with `capture_output=True, check=True` and NO `timeout=`, so a wedged registry read (hung TLS, a stalled auth challenge) blocks the CI step until the 20-minute job timeout kills it with no diagnostic. The CalledProcessError it raises on a docker failure is also not caught by the CLI handler, which only catches ValueError — so docker's own captured stderr never reaches the log."
evidence = "python/src/dotfiles_setup/image_manifest.py:124-130 (`subprocess.run([...], capture_output=True, text=True, check=True)` — no timeout); python/src/dotfiles_setup/image.py:2272-2274 (`except ValueError` only); .github/workflows/build-publish.yml:1172 (`timeout-minutes: 20` is the only bound that exists today)"
native_alternative = "stdlib `subprocess.run(..., timeout=<secs>)` — already the established pattern in this package for every other outbound subprocess."
alternative_evidence = "python/src/dotfiles_setup/apt_pins.py:228 (`timeout=_PROBE_TIMEOUT_S`), python/src/dotfiles_setup/audit.py:494 (`timeout=SSH_ADD_TIMEOUT_SECONDS`), python/src/dotfiles_setup/autofix.py:63 (`timeout=_DOWNLOAD_TIMEOUT_S`), python/src/dotfiles_setup/branch_guard.py:96,150 (`timeout=5`); policy: .claude/rules/long-running-command-hangs.md rule 2 ('for any command expected to exceed ~30s, never wait blind') and rule 4 ('a stalled process is a hang')."
rationale = "Three network round-trips per architecture run unbounded inside a gate that cannot be run locally, so a hang here is a 20-minute opaque CI failure. The fix is one keyword argument plus surfacing `exc.stderr` in the `except` in image.py — no new dependency, and it brings the module onto the pattern four other modules in the same package already use."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_manifest-3"
unit = "mod-image_manifest"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "Breaches are reported as hand-formatted `FAIL: ...` strings raised as ValueError and written to plain stderr by the CLI handler, so on a real AC2 failure the operator has to open the raw job log to see which architecture broke and which two digests disagreed."
evidence = "python/src/dotfiles_setup/image_manifest.py:182-187, :252-263, :299, :309-320, :324 (the `FAIL: ...` message construction); python/src/dotfiles_setup/image.py:2275-2277 (`sys.stderr.write(f\"{exc}\\n\")`)"
native_alternative = "GitHub Actions error annotations — a step printing `::error title={title}::{message}` renders the failure as an annotation at the top of the workflow-run page and on the job, not only in the raw log."
alternative_evidence = ".agent/kb/raw/gha-workflow-commands-error-annotation.md:9-16 (verbatim syntax `::error file={name},line={line},endLine={endLine},title={title}::{message}`, only the message required), saved from https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-commands. Repo state: zero `::error`/`::warning`/`::notice` occurrences in `.github/workflows/*.yml` or `python/src/dotfiles_setup/*.py`; control arm on the same command shape — `GITHUB_STEP_SUMMARY` returns hits at build-publish.yml:126,192,341,1339 — so the 0 is a real absence, not a broken grep."
rationale = "The success path already uses the platform's native reporting surface (`$GITHUB_STEP_SUMMARY`, build-publish.yml:1338-1343); only the failure path — the one anyone actually needs to read fast — does not. Prefixing the single stderr write in image.py with `::error title=AC2 (#703)::` is a one-line adoption of a native feature, costs nothing locally (the prefix is inert outside Actions), and does not change the exit code the gate depends on. Not a 'retire': the annotation is a rendering channel, it does not compute any of the four assertions."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_manifest-4"
unit = "mod-image_manifest"
kind = "module"
disposition = "refactor"
confidence = 0.68
custom_thing = "The AC1 workflow step re-implements, in 21 lines of inline bash + jq, the index-membership assertion that verify_arch_tags() already makes in python three steps later: it mapfiles `.manifests[].platform`, filters `unknown`, sorts unique, and string-compares the set against the matrix's arch set."
evidence = ".github/workflows/build-publish.yml:1267-1288 (the AC1 step body); the same assertion in python: image_manifest.py:284-300 (rejects any real index entry outside the expected {linux/<arch>} set) and :252-263 via _index_entry_digest (requires exactly one linux/<arch> entry per matrix target — the other half of set equality). The python pair is strictly stronger: AC1's arch-only comparison would pass a `windows/amd64` entry, which image_manifest.py:81 EXPECTED_OS rejects."
native_alternative = "The existing `dotfiles-setup image verify-arch-tags` CLI already invoked by the very next step — an existing tool in this repo rather than a second hand-rolled implementation."
alternative_evidence = ".github/workflows/build-publish.yml:1334-1337 (`uv run --project python dotfiles-setup image verify-arch-tags --image-ref ... --matrix ...`); policy: .claude/rules/zero-bash-logic.md ('non-trivial logic ... lives in python/; bash is restricted to thin check/smoke wrappers') and .claude/rules/use-tool-builtins.md ('prefer an existing tool ... over ANY homegrown code')."
rationale = "Two implementations of one assertion is exactly the drift surface the zero-bash-logic rule exists to remove, and the bash one is the weaker of the two (arch-only, no OS check, no attestation-vs-real distinction beyond a string filter). AC1 could be folded into the verifier's report lines. Held at refactor rather than retire because deleting a green CI assertion needs the suites.toml contract token 'Assert the index lists every published architecture (AC1)' (python/verification/suites.toml:800) updated in the same change, and because the AC1 step is what prints the informational `index architectures:` line — the merge should preserve that output, not just drop it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_manifest-5"
unit = "mod-image_manifest"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The module as a whole: the four AC2 conditions (exactly one real platform per per-architecture tag; that platform equals linux/<tag suffix>; its digest equals the digest the multi-arch index lists for that arch; no two index entries share a digest), plus attestation exclusion, plus reporting the observed document shape instead of asserting it, plus the injected Inspector seam that gives the gate a FAIL arm at all."
evidence = "python/src/dotfiles_setup/image_manifest.py:28-51 (the stated contract), :267-326 (verify_arch_tags), :74-81 (UNKNOWN / EXPECTED_OS), :109-122 (Inspector); tests/test_image_manifest.py:1-28 (four test layers incl. eight FAIL arms and the pinned pre-#703 defect), :419-436 (the workflow-wiring tests that strip shell comments so prose cannot satisfy them)"
native_alternative = "Surveyed and rejected as non-equivalent: `docker buildx imagetools create` (writes the index, asserts nothing about it), `gh attestation verify` / `actions/attest-build-provenance` (verify provenance signatures, not that a `:<sha>-<arch>` tag and the `:<sha>` index name the same manifest), zizmor/ghalint/actionlint (lint workflow YAML, not registry contents), and pytest fake-subprocess plugins (would replace the Inspector seam with a coarser one that stubs `docker` rather than the three semantic reads)."
alternative_evidence = ".agent/kb/raw/docker-buildx-imagetools-inspect-help-0.37.0.md:1-20 (buildx has no per-platform resolution); no gap digest under .agent/kb/raw/ covers docker/buildx or GitHub Actions registry tooling — the ten digests present are claude-code, codex-cli, mise, hk, uv, renovate, graphify, planning-with-files and fable-orchestrator, none of which touch this unit's tools."
rationale = "Required justification: no tool in the pin set publishes an assertion of the form 'this per-architecture tag and this index entry are the same manifest'. That relation is a property of THIS repo's split-push-then-merge design (build-publish.yml:1150-1247), which no upstream product models. The module is also the only place the gate's FAIL direction can exist — the step reads a registry only CI writes to (tests/test_image_manifest.py:3-6) — so retiring the python layer would return the project to the pre-#703 state where a check that could only fail shipped green-adjacent for weeks. Findings 2, 3 and 4 are the improvements; the unit itself stays."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-image_manifest-6"
unit = "mod-image_manifest"
kind = "module"
disposition = "unknown"
confidence = 0.35
custom_thing = "The ~45 lines of OCI-document walking in resolve_arch_tag()/real_platform_entries() that turn a ref into one platform digest."
evidence = "python/src/dotfiles_setup/image_manifest.py:156-233"
native_alternative = "A dedicated registry client CLI — regclient's `regctl` (`regctl image digest --platform linux/amd64 <ref>`) or google/go-containerregistry's `crane` (`crane digest --platform linux/amd64 <ref>`) — either of which resolves a ref to a single platform's manifest digest in one call, which is precisely the primitive buildx lacks (finding 1)."
alternative_evidence = "NONE HELD. Neither tool is in the repo pin set (mise.toml / .config/mise/conf.d/shared.toml), neither has a source tree in the offline corpus, and no saved page under .agent/kb/raw/ documents the flag — so per hard rule 4 this stays 'unknown' rather than being asserted from memory."
rationale = "Worth settling because it is the one credible route to deleting the document-walking, but it must be measured before it is believed, on two axes: (a) does the flag actually exist and does it exclude `unknown/unknown` attestation descriptors, and (b) would adopting it be a net win given it adds a tool to the pin set, still leaves the shared-digest and unexpected-platform assertions in python, and loses the shape reporting the module deliberately emits. My prior is that the answer is 'no, keep the python' — the module is not primarily a resolver, it is four assertions — but the probe is cheap."
probe = "mise x aqua:regclient/regclient@latest -- regctl image digest --platform linux/amd64 ghcr.io/ray-manaloto/dotfiles-devcontainer:dev; echo \"rc=$?\"  # then the control arm, which MUST fail: mise x aqua:regclient/regclient@latest -- regctl image digest --platform linux/riscv64 ghcr.io/ray-manaloto/dotfiles-devcontainer:dev; echo \"rc=$?\"  # and compare both against: docker buildx imagetools inspect ghcr.io/ray-manaloto/dotfiles-devcontainer:dev --raw | jq -r '.manifests[] | select(.platform.os==\"linux\" and .platform.architecture==\"amd64\") | .digest'"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_observer-1"
unit = "mod-instructions_observer"
kind = "module"
disposition = "keep"
confidence = 0.92
custom_thing = "The whole observation channel: a per-load JSONL recorder for the InstructionsLoaded payload (ts + session_id, memory_type, load_reason, agent_id, agent_type, file_path, trigger_file_path, parent_file_path, globs), written one record per instruction-file load into .agent/instructions-loaded/<session_id>.jsonl, so `instructions-report` can partition eager / fired / never-fired rules across REAL sessions."
evidence = "python/src/dotfiles_setup/instructions_observer.py:2-32 (module contract), :79-86 (_STRING_FIELDS/_PATH_FIELDS), :180-194 (build_record), :238-282 (_write_record), :285-315 (observe_main); wired at .claude/settings.json:119-127; asserted end-to-end by python/verification/suites.toml:1410-1426; consumed by mise.toml:828-842."
native_alternative = "Claude Code's own InstructionsLoaded hook event is the native mechanism, and this module is already its consumer. No native feature RECORDS the loads for you: the OpenTelemetry events surface has no instructions event (enumerated: 26 `**Event Name**` entries, user_prompt through retention_sweep), and `/context` + `/doctor`'s CLAUDE.md trim check + 2.1.261's `/skill-doctor` are interactive, single-session, and say nothing about which path-scoped rule ever fired."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:447 (\"Use the InstructionsLoaded hook to log exactly which instruction files are loaded, when they load, and why\"); hooks.md:1261-1294 (event, payload fields, \"Use this event for audit logging, compliance tracking, or observability\"); monitoring-usage.md:588-1144 (full event list; control arm: `claude_code.skill_activated` at :944 IS present, so the enumeration discriminates — there is simply no instructions-load event); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (2.1.258-2.1.266 adds `/skill-doctor` for SKILLS only, nothing for rules)."
rationale = "Required justification: the harness emits the event but persists nothing. Its own docs point at exactly this hook for this job, the OTel surface (the only machine-readable native channel) has no instructions event, and every native inspector (/context, /doctor, /skill-doctor) is interactive and single-session, so none of them can answer 'which rule never fired across N sessions'. The module already adopts the native seams it can: the documented payload field names verbatim, and $CLAUDE_PROJECT_DIR (instructions_observer.py:89-105) instead of cwd."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_observer-2"
unit = "mod-instructions_observer"
kind = "module"
disposition = "refactor"
confidence = 0.62
custom_thing = "The hot-path invocation wrapper: the hook shells `uv run --project \"${CLAUDE_PROJECT_DIR:-.}/python\" python -m dotfiles_setup.instructions_observer` once per instruction-file load (~37 eligible files per session start, again per subagent), so every load pays uv's environment-freshness check on top of the interpreter start the module's C1 budget was written to protect."
evidence = ".claude/settings.json:124 (the wired command); python/src/dotfiles_setup/instructions_observer.py:13-22 (C1 hot-path budget: 0.04s stdlib-only interpreter vs 0.23-0.27s importing dotfiles_setup — the module optimizes the import half while the uv wrapper's cost is unmeasured); python/src/dotfiles_setup/hook_selfcheck.py:110-113 (the wiring token asserted on every ship/land)."
native_alternative = "uv's native `uv run --no-sync` (run without checking that the environment is up to date), or `--frozen` to also skip locking."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/docs/concepts/projects/sync.md:28-32 (\"to run a command without checking if the environment is up-to-date, use the `--no-sync` option\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/docs/guides/package.md:118 (`--frozen` prevents locking and syncing, `--no-sync` just syncing). Live probe on this host, 3 runs per arm through the real hook command with a real InstructionsLoaded payload: plain `uv run` avg 0.086s vs `uv run --no-sync` avg 0.066s (~20ms, ~23% per load; ~0.7s per 37-file session start, multiplied again per subagent). Both arms wrote a well-formed record (positive control), so the probe measured the working path, not a failure."
rationale = "Native covers part of the cost the module already treats as load-bearing: the module fights a 0.19s import tax while the wrapper spends ~20ms per load re-verifying a venv that a hook can neither fix nor report on (exit code is ignored). Adding `--no-sync` to .claude/settings.json:124 adopts the native flag for exactly that. It is a refactor, not a retire: the trade-off is that a genuinely stale venv stops being repaired by this hook — acceptable here because the observer is fail-open by contract and every other entrypoint (mise tasks, pytest) still syncs. Confidence is mid because the win is small and the change touches settings.json wiring that hook_selfcheck and suites.toml both bind (the asserted token is the `python -m ...` fragment, so it survives the flag)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_observer-3"
unit = "mod-instructions_observer"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Recording EVERY load and doing the eager/lazy partition later in Python (`load_reason` is copied through as a plain string field and the report classifies it), rather than filtering at the hook level."
evidence = "python/src/dotfiles_setup/instructions_observer.py:79-85 (`load_reason` copied verbatim), :188-194 (build_record makes no classification); .claude/settings.json:119-127 (no `matcher` key on the InstructionsLoaded entry)."
native_alternative = "Claude Code's native hook `matcher`, which for InstructionsLoaded runs against `load_reason` (e.g. `\"matcher\": \"session_start\"`, or `\"path_glob_match|nested_traversal\"`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1265 (\"The matcher runs against `load_reason`...\"); the five legal values at hooks.md:323 / :1279."
rationale = "Required justification: the native matcher exists and was already evaluated and deliberately rejected in-tree — the never-fired report needs the complete baseline, so every load_reason is wanted and a matcher would only subtract records (python/src/dotfiles_setup/hook_selfcheck.py:105-108 records exactly that reasoning, with `None` matchers asserted as intentional). The matcher also cannot express the one filter that would cut work (scope: `memory_type` User/Managed), because it binds `load_reason` only. Nothing to change; this finding exists so a later audit does not re-propose the matcher."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_observer-4"
unit = "mod-instructions_observer"
kind = "module"
disposition = "keep"
confidence = 0.78
custom_thing = "The fail-open machinery: swallow every exception, always `return 0`, never write to stdout/stderr, and append the reason to a sibling `.agent/instructions-loaded/errors.log` with an OS-tempdir fallback (O_NOFOLLOW, 0600) — plus the drop-with-a-logged-reason paths for a containment miss, an oversize record, and a short write."
evidence = "python/src/dotfiles_setup/instructions_observer.py:24-31 (the C2 contract), :197-235 (_log_error + fallback), :251-282 (drop paths), :296-315 (catch-all, returns 0)."
native_alternative = "Partially native already: the harness ignores this event's exit code and gives it no decision control, so the `return 0` half is belt-and-braces rather than load-bearing. Nothing native replaces the error CHANNEL — hooks' own OTel events (`claude_code.hook_execution_complete`) report only counts (`num_non_blocking_error`, `total_duration_ms`), never why a record was dropped, and require an OTLP exporter this repo does not configure."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:885 (\"InstructionsLoaded | No | Exit code is ignored\"); hooks.md:1025 (\"Setup, ... InstructionsLoaded, ... | None | No decision control. Used for side effects like logging or cleanup\"); hooks.md:1294 (\"Claude Code discards their JSON output fields\"); monitoring-usage.md:1037-1060 (hook_execution_complete attributes — counts only, no per-hook diagnostic text)."
rationale = "Required justification: the only native part is the guarantee that a non-zero exit cannot hurt, which the module does not depend on anyway; the parts that matter — silent stdout/stderr and a durable record of WHY a line was dropped — have no native counterpart at these pins, and the nearest one (OTel hook_execution_complete) is count-only and needs an exporter that is not configured here. Deleting any of it would trade a real diagnostic for nothing. No change."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_observer-5"
unit = "mod-instructions_observer"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "One OS process per instruction load as the transport for the record (command-type hook writing the JSONL itself)."
evidence = ".claude/settings.json:121-126 (`\"type\": \"command\"`, timeout 10); python/src/dotfiles_setup/instructions_observer.py:13-22 (the per-file spawn cost this design accepts)."
native_alternative = "Claude Code natively supports non-command hook types — notably `\"type\": \"http\"`, which POSTs the same payload to an endpoint (documented status/body contract) and would collapse ~37+ spawns per session into HTTP calls; `hook_type` values are `command`, `prompt`, `mcp_tool`, `http`, `agent`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:529 (`\"type\": \"http\"` example), hooks.md:890-905 (HTTP response handling contract); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/monitoring-usage.md:1007 (`hook_type`: \"command\", \"prompt\", \"mcp_tool\", \"http\", \"agent\")."
rationale = "Required justification: the native http hook type exists and is genuinely cheaper per load, but it requires a resident local collector — a new always-on process, a port, a lifecycle, and a failure mode (connection refused = silently no records) strictly worse than the current fail-open spawn, for a measured saving of ~66-86ms per load on a hook the harness runs asynchronously. Trade is not worth it at this repo's scale; revisit only if the observer is ever measured to delay session start. The cheap win is finding -2 (`--no-sync`), not a transport change."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_report-1"
unit = "mod-instructions_report"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "`scoped_rules_on_disk()` — a private frontmatter parser (`_FRONTMATTER_RE` + `yaml.safe_load`) that walks `.claude/rules/**.md` and returns the repo-relative paths of every rule carrying a list-valued `paths:` key."
evidence = "python/src/dotfiles_setup/instructions_report.py:59 (`_FRONTMATTER_RE = re.compile(r\"\\A---\\n(.*?)\\n---\\n?\", re.DOTALL)`); :110-147 (`def scoped_rules_on_disk`, rglob + yaml.safe_load + `isinstance(front.get(\"paths\"), list)`); :289 (its only caller, `run_report`). Contract pins the symbol: python/verification/suites.toml:1426 (`per_path_tokens` … \"def scoped_rules_on_disk(\")."
native_alternative = "The in-repo single-source-of-truth rule parser shipped later by #918: `rule_registry.build_registry(rules_dir).by_load_class(\"scoped\")`, plus the shared knowledge-base engine `kb_setup.md_budget.has_paths_frontmatter` (already a pinned dependency of this repo — `kb-setup` git pin c70f0f8). `rule_registry` deliberately pins its traversal and regex to match this function byte-for-byte, and additionally surfaces unparsable rules as `load_class == \"malformed\"` instead of silently dropping them."
alternative_evidence = "IN-REPO (not a corpus doc — no external tool provides this): python/src/dotfiles_setup/rule_registry.py:11-20 (\"This is deliberately NOT the source of truth for the md-size-budget gate. That gate runs a THIRD, independent load-class classifier (`kb_setup.md_budget.has_paths_frontmatter`, in the knowledge-base repo) which already disagrees with both this registry and `instructions_report.scoped_rules_on_disk` on some frontmatter shapes … Reconciling the three is tracked by **issue #951**\"); :22-27 (\"that function is a SECOND parser of the same frontmatter, and #917 shipped three independent bugs that coexisted precisely because nothing asserted the two sides agreed\"); :54-58 (`_FRONTMATTER_RE` \"Matches `instructions_report._FRONTMATTER_RE` exactly (C1)\"); :182 (`def by_load_class`); :363 (`def build_registry`). Supporting external gate: agnix 0.52.1 (`mise run lint-docs`) already validates the same frontmatter — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/locales/en.yml:327 (CC-MEM-011 invalid `paths` glob) and :331 (CC-MEM-012 unknown frontmatter key), recursively since /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/CHANGELOG.md:401."
rationale = "Three parsers of one frontmatter shape exist in this tree and are documented to already disagree (#951). `rule_registry` is the newer, richer, explicitly-named source of truth and its own docstring names `scoped_rules_on_disk` as the redundant sibling it had to pin itself against. The refactor is to have `run_report` consume `build_registry(rules_dir).by_load_class(\"scoped\")` and delete this module's private regex/YAML pair, which also removes the silent-drop behaviour (`except yaml.YAMLError: continue`, :143-144) that makes a malformed rule invisible in every bucket. Not 'retire' for the module — only this one function is superseded, and the consolidation is a live ticket rather than a settled decision, so the ordering is #951's to make."
probe = "uv run --project python python -c \"from pathlib import Path; from dotfiles_setup import instructions_report as r, rule_registry as rr; d=Path('.claude/rules'); a=set(r.scoped_rules_on_disk(d)); b={x.path for x in rr.build_registry(d).by_load_class('scoped')}; print(sorted(a^b) or 'AGREE')\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_report-2"
unit = "mod-instructions_report"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The `eager` bucket — aggregating `load_reason == \"session_start\"` records into the set of instruction files that load unconditionally, rendered by `_render` as \"eager (session_start): N\"."
evidence = "python/src/dotfiles_setup/instructions_report.py:241-251 (`if reason == \"session_start\": … eager.add(file_path)`); :337-339 (render); :7-8 (docstring: \"**eager** — files that loaded at ``session_start`` (the unscoped corpus …)\")."
native_alternative = "`/context` natively lists exactly which memory/instruction files loaded into the live session, with per-file token cost; the same payload is available programmatically from the Agent SDK as `getContextUsage()` → `memoryFiles: { path, type, tokens }[]`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:421 (\"To check which files actually loaded into the current session, run `/context`.\"); memory.md:69 (\"To confirm the file loaded, run `/context` … check the list under **Memory files**\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__typescript.md:713 (`memoryFiles: { path: string; type: string; tokens: number; }[]`) and :790 (\"`memoryFiles` lists each loaded memory file with its cost\")."
rationale = "Justification required, so stated explicitly: the native surface answers a strictly narrower question. `/context` / `getContextUsage()` is a snapshot of ONE live session, reports no `load_reason` (so it cannot separate `session_start` from `path_glob_match`/`include`/`compact`, which is the whole partition this module exists for), and is reachable only from inside a running interactive session or an SDK query — not from a batch `mise run instructions-report` over historical JSONL. What the native side has and the custom side lacks is the per-file TOKEN COST; that is the one thing worth borrowing (report the `tokens` field alongside the eager list if a future ticket wires an SDK read), which is why this is not a plain 'nothing exists'."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_report-3"
unit = "mod-instructions_report"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The `never_fired` bucket + its `_MIN_SESSIONS_FOR_NEVER_FIRED = 3` distinct-session sufficiency gate — detecting a scoped rule whose `paths:` glob is dead (present on disk, never observed loading by ANY reason), and refusing to print the absence claim until coverage supports it."
evidence = "python/src/dotfiles_setup/instructions_report.py:16-20 (docstring: \"A rule scoped with a glob that matches nothing is, on disk, indistinguishable from one loading fine\"); :61-78 (`_MIN_SESSIONS_FOR_NEVER_FIRED`, and why counting EVENTS was wrong); :258-259; :312-324 (`_json_payload`: `never_fired` becomes `null`, never `[]`); :347-360 (the NOT SHOWN branch)."
native_alternative = "None for rules. The closest native feature is `/skill-doctor` (Claude Code 2.1.261), which does exactly this class of analysis for the SKILLS corpus — which loaded skills go unused and what they cost. No equivalent exists for `.claude/rules/`, and agnix (the repo's doc linter, pinned 0.52.1) validates `paths:` glob SYNTAX but has no dead-glob / never-matched rule."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.md:71 (\"Added `/skill-doctor` to show which loaded skills go unused and what they cost in context, so you can prune them\") — also in .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md under version 2.1.261. Absence of a rules equivalent, control-armed: `grep -rn \"matches no|no files match|never match|unused rule\" /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/locales/en.yml` → 0 hits, while the SAME command shape over the SAME file returns cc_mem_011/cc_mem_012 at en.yml:327 and :331, so the probe discriminates. `/skill-doctor` likewise returns 0 hits across the offline claude-code doc tree (it postdates the 2.1.257 snapshot), which is why the changelog digest is the citation."
rationale = "Justification required: the dead-glob question is only answerable from OBSERVED loads accumulated across sessions, and nothing in the harness persists or aggregates InstructionsLoaded events — the event is explicitly documented as fire-and-forget observability with the exit code ignored, i.e. the storage and the query are the integrator's job. `/skill-doctor` proves Anthropic ships this analysis natively for skills, which makes a rules equivalent a plausible future supersession — hence the probe, to be re-run at each Claude Code bump rather than assumed absent forever."
probe = "claude --help 2>&1 | grep -i 'doctor\\|rules'; and in an interactive session: type `/` and check for a rules/instructions analogue of `/skill-doctor`; cross-verify against the next changelog fetch: curl -s https://code.claude.com/docs/en/changelog.md | grep -in 'rule-doctor\\|instructions-doctor\\|unused rule'"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-instructions_report-4"
unit = "mod-instructions_report"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "The whole read/aggregate/report pipeline over the observer's JSONL: `_iter_records` (per-line tolerant parse that COUNTS rather than silently skips malformed lines), `_count_errors_log_lines`, `build_report`'s reason-blind membership partition and distinct-`session_id` counting via `usable_session_id`, `run_report`'s `dataclasses.replace` fill-in, and the dual human/`--json` renderers behind `dotfiles-setup instructions-report` / `mise run instructions-report`."
evidence = "python/src/dotfiles_setup/instructions_report.py:150-187 (`_iter_records`, `_count_errors_log_lines`); :190-274 (`build_report`); :277-309 (`run_report`); :327-363 (`_render`); :366-393 (argparse + `instructions_report_main`). Wiring: mise.toml:828-842 (`[tasks.instructions-report]` → `uv run --project python dotfiles-setup instructions-report`); python/src/dotfiles_setup/main.py:81, :447-472, :653, :2407-2408 (subparser + dispatch); python/verification/suites.toml:1411-1426 (the end-to-end wiring contract). Tests: tests/test_instructions_report.py (719 lines)."
native_alternative = "None. Claude Code emits the `InstructionsLoaded` event and nothing else — no store, no query, no aggregation surface; the docs assign audit logging/compliance/observability to the hook consumer. mise contributes only the task wrapper (already used); hk, uv, gh, renovate, graphify, pytest plugins and every doc linter surveyed (agnix, cclint, agents-lint, claude-code-lint, ast-grep, zizmor, ghalint) operate on files at rest, not on recorded session events."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1294 (\"InstructionsLoaded hooks have no decision control … Use this event for audit logging, compliance tracking, or observability.\"); hooks.md:885 (`InstructionsLoaded` | No | Exit code is ignored); hooks.md:1025 (\"No decision control. Used for side effects like logging or cleanup\"); hooks.md:1269-1281 (the input field table — `file_path`, `memory_type`, `load_reason`, `globs`, `trigger_file_path`, `parent_file_path` — i.e. the raw event, with no persistence or reporting API alongside it); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__hooks.md:176 (\"`InstructionsLoaded` … Audit which instruction files load\" — the stated use case, delegated to the integrator)."
rationale = "Justification required: the harness hands out one event per load and explicitly stops there, so anything that answers 'across N sessions, which rules fired / never fired' has to be built. The custom code is also carrying two judgments no generic tool would make — malformed lines are COUNTED not dropped (:150-158), and the absence claim is coverage-gated by DISTINCT SESSIONS rather than events (:61-78, a documented false-positive this module already tripped over). The one deliberate duplication (`_RECORDS_DIRNAME`/`_ERROR_LOG_NAME` copied rather than imported from `instructions_observer`, :51-56) is justified in place by the observer's stdlib-only hot-path contract C1 and should stay; it is a different case from finding 1, where the duplicate carries actual parsing LOGIC."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lint-1"
unit = "mod-lint"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "In-process hard timeout around `hk run check --all`, with the child spawned into a fresh session (`start_new_session=True`) and the whole process GROUP killed TERM -> 10s grace -> KILL on expiry."
evidence = "python/src/dotfiles_setup/lint.py:213-224 (`_terminate_group`: `os.killpg(pgid, SIGTERM)`, `proc.wait(timeout=KILL_GRACE_SECONDS)`, `os.killpg(pgid, SIGKILL)`); python/src/dotfiles_setup/lint.py:281-285 (`subprocess.Popen(command, start_new_session=True, ...)` + `proc.wait(timeout=timeout)`); module docstring lint.py:10-23 states hk has no native timeout, verified against hk 1.46/1.48."
native_alternative = "mise's per-task `timeout` option (already wired as a 700s outer backstop at mise.toml:213), enforced by mise's `TimeoutGuard`, which SIGTERMs the child's process TREE, waits a 5s grace, then SIGKILLs it. Still true at hk 1.57.0: `hk check` exposes no `--timeout`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060-1078 (`timeout` task option, `mise run --timeout`, `task.timeout` global, shorter-of-two wins); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cmd.rs:200-207 (`signal_process_tree(pid, SIGTERM)`, 5s grace deadline, `signal_process_tree(pid, SIGKILL)`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/task/task_executor.rs:1813-1824 (`effective_timeout` -> `cmd.with_timeout`). hk absence control-armed: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/check.md:25-46 lists every flag (`--fail-fast`, `--sarif`, `--unstaged`, ...) and no `--timeout`; the corpus tree is exactly the pinned version (Cargo.toml:23 `version = \"1.57.0\"`), and .agent/kb/raw/hk-releases-since-pin.digest.md records v1.58.1 as documentation-only with no runtime change."
rationale = "The ENFORCEMENT half of this capability is now natively covered and already half-adopted (mise.toml:207-213 documents the 700s backstop and the decision to keep the wrapper). Two things stop it being a clean 'retire'. (a) mise's tree kill is CONDITIONAL: `should_use_pgroup()` returns false when mise is the session leader or `TASK_PGID_MANAGED_ENV` is set, in which case `signal_process_tree` degrades to a single-PID `kill(pid)` and hk's grandchildren (ruff, pkl, docker) leak — lint.py's `start_new_session=True` + `killpg` is unconditional. (b) mise reports only `timed out after {duration}` with no hk-debug-log tail and no 124. The refactor is therefore to shrink lint.py to a diagnostics wrapper (set HK_LOG_FILE, tail on failure) and let mise own the ceiling — NOT to keep two independent ceilings (600s in-process, 700s task) that can silently disagree."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && printf '[tasks.probe-wedge]\\ntimeout = \"5s\"\\nrun = \"bash -c \\\"sleep 300 & wait\\\"\"\\n' >> mise.local.toml && mise run probe-wedge; echo \"rc=$?\"; pgrep -fl 'sleep 300'   # two arms: does mise return non-zero at ~5s, and is the GRANDCHILD sleep reaped or orphaned?"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-lint-2"
unit = "mod-lint"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "A bespoke three-tier timeout precedence chain: `--timeout` CLI flag > `DOTFILES_LINT_TIMEOUT` env var > 600s default, with its own positive-integer validation."
evidence = "python/src/dotfiles_setup/lint.py:59-62 (`DEFAULT_TIMEOUT_SECONDS = 600`, `TIMEOUT_ENV_VAR = \"DOTFILES_LINT_TIMEOUT\"`), lint.py:189-210 (`resolve_timeout`); python/src/dotfiles_setup/main.py:1690-1696 (the `--timeout` argparse flag) and main.py:2346-2352 (`_lint` calls `resolve_timeout(args.timeout)`)."
native_alternative = "mise ships the identical three-tier chain for task timeouts: the `mise run --timeout <dur>` CLI flag, the `MISE_TASK_TIMEOUT` env var behind the `task.timeout` setting, and a per-task `timeout` default in mise.toml — with documented precedence (CLI flag overrides the global setting; shorter of global vs per-task wins)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/run.md:124-125 (`--timeout <TIMEOUT>` — Timeout for the task to complete, e.g. 30s, 5m); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:3204-3210 (`env = \"MISE_TASK_TIMEOUT\"`, type Duration, and the shorter-of-two precedence prose); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1074-1078."
rationale = "This is a duplicated configuration surface, not a missing capability: two `--timeout` flags and two env vars with the same meaning, on the same command path, whose values can disagree (today 600s in-process vs 700s task-level). mise's version additionally accepts durations (`10m`) rather than bare seconds. The refactor is to make `DOTFILES_LINT_TIMEOUT`/`--timeout` either a thin alias that mise's chain resolves, or to drop them and document `mise run --timeout 20m lint` / `MISE_TASK_TIMEOUT`. Not 'retire' outright because `.claude/rules/long-running-command-hangs.md` and the direct `uv run --project python dotfiles-setup lint` entrypoint are documented, non-mise call paths where mise's chain does not apply — dropping the module's own ceiling would leave that path unbounded."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && MISE_TASK_TIMEOUT=5s mise run lint; echo \"rc=$?\"   # control arm: does the global env setting actually bind a task that already declares timeout = \"700s\" (docs say shorter wins), or is the per-task value sticky?"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-lint-3"
unit = "mod-lint"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Log-file lifecycle management around hk: a per-workspace+per-pid log path, an atomically-repointed stable `hk-lint-<hash>.log` symlink, 24h age-floor pruning of stale per-run logs and orphaned tmp links, and best-effort removal of the pre-#895 fixed `hk-lint.log`."
evidence = "python/src/dotfiles_setup/lint.py:85-92 (`_per_run_log_path`, `_stable_log_path`), lint.py:95-116 (`_update_stable_symlink`, single `os.replace`), lint.py:118-167 (`_prune_stale`, `_prune_old_logs`), lint.py:170-186 (`_remove_legacy_log_file`), lint.py:273-277 (`HK_LOG_FILE` / `HK_LOG_FILE_LEVEL=debug` injected into the child env)."
native_alternative = "hk natively provides only the log-file LOCATION and LEVEL knobs this module already uses — `HK_LOG_FILE` (default `~/.local/state/hk/hk.log`) and `HK_LOG_FILE_LEVEL`. hk 1.57.0 has no per-run scoping, no rotation, and no retention/pruning of its log file."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/settings.toml:258-281 (`[log_file]` sources.env = [\"HK_LOG_FILE\"]; `[log_file_level]` sources.env = [\"HK_LOG_FILE_LEVEL\"]) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/environment_variables.md:162,169. Absence control-armed: `grep -n 'rotat|prune|cleanup|retention' docs/logging.md docs/environment_variables.md` -> 0 hits, while `grep -c 'HK_LOG' docs/environment_variables.md` -> 5 in the same corpus with the same command shape, so the zero is real. Corpus is the pinned 1.57.0 (Cargo.toml:23) and the gap digest records 1.58.1 as docs-only."
rationale = "Required justification: the only native piece is the pair of env vars, and the module already consumes them. The concurrency-safety property this code exists for (two clones or two windows on one host must not overwrite each other's debug log, while `long-running-command-hangs.md` rule 2 still has one predictable name to read) is a property of the CALLER choosing distinct HK_LOG_FILE values — hk cannot provide it, because hk sees exactly one path per invocation. No rotation or retention exists in hk to delegate the pruning to, and mise offers nothing here either (its task `env` is static/Tera-templated and cannot inject the runtime pid)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lint-4"
unit = "mod-lint"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "On-timeout diagnostics: print the last 40 lines of hk's own debug log, and return the GNU-`timeout` exit code 124 so the caller can distinguish a hang from a lint failure."
evidence = "python/src/dotfiles_setup/lint.py:61 (`TIMEOUT_EXIT_CODE = 124`), lint.py:226-235 (`_print_log_tail`), lint.py:288-300 (`_handle_timeout`: log the overrun, kill the group, tail the log, return 124)."
native_alternative = "None. hk's timed-out-run surface does not exist at all (no `--timeout`), and its new machine-readable diagnostics (`--format`, `--sarif`) describe finished STEP results, so they cannot speak to a wedged run. mise's timeout path emits only `timed out after {duration:?}` and surfaces no child log file; there is no documented per-task failure-output option to delegate the tail to."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/check.md:27 (`--format <FORMAT>`) and :36 (`--sarif <PATH>`) — the complete flag list, with no timeout/hang diagnostic; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cmd.rs:968-971 (`bail!(\"timed out after {duration:?}\")`, the entire timeout report). `grep -rn 'failure_output' docs/ schema/mise-task.json settings.toml` in the mise corpus -> 0 hits (the identifier exists only in src/cmd.rs, i.e. internal, not a task option); control arm: the same grep shape for `timeout` over those files returns hits (schema/mise-task.json:341, settings.toml:3208)."
rationale = "Required justification: this is the exact reason mise.toml:207-212 already records for retaining the wrapper under the native 700s backstop, and it re-verifies at the current pins. Without it a hang produces `timed out after 700s` and nothing about WHICH step wedged — which is the whole diagnostic content of the 7h incident in `.claude/rules/long-running-command-hangs.md`, and what rule 7 (`grep for a step with no matching completion`) depends on. The 124 convention likewise has no native producer: mise's timeout exits with its generic error code, so a caller cannot tell a hang from a red gate."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lint-5"
unit = "mod-lint"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "The hard-coded gate invocation `hk run check --all` (read-only, whole tree, for CI parity)."
evidence = "python/src/dotfiles_setup/lint.py:66 (`HK_COMMAND = (\"hk\", \"run\", \"check\", \"--all\")`) and the docstring rationale at lint.py:4-8."
native_alternative = "hk 1.57.0 added narrower selection flags that did not exist when this was written — notably `--unstaged` (\"Run on unstaged and untracked files only ... Useful for linting files an agent just changed\") and `--pr` (files changed on the current branch) — plus `--format`/`--sarif` for machine-readable output."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/check.md:46 (`--unstaged`), :34 (`--pr`), :27 (`--format`), :36 (`--sarif`); corpus at the pinned 1.57.0 (Cargo.toml:23)."
rationale = "Required justification: `--all` is load-bearing, not incidental — `.claude/rules/ci-local-parity.md` and the module docstring require `mise run lint` to check exactly what CI's `hk run check --all` checks, and a `--unstaged`/`--pr` narrowing would make the local gate weaker than the CI gate while still reporting green. The new flags are a genuine opportunity, but for a DIFFERENT, additional fast task (an agent-scoped pre-flight), never as a substitute inside this gate. Nothing here is superseded."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lint_delta-1"
unit = "mod-lint_delta"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "parse_ruff() hand-rolls the per-rule-code tally: it runs `ruff check --output-format json`, decodes the full per-diagnostic array, and reduces it to a Counter keyed on entry[\"code\"]."
evidence = "python/src/dotfiles_setup/lint_delta.py:72-82 (parse_ruff builds Counter over per-diagnostic JSON entries); python/src/dotfiles_setup/lint_delta.py:115-119 (TOOLS[\"ruff\"].argv = (\"check\", \"--output-format\", \"json\")); python/uv.lock:1744-1745 (ruff pinned 0.16.5)"
native_alternative = "`ruff check --statistics --output-format json` — ruff emits the per-rule aggregate directly as a JSON array of {code, name, count, fixable, fixable_count}, already sorted by descending count. That is exactly the Counter the module reconstructs, at a fraction of the payload (one object per RULE, not per diagnostic)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/docs/configuration.md:641 (`--statistics  Show counts for every rule with at least one violation`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff/src/printer.rs:384-386 (`OutputFormat::Json => writeln!(writer, \"{}\", serde_json::to_string_pretty(&statistics)?)`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff/src/printer.rs:36-47 (struct ExpandedStatistics { code, name, count, fixable, fixable_count }). Corpus ruff crate version is 0.16.5 (/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff/Cargo.toml:3), byte-identical to the repo pin, so the corpus is authoritative for the pinned tool."
rationale = "Only the ruff COUNTING half is superseded — the delta, the baseline derivation and the refusal are not (see mod-lint_delta-5). Adding `--statistics` to TOOLS[\"ruff\"].argv and reading `{s[\"code\"]: s[\"count\"]}` deletes the per-diagnostic reduction and stops the module carrying its own aggregation of ruff's data. Two real caveats keep this at 'refactor' rather than 'retire' of parse_ruff outright: (a) `code` is `Option<&SecondaryCode>` (printer.rs:37), so a null-code entry (e.g. a syntax error) still needs the existing isinstance filter; (b) the BASELINE arm runs an ARBITRARY older ruff via `uvx ruff@<version>` (lint_delta.py:235) and the corpus only proves the flag pair at 0.16.5 — a baseline old enough to lack `--statistics`+json would bail (`Unsupported serialization format for statistics`, printer.rs:388-392) and the module would silently count zero, which is the one-faced-probe failure this module exists to refuse. Adopt it only with a non-empty-output assertion on both arms."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && for v in 0.15.20 0.16.5; do echo \"== ruff $v\"; uvx ruff@$v check --statistics --output-format json python/src tests > /tmp/rs-$v.json 2>/tmp/rs-$v.err; echo \"rc=$?\"; head -c 300 /tmp/rs-$v.json; echo; head -3 /tmp/rs-$v.err; done"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lint_delta-2"
unit = "mod-lint_delta"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "parse_ty() recovers ty rule names by regexing the human-readable concise output with `^\\w+\\[([a-z0-9-]+)\\]`, on the documented premise that \"ty has no JSON output\"."
evidence = "python/src/dotfiles_setup/lint_delta.py:85-95 (_TY_CODE regex + parse_ty docstring: \"ty has no JSON output (full, concise, gitlab, github, junit), so the concise form is parsed\"); python/src/dotfiles_setup/lint_delta.py:120-124 (TOOLS[\"ty\"].argv = (\"check\", \"--output-format\", \"concise\")); python/uv.lock:2431-2432 (ty pinned 0.0.76)"
native_alternative = "The premise still holds — ty at 0.0.77 offers full / concise / gitlab / github / junit and no `json` — but `gitlab` IS a machine-readable JSON emitter that carries the rule identifier per diagnostic in a `check_name` field, via the shared ruff_db renderer. `ty check --output-format gitlab` + `json.loads` + `Counter(e[\"check_name\"] for e in entries)` would replace the regex over prose with a parse of a declared schema."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ty/docs/reference/cli.md:72-78 (the complete `--output-format` value list: full, concise, gitlab, github, junit — confirms NO json, so the module's stated premise is still true at corpus ty 0.0.77, /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ty/CHANGELOG.md:3); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff_db/src/diagnostic/render/gitlab.rs:105-137 (check_name emitted per diagnostic); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff_db/src/diagnostic/render/snapshots/ruff_db__diagnostic__render__gitlab__tests__output.snap:7 (`\"check_name\": \"F401\"`) and .../ruff_db__diagnostic__render__gitlab__tests__syntax_errors.snap:7 (`\"check_name\": \"invalid-syntax\"`)"
rationale = "The module's own justification for the regex was verified and is STILL CORRECT at the pin — this is not a stale premise. The refactor is optional and modest: gitlab JSON is a declared schema, whereas the concise prefix is human output that can be reformatted by any ty release without a deprecation. Held at 0.5 because gitlab.rs:105 switches `check_name` between the rule NAME and the rule CODE depending on `config.preview && !prefer_rule_codes`, so the identifier a delta is keyed on could change meaning under a flag — which is exactly the kind of silent key-shift that makes 'introduced' and 'retired' buckets lie. Do not adopt without pinning that behaviour on both arms."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && uvx ty@0.0.76 check --output-format gitlab python/src tests > /tmp/ty-gitlab.json 2>/tmp/ty-gitlab.err; echo rc=$?; python3 -c \"import json,collections,sys; d=json.load(open('/tmp/ty-gitlab.json')); print(collections.Counter(e.get('check_name') for e in d))\"; uvx ty@0.0.76 check --output-format concise python/src tests | head -5"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-lint_delta-3"
unit = "mod-lint_delta"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "locked_version() reads the pinned version out of uv.lock with a multi-line regex that assumes `[[package]]` / `name = \"...\"` / `version = \"...\"` appear on three consecutive lines in that order."
evidence = "python/src/dotfiles_setup/lint_delta.py:179-185 (`re.search(rf'\\[\\[package\\]\\]\\nname = \"{re.escape(package)}\"\\nversion = \"([^\"]+)\"', lock_text)`)"
native_alternative = "stdlib `tomllib.loads(lock_text)` then `next(p[\"version\"] for p in data[\"package\"] if p[\"name\"] == package)`. uv documents uv.lock as a TOML file, and five sibling modules in this same package already parse TOML this way rather than by regex."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/docs/concepts/projects/layout.md:96 (\"`uv.lock` is a human-readable TOML file but is managed by uv and should not be edited manually\"); repo precedent for the native parser: python/src/dotfiles_setup/lock_integrity.py:49, python/src/dotfiles_setup/parity.py:36, python/src/dotfiles_setup/bootstrap_packages.py:23, python/src/dotfiles_setup/dependency_ownership.py:8, python/src/dotfiles_setup/workflow_hooks.py:48 (all `import tomllib`)"
rationale = "Text this module reads is TOML, and Python 3.14 (the repo's floor) ships a TOML parser — a hand-rolled regex over a format with a stdlib parser is exactly the homegrown-over-native shape use-tool-builtins.md rejects, and the rest of dotfiles_setup already does it the native way. The regex binds uv's line ORDER inside `[[package]]`, an undocumented incidental of today's writer; a reordering (or an inline table, or a version written before name) turns a real pin into `None`, and `None` routes to a loud `return 2` (lint_delta.py:313-316) so it fails safe rather than silently — that is why this is a robustness refactor and not a defect. Note the existing behaviour that must survive the swap: test_a_package_whose_name_is_a_prefix_is_not_matched (tests/test_lint_delta.py:60) — tomllib's exact-key comparison satisfies it more strongly than the regex does."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lint_delta-4"
unit = "mod-lint_delta"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "Delta.introduced classifies a rule code as \"the upgrade's\" purely by absence from the baseline run's counts — it cannot distinguish a rule that did not EXIST at the baseline version from one that existed but did not fire (or was not enabled) on this tree."
evidence = "python/src/dotfiles_setup/lint_delta.py:138-148 (`introduced` = codes in current and not in baseline); python/src/dotfiles_setup/lint_delta.py:273 and the SKILL's policy guidance at .claude/skills/lint-delta/SKILL.md:41-46 (\"The upgrade's — new rule classes. This is a policy call\")"
native_alternative = "`ruff rule --all --output-format json` — ruff ships a machine-readable catalogue of every rule at whatever version is invoked (code, name, preview status, fix availability, explanation). Run it under the same `uvx ruff@<version>` seam already in the module and diff the two catalogues: a code in `introduced` that is absent from the baseline catalogue is genuinely NEW; one present in both changed behaviour, was promoted out of preview, or was newly selected by config."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff/src/args.rs:136-149 (`Rule { rule, --all, --output-format }` subcommand: \"Explain a rule (or all rules)\", with a HelpFormat output-format arg); same file lines 162-167 for the sibling `Linter { --output-format }`"
rationale = "Native ruff already answers the question the report currently leaves to the reader, and it answers it without touching the tree, using a seam the module already has. It does not supersede the module — running both versions over the same tree is still the only way to get COUNTS — so this is an adopt-the-native-mechanism refactor that would sharpen the 'The upgrade's' section from \"new to your tree\" into \"new to the tool\" vs \"newly fired\". Held below 0.7 because it is additive scope rather than a defect, it is ruff-only (ty has `ty explain-rule --output-format`, cli.md:204, but the equivalence was not verified), and the corpus proves the subcommand at 0.16.5 only — an arbitrary baseline version's `ruff rule --all --output-format json` shape is unproven, the same compatibility caveat as finding 1."
probe = "cd /tmp && for v in 0.15.20 0.16.5; do uvx ruff@$v rule --all --output-format json > /tmp/rules-$v.json 2>&1; echo \"$v rc=$? bytes=$(wc -c < /tmp/rules-$v.json)\"; done; python3 -c \"import json; a={r['code'] for r in json.load(open('/tmp/rules-0.15.20.json'))}; b={r['code'] for r in json.load(open('/tmp/rules-0.16.5.json'))}; print('added:', sorted(b-a)[:20]); print('removed:', sorted(a-b)[:20])\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lint_delta-5"
unit = "mod-lint_delta"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The unit as a whole: run the SAME tree through two pinned versions of a linter and partition the violations by rule code into mine / the-upgrade's / retired; derive the baseline from the previous revision that touched python/uv.lock (not HEAD~1); and REFUSE with exit 2 when the comparison cannot discriminate (unknown tool, unpinned tool, underivable baseline, baseline == current pin)."
evidence = "python/src/dotfiles_setup/lint_delta.py:128-176 (Delta.introduced/retired/mine), :188-202 (previous_lock_revision — the lockfile-touching revision, explicitly not HEAD~1), :294-357 (lint_delta_main, three `return 2` refusals incl. `if baseline == current:` at :334), :252-291 (render_report); wiring: mise.toml:1526-1536 ([tasks.lint-delta] -> `uv run --project python dotfiles-setup lint-delta`), python/src/dotfiles_setup/main.py:88, :516, :2456-2457 (registration + dispatch), .claude/skills/lint-delta/SKILL.md:1-6, python/verification/suites.toml:2361-2377 (workflow.lint-delta-control-arm binds the whole chain incl. `if baseline == current:`), tests/test_lint_delta.py:50-262 (20 tests)"
native_alternative = "None found. Probed for a native cross-version violation partition in: ruff (no baseline/delta/since-version mode — the CLI surface is check/rule/config/linter/clean/format, args.rs:131-176), ty (--output-format list has no baseline notion, ty/docs/reference/cli.md:72-78), uv (uvx tool@version is the RUN-a-pinned-version primitive and the module already uses it; nothing aggregates two runs), hk (steps run one pinned tool, no A/B), mise, gh, renovate, pytest plugins, and the repo's linter fleet (agnix, claude-code-lint, cclint, agents-lint, ast-grep, zizmor, ghalint). Claude Code 2.1.266 adds nothing here either — the gap digest .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md covers harness capabilities, not linter-bump triage."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff/src/args.rs:131-176 (the complete ruff Command enum: Check, Rule, Config, Linter, Clean, GenerateShellCompletion, Format — no baseline/diff/since subcommand); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ty/docs/reference/cli.md:72-78 (ty check's full flag surface for output, no baseline); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/docs/concepts/projects/layout.md:96 (uv.lock is uv's own format, \"not usable by other tools\" — nothing else reads a pin out of it at a git revision)"
rationale = "REQUIRED JUSTIFICATION: no linter in this stack ships a baseline/delta mode, so the attribution question (\"is this wall of diagnostics mine or the bump's?\") has no native answer at the pinned versions. The unit is also already native-first where a native mechanism exists: it runs the off-pin version through `uvx <tool>@<version>` (lint_delta.py:220-239) rather than hand-installing, derives the baseline from git rather than remembering it, and keeps every seam (tool, baseline, paths, runner, lockfile) as a parameter with this repo's case as the DEFAULT — the reusable-by-parameter shape agent-artifact-conventions.md rule 6 asks for. The load-bearing part is the REFUSAL, not the report: a version compared against itself can only ever print 'no change', and printing that as a clean bill is the one-faced probe probes-need-a-control-arm.md exists to reject. Findings 1-4 are internal adoptions of native mechanisms inside this unit; none of them removes the unit. Caveat on scope, not on disposition: 0.85 rather than higher because the marketplace/plugin surface was probed only through the offline corpus and the ten gap digests, not through a live registry search — a third-party 'lint baseline' plugin could exist without appearing there."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && uvx ruff@0.16.5 --help 2>&1 | sed -n '/^Commands:/,/^Options:/p'; uvx ruff@0.16.5 check --help 2>&1 | grep -iE 'baseline|since|diff|previous' ; echo \"grep-rc=$? (1 == no native baseline flag)\"; uvx ruff@0.16.5 check --help 2>&1 | grep -c 'statistics'"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-listing_budget-1"
unit = "mod-listing_budget"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "Hand-rolled measurement of the standing per-turn cost of the skill/agent listing: collect_listing() walks each enabled plugin's cache root and the project .claude/{skills,agents}, parses frontmatter, and total_chars() sums len(name)+len(description) into one number the doctor compares to a ceiling."
evidence = "python/src/dotfiles_setup/listing_budget.py:161-220 (_scan, collect_listing, total_chars); python/src/dotfiles_setup/doctor.py:1055-1088 (check_listing_budget); registered at python/src/dotfiles_setup/doctor.py:1206; contract python/verification/suites.toml:2195-2207"
native_alternative = "`claude plugin details <name>` prints a plugin's component inventory (Skills/Agents counts and names) plus a 'Projected token cost -> Always-on: ~N tok added to every session' figure and a per-component always-on/on-invoke table; `/doctor` reports how much context the skill listing uses and which skills contribute most; `/skills` sorts the listing by token count (press `t`); `/skill-doctor` (2.1.261) shows which loaded skills go unused and what they cost."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:1160-1200 (plugin details: inventory + Always-on/On-invoke token cost); .../settings-reference.md:2735 ('To see how much context the listing uses and which skills contribute most, run `/doctor`'); .../commands.md:139 (`/skills` ... 'Press `t` to sort by token count'); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (`/skill-doctor`, 2.1.261); live probe P1 in .agent/kb/raw/mod-listing_budget-native-probes.md"
rationale = "The harness now computes this quantity itself, in TOKENS rather than the module's char proxy, and per component. But no native surface emits ONE machine-readable aggregate across project skills + project agents + every enabled plugin: `plugin details` is per-plugin and human-formatted (no --json), `/doctor` and `/skills` are interactive, and none of them is callable from the SessionStart doctor. So the collection pass is superseded in substance but not in interface: the module should shell out to `claude plugin details` per enabled plugin for the plugin half (and stop re-deriving token cost from raw chars), keeping only the aggregation, the project-local half, and the threshold. Retiring outright would lose the one thing nothing native provides — a single number a gate can fail on."
probe = "for p in $(python3 -c \"import json;print(' '.join(k.split('@')[0] for k,v in json.load(open('.claude/settings.json'))['enabledPlugins'].items() if v))\"); do claude plugin details \"$p\" 2>/dev/null | grep -E 'Always-on'; done   # then compare the token sum against total_chars(setup.listing)/4"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-listing_budget-2"
unit = "mod-listing_budget"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "The 1,536-character description cap is treated as a fixed constant (SKILL_DESCRIPTION_MAX, imported from kb_setup.md_budget) and applied unconditionally by over_cap()."
evidence = "python/src/dotfiles_setup/listing_budget.py:42 (`from kb_setup.md_budget import SKILL_DESCRIPTION_MAX, skill_description`), :223-236 (over_cap compares desc_chars > SKILL_DESCRIPTION_MAX); doctor message hardcodes 'the HARD {SKILL_DESCRIPTION_MAX} cap' at python/src/dotfiles_setup/doctor.py:1073-1077"
native_alternative = "`skillListingMaxDescChars` settings key — the cap is a configurable setting (default 1536, any positive integer, scope 'Any file'), so the harness's real cap is whatever the settings chain resolves, not a constant."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:2739-2753 ('### `skillListingMaxDescChars`' ... 'Type: number of characters, a positive integer' ... 'Default: `1536`'); .../skills.md:1058 ('The cap is configurable with skillListingMaxDescChars')"
rationale = "Not a retirement — nothing native fails a build on an over-cap description — but the constant is now a stale copy of a setting the operator can change in any settings file. Read the effective value from the settings chain (falling back to 1536) instead of hardcoding it, or the check silently reports false positives the moment anyone raises the cap, and false negatives if anyone lowers it. Probed: this repo's .claude/settings.json sets none of skillListingMaxDescChars / skillListingBudgetFraction / skillOverrides, so the constant is correct TODAY and the drift is latent, not live (.agent/kb/raw/mod-listing_budget-native-probes.md P3)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-listing_budget-3"
unit = "mod-listing_budget"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "over_cap() applies the SKILL listing cap to AGENT entries too, and check_listing_budget reports an over-cap agent as 'the tail is TRUNCATED SILENTLY'."
evidence = "python/src/dotfiles_setup/listing_budget.py:223-236 (over_cap filters every entry regardless of kind) fed by :161-194 (_scan runs for kind 'agent' as well); the claim is stated at :226-234 and repeated at python/src/dotfiles_setup/doctor.py:1073-1077"
native_alternative = "Subagent descriptions are governed by a different, documented mechanism: a combined 15,000-TOKEN budget across all non-built-in subagents, for which Claude Code itself shows a startup warning with the total token count — and it 'still loads every subagent', i.e. no per-agent silent truncation is documented. The 1,536-char cap is documented only for the SKILL listing."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:27 ('When the combined descriptions of your subagents, except the built-in ones, exceed 15,000 tokens, Claude Code shows a warning at startup with the total token count'); .../sub-agents.md:772 ('shows a startup warning when your subagents combined descriptions pass the 15,000-token limit, and still loads every subagent'); the per-entry cap is documented as skill-only at .../skills.md:337-338 and .../settings-reference.md:2739-2753"
rationale = "The agent half of the check applies a skill-listing rule to a surface the docs govern differently: agents get a combined token budget with a native startup warning, not a per-description silent truncation. If the docs are right, every over-cap AGENT finding this check emits is a false alarm, and the real agent risk (combined > 15,000 tokens) is not measured at all — while the native warning already covers it. Refactor: keep over_cap for skills, and replace the agent arm with a combined-token check that mirrors the documented 15,000-token budget (or drop it and defer to the startup warning). Held at 0.6 rather than higher because the module's docstring at :230-234 cites a MEASURED agent truncation (1,789 chars, 253 cut), which the docs do not describe — one of the two is wrong and the probe settles it."
probe = "Write a throwaway agent under .claude/agents/ with a 2,000-char description containing a unique sentinel at char ~1,700, start a fresh session, and ask the model to repeat the sentinel from its agent listing; control arm: the same sentinel at char ~200 must come back. Repeat with a skill to confirm the harness truncates there."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-listing_budget-4"
unit = "mod-listing_budget"
kind = "module"
disposition = "refactor"
confidence = 0.58
custom_thing = "A fixed aggregate ceiling (`max_chars = 34000`) in doctor.toml's [listing] section, failing the doctor when project+plugin listing chars exceed it."
evidence = "doctor.toml:138-150 ([listing] section and its rationale); consumed at python/src/dotfiles_setup/doctor.py:1078-1088 (`ceiling = setup.listing_baseline().get('max_chars')`); pinned by python/verification/suites.toml:2207 (per_path_tokens 'doctor.toml' = ['[listing]', 'max_chars = 34000'])"
native_alternative = "`skillListingBudgetFraction` — Claude Code already caps the skill listing at a share of the context window (default 0.01) and, when it overflows, keeps every skill's NAME and drops the descriptions of the least-used skills, so overflow degrades gracefully instead of being an unbounded cost; the budget can be raised per settings, or individual entries collapsed to name-only via `skillOverrides`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:1052-1058 ('the listing's character budget ... scales at 1% of the model's context window. When the listing overflows, Claude Code drops descriptions starting with the skills you invoke least'); .../settings-reference.md:2723-2737 (`skillListingBudgetFraction`, default 0.01); .../settings-reference.md:3885-3905 and .../skills.md:770-783 (`skillOverrides`, `name-only`)"
rationale = "The ceiling's premise — that unbounded listing growth is silently paid for every turn — is now only half true for skills: the harness enforces its own budget and sheds the least-used descriptions, and on this host's 1M-context model 1% is far above 34,000 chars, so the doctor fires while the harness is nowhere near its cap. The ceiling should be DERIVED (skillListingBudgetFraction x context window, minus the separate 15,000-token agent budget) rather than a hand-set char constant, and the remedy text should offer `skillOverrides: name-only` alongside 'disable a plugin'. Keeping a reviewed ceiling is still defensible as a cost nudge, but a fixed number that tracks neither the model nor the setting is a stale proxy."
probe = "claude plugin details <each enabled plugin> | grep Always-on   # sum tokens, then compare against 0.01 * context window for the session model, and against doctor.toml's 34000/4"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-listing_budget-5"
unit = "mod-listing_budget"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "plugin_root() resolves an enabled plugin's load directory by taking the HIGHEST NUMERIC version directory under ~/.claude/plugins/cache/<marketplace>/<plugin>/, explicitly rejecting both mtime and ~/.claude/plugins/installed_plugins.json."
evidence = "python/src/dotfiles_setup/listing_budget.py:110-158 (_version_key, plugin_root, and the two rejected alternatives documented in the docstring); reused by python/src/dotfiles_setup/plan_attest.py:40,53 to locate the plugin script the plan-attest wrapper runs"
native_alternative = "`claude plugin list --json` emits one row per installed plugin with `id`, `version`, `enabled`, `scope`, `projectPath` and an explicit `installPath` — the harness's own answer to 'where is this plugin on disk'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:1134-1152 ('### plugin list' ... '`--json` Output as JSON'); live probe P2 in .agent/kb/raw/mod-listing_budget-native-probes.md"
rationale = "Adopting the native record would REGRESS the module. Cross-check on this host: `claude plugin list --json` reports antigravity 0.23.0 and planning-with-files 3.12.0 for this repo's projectPath, while `claude plugin details` resolves 0.26.0 and 3.17.2 — which is what this session actually loaded and what plugin_root's version-max rule returns. That is the identical per-project staleness the docstring already recorded for installed_plugins.json, reproduced through a different, official surface. Control arm: the same JSON is CORRECT for mattpocock-skills 1.2.3, fable-orchestrator 1.21.0 and codex 1.0.6, so the probe discriminates rather than being uniformly wrong. `claude plugin details` agrees on the version but prints no path, so no native surface currently yields a trustworthy install path. Keep the version-max rule; re-test when the CLI exposes a path that agrees with the loaded version."
probe = "claude plugin list --json | python3 -c \"import json,sys;[print(r['id'],r['version'],r['installPath']) for r in json.load(sys.stdin) if r['enabled']]\" ; for p in antigravity planning-with-files; do claude plugin details $p | head -1; done   # native path source is adoptable only once these two agree"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_integrity-1"
unit = "mod-lock_integrity"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Lockfile platform-coverage regression gate: regressions()/check_lockfiles()/main() compare the four mise lockfiles against their `git show HEAD:` bytes and fail when a tool present in BOTH lost platform entries, or when the [conda-packages] section lost a platform family. Wired as the hk step `mise_lock_integrity` and the `lock-check` CLI subcommand."
evidence = "python/src/dotfiles_setup/lock_integrity.py:111-154 (regressions), :170-202 (check_lockfiles), :205-222 (main); hk.pkl:337-339 (`[\"mise_lock_integrity\"] { check = \"uv run --project python dotfiles-setup lock-check\" }`); python/src/dotfiles_setup/main.py:2451 (`\"lock-check\": lambda: sys.exit(lock_integrity_main(project_root))`); python/verification/suites.toml:1580-1594 (workflow.lock-integrity-wiring); tests/test_lock_integrity.py:53-149"
native_alternative = "mise's own `mise lock` surface (--dry-run, --json, --platform, --bump, --minimum-release-age, --upgrade) and `mise doctor`. Neither offers a verify/check mode over an existing lockfile's platform coverage: `--dry-run --json` reports only VERSION-level changes ('checksum/URL refreshes for unchanged versions produce no entries, so plain `mise lock --json` typically prints []'), and `mise doctor` has no lockfile analyzer at all."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/lock.md:16-70 (complete flag list at mise 2026.9.0 — no --check/--verify); same file lines 44-50 (--json semantics: version-level only); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:61-141 (the Args struct, authoritative); control-armed absence: `grep -rn 'lockfile|lock' src/cli/doctor/mod.rs` -> 0 hits while `grep -rn 'fn '` on the same file -> 5+ hits (/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/doctor/mod.rs:85,97,262,349,398). Corpus is mise 2026.9.0 (Cargo.toml version = \"2026.9.0\"); host mise is 2026.9.3 and the gap digest .agent/kb/raw/mise-releases-since-pin.digest.md reports ZERO releases newer than the host, i.e. no post-corpus lock capability."
rationale = "Nothing native answers the question this gate asks — 'did the committed lockfile lose platform coverage?'. mise has no lockfile-verify command, `mise doctor` does not look at lockfiles, hk has no lockfile builtin, and the damage class (same tool count, fewer platform entries; #370 measured 226 -> 226 tool blocks while 548 lines of linux conda entries vanished) is invisible to any version- or tool-level check including the repo's own test_lock_coverage.py. The gate is also deliberately artifact-shaped rather than command-shaped because mise.toml:101 sets `auto_install = true`, so any task can reach the destructive re-lock path and no command allowlist can bound it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_integrity-2"
unit = "mod-lock_integrity"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "scoped_lock_main() refuses a bare `mise lock` outright and loops `mise lock <tool>` per named tool, because a whole-file re-lock on this macOS host strips linux conda entries (measured conda 962 -> 427, linux-x64 628 -> 80)."
evidence = "python/src/dotfiles_setup/lock_integrity.py:251-301; mise.toml:1286-1298 ([tasks.lock] -> `dotfiles-setup lock-tools`); python/src/dotfiles_setup/main.py:2452"
native_alternative = "mise setting `lockfile_platforms` (env MISE_LOCKFILE_PLATFORMS): 'Restricts which platforms are included when auto-locking (triggered by `mise install`/`mise use`) or when running `mise lock` without `--platform`', plus the `mise lock --platform <list>` flag. The IMAGE config already uses it (.devcontainer/mise-system.toml:319 `lockfile_platforms = [\"linux-x64\", \"linux-arm64\"]`); the HOST configs declare none (grep over mise.toml and .config/mise/conf.d/shared.toml returns 0, control-armed by the same pattern matching mise-system.toml:319)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:1574-1599 ([lockfile_platforms], docs body + 'The current platform is always included'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/lock.md:29-31 (-p/--platform: 'If not specified, all platforms already in lockfile will be updated')"
rationale = "`lockfile_platforms` is a real, native, partially-overlapping lever that the host config does not use, and it is the only mise mechanism that scopes what an UNFILTERED lock touches. But it plausibly cannot supersede the refusal: the #370 damage is that darwin mise cannot RESOLVE linux conda checksums (jdx/mise#7700), so naming linux platforms in the setting may still end in a prune rather than a preserve — and the mise docs never promise that unresolvable targeted platforms are preserved. This must be measured, not reasoned: the module's own docstring records that `mise lock --dry-run` is a probe that can only reassure, so only a real write settles it."
probe = "cd $(mktemp -d) && git clone --depth 1 /Users/rmanaloto/dev/github/ray-manaloto/dotfiles probe && cd probe && grep -c 'conda-packages.linux-x64' mise.lock && MISE_LOCKFILE_PLATFORMS=linux-x64,linux-arm64,macos-arm64 mise lock && grep -c 'conda-packages.linux-x64' mise.lock   # counts must be equal for the native setting to supersede the bare-lock refusal; run in a THROWAWAY clone only, never in the repo"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_integrity-3"
unit = "mod-lock_integrity"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "declared-name validation inside scoped_lock_main(): every requested tool must appear as a key in the host mise configs before shelling out, because `mise lock <unrecognised-name>` exits 0 having done nothing (same trap fires on a short name for a backend-qualified key, e.g. `mise lock betterleaks`)."
evidence = "python/src/dotfiles_setup/lock_integrity.py:276-291 (unknown-name refusal), :225-248 (declared_tools/declared_host_tools); tests/test_lock_integrity.py (test_bare_lock_is_refused, bound as a contract token at python/verification/suites.toml:1594)"
native_alternative = "None — mise still treats an unmatched TOOL argument as 'nothing to do' rather than an error. In src/cli/lock.rs the run prints `! No tools configured to lock` when no lock targets were produced and then falls through to `Ok(())`; the only `bail!`s in the command are for `--upgrade` + tool args, a concurrent-write race, and joined resolution errors — none for an unknown tool name."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:567-579 (the `!has_lock_targets` branch, `miseprintln!(\"{} No tools configured to lock\")`), :714 (`Ok(())` — the same function returns success), :227/:256/:649 (the only bail! sites, none about tool names), :786-788 (is_unfiltered_lock_run = tool list empty)"
rationale = "The exit code still cannot distinguish 'locked' from 'silently no-op', so validating the name against the config BEFORE shelling out is the only cheap discriminator, and it is what makes `mise run lock -- <name>` fail in milliseconds instead of appearing to succeed. Confidence is 0.8 rather than higher only because the read is of the 2026.9.0 corpus while the host runs 2026.9.3; the gap digest reports no releases newer than the host, so a behaviour change in those three patch releases is unlikely but not source-verified here."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_integrity-4"
unit = "mod-lock_integrity"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "declared_tools()/declared_host_tools(): tomllib parse of `[tools]` keys out of named config files, parameterised so lock_shared can validate against the SHARED fragment alone while lock-tools unions root mise.toml + shared.toml."
evidence = "python/src/dotfiles_setup/lock_integrity.py:225-248; consumed at python/src/dotfiles_setup/lock_shared.py:139 and python/verification/suites.toml:2268"
native_alternative = "`mise ls --json --all-sources` reports each tool with a `source` object naming the config file it came from, which is the same per-file attribution this function computes."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/ls.md:27 (`--all-sources` — 'Display all tracked config sources for tools'), :58 (the `\"source\": {` field in the --json example)"
rationale = "The native listing would answer the question but requires mise to build and resolve the tool graph, and this repo has measured that path being fatal in exactly this context: with `auto_install = true` (mise.toml:101), a mise invocation in the lock family resolved the entire declared toolset before doing any work and died on host-only tools (python/verification/suites.toml:2255, the lock-shared round-1 failure). A pure tomllib read is also the only form that yields the literal config KEY string `mise lock` accepts (`aqua:jackchuka/mdschema`), which is precisely what the finding-3 validation must compare against; mise's short names would re-introduce the silent no-op. Custom code survives with that written justification per use-tool-builtins.md rule 6."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_integrity-5"
unit = "mod-lock_integrity"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "The hk wiring is deliberately UNGLOBBED — `[\"mise_lock_integrity\"]` declares only `check`, so the gate runs on every hk invocation instead of being filtered to the four lockfiles."
evidence = "hk.pkl:337-339 (no `glob`, no `condition`), contrasted with the globbed steps at hk.pkl:62, :132, :150"
native_alternative = "hk's native step `glob` (filters the staged/selected file set) and the `git` condition object (`git.staged_files`, `git.modified_files`, `git.unstaged_modified_files`) usable in expr conditions to run a step only when a matching file changed."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:54-55 ('this will filter the staged files and return the subset matching these globs'), :311-341 (git status in conditions and templates, field list + `condition = \"git.staged_files != []\"` examples). hk gap digest .agent/kb/raw/hk-releases-since-pin.digest.md: the only release after the 1.57.0 pin is v1.58.1, documentation-only, no runtime capability."
rationale = "Adopting either native filter would put a hole in the gate, so the unglobbed shape is the correct one and should not be 'optimized'. The #370 signature is a stray `mise install`/bare `mise lock` that WRITES a lockfile and never stages it; a `glob` filters the staged set, so the step would skip precisely the unstaged damage the artifact comparison exists to catch. A `git.modified_files` condition is closer to correct but converts a always-runs gate into one whose firing depends on hk's own git-status classification — a fail-open failure mode on a gate whose entire value is catching a silent write. The only cost of the current shape is one cheap python run (four `git show` calls plus four regex passes) per hk invocation."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_refresh-1"
unit = "mod-lock_refresh"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Hand-rolled stale-entry pruning of the root mise.lock: _prune_unknown_lock_tools / _strip_tool_blocks / _header_tool_owner / _tool_name_from_array_header remove [[tools.X]] blocks that are no longer in the config, with a sibling-table (conda-packages) collateral check and an atomic os.replace write."
evidence = "python/src/dotfiles_setup/lock_refresh.py:87-234 (block scan, prune, collateral validation, tmp.replace) called from python/src/dotfiles_setup/lock_refresh.py:274; entrypoint wired at python/src/dotfiles_setup/main.py:2137-2139 and mise.toml:1278-1283 ([tasks.lock-refresh-root])."
native_alternative = "mise lock's own stale-entry prune: prune_stale_entries_if_needed -> Lockfile::retain_tools_by_short_or_backend, with a 'Dry run - would prune' / prune report, i.e. `mise lock` already deletes lockfile tools that are no longer configured."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:876-892 (prune_stale_entries_if_needed + retain_tools_by_short_or_backend); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:355 (\"For unfiltered runs (`mise lock`), this means \\\"prune all stale lockfile entries\\\"\"); gating at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:786-788 (fn is_unfiltered_lock_run -> self.tool.is_empty())."
rationale = "The native prune exists but is unreachable from this call site by construction: it fires only when `self.tool.is_empty()` (lock.rs:786-788), and this entrypoint MUST pass explicit tool names, because an unfiltered `mise lock` also locks task-scoped tools (docs/cli/lock.md:12,19) — the exact regression (#821) that lock-refresh-root exists to prevent. So at mise 2026.9.1/2026.9.3 the two native behaviours are mutually exclusive and the custom prune is the only way to have both. Keep, and treat 'a --prune flag that works with TOOL args' as the upstream feature request that would retire it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_refresh-2"
unit = "mod-lock_refresh"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "top_level_config_tools(): parse mise.toml with tomllib, take only the top-level [tools] table, strip pipx bracket extras, and pass the resulting explicit name list to `mise lock` so [tasks.*].tools never enter mise.lock; refuses an empty set rather than degrading to bare `mise lock`."
evidence = "python/src/dotfiles_setup/lock_refresh.py:75-84 and :237-278 (explicit-argv build + empty-set refusal); tests/test_lock_refresh.py:84,118,232; rationale restated at mise.toml:1280-1282."
native_alternative = "None found in mise: `mise lock`'s flag surface (--global, --jobs, --dry-run, --platform, --bump, --json, --local, --minimum-release-age, --upgrade) has no task-tool exclusion, and including task tools was a deliberate upstream feature."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/lock.md:21-65 (complete flag list — no exclusion flag) and :12,19 (\"including tools declared by tasks\" / \"all configured and task-specific tools will be updated\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/CHANGELOG.md:965 (\"**(lock)** include task-specific tools ... #11976\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:1483-1557 (add_task_tools_to_lock). Control arm for the 0-hit settings search: `grep -c lockfile settings.toml` = 25 with the same command shape, so the absence of a task-tool setting is real."
rationale = "Nothing native expresses 'lock the config's tools but not the tasks' tools'. Upstream moved the other way on purpose (#11976), so the explicit-argv filter is the only mechanism available, and deriving the names from mise.toml (rather than a second hard-coded inventory) is already the minimum-drift shape."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_refresh-3"
unit = "mod-lock_refresh"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "strip_provenance(): line-level removal of every `provenance = ` key and `*.provenance*` table from the regenerated image lock, plus a fail-loud re-parse assert, because the image disables attestation verification but `mise lock` (2026.7.0, per the docstring) recorded provenance anyway and `mise install --locked` then fail-closes."
evidence = "python/src/dotfiles_setup/lock_refresh.py:470-513, called from _collect_one at python/src/dotfiles_setup/lock_refresh.py:443; the disabled settings it compensates for are .devcontainer/mise-system.toml:344-345 (github_attestations = false / slsa = false); tests/test_lock_refresh.py:401,429,507."
native_alternative = "At the pinned mise version the recording side is now settings-gated: the github backend only records attestation/SLSA provenance when `settings.github_attestations && settings.github.github_attestations && opts.github_attestations()` (and the SLSA branch when `settings.slsa && settings.github.slsa`), and mise's own error text tells you to 'Re-run `mise lock` to refresh the lockfile' when a lock carries provenance a disabled setting would reject. If the staged config's [settings] are honoured, the lock would come out provenance-free with no post-processing."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/backend/github.rs:1049-1051 and :1170-1172 (attestation recording gate), :1102,:1210 (slsa gate), :264-268 (github_attestations_disabled_for_tool_error -> 'Re-run `mise lock` to refresh the lockfile'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/backend/aqua.rs:1290,1302,2763-2764,2821 (aqua cosign/slsa gates — note aqua.cosign is NOT disabled by mise-system.toml, so an aqua cosign entry could still be recorded). Consumer-side fail-closed still present: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/backend/mod.rs:5215-5234 (ensure_provenance_setting_enabled, 'may indicate a downgrade attack')."
rationale = "The custom code's premise ('mise lock records provenance regardless of those settings') was verified against mise 2026.7.0; the source at 2026.9.3 (and the image pin is 2026.9.1, .devcontainer/Dockerfile:115) gates recording on exactly the settings the staged config disables, and the committed image lock currently has 0 provenance keys only because the strip runs. Retiring it on source reading alone is unsafe — the consumer-side fail-closed at backend/mod.rs:5215 turns a wrong call into a broken image build, and aqua cosign is not disabled — so this needs a live arm before any change. If the probe shows zero provenance keys, strip_provenance becomes a no-op guard (keep the assert, drop the surgery); if it shows entries, the docstring stands unchanged."
probe = "stage=$(mktemp -d); uv run --project python dotfiles-setup lock-stage --dir \"$stage\"; curl https://mise.run | MISE_VERSION=v2026.9.1 MISE_INSTALL_PATH=\"$stage/mise-pinned\" sh; MISE_TRUSTED_CONFIG_PATHS=\"$stage\" MISE_ENV=runtime \"$stage/mise-pinned\" lock --bump --platform linux-x64 -C \"$stage\"; grep -c 'provenance' \"$stage/mise.lock\" \"$stage/mise.runtime.lock\"   # 0/0 => recording is settings-gated and the strip is dead weight; >0 => keep it. Run inside the amd64 devcontainer (macOS cannot write linux conda entries, jdx/mise#7700). Control arm: same grep against the repo's root mise.lock, which has 90 provenance hits."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_refresh-4"
unit = "mod-lock_refresh"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "_collect_one()/coverage_baseline(): before overwriting a committed image lock, compare the provenance-stripped candidate against the git-HEAD bytes of the committed lock and refuse the write when any tool lost per-platform coverage within the declared OS families (#648 — the 131->64 linux-x64 truncation at an unchanged tool count)."
evidence = "python/src/dotfiles_setup/lock_refresh.py:364-467 (collect_system_lock, coverage_baseline, _collect_one -> regressions(committed, candidate, families=families)); contract-bound at python/verification/suites.toml:2248 (per_path_tokens pins 'regressions(committed, candidate, families=families)' and 'def coverage_baseline('); tests/test_lock_refresh.py:478,495,507,529."
native_alternative = "mise lock now carries its own platform-regression refusal: platform_regression_errors() refuses to replace a locked version when the new resolution failed for a platform that the currently-locked version(s) support ('refusing to replace locked version(s) ... that support this platform')."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:930-986 (platform_regression_errors, including its explicit 'cross-platform locking is best-effort' scoping comment at :936-941) and :2258 (test_platform_regression_rejects_provenance_failure)."
rationale = "The native guard is strictly narrower on three axes and cannot replace this one: (a) it is intra-run — it compares against the lockfile mise itself just read, never against the git-committed baseline, so a run that already overwrote the working-tree lock is graded against its own damage, which is precisely what coverage_baseline's HEAD-first choice defends against; (b) it fires only when a stale VERSION prune would remove a platform-supporting entry, so a metadata-only refresh that drops platform entries at unchanged versions — the measured #648 shape, tool count and versions unmoved — produces no error; (c) it says nothing about conda-package family tables. Keeping the custom guard also keeps the refusal at the moment of overwrite (collect), which is the only point where a bad lock can reach the committed tree."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_refresh-5"
unit = "mod-lock_refresh"
kind = "module"
disposition = "unknown"
confidence = 0.45
custom_thing = "_merge_shared_tools()/stage_system_lock_dir(): text-level splice of .config/mise/conf.d/shared.toml's [tools] body into a copy of mise-system.toml, to force one staged mise.lock covering both, because 'in a PROJECT layout the conf.d fragment ... mise writes one lock PER CONFIG DIR'."
evidence = "python/src/dotfiles_setup/lock_refresh.py:20-37 (module docstring stating the constraint) and :299-361 (stage + splice, with three fail-loud splice-point checks); tests/test_lock_refresh.py:353,368."
native_alternative = "mise's own lockfile-path resolution still confirms the constraint (a conf.d fragment locks to the parent of conf.d, not the project root) — BUT the same function redirects every config's lockfile to a single root when a monorepo root is in play, and mise now ships root-lockfile monorepos as a tri-state `[monorepo] lockfile = true` setting that migrates subproject lockfiles into one root lock."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/lockfile.rs:1489-1500 (conf.d -> parent-of-conf.d, then the monorepo_root redirect); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/monorepo.md:267-287 ('Monorepos can use one lockfile at the monorepo root ... Set `lockfile = true` to opt into root lockfiles now'); project-layout conf.d support at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/configuration.md:71-74."
rationale = "The docstring's premise is still true for a PLAIN project layout at 2026.9.3 (lockfile.rs:1489-1494), so the splice is not simply stale. What is new is a native single-root-lockfile mode that would let the stage mirror the image's real conf.d layout instead of hand-splicing TOML text — removing three bespoke splice-point failure modes. It is not obviously worth it (monorepo mode brings its own experimental surface and a rollout warning at mise 2026.12.0), so this must be probed before anyone touches the splice; do NOT refactor on the strength of the doc alone."
probe = "stage=$(mktemp -d); mkdir -p \"$stage/.config/mise/conf.d\"; cp .devcontainer/mise-system.toml \"$stage/mise.toml\"; cp .config/mise/conf.d/shared.toml \"$stage/.config/mise/conf.d/shared.toml\"; printf '\\n[monorepo]\\nlockfile = true\\n' >> \"$stage/mise.toml\"; MISE_TRUSTED_CONFIG_PATHS=\"$stage\" \"$stage/mise-pinned\" lock --bump --platform linux-x64 -C \"$stage\"; ls \"$stage\"/*.lock \"$stage/.config/mise\"/*.lock 2>&1; grep -c '^\\[\\[tools\\.' \"$stage/mise.lock\"   # PASS = shared.toml's tools appear in <stage>/mise.lock and no .config/mise/mise.lock is created. Control arm: repeat WITHOUT the [monorepo] block and confirm the shared tools land in <stage>/.config/mise/mise.lock, i.e. that the probe can produce the other answer."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_refresh-6"
unit = "mod-lock_refresh"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "pinned_mise_version(): regex-extract `ARG MISE_VERSION=<v>` out of .devcontainer/Dockerfile so CI installs the exact mise the image consumes the lock with, raising rather than defaulting when the ARG is absent."
evidence = "python/src/dotfiles_setup/lock_refresh.py:58,281-296; consumed by stage_system_lock_dir at :310 and printed for the composite at .github/actions/lock-refresh/action.yml (the `version=$(... lock-stage --dir \"$stage\")` line feeding `MISE_VERSION=\"v${version}\"`); tests/test_lock_refresh.py:340,346."
native_alternative = "None. Renovate owns the VALUE of the pin (a custom manager described at renovate.json:153), and mise's own version knobs (`min_version` in config, the MISE_VERSION installer env) neither read a Dockerfile ARG nor propagate it to a CI installer step."
alternative_evidence = "renovate.json:153 (the pin's Renovate description: 'mise installer version pin (curl https://mise.run honors MISE_VERSION) ... the CI lock-refresh job regenerates the lock in lockstep'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/lock.md:1-19 (no version-source input); .devcontainer/Dockerfile:115 (ARG MISE_VERSION=2026.9.1, the single source of truth)."
rationale = "This is a 15-line single-source-of-truth read, not reinvented tooling: the pin has to live in the Dockerfile (the image build is what consumes it), and no native mechanism carries a Dockerfile ARG into a CI installer step. The alternative — restating the version in the workflow — is the drift this function prevents, and the fail-loud raise is what keeps a missing ARG from silently producing an unconsumable 'latest' lock."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_shared-1"
unit = "mod-lock_shared"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "The routed `mise lock` pins the platform set through an env-var override injected as `devcontainer exec --remote-env MISE_LOCKFILE_PLATFORMS=<comma list>`, relying on implicit env>config-setting precedence to beat the image's `lockfile_platforms = [\"linux-x64\", \"linux-arm64\"]`."
evidence = "python/src/dotfiles_setup/lock_shared.py:186 (LOCKFILE_PLATFORMS_VAR), python/src/dotfiles_setup/lock_shared.py:267-268 (the `--remote-env` pair), python/src/dotfiles_setup/lock_shared.py:164-185 (the rationale comment); the setting it fights is .devcontainer/mise-system.toml:319."
native_alternative = "`mise lock --platform <comma-separated list>` — a first-class CLI flag whose precedence over the `lockfile_platforms` SETTING is documented explicitly, and which bypasses `determine_existing_platforms` entirely."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:84-88 (`/// Comma-separated list of platforms to target` … `pub platform: Vec<String>`, `delimiter = ','`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:1243-1249 (`if !self.platform.is_empty() { return Platform::parse_multiple(&self.platform); }` — the settings path is never reached); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:1585 (`Does NOT override explicit \\`mise lock --platform\\` flags.`). In-repo precedent for the flag already exists: .github/actions/lock-refresh/action.yml:68 uses `lock --bump --platform linux-x64`."
rationale = "Same effect, one fewer `--remote-env` pair, and the precedence is guaranteed by documented CLI semantics instead of by env-var-beats-config-file ordering that no test in tests/test_lock_shared.py exercises against a real container. One behavioural difference to preserve deliberately: the settings/env path inserts `Platform::current()` into the set (mise/src/lockfile.rs:2557-2560), the `--platform` path does not — harmless here because `shared_lock_platforms()` already derives a union that contains the container's own linux-x64, but it must be stated in the change rather than discovered."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_shared-2"
unit = "mod-lock_shared"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "`shared_lock_platforms()` re-derives the union of platform keys from the committed `.config/mise/mise.lock` so the routed call can pin them back, plus a ValueError guard against an empty set."
evidence = "python/src/dotfiles_setup/lock_shared.py:195-220 (`shared_lock_platforms`), python/src/dotfiles_setup/lock_shared.py:289 (its only call site), tests/test_lock_shared.py `test_routed_command_pins_the_full_shared_platform_set` / `test_shared_lock_platforms_refuses_an_empty_lockfile`."
native_alternative = "mise already does exactly this derivation itself: `determine_existing_platforms()` reads the target lockfile's existing platform keys and targets precisely those — the custom function is a reimplementation that only exists because the container's SYSTEM config sets `lockfile_platforms`, which short-circuits it. The native lever to drop that inherited setting for one invocation is `MISE_SYSTEM_CONFIG_FILE` (env-only setting)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/lockfile.rs:2551-2578 (`determine_existing_platforms`: `let existing_keys = lockfile.all_platform_keys(); … return Ok(platforms…)` — but guarded at 2556-2561 by `if let Some(configured) = Settings::get().lockfile_platforms()?`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:2733-2738 (`[system_config_file]` … `env = \"MISE_SYSTEM_CONFIG_FILE\"`, `env_only = true`). The image puts its settings in the system config dir: .devcontainer/Dockerfile:77 (`MISE_SYSTEM_CONFIG_DIR=/usr/local/share/mise`) + .devcontainer/Dockerfile:127 (`COPY .devcontainer/mise-system.toml /usr/local/share/mise/config.toml`), while the shared fragment the lock needs lives in the separate `conf.d/` file (.devcontainer/devcontainer.json:179)."
rationale = "If pointing the routed mise at an empty system config drops BOTH image overrides at once (`lockfile_platforms` and `github_attestations = false`) while leaving `/usr/local/share/mise/conf.d/shared.toml` and the container PATH intact, then `shared_lock_platforms()`, both `--remote-env` overrides and their two tests can be deleted in favour of mise's native lockfile-derived platform set. If it also drops the baked `[env]`/PATH the container needs, the custom derivation stays and is justified. Nothing in the offline corpus settles which — the system-config load path and the conf.d load path have to be observed, not reasoned about."
probe = "mise run up && devcontainer exec --workspace-folder . --remote-env MISE_IGNORED_CONFIG_PATHS=/workspaces/dotfiles/mise.toml --remote-env MISE_SYSTEM_CONFIG_FILE=/dev/null mise lock --dry-run uv 2>&1 | tee /tmp/probe-syscfg.log; echo rc=$? >> /tmp/probe-syscfg.log   # PASS = the '→ Targeting N platform(s)' line names all 11 platforms of .config/mise/mise.lock (not 2) and no 'No tools configured to lock'; CONTROL ARM = re-run WITHOUT the MISE_SYSTEM_CONFIG_FILE pair and confirm the same line reports the image's 2-platform set, proving the probe discriminates."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_shared-3"
unit = "mod-lock_shared"
kind = "module"
disposition = "refactor"
confidence = 0.58
custom_thing = "A macOS-host -> amd64-devcontainer routing layer (host capability check, tri-state `--container/--no-container`, `/workspaces/<basename>` path translation, `--remote-env` plumbing) whose entire purpose is to get `mise lock` executed on linux."
evidence = "python/src/dotfiles_setup/lock_shared.py:239-277 (`_lock_command`), :223-236 (`_workspace_mise_toml`), :391-419 (host_can_lock + routing decision); mise.toml:1299-1316 (the task)."
native_alternative = "The repo already runs `mise lock` on a GitHub-hosted linux-x64 runner for the OTHER two lockfiles — the `lock-refresh` composite action, which states the linux-runner requirement as its own contract — and that lane does NOT cover `.config/mise/mise.lock`: `lock-refresh-root` is handed only the root `mise.toml`. Extending the existing native linux lane (plus `gh workflow run` / `mise run gha-dispatch` to trigger it on demand) covers the same need without a local container round-trip."
alternative_evidence = ".github/actions/lock-refresh/action.yml:12-14 (\"Linux-x64 runner REQUIRED — macOS mise silently omits linux-x64 conda checksums\"), .github/actions/lock-refresh/action.yml:26-30 (root step) and :68 (`\"$stage/mise-pinned\" lock --bump --platform linux-x64 -C \"$stage\"`); the gap: python/src/dotfiles_setup/main.py:2139 (`lock_top_level_config_tools(project_root / \"mise.toml\")` — root config only, so shared.toml's tools are never named) and python/src/dotfiles_setup/lock_refresh.py:31-35 (states that shared tools land in a separate `.config/mise/mise.lock`)."
rationale = "Not a supersession by an upstream tool — mise has no cross-host execution and `devcontainer exec --remote-env` is already the native transport this module uses. But the linux-executor need is solved TWICE in this repo by two different mechanisms, and the CI one is the one that already runs unattended, is already gated, and needs no local ~21.5GB base to be current. The honest refactor is to add the shared fragment to `lock-refresh` (one more `lock_top_level_config_tools` call against `.config/mise/conf.d/shared.toml`) and demote `lock-shared` to the interactive escape hatch, rather than keeping the on-demand path as the only route to a linux-resolved shared lock."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-lock_shared-4"
unit = "mod-lock_shared"
kind = "module"
disposition = "keep"
confidence = 0.78
custom_thing = "Silent-no-op detection by string-matching mise's own console message `\"No tools configured to lock\"` in the captured output of every `mise lock` call, treated as a hard failure regardless of exit code."
evidence = "python/src/dotfiles_setup/lock_shared.py:192 (`_NO_TOOLS_MARKER`), :307-322 (the check), tests/test_lock_shared.py `test_a_local_silent_no_op_fails_even_at_rc_zero` / `test_a_routed_silent_no_op_fails_even_at_rc_zero` / `test_ordinary_output_does_not_trigger_the_no_op_check`."
native_alternative = "`mise lock --json` — documented structured output of lockfile version changes, the obvious candidate for replacing a message sniff with a parsed result."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:99-108 (`/// Only version-level changes are reported: checksum/URL refreshes for unchanged versions produce no entries, so plain \\`mise lock --json\\` typically prints \\`[]\\` while still updating the lockfile.`) — so `[]` cannot distinguish \"refreshed metadata\" from \"did nothing\". Worse for adoption: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:567 (`if !has_lock_targets && !self.json`) — under `--json` the no-tools message is SUPPRESSED, so adding `--json` would silently delete this module's only no-op signal. mise emits no non-zero exit for this case (bail! sites exist at lock.rs:227/256/393/514/649 for other conditions — control arm that the grep for a bail can find one)."
rationale = "Required justification: no native mise mechanism reports \"this invocation locked nothing\" — not the exit code (0), not `--json` (`[]` is ambiguous and additionally suppresses the message). The custom check stays, but it is a symptom sniff bound to upstream wording (the exact hazard .claude/rules/probes-need-a-control-arm.md rule 9 names), and it is mutually exclusive with `--json`. Two durable guards worth adding in the same breath: a comment at lock_shared.py:192 that `--json` must never be added to these argvs, and a positive assertion (mise's `✓ Lockfile written to` line, /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:558-563, or a pre/post content hash of `.config/mise/mise.lock`) so the gate requires evidence of work rather than only forbidding evidence of its absence."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-lock_shared-5"
unit = "mod-lock_shared"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The module's remaining core: refusing a bare (whole-file) re-lock, validating every tool name against `.config/mise/conf.d/shared.toml` ALONE before any routing decision, the tri-state container policy with a loud refusal on an incapable host, and delegating coverage verification to `lock_integrity.main` rather than reimplementing it."
evidence = "python/src/dotfiles_setup/lock_shared.py:364-389 (bare-tools refusal + scoped `declared_tools` validation), :391-406 (refusal on an incapable host), :421-424 (delegated coverage check); mise.toml:1299-1316; python/verification/suites.toml:2254-2268 (`workflow.lock-shared-wiring`)."
native_alternative = "None found. `mise lock <name>` neither validates the name nor scopes it to one config file: src/cli/lock.rs has no bail for an unrecognised tool argument, and mise's own guidance is the opposite of scoping (`mise lock --global`, and the `No tools configured to lock in this project, but global config declares tools` redirect). `mise doctor`, `mise tool-sync`, hk builtins, gh, renovate's mise manager and the linters (agnix / claude-code-lint / cclint / agents-lint / zizmor / ghalint) all operate on config or repo hygiene, not on which lockfile a named tool belongs to."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:567-578 (the no-targets branch: a message, then normal exit — no error path for an unknown name; control arm: `bail!` DOES appear at lock.rs:227, 256, 393, 514, 649 for other failure conditions, so the absence at the unknown-tool site is real, not a broken grep); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/lock.rs:69-72 (`--global`) and settings.toml:1590 (`lockfile_platforms` is per-settings, not per-tool). mise gap digest: .agent/kb/raw/mise-releases-since-pin.digest.md (host 2026.9.3 is the newest published release; zero newer releases to supersede this)."
rationale = "Required justification: the three behaviours this module is actually made of — (a) \"a bare re-lock is destructive here\" (#370), (b) \"this name is declared in the SHARED fragment, not the root config\", and (c) \"this host cannot resolve linux assets, so refuse or route\" — are all statements about THIS repo's two-config topology and this Mac's OS, and mise has no vocabulary for any of them. The one place a native mechanism could have eliminated custom code is asset resolution itself, and it has not: the aqua glibc-preference change that would be the candidate (`prefer glibc assets with musl fallback`, mise CHANGELOG.md:119 under `## [2026.8.9] - 2026-08-19`) landed BEFORE the 2026-08-27 measurement this module was built on, so it cannot be the fix. Caveat to carry forward: the offline mise changelog stops at 2026.9.0 (CHANGELOG.md:3) while the host runs 2026.9.3, so the 2026.9.1-2026.9.3 window is unread — the premise is unrefuted, not re-confirmed, at the host's exact version."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-main-1"
unit = "mod-main"
kind = "module"
disposition = "refactor"
confidence = 0.88
custom_thing = "Two independent, hand-maintained registries for the same 68 subcommands: the argparse registration tree (python/src/dotfiles_setup/main.py:1652-1750 `setup_parser` plus 20 `_add_*_subcommands` helpers) and a separate name->callable dict built by `_build_command_handlers` (python/src/dotfiles_setup/main.py:2312-2523), joined only by a string lookup at python/src/dotfiles_setup/main.py:2539 (`handler = handlers.get(args.command)`) that SILENTLY no-ops when the two disagree (python/src/dotfiles_setup/main.py:2540 `if handler is not None:` with no else)."
evidence = "Measured in-process against the real parser: 68 parsers registered, 67 handlers, PARSER WITHOUT HANDLER = ['query-latest'] (registered at python/src/dotfiles_setup/main.py:1674-1677). Live, control-armed: `uv run --project python dotfiles-setup query-latest jq` -> rc=0 with no output; control arm `dotfiles-setup version` -> rc=0 printing '0.1.0', so the probe discriminates. A prior cold review (docs/research/kb/reports/agents/20260829-session-resume-cold-review-round2.md:119) inspected this very subcommand and concluded it 'works' — it parses, it just dispatches to nothing. The drift risk is already being paid for elsewhere: python/verification/suites.toml cites `python/src/dotfiles_setup/main.py` on 62 lines, pinning dispatch call sites by contract token because the join is stringly-typed. Probe artifact: .agent/kb/raw/argparse-stdlib-dispatch-probe.md"
native_alternative = "argparse's stdlib sub-command dispatch: `subparser.set_defaults(func=handler)` at registration, then `args.func(args)` — one object carries both the parser and its handler, so a registered-but-unhandled subcommand becomes structurally impossible rather than a silent rc=0."
alternative_evidence = "/Users/rmanaloto/.local/share/uv/python/cpython-3.14-macos-aarch64-none/lib/python3.14/argparse.py:1485 (`def set_defaults(self, **kwargs)`); live probe saved at .agent/kb/raw/argparse-stdlib-dispatch-probe.md ('PROBE set_defaults dispatch -> ran-a', i.e. the handler attached at add_parser time is reachable as `ns.func`)."
rationale = "The custom dict is not buying anything argparse does not already give: the only thing it adds over `set_defaults` is the possibility of the two halves diverging, and they HAVE diverged (`query-latest` is dead today). Adopting `set_defaults` also lets `run_command` fail loud on an unknown command instead of exiting 0. Caveat for the synthesis stage, so this is not sold as free: the module's helper-splitting is dictated by ruff PLR0915 (8 mentions in main.py, no `max-statements` override in python/pyproject.toml), and moving handlers into the `_add_*` registration functions moves statements INTO the functions already at the cap — so this is a real refactor of `setup_parser`'s decomposition, not a mechanical rewrite. The already-dead subcommand should be fixed (or deleted) regardless."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-main-2"
unit = "mod-main"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "`parser.add_subparsers(dest=\"command\", help=\"Commands\")` at python/src/dotfiles_setup/main.py:1655 leaves the subcommand OPTIONAL, and `main()` (python/src/dotfiles_setup/main.py:2546-2565) has no fallback that prints help or errors — so a bare `dotfiles-setup`, or any typo'd invocation that argparse still accepts, terminates successfully having done nothing."
evidence = "Live probe: `uv run --project python dotfiles-setup` -> rc=0 and 0 bytes on stdout+stderr (saved in .agent/kb/raw/argparse-stdlib-dispatch-probe.md). Control arm: `dotfiles-setup version` -> rc=0 with output, so the zero-byte result is the CLI's real behaviour, not a captured-output artefact. This matters because 73 mise.toml call sites shell out to this binary (mise.toml:220, 232, 373, 620, 634, 669, ...) and several are gates whose only signal is the exit code — a subcommand rename upstream would degrade a gate to a silent pass rather than a failure."
native_alternative = "`add_subparsers(dest=..., required=True)` — argparse's own flag making the subcommand mandatory, producing `error: the following arguments are required: cmd` and exit 2 instead of a silent success."
alternative_evidence = "/Users/rmanaloto/.local/share/uv/python/cpython-3.14-macos-aarch64-none/lib/python3.14/argparse.py:1225 (`_SubParsersAction.__init__` defaults `required=False`), /Users/rmanaloto/.local/share/uv/python/cpython-3.14-macos-aarch64-none/lib/python3.14/argparse.py:1946 (`def add_subparsers`); control-armed probe in .agent/kb/raw/argparse-stdlib-dispatch-probe.md: with `required=True` a bare invocation raises SystemExit 2, with it omitted the same parser returns `cmd=None` and parses fine."
rationale = "One keyword argument replaces a class of silent-pass failures across a 68-subcommand CLI that is wired into gates, hooks and CI. Nothing outside argparse offers this — mise cannot see inside the child's argument parsing, and hk/uv only observe the exit code the CLI chooses to return. Pairs naturally with mod-main-1: `required=True` closes the empty-command hole, `set_defaults` closes the unhandled-command hole."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-main-3"
unit = "mod-main"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "The root parser is constructed with no Python-3.14 argparse ergonomics: `argparse.ArgumentParser(description=\"Reproducible Dotfiles Orchestrator\")` (python/src/dotfiles_setup/main.py:1654) — no `suggest_on_error`, no `color` — while exposing 68 sibling subcommands with near-collinear names (`lock-check` / `lock-tools` / `lock-shared` / `lock-stage` / `lock-collect` / `lock-refresh-root`, `platform` / `platform-matrix` / `platform-literals`)."
evidence = "python/src/dotfiles_setup/main.py:1654 (bare constructor); measured 68 registered subcommands (.agent/kb/raw/argparse-stdlib-dispatch-probe.md). The repo pins python 3.14.7 (mise/shared pins), so the 3.14 argparse surface is available at the host's actual interpreter — the probe ran on cpython-3.14.0 from the uv-managed toolchain."
native_alternative = "`argparse.ArgumentParser(..., suggest_on_error=True)` (Python 3.14): argparse itself suggests the nearest valid choice on a mistyped subcommand. The same 3.14 argparse carries a colorised help/error theme (`color=` / `get_theme`)."
alternative_evidence = "/Users/rmanaloto/.local/share/uv/python/cpython-3.14-macos-aarch64-none/lib/python3.14/argparse.py:1860 (`- suggest_on_error - Enables suggestions for mistyped argument choices`), /Users/rmanaloto/.local/share/uv/python/cpython-3.14-macos-aarch64-none/lib/python3.14/argparse.py:170 (`color=True`); control-armed probe in .agent/kb/raw/argparse-stdlib-dispatch-probe.md — with the flag: \"invalid choice: 'lock-shred', maybe you meant 'lock-shared'?\"; without it (control arm): \"invalid choice: 'lock-shred'\" and nothing more."
rationale = "A stdlib feature the interpreter already ships, unused, on exactly the shape it was designed for — a long list of similar subcommand names that agents and mise tasks type by hand. It is ergonomics rather than correctness, hence the lower confidence, but it is a one-keyword adoption with a measured behavioural difference and no dependency cost. Do not confuse it with mod-main-2: the exit code is 2 either way; what changes is whether the error names the intended command."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-main-4"
unit = "mod-main"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The unit as a whole: a single hand-written Python entrypoint (`main()`, python/src/dotfiles_setup/main.py:2546) that owns environment validation (`EnvironmentValidator`, python/src/dotfiles_setup/main.py:148-159), logging setup, repo-root resolution (python/src/dotfiles_setup/main.py:2553), one shared `DotfilesConfig` instance, and the aggregation of ~40 sibling modules' `*_main` functions behind one binary — including the Claude Code hook entrypoints (`hook pretooluse` / `hook selfcheck`, python/src/dotfiles_setup/main.py:1327-1341)."
evidence = "python/pyproject.toml:45 (`dotfiles-setup = \"dotfiles_setup.main:main\"`); 73 `dotfiles-setup` invocations in mise.toml; 62 lines of python/verification/suites.toml bind `python/src/dotfiles_setup/main.py` as the dispatch layer under contract."
native_alternative = "None supersedes the aggregator. The two native mechanisms that touch it are already adopted: PEP 621 console scripts (`[project.scripts]`) provide the binary, and mise TOML tasks provide the wrapper/argument surface that fronts it (mise docs: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-arguments.md)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md (TOML task definition — what the 73 wrappers already use); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-arguments.md (mise's usage-spec argument surface, which passes through to the child rather than replacing its parser). Gap digests checked and found empty for this unit: .agent/kb/raw/mise-releases-since-pin.digest.md (zero releases newer than the host pin), .agent/kb/raw/hk-releases-since-pin.digest.md (v1.58.1 is documentation-only, no capability), .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 new capabilities, none touching how a hook command is invoked or how a project CLI is structured)."
rationale = "Required justification: nothing in mise, hk, uv, gh, Claude Code 2.1.266, codex-cli 0.152.1 or any pinned linter provides a multi-subcommand, typed, testable Python command surface — mise tasks are process wrappers that cannot own argument parsing, validation or a shared config object, and hk/uv only consume the exit code. The zero-bash-logic rule (.claude/rules/zero-bash-logic.md) actively requires this layer to exist in python/. Adopting a third-party CLI framework (click/typer) was NOT evaluated as a native alternative because it would add a runtime dependency to replace a stdlib module that already does the job once mod-main-1..3 are applied. The unit therefore stays, with the three argparse-native corrections above applied to it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-memory_index-1"
unit = "mod-memory_index"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "Hard-coded auto-load budget enforcement: LINE_BUDGET=200 / BYTE_BUDGET=25_000, load_cutoff_line(), MemoryIndexResult.over_cap, the _budget_section() table, and over_cap gating the rc via .ok"
evidence = "python/src/dotfiles_setup/memory_index.py:73-74 (LINE_BUDGET/BYTE_BUDGET), :269-283 (load_cutoff_line), :191-193 (over_cap), :206-212 (.ok includes over_cap), :474-502 (_budget_section), :279-280 (total += len(line.encode()) — every byte of every line counted)"
native_alternative = "Claude Code measures MEMORY.md against the same 200-line / 25KB read limits after every write to it: near a limit it reminds Claude to shorten; over a limit it returns a tool error naming the exact overage and a target size. Behaviour present since v2.1.210 (host is 2.1.266). Its measurement is also stricter than the custom one: it counts only loaded content, excluding frontmatter and HTML comments."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:403; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/errors.md:2551 and :2556 (\"Before v2.1.210, an over-limit index was silently truncated on the next load with no write-time signal\"); errors.md:180 (error-index row); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/changelog.md:1207 (\"Improved the memory index over-limit warning to measure only loaded content, excluding frontmatter and HTML comments\")"
rationale = "The 'is the index over its cap' question is now answered natively at write time, on every write, without anyone remembering to run a task — that is strictly better than an on-demand checker for the same question, and the module's own comment block (:61-72) justifies the budget code by the harness having been silent, which stopped being true at v2.1.210. Two parts do NOT retire: beyond_budget enumerates WHICH entries fell past the cutoff (memory_index.py:419, :493-501), which the native error does not do, and the module needs a cutoff line to compute it. So: keep load_cutoff_line for the beyond_budget enumeration, but align its measurement with the harness (exclude frontmatter and HTML comments per changelog.md:1207 — the current byte count includes them, so the two can disagree in both directions), and drop over_cap from .ok rather than re-litigating a verdict the harness already delivers with more authority."
probe = "printf 'x%.0s' {1..30000} > /tmp/idx.md; then compare `dotfiles-setup memory-index` byte count against a MEMORY.md carrying frontmatter + an HTML comment, to measure how far the custom count diverges from the harness's loaded-content count"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-memory_index-2"
unit = "mod-memory_index"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "memory_dir(): derives the auto-memory directory as transcripts_base(env, home) / encode_cwd(project_root) / 'memory' — i.e. CLAUDE_CONFIG_DIR (or ~/.claude) / projects / <encoded CWD> / memory, honouring only CLAUDE_CONFIG_DIR"
evidence = "python/src/dotfiles_setup/memory_index.py:215-227 and its docstring :4-6; python/src/dotfiles_setup/command_audit.py:213-226 (transcripts_base reads only CLAUDE_CONFIG_DIR; encode_cwd rewrites the CWD path). Consequence: python/src/dotfiles_setup/memory_index.py:634-640 — a directory miss prints 'no memory directory' and returns 0, so a wrong derivation is a silent all-pass, the exact failure class the module exists to prevent."
native_alternative = "The harness's storage-location contract has three inputs the custom derivation ignores: (a) <project> is derived from the GIT REPOSITORY, so all worktrees and subdirectories of one repo share ONE auto-memory directory; (b) CLAUDE_CODE_PROJECT_DIR_NAME (v2.1.234+) overrides the <project> segment; (c) the autoMemoryDirectory setting relocates the directory entirely and is readable from any settings scope (user, project, local, policy, --settings)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:369 (git-repo derived, worktrees share one dir), :371 (CLAUDE_CODE_PROJECT_DIR_NAME, v2.1.234+), :373-381 (autoMemoryDirectory); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:2574-2588 (autoMemoryDirectory, scope 'Any file'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/changelog.md:4236 (\"Project configs & auto memory now shared across git worktrees of the same repository\")"
rationale = "Probed, both arms: this session has a live worktree at /private/tmp/.../scratchpad/main-audit; its encode_cwd form resolves to ~/.claude/projects/-private-tmp-...-main-audit/memory which does NOT exist (ls: No such file or directory), while the control arm ~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/memory DOES. So `mise run memory-index` run from a worktree of this repo audits nothing and exits 0, while the harness is reading and writing the main repo's shared memory dir. autoMemoryDirectory produces the same silent-zero. The fix is not to hand-roll git-root detection but to resolve in the documented precedence — autoMemoryDirectory from merged settings, then CLAUDE_CODE_PROJECT_DIR_NAME, then the git repository root (git rev-parse --show-toplevel) rather than the CWD — and to make an unresolvable directory rc=1, not rc=0, since 'nothing to check' and 'I looked in the wrong place' are currently indistinguishable."
probe = "cd <a git worktree of this repo> && uv run --project python dotfiles-setup memory-index; echo rc=$?   # expect the 'no memory directory' line and rc=0 today"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-memory_index-3"
unit = "mod-memory_index"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "The fact-survival audit itself: distinctive_facts() extraction (issue refs, commit shas, byte sizes), per-kind fact_present() matching, _elsewhere() severity split, _audit_entry() over title+hook, and the index_only report that fails the check"
evidence = "python/src/dotfiles_setup/memory_index.py:299-317 (distinctive_facts), :320-353 (per-kind matching), :356-373 (_elsewhere), :376-387 (_audit_entry), :161-164 + :181-183 (index_only severity), :505-525 (report section)"
native_alternative = "None found. The harness's only memory-curation behaviour is size-triggered prose guidance to Claude ('keep one line per entry, move detail into topic files, and merge or drop stale entries') — it never verifies that a fact in an index entry also exists in the topic file that entry links to. The nearest agent linter rule touching auto memory, agnix GM-010, is a Gemini-CLI settings rule (memoryManager/autoMemory), not a Claude MEMORY.md content check."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:403 (the guidance is 'merge or drop stale entries', with no survival check); memory.md:393 and :407 (topic files are read on demand, never compared to the index); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/website/docs/rules/generated/gm-010.md:1-22 (GM-010 is tool: gemini-cli, about experimental.memoryManager); control arm: grep -c 'capabilit' .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md -> 15 while grep -i memory over all .agent/kb/raw/*.digest.md -> 0 hits outside fable-orchestrator, so the 2.1.258-2.1.266 gap adds nothing here"
rationale = "Required justification: the native mechanism and this checker answer opposite questions. Native asks 'is the index too long?' and tells Claude to shorten — which is precisely the operation that destroys index-only facts; nothing native asks 'will shortening this line lose a fact?'. The harness in fact makes the hazard WORSE by instructing a rewrite under size pressure, so the pre-trim survival check is the complement of the native warning rather than a duplicate of it. No linter in the pinned set (agnix 0.52.1, claude-code-lint 0.8.0, cclint, agents-lint 0.5.0, contextlint, ast-grep) has a rule over ~/.claude memory content, and the checker's calibration (narrow classes, prefix-tolerant shas, KB-means-knowledge-base exclusion) is repo-specific vocabulary no general tool would carry."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-memory_index-4"
unit = "mod-memory_index"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "inbound_refs() / render_refs(): the pre-delete citation check — which other memory files cite a memory, matched by bare boundary-anchored stem so [[link]], [[link|Alias]], `link` and link.md all count"
evidence = "python/src/dotfiles_setup/memory_index.py:424-452 (inbound_refs), :597-622 (render_refs), :642-647 (the --refs path, always rc=0)"
native_alternative = "None. The harness documents [[wikilink]]-style memory linkage and on-demand reads but exposes no citation/backlink query and no delete-time check; graphify (the repo's own graph tool, 0.9.53) indexes the repository tree, not ~/.claude/projects/<project>/memory, so mise run graphify-affected cannot answer it either. rumdl's nearest rule, MD051, validates link ANCHORS (the part after #), not the existence of a linked file, and is configured over repo markdown."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:383-393 (directory layout; MEMORY.md is the only index, no backlink surface) and :407 (topic files read on demand); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/rumdl/docs/md051.md:1-8 (\"Ensures that link anchors (the part after #) point to actual headings\")"
rationale = "Required justification: deleting a memory has the same silent-loss shape as trimming one, and no native or third-party mechanism reads the auto-memory directory as a citation graph. The target tree lives outside the repo, which rules out every repo-scoped tool the project already pins (rumdl, markdownlint-cli2, mdschema, contextlint, graphify). The one caveat worth carrying forward is finding mod-memory_index-2: --refs inherits the same wrong-directory derivation, so a worktree invocation reports 'not a memory file' and rc=0."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-memory_index-5"
unit = "mod-memory_index"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "Structural integrity of the index: broken links (entry target missing from the memory dir), orphans (topic files no entry links), and unparsed_lines() (a link-shaped line _ENTRY could not read, which every other check would skip in silence)"
evidence = "python/src/dotfiles_setup/memory_index.py:88 (_LINK_ITEM), :286-296 (unparsed_lines), :407-409 (broken), :418 (orphans), :206-212 (.ok fails on broken and unparsed but not orphans), :538-582 (report sections)"
native_alternative = "None covers it. The harness never validates that a MEMORY.md entry resolves to an existing topic file, and treats a dangling [[wikilink]] as intent rather than breakage. Repo markdown linters cannot see the directory (it is outside the repo) and in any case check anchors, not link targets: rumdl MD051 is anchor-scoped; agnix's only auto-memory rule (GM-010) is Gemini-CLI settings."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:383-393 (the directory contract: an index plus topic files, with no stated validation); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/rumdl/docs/md051.md:1-8; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/website/docs/rules/generated/gm-010.md:11-22"
rationale = "Required justification: nothing native or pinned inspects the auto-memory directory's internal consistency, and the unparsed_lines check in particular is a coverage guard on this module's own regex — a class of self-check no external tool could provide because it is about what THIS parser could not read. Its value is unchanged by the harness's write-time budget warning, which says nothing about link resolution."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-mise_config_context-1"
unit = "mod-mise_config_context"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "Hand-rolled path filtering: MISE_CONFIG_GLOBS (7 fnmatch globs) plus matches(), which resolves the edited path, enforces repo containment via Path.relative_to(repo_root), and fnmatch-matches the repo-relative posix path. The hook is wired unconditionally on matcher 'Edit|Write|NotebookEdit', so a `uv run --project .../python dotfiles-setup mise-config-context` process is spawned for EVERY file write in the session and 99% of those spawns exit 0 with no output."
evidence = "python/src/dotfiles_setup/mise_config_context.py:57-65 (MISE_CONFIG_GLOBS), :95-108 (matches(); resolve + relative_to containment + fnmatch), :212-214 (build_payload gate); .claude/settings.json:85-95 (PostToolUse matcher \"Edit|Write|NotebookEdit\", command with no `if` filter, timeout 20). Control arm on the grep shape: `grep -c '\"matcher\"' .claude/settings.json` -> 5, `grep -n '\"if\"' .claude/settings.json` -> 0 hits, so the repo genuinely uses no `if` filter anywhere."
native_alternative = "Claude Code's per-handler `if` field on tool-event hooks. It takes permission-rule syntax matching the tool name AND its arguments together, is evaluated on PostToolUse, and skips the handler (no process spawn) when the pattern does not match. Path anchoring is native too: a leading `/` anchors the pattern at the primary working directory, which is exactly the repo-containment property matches() implements by hand, and `Edit(path)` is the rule form that covers Write and NotebookEdit as well."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:427 (`if` field table row: \"Permission rule syntax to filter when this hook runs... Only evaluated on tool events: PreToolUse, PostToolUse, ...\"); hooks.md:353 (\"`\\\"Edit(*.ts)\\\"` runs only for TypeScript files\"); hooks.md:209 (\"the `if` check would fail and `block-rm.sh` would never run, avoiding the process spawn overhead\"); hooks.md:434 (single-segment directory anchoring, `Edit(**/src/**)` for any depth); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:316 (\"Claude Code checks file permissions against `Edit(path)` and `Read(path)` rules only... Use `Edit(docs/**)` in place of `Write(docs/**)`, `NotebookEdit(docs/**)`\"); permissions.md:328,355 (`/path` anchors at the primary working directory)."
rationale = "The native `if` filter covers the whole of matches() -- glob matching, and the repo-containment half via `/`-anchoring -- and it does so BEFORE the process spawn, which the current wiring cannot. That matters here specifically: #536 measured PreToolUse guards at ~393ms median on every call and #524/#528 exist to cut exactly this cost, and this hook pays a full `uv run` import on every unrelated Edit. Not a retire, for two reasons that are both documented: (a) `if` holds exactly one rule with no `&&`/`||`/list syntax (hooks.md:432), so the 7 globs become 7 sibling handlers sharing one command -- more settings.json, less runtime; (b) the module still owns the payload, the JSON-escaping of the path, and the dedup, so it shrinks rather than disappears. The one thing not settled on paper is whether an `if: \"Edit(...)\"` handler actually fires for a Write/NotebookEdit tool call: permissions.md:316 states that normalisation for the PERMISSION engine, and hooks.md:427 says `if` 'uses the same syntax', but syntax is not the same claim as tool-name normalisation. Probe it before rewiring, or the filter silently drops the wholesale-Write case this hook exists for."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && cp .claude/settings.json /tmp/settings.bak && python - <<'PY'\nimport json,pathlib\np=pathlib.Path('.claude/settings.json'); s=json.loads(p.read_text())\ns['hooks']['PostToolUse']=[{\"matcher\":\"Edit|Write|NotebookEdit\",\"hooks\":[{\"type\":\"command\",\"if\":\"Edit(/mise.local.toml)\",\"command\":\"cat >> /tmp/if-probe.log\",\"timeout\":10}]}]\np.write_text(json.dumps(s,indent=2))\nPY\n# then, in a NEW claude session in this repo: Write mise.local.toml (in scope) and README.md (out of scope), and\n# wc -l /tmp/if-probe.log   # expect exactly one record, for mise.local.toml, proving `if` fires under the Write tool\n# control arm: repeat with if=\"Edit(/zzq-nonexistent.toml)\" -> expect 0 records\ncp /tmp/settings.bak .claude/settings.json"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 3
survived = false
schedule = ""

[[finding]]
id = "mod-mise_config_context-2"
unit = "mod-mise_config_context"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Once-per-session-per-agent dedup implemented as marker files: _key() composes `<session_id>--<agent_id>`, _marker() writes `.agent/state/mise-config-context/<key>.seen`, _prune() sweeps markers older than 7 days, and already_seen() fails OPEN (emits) when the marker cannot be written."
evidence = "python/src/dotfiles_setup/mise_config_context.py:48-52 (STATE_DIR, MARKER_MAX_AGE_SECONDS), :128-186 (_key/_marker/_prune/already_seen), :215-217 (the gate in main); tests/test_mise_config_context.py:330-343 (per-agent dedup, replay arm)."
native_alternative = "None. Claude Code exposes no per-session firing budget for tool-event hooks: the only once-per-session events are SessionStart and SessionEnd, and those carry no `tool_input.file_path`, so they cannot know a mise config was written. The docs state the absence directly for the closest thing (async hooks): \"There is no deduplication across multiple firings of the same async hook.\" The `agent_id` the custom key uses IS a documented common input field present only inside subagents, so the custom key is at least built on native ground."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:21 (\"once per session: `SessionStart` and `SessionEnd`\"); hooks.md:3687 (\"There is no deduplication across multiple firings of the same async hook.\"); hooks.md:740 (`agent_id` common input field: \"Present only when the hook fires inside a subagent call\"); hooks.md:267 (tool events fire inside subagents and carry `agent_id`)."
rationale = "Required justification: the harness has no notion of 'inject this context at most once per session', and the two events that ARE once-per-session cannot see which file was edited. A grep of hooks.md for 'once per session|only once|deduplicat|fires once' returns exactly three hits, none of them a budget mechanism -- and the one that speaks to the topic says the deduplication does not exist. So the marker-file layer is the whole of the capability, not a reinvention. Its fail-open-on-unwritable-marker posture is also the right way round and has no native equivalent to inherit."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-mise_config_context-3"
unit = "mod-mise_config_context"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "A whole module dedicated to ONE rule: CONTEXT hard-codes the body of `.claude/rules/use-tool-builtins.md` as applied to mise configs, MISE_DOCS hard-codes one tool's offline doc path, and MISE_CONFIG_GLOBS hard-codes that one rule's path set. Adding a second write-triggered rule means a second module and a second PostToolUse handler."
evidence = "python/src/dotfiles_setup/mise_config_context.py:69-92 (MISE_DOCS + CONTEXT, one rule's text inline), :57-65 (one rule's globs), mise.toml:1262-1276 (a task per injector), .claude/settings.json:85-95 (a handler per injector)."
native_alternative = "The repo's own generic replacement already exists half-built: `rule_registry.py` parses every `.claude/rules/*.md` into records carrying id, repo-relative path, load class, `paths:` globs and an `inject: bool` flag, and its module docstring names this exact consumer -- \"the #928 write-trigger dispatcher\". The swap-and-delete is a filed, open ticket (#935 'Swap the write-hook over to the dispatcher and retire the old injector'), and #928 is the dispatcher itself."
alternative_evidence = "python/src/dotfiles_setup/rule_registry.py:3-9 (docstring: \"the #928 write-trigger dispatcher\" ... \"whether the write-hook should inject it\"), :92 (\"The inject set lives here, not in frontmatter\"), :147 (`inject: bool` field), :305 (`inject = rel_path in INJECT_PATHS`); `gh issue list --state open` -> #928 \"Write-trigger dispatcher: inject the rules that govern a written path\", #935 \"Swap the write-hook over to the dispatcher and retire the old injector\", #954 \"hook-selfcheck: no verification contract binds the PostToolUse mise-config-context dispatcher\"."
rationale = "This is the largest single lever on the unit and it is already specced rather than speculative: the registry that would drive a generic dispatcher is landed (#918), the dispatcher (#928) and the swap (#935) are open, and the ticket-cut advisor already ruled this file 'excluded -- deleted by the dispatcher ticket' (docs/research/kb/reports/agents/2026-09-02-ticket-cut-advisor.md:103,152). Not marked 'retire' because the dispatcher does not exist yet -- deleting the module today removes a live gate and leaves nothing. Sequence it after #928, and note that #954 means no verification contract currently binds the wiring, so the swap has no gate watching it: pin the new command string with `per_path_tokens` in suites.toml in the same change (`mise run token-check` first)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-mise_config_context-4"
unit = "mod-mise_config_context"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "A hook, rather than a `paths:`-scoped rule file, used to deliver path-specific instruction context -- justified in the module docstring on the grounds that a scoped rule fires on READ and a wholesale Write never reads the file."
evidence = "python/src/dotfiles_setup/mise_config_context.py:8-12 (the docstring's stated justification), :2-6 (PostToolUse on Edit/Write/NotebookEdit)."
native_alternative = "Claude Code's native path-specific rules -- `paths:` frontmatter on a memory/rule file -- and the `InstructionsLoaded` hook event with the `path_glob_match` load reason. Both are the native mechanism for path-scoped instruction delivery."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:220 (\"Path-scoped rules trigger when Claude reads files matching the pattern, not on every tool use\"); memory.md:462 (scoped rules \"reload as Claude reads files they apply to\"); hooks.md:1265,1275-1276 (`InstructionsLoaded` matcher on `load_reason`, values include `path_glob_match`, with `globs` from the file's `paths:` frontmatter)."
rationale = "Required justification: the vendor doc states the read-trigger limitation in as many words, so the module's premise is CONFIRMED against the native source rather than assumed -- a `paths:`-scoped rule cannot cover a blind Write, which is precisely the motivating case (the 2026-09-01 near-miss where a mise.toml decision was made without reading mise's docs). `InstructionsLoaded` is a read-side OBSERVER of the same mechanism (that is #917's use), not a write trigger. Nothing in the 2.1.258-2.1.266 gap digest or the llms-index digest adds a write-triggered instruction-loading path, and the llms-index digest reports the offline tree is current through the 2026-w34 What's New with zero new capability pages, so the negative here is armed rather than merely unfound."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-mise_config_context-5"
unit = "mod-mise_config_context"
kind = "module"
disposition = "keep"
confidence = 0.62
custom_thing = "The hook runs SYNCHRONOUSLY with a 20s timeout on every Edit/Write/NotebookEdit, and PostToolUse fires once per tool -- so a parallel batch of edits spawns N `uv run` processes, N-1 of which the marker dedup then silently discards."
evidence = ".claude/settings.json:85-95 (no `async`, `\"timeout\": 20`); python/src/dotfiles_setup/mise_config_context.py:215-217 (the discarded-work path: payload built, then dropped by already_seen)."
native_alternative = "Two native levers: (a) `\"async\": true` on the handler, which moves it off the write's critical path and delivers its output on the next conversation turn; (b) the `PostToolBatch` event, which \"fires exactly once with the full batch, so it is the right place to inject context that depends on the set of tools that ran rather than on any single tool\"."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:3665-3686 (async handler shape and its Limitations: \"Hook output is delivered on the next conversation turn\"); hooks.md:1304 (async is the exception to output being discarded on timeout); hooks.md:2088 (`PostToolBatch`: \"`PostToolUse` fires once per tool, which means it fires concurrently when Claude makes parallel tool calls. `PostToolBatch` fires exactly once with the full batch... There is no matcher for this event.\")."
rationale = "Required justification: both natives were considered and neither is an improvement as the unit stands. `PostToolBatch` has NO matcher (hooks.md:2088), so adopting it trades a filtered spawn for an unconditional one on every batch -- strictly worse than finding 1's `if` filter -- and this repo already recorded that rejection once (docs/research/kb/reports/agents/2026-09-02-ticket-cut-advisor.md:152, \"the `PostToolBatch` rejection\"); re-deriving it here is a second, independent arrival at the same answer. `async: true` defers delivery to the next conversation turn, which for a reminder whose whole purpose is to land BEFORE the next mise-config turn is a real behavioural loss, and it would sit on top of a filter (finding 1) that already removes the spawn cost this would be buying. Revisit `async` only if the probe in finding 1 shows `if` cannot filter Write calls."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_hash-1"
unit = "mod-p2996_hash"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "_extract_bake_target_block() — a hand-written brace matcher that slices the literal `target \"dev\" { ... }` text out of docker-bake.hcl, whose sha256 becomes DevHashInputs.dev_target_digest."
evidence = "python/src/dotfiles_setup/p2996_hash.py:264-291 (brace matcher), :499-501 (dev_target_digest = sha256 of that raw text), :143-146 (docstring: \"under-hashing here is the cardinal sin — a probe hit skips smoke-test\"). The `dev` target it slices is docker-bake.hcl:131 `inherits = [\"_common\", \"docker-metadata-action\"]`, and docker-bake.hcl `target \"_common\"` carries context, dockerfile, platforms, args.DEVCONTAINER_USER and secret — none of which appear inside the `dev` block text, so none reach the digest."
native_alternative = "`docker buildx bake --print <target>` (docker-cli 29.7.2 / buildx, already a hard dependency of this pipeline) prints the FULLY MERGED target as JSON on stdout — inherited fields resolved, variables interpolated, no build performed."
alternative_evidence = ".agent/kb/raw/mod-p2996_hash-native-probes.md P1 — live `docker buildx bake --print dev` output showing `context`, `dockerfile` and `args.DEVCONTAINER_USER` (all defined only in `_common`) present in the merged `dev` target; docker buildx bake --help lists `--print  Print the options without building` (same file, header probe)."
rationale = "This is a live under-hashing gap, not a style point: editing `variable \"DEVCONTAINER_USER\"` or `_common`'s secret/context wiring changes what the dev image contains while dev_target_digest — and therefore the `:dev-<hash>` tag — stays put, so dev-cache-probe HITs and build+smoke are skipped for content that never passed smoke. That is exactly the invariant the module's own docstring calls the cardinal sin. Digesting the canonicalised `bake --print` JSON for the target closes it and deletes the brace matcher. Two costs to accept in the same change: (a) it makes hash computation depend on docker being installed on whatever host runs `dotfiles-setup dev-hash` (true on GHA runners and on this Mac, false in a bare unit-test env — the tests at tests/test_p2996_hash.py:736-760 feed synthetic HCL strings and would need a recorded-JSON fixture instead), and (b) a buildx upgrade that reorders/adds JSON keys moves every dev hash, so the migration must land with a SCHEMA_VERSION bump and the JSON must be re-serialised canonically (sorted keys) rather than hashed as printed."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && cp docker-bake.hcl /tmp/bake.orig && orig=$(uv run --project python dotfiles-setup dev-hash) && python3 -c \"import pathlib;p=pathlib.Path('docker-bake.hcl');t=p.read_text();n=t.replace('variable \\\"DEVCONTAINER_USER\\\" {\\n  default = \\\"devcontainer\\\"','variable \\\"DEVCONTAINER_USER\\\" {\\n  default = \\\"someone-else\\\"');assert n!=t,'anchor missed';p.write_text(n)\" && new=$(uv run --project python dotfiles-setup dev-hash); cp /tmp/bake.orig docker-bake.hcl; echo \"orig=$orig new=$new  -- EQUAL proves the under-hashing gap (bake --print WOULD differ)\"; docker buildx bake --print dev 2>/dev/null | python3 -c 'import sys,json;print(\"merged args:\",json.load(sys.stdin)[\"target\"][\"dev\"][\"args\"])'"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_hash-2"
unit = "mod-p2996_hash"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "_extract_bake_variable() — a regex (`variable \"NAME\" { ... default = \"([^\"]*)\" }`) that reads a bake variable's raw quoted default out of docker-bake.hcl text. Feeds BASE_IMAGE, BUILDER_IMAGE and CLANG_P2996_REF into the base/p2996 hashes, and is re-exported to two other modules."
evidence = "python/src/dotfiles_setup/p2996_hash.py:197-211 (the regex), :378 (base_image), :439-441 (clang_p2996_ref, builder_image); imported cross-module at python/src/dotfiles_setup/image.py:27,99,2084 and python/src/dotfiles_setup/p2996_refresh.py:32,128,160."
native_alternative = "`docker buildx bake --list variables` (and the `args` map of `docker buildx bake --print <target>`) — buildx evaluates the HCL, so interpolation and HCL functions are applied and env overrides are folded in."
alternative_evidence = ".agent/kb/raw/mod-p2996_hash-native-probes.md P2 — live `--list variables` table vs. the SAME four names through the custom regex: they agree on BASE_IMAGE/PLATFORM, DISAGREE on IMAGE_REF (bake `ghcr.io/ray-manaloto/dotfiles-devcontainer` vs regex `${DEFAULT_REGISTRY}/${IMAGE}`), and the regex RAISES on LEG. Repo side of the disagreement: docker-bake.hcl:11-13 (`default = \"${DEFAULT_REGISTRY}/${IMAGE}\"`) and docker-bake.hcl:38-40 (`default = replace(PLATFORM, \"/\", \"-\")`)."
rationale = "The extractor is correct only by accident: all four variables it currently reads happen to be quoted literals. It has no notion of interpolation or HCL functions, so the day someone writes `default = \"${BASE_REGISTRY}/ubuntu@${DIGEST}\"` the base hash starts digesting template text — a value that is stable across real BASE_IMAGE changes, i.e. a cache tag that can never miss when it should. The failure is silent by construction (a plausible-looking string, no exception), which is the same class as #676. Routing the three reads through the native resolver removes a parser this repo does not own the grammar of. Not 'retire': the module still needs to compose those values into a canonical hash string, and the two downstream importers (image.py, p2996_refresh.py) must move together — p2996_refresh.py also WRITES the pin back, and bake gives no write path, so its rewrite half stays custom."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && docker buildx bake --list variables 2>/dev/null | grep -E '^(BASE_IMAGE|BUILDER_IMAGE|CLANG_P2996_REF)' && uv run --project python python -c \"from pathlib import Path; from dotfiles_setup.p2996_hash import _extract_bake_variable as f; t=Path('docker-bake.hcl').read_text(); print([f(t,n) for n in ('BASE_IMAGE','BUILDER_IMAGE','CLANG_P2996_REF')])\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_hash-3"
unit = "mod-p2996_hash"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "resolve_bake_platform() — hand-mirrors docker bake's own variable-resolution precedence (explicit override -> `PLATFORM` env var -> HCL default) in Python, so the content hash agrees with the architecture bake will actually build."
evidence = "python/src/dotfiles_setup/p2996_hash.py:214-261, with the constants at :60-68; called from all three tiers at :379, :442, :497. Its own docstring states the whole point: bake reads a same-named env var into each HCL variable, and reading only the HCL default made python and bake disagree silently before #676."
native_alternative = "Ask bake instead of re-implementing it: `PLATFORM=<x> docker buildx bake --print dev` returns `.target.dev.platforms`, and `--list variables` returns the resolved PLATFORM plus every variable DERIVED from it."
alternative_evidence = ".agent/kb/raw/mod-p2996_hash-native-probes.md P3 — both arms run: unset -> `[\"linux/amd64/v2\"]`, `PLATFORM=linux/arm64` -> `[\"linux/arm64\"]`, and `--list variables` under the override also recomputes `LEG` to `linux-arm64` (docker-bake.hcl:38-40)."
rationale = "The correctness requirement here is literally 'resolve it the way bake resolves it', and the only thing that is definitionally in sync with bake is bake. The custom mirror is one precedence rule deep; bake's real rule set (env var, `--var`, `--set`, HCL functions over the result) is larger, and every rule the mirror does not know about reintroduces the #676 failure mode — two architectures collapsing to one `:base-`/`:p2996-`/`:dev-<hash>` tag. The probe already shows one thing the mirror structurally cannot do: recompute `LEG` from the override. Keep the assertion half (p2996_hash.py:252-260 rejects an unparsable value so no unhittable tag is minted) — that is a project policy bake has no opinion about — and delete the precedence half. Not confidence >0.8 because the swap adds a subprocess to a pure function that CI calls six times per run, and the whole hash surface (findings 1-3) should move in one SCHEMA_VERSION epoch rather than three."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && for p in '' linux/arm64; do echo \"PLATFORM='$p'\"; env ${p:+PLATFORM=$p} docker buildx bake --print dev 2>/dev/null | python3 -c 'import sys,json;print(\"  bake:\",json.load(sys.stdin)[\"target\"][\"dev\"][\"platforms\"])'; env ${p:+PLATFORM=$p} uv run --project python python -c 'from pathlib import Path;from dotfiles_setup.p2996_hash import resolve_bake_platform as r;print(\"  python:\",r(Path(\"docker-bake.hcl\").read_text()))'; done"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_hash-4"
unit = "mod-p2996_hash"
kind = "module"
disposition = "retire"
confidence = 0.95
custom_thing = "compute_repo_hash() — a documented-deprecated back-compat shim from the retired single-hash design, aliasing compute_repo_p2996_hash."
evidence = "python/src/dotfiles_setup/p2996_hash.py:560-566 (comment + `def compute_repo_hash`, docstring 'Deprecated: prefer `compute_repo_p2996_hash`')."
native_alternative = "None required — the function has zero call sites. The 'existing callers' its comment preserves compatibility for no longer exist, so nothing native has to replace it."
alternative_evidence = ".agent/kb/raw/mod-p2996_hash-native-probes.md P5 — `git grep -n \"compute_repo_hash\" -- python tests .github mise.toml` returns only the definition and its own comment; the identical command shape over `compute_repo_base_hash` returns real call sites (python/src/dotfiles_setup/main.py:100,2306), which is the positive control proving the probe is not blind. No CLI verb maps to it either: main.py:2277-2308 dispatches only base-hash / p2996-hash / dev-hash."
rationale = "Dead code with a deprecation notice addressed to callers that no longer exist. It is not merely inert: it is a second, undocumented entry point that returns the P2996 hash under a name that reads like 'the repo hash', which is precisely the confusion the v2 split existed to end. Note that no linter here catches it — ruff's F401/F811 do not flag an unused public function, and the repo runs no dead-code pass — so this is exactly the kind of thing a manual audit is for. Deleting it is a one-line change with no import to update."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && git grep -n 'compute_repo_hash' -- python tests .github mise.toml docs/receipts && echo '--- positive control ---' && git grep -n 'compute_repo_base_hash' -- python | head -3"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_hash-5"
unit = "mod-p2996_hash"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "_file_digest() / _sha256_hex() — read a whole file into memory with `path.read_bytes()` and hash the buffer."
evidence = "python/src/dotfiles_setup/p2996_hash.py:322-330; used at :374-375 (both hk pkl files), :381-383 (mise-system.lock, mise-system.toml, shared.toml)."
native_alternative = "`hashlib.file_digest(fileobj, 'sha256')` — stdlib since Python 3.11, present on this interpreter; reads in chunks and uses the platform's zero-copy path where available."
alternative_evidence = ".agent/kb/raw/mod-p2996_hash-native-probes.md P4 — `uv run --project python python -c \"import hashlib,sys;print(sys.version.split()[0], hasattr(hashlib,'file_digest'))\"` -> `3.14.0 True`. The project floor is Python 3.14 (python/AGENTS.md 'Requires **Python 3.14**'), well above the 3.11 introduction."
rationale = "Small but genuinely native-first: `_file_digest` is the stdlib function re-implemented, and the `_sha256_hex(str | bytes)` union that supports it is what lets a caller accidentally hash TEXT where bytes were meant. That distinction is load-bearing in this module — gather_dev_inputs at :502-507 hashes mise-runtime.toml/.lock via `.read_text()` while gather_base_inputs at :381-383 hashes mise-system.toml via `.read_bytes()`, so the same file content produces different digests depending on which tier reads it, and on a CRLF checkout or a non-UTF-8 byte the text path is not even a function of the bytes the Dockerfile COPYs. Standardising every file input on `hashlib.file_digest` makes 'the hash covers the BYTES' — the rule #140/#156 established and this module's comments repeat — true by construction. Deliberately kept as one narrow change: it moves every base and dev hash, so it belongs in the same SCHEMA_VERSION epoch as findings 1-3."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && uv run --project python python -c \"import hashlib;from pathlib import Path;p=Path('.devcontainer/mise-runtime.toml');print('bytes ',hashlib.sha256(p.read_bytes()).hexdigest());print('text  ',hashlib.sha256(p.read_text().encode()).hexdigest());f=open(p,'rb');print('native',hashlib.file_digest(f,'sha256').hexdigest())\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_hash-6"
unit = "mod-p2996_hash"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "_validate_hex_digest() plus three frozen-dataclass __post_init__ hooks that reject empty strings and non-64-hex / non-16-hex fields at construction time."
evidence = "python/src/dotfiles_setup/p2996_hash.py:188-194 (_validate_hex_digest), :83-98 (BaseHashInputs.__post_init__), :118-127 (P2996HashInputs), :162-185 (DevHashInputs)."
native_alternative = "msgspec — this project's declared model system (#669; python/AGENTS.md 'Serialization: route every call through codec') — offers declarative constraints: `Annotated[str, msgspec.Meta(pattern=..., min_length=..., max_length=...)]`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/constraints.rst:61 (`Annotated[str, Meta(min_length=1, max_length=32, pattern=\"^[a-z_][a-z0-9_-]*$\")]`) — and the disqualifying line: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/structs.rst:317-321, \"the type annotations on a msgspec.Struct class are not checked at runtime during normal use. Types are only checked when *decoding* a serialized message when using a typed decoder.\""
rationale = "Justification, not a default: the native mechanism exists and does not reach this code. Every one of these three structs is CONSTRUCTED in-process by gather_*_inputs (p2996_hash.py:377, :435, :494) and never decoded from a serialized message, and msgspec constraints are decode-only by documented design. Porting to msgspec.Struct + Meta(pattern=...) would therefore delete the validation outright while looking like it declared it — a gate that can only pass, in the exact place the module says a bad value must raise rather than mint an unhittable cache tag (:238-241). msgspec's own escape hatch for this case is __post_init__ (structs.rst:150-170), i.e. the same hand-written check, so the port would buy nothing. The only genuinely native shape available is the one already in use: frozen dataclass + __post_init__. Worth one cleanup that is NOT a native swap — the 16-hex check at :167-176 is inlined instead of reusing a helper alongside _validate_hex_digest — but that is internal duplication, not superseded custom code."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && uv run --project python python -c \"import msgspec;from typing import Annotated;C=Annotated[str,msgspec.Meta(pattern='^[0-9a-f]{64}$')];S=msgspec.defstruct('S',[('d',C)]);s=S(d='nope');print('construct accepted bad value:',s.d);\\nimport traceback\\ntry: msgspec.convert({'d':'nope'},type=S)\\nexcept msgspec.ValidationError as e: print('decode rejected:',e)\""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_hash-7"
unit = "mod-p2996_hash"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The unit as a whole: a 3-tier content-addressed cache scheme (SCHEMA_VERSION-stamped canonical record joined by ASCII 0x1f, sha256 truncated to 16 chars) that mints `:base-<h>` / `:p2996-<h>` / `:dev-<h>` registry tags, plus the sentinel-comment slicing of the Dockerfile (BASE_HASH_BEGIN/END, P2996_HASH_BEGIN/END) that keeps the two lower tiers independent."
evidence = "python/src/dotfiles_setup/p2996_hash.py:42-58, :388-403, :447-459, :511-526, :294-319 (sentinel slicing); the sentinels live at .devcontainer/Dockerfile:32,401,403,545 and are contract-pinned by python/verification/suites.toml:496; consumed by .github/workflows/build-publish.yml:181 (base), :330 (p2996), :1124 (dev-tag) and .github/actions/dev-cache-probe/action.yml:51."
native_alternative = "Two native mechanisms cover parts of this: (a) BuildKit's own content-addressed layer cache via bake `cache-from`/`cache-to` (`type=gha`), which this repo already declares; (b) GitHub Actions' built-in `hashFiles()` expression for file-content fingerprints."
alternative_evidence = "(a) .agent/kb/raw/mod-p2996_hash-native-probes.md P1/P6 — live `bake --print dev` shows `cache-from`/`cache-to` `{\"type\":\"gha\",\"scope\":\"dotfiles-dev-linux-amd64-v2\"}` already configured; (b) `hashFiles()` is in use in this repo at .github/workflows/image-analysis.yml:170."
rationale = "Justification, not a default. (a) BuildKit's layer cache operates INSIDE a build: the ~80-120 min p2996 job would still have to start, pull its cache manifest and walk the graph. These hashes decide whether the GHA job runs at all — build-publish.yml probes the registry for the tag and short-circuits the whole leg (workflows/AGENTS.md:71-84). Layer cache also cannot express the property `:dev-<hash>` exists for: 'this exact content PASSED SMOKE', a marker pushed only after smoke-test, which is the entire reason a dev-probe hit is safe to skip validation on (p2996_hash.py:143-146). No BuildKit primitive carries a post-hoc validation verdict. (b) `hashFiles()` fingerprints whole files by glob and has no way to hash a SLICE of a file between sentinel comments — and section independence is the point of the design (the docstring's 'edit any Dockerfile line and the entire 2.5h pipeline rebuilds' anti-pattern), nor can it resolve a bake variable. It also runs only inside a GHA expression, while `mise run p2996-hash` must give an operator the same value locally. So the scheme stays. What findings 1-3 and 5 argue is narrower and unchanged by this: every INPUT it reads should be read through the tool that owns that input's grammar."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && sed -n '65,90p' .github/workflows/AGENTS.md && grep -n 'dev-tag\\|smoke-test' .github/workflows/build-publish.yml | sed -n '1,12p'"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_refresh-1"
unit = "mod-p2996_refresh"
kind = "module"
disposition = "retire"
confidence = 0.85
custom_thing = "Hand-rolled resolution of the latest HEAD SHA of the tagless `p2996` branch: `_default_runner` shells out to `git ls-remote <repo> refs/heads/p2996`, `fetch_latest_ref` parses the first field of the first line, and `_validate_sha` asserts a 40-char lowercase-hex SHA-1."
evidence = "python/src/dotfiles_setup/p2996_refresh.py:82-96 (_default_runner subprocess git ls-remote), :99-122 (fetch_latest_ref parse), :67-79 (_validate_sha), :40-43 (CLANG_P2996_REPO/TRACKED_BRANCH constants)"
native_alternative = "Renovate's `git-refs` datasource in digest mode: put the branch name in `currentValue` and capture the SHA as `currentDigest`, and Renovate resolves the branch HEAD digest itself. It is ALREADY wired in this repo (`datasourceTemplate: \"git-refs\"`, `currentValueTemplate: \"p2996\"`, `packageNameTemplate: https://github.com/bloomberg/clang-p2996`) and is demonstrably running in production."
alternative_evidence = ".agent/kb/raw/renovate-datasource-git-refs.md:149 (\"The git-refs datasource returns a reference from a Git repository.\") and :151 (\"To fetch the latest digest of a reference instead of the named reference: put the named reference in currentValue and match on the currentDigest.\"), saved verbatim from https://docs.renovatebot.com/modules/datasource/git-refs/ (http-status 200, fetched 2026-09-09T10:22:42Z). Live wiring: renovate.json:110-124. Live proof it runs: commit e85c35b \"Update bloomberg/clang-p2996 digest to f349a2d (#904)\"."
rationale = "The upstream-resolution half of this module is fully covered by a native Renovate datasource that is already configured and has already opened and merged a bump PR against this repo. Nothing in `fetch_latest_ref`/`_validate_sha` adds anything the `git-refs` datasource plus the `[a-f0-9]{40}` capture group does not do. Retire this half ONLY together with finding 2 — the rewrite half is not yet covered, so deleting the module before renovate.json is fixed would leave the authoritative pin with no writer at all."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_refresh-2"
unit = "mod-p2996_refresh"
kind = "module"
disposition = "refactor"
confidence = 0.90
custom_thing = "Regex rewrite of the pinned SHA inside the `variable \"CLANG_P2996_REF\" { default = \"...\" }` block of docker-bake.hcl, with a `count != 1` assertion so a renamed/removed pin cannot silently no-op."
evidence = "python/src/dotfiles_setup/p2996_refresh.py:47-51 (_PINNED_REF_PATTERN matching the HCL variable block), :131-145 (replace_pinned_ref + the exactly-one assertion), :158-169 (refresh writes docker-bake.hcl only on change)"
native_alternative = "Renovate's `custom.regex` manager entry for clang-p2996, which declares BOTH `docker-bake.hcl` and `.devcontainer/Dockerfile` in `managerFilePatterns` and is intended to bump them in lockstep — the mechanism the refresh.yml comment says replaced this job."
alternative_evidence = "renovate.json:110-124 (the customManager; `managerFilePatterns` lists `/docker-bake\\.hcl$/` at :115 and `/\\.devcontainer/Dockerfile$/` at :116; `matchStrings` at :118). Retirement claim: .github/workflows/refresh.yml:21-23 (\"p2996-refresh was retired in favor of the Renovate git-refs manager, which bumps CLANG_P2996_REF in docker-bake.hcl + Dockerfile in lockstep\"). Datasource semantics: .agent/kb/raw/renovate-datasource-git-refs.md:151-164."
rationale = "The native replacement DOES NOT actually cover docker-bake.hcl, so the retirement was only half-true. Control-armed probe: the configured pattern `CLANG_P2996_REF[\"= ]+(?<currentDigest>[a-f0-9]{40})` yields ['f349a2d8...'] against .devcontainer/Dockerfile and [] against docker-bake.hcl, while two control patterns known to be present match both files (`default\\s*=\\s*\"([0-9a-f]{40})\"` -> ['7220baff...'] in bake; `ARG CLANG_P2996_REF=([0-9a-f]{40})` -> ['f349a2d8...'] in the Dockerfile), so the probe discriminates. Cause: in HCL the token order is `CLANG_P2996_REF\" {\\n  default = \"<sha>`, and `{` / newline are outside the `[\"= ]` character class. Git history confirms the consequence: docker-bake.hcl:101 still carries 7220baff from commit 6593d80 (#138, the last run of THIS module), while Renovate's e85c35b (#904) moved only the Dockerfile to f349a2d8. docker-bake.hcl is the authoritative pin — it feeds `_extract_bake_variable` in p2996_hash.py:439 and image.py:99, i.e. the `:p2996-<hash>` cache key and the bake `args` — so the pin the real build uses has gone unmaintained since #138 while Renovate bumped a Dockerfile ARG that bake overrides. Correct move: widen renovate.json's `matchStrings` with a second entry for the HCL form (the ubuntu customManager at renovate.json:140-149 is the existing two-file precedent), verify with `mise run renovate-dryrun`, and only then delete this module. If the regex is not fixed, this module must be KEPT and re-wired, not deleted."
probe = "uv run --project python python -c \"import re,pathlib;p=re.compile(r'CLANG_P2996_REF[\\\"= ]+(?P<d>[a-f0-9]{40})');print({f:p.findall(pathlib.Path(f).read_text()) for f in ['docker-bake.hcl','.devcontainer/Dockerfile']})\" && mise run renovate-dryrun -- --json"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_refresh-3"
unit = "mod-p2996_refresh"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "The module's public surface is still fully wired as an on-demand command — a `p2996-refresh` argparse subcommand, a `_p2996_refresh` dispatch entry printing `RefreshResult.as_json()`, a `[tasks.p2996-refresh]` mise task, and a 125-line test file — even though the CI job that consumed the JSON was retired."
evidence = "python/src/dotfiles_setup/main.py:1745-1749 (subparser), :2343-2344 (_p2996_refresh), :2518 (dispatch table), :104 (import); mise.toml:1354-1356 ([tasks.p2996-refresh], whose description still asserts \"the scheduled refresh.yml workflow's p2996-refresh job does this in CI\"); tests/test_p2996_refresh.py:1-125; .devcontainer/P2996-CACHE.md:92-99 and :111-114 (same stale claim). Retirement: .github/workflows/refresh.yml:21-23 — and `grep -rn p2996 .github/workflows/` returns zero references to a p2996-refresh job (control arm: the same grep returns 40+ hits for the live p2996-prep/p2996-hash chain, e.g. .github/workflows/build-publish.yml:276)."
native_alternative = "GitHub-native dependency automation (Renovate) owning the pin end-to-end, as the repo already decided for the other five git/digest pins in renovate.json's `customManagers`; the manual on-demand question \"has upstream advanced?\" is answered natively by Renovate's `dryRun` lookup, already wrapped as `mise run renovate-dryrun`."
alternative_evidence = ".agent/kb/raw/renovate-config-options.md:15422-15424 (`dryRun` semantics: \"PRs won't be created\", `dryRun=lookup`); local wrapper at mise.toml:844-877 (`[tasks.renovate-dryrun]`, `run = 'uv run --project python dotfiles-setup renovate-dryrun'`, `--check` / `--json` modes documented at mise.toml:850-852)."
rationale = "Three docs (mise.toml:1355, .devcontainer/P2996-CACHE.md:92-99, p2996_refresh.py:16-18) all still describe a CI job that no longer exists — the module is an orphan whose own docstring is the stalest description of it. Worse, running `mise run p2996-refresh` TODAY is actively harmful under the current split: it rewrites only docker-bake.hcl and would move the authoritative pin to upstream HEAD while leaving the Dockerfile ARG at whatever Renovate last wrote, deepening the divergence finding 2 documents. The refactor is a single decision with two acceptable ends — (a) fix renovate.json per finding 2, then delete module + test + mise task + subcommand + the three doc claims in one change; or (b) keep the module as the single writer and make it rewrite BOTH files, dropping the clang-p2996 customManager so there is one writer. Either way the current state — two half-writers and three stale docs — must not persist. Not 'retire' outright, because deleting the module while renovate.json cannot see docker-bake.hcl would leave the pin with no writer at all."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_refresh-4"
unit = "mod-p2996_refresh"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "The module's implicit single-file contract — its docstring asserts \"The SHA stays pinned in docker-bake.hcl\" and `refresh()` writes only that file — while the Dockerfile carries a duplicate `ARG CLANG_P2996_REF` whose lockstep with the bake default is stated only in a code comment and enforced nowhere."
evidence = "python/src/dotfiles_setup/p2996_refresh.py:8-14 (docstring: SHA stays pinned in docker-bake.hcl), :158-167 (writes only bake_path); .devcontainer/Dockerfile:421 (\"Reconciled to match docker-bake.hcl's CLANG_P2996_REF default (#160 T3)\") and :426 (`ARG CLANG_P2996_REF=f349a2d8...`) vs docker-bake.hcl:100-102 (`default = \"7220baff...\"`) — the invariant is currently BROKEN."
native_alternative = "This repo's own native contract engine: a `[[suite]]` in python/verification/suites.toml with `per_path_tokens` (or a `_add_consistency_subcommands` cross-file consistency check) run by `mise run verify` / CI `contract-preflight` — the same mechanism that already guards the other CLANG_P2996_REF wiring facts."
alternative_evidence = "python/verification/suites.toml:433-443 (`build.clang-p2996-ref-in-bake`, `handler = require_tokens`, `per_path_tokens` over docker-bake.hcl) — it asserts the variable EXISTS and is threaded into args, but says nothing about its VALUE matching the Dockerfile; python/src/dotfiles_setup/main.py:1750 (`_add_consistency_subcommands`) is the existing home for cross-file value checks. Control arm for the absence: `grep -n p2996 python/verification/suites.toml` returns 9 hits, none naming p2996-refresh or a bake/Dockerfile value comparison."
rationale = "The divergence in finding 2 survived a full Renovate bump cycle precisely because no gate compares the two pins — an unenforced comment is the weakest possible layer, and it failed silently. The native mechanism is already in the repo and already names this exact variable, so the fix is a contract, not new code: add a consistency check asserting docker-bake.hcl's `CLANG_P2996_REF` default equals `.devcontainer/Dockerfile`'s `ARG CLANG_P2996_REF`. That gate is what makes either end of finding 3 safe to land, and it is the thing that would have caught #904 the day it merged."
probe = "uv run --project python dotfiles-setup verify run 2>&1 | tail -5   # then, after adding the suite, mutate .devcontainer/Dockerfile:426 to a different SHA and confirm the suite FAILS (fail-arm required per probes-need-a-control-arm)"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-p2996_refresh-5"
unit = "mod-p2996_refresh"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "Dependency-free upstream fetch: `subprocess.run(['git','ls-remote', ...])` deliberately instead of an HTTP client, with a `runner` callable injected as the test seam so the whole module is unit-testable offline."
evidence = "python/src/dotfiles_setup/p2996_refresh.py:88-96 (subprocess git ls-remote, stderr logged on CalledProcessError), :99-113 (`runner` test seam); the deliberateness is recorded in python/src/dotfiles_setup/schema_vendor.py:317 (\"gcc_sha._default_fetcher, p2996_refresh's git ls-remote — no HTTP client\") and python/src/dotfiles_setup/gcc_sha.py:20,:119 (\"Mirrors p2996_refresh.py\")."
native_alternative = "None that preserves the constraint. `gh api repos/bloomberg/clang-p2996/commits/p2996` would resolve the same SHA natively via the gh CLI, but it swaps a zero-dependency, unauthenticated git protocol call for an authenticated GitHub API call and drops the offline test seam."
alternative_evidence = "No corpus doc supports a stronger native path for this specific constraint; the repo-internal record of the constraint is python/src/dotfiles_setup/schema_vendor.py:317. Control arm on the corpus: the gh source tree at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh documents `gh api` but offers nothing that removes the auth dependency for an unauthenticated ref lookup."
rationale = "Justification (required, not a default): if finding 3 lands on option (b) — the module stays as the single writer of both files — then this fetch mechanism is the right one and should not be swapped for `gh api`. `git ls-remote` needs no token, no HTTP library, and no network in tests thanks to the `runner` seam, and it is the house pattern shared with `gcc_sha.py`. If finding 3 lands on option (a) this finding is moot because the module is deleted wholesale; it is recorded so that a future session choosing (b) does not 'modernize' the fetch into a `gh api` call and thereby introduce an auth dependency into a scheduled job."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-parity-1"
unit = "mod-parity"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "`enabled_plugins()` decides a plugin is ENABLED iff `.claude/settings.json` -> `enabledPlugins[name] is True` in that ONE file, and `find_parity_gaps` turns anything else into a hard GAP."
evidence = "python/src/dotfiles_setup/parity.py:84-101 (`settings = repo_root / \".claude\" / \"settings.json\"`; `return {name for name, on in plugins.items() if on is True}`), consumed at python/src/dotfiles_setup/parity.py:146-150; pinned by tests/test_parity.py:106 `test_a_plugin_declared_false_counts_as_absent` and asserted as a contract token `\"if on is True\"` at python/verification/suites.toml:2098."
native_alternative = "Claude Code's own enablement resolution: `enabledPlugins` is read at ANY settings scope and a plugin with no entry at all falls back to `defaultEnabled`, which itself defaults to `true`; `claude plugin list --json` prints the resolved enable status machine-readably."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:4250 (\"A plugin with no entry at any scope falls back to its defaultEnabled value\"); .../plugins-reference.md:524 (`defaultEnabled` \"Defaults to `true`\"); .../plugins-reference.md:1134-1148 (`claude plugin list [--json]`, \"List installed plugins with their version, source marketplace, and enable status\")."
rationale = "The custom reader is a STRICTER proxy than the harness's own rule, and the two disagree in both directions. A plugin that is genuinely active because nothing set it (defaultEnabled true) carries no `enabledPlugins` entry, so this gate reports it as a missing plugin and turns `main` red on a repo that is in fact compliant; conversely an entry written at user scope (`~/.claude/settings.json`) is invisible here. Full retirement is wrong — the gate's subject is the COMMITTED, reviewable declaration in each repo's tree, and `claude plugin list` cannot speak for a sibling checkout on a CI runner where nothing is installed. The refactor is to (a) say in the docstring that the predicate is 'declared true in committed project settings', not 'enabled', and (b) where the CLI is available, cross-check with `claude plugin list --json` so a defaultEnabled-only plugin is distinguishable from an absent one rather than collapsing to the same GAP."
probe = "claude plugin list --json | jq '.[] | {name, enabled}'  # then compare against `jq '.enabledPlugins' .claude/settings.json` in both repos; a plugin present in the CLI output but absent from the file is exactly the false-GAP case"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-parity-2"
unit = "mod-parity"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "The cross-repo parity gate itself: `find_parity_gaps` / `run` compare a declared set (`parity.toml` -> plugins, whole CLAUDE.md lines, `.claude/rules/` stems) across dotfiles AND the sibling knowledge-base checkout, hard-FAILing in CI and loudly SKIPping locally."
evidence = "python/src/dotfiles_setup/parity.py:133-163 (`find_parity_gaps`), :207-250 (`run`, CI-vs-local asymmetry at :226-234); wired at mise.toml:1220-1227 (`[tasks.parity]`) and .github/workflows/ci.yml:192-226 (sibling checkout into `.parity/knowledge-base` + `KB_REPO_PATH` + `mise run parity`); contract `eval.cross-repo-parity` at python/verification/suites.toml:2079-2111."
native_alternative = "None. The three candidates all fail on inspection: (1) a Claude Code PLUGIN cannot carry the doctrine — \"A `CLAUDE.md` file at the plugin root is not loaded as project context\", and the component table ships skills/agents/hooks/workflows/output-styles but no `rules/`; a plugin's own bundled `settings.json` supports only the `agent` and `subagentStatusLine` keys, so it cannot enable other plugins either. (2) User-scope `~/.claude/settings.json` does apply to \"every project on your machine\", which would make the PLUGINS axis structurally true — but only on one machine, and not as a committed declaration. (3) agnix knows `enabledPlugins` as a settings field to validate within one tree, but neither it nor cclint/agents-lint has any cross-repo mode."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:902 (plugin CLAUDE.md is not project context) and :906-920 (component/file-locations table; Settings row: \"Only the `agent` and `subagentStatusLine` keys are supported\"); .../settings.md:486 (`~/.claude/settings.json`: \"every project on your machine, and nothing on your teammate's or in the cloud session\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/knowledge-base/agent-config-optional-fields.md:222 (`enabledPlugins` | object | ... | \"Cross-reference with installed plugins\") — single-tree only; control arm for the agnix/cclint/agentlinter sweep: `grep -rIl enabledPlugins` returned 1 agnix file and 1 cclint file while the control term `claude` returned files in all three trees, so the 'no cross-repo mode' result is a real absence, not a broken grep."
rationale = "Required justification: no harness, CLI, linter or CI feature at the host's versions observes TWO repository trees at once and asserts a declared set holds in both. The distribution mechanisms that could make the check unnecessary (plugin packaging, user-scope settings) each cover at most the plugins axis, and none can carry `.claude/rules/*.md` or `.claude/CLAUDE.md` lines — the two axes that actually carry the doctrine. The CI-vs-local asymmetry (hard FAIL when the sibling checkout is missing) is policy no tool provides. Keep the module."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-parity-3"
unit = "mod-parity"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "`declared_rules()` + the `rules` axis: parity by rule STEM (`.claude/rules/<stem>.md` exists in both repos), deliberately NOT by content."
evidence = "python/src/dotfiles_setup/parity.py:112-123 (`declared_rules`, docstring: \"Presence, never content\"), gap emission at :157-162; the 22 declared stems at parity.toml:63-86 with the design note at parity.toml:54-58 (\"ADAPTED per repo ... Gating on byte-equality would force one repo to carry the other's false statements\"); pinned by tests/test_parity.py:239 `test_rule_parity_is_by_stem_not_by_content`."
native_alternative = "Claude Code natively supports SYMLINKED rules: \"The `.claude/rules/` directory supports symlinks, so you can maintain a shared set of rules and link them into multiple projects\" — one shared rule source linked into both repos, which removes drift by construction instead of detecting it."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:248-256 (\"#### Share rules across projects with symlinks\" + `ln -s ~/shared-claude-rules .claude/rules/shared`)."
rationale = "Required justification: the native mechanism exists and does NOT fit, for a stated design reason. Symlinking yields byte-identical rules, which is precisely what parity.toml:54-58 rejects — each rule is adapted per repo (knowledge-base's `local-devcontainer-first` is an ingestion-cost ladder; its `ci-local-parity` states `.github/` does not exist there), so a shared source would force one repo to carry the other's false statements. A symlink is also machine-local (`~/shared-claude-rules` is outside both clones), so it survives neither a fresh clone nor a CI checkout, while the gate's whole point is that CI can see the divergence. Keep the stem-presence check; do not convert the rules tree to symlinks."
probe = ""
correction = ""
correction_custom = "**⚠ CITATION CORRECTED (round 5) — RANGE END PAST EOF.** `parity.toml:63-86` overshoots by four: the file is **82** lines and the rules array closes with `]` at `:82`. Read it as **`:63-82`**. Found by the round-5 mechanical anchor sweep."
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-parity-4"
unit = "mod-parity"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "`_claude_md_lines()` + the `lines` axis: whitespace-normalised whole-line presence of the orchestration trigger/mode lines in each repo's `.claude/CLAUDE.md`, as the proxy for 'the doctrine is live in this repo'."
evidence = "python/src/dotfiles_setup/parity.py:70-71 (`_normalise`), :126-131 (`_claude_md_lines`), :151-156 (line gaps); the two gated lines at parity.toml:41-44; pinned by tests/test_parity.py:140 `test_a_narrated_trigger_line_is_still_a_gap` and :152 `test_line_matching_tolerates_reindentation`."
native_alternative = "The `InstructionsLoaded` hook event fires when a `CLAUDE.md` or `.claude/rules/*.md` file is actually loaded into context (session start and lazy loads) — a runtime observation of whether an instruction reached the model, which this repo already runs."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:56 and :1263 (\"Fires when a `CLAUDE.md` or `.claude/rules/*.md` file is loaded into context ... runs asynchronously for observability purposes\"); already wired here at .claude/settings.json:119 -> python/src/dotfiles_setup/instructions_observer.py:2."
rationale = "Required justification: `InstructionsLoaded` is the STRONGER signal for the question 'is this doctrine actually in context', and it is already adopted for this repo (instructions_observer/instructions_report) — but it is per-session and per-machine. It cannot observe the sibling repo's sessions, and CI (where this gate must fail) runs no interactive session at all, so there is no hook event to read. Static whole-line presence in the committed `.claude/CLAUDE.md` remains the only cross-repo, CI-observable signal. No refactor: routing the lines axis through the observer would make the gate unable to fail in the one environment that matters."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-parity-5"
unit = "mod-parity"
kind = "module"
disposition = "refactor"
confidence = 0.45
custom_thing = "The gate's trigger surface: `mise run parity` runs ONLY from dotfiles (its `[tasks.parity]` and the `contract-preflight` job), so a knowledge-base-side regression is detected only when a dotfiles PR/nightly happens to run — the knowledge-base repo has no parity task of its own."
evidence = "mise.toml:1220-1227 and .github/workflows/ci.yml:192-226 are the only invocation sites; the sibling repo has no counterpart — `grep -n parity /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/mise.toml` returns only three unrelated comment hits (lines 413, 685, 1485) and no `[tasks.parity]`, with the control arm being this repo's own `grep -n parity mise.toml`, which does return `[tasks.parity]` at :1220."
native_alternative = "GitHub Actions' native cross-repo triggers — a `schedule` cron and/or `repository_dispatch` — so the two-repo gate runs on a clock (and on a knowledge-base push) rather than only when dotfiles happens to build. Both trigger types are already in use in this repo, so no new mechanism is needed."
alternative_evidence = ".github/workflows/ci.yml:3-10 (`schedule: - cron: \"0 2 * * *\"`) and .github/workflows/build-publish.yml:8 (`refresh/repository_dispatch callers`) — in-repo proof the native triggers work here. NOTE (honest gap): GitHub Actions' own documentation is NOT in the offline corpus for this audit, so this citation is an in-repo demonstration rather than a vendor doc path; the probe below settles the remaining question, which is whether the knowledge-base repo has CI that could host the reciprocal call."
rationale = "The gate's LOGIC is sound (finding mod-parity-2) but its coverage is one-directional in time: `main` in knowledge-base can drop a declared rule and stay green in its own CI indefinitely, because nothing there runs the gate and nothing in dotfiles is triggered by a KB push. The nightly `ci.yml` cron already narrows the window to ~24h — which may be the accepted answer — but the module is invoked from exactly one repo, and that asymmetry is not stated anywhere in parity.py's docstring. Refactor = either document that the nightly IS the reciprocal coverage, or add a knowledge-base-side task/`repository_dispatch` so a KB push exercises the same gate. No code change to the comparison itself."
probe = "gh api repos/ray-manaloto/knowledge-base/actions/workflows --jq '.workflows[] | {name, path, state}'   # does KB have CI that could run the reciprocal gate at all?"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-path_drift-1"
unit = "mod-path_drift"
kind = "module"
disposition = "keep"
confidence = 0.82
custom_thing = "The core capability: compare the AMBIENT shell's $PATH mise-install entries against mise's current active resolution and report per-tool VERSION drift (`compare()` / `Drift` / `Report`), wired as doctor check `path-drift` and CLI `dotfiles-setup path-drift`."
evidence = "python/src/dotfiles_setup/path_drift.py:256-276 (compare), :295-326 (check_path_drift), :119-133 (Drift.describe), python/src/dotfiles_setup/doctor.py:1159-1187 + :1207 (registration), python/src/dotfiles_setup/main.py:750-765 + :2427-2433, python/verification/suites.toml:2213."
native_alternative = "mise's own `mise doctor` PATH checks: `check_path_ordering` (warns 'mise tool paths are not first in PATH') and `check_shim_shadowing` ('mise shims are shadowed by executables earlier in PATH'). These are the only native probes that read the process's real inherited PATH (`env::PATH_NON_PRISTINE`) rather than mise's own recomputed one."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/doctor/mod.rs:931-999 (check_path_ordering; :951 reads env::PATH_NON_PRISTINE, :995 warning text), :1001-1049 (check_shim_shadowing); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/env.rs:785-792 (PATH = pristine vs PATH_NON_PRISTINE = the process's real PATH); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor.md:1-17 (`mise doctor [-J --json]`, only subcommand is `path`)."
rationale = "Neither native check answers the module's question. check_path_ordering reports PRECEDENCE (is a non-mise dir ahead of mise's dirs) and never compares versions, returns early on `!env::is_activated()` and returns early again when NO mise path is found in the inherited PATH -- which is the exact shape of a badly drifted shell; check_shim_shadowing runs only when NOT activated and shims are on PATH, and the module already excludes shims by construction (path_drift.py:63-66). mise's cause-side fixes do not reach the case either: the reactivation drop of stale install dirs (#10162, v2026.5.18) only fires when hook-env decides to do a full run, gated on loaded-config / tera / watch_files mtimes (src/hook_env.rs:274-300), and the mise x/run/env child-PATH filter (#10345/#10422) repairs the CHILD, which is precisely why a probe run under `mise run` is blind. A long-lived agent process (Claude Code and its Bash tool) inherits a frozen PATH and never runs a prompt hook, so nothing native reactivates it. Control arm on the 'nothing native' claim: the same grep shape over src/cli/doctor/mod.rs returns 10 PATH hits and 60 warn/check hits, so a 0-result would have been informative -- the two PATH checks it DID find are the ones assessed above. Gap digests confirm no newer capability: mise has zero releases newer than the host's 2026.9.3 (.agent/kb/raw/mise-releases-since-pin.digest.md), hk 1.58.1 is docs-only (.agent/kb/raw/hk-releases-since-pin.digest.md), and Claude Code 2.1.258-2.1.266 shipped nothing env/PATH-related (.agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-path_drift-2"
unit = "mod-path_drift"
kind = "module"
disposition = "refactor"
confidence = 0.66
custom_thing = "The ACTIVE side of the comparison: `mise ls --current --json`, then treating each entry's `install_path` as `<installs_root>/<slug>/<version>` and re-deriving the installs root from those paths (`MISE_LS_COMMAND`, `active_tools`, `path_versions`, `_SLUG_VERSION_DEPTH`)."
evidence = "python/src/dotfiles_setup/path_drift.py:94-98 (MISE_LS_COMMAND, _SLUG_VERSION_DEPTH), :210-232 (active_tools reads install_path, splits parent.name/name, derives root), :235-253 (path_versions parses $PATH entries the same way)."
native_alternative = "`mise doctor path` (alias `paths`) prints exactly the PATH entries mise is providing -- it calls `ts.final_env()` + `ts.list_final_paths()`, the same code path activation uses to build PATH. Comparing the ambient PATH against that output is a set difference in the SAME path vocabulary, with no `<slug>/<version>` parsing and no installs-root reconstruction."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor/path.md:2-23 (`mise doctor path [-f --full]`, effect read-only, sample output is `installs/<tool>/<ver>/bin` lines); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/doctor/path.rs:14-31 (list_final_paths); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/toolset/toolset_paths.rs:96-121 (list_final_paths = config path dirs + uv venv + tool_add_paths + list_paths)."
rationale = "`install_path` is not the path activation puts on PATH, so the two sides of the comparison currently speak different vocabularies and that opens two defect classes. (a) FALSE DRIFT on a fuzzy, unlocked pin: mise puts the requested-version SYMLINK dir on PATH (`~/.local/share/mise/installs/python/3.15/bin`) while `install_path` reports the resolved patch dir, so the version sets share nothing and compare() reports drift that does not exist -- see /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/dev-tools/shims.md:35 and src/toolset/tool_version.rs:243-249 + :339-349 (`runtime_path()` returns install_path only when `self.locked`). This repo's lockfiles mask it today, which is exactly what makes it a latent trap rather than a visible bug. (b) The `rust` anomaly the module documents (path_drift.py:60-63) is an artifact of the same mismatch: a tool whose real bin dir is not under the installs root can never match, so it is reported as 'absent' rather than recognised. Refactor the active side to parse `mise doctor path` output (still one subprocess, still enumerating rather than asserting a tool list) and keep `mise ls --current --json` only if the human-readable tool name (`npm:renovate` vs the slug) is still wanted for the finding text."
probe = "mise ls --current --json | jq -r 'to_entries[] | .value[]? | .install_path' | sort > /tmp/a; mise doctor path | sort > /tmp/b; diff /tmp/a /tmp/b; echo rc=$?   # any tool whose final path is not <install_path>/... is a case the current parser mis-handles"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-path_drift-3"
unit = "mod-path_drift"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`absent_findings()` -- active mise tools with no $PATH entry at all, kept as an opt-in reporting mode."
evidence = "python/src/dotfiles_setup/path_drift.py:279-292; no production caller (grep for `absent_findings` across the repo returns only its definition, its docstring reference at :63, and tests/test_path_drift.py:199-202)."
native_alternative = "mise doctor already owns the 'a tool mise provides is not reachable from this shell' question, in the two forms that actually happen: 'mise shims are not on PATH' (src/cli/doctor/mod.rs:469-478) and the activation/ordering warning at :973-999. The residual case the custom function reports is not that -- it is a parsing artifact, and `mise doctor path` (finding 2) removes it."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/cli/doctor/mod.rs:469-478 (shims-not-on-PATH warning), :973-999 (no mise path found in current PATH -> deferred to the activation check); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor/path.md:9."
rationale = "Dead surface with a measured 1-in-148 signal that the module itself calls too noisy to report -- and that single signal (`rust`) is the install_path-vs-final-path artifact from finding 2, not a real absence. Delete it with the finding-2 refactor, or keep it only after re-measuring against `mise doctor path`, where an absent entry would mean something. Not a 'retire' on native-supersession grounds: mise's warnings cover the real absence cases, but they are not a drop-in for this function's output."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-path_drift-4"
unit = "mod-path_drift"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The ambient-PATH capture seam and its blindness detector: `DOTFILES_AMBIENT_PATH` set by the SessionStart hook, `MISE_TASK_NAME` as the non-circular 'mise rewrote this environment' marker, `Provenance.EXPLICIT/INHERITED/BLIND`, exit code 2 for blind, and the contract that pins the hook prefix."
evidence = "python/src/dotfiles_setup/path_drift.py:85-92 (AMBIENT_PATH_ENV, MISE_TASK_MARKER), :108-117 (Provenance), :158-177 (resolve_ambient_path), :329-334 (BLIND_ADVICE), :364-366 (rc 2); .claude/settings.json:102 (`DOTFILES_AMBIENT_PATH=\"$PATH\" mise -C ... run doctor`); python/verification/suites.toml:2213-2229 (per_path_tokens binds the hook prefix and `return inherited, Provenance.BLIND`)."
native_alternative = "None found. Claude Code's SessionStart command hook is the native mechanism the repo already uses to run the capture, and nothing in the harness exposes a pre-activation PATH; on the mise side the only pristine-PATH accessor (`env::PATH` / `__MISE_ORIG_PATH`) holds the PRE-activation environment with zero installs/ entries, and `env::PATH_NON_PRISTINE` is internal to the mise binary, reachable only by running `mise doctor` itself directly from the ambient shell."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:21 + :37 + :310 (SessionStart fires once per session, matchers startup|resume|clear|compact|fork; command hooks are the only mechanism); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/env.rs:785-792 (PATH from PRISTINE_ENV vs PATH_NON_PRISTINE from the live var); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 new capabilities 2.1.258-2.1.266, none touching hook environment or PATH)."
rationale = "Required justification: the capability is 'know when the probe cannot see the shell it is asked about', and it exists only because mise deliberately repairs PATH for every child it spawns (src/toolset/toolset_env.rs:20-31, CHANGELOG.md:3069 #10422). No harness or tool feature hands a child its parent's pre-activation PATH, so the capture must be done by the caller and the blindness branch must exist or the check becomes a probe with one face. The only structural alternative is to stop invoking the doctor through `mise run` (call `mise doctor` or the dotfiles CLI straight from the ambient shell), which would trade the capture for losing mise-managed tool resolution for the doctor itself -- a worse trade, and it still would not supply the version comparison of finding 1."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-path_drift-5"
unit = "mod-path_drift"
kind = "module"
disposition = "unknown"
confidence = 0.50
custom_thing = "The whole module exists because PATH activation can hold a stale `installs/<tool>/<ver>/bin` entry in a long-lived shell."
evidence = "python/src/dotfiles_setup/path_drift.py:2-16 (four measured occurrences, #596), :63-66 ('Shims are excluded by construction ... resolves the version at exec time, so it is never stale'); home/dot_zshrc.tmpl:53 and home/dot_bashrc.tmpl:26 both use PATH activation (`eval \"$(mise activate zsh|bash)\"`), not `--shims`."
native_alternative = "`mise activate --shims`: a single shims directory on PATH whose entries resolve the active version at exec time, so no versioned install dir is ever on PATH and the drift class cannot occur."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/dev-tools/shims.md:3-8 (the three activation methods), :35 (PATH activation puts `installs/<tool>/<ver>/bin` on PATH, possibly as a fuzzy symlink); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/troubleshooting.md:143 ('Shims keep your shell's Path from growing ... Shims also do not support all the features of mise activate')."
rationale = "A native mechanism exists that would retire the entire module rather than refactor it, but adopting it is a host-wide activation change with documented feature loss (env directives / hooks that only PATH activation provides), and the Mac host's own rc file is outside this repo (the two rc templates here are the chezmoi devcontainer surface). This is a judgement call for the operator, not something the audit can settle from the corpus: it needs a live check of what the host shell actually does and whether shims break the repo's flows. Until that is answered the detection module is the right thing to keep."
probe = "grep -n 'mise activate' ~/.zshrc; command -v hk | sed 's|/[^/]*$||'   # does the interactive host shell use PATH activation, and does the resolved binary sit under installs/ (PATH activation) or shims/ (shims activation)?"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-plan_attest-1"
unit = "mod-plan_attest"
kind = "module"
disposition = "refactor"
confidence = 0.68
custom_thing = "The write-form hazard is carried by PROSE only: the module's docstring and the mise task description both warn that the bare `mise run plan-attest` WRITES (it locked a tampered plan over the operator's hash during a smoke test), but nothing in the code or the task asks the operator to confirm before the write. `plan_attest_main` passes argv straight to `sh <script>` with no gate (python/src/dotfiles_setup/plan_attest.py:109-119), and `[tasks.plan-attest]` is a bare `run = 'uv run --project python dotfiles-setup plan-attest'` (mise.toml:918-921)."
evidence = "python/src/dotfiles_setup/plan_attest.py:27-33 (the \"bare form WRITES\" warning), python/src/dotfiles_setup/plan_attest.py:118 (`subprocess.run([\"sh\", str(script), *args], check=False)` — no confirmation), mise.toml:918-921 (task has `description` + `run`, no `confirm`)"
native_alternative = "mise's task-level `confirm` field: `confirm = { message = \"...\", default = \"no\" }` on `[tasks.plan-attest]`. mise prompts before the task's own `run` executes; with no TTY the run HARD-FAILS (`Confirmation::Unavailable` -> \"task requires confirmation but there was nobody to ask; pass --yes to accept\"), and only an explicit `--yes`/`MISE_YES` skips the prompt (`Settings::get().yes`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:349-366 (`confirm` type + `{ message, default }` form, `default = \"no\"`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/src/task/task_executor.rs:1876-1909 (`!Settings::get().yes` gate; `Confirmation::No` -> \"aborted by user\"; `Confirmation::Unavailable` -> error); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/schema/mise-task.json:63-82 (schema)"
rationale = "This is the exact accident the module documents: a non-interactive agent shell running the bare form and attesting. A `confirm` with `default = \"no\"` converts that into a hard error in any TTY-less context (the model's Bash tool), which is a second, mechanical layer under the permissions.deny rule — and unlike the deny it also protects an OPERATOR who typed the bare form by habit. It is a mise.toml change, not a module change, so the wrapper itself survives. Held below high confidence because the operator's sanctioned path is `! mise run plan-attest` (Claude Code shell mode) and it is unproven whether that child process gets a TTY; if it does not, `confirm` would break the very path this module exists to provide unless the operator adds `--yes` (which re-opens the bare-write hazard). Settle it with the probe before adopting."
probe = "printf '[tasks.confirm-probe]\\nconfirm = { message = \"probe?\", default = \"no\" }\\nrun = \"echo RAN\"\\n' >> mise.local.toml && (echo '--- non-tty arm (must FAIL) ---'; mise run confirm-probe </dev/null; echo \"rc=$?\") ; echo 'OPERATOR ARM: now type  ! mise run confirm-probe  in Claude Code shell mode and record whether it prompts or errors with \"nobody to ask\"'"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-plan_attest-2"
unit = "mod-plan_attest"
kind = "module"
disposition = "unknown"
confidence = 0.55
custom_thing = "Resolving the installed plugin's script path by scanning `~/.claude/plugins/cache/<marketplace>/<plugin>/` and taking the highest numeric version directory (`resolve_attest_script` -> `listing_budget.plugin_root`), with `installed_plugins.json` explicitly rejected on evidence."
evidence = "python/src/dotfiles_setup/plan_attest.py:71-94 (`resolve_attest_script`, `plugin_root(home, PLUGIN_ID)`), python/src/dotfiles_setup/listing_budget.py:122-158 (version-max rule; `installed_plugins.json` rejected as per-project and disagreeing with the observed load)"
native_alternative = "`claude plugin list --json` — the harness's own CLI listing of installed plugins, which is a different route from the rejected `installed_plugins.json` record file. Whether its JSON carries an install path is UNDOCUMENTED: the reference documents only \"version, source marketplace, and enable status\" plus the `--json`/`--available` flags, and no doc page in the corpus mentions an `installPath` field."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:1134-1150 (`plugin list` + `--json`); same file:781-796 (cache layout: one directory per installed version, orphaned versions swept ~14 days after an update); grep for `installPath` across the claude-code doc tree returns 0 hits while the control-arm token `plugin list` returns 5 files"
rationale = "Two things make this worth a probe rather than a shrug. (a) If `claude plugin list --json` emits the resolved install path, that is the harness's own answer to which directory this session actually loaded, and both this module and `listing_budget.plugin_root` should read it instead of re-deriving a version rule. (b) The version-max heuristic has a documented blind spot the docstring does not mention: an updated or uninstalled version's directory stays on disk for a ~14-day grace period (plugins-reference.md:792), so after a DOWNGRADE the highest-numbered directory is the orphaned one — and `resolve_attest_script` would then hand the operator a script from a version the session is not running. Nothing here is superseded until the probe says what the CLI returns at 2.1.266."
probe = "claude plugin list --json | jq '.[] | select(.name==\"planning-with-files\")' ; echo '--- control arm: does any key hold a cache path? ---' ; claude plugin list --json | jq -r '.. | strings | select(test(\"/.claude/plugins/cache/\"))' | head"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-plan_attest-3"
unit = "mod-plan_attest"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The wrapper as a whole: a `dotfiles-setup plan-attest` subcommand + `[tasks.plan-attest]` that locates a bundled script inside an installed plugin and execs it with every argument passed straight through (no argparse front end over upstream's `--show`/`--clear`), so the operator never types a version-pinned cache path."
evidence = "python/src/dotfiles_setup/plan_attest.py:97-119 (`plan_attest_main`, deliberate non-enumeration of upstream flags), python/src/dotfiles_setup/main.py:107,1277-1283,2364 (subcommand registration + dispatch), mise.toml:918-921 (the task)"
native_alternative = "None. Claude Code's plugin CLI has no path-printing or script-running subcommand — the documented set is `init, install, uninstall, prune, enable, disable, update, list, details, tag` (+ `validate`, `marketplace`). `${CLAUDE_PLUGIN_ROOT}` is substituted only in plugin-provided execution contexts (hook/MCP/command definitions inside a plugin), never exported to an unrelated shell or mise task."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:928-1209 (every `### plugin <verb>` heading — no `path`/`exec`/`run`); same file:693 and mcp.md:941 (`CLAUDE_PLUGIN_ROOT` is \"Set only when a plugin provides the server\" / applies to plugin-bundled scripts and config)"
rationale = "Required justification: the capability is \"give a human a stable, typeable command that runs a script living at a version-pinned path inside the plugin cache\", and the harness exposes no primitive for it — no subcommand prints the install directory (finding 2's probe is the only candidate, and even a positive result would only replace the resolver INSIDE this wrapper, not the wrapper), and the one native path variable is scoped to plugin-provided contexts. The passthrough-not-argparse choice is also correct against `.claude/rules/ai-cli-invocation.md`: enumerating upstream's flags would rot. Keep, with finding 1 layered on the task and finding 2 possibly replacing the resolver call."
probe = "claude plugin --help 2>&1 | grep -iE 'path|exec|root|where'   # control arm: the same command must show the known-present `list` and `details` verbs"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-plan_attest-4"
unit = "mod-plan_attest"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "The human-boundary enforcement this module is the escape hatch for: five `permissions.deny` rule pairs in .claude/settings.json covering both the bare and the argument-carrying form of every route to attestation, plus a presence check (`check_plan_attest_deny`) run by `hook selfcheck` on every ship/land."
evidence = ".claude/settings.json:30-40 (the ten deny rules), python/src/dotfiles_setup/hook_selfcheck.py:493-540 (`_ATTEST_DENY_BASES`, `check_plan_attest_deny`, and its stated limit), python/verification/suites.toml:1433-1466 (`workflow.plan-attest-operator-only`), tests/test_plan_attest.py:80-123 (both-forms parametrisation + half-written-ban control arm)"
native_alternative = "`permissions.deny` IS the native mechanism, and it is already the one adopted. The two candidate alternatives are strictly weaker here: the plugin's own `disable-model-invocation: true` gates the slash COMMAND only (the 2.1.x improvement merely tells Claude to ask the user rather than replicate the workflow), and a `permissions.ask` rule resolves to a PROMPT, which `bypassPermissions` mode — the mode this repo's sessions run in — skips, whereas deny rules \"still block ... outright in every mode\"."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permission-modes.md:378 (\"`permissions.deny` rules can still block pushes to specific branches outright in every mode\"); same file:422 (allow/ask/deny precedence; ask falls back to a permission prompt); permissions.md:87,90 (bypassPermissions \"Skips permission prompts\"); permissions.md:495 (a matching deny rule blocks the call regardless of a PreToolUse hook's decision — why this is not a hook); changelog.md:890 (`disable-model-invocation` refusal improvement — still command-scoped, not script-scoped)"
rationale = "Required justification: nothing is superseded because the native feature was already chosen correctly, and the two nearby natives that a modernization pass would reach for are both weaker at this host's settings — an `ask` rule is skipped by the permission mode these sessions actually run in, and `disable-model-invocation` provably never covered the script route (crossed twice on 2026-09-02). The only custom code in this capability is the PRESENCE check, and it exists because the harness ships no validator for permission-rule completeness: `claude plugin validate --json` (new at 2.1.259) validates plugin manifests, not settings.json. The check's own honest limit is already documented in its docstring — it proves the rules were not silently half-written, not that the harness still honours them."
probe = "claude plugin validate --help 2>&1 | head -20   # confirms its scope is a plugin directory, not settings.json permissions"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-platform_target-1"
unit = "mod-platform_target"
kind = "module"
disposition = "keep"
confidence = 0.86
custom_thing = "Repo-wide hard-coded platform-literal scan: find_violations() + platform_literals_main(), run as the hk step no_platform_literals, enumerating the whole tracked tree via `git ls-files -z` and regex-matching `linux/<arch>[/vN]` outside two permitted sites."
evidence = "python/src/dotfiles_setup/platform_target.py:139-183 (_LITERAL_RE, _SCANNED_SUFFIXES, exclusions), :527-569 (_tracked_files/_in_scope/find_violations), :799-832 (platform_literals_main); hk.pkl:268-270 (step no_platform_literals -> `dotfiles-setup platform-literals`); tests/test_platform_target.py:1-13 (both arms, planted-literal FAIL arm)."
native_alternative = "Candidates checked and rejected: (a) hk builtin steps — there is no generic regex/forbid builtin; (b) ast-grep 0.45.3 structural rules; (c) this repo's own verify handler `regex_forbid`."
alternative_evidence = "hk builtins enumerated at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ (only per-tool linters; grep for forbid|regex|grep|ban|pattern over that dir returns only check_* tool wrappers, control arm: the same grep shape returns check_added_large_files.pkl etc.); ast-grep's SupportLang enum has Hcl/Json/Yaml/Bash/Python but NO Toml — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/lib.rs:269-296 and :386-527 (extension table), control arm: `grep -c python parsers.rs` = 2, so the grep discriminates; the repo's regex_forbid handler takes an explicit path list with no glob or tracked-tree enumeration — python/src/dotfiles_setup/verify.py:446-452 and :117-127 (_resolve_paths iterates entry['paths'] only)."
rationale = "The gate's whole value is that it is GLOB-LESS — a literal can arrive in any tracked file, including .toml, which is where the pin itself lives. Every native candidate is path- or language-scoped: hk ships no generic forbidden-pattern step, ast-grep cannot parse TOML at the pinned version, and the in-repo regex_forbid handler would require someone to predict the file list, which is exactly the failure mode (#673 AC3) the scan exists to cover."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-platform_target-2"
unit = "mod-platform_target"
kind = "module"
disposition = "keep"
confidence = 0.82
custom_thing = "CI build-matrix generation: PublishTarget / published_targets() / _validation_target() / ci_matrix_targets() / publish_matrix_json(), emitted by `dotfiles-setup platform-matrix` and consumed by the workflow's fromJSON fan-out, with a separate manifest job merging per-arch tags into an OCI index."
evidence = "python/src/dotfiles_setup/platform_target.py:188-207 (PUBLISHED_ARCHES, _RUNNER_LABELS, #840 validation leg), :290-393 (PublishTarget, published_targets, ci_matrix_targets, publish_matrix_json/main); .github/workflows/build-publish.yml:118 (`matrix=$(uv run --project python dotfiles-setup platform-matrix)`); python/verification/suites.toml:783-794 (contract binding the fan-out)."
native_alternative = "docker buildx / docker-bake's native multi-platform build: one target with `platforms = [\"linux/amd64/v2\",\"linux/arm64/v8\"]` builds both and pushes a single OCI index, removing both the GHA matrix generator and the manifest-merge job."
alternative_evidence = "The module states the measured reason this native path is rejected: python/src/dotfiles_setup/platform_target.py:190-195 — a single bake emitting both platforms builds the arm64 half under QEMU, and that half compiles GCC 16.2 and clang-p2996 (the '~2h compiler' rebuild docker-bake.hcl already names); native runner availability was control-armed by a probe asserting `uname -m` = aarch64 (#676). Per-leg native runners are wired at .github/workflows/build-publish.yml:160,291,448,562,843,1076 (PLATFORM per matrix leg) with bake-action at :238,:390,:709."
rationale = "A native mechanism exists and is deliberately not used, with a measured cost justification recorded in the code (emulated compilation of two compilers). The custom matrix is therefore not reinvention — it is the declaration of WHICH native runner builds WHICH arch, plus the #840 validate-only leg (same platform tuple, different runner) that an OCI index literally cannot express. Nothing in mise/hk/gh/GHA supplies a second row for one platform tuple."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-platform_target-3"
unit = "mod-platform_target"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "Platform resolution precedence: resolve_platform() (override -> $DOTFILES_PLATFORM -> host native) and host_arch()/host_platform()."
evidence = "python/src/dotfiles_setup/platform_target.py:410-456; callers python/src/dotfiles_setup/docker.py:33, sync.py:80, main.py:108-114."
native_alternative = "mise already supplies the pin natively: `[env] DOTFILES_PLATFORM = \"{{ env.DOTFILES_PLATFORM | default(value='linux/amd64/v2') }}\"` uses mise's own template `default()` filter, and mise exposes `arch()` / `os()` template functions for the host fallback."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/templates.md:295 ('arch() -> String - Retrieves the system architecture, such as x64 or arm64') and :541 (functions available in env templates); the repo already consumes the native half at mise.toml:153."
rationale = "The native half (env pin + default) is ALREADY adopted — this function only wraps it for the Python/CI callers that do not run inside a mise template. mise's arch() returns mise's own spelling (x64/arm64), not docker's triple with a microarchitecture level, and is unavailable to Python, GHA steps, or an in-container smoke; no mise/uv/gh feature returns `linux/<arch>/<level>`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-platform_target-4"
unit = "mod-platform_target"
kind = "module"
disposition = "keep"
confidence = 0.88
custom_thing = "The four-spelling translation tables and their accessors: _ARCH_ALIASES/normalize_arch, _MICROARCH_LEVEL, _UNAME_MACHINE/expected_uname_machine, _MISE_LOCK_PLATFORM/mise_lock_platforms, LLVM_TARGETS, GCC_LATEST_ARCHES/ships_gcc_latest, os_arch, is_emulated, plus the `dotfiles-setup platform {triple,arch,machine}` CLI the verify-arch shell task reads."
evidence = "python/src/dotfiles_setup/platform_target.py:109-123, :225-247, :396-402, :474-506, :650-668, :770-796; mise.toml:966-967 (`dotfiles-setup platform arch` / `platform machine` in verify-arch), :956-994."
native_alternative = "No single native source maps docker arch names <-> `uname -m` <-> mise lock-platform names <-> LLVM target names. The closest natives each cover one spelling only: mise's `arch()` template function and mise's `os`/`arch` tool filters (mise spelling), GHA's `runner.arch` (X64/ARM64), docker's `TARGETARCH` build arg (docker spelling)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/templates.md:295 (arch() -> 'x64' or 'arm64' — a third spelling, not docker's and not uname's); mise settings reference /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:1574-1589 (lockfile_platforms values are 'macos-arm64', 'linux-x64' — mise's spelling again)."
rationale = "Required justification: the axis genuinely has four vocabularies (docker, uname, mise, LLVM) and every native source speaks exactly one of them; #698 (131 x86_64 tool resolutions in an arm64 image) and #703 (LLVM_TARGETS_TO_BUILD=X86 on arm64) are the recorded costs of treating any two as interchangeable. Centralising the maps here also keeps the shell tasks free of bash logic ([[zero-bash-logic]]) — verify-arch reads the CLI instead of re-implementing the mapping."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-platform_target-5"
unit = "mod-platform_target"
kind = "module"
disposition = "unknown"
confidence = 0.55
custom_thing = "find_default_drift(): a custom cross-file consistency check holding mise.toml's DOTFILES_PLATFORM default equal to docker-bake.hcl's `variable \"PLATFORM\"` default, which exists only because the literal is deliberately duplicated into HCL."
evidence = "python/src/dotfiles_setup/platform_target.py:38-44 (docstring: 'docker-bake.hcl must repeat it because CI's bake jobs do not run under mise'), :575-618; docker-bake.hcl:19-24."
native_alternative = "docker bake's native variable resolution: an HCL `variable` is overridden by a same-named environment variable, and every bake-invoking CI job already exports PLATFORM per matrix leg — so the HCL default may be dead weight, and removing it would retire this whole check."
alternative_evidence = ".github/workflows/build-publish.yml:155-160 ('Read by BOTH docker/bake-action (into the PLATFORM HCL variable) and ...', `PLATFORM: ${{ matrix.target.platform }}`), repeated at :291, :448, :562, :843, :1076; bake-action call sites at :238, :390, :709; local bake is banned outright (.claude/rules/do-not.md item 2)."
rationale = "If no bake invocation can ever run without PLATFORM in the environment, the duplicated HCL default is unnecessary and find_default_drift protects a duplication that need not exist. I could not prove the negative from the workflow alone (job-to-step mapping for :709's reusable target, plus any future manual bake), and a bake variable that resolves to an empty string would silently build the runner's native platform — the exact false pass this module exists to prevent. A live probe settles it."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && env -u PLATFORM -u DOTFILES_PLATFORM docker buildx bake -f docker-bake.hcl --print base 2>&1 | grep -n '\"platforms\"' ; then repeat with the `default =` line removed from docker-bake.hcl's variable \"PLATFORM\" block (in a scratch copy) to see whether bake errors loudly or emits an empty/native platform"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-platform_target-6"
unit = "mod-platform_target"
kind = "module"
disposition = "keep"
confidence = 0.84
custom_thing = "Image-config consistency gates: declared_lock_platforms(), find_lock_platform_drift() (mise-system.toml's lockfile_platforms must cover every PUBLISHED_ARCHES entry) and find_pinned_image_arch() (no arch / MISE_ARCH / CONDA_SUBDIR pin in the image configs), plus find_unpublished_pin() (the repo pin must name an arch CI publishes)."
evidence = "python/src/dotfiles_setup/platform_target.py:252-287, :621-647, :678-767; consumers python/src/dotfiles_setup/lock_integrity.py:52, lock_refresh.py:50, image_lock.py:58; contract python/verification/suites.toml:379-398."
native_alternative = "mise natively provides the settings being checked — `lockfile_platforms` (added in mise, restricts which platforms the lockfile holds) and the `arch` setting/MISE_ARCH env — but provides no cross-check between them and an external publish matrix. `mise doctor` is scoped to installation problems and has exactly one subcommand (`path`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/settings.toml:1574-1589 ([lockfile_platforms], example `lockfile_platforms = [\"macos-arm64\", \"linux-x64\"]`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/CHANGELOG.md:5286 ('add lockfile_platforms setting to restrict lockfile platforms'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/doctor.md:1-18 ('Check mise installation for possible problems', only subcommand `mise doctor path`)."
rationale = "Required justification: the native settings are already adopted — the custom code is only the AGREEMENT check between three declarations mise cannot see together (what CI publishes, what the image locks, what the running container pins). No mise, hk, uv or GHA feature knows about PUBLISHED_ARCHES, and the failure mode is silent by construction (#698: a wrong-architecture binary is present, executable and counted exactly like a correct one), so there is nothing upstream to defer to."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-pr-1"
unit = "mod-pr"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "`automerge_main` + the `BOT_PR_AUTHORS` login allowlist: a per-PR verb that arms GitHub-native auto-merge on a bot-opened PR (`mise run automerge -- <PR#>`)."
evidence = "python/src/dotfiles_setup/pr.py:210-216 (BOT_PR_AUTHORS = app/renovate, app/dotfiles-refresh-bot-org, app/dependabot); python/src/dotfiles_setup/pr.py:670-738 (automerge_main); mise.toml:903-911 ([tasks.automerge] -> `dotfiles-setup pr automerge`); python/verification/suites.toml:1204 (the wiring contract)."
native_alternative = "Renovate's own `automerge` + `platformAutomerge` config options, which make Renovate itself call GitHub's auto-merge on its PRs — and which THIS repo already enables for minor/patch/digest."
alternative_evidence = ".agent/kb/raw/renovate-config-options.md:8269 (`automerge` — \"Whether to automerge branches/PRs automatically, without human intervention\"), :8341-8342 (the branch-protection recipe: `automerge=true` + `platformAutomerge=true`, \"Renovate defaults to true\"); already configured at renovate.json:31-40 (`matchUpdateTypes: [minor, patch, digest]`, `automerge: true`, `automergeType: pr`, `platformAutomerge: true`)."
rationale = "For the minor/patch/digest subset of `app/renovate` PRs the native path already arms GitHub auto-merge, so running the verb on one is a duplicate of work Renovate did itself. The verb is NOT fully superseded: (a) `app/dependabot` has no automerge option at all — .github/dependabot.yml is this repo's only Python updater and GitHub's dependabot.yml schema has no equivalent knob, which is exactly why #901 sat CI-green and unmergeable until the login joined the allowlist (pr.py:198-203); (b) renovate.json:47 deliberately sets `automerge: false` for graphify and majors are excluded, so the human-in-the-loop cases need a named, per-PR verb. The refactor is scoping/documentation — record in the verb (and in BOT_PR_AUTHORS' comment) that renovate minor/patch PRs self-arm, so the verb's load-bearing cases are dependabot + deliberately-held renovate PRs — not deleting it."
probe = "gh pr list --author app/renovate --state open --json number --jq '.[0].number' | xargs -I{} gh pr view {} --json autoMergeRequest,author  # non-null autoMergeRequest on a minor/patch renovate PR that never ran `mise run automerge` proves Renovate armed it natively; control arm: the same probe on an app/dependabot PR must return null."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-pr-2"
unit = "mod-pr"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`_await_checks_registered`: a 40 x 15s poll for GitHub to report at least ONE check on a freshly-created PR, treating the `gh pr checks` \"no checks reported\" nonzero exit as pending rather than failure."
evidence = "python/src/dotfiles_setup/pr.py:398-417."
native_alternative = "`gh pr checks --watch` (with `--fail-fast`/`--interval`) — the flag `.claude/rules/gh-cli-watch.md` mandates instead of hand-rolled poll loops."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/pr/checks/checks.go:184-186 (`populateStatusChecks` is called and its error returned BEFORE the `if opts.Watch` block at :193 and the refresh loop at :203), :303 (`return nil, checkCounts{}, fmt.Errorf(\"no checks reported on the '%s' branch\", ...)`), :81 (`cannot use --watch with --json flag`)."
rationale = "The native flag cannot cover this capability: gh populates checks once before entering watch mode, so `--watch` returns the \"no checks reported\" error immediately in exactly the window this function exists for (the seconds-to-minutes after `gh pr create` while GitHub registers check runs). `--watch` is also mutually exclusive with `--json` (checks.go:81), so it cannot be used to read the machine-readable buckets `pr_checks_green` depends on. This is the documented exception to the never-hand-roll-a-poll rule, not a violation of it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-pr-3"
unit = "mod-pr"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`_main_run_conclusion`: discovery poll (40 x 15s, or a 4-attempt grace poll) for the main `ci.yml` run belonging to a specific merge commit, before handing off to the native watch."
evidence = "python/src/dotfiles_setup/pr.py:752-805 (the `gh run list --commit <oid> --json databaseId` discovery loop at :760-786, then `gh run watch <id> --exit-status` at :796 cross-verified with `gh run view --json conclusion` at :797-800)."
native_alternative = "`gh run watch <run-id> --exit-status` (already adopted for the watch half) — but it accepts only a run ID or an interactive picker, never a commit SHA."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/run/watch/watch.go:74 (`opts.RunID = args[0]`), :109-122 (`runID := opts.RunID`; otherwise `shared.SelectRun(opts.Prompter, ...)` — an interactive prompt, unusable from a gate)."
rationale = "The watch/verify half is already native and follows `.claude/rules/gh-cli-watch.md` (watch + independent `conclusion` cross-check). The only custom part left is resolving commit-SHA -> run-id, which gh has no native form of: `gh run watch` requires an id and falls back to an interactive prompt. The poll also has to distinguish 'no run yet' from 'no run will ever exist' (the path-filtered push trigger), which no gh flag models."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-pr-4"
unit = "mod-pr"
kind = "module"
disposition = "refactor"
confidence = 0.45
custom_thing = "`CI_PUSH_PATHS`: a hand-maintained copy of `.github/workflows/ci.yml`'s `on.push.paths`, used by `expects_main_run` to decide whether a missing main CI run is legitimate."
evidence = "python/src/dotfiles_setup/pr.py:142-160 and :290-297 (`expects_main_run`); the lockstep is asserted only by tests/test_pr.py per the comment at pr.py:145."
native_alternative = "Read the workflow's own YAML instead of mirroring it — `gh workflow view ci.yml --yaml` (or parsing the tracked `.github/workflows/ci.yml` with the already-present YAML tooling), so the single source of truth is the file GitHub actually evaluates."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/workflow/view/view.go:91 (`cmd.Flags().BoolVarP(&opts.YAML, \"yaml\", \"y\", false, \"View the workflow yaml file\")`), :160-193 (fetches and prints the workflow content); the mirrored source is .github/workflows/ci.yml:12-27."
rationale = "Nothing native answers 'will this merge trigger a run?', but the duplication itself is avoidable: the constant is a second copy of data that already lives in a tracked file, kept honest only by a test that must be remembered when ci.yml changes. Deriving the globs from ci.yml at call time (locally, no network) removes the drift class entirely. Downgraded to 0.45 because the offline read must also replicate the actions path-glob semantics (`**` handling) that `fnmatch` currently approximates, so the refactor is not free."
probe = "uv run --project python python -c \"import yaml,pathlib; print(yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text())['on']['push']['paths'])\"  # confirm the paths can be read locally, then diff against pr.CI_PUSH_PATHS; control arm: mutate a path in a scratch copy and confirm the diff is detected."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-pr-5"
unit = "mod-pr"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`gate_matrix` / `run_gates`: an ordered, cheap-first, fail-fast, PATH-CONDITIONAL local gate list (lint, pytest, verify-contracts, hook-selfcheck, eval, plus pin-actions / lint-docs / verify-apt-pins / sync-full selected by which repo paths the diff touches), streamed to the terminal with explicit PASS/FAIL rc lines."
evidence = "python/src/dotfiles_setup/pr.py:311-379 (gate_matrix + run_gates), :98-188 (SURFACE_PATTERNS / BASE_INPUT_PATTERNS / _APT_PIN_PATTERNS / _GHA_PATTERNS / _DOCS_PATTERNS), :365-366 (the base-input deferral rule)."
native_alternative = "mise task dependencies (`depends` / `wait_for` / `depends_post`) or a sequential `run` array; hk's own step graph."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:99-109 (`depends`, and \"mise will run whatever it can in parallel (up to --jobs)\" at :105), docs/tasks/architecture.md:26 (\"Independent tasks run in parallel\"); hk's steps are git-hook-scoped and are already the *content* of the `lint` gate, not a composer of the other gates."
rationale = "Required justification: no native mechanism expresses gate SELECTION from the branch's changed-path set. mise's `depends` graph is explicitly parallel (task-configuration.md:105) — which destroys the cheap-first ordering and the fail-fast semantics that make the first failure the task — and mise has no conditional-on-diff dependency edge at all; hk runs on git-hook events over staged files and cannot decide 'this diff touched a base-image build input, so DEFER the container gate to CI'. That path-aware policy (surface vs base-input vs apt-pin) is repo-specific judgement encoded as code, and it is what makes the ship verb a gate rather than a script."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-pr-6"
unit = "mod-pr"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "`enable_auto_merge`: arms auto-merge pinned to the gated SHA, with a 5-attempt / 20s-backoff retry around GitHub's transient 422 on the enable call."
evidence = "python/src/dotfiles_setup/pr.py:429-476 (`_AUTOMERGE_ENABLE_ATTEMPTS`, `_AUTOMERGE_ENABLE_BACKOFF_S`, the `gh pr merge --auto --squash --delete-branch --match-head-commit <sha>` command at :449-459)."
native_alternative = "`gh pr merge --auto --squash --match-head-commit` — GitHub-native auto-merge, already fully adopted here; the retry wrapper is the only non-native part, and `gh pr merge` exposes no retry/backoff flag."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/pr/merge/merge.go:182 (`--auto`), :183 (`--disable-auto`), :184 (`--match-head-commit`), :181 (`--squash`), :175 (`--delete-branch`); control arm for the absence: `grep -i retry pkg/cmd/pr/merge/merge.go` returns 0 hits while the same file's `cmd.Flags()` block lists 12+ flags at :174-185, so the grep is not blind — there is no native retry."
rationale = "Required justification: the wait itself is already delegated to GitHub (no client poll, no `gh run watch` on the PR), so the only custom code is the bounded retry over a documented transient 422 on the enable call, for which gh offers no flag. The single shared `enable_auto_merge` used by both `ship` and `automerge` is also what keeps the arming semantics (squash + `--match-head-commit` pinning) from drifting between the two entrypoints — a property no native mechanism provides."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-process_env-1"
unit = "mod-process_env"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "command_after_separator() — hand-rolled normalisation of an argparse REMAINDER child command: strips a leading '--' and raises ValueError when the remainder is empty, with main.py converting that to exit 2."
evidence = "python/src/dotfiles_setup/process_env.py:29-35 (the helper); python/src/dotfiles_setup/main.py:1316 (`git_parser.add_argument(\"child_command\", nargs=argparse.REMAINDER)`); python/src/dotfiles_setup/main.py:1914-1919 (ValueError -> `sys.exit(2)`); tests/test_process_env.py:84-86 (the only test of it)."
native_alternative = "argparse's own '--' handling: `nargs=\"+\"` on the positional makes argparse (a) strip the first '--' itself and (b) fail with the standard `the following arguments are required` usage error at rc=2 — the same exit code main.py already produces."
alternative_evidence = ".agent/kb/raw/argparse-dashdash-probe.md (host probe, python 3.14.7, both arms: under `nargs='+'` `['--']` and `[]` -> SystemExit rc=2 while `['--','uv','run','--project','python','pytest']` -> the command with '--' removed; the control arm under `nargs=argparse.REMAINDER` keeps '--' and accepts `[]`, so the probe discriminates)."
rationale = "Both jobs the helper does are native argparse behaviour once the positional stops using REMAINDER. The one behaviour lost is tolerating an option-shaped command with no '--' separator (`['pytest','-x']` errors under nargs='+'), and the repo's only call site always passes '--' (mise.toml:232). Not a straight 'retire' because the probe was run on a flat parser, not through this CLI's subparser chain (`process` -> `git-isolated`), where '--' removal happens once and could be consumed earlier; settle that with the probe before deleting the helper and its test."
probe = "uv run --project python dotfiles-setup process git-isolated -- /usr/bin/env true; echo rc=$?  # then, on a scratch branch, swap main.py:1316 to nargs=\"+\", delete command_after_separator, and re-run the same line plus `... process git-isolated` (no command, expect rc=2) and `... process git-isolated -- uv run --project python pytest -q --collect-only tests/test_child_env.py`"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-process_env-2"
unit = "mod-process_env"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "git_local_env_names() — derives the complete repository-local GIT_* variable set at runtime by shelling out to `git rev-parse --local-env-vars`, scrubbing GIT_* from the discovery child's own env and failing closed (RuntimeError) on a non-zero rc or an empty list."
evidence = "python/src/dotfiles_setup/process_env.py:65-91; tests/test_process_env.py:127-135 (fail-closed arm); tests/test_process_env.py:202-206 (the hostile pre-push hook exports every derived name)."
native_alternative = "This IS the native mechanism — `git rev-parse --local-env-vars` is git's own documented API for exactly this purpose ('List the GIT_* environment variables that are local to the repository ... Only the names of the variables are listed, not their value')."
alternative_evidence = ".agent/kb/raw/git-local-env-vars-probe.md (host probe: `man git-rev-parse` lines 179-182, git 2.50.1, and the 15 names it returns on this host)."
rationale = "Required justification: the module already delegates the list to the tool that owns it, which is what use-tool-builtins.md asks for; the custom code is only the fail-closed wrapper, and nothing outside git can produce this list. The measured payoff is concrete — git returns 15 names on this host while the nearest hand-maintained list in the repo (child_env.GIT_CONTEXT_NAMES, python/src/dotfiles_setup/child_env.py:36-45) carries 6, so a static list would leak 9 variables including GIT_CONFIG, GIT_CONFIG_PARAMETERS and GIT_PREFIX. No linter, hook or task can replace a runtime query of the installed git."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-process_env-3"
unit = "mod-process_env"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "git_isolated_env()/run_git_isolated() plus the `dotfiles-setup process git-isolated --` CLI seam: builds the pre-push test child's environment by removing every derived Git-local name AND every credential-shaped name (child_env.clean_env), then spawns the command with it."
evidence = "python/src/dotfiles_setup/process_env.py:94-109; mise.toml:232 (`[tasks.test-hook-isolated]` run line); hk.pkl:780 (pre-push `test` step invoking that task); tests/test_process_env.py:265-284 (wiring gate) and tests/test_process_env.py:137-236 (real poisoned-hook arm + the no-scrub control at :239-260)."
native_alternative = "mise's env-clearing (`[env] VAR = false` unsets a variable), POSIX `env -u VAR` (already used one layer up in hk.pkl:780 for MISE_IGNORED_CONFIG_PATHS), and hk's per-step `env` block."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/environments/index.md:15-19 ('To clear an env var, set it to `false`' ... 'NODE_ENV = false # unset a previously set NODE_ENV'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:281 (`env: Mapping<String, String>` — a step env can only SET a string, there is no unset form); mise digest .agent/kb/raw/mise-releases-since-pin.digest.md (host 2026.9.3 is the newest release, so no newer mise env feature exists to consider)."
rationale = "Required justification: every native unset mechanism takes a STATIC, hand-written list of names, which is precisely the failure mode the module's docstring exists to avoid — the set is derived per-host from git (15 names here, and a future git release adds more silently), and clean_env additionally removes credential-shaped names by regex, which no toml env block can express. hk's step env is typed String-only and cannot unset at all. Doing it in a shell wrapper would also violate zero-bash-logic. The native layers are already adopted where they fit (`env -u` and `mise --cd` in hk.pkl:780)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-process_env-4"
unit = "mod-process_env"
kind = "module"
disposition = "refactor"
confidence = 0.45
custom_thing = "fnox_command()/run_with_fnox() — builds `fnox exec --non-interactive -- <cmd>` and runs it as a Python subprocess to publish (git push) with fnox-resolved credentials."
evidence = "python/src/dotfiles_setup/process_env.py:38-40 and :57-62; python/src/dotfiles_setup/pr.py:584-586 (the ship push call site); python/src/dotfiles_setup/skillopt_provenance.py:21,173 (second consumer); tests/test_process_env.py:89-98."
native_alternative = "`fnox exec --replace` — process-replacement mode: 'Run the command in fnox's process, keeping the same PID and receiving signals directly; supports environment-only secrets without leases.' Added in fnox 1.32.0 (exec: add process replacement mode, jdx/fnox#654); the host runs fnox 1.35.1, so it is available."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/docs/cli/exec.md (Flags: `--replace`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/CHANGELOG.md:149 ('**(exec)** add process replacement mode ... #654', under the 1.32.0 heading at CHANGELOG.md:144); host probe `fnox --version` -> `fnox 1.35.1`."
rationale = "The wrapper itself is thin and correct, but it currently stacks python -> fnox -> git, so a Ctrl-C during a long `git push` is delivered to fnox rather than to git and the rc travels through an extra hop. `--replace` is the native mechanism for exactly that and did not exist when this was written. It is a refactor, not a retire: fnox is still the credential boundary, only the spawn shape changes, and the flag's lease semantics ('environment-only secrets without leases') need a live check against this repo's fnox config before adopting."
probe = "fnox exec --replace --non-interactive -- /bin/sh -c 'echo rc-probe; exit 7'; echo rc=$?   # expect rc=7 with no lease error; then compare `fnox exec --non-interactive -- ...` on the same command, and confirm SIGINT reaches the child (run `fnox exec --replace --non-interactive -- sleep 30` and press Ctrl-C)"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-process_env-5"
unit = "mod-process_env"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "fnox_parent_env() + GITHUB_TOKEN_NAMES — removes GH_TOKEN and GITHUB_TOKEN from the PARENT environment handed to fnox, so a stale long-lived token cannot out-rank the scoped token fnox resolves for the child."
evidence = "python/src/dotfiles_setup/process_env.py:26 and :43-54; used at python/src/dotfiles_setup/process_env.py:60 and python/src/dotfiles_setup/skillopt_provenance.py:173; tests/test_process_env.py:100-112 (asserts only those two names are dropped, provider inputs kept)."
native_alternative = "None found. fnox's own resolution order treats an already-set shell variable as a valid SOURCE ('3. Environment variable (if already set in shell) ... First match wins!'), so fnox will adopt a stale value rather than override it, and `fnox exec` exposes no unset/no-inherit flag. gh documents the precedence but offers no setting to invert it ('GH_TOKEN, GITHUB_TOKEN (in order of precedence) ... takes precedence over previously stored credentials')."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/docs/guide/how-it-works.md:38-45 (secret resolution order, env var as source, first match wins); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/docs/cli/exec.md (the complete flag list is `--replace` and `--help` — no unset/clear-env flag); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/root/help_topic.go:45-47."
rationale = "Required justification: the two tools involved both make the stale value WIN — fnox falls back to an already-set variable as a resolution source, and gh/git-credential prefer GH_TOKEN over GITHUB_TOKEN — and neither exposes a native way to drop a name from the child environment. The custom code is a two-name deletion on a copy of os.environ, it never reads or prints a value, and it is the minimum that closes the precedence hole. Re-check if fnox ever ships an `--unset`/`--no-inherit` flag on `exec`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-reap-1"
unit = "mod-reap"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "Hand-rolled process-table mechanics: a `ps -eo pid=,ppid=,etime=,stat=,args=` subprocess call, a bespoke parser, a four-format elapsed-time parser, an ancestor walk, a command-string identity re-check against PID reuse, and a TERM-then-KILL escalation with a liveness re-check."
evidence = "python/src/dotfiles_setup/reap.py:70 (PS_COMMAND), :110 (parse_etime, 4 ps formats), :156 (run_ps subprocess), :178 (parse_processes), :217 (ancestor_pids), :347 (confirm_targets, PID-reuse guard by command string), :412 (reap: TERM, sleep, re-snapshot, KILL)"
native_alternative = "psutil: `Process.parents()` returns the ancestor chain; `Process.create_time()` gives the age without parsing etime; `psutil.wait_procs(procs, timeout, callback)` IS the documented terminate-then-kill-survivors recipe; and psutil's signal-related methods already re-check PID + creation time and raise NoSuchProcess on a reused PID — a stronger identity guard than comparing the command string."
alternative_evidence = ".agent/kb/raw/psutil-api-process-reaping.md:10 (wait_procs + the terminate/kill recipe), :26 (Process.parents()), :32 (\"perform an additional check using PID + creation time, and will raise NoSuchProcess if the PID has been reused\"), :36 (create_time). Source https://psutil.io/api/ fetched 2026-09-09."
rationale = "Four of the module's six mechanics are library primitives, and one of them (the PID-reuse guard) is strictly weaker here than psutil's: reap.py re-compares the command string, while psutil compares PID+creation time inside the signal call itself, closing the window between confirm_targets() and os.kill() that this module cannot close. `use-tool-builtins.md` makes 'prefer the existing library' the default answer. The POLICY layer must stay custom (see mod-reap-2/-3), so this is a wrap, not a delete: keep ReapRequest/Selection/format_plan and replace run_ps/parse_processes/parse_etime/ancestor_pids/_signal_all with psutil calls. Confidence is 0.6 rather than higher because the cost is real and un-probed here: psutil is a C extension and is not currently in the tree (grep -> 0 hits, control arm msgspec -> 21), so it adds a wheel that must resolve for both macOS-arm64 (host) and linux-amd64 (image) in uv.lock, and the module's 31 tests inject `ps` text as a plain string seam that a psutil rewrite would have to re-plumb (psutil needs monkeypatching rather than a text fixture)."
probe = "uv add --project python psutil && uv run --project python python -c \"import psutil,os; p=psutil.Process(); print([q.pid for q in p.parents()]); print(p.create_time())\" && uv run --project python python -c \"import tomllib,pathlib; print('linux-amd64 wheel resolved:' , 'psutil' in pathlib.Path('python/uv.lock').read_text())\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-reap-2"
unit = "mod-reap"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "Ancestor-chain protection: the reaper excludes itself, every ancestor up to init, and init, so a pattern can never signal the shell that launched it (`protected_pids` wired into the CLI path at reap_main)."
evidence = "python/src/dotfiles_setup/reap.py:239 (protected_pids), :247 (`frozenset(ancestor_pids(...)) | {INIT_PID} | set(extra)`), :503 (`protected=protected_pids(processes, self_pid=self_pid)` — the wiring a mutation proved was unasserted); contract python/verification/suites.toml:2380 (workflow.reap-protects-before-it-signals)"
native_alternative = "Claude Code v2.1.214+ ships a `pkill` self-protection guard: the Bash tool's shell integration sets `CLAUDE_PID` and pre-tests the pattern with `pgrep`, refusing `pkill` when the Claude Code process itself matches."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/errors.md:2572 (\"The check runs on Linux only; on macOS, `pkill` runs unmodified.\"); env-vars.md:396 (CLAUDE_PID, \"On Linux, the Bash tool's shell integration uses it to refuse a `pkill` pattern that would match the Claude Code process itself\"); changelog.md:1094"
rationale = "The native guard does not cover this capability on the machine that has the problem. Three gaps, each measured: (1) it is Linux-only and this host is darwin — probed `test -r /proc/1/comm` -> PROC_ABSENT while `CLAUDE_PID` is SET, so the shipped wrapper function short-circuits to `command pkill \"$@\"` here (the wrapper body was read from the live shell; its own `[ -r \"/proc/${CLAUDE_PID}/comm\" ]` gate is the Linux test); (2) it protects exactly ONE pid, the Claude CLI, whereas the measured #653 incident had `--pattern 'fnox export'` matching FOUR processes, all of them the reaper's own chain (zsh wrapper -> uv -> the python entry) — a guard on the CLI pid alone would still have killed the uv/python links; (3) it only exists inside Claude Code's Bash tool shell, so it protects nothing when the operator runs `mise run reap` from a plain terminal. Nothing in mise, hk, gh, or BSD `pkill` offers ancestor exclusion."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-reap-3"
unit = "mod-reap"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The policy layer: dry-run by default (`--kill` required to signal anything), an age floor (DEFAULT_MIN_AGE_S=300, unparsable age EXCLUDES rather than assumes old), a four-bucket auditable plan (protected / too young / targets / age range) carrying the load caveat, and exit codes that separate 'nothing matched' (0) from 'the probe could not see' (2)."
evidence = "python/src/dotfiles_setup/reap.py:85 (DEFAULT_MIN_AGE_S), :117 (unparsable etime excludes), :201-206 (empty table raises ReapError, not an all-clear), :321 (format_plan four buckets), :510 (`if not request.kill:` dry-run gate), :536 (exit codes); mise.toml:1538 ([tasks.reap]); .claude/skills/reap/SKILL.md:1"
native_alternative = "None found. The closest native surfaces are BSD `pkill`/`pgrep` (the two-step pgrep-then-pkill 'preview'), and psutil, whose API reference has no dry-run and no age filter."
alternative_evidence = "Probed on this host: `pgrep --help` -> `usage: pgrep [-Lfilnoqvx] [-d delim] [-F pidfile] [-G gid] [-P ppid] [-U uid] [-g pgrp] [-t tty] [-u euid] pattern ...` — no age/older flag (control arm: `-P ppid` IS listed, so the usage string is being read, not missed). .agent/kb/raw/psutil-api-process-reaping.md:40 (\"No built-in dry-run mode, no built-in 'age floor' filter ... and no built-in auditable plan output.\")"
rationale = "Required justification: no tool in the pinned set expresses this policy. BSD `pkill` on macOS has no age predicate at all (probed above — procps-ng's `--older` is a GNU/Linux extension absent from the host binary), no dry run, and exit code 1 conflates 'no process matched' with failure, which is exactly the blind-probe conflation `probes-need-a-control-arm.md` rule 9 forbids and which reap.py:201 deliberately splits. A pgrep-then-pkill two-step is not equivalent either: the two invocations take two different snapshots, so the set previewed is not the set signalled — the very gap `confirm_targets` exists to close. Even after the mod-reap-1 psutil refactor this layer survives unchanged, so it is the module's real content."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-reap-4"
unit = "mod-reap"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "The general-purpose 'clear a pile of accumulated stuck processes' remit, as stated in the skill description (hung git, fnox, shims, orphaned workers)."
evidence = ".claude/skills/reap/SKILL.md:3 (description: \"whenever a host has accumulated stuck or duplicated processes (hung `git`, `fnox`, shims, orphaned workers)\"); python/src/dotfiles_setup/reap.py:4-9 (the #653 incident: 1,174 wedged mise/shims/git + 1,188 fnox children)"
native_alternative = "Claude Code now reaps its OWN background shell commands automatically: memory-pressure reaping of idle background shells on macOS and Linux (opt out with CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP), and a hard lifetime cap on subagent-started background shells (CLAUDE_SUBAGENT_BG_SHELL_MAX_MS, default 60 min)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/changelog.md:1598 (\"Added automatic memory-pressure reaping for idle background shell commands\"); env-vars.md:240 (CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP, \"Requires Claude Code v2.1.193 or later\" — host is 2.1.266); env-vars.md:400 (CLAUDE_SUBAGENT_BG_SHELL_MAX_MS)"
rationale = "The native reaping narrows the module's remaining domain but does not cover it. It applies only to processes Claude Code itself started as background shells and tracks by handle; the measured #653 pile was `mise/shims/git` and `fnox export --format json` spawned by the shell/mise plugin outside any Claude process tree, with the root cause still open upstream (knowledge-base#243). Nothing in that native path can be pointed at an arbitrary argv pattern on the host. The narrowing IS worth recording in the skill, because one recurring source of orphaned workers is now self-clearing and a session should not reach for `reap` for it — that is a doc edit, not a retirement."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate-1"
unit = "mod-renovate"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "A hand-written 5-entry REQUIRED_PERMISSIONS table (contents/pull_requests/issues/checks/statuses = write) that `permission_gaps()` diffs the installation record against, and that `healthy` gates rc on."
evidence = "python/src/dotfiles_setup/renovate.py:36-42 (the literal table), :105-114 (`permission_gaps`), :63 (`healthy`), :192-196 (the '✓ privileges present' line); tests/test_renovate.py:16-40 pin the table's contents."
native_alternative = "GitHub REST `GET /apps/{app_slug}` returns the app's OWN declared permission set, so the requirement can be derived from the app instead of transcribed: `gh api /apps/renovate --jq .permissions` → 12 permissions including `workflows: write` and `vulnerability_alerts: read`. The correct derived check is 'every permission the app declares is granted at >= the declared level' against the same `/orgs/{owner}/installations` record the module already reads."
alternative_evidence = ".agent/kb/raw/mod-renovate-native-probes.md P1 (verbatim `gh api /apps/renovate` output, 12 permissions) and P2 (the granted installation record, 11 — declared minus `emails`)."
rationale = "The hand-written table is not merely redundant, it currently UNDER-SPECIFIES in a load-bearing way: `workflows: write` is absent from it, and GitHub requires that permission to push changes under `.github/workflows/**` — which this repo depends on, because `renovate.json:3-6` extends `github>jdx/renovate-config`, whose `helpers:pinGitHubActionDigests` re-pins every GHA action SHA. An install scoped down to lose `workflows: write` would leave every action pin frozen while `mise run renovate-status` printed '✓ privileges present' and exited 0 — a gate that can only pass on the very drift it exists to catch. Deriving the requirement from `/apps/renovate` makes the check self-updating when Renovate adds a permission, and removes a constant that no test can tell apart from a complete one. Note the diff must be 'declared ⊆ granted', not equality: the live installation legitimately lacks `emails`."
probe = "gh api /apps/renovate --jq '.permissions | keys' && gh api /orgs/ray-manaloto/installations --jq '.installations[]? | select(.app_slug==\"renovate\") | .permissions | keys'"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-renovate-2"
unit = "mod-renovate"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "`_pr_rows()` + the report's 'Open update PRs' / 'Recently merged' sections — two `gh pr list --author app/renovate` calls (open, limit 50; merged, limit 10) rendered as number+title rows."
evidence = "python/src/dotfiles_setup/renovate.py:117-143 (`_pr_rows`), :162-163 (both call sites), :198-203 (rendering); the module docstring at :14-15 names these as questions 2 and 3 of 3."
native_alternative = "Renovate's own Dependency Dashboard issue — `dependencyDashboard: true`, already ON for this repo transitively (the `github>jdx/renovate-config` preset sets it explicitly) and live as issue #193 authored by app/renovate. It lists the open queue plus everything `gh pr list` structurally cannot see: 'Repository Problems' (Mend-side run warnings), 'Awaiting Schedule', 'Rate-Limited', 'PR Edited (Blocked)', 'Abandoned Dependencies', and a link to the Mend web portal run logs."
alternative_evidence = ".agent/kb/raw/mod-renovate-native-probes.md P3 (issue #193 exists; verbatim body headings; jdx preset `\"dependencyDashboard\": true` from https://raw.githubusercontent.com/jdx/renovate-config/main/default.json); Renovate docs saved verbatim at .agent/kb/raw/renovate-config-options.md:11739 and :11776."
rationale = "The custom view is strictly weaker than the native one on the same data, and the difference is currently REAL, not hypothetical: PR #947 sits under 'PR Edited (Blocked)' on the dashboard — Renovate has stopped updating it — while `gh pr list --author app/renovate --state open` reports it as an ordinary open PR, so the module's 'Open update PRs' section shows a healthy-looking queue containing a dead entry. The module's own docstring concedes the blind spot ('The scan cadence itself is Mend-side (this tool only observes GitHub state)'), and the dashboard is exactly the Mend-side view it says it cannot provide. The refactor is to stop re-deriving the queue and instead surface the dashboard issue (number, updatedAt, and its 'Repository Problems' section) — keeping the command as the one auditable entrypoint per mise-tasks-only.md, but pointing it at the authoritative artifact. Not a full retire: the dashboard says nothing about whether the app is installed or privileged (finding 1/4), and a merged-PR history is not on it."
probe = "gh issue list --search \"Dependency Dashboard in:title\" --json number,title,author && gh issue view 193 --json body --jq .body | grep -n '^## '"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate-3"
unit = "mod-renovate"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "`_gh()` — a wrapper that runs `gh`, discards stderr, and returns the empty string on ANY non-zero exit, so every failure funnels into `installed=False`."
evidence = "python/src/dotfiles_setup/renovate.py:66-77 (`if result.returncode != 0: return \"\"`), :86-102 (`_installation` reads that empty string as 'no record' → None), :150-152 (`install is None` → `RenovateStatus(installed=False)`), :172-178 (renders '✗ App NOT installed' + 'Install: https://github.com/apps/renovate'), :218 (rc=1)."
native_alternative = "gh already reports failures precisely: on any HTTP error it writes `gh: HTTP <code>` to stderr, adds `api.ScopesSuggestion(resp)` when the token is missing a scope, and returns `cmdutil.SilentError` so the process exits non-zero — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/api/api.go:548-560. Propagating gh's rc + stderr (or `check=True`) instead of collapsing to \"\" is the native behaviour the wrapper is currently throwing away."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/api/api.go:551-560; control arm for that grep in .agent/kb/raw/mod-renovate-native-probes.md P4 (`func NewCmdApi` → pkg/cmd/api/api.go:66 present, so the file and the search discriminate)."
rationale = "This is a probe with one face in the sense of .claude/rules/probes-need-a-control-arm.md: an expired token, a missing `admin:org`/`read:org` scope, a rate limit, a network blip, or an owner that is a USER rather than an org (in which case `/orgs/{owner}/installations` 404s by construction, renovate.py:88-95) all render as the single confident sentence '✗ App NOT installed ... Install: https://github.com/apps/renovate' with rc=1 — an actionable-looking remedy for a condition that is not true. gh's own scope suggestion, which is exactly the diagnostic a human needs here, is discarded before it can be printed. Fix: distinguish 'answered no' from 'never asked' — keep gh's rc and stderr, and give RenovateStatus a third state (unknown/unreachable) that renders and exits differently from 'not installed'."
probe = "GH_TOKEN=deliberately-invalid gh api /orgs/ray-manaloto/installations; echo \"rc=$?\"  # then: GH_TOKEN=deliberately-invalid uv run --project python dotfiles-setup renovate-status; echo \"rc=$?\" — expect the current build to print 'App NOT installed' rather than an auth error"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate-4"
unit = "mod-renovate"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "The unit as a whole — `dotfiles-setup renovate-status` (mise task `renovate-status`): one read-only command that answers 'is Renovate installed, privileged, and what is queued', exiting non-zero when the app is absent or under-privileged so it can double as a gate."
evidence = "python/src/dotfiles_setup/renovate.py:2-19 (docstring stating the three questions), :207-218 (`renovate_status_main`, rc semantics); mise.toml:879-885 (the only caller); python/src/dotfiles_setup/main.py:123, :1561-1569, :2377-2379 (registration/dispatch)."
native_alternative = "None for the residual capability. `gh` has no app/installation verb at all (control arm: `gh app --help` → 'unknown command \"app\" for \"gh\"', while `gh api`, `gh repo`, `gh extension` are listed in `gh --help`), so the granted-vs-required permission diff has to be assembled from `gh api` by something. The Mend developer portal (https://developer.mend.io/github/ray-manaloto/dotfiles, linked from issue #193) shows install state and run logs, but it is a web UI — not a command that can exit non-zero."
alternative_evidence = ".agent/kb/raw/mod-renovate-native-probes.md P1/P3/P4; `gh --help` and `gh app --help` transcript in the same file's control-arm section."
rationale = "After findings 1 and 2 the module shrinks to one thing nothing native does: turn 'this GitHub App installation is missing a permission it declares' into a non-zero exit code from a CLI. That justification is real, but it comes with two caveats the synthesis should carry. (a) The gate has no consumer: `renovate-status` is invoked only by its mise task — no hk step, no `python/verification/suites.toml` contract, and no `doctor.toml` check reference it (grep over mise.toml, hk*.pkl, .claude/settings.json, suites.toml, doctor.toml returns the mise task and two research docs only), unlike its sibling `renovate_validate`, which hk shells out to and `workflow.renovate-re2-engine-assertion` binds. So an rc=1 nobody runs is documentation, not enforcement — either wire it (a doctor check runs every session already) or drop the gate pretence. (b) Its whole rc semantic rests on the hard-coded table of finding 1 and the false-negative funnel of finding 3, so 'keep' is conditional on both being fixed; kept as-is it is a gate that under-checks in one direction and over-fires in the other."
probe = "grep -rn \"renovate-status\\|renovate_status\" mise.toml hk.pkl hk-common.pkl hk-image.pkl .claude/settings.json doctor.toml python/verification/suites.toml  # control arm: the same grep for 'renovate-validate' returns hk.pkl + suites.toml hits"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_dryrun-1"
unit = "mod-renovate_dryrun"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "The INCOMPLETE/FLOOR verdict is derived OUT OF BAND, by sniffing five env-var names for a github.com token and passing the boolean into the parse: `_TOKEN_ENV_VARS` (python/src/dotfiles_setup/renovate_dryrun.py:95-101), `resolve_github_token()` (:164-177), and `complete=resolve_github_token() is not None` at the call site (:376), rendered as the `!! INCOMPLETE` block (:288-310)."
evidence = "python/src/dotfiles_setup/renovate_dryrun.py:95-101,164-177,288-310,376 — `complete` is a function of os.environ only; the parsed report is never consulted for it. The module ALREADY reads the authoritative field two lines away: `skipped = 1 if dep.get(\"skipReason\") else 0` (python/src/dotfiles_setup/renovate_dryrun.py:248), but only tallies it, never uses it to set `complete`."
native_alternative = "Renovate's own per-dep `skipReason: \"github-token-required\"`, set by `checkGithubToken()` during extraction and carried in the same `reportType=file` payload the module already parses. It keys on the RESOLVED hostRule (`find({hostType:\"github\", url:\"https://api.github.com\"})`), not on any env-var spelling."
alternative_evidence = "/Users/rmanaloto/.local/share/mise/installs/npm-renovate/44.59.2/node_modules/renovate/dist/util/check-token.js:9-36 (`dep.skipReason = \"github-token-required\"` at :24), called unconditionally from .../dist/workers/repository/process/extract-update.js:116. Verbatim excerpt saved at .agent/kb/raw/mod-renovate_dryrun-native-probes.md:20-45 (P1)."
rationale = "Two failure modes the env-sniff cannot see, both of which invert the report's headline. (a) A token that IS exported but is expired, revoked or wrong-scoped: `resolve_github_token()` returns it, `complete=True`, and the run prints a bare authoritative total while renovate has marked every github-datasource dep `github-token-required` — precisely the 8-vs-33 lie the INCOMPLETE banner exists to prevent, now printed WITHOUT the banner. (b) A token supplied by a route renovate supports but the list does not name (`RENOVATE_HOST_RULES`, a `config.js` hostRules block, `RENOVATE_TOKEN` with a matchHost): the run is complete, and the module still shouts INCOMPLETE and downgrades a true total to 'at least N'. Deriving `complete` from `any(dep.skipReason == \"github-token-required\")` in `parse_report` makes the verdict a property of the run rather than of the caller's shell, needs no new renovate feature, and deletes the env dependency from the parse seam. Keep `renovate_env`'s alias fan-in (finding -6 explains why it is still required) — only the VERDICT moves."
probe = "uv run --project python python -c \"from dotfiles_setup.renovate_dryrun import parse_report; import json; r=parse_report(json.dumps({'repositories':{'local':{'packageFiles':{'mise':[{'packageFile':'mise.toml','deps':[{'depName':'x','datasource':'github-tags','skipReason':'github-token-required','updates':[]}]}]}}}})); print(r.complete)\"  # today prints True (env-derived); after the refactor it must print False"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_dryrun-2"
unit = "mod-renovate_dryrun"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "Hand-tallied headline counts: `total_deps` incremented per dep in `parse_report` (python/src/dotfiles_setup/renovate_dryrun.py:238,243) and `len(result.updates)` rendered as 'Scanned N deps; M would be updated.' (:285-287, :307-310)."
evidence = ""
native_alternative = "Renovate's `libYearsWithStatus` block on the same report — `{totalDepsCount, outdatedDepsCount, totalLibYears, ...}` — which is computed inside the lookup path and written to `report.repositories[<repo>]` alongside `packageFiles`. The module's `parse_report` reads `repositories.local` (:234) and drops this key on the floor."
alternative_evidence = "/Users/rmanaloto/.local/share/mise/installs/npm-renovate/44.59.2/node_modules/renovate/dist/workers/repository/process/libyear.js:41 (`addLibYears(config, libYearsWithStatus)`), called from .../dist/workers/repository/process/extract-update.js:134 inside `async function lookup(...)` at :129 — i.e. it runs in exactly the `--dry-run=lookup` mode this task uses; written to the report at .../dist/instrumentation/reporting.js:22-25. Saved excerpt: .agent/kb/raw/mod-renovate_dryrun-native-probes.md:78-92 (P4)."
rationale = "Renovate already computes and ships the two numbers this report leads with, plus a third (total libYears behind) that the module cannot compute at all. Adopting them is cheap and gives the parser a free cross-check: `libYearsWithStatus.totalDepsCount` vs the hand tally is a second probe of the same fact by a different route, so a silent parse regression (a renamed `deps` key, a manager whose files stop being iterated) shows up as a disagreement instead of as a quietly smaller number — the exact failure class #288/#299 were about. Not a straight retire, because the semantics differ deliberately: `calculateLibYears` skips `dep.enabled === false` deps (libyear.js:9) and counts outdated DEPS, whereas the module counts pending UPDATE objects (a dep with major+minor updates contributes 2). Surface both, labelled; do not silently swap one for the other."
probe = "mise run renovate-dryrun -- --json | jq '.total_deps' , and in the same run dump the raw report's .repositories.local.libYearsWithStatus (temporarily keep the tempdir) to confirm the key is present and how totalDepsCount compares on this repo."
correction = ""
correction_custom = "**⚠ EMPTY CUSTOM-EVIDENCE FIELD IN THE FINDER OUTPUT (round 5) — the anchors were never lost, only misfiled.** This is the ONLY finding of the run (742 rows when this was written, 743 after round 6 added `mise-tasks-1-11`) whose `evidence` field is empty; the finder's JSON record for it has no `evidence` key at all, so `aggregate.py`'s `x.get('evidence','')` renders `\"\"` and the .md cell renders a bare em-dash. Control arm: a scan for `rows[] | select(.evidence == \"\")` over the aggregate returns exactly this one id, so the check discriminates. The `file:line` anchors the field should hold are sitting inside the row's own `custom_thing`: **`python/src/dotfiles_setup/renovate_dryrun.py:238,243`** (`total_deps` incremented per dep in `parse_report`) and **`:285-287`, `:307-310`** (the `'Scanned N deps; M would be updated.'` render). Read those as this row's custom-side citation. This is a render/carry defect, not a missing probe — but it is the first row a reader auditing citation discipline finds, and it was reported by two consecutive critics before being repaired here."
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_dryrun-3"
unit = "mod-renovate_dryrun"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Per-manager and per-datasource extraction tallies carrying deps/updates/skipped — `GroupStats` (python/src/dotfiles_setup/renovate_dryrun.py:122-144), `_tally`/`_stats` (:206-221), and the 'Extracted by manager (absent == matched no files)' / 'Looked up by datasource' report blocks (:314-321)."
evidence = "python/src/dotfiles_setup/renovate_dryrun.py:122-144,206-221,249-255,314-321. The docstring states the requirement this serves: 'a custom manager that matched zero files and a custom manager whose deps are all current both render as silence' (:128-131) — the question #251 posed and #288 answered only by bypassing the task."
native_alternative = "Nearest native is `extractStats()`, which builds `{managers: {<manager>: {fileCount, depCount}}, total: {...}}` — but it is emitted only to the log stream, never into the structured report, and carries no update or skip counts. The report schema itself has no rollup: `coerceRepo` creates exactly `{problems, branches, packageFiles}`, and the only writers are `addBranchStats`, `addExtractionStats`, `addLibYears`, `finalizeReport`."
alternative_evidence = "/Users/rmanaloto/.local/share/mise/installs/npm-renovate/44.59.2/node_modules/renovate/dist/workers/repository/process/extract-update.js:21-39 (the rollup) and :110-114 (`logger.info({baseBranch, stats}, 'Dependency extraction complete')` — log-only); /Users/rmanaloto/.local/share/mise/installs/npm-renovate/44.59.2/node_modules/renovate/dist/instrumentation/reporting.js (coerceRepo, end of file). Saved excerpt: .agent/kb/raw/mod-renovate_dryrun-native-probes.md:57-77 (P3)."
rationale = "Justification, not a default: the native rollup is strictly weaker on two axes and unreachable on a third. It has fileCount/depCount but no `updates` and no `skipped`, so it cannot distinguish 'this manager matched files and everything is current' from 'this manager matched files and every dep was skipped' — the distinction the skipped column exists for and the one that made #288 mistrust the task. It has no per-DATASOURCE view at all. And it lives in the debug log, so consuming it means scraping a ~5k-line log stream, which the module rejected on the record for `use-tool-builtins`' own reasoning (:34-39) and which would re-introduce a parser bound to log wording nobody here owns (`probes-need-a-control-arm.md` rule 9). Nothing at renovate 44.59.2 or 44.70.1 puts these tallies in the report."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_dryrun-4"
unit = "mod-renovate_dryrun"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`--check` turning pending updates into rc=1 — `decide_exit_code` (python/src/dotfiles_setup/renovate_dryrun.py:332-350) and the two-failure-mode diagnostic on a non-zero renovate exit that distinguishes 'report written, exit code is the complaint' from 'no report at all' (:358-374)."
evidence = "python/src/dotfiles_setup/renovate_dryrun.py:332-350 (`if check and result.updates: return 1`, mirroring `mise run sync -- --check`), :358-374 (the report-on-disk vs no-report branch, added in #299)."
native_alternative = "None found. Renovate's dry-run modes are `extract` / `lookup` / `full`; the local platform coerces every non-extract value to `lookup` and the process exit code reports renovate's own health (config errors, unsupported node), never 'updates are pending'. No self-hosted option in the saved configuration-options page turns a pending update into a non-zero exit."
alternative_evidence = "/Users/rmanaloto/.local/share/mise/installs/npm-renovate/44.59.2/node_modules/renovate/dist/modules/platform/local/index.js:33-34 (`const dryRun = params.dryRun === \"extract\" ? \"extract\" : \"lookup\";`); 0-result search for an exit-code option in .agent/kb/raw/renovate-config-options.md, control-armed by `cloneSubmodules` returning hits at .agent/kb/raw/renovate-config-options.md:940 and :4828 with the same command shape. Saved: .agent/kb/raw/mod-renovate_dryrun-native-probes.md:94-120 (P5) and the control-arm note at :10-19."
rationale = "Justification: the gate semantics ('any pending update => rc=1') are this repo's convention, deliberately aligned with `mise run sync -- --check` so the flag means one thing across the task set, and no renovate mode expresses it. The exit-code triage message is likewise about a renovate behaviour (extract succeeds, process still exits non-zero under an unsupported node) that renovate itself gives no structured signal for — the module distinguishes the two cases by testing whether the report file exists, which is the only available discriminator. A separate note, not a finding: the node-24 pin that makes this rare is already the NATIVE mechanism (`tasks.*.tools` per-task version override, mise.toml:876), not custom code."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_dryrun-5"
unit = "mod-renovate_dryrun"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "`_FORCE_CONFIG = '{\"cloneSubmodules\":false}'` injected as `RENOVATE_FORCE` (python/src/dotfiles_setup/renovate_dryrun.py:70,185) to stop the `github>jdx/renovate-config` preset's `cloneSubmodules: true` from killing every local run."
evidence = "python/src/dotfiles_setup/renovate_dryrun.py:67-70,185; the docstring's fact 1 at :17-27 records why env/CLI lose to the preset and why `force` is the only layer that wins."
native_alternative = "This IS the native mechanism — `force` is Renovate's documented self-hosted config layer applied after repo config resolution — and the crash it works around is NOT fixed upstream, so there is nothing to retire. Checked against the newest renovate build installed on this host (44.70.1), not only the pin (44.59.2)."
alternative_evidence = ".agent/kb/raw/renovate-config-options.md:10165 ('If you need to override constraints that Renovate detects from the repository, wrap it in the global self-hosted configuration's `force` object'). Still-broken chain, byte-identical in 44.59.2 and 44.70.1: .../dist/workers/repository/init/index.js:58 -> .../dist/util/git/index.js:282-286 (`await syncGit()`) -> .../dist/util/git/index.js:334 (`throw new Error(\"Cannot sync git when platform=local\")`). Saved: .agent/kb/raw/mod-renovate_dryrun-native-probes.md:94-120 (P5)."
rationale = "Justification: the custom surface here is three tokens of JSON handed to a documented native config layer — the opposite of homegrown machinery — and the alternative (dropping the jdx preset, or adding `cloneSubmodules: false` to renovate.json) would make the file the local task reads differ from the file hosted Renovate reads, which is the property the module deliberately preserves (:26-27). The retire question was asked properly and answered NO by source, not by assumption: `cloneSubmodules(true)` still calls `syncGit()` before it looks at `cloneSubmodulesFilter`, and `syncGit()` still throws unconditionally under `platform=local`, at the newest build available on this host. The sibling node-24 pin is also still load-bearing: 44.70.1's package.json still declares `engines.node ^24.11.0`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_dryrun-6"
unit = "mod-renovate_dryrun"
kind = "module"
disposition = "unknown"
confidence = 0.40
custom_thing = "The multi-name token fan-in itself — trying GITHUB_COM_TOKEN, RENOVATE_GITHUB_COM_TOKEN, GITHUB_TOKEN, GITHUB_API_TOKEN, MISE_GITHUB_TOKEN and re-exporting the winner as GITHUB_COM_TOKEN (python/src/dotfiles_setup/renovate_dryrun.py:95-101,164-191), with no fallback when none is set."
evidence = "python/src/dotfiles_setup/renovate_dryrun.py:95-101 (the alias list), :180-191 (`renovate_env` re-exports as GITHUB_COM_TOKEN). Renovate reads only two of those five names and actively deletes GITHUB_TOKEN from its own env, so the alias fan-in has no native equivalent and must stay."
native_alternative = "`gh auth token` (optionally `--hostname github.com`) as a LAST-RESORT fallback when none of the five env vars is set — the canonical native way to obtain the operator's already-authenticated github.com token without inventing a credential path."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/auth/token/token.go (the command) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/docs/multiple-accounts.md:92 ('We can use `gh auth token --user` to get a specific token'); control arm: `gh auth login` hits the same corpus at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/docs/macos-keyring.md:6. Renovate-side: .../dist/workers/global/config/parse/env.js:151,165. Saved: .agent/kb/raw/mod-renovate_dryrun-native-probes.md:47-56 (P2) and :122-136 (P6)."
rationale = "Genuinely undecided, and the deciding facts are local to this host rather than to renovate. FOR: a fallback would make an INCOMPLETE run impossible for any operator who has run `gh auth login`, removing the report's one remaining foot-gun. AGAINST, and it is serious: this repo's own record (.claude/rules/secrets-out-of-the-shell-env.md, memory feedback_secrets_live_in_the_shell_env) is that `gh` reading its token from the macOS keychain HUNG FOREVER from non-GUI/background processes — 190 stuck processes, load 13.5 — until the `gh:github.com` keychain entry was deleted, after which `gh` falls through to the ENV token, i.e. to exactly what the module already reads. Under the current env=true posture the fallback would therefore buy nothing and could reintroduce a hang inside a 900s-timeout subprocess. Do not add it on reasoning alone; settle it with the probe. Also record: never print or compare the value — presence flag only."
probe = "env -u GITHUB_COM_TOKEN -u RENOVATE_GITHUB_COM_TOKEN -u GITHUB_TOKEN -u GITHUB_API_TOKEN -u MISE_GITHUB_TOKEN sh -c 'timeout 10 gh auth token >/dev/null 2>&1; echo rc=$?'  # rc=0 => a fallback would work and returns promptly; rc=124 => the keychain-hang class is back and the fallback must NOT be added; rc=1 => no stored token, fallback buys nothing here"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_validate-1"
unit = "mod-renovate_validate"
kind = "module"
disposition = "keep"
confidence = 0.95
custom_thing = "The RE2 engine-health assertion: a lookahead canary config that the native validator MUST reject before its verdict on the real config is believed (RE2_CANARY_CONFIG at python/src/dotfiles_setup/renovate_validate.py:84-95; engine_rejects_lookahead() at :117-127; the inverted rc check + short-circuit at :137-151)."
evidence = "python/src/dotfiles_setup/renovate_validate.py:117-127 (canary written to a tempdir and validated), :137-151 (rc==0 from the canary is the alarm and returns 1 BEFORE the real config is read); hk.pkl:485-489 wires the step to `uv run --project python dotfiles-setup renovate-validate` rather than the bare validator; python/verification/suites.toml:1534-1546 (workflow.renovate-re2-engine-assertion) binds the chain; tests/test_renovate_validate.py:49-60 (canary keeps an RE2-unsupported construct) and :113-127 (degraded engine never reaches the real config)."
native_alternative = "renovate-config-validator's own `--strict` flag (\"Fail command if any configuration warnings, errors, or a migration is needed\") plus its built-in `regexEngineStatus` reporting — i.e. letting the native tool's exit code speak for engine health."
alternative_evidence = ".agent/kb/raw/mod-renovate_validate-native-probes.md (P1, P2), quoting the host's pinned install verbatim: /Users/rmanaloto/.local/share/mise/installs/npm-renovate/44.59.2/node_modules/renovate/dist/config-validator.js:104 `logger.warn(..., \"RE2 not usable, falling back to RegExp: regex validation may be inaccurate\")` with no assignment to returnVal, :105 the RENOVATE_X_IGNORE_RE2 branch at debug level only, :100 the `--strict` option declaration, and every returnVal=1 site (:61 migration, :70 errors, :77 warnings, :113/:126/:133/:150/:157/:189 file read) all inside validate()/the file loop. package.json at the same install still declares optionalDependencies {openpgp, re2}, so the optional-dep shape is unchanged at pin 44.59.2."
rationale = "Required justification: at the repo's own pin (npm:renovate 44.59.2) the degradation is STILL silent — the RE2-unavailable warning is emitted outside every code path that sets the exit code, so `--strict` promotes schema warnings but not this one, and no flag or env var makes the process exit non-zero on a degraded engine (RENOVATE_X_IGNORE_RE2 only logs at debug). Nothing in hk, mise, uv, gh, renovate presets, or any pinned linter observes another tool's internal regex engine. The custom code is also already minimal and native-first: it parses no regex and keeps no blacklist of RE2-unsupported constructs; the only added logic is the inverted canary, and it is deliberately capability-shaped rather than a log-string sniff (.claude/rules/probes-need-a-control-arm.md:135). Retiring it would restore exactly the #644 failure mode: a green rc over unchecked regexes."
probe = "Re-run at any future renovate bump: `grep -n 'returnVal' \"$(dirname $(dirname $(mise which renovate-config-validator)))/node_modules/renovate/dist/config-validator.js\"` and confirm no returnVal assignment sits in the `regexEngineStatus.type === \"unavailable\"` branch; if one appears, this finding flips to retire."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_validate-2"
unit = "mod-renovate_validate"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "The thin subprocess wrapper around the native validator — VALIDATOR/VALIDATOR_FLAGS (`--strict --no-global`) and run_validator()/ValidatorRun (python/src/dotfiles_setup/renovate_validate.py:72-78, :98-114), invoked through a `dotfiles-setup renovate-validate` CLI subcommand (python/src/dotfiles_setup/main.py:663-668, :2417) instead of hk calling the binary directly."
evidence = "python/src/dotfiles_setup/renovate_validate.py:106-114 (subprocess.run of `renovate-config-validator --strict --no-global <path>`, capturing rc + merged stdout/stderr); hk.pkl:485-489 (`glob = List(\"renovate.json\")`, `check = \"uv run --project python dotfiles-setup renovate-validate\"`); hk.pkl:456-484 records that the step previously called the validator directly and was rerouted only because the validator degrades silently."
native_alternative = "An hk builtin step for renovate (the pattern hk already ships for actionlint, hadolint, gitleaks, pinact, shellcheck, ty, typos, zizmor, mdschema, ...), which would let hk.pkl call the tool with no repo-side python at all."
alternative_evidence = ".agent/kb/raw/mod-renovate_validate-native-probes.md (P3): `ls /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins | grep -i renovate` -> rc=1, zero hits across ~145 builtins; control arm — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/shellcheck.pkl and zizmor.pkl are present in the same listing, and the only `renovate` string under pkl/ is the Renovate comment at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/aqua_update_checksum.pkl:45. hk 1.58.1, the only release newer than the 1.57.0 pin, is documentation-only (.agent/kb/raw/hk-releases-since-pin.digest.md)."
rationale = "Required justification: two independent reasons. (a) No renovate builtin exists in hk at the pinned 1.57.0 (or in 1.58.1), control-armed above, so there is nothing native to adopt for the wiring. (b) Even if one shipped, it would call the bare binary, whose rc cannot be trusted per finding 1 — the wrapper exists precisely to order the canary FIRST, a property that suites.toml:1534 and tests/test_renovate_validate.py:113-127 bind as a safety guarantee. The wrapper adds no validation logic of its own: the flags are the tool's documented ones (`--strict`, `--no-global`, declared at config-validator.js:100 in the pinned install) and every verdict comes from the native process's exit code."
probe = "If hk later ships a renovate builtin: `ls $(mise where hk 2>/dev/null)/pkl/builtins | grep -i renovate` — a hit would make the flags/glob portion adoptable, but the canary ordering would still have to wrap it."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-renovate_validate-3"
unit = "mod-renovate_validate"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Running renovate.json validation locally at all — a repo-side pre-commit/CI gate that says whether the Renovate config is valid (renovate_validate_main at python/src/dotfiles_setup/renovate_validate.py:130-169, wired as the hk step `renovate_config_validate`)."
evidence = "hk.pkl:485-489 (the step, glob renovate.json); python/src/dotfiles_setup/renovate_validate.py:153-169 (locates repo_root/renovate.json, runs the validator, propagates its rc); tests/test_renovate_validate.py:181-186 asserts this repo's renovate.json passes the full gate."
native_alternative = "Renovate the hosted app's own config validation, surfaced as the native status check `renovate/config-validation` (option `statusCheckNames.configValidation`, with `statusCheckWhen.configValidation` defaulting to `\"always\"`)."
alternative_evidence = ".agent/kb/raw/renovate-config-options.md:19976 (`\"configValidation\": \"renovate/config-validation\"` in the statusCheckNames default), :20077 (the `configValidation` option row) and :20031 (`\"configValidation\": \"always\"` in the statusCheckWhen default); summarised with the probe's control arm in .agent/kb/raw/mod-renovate_validate-native-probes.md (P4)."
rationale = "Required justification: the native check covers the same question but at the wrong time and with the wrong instrument. It only fires during a hosted Renovate run — after the config has been pushed, on branches Renovate itself processes — so it cannot gate a commit, and a broken renovate.json would silently stop dependency updates until the next scheduled run rather than failing lint. It also runs on an install that has re2, so by construction it can never surface the local-validator degradation this module was built for (#644's inversion: the LOCAL gate being weaker than the remote one). It is a complementary backstop, not a superseding mechanism; nothing in the module should be deleted in its favour. hk.pkl:463-471 additionally records that the validator is complementary to the repo-specific suites.toml contracts (ci.regex-managers-enabled, ci.renovate-extends-jdx-preset), which neither instrument covers."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-rule_registry-1"
unit = "mod-rule_registry"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "Malformed-frontmatter detection: the `load_class == \"malformed\"` arm plus `malformed_detail`, which classifies a rule file whose YAML frontmatter will not parse, or whose `paths:` list holds a non-string item."
evidence = "python/src/dotfiles_setup/rule_registry.py:253-266 (`_malformed`), :326-341 (the `yaml.YAMLError` arm and the non-string-item arm), :29-33 (module docstring: 'never silently drops a rule it cannot parse')."
native_alternative = "agnix CC-MEM-011 (`Invalid Paths Glob in Rules`, severity HIGH, normative MUST) and CC-MEM-012 (`Rules File Unknown Frontmatter Key`, MEDIUM) already parse `.claude/rules/**/*.md` frontmatter, emit a diagnostic for an unparsable/unterminated block, and reject any key other than `paths`. Both are enabled here (`[rules] memory = true`, `frontmatter_validation = true`, `disabled_rules = [\"CC-SK-017\"]` only) and gated by `mise run lint-docs` = `agnix . --strict`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/claude_rules.rs:1-5 and :75-87 (parse_error -> CC-MEM-011 diagnostic); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/locales/en.yml:329-331 (CC-MEM-012 text); live probe at the pinned 0.52.1: `agnix explain CC-MEM-011` -> 'Severity: HIGH / Normative level: MUST / Applies to: claude-code', control arm `agnix explain CC-MEM-999` -> 'Error: unknown rule ID'. Wiring: mise.toml:1257-1260, .agnix.toml:141-149."
rationale = "agnix already DETECTS and GATES exactly this class, so the registry should not grow its own enforcement on top; what it uniquely adds is a machine-readable RECORD (a typed `RuleRecord` a downstream consumer can join against) rather than a lint diagnostic. Keep the record, but the #927 gate must not re-report what CC-MEM-011/012 already fail the build on — that would be two gates for one defect with two different severities."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-rule_registry-2"
unit = "mod-rule_registry"
kind = "module"
disposition = "retire"
confidence = 0.75
custom_thing = "The glob-resolution half of the #927 gate this registry exists to feed. `RuleRecord.globs` stores the `paths:` list verbatim with zero syntax or resolution checking, and #927 ('Rule-corpus gate: globs resolve, and every eager rule states a reason') plans to add that checking on top of this module."
evidence = "python/src/dotfiles_setup/rule_registry.py:143 (`globs: tuple[str, ...]`), :332-343 (only an isinstance/all-strings check, no glob parse), :4-9 (docstring names the #927 gate as a consumer); `gh issue view 927` -> OPEN, 'Rule-corpus gate: globs resolve, and every eager rule states a reason'."
native_alternative = "agnix CC-MEM-011 already validates every `paths:` pattern's glob syntax per item and reports the offending index and line, at HIGH/MUST, on every `mise run lint-docs` and in CI. Its bad-example is literally `paths: - \"[unclosed\"`. Claude Code's own documented failure semantics for an unparsable bracket expression are that the pattern matches nothing while the rule's other patterns keep working."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/src/rules/claude_rules.rs:89-118 (per-index `validate_glob_pattern` loop); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/locales/en.yml:326-328; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:255-257 (invalid `[` matches nothing, other patterns keep working)."
rationale = "Do not build the 'globs resolve' half of #927 on this module: an existing, pinned, already-gated linter checks glob VALIDITY better than a bespoke checker would (it knows Claude Code's bracket-expression semantics and reports the item index). What agnix cannot answer is whether a syntactically-valid glob matches any file that exists today — a dead-glob check is the only part of #927's glob half that is genuinely un-covered, and it should be scoped down to that."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-rule_registry-3"
unit = "mod-rule_registry"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "A THIRD independent `paths:`-frontmatter parser and load-class classifier in this repo: `_FRONTMATTER_RE` + the `isinstance(front.get(\"paths\"), list)` predicate producing the scoped/eager/malformed partition."
evidence = "python/src/dotfiles_setup/rule_registry.py:58 (`_FRONTMATTER_RE`, byte-identical to instructions_report.py:59), :332-348 (the predicate), :11-20 (the module's own docstring admits three classifiers exist and already disagree on four frontmatter shapes, tracked by #951); the second parser is python/src/dotfiles_setup/instructions_report.py:110-144 (`scoped_rules_on_disk`); `gh issue view 951` -> OPEN."
native_alternative = "`kb_setup.md_budget.has_paths_frontmatter` / `classify` / `tracked_files` — the SHA-pinned shared engine in the knowledge-base repo that hk's own `md_size_budget` step already runs, consumed here as a `uv` git dep. The project has an explicit, executed precedent for this consolidation: dotfiles' own `md_budget.py` and its tests were DELETED when the enforcer moved to `kb_setup`, 'so there is nothing left to diverge'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/python/src/kb_setup/md_budget.py:282 (`def has_paths_frontmatter`), :475 (`if cls == \"rule_unscoped\" and has_paths_frontmatter(raw)`); precedent recorded at hk.pkl:610-616 ('The enforcer MOVED to kb_setup.md_budget (2026-07-25) ... ONE implementation both repos run ... dotfiles' md_budget.py + tests were DELETED in the same change')."
rationale = "The repo's own stated doctrine (hk.pkl:610-616, and tool-currency-and-native-first.md rule 3) is one implementation, not a second copy that drifts — and here there are three, whose disagreement is a known OPEN defect (#951) rather than a hypothetical. rule_registry should call `kb_setup.md_budget.has_paths_frontmatter` for the scoped/eager decision and keep only what the shared engine has no opinion on (`malformed_detail`, `eager_reason`, `inject`). The live tripwire in tests/test_rule_registry.py:463-480 asserting all three currently agree is a symptom-watcher, not a fix; it will go red the day someone authors any of the four divergent shapes."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-rule_registry-4"
unit = "mod-rule_registry"
kind = "module"
disposition = "refactor"
confidence = 0.65
custom_thing = "`RuleRecord.body_bytes` — a per-rule whole-file on-disk byte measurement via `path.stat().st_size`, with its own CRLF-undercount rationale."
evidence = "python/src/dotfiles_setup/rule_registry.py:148-155 (the field and its docstring), :245-250 (`_disk_size`), :278-298 (`_read_source` returning the byte count alongside the text)."
native_alternative = "`kb_setup.md_budget` already measures every rule file's size and enforces a per-load-class byte and line budget over the same corpus, reached via `tracked_files()` (`git ls-files`) -> `classify()`. It is run as hk's `md_size_budget` step on every `mise run lint`, so the measurement already exists on the same files, under the same shared dep."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/python/src/kb_setup/md_budget.py:475 (classify + has_paths_frontmatter feeding the per-class budget); hk.pkl:592-604 (the per-load-class line/byte ceilings the step enforces: eager 200, lazy/cond 400, skill 500)."
rationale = "A second size measurement over the same corpus is exactly the drift surface #951 is about, one axis over. If a consumer needs bytes per rule, expose them from the shared engine rather than re-`stat`ing; the CRLF subtlety documented at :148-155 is real, which is an argument for having it in ONE place, not two. Lower confidence than finding 3 because I did not confirm kb_setup exposes a per-file byte figure as a public return value rather than only as an internal budget comparison."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-rule_registry-5"
unit = "mod-rule_registry"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "Deriving each rule's load class (eager vs conditional) by parsing frontmatter off disk — i.e. predicting what the harness will do, rather than observing it."
evidence = "python/src/dotfiles_setup/rule_registry.py:316-348 (`load_class` decided entirely from the file's own bytes), :182-184 (`by_load_class`)."
native_alternative = "The `InstructionsLoaded` hook event — Claude Code's own notification of which CLAUDE.md and `.claude/rules/*.md` files were loaded into context, and therefore the harness's authoritative load-class signal. It is already wired in this repo (settings.json registration -> `python -m dotfiles_setup.instructions_observer`, reported by `dotfiles-setup instructions-report`), and the whole chain is contract-asserted."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/changelog.md:4130 ('Added `InstructionsLoaded` hook event that fires when CLAUDE.md or `.claude/rules/*.md` files are loaded into context'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:199 and :205-225 (unscoped rules load at launch; `paths:` rules trigger on READ, 'not on every tool use'); in-repo wiring: python/verification/suites.toml:1411-1426."
rationale = "Partial cover, and the gap is precisely why the disk parser exists: an observation can only report a rule that actually fired, so a scoped rule with a dead glob is invisible to the observer (stated at mise.toml:836-838), and a fresh corpus has no session history. But for every rule that HAS loaded, the hook is ground truth and the frontmatter parse is a guess — so the registry should JOIN against the observer's records rather than be a standalone oracle, and any consumer reporting 'this rule is eager' should prefer the observed answer where one exists."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-rule_registry-6"
unit = "mod-rule_registry"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "`INJECT_PATHS`, a hard-coded two-element frozenset of repo-relative rule paths saying which rules a future write-trigger hook (#928) should inject — deliberately kept OUT of frontmatter, because #916 decision 1 pins `paths:` as the only frontmatter key."
evidence = "python/src/dotfiles_setup/rule_registry.py:92-102 (the frozenset and its 'do not add a second frontmatter key' note), :305 (`inject = rel_path in INJECT_PATHS`), :147 (`inject: bool`)."
native_alternative = "Two native mechanisms split this: (a) READ-time conditional injection is already `paths:` frontmatter itself, which Claude Code applies natively and recursively including nested subdirectories; (b) WRITE-time injection is a PostToolUse hook on `Edit|Write|NotebookEdit` returning `additionalContext` — the exact seam this repo already runs for a different purpose (`mise run mise-config-context`, wired from .claude/settings.json)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:205-225 (paths: frontmatter, read-trigger, 'not on every tool use') and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/claude-directory.md:169 ('Use `paths:` frontmatter with globs to scope rules'; 'Subdirectories work ... discovered automatically'); in-repo precedent for the write-time seam: mise.toml:1262-1269 ('PostToolUse hook body: ... reads the hook payload on stdin and writes JSON to stdout')."
rationale = "The write trigger genuinely has no native cover — the docs state outright that `paths:` fires on READ, not on every tool use — so #928 is not superseded. But the SET of injectable rules should not be a second, out-of-band source of truth living in Python: it duplicates, in a different spelling, what `paths:` already expresses for the read direction, and the two can silently disagree about the same rule. Adopt the PostToolUse `additionalContext` seam the repo already runs, and derive the inject set from each rule's own `paths:` globs (matched against the WRITTEN path) instead of a hard-coded frozenset."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-rule_registry-7"
unit = "mod-rule_registry"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`_find_eager_reason` — fence-aware, frontmatter-stripped ATX-heading scanning that extracts the stated justification for a rule being eager, plus `KNOWN_UNDETECTED_EAGER_REASONS`, a two-entry debt list for rules whose rationale is prose rather than a heading."
evidence = "python/src/dotfiles_setup/rule_registry.py:71-90 (`_ATX_HEADING_RE`, `_FENCE_RE`, `_EAGER_HEADING_MARKERS`), :206-242 (`_find_eager_reason`), :104-121 (`KNOWN_UNDETECTED_EAGER_REASONS`)."
native_alternative = "None found. agnix's Claude-memory rule family covers frontmatter validity (CC-MEM-011/012), generic/negative/weak instructions (CC-MEM-005/006/007), lost-in-the-middle placement (CC-MEM-008), token limits (CC-MEM-009) and CLAUDE.md/README overlap (CC-MEM-010) — nothing asserting that an unscoped rule states WHY it is unscoped. Claude Code has no such concept: the docs treat 'no paths field' as the plain default, with no justification requirement. `/skill-doctor` (2.1.261) is the nearest shipped analogue and it reports unused SKILLS and their context cost, not rule rationale."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agnix/crates/agnix-core/locales/en.yml:302-331 (the full CC-MEM-005..012 message set — no eager-rationale rule); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:199 ('Rules without paths frontmatter are loaded at launch' — stated as a default, no rationale requirement); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (`/skill-doctor`, 2.1.261 — skills, not rules)."
rationale = "This is the one capability with no native or third-party equivalent, and it encodes a project-specific policy no vendor could know: that eager context is the scarce resource here and an unscoped rule owes a written justification (md-size-budgets.md's 'trigger test'). Control arm on the absence: grepping the same agnix locale file for a term that IS present ('frontmatter', 'paths field') returns hits, so the zero result for an eager-rationale rule is the corpus's answer, not a broken probe. Keep — but note it is heading-anchored by operator decision (C5), so its two known blind spots are carried as a hard-coded debt list rather than by the parser."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-rule_registry-8"
unit = "mod-rule_registry"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "The module as a whole: 391 lines with no consumer anywhere in the repo — no `dotfiles-setup` subcommand, no mise task, no hk step, no `.claude/settings.json` hook, and no `python/verification/suites.toml` contract. Its only caller is its own 969-line test file."
evidence = "Control-armed grep across *.py/*.pkl/*.toml/*.json/*.md/*.sh for `rule_registry|build_registry|RuleRegistry|RuleRecord|INJECT_PATHS`: every hit outside the module itself is in tests/test_rule_registry.py. Control arm with the same command shape on a token known to be wired (`hook_guard`) returns python/verification/suites.toml plus four source modules, so the probe discriminates. Specifically: `grep -n rule_registry python/src/dotfiles_setup/main.py` -> 0 lines; suites.toml's only nearby contract is `instructions-report` (:1411-1426), which does not name this module."
native_alternative = "Not applicable — the question here is whether the unit should exist yet, not whether a vendor supersedes it. Its four named consumers are all still live: #916 (parent), #927, #928, #929 are OPEN as of 2026-09-09, as is #918 (this module's own ticket) and #951 (the classifier reconciliation)."
alternative_evidence = "`gh issue view` on each: #916 OPEN 'Rule scoping + enforcement: declare load posture, gate it, and cover the write trigger'; #918 OPEN 'Rule registry: parse rule frontmatter and bodies into records'; #927 OPEN 'Rule-corpus gate: globs resolve, and every eager rule states a reason'; #928 OPEN 'Write-trigger dispatcher: inject the rules that govern a written path'; #929 OPEN 'Corpus lane 1'; #951 OPEN 'Reconcile the three independent rule load-class classifiers'."
rationale = "Required justification: this is ahead-of-consumers infrastructure with every consumer ticket still OPEN, not abandoned code, so 'retire' would be wrong. But it is unwired today, which means the repo's own enforcement doctrine does not yet apply to it — nothing in suites.toml asserts it stays reachable, so when #927/#928 land there is no contract preventing a wiring line from being deleted while the module and its tests stay green (the exact failure `workflow.apt-pins-enforcement` and the `per_path_tokens` doctrine exist to prevent). The concrete debt: whichever ticket first wires it must ship a `[[suite]]` binding the CALL SITE, and should land findings 3 and 6 at the same time so the third classifier and the second inject source of truth never reach production."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-schema_vendor-1"
unit = "mod-schema_vendor"
kind = "module"
disposition = "refactor"
confidence = 0.82
custom_thing = "The whole vendoring design rests on one stated premise, repeated verbatim in three tracked places: 'taplo does NOT cache schemas between runs — measured: a second `taplo lint` with the network blocked still returns rc=1'."
evidence = "python/src/dotfiles_setup/schema_vendor.py:6-11; python/verification/suites.toml:2460; .github/workflows/refresh.yml:472-474"
native_alternative = "taplo DOES have a persistent on-disk schema cache — it is simply off unless `--cache-path <dir>` is passed. `GeneralArgs.cache_path` is a global CLI flag ('Set a cache path'); `taplo lint` wires it via `set_cache_path`; the cache stores each schema keyed by URL hash with a 10-minute freshness window (DEFAULT_CACHE_EXPIRATION_TIME = 60*10), and — decisive for the offline claim — `load_schema` falls back to the EXPIRED cached copy when the network fetch fails. So `taplo lint --cache-path .taplo-cache` is network-tolerant after one warm fetch."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/taplo/crates/taplo-cli/src/args.rs:29-31 (`/// Set a cache path` + `pub cache_path`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/taplo/crates/taplo-cli/src/commands/lint.rs:19 (`.set_cache_path(cmd.general.cache_path.clone())`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/taplo/crates/taplo-common/src/schema/cache.rs:14 (10-min disk expiry), :70-83 (`cache path not set` => the measured rc=1), :112 (`is_cache_path_set`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/taplo/crates/taplo-common/src/schema/mod.rs:192 (fresh cache hit), :204 (expired-cache fallback after a failed fetch)"
rationale = "The MECHANISM stays — a cold CI runner and a fresh clone both have an empty cache, so `--cache-path` cannot make the lint gate network-free on the run that matters, and it gives no byte-pinning or hash verification. But the premise as WRITTEN is false without its condition (`unless --cache-path is set`), and it is the sole justification carried in a module docstring, a verification-suite description and a workflow comment. Per `.claude/rules/probes-need-a-control-arm.md` rule 6 ('carry a number with its CONDITION'), the three copies should be corrected to 'taplo caches only when `--cache-path` is passed, and even then a cold runner still fetches' — otherwise the next reader retires the vendoring on a true-sounding sentence, or keeps it for a reason that does not hold."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-schema_vendor-2"
unit = "mod-schema_vendor"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "`_HK_HYGIENE_FIXERS` — a hand-maintained tuple naming four `hk util` subcommands and the arg that makes each one WRITE, shelled out one-by-one over a scratch copy in `_hygiene_normalize`, so the vendored bytes are a fixed point of the lint gate before they are hashed."
evidence = "python/src/dotfiles_setup/schema_vendor.py:268-273 (the table) and :276-310 (`_hygiene_normalize` loop, with the rc-1-means-changed special case)"
native_alternative = "`hk fix [FILES]… --no-stage` runs THIS repo's own configured steps against the named files, so the fixer set comes from `hk-common.pkl`'s `hygiene` group rather than a second copy in Python; `-S/--step` narrows it to those steps and `--no-stage` suppresses auto-staging."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/fix.md:5 (`hk fix [FLAGS] [FILES]…`), :11-13 (`[FILES]… — Run on specific files`), :22 (`-S --step`), :36 (`--no-stage`); the duplicated source of truth is hk-common.pkl:101-106 (`hygiene` = trailing_whitespace, newlines, mixed_line_ending, fix_smart_quotes)"
rationale = "Two lists of the same fixers drift silently in one direction: adding a fifth builtin to `hk-common.pkl`'s hygiene group leaves this table stale, `refresh` writes bytes the lint gate then rewrites, and the recorded sha256 is wrong the moment anyone runs `mise run fmt` — exactly the deadlock the table was added to remove. The docstring already concedes the vocabulary is not uniform and had to be verified per-subcommand. Adopting `hk fix` deletes the table and the rc-1 special case. Not a clean retire: `hk fix` resolves globs relative to the repo, so the scratch-file-in-tmpdir shape must become write-then-fix-in-place under `schemas/`, and the step names must be pinned with `-S` or an unrelated step could touch the file."
probe = "cp schemas/typos.json /tmp/probe.json && printf 'x  \\n' >> schemas/typos.json && hk fix schemas/typos.json --no-stage -S trailing_whitespace -S newlines -S mixed_line_ending -S fix_smart_quotes; echo rc=$?; diff <(hk util trailing-whitespace --fix /tmp/probe.json; hk util end-of-file-fixer --fix /tmp/probe.json; cat /tmp/probe.json) schemas/typos.json; git checkout -- schemas/typos.json"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-schema_vendor-3"
unit = "mod-schema_vendor"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "`_SETUP_MISE_VERSION_RE` — a hand-rolled MULTILINE regex over `.github/actions/setup-mise/action.yml` that takes the FIRST quoted `version:` key in the file, in a file the comment says carries TWO `jdx/mise-action` steps that 'both pin the same version today'."
evidence = "python/src/dotfiles_setup/schema_vendor.py:89-103 (regex + the comment conceding the first-match hazard); tests/test_schema_vendor.py:134 (`test_read_setup_mise_pin_matches_the_first_of_two_version_keys` — the test pins the hazard rather than removing it)"
native_alternative = "`yq` is already pinned in this repo (`.config/mise/conf.d/shared.toml`, yq 4.53.6) and parses YAML structurally, so the pin can be read from the specific step by path — e.g. `yq '.runs.steps[] | select(.uses | test(\"jdx/mise-action\")) | .with.version' .github/actions/setup-mise/action.yml | sort -u` — which returns EVERY mise-action step's pin and makes a future divergence between the two steps a visible multi-value result instead of a silently-wrong first match."
alternative_evidence = "Repo pin: `.config/mise/conf.d/shared.toml` (`yq = 4.53.6`, listed in this audit's REPO PINS); the yq corpus is not in the offline set, so the specific expression above is stated as the probe, not as a doc citation."
rationale = "A regex whose correctness rests on 'the two steps agree today' is a probe with one face: when they diverge, `check_drift` reports the wrong pin and either fails spuriously or — worse — passes while the schema is vendored against the version the other step uses. A structural read makes the divergence loud. Held at 0.5 because shelling out to `yq` adds a subprocess to an otherwise pure-stdlib offline check, and a stdlib alternative (anchoring the regex to the step block) removes the hazard without the dependency; either is better than the current first-match read."
probe = "mise exec -- yq '.runs.steps[] | select(.uses | test(\"jdx/mise-action\")) | .with.version' .github/actions/setup-mise/action.yml"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-schema_vendor-4"
unit = "mod-schema_vendor"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`check_drift`'s two-fact offline check (recorded version vs current pin; recorded sha256 vs bytes on disk) explicitly does NOT prove the vendored JSON is a valid JSON Schema — the suite says so in its own description: 'it cannot prove the vendored JSON is itself valid JSON Schema'."
evidence = "python/src/dotfiles_setup/schema_vendor.py:168-205; python/verification/suites.toml:2460 (the 'WHAT THIS CONTRACT CANNOT DO' clause)"
native_alternative = "`check-jsonschema --check-metaschema` — 'Instead of validating the instances against a schema, treat each file as a schema and validate them under their matching metaschemas.' The tool is ALREADY pinned in this repo (`pipx:check-jsonschema` 0.38.0) and already used as layer 3 of the devcontainer.json validation in hk.pkl, so adding `check-jsonschema --check-metaschema schemas/*.json` as an hk step costs no new dependency and no network call."
alternative_evidence = "Live probe on the repo-pinned binary (control-armed against the same `--help` output, which also shows `--schemafile`/`--no-cache`): `mise exec -- check-jsonschema --help` prints `--check-metaschema  Instead of validating the instances against a schema, treat each file as a schema and validate them under their matching metaschemas.`; pin at `.config/mise/conf.d/shared.toml:40` (`\"pipx:check-jsonschema\" = \"0.38.0\"`); existing use at hk.pkl:143 ('Layer 3: check-jsonschema vs official spec schema URL')"
rationale = "The gap the contract names in its own text is closed by a flag on a tool the repo already installs and already invokes. The sha256 half stays (it detects a hand-edit that still parses); the metaschema check adds what the hash cannot say — that a fetched-and-normalised file is still something taplo can load as a schema. This is a wrap/adopt, not a retire: nothing native knows about `sources.toml`, the pins, or the fetch."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-schema_vendor-5"
unit = "mod-schema_vendor"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "`refresh` + the standalone `schema-refresh` job in refresh.yml: a scheduled CI job that re-downloads each vendored schema at the tool's CURRENT pin and opens a PR when the bytes or version moved — i.e. our own 'run a command after a dependency bump and commit the result' machinery."
evidence = "python/src/dotfiles_setup/schema_vendor.py:338-394; .github/workflows/refresh.yml:477-526 (job `schema-refresh`, `run: mise run schema-vendor-refresh`, then `open-refresh-pr`)"
native_alternative = "Renovate's `postUpgradeTasks` ('Post-upgrade tasks that are executed before a commit is made by Renovate', with `commands`/`fileFilters`/`executionMode`) would run `mise run schema-vendor-refresh` inside the very PR that bumps typos/ruff/mise, so the schema and its pin move in ONE commit instead of a follow-up PR raised a day later."
alternative_evidence = ".agent/kb/raw/renovate-config-options.md:17713 (`postUpgradeTasks` heading), :17714 ('Post-upgrade tasks that are executed before a commit is made by Renovate'), :17769 (`postUpgradeTasks.commands`), :17812 ('They will be compiled prior to the comparison against allowedCommands' — `allowedCommands` is a self-hosted admin option). This repo runs the HOSTED Renovate app: mise.toml:859 ('leaving renovate.json byte-identical to what hosted Renovate reads') and renovate-dryrun is explicitly a local read-only dry run, not the bot."
rationale = "Justified keep, not a default: `postUpgradeTasks.commands` is gated on `allowedCommands`, which only exists in self-hosted Renovate admin config, and this repo is on the hosted app — so the native mechanism is unreachable at the current deployment. The sibling idea (a Renovate `customManager` bumping the `version`/`source` fields of `schemas/sources.toml`, in the shape of the existing hk-pkl customManager at renovate.json:85-97) is actively WRONG here: Renovate can rewrite the version string but cannot re-download the JSON or recompute `sha256`, so it would make `sources.toml` assert a version the bytes do not have — the precise failure `check_drift`'s hash arm exists to catch."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-schema_vendor-6"
unit = "mod-schema_vendor"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "`_source_url` + `_curl_fetch` — three hard-coded raw.githubusercontent tag templates and a `curl -fsSL` subprocess, rather than generating each schema from the installed tool."
evidence = "python/src/dotfiles_setup/schema_vendor.py:208-217 (`_source_url`), :313-335 (`_curl_fetch`)"
native_alternative = "The obvious native route would be asking each tool to emit its own schema locally (no network, always version-matched). It does not exist for these three: mise's shipped CLI has no schema-emitting subcommand — `mise generate` offers config, devcontainer, git-pre-commit, github-action, install-script, task-docs, task-stubs, tool-stub and nothing else, and `schema/*.json` is a file in mise's repo, not a binary output; ruff's shipped `Command` enum is Check, Rule, Config, Linter, Clean, GenerateShellCompletion, Format, Server, Analyze, Version — no schema command (its `ruff.schema.json` is generated by the dev crate, not the release binary)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/generate/ (full subcommand listing: config.md, devcontainer.md, git-pre-commit.md, github-action.md, install-script.md, task-docs.md, task-stubs.md, tool-stub.md) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/schema/mise.json (repo file, matching the vendored source URL); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff/src/args.rs:133-186 (the complete `enum Command`)"
rationale = "Justified keep by enumeration rather than by a 0-result grep: both tools' full command surfaces were listed and neither can print its schema, so a tagged fetch is the only way to obtain the exact bytes for a pinned version. `curl` as the fetch mechanism is this repo's existing convention (`gcc_sha._default_fetcher`), needs no auth for raw.githubusercontent, and `-f` already turns an HTTP error page into a non-zero exit; routing it through `gh api` would add an auth dependency to a job that needs none."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-schema_vendor-7"
unit = "mod-schema_vendor"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "The unit as a whole: a vendored-schema manifest (`schemas/sources.toml`) plus an offline drift check wired into `mise run verify` via the `schema_drift` handler, and two CLI verbs surfaced as mise tasks."
evidence = "python/src/dotfiles_setup/verify.py:651-670 (`_handle_schema_drift`, handler registry); python/verification/suites.toml:2459-2463 (`config.schema-vendor-drift`), :2479-2480 (`config.schema-vendor-directives-bound`); mise.toml:1358-1364 (both tasks); python/src/dotfiles_setup/main.py:1345-1355 (subcommand wiring); tests/test_schema_vendor.py (420 lines, incl. `test_check_drift_is_not_a_stub_that_always_passes` at :222)"
native_alternative = "No native mechanism binds 'a file vendored from URL U at version V, where V is read from three different local pin files'. The nearest candidates were checked and each covers only a slice: `check-jsonschema --schemafile <URI>` does keep a persistent cross-run cache of REMOTE schemas ('If a remote file is used, it will be downloaded and cached locally based on mtime') and can validate TOML instances (`--force-filetype toml`), so it could in principle replace the taplo `#:schema` route entirely; mise's own `mise doctor`/`mise lock` know nothing about third-party schema files; hk has no integrity or provenance builtin."
alternative_evidence = "Live probe of the pinned binary: `mise exec -- check-jsonschema --help` → `--schemafile [PATH|URI] … If a remote file is used, it will be downloaded and cached locally based on mtime`, `--force-filetype [json|toml|yaml]`, `--no-cache`; hk builtin surface at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/builtins.md (no integrity/provenance builtin)"
rationale = "Justified keep: the check-jsonschema cache is per-machine under the user cache dir, so on a cold CI runner the first validation still fetches — swapping to it would move the lint gate's network dependency from 'never' to 'once per runner', which is the exact property the vendoring buys, and it would also drop byte-pinning (an mtime-keyed cache does not verify what it fetched). What no native tool provides at all is the pin-to-vendored-version binding: reading typos' pin from shared.toml, ruff's from uv.lock and mise's from a GHA action file, and asserting all three against one manifest. That mapping is repo-specific by construction."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-script_guard-1"
unit = "mod-script_guard"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Write-time deny of a NEW shell script by SUFFIX (.sh/.bash/.zsh/.ksh), dispatched from the PreToolUse guard on Edit/Write/NotebookEdit."
evidence = "python/src/dotfiles_setup/script_guard.py:47 (TOOLS), :50 (SHELL_SUFFIXES), :159 (suffix branch of is_shell_script), :188-209 (decide); wired at python/src/dotfiles_setup/hook_guard.py:805 and .claude/settings.json:52-62 (PreToolUse matcher 'Bash|AskUserQuestion|Edit|Write|NotebookEdit')."
native_alternative = "Claude Code permission rules: a settings deny (or ask) entry such as \"Edit(**/*.sh)\" in permissions.deny, which blocks the file tools on matching paths in every mode without any custom code."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:316 (file checks consult Edit(path)/Read(path) only — a Write()/NotebookEdit() path rule is accepted but never consulted); :369-372 (**/ needed to match a directory at any depth); :64 and :66 (rules evaluate deny, then ask, then allow; a broad deny rule cannot carry allowlist exceptions, and the same precedence holds between ask and allow); permission-modes.md:30 (deny rules block in every mode, including bypassPermissions)."
rationale = "The native rule is coarser in exactly the three dimensions that make this guard usable. (a) It cannot carry exceptions: permissions.md:66 states a broad deny cannot be narrowed by a more specific allow, so \"Edit(**/*.sh)\" would also block every edit to the ~20 sanctioned wrappers in bash_budget.ALLOWLIST (python/src/dotfiles_setup/bash_budget.py:66+) — including scripts/pretooluse-guard.sh, the wrapper this very hook runs from. An ask rule has the identical precedence problem and would prompt on every legitimate edit. (b) It cannot distinguish a NEW file from an edit to an existing one; the guard's contract is explicitly new-only (script_guard.py:175-184, added after a cold review found a tracked wrapper being denied with no usable escape hatch). (c) It cannot consult git-ignore state, so a scratchpad .sh would be denied too (script_guard.py:185). The commit-time siblings (hk's bash_logic_budget, hk.pkl:247) are git-hook-based and structurally cannot fire at write time, which is the whole reason this module exists (script_guard.py:7-15). Nothing at the pinned host version (Claude Code 2.1.266, incl. the 2.1.258-2.1.266 gap digest, which adds no permission-rule expressiveness) closes those three gaps."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-script_guard-2"
unit = "mod-script_guard"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "Content-based detection: has_shell_shebang() classifies a file as a shell script from the first line of the pending content (env -S handling, tab separator, whole-token interpreter match, case-folded), so an extensionless or mis-suffixed script (scripts/deploy, scripts/deploy.txt) is caught."
evidence = "python/src/dotfiles_setup/script_guard.py:107-139 (has_shell_shebang), :142-148 (_written_content reads content/new_string/new_source across the three write tools), :151-161 (is_shell_script falls through to content)."
native_alternative = "None in the permission system — Claude Code file rules are path patterns only; a runtime PreToolUse hook is the documented mechanism for a decision that depends on the tool INPUT rather than the path, which is what this module already is."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:316 and :328-372 (every file rule form is a path pattern; no content predicate exists); permissions.md:493-495 (PreToolUse hooks evaluate permissions at runtime and can deny the call); hooks.md:1540 (PreToolUse event) — i.e. the native answer IS the hook this module implements."
rationale = "No native path-glob can see a shebang, and the linters that can (shellcheck 0.11.0, ast-grep 0.45.3, hk builtins) all run at commit time through hk — the layer this module exists to precede. bash_budget's own SCOPE_PATHSPECS are *.sh globs, so the extensionless case is invisible to the commit gate too (script_guard.py:17-21, :151-158). The custom code is the minimum: it wraps the harness's own hook contract rather than reimplementing a permission engine."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-script_guard-3"
unit = "mod-script_guard"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "The guard's tool set is Claude-Code-only: TOOLS = {Edit, Write, NotebookEdit} and decide() keys on tool_input['file_path'], so it fires only for Claude Code's file tools. Codex-lane writes — the repo's declared implementation lane — are not covered."
evidence = "python/src/dotfiles_setup/script_guard.py:47 (TOOLS), :195-199 (decide returns None unless tool_input['file_path'] is a non-empty str), :212-214 (handles); python/src/dotfiles_setup/hook_guard.py:805 (dispatch is gated on branch_guard.handles(tool_name), the same three names) — so a tool_name of 'apply_patch' falls through to the Bash-command branch and the patch body is matched as if it were a shell command. .claude/CLAUDE.md declares 'fable-orchestrator: implementation lane = codex'. An UNTRACKED .codex/hooks.json (git ls-files returns nothing for it) already points codex's PreToolUse at scripts/pretooluse-guard.sh with the Claude tool names in its matcher."
native_alternative = "codex-cli 0.152.1 PreToolUse hooks: repo-local .codex/hooks.json, matcher on tool name with apply_patch aliased to Edit|Write, and the identical hookSpecificOutput {permissionDecision: 'deny', permissionDecisionReason} shape (or exit 2 + stderr). Adopting it properly means one adapter that maps codex's apply_patch payload onto the same decide() call."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/hooks.md:29-47 (hook discovery: <repo>/.codex/hooks.json, project layer must be trusted); :340-343 (matcher alias note: for apply_patch, matcher values can also use Edit or Write); :354-370 (Tool coverage table: apply_patch is Yes for PreToolUse; 'Treat tool hooks as a useful guardrail, not a complete enforcement boundary'); :579-600 (PreToolUse input — hook input still reports tool_name: 'apply_patch', and Bash and apply_patch use tool_input.command; deny shape identical to Claude Code's). Gap digest: .agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md (hooks async + MCP handlers at 0.148.0)."
rationale = "The native mechanism now exists on the lane that writes most of this repo's code, and the repo has already half-wired it — but the wiring is a no-op for this guard on two counts, both readable in the sources above: codex reports tool_name 'apply_patch' (not in TOOLS, so handles() is False and hook_guard routes it to the Bash rules), and codex sends the patch in tool_input.command with no 'file_path' key, so decide() returns None on the first line. The refactor is scoped: add 'apply_patch' to the dispatch and an adapter that extracts each target path plus its new content from the apply_patch envelope, then call the same decide(); and track .codex/hooks.json so the wiring is reviewed rather than machine-local (it also hard-codes $CLAUDE_PROJECT_DIR, which codex does not set — the ':-.' fallback makes it cwd-dependent, cf. memory feedback_codex_worktree)."
probe = "printf '%s' 'Write a new file scripts/probe-guard.sh containing #!/bin/bash and nothing else. Do not explain.' | codex exec --full-auto --sandbox workspace-write - ; git -C /Users/rmanaloto/dev/github/ray-manaloto/dotfiles status --short scripts/probe-guard.sh   # control arm: the same request to a Claude Code Write must be DENIED by script_guard; if the codex run creates the file, the codex lane bypasses the guard"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-script_guard-4"
unit = "mod-script_guard"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "repo_root(): a four-level Path(__file__).resolve().parent climb, used to make the edited path repo-relative and to bound the git-ignore probe."
evidence = "python/src/dotfiles_setup/script_guard.py:78-86; the same climb appears at python/src/dotfiles_setup/__init__.py:13, instructions_report.py:107, instructions_observer.py:105, lint.py:82 and main.py:2553 — six copies of one constant."
native_alternative = "In-repo, not native: the package already exports the helper at dotfiles_setup/__init__.py:13. (No harness feature supplies a project root to a hook module; Claude Code exposes CLAUDE_PROJECT_DIR to the hook COMMAND, not to library code, and codex does not set it at all.)"
alternative_evidence = "python/src/dotfiles_setup/__init__.py:10-13 (project_root helper, same climb); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:589 ('Reference scripts by path' — CLAUDE_PROJECT_DIR is a hook-command convenience, not a runtime API)."
rationale = "Low-severity duplication rather than a native supersession: six independent copies of the same fragile climb mean a package layout change breaks five call sites silently. Consolidating on the package helper is safe ONLY if the helper adopts .resolve() first — script_guard resolves and __init__.py:13 does not, and the resolve is load-bearing here because _relative() compares resolved paths (script_guard.py:99-104) on a case-insensitive, symlinked macOS /tmp. Keep the module's docstring justification for deriving the root from the module rather than from the edited path (script_guard.py:79-85); that decision is correct and unaffected."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-script_guard-5"
unit = "mod-script_guard"
kind = "module"
disposition = "keep"
confidence = 0.90
custom_thing = "Scope exemptions: outside-the-repo paths, git-ignored paths, plugins/** and bash_budget.ALLOWLIST membership are waved through, with the allowlist deliberately NOT duplicated in this module."
evidence = "python/src/dotfiles_setup/script_guard.py:34-36 (single source of truth is bash_budget.ALLOWLIST), :89-96 (_allowlisted, case-folded), :99-104 (_relative), :164-185 (_exempt, ending in branch_guard.is_ignored); branch_guard.py:143-149 (is_ignored shells out to `git check-ignore -q --`)."
native_alternative = "Already adopted where a native mechanism exists: gitignore evaluation is delegated to git's own `git check-ignore` rather than reimplemented, and repo containment is a Path.relative_to check rather than a hand-rolled prefix compare. Claude Code's nearest native analogues — working-directory scoping and protected paths — do not express either exemption."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permission-modes.md:524-549 (Protected paths is a FIXED list of directories — .git, .vscode, .devcontainer, ... — not configurable, and it prompts rather than denies); permissions.md:328-349 (path rules anchor to the settings source / primary working directory; there is no gitignore-aware rule form)."
rationale = "This is the native-first pass already done: the only genuinely native facility in reach (git's ignore engine) is shelled out to, and the rest of the exemption set has no native expression — Claude Code's protected-path list is fixed and prompt-based, and permission patterns carry no ignore-awareness or single-source-of-truth link to bash_budget.ALLOWLIST. Duplicating the allowlist into settings.json allow rules would also be inert, since deny beats allow (permissions.md:66)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_gate-1"
unit = "mod-session_gate"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "Hand-rolled `--` separator strip and hand-rolled empty-argv rejection for the `command` subcommand's trailing argv, built on `nargs=argparse.REMAINDER`."
evidence = "python/src/dotfiles_setup/session_gate.py:470 (`command.add_argument(\"argv\", nargs=argparse.REMAINDER)`); python/src/dotfiles_setup/session_gate.py:482 (`command_argv = args.argv[1:] if args.argv[:1] == [\"--\"] else args.argv`); python/src/dotfiles_setup/session_gate.py:179-181 (`if command.role not in {\"mutation\", \"gate\"} or not command.argv: ... raise ValueError`)."
native_alternative = "stdlib argparse `nargs=\"+\"` (or `\"*\"`): argparse strips a leading `--` natively and `\"+\"` fails closed with rc=2 on an empty trailing argv, removing both hand-rolled behaviours."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/raw/argparse-dashdash-probe.md:6-8 (REMAINDER arm keeps `['--', 'pytest', '-q']`; nargs='*' arm returns `['pytest', '-q']`) and :12-16 (nargs='+' arm: `[]` and `['--']` both -> SystemExit rc=2; `['--', 'pytest', '-q', 'tests/']` -> stripped). Probe is control-armed on host python 3.14.7 (same interpreter that runs this module)."
rationale = "Two lines of custom argv handling are exactly what a different `nargs` value does for free, and the empty-argv guard currently lives in `run_command` (the library API) rather than at the CLI boundary, so the CLI path can construct a GateCommand only to have it rejected one frame later. One caveat the probe already names: under `nargs=\"+\"` an argv passed WITHOUT a leading `--` and starting with a dash (`['pytest','-x']`) becomes `unrecognized arguments`. Every in-repo caller goes through the mise tasks or the Python API (mise.toml:1506-1524; tests/test_session_gate.py:31-36 constructs `GateCommand` directly), so no caller relies on the dash-less form — but the switch should keep the `--` in any documented invocation."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_gate-2"
unit = "mod-session_gate"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "The `fnox exec -- gh api <url>` child command is assembled inline at two sites, without `--non-interactive` and without the parent-env scrub that strips stale GitHub-token precedence."
evidence = "python/src/dotfiles_setup/session_gate.py:222 (`argv = [\"fnox\", \"exec\", \"--\", \"gh\", \"api\", api_url]`); python/src/dotfiles_setup/session_gate.py:411-416 (`subprocess.run([\"fnox\", \"exec\", \"--\", \"gh\", \"api\", api_url], ...)`). Neither call passes `env=process_env.fnox_parent_env()`."
native_alternative = "fnox's own global `--non-interactive` flag (disables prompts and browser auth flows, uses cached auth only), already wrapped by this repo's `process_env.fnox_command` / `process_env.run_with_fnox`, which additionally applies `fnox_parent_env()`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/fnox.usage.kdl:24 — `flag --non-interactive help=\"Disable prompts and browser-based auth flows; use cached/non-interactive auth only (env: FNOX_NON_INTERACTIVE)\" global=#true env=FNOX_NON_INTERACTIVE`; shipped per /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/fnox/CHANGELOG.md:326 (\"**(exec)** add non-interactive auth mode ... #565\"). In-repo wrapper: python/src/dotfiles_setup/process_env.py:38-40 (`fnox_command` returns `(\"fnox\", \"exec\", \"--non-interactive\", \"--\", *command)`), :43-55 (`fnox_parent_env` removes stale GitHub-token precedence), :57-62 (`run_with_fnox`)."
rationale = "Two concrete consequences, not stylistic. (a) A bare `fnox exec` may open a prompt or browser auth flow; both call sites run inside a bounded gate (`timeout=120`) whose entire purpose is to fail closed, and an interactive fnox is precisely the shape that hangs a background/CI invocation. (b) Without `fnox_parent_env()` an inherited `GH_TOKEN`/`GITHUB_TOKEN` can win precedence over the credential fnox resolves, so the receipt attests a readback made with a different identity than the one the boundary intends. The irony is load-bearing: the `credential-launcher` prevention this very module enforces (session_gate.py:66-78) exists to require `process_env.run_with_fnox` in `pr.py`, and `workflow_hooks.GIT_WRITING_MODULES` (python/src/dotfiles_setup/workflow_hooks.py:420) registers `session_gate` alongside `pr` — yet `session_gate` itself bypasses that wrapper. These calls capture output, so a straight `run_with_fnox` swap is not sufficient; the reuse is `process_env.fnox_command(...)` + `env=process_env.fnox_parent_env()` into the existing `subprocess.run(capture_output=True)`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-session_gate-3"
unit = "mod-session_gate"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "Bounded child execution via `subprocess.run(..., timeout=...)`, which on expiry terminates only the direct child."
evidence = "python/src/dotfiles_setup/session_gate.py:156-170 (`_execute`: `subprocess.run(argv, capture_output=True, check=False, timeout=timeout)`, `except subprocess.TimeoutExpired` -> rc=124); python/src/dotfiles_setup/session_gate.py:261-264 (`_execute_private`, no TimeoutExpired handling at all); python/src/dotfiles_setup/session_gate.py:292-293 and :411-416 (same shape)."
native_alternative = "This repo's own guarded runner pattern in `lint.py`: `subprocess.Popen(..., start_new_session=True)` plus `os.killpg(pgid, SIGTERM)` then `SIGKILL`, so a timeout reaps the whole process group."
alternative_evidence = "python/src/dotfiles_setup/lint.py:19 (docstring: \"children run in a fresh process group (`start_new_session=True`) so the ...\"), :213-223 (`_terminate_group`: `os.getpgid(proc.pid)`, `os.killpg(pgid, signal.SIGTERM)`, escalating to `signal.SIGKILL`), :281 (`subprocess.Popen(command, start_new_session=True, env=env)`)."
rationale = "Every argv this module bounds is `[\"mise\", \"run\", <task>]` (session_gate.py:290, :401-402), and each of those tasks expands to `uv run --project python python -m ...` (mise.toml:1512, 1516, 1524) — a three-deep process tree. `subprocess.run`'s timeout kills only `mise`, so an expiry leaves the `uv` and python grandchildren running while the receipt records rc=124 as if the work had been stopped. `.claude/rules/long-running-command-hangs.md` was written for exactly this failure and `lint.py` is the solved instance; the fix is to reuse that runner rather than write a third variant. `_execute_private` additionally has no `TimeoutExpired` handler, so a slow gate raises out of `finalize` instead of failing closed with a typed error."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_gate-4"
unit = "mod-session_gate"
kind = "module"
disposition = "refactor"
confidence = 0.80
custom_thing = "Timeout bounds for the registered mutation/gate tasks live only in the Python caller (`DEFAULT_TIMEOUT = 600`, and `timeout=120` inside `mutation_sentinel`); the mise tasks themselves declare no bound, so a direct `mise run session-review-focused-gate` is unbounded."
evidence = "python/src/dotfiles_setup/session_gate.py:24 (`DEFAULT_TIMEOUT = 600`), :292-293 (`subprocess.run(gate_argv, ..., timeout=120)`); mise.toml:1506-1524 — `[tasks.session-review-gate]`, `[tasks.session-review-mutation-credential-launcher]`, `[tasks.session-review-mutation-git-hook-contamination]`, `[tasks.session-review-focused-gate]` each declare only `description` and `run`, no `timeout`."
native_alternative = "mise's native per-task `timeout` key (durations like `30s`, `5m`, `10m`; the task fails if it does not complete within the duration), plus the global `mise run --timeout` / `task.timeout` setting."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:1060-1077 — `### timeout`, \"**Type**: `string`\", \"**Default**: unset\", \"Maximum execution time for this task ... The task fails if it does not complete within the configured duration\", example `timeout = \"10m\"`, and \"Use [`mise run --timeout`] or the [`task.timeout`] setting to limit the entire task run ... the shorter of the two wins\". Host mise is already current with the latest release (.agent/kb/raw/mise-releases-since-pin.digest.md — host `2026.9.3` equals newest published tag), so the key is available at the pinned version."
rationale = "Declaring `timeout` on the four session-review tasks moves the bound to where every caller sees it — including a human or hook that runs `mise run session-review-focused-gate` directly, which today has no bound in any layer. It does not retire the Python timeout: `_execute` must still observe expiry to write rc=124 into the receipt, and mise's per-task bound is the shorter-wins outer guard. This is defence-in-depth that the tool provides declaratively, per `.claude/rules/use-tool-builtins.md`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_gate-5"
unit = "mod-session_gate"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "A hand-written GitHub issue-endpoint URL parser and prefix check: the finalize spec carries a free-text `api_url`, which is validated by string prefix and then by `urlparse` + a five-segment path shape check before being re-assembled."
evidence = "python/src/dotfiles_setup/session_gate.py:218-220 (`if not api_url.startswith(\"https://api.github.com/repos/\")`); :326-341 (`_issue_identity`: `urlparse`, `len(parts) != ISSUE_PATH_PARTS`, `parts[0] != \"repos\"`, `parts[3] != \"issues\"`, `parts[4].isdigit()`); :400 (`api_url = f\"https://api.github.com/repos/{owner}/{repo}/issues/{issue_number}\"`); :382-388 (`expected_html` reconstruction and `html_url` comparison)."
native_alternative = "`gh issue view <number> -R <owner>/<repo> --json number,url,body` — gh takes a typed repo + issue number and exposes JSON field selection natively, so a non-issue endpoint is unrepresentable rather than rejected by a parser."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/issue/view/view.go:50 (`Use: \"view {<number> | <url>}\"`), :83 (`cmd.Flags().BoolVarP(&opts.Comments, ...)` context), :86 (`cmdutil.AddJSONFlags(cmd, &opts.Exporter, api.IssueFields)` — the `--json` surface), :90-94 (`defaultFields` includes `\"number\", \"url\", ... \"body\"`), :74-76 (`// support -R, --repo override`)."
rationale = "Changing the finalize spec's `api_url` string into typed `owner`/`repo`/`issue_number` fields (the spec already rejects unknown keys at session_gate.py:354-357) deletes `_issue_identity`, the prefix check, and the `expected_html` reconstruction, and makes the class of input the parser guards against structurally impossible. Confidence is held at 0.6 for one real difference the migration must absorb: `gh issue view --json` returns `url`, not the REST `html_url`, and returns only the requested fields, so the canonical body currently hashed from the full REST object (session_gate.py:392, :446-455) would change shape — that is a receipt-format change, and any persisted digest comparisons in session_ledger would need re-baselining. If preserving the exact REST payload is the requirement, keep `gh api` and take only the typed-fields half of this change."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-session_gate-6"
unit = "mod-session_gate"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "A private atomic-JSON writer: mkstemp in the destination directory, write, flush, fsync the file, `Path.replace`."
evidence = "python/src/dotfiles_setup/session_gate.py:95-107 (`_atomic_json`)."
native_alternative = "`session_store.SessionStore._atomic_write` — the same pattern already in this package, and strictly stronger: it additionally `os.fsync`s the parent directory after the rename, so the rename itself survives a crash."
alternative_evidence = "python/src/dotfiles_setup/session_store.py:160-178 (`_atomic_write`: mkstemp with `dir=path.parent`, `stream.flush()`, `os.fsync(stream.fileno())`, `temporary.replace(path)`, then `directory = os.open(path.parent, os.O_RDONLY)` / `os.fsync(directory)`). Two further near-copies exist at python/src/dotfiles_setup/docker.py:150 and python/src/dotfiles_setup/sync.py:371."
rationale = "Four independent implementations of the same write-temp-then-rename primitive live in this package and they have already drifted on durability: only the session_store copy fsyncs the parent directory, so a receipt this module writes can be lost by a crash between the rename and the directory flush — which is exactly the guarantee an attestation receipt is claiming. There is no stdlib atomic-write, so the native alternative is the repo's own helper; lifting it into a shared module and having all four call it is the change. Serialization is deliberately NOT part of this: the receipts are canonicalized with `json.dumps(..., sort_keys=True)` because the digests depend on byte-stable key order, and `codec.encode` (python/src/dotfiles_setup/codec.py:339) wraps msgspec, which does not sort keys — so stdlib `json` here is justified and should stay."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_gate-7"
unit = "mod-session_gate"
kind = "module"
disposition = "keep"
confidence = 0.65
custom_thing = "`prevention_check`: assert that each registered carrier file contains its `required_token` exactly `required_count` times, resolved under a relocatable candidate root (`SESSION_REVIEW_CANDIDATE_ROOT`)."
evidence = "python/src/dotfiles_setup/session_gate.py:313-323 (`prevention_check`: `text.count(registration.required_token) != registration.required_count` -> rc=1); :501-504 (candidate root from `SESSION_REVIEW_CANDIDATE_ROOT`, defaulting to `--repo-root`); :289 (the mutation sets that env var for the candidate copy)."
native_alternative = "This repo's verification engine — `suites.toml` `require_tokens` handler with `per_path_tokens` — is the nearest existing mechanism for 'this file must carry this token'."
alternative_evidence = "python/src/dotfiles_setup/verify.py:184-214 (`require_tokens`), specifically :209-210 (`combined = \"\\n\".join(p.read_text() for p in paths if p.exists())` / `missing = [t for t in tokens if t not in combined]`) — membership only, no occurrence count; :563-583 (`_handle_require_tokens`, reading `per_path_tokens` from the suite entry) and :660-662 (`HANDLERS` map). Paths are resolved relative to the repo root, with no candidate-root indirection anywhere in the handler."
rationale = "Justification, not a default: the engine measurably cannot express this contract. (a) It tests membership (`t not in combined`), not an exact occurrence count — and the count is the point here, since the mutation replaces one of N occurrences and a membership check would still pass on the survivors. (b) It resolves paths under the repo root only, while the whole mutation design depends on pointing the SAME gate at an isolated candidate tree (session_gate.py:278-293), which is what makes the arm real rather than a self-referential check. Retiring `prevention_check` in favour of `require_tokens` today would silently weaken both properties — precisely the `feedback_forbid_tokens_substring_fragile` failure. The constructive option is the inverse: teach `verify.require_tokens` an occurrence-count form and an optional root override, then have `prevention_check` call it; that is a change to verify.py, not to this module, so it is recorded here rather than dispositioned as a refactor of this unit."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_gate-8"
unit = "mod-session_gate"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The unit's core design: a registry of prevention classes (`PREVENTIONS`) whose mutation sentinel copies registered targets into an isolated candidate root, replaces one token, re-runs the normal mise gate against the candidate, and requires a non-zero exit; plus the nonce-bound receipt/finalize protocol that re-evaluates mutation, gate and a live GitHub readback in one process and returns a deliberately non-persisted verdict."
evidence = "python/src/dotfiles_setup/session_gate.py:66-88 (`PREVENTIONS` registry), :267-310 (`mutation_sentinel` — isolated candidate, byte replacement, before/after SHA-256, `gate_rc != 0` required, typed ARMED sentinel on fd 1), :395-456 (`finalize` — ephemeral nonce, distinct mutation/gate tasks, sentinel identity match, digest-change and gate-rejection assertions, live readback, `FinalizeResult(\"TEMPORARILY_COMPLETE\", ...)`), :40-50 (`FinalizeResult` docstring: \"deliberately never persisted\")."
native_alternative = "None found. The two closest families were checked and both are out of scope: pytest mutation-testing plugins (they mutate Python AST and score the pytest suite), and `gh attestation` (Sigstore/OIDC-signed provenance for build artifacts and OCI images)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/pytest/doc/en/reference/plugin_list.rst:850 (`pytest-gremlins` — \"Fast-first mutation testing for pytest\"), :1062 (`pytest-leela` — \"Type-aware mutation testing for Python\"), :1227 (`pytest-mutagen` — \"Add the mutation testing feature to pytest\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/attestation/verify/verify.go:50-52 (`Use: \"verify [<file-path> | oci://<image-uri>] [--owner | --repo]\"`, \"Verify an artifact's integrity using attestations\") and :193/:220-224 (`verification.SigstoreConfig`, `NewLiveSigstoreVerifier`). Control arm for the pytest grep: the same corpus and command shape returns hits for `fixture` across the tree, so the mutation hits are a real signal and their scope (pytest-only) is read from the plugin descriptions themselves, not from an empty result."
rationale = "The mutation-testing plugins arm a TEST SUITE against AST mutants; this module arms a mise GATE TASK against a source-token mutant in a relocated candidate tree, and asserts the gate's non-zero exit — a different subject (the gate, not the tests) and a different mutation domain (bytes of a carrier contract, not Python AST). `gh attestation` verifies Sigstore-signed provenance for artifacts and images produced under an OIDC issuer; it has nothing to say about 'this local gate rejected this local mutant at this moment', and the finalize verdict is deliberately ephemeral precisely so it cannot become a replayable artifact — the opposite of what an attestation store is for. Nothing in the Claude Code 2.1.258-2.1.266 changelog digest, the hk 1.58.1 digest (documentation-only), the mise digest (host already at newest release) or the uv 0.12.10-0.12.11 digest touches this surface. Findings 1-6 are the parts that ARE superseded; the design itself is not."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_ledger-1"
unit = "mod-session_ledger"
kind = "module"
disposition = "refactor"
confidence = 0.75
custom_thing = "Hand-rolled discovery AND raw-JSONL parsing of Claude Code transcripts: the Claude leg of discover_sources() resolves ~/.claude/projects via command_audit.transcripts_base()/encode_cwd()/project_transcripts(), and _parse_claude() plus _claude_content()/_claude_message_event()/_claude_attachment_event()/_claude_non_message_event() decode the per-line record shapes (uuid, sessionId, message.content blocks, tool_use/tool_result pairing) by hand."
evidence = "python/src/dotfiles_setup/session_ledger.py:1435-1453 (discover_sources -> command_audit.transcripts_base / project_transcripts); python/src/dotfiles_setup/session_ledger.py:2877 (_parse_claude); python/src/dotfiles_setup/session_ledger.py:2618 (_claude_content); python/src/dotfiles_setup/session_ledger.py:2818 (_claude_message_event); python/src/dotfiles_setup/command_audit.py:213-260 (transcripts_base/encode_cwd/project_dir/project_transcripts)"
native_alternative = "Claude Agent SDK for Python: list_sessions(directory=..., limit=..., offset=..., include_worktrees=True) -> list[SDKSessionInfo] (session_id, summary, last_modified, cwd, git_branch, created_at) and get_session_messages(session_id, directory=..., limit=..., offset=...) -> list[SessionMessage] (type, uuid, session_id, message, parent_tool_use_id, parent_agent_id). The docs name exactly this use case: 'Use them to build custom session pickers, cleanup logic, or transcript viewers.' Anthropic additionally states the on-disk entry format is unsupported for direct parsing."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__python.md:231-322 (list_sessions/get_session_messages signatures, SDKSessionInfo and SessionMessage field tables); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__sessions.md:411 ('Both SDKs expose functions for enumerating sessions on disk and reading their messages ... Use them to build custom session pickers, cleanup logic, or transcript viewers.'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sessions.md:224 ('The entry format is internal to Claude Code and changes between versions, so scripts that parse these files directly can break on any release. To build on session data, use /export or the script interfaces instead.')"
rationale = "Native covers the two lowest, most version-fragile layers — locating a project's sessions (including the <encoded-cwd> mangling and the 200-char truncate+hash rule the repo reimplements in command_audit.encode_cwd) and reading a session's messages — and the vendor explicitly warns that the direct parse this module does 'can break on any release'. It does NOT cover the ledger's value: parent_tool_use_id/parent_agent_id give lineage but SessionMessage.message is raw Any, so attachment hashing, form-question/answer pairing, compaction envelopes, byte-exact SourceCutoff prefixes and the append-only cache still need this module's decoders. Refactor = adopt list_sessions()/get_session_messages() as the discovery+read boundary and keep the claim layer on top. Cost to weigh: claude-agent-sdk is not currently a dependency (probed: `python3 -c 'import claude_agent_sdk'` -> ModuleNotFoundError; no claude_agent_sdk entry in python/pyproject.toml), and it couples the gate to a bundled CLI version."
probe = "uv run --project python --with claude-agent-sdk python -c \"from claude_agent_sdk import list_sessions, get_session_messages; s=list_sessions(directory='/Users/rmanaloto/dev/github/ray-manaloto/dotfiles', limit=3); print(len(s)); print([x.session_id[:8] for x in s]); m=get_session_messages(s[0].session_id) if s else []; print(len(m))\"  # control arm: repeat with directory='/tmp/does-not-exist' and require 0 rows, proving the probe discriminates"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-session_ledger-2"
unit = "mod-session_ledger"
kind = "module"
disposition = "refactor"
confidence = 0.70
custom_thing = "Hand-rolled Codex thread discovery: glob every ~/.codex/sessions/**/*.jsonl, open each file to read its first session_meta record, re-open and scan EVERY line of every file to derive a latest-activity timestamp, filter roots by cwd string equality, sort, and separately glob a sibling archived_sessions/ directory to count archived threads."
evidence = "python/src/dotfiles_setup/session_ledger.py:1392-1432 (discover_codex_transcripts: base.glob('**/*.jsonl'), cwd string compare, manual sort+limit); python/src/dotfiles_setup/session_ledger.py:1322-1330 (first_session_meta); python/src/dotfiles_setup/session_ledger.py:1364-1389 (_codex_activity_timestamp reads every record of every file); python/src/dotfiles_setup/session_ledger.py:1333-1334 (_is_codex_root); python/src/dotfiles_setup/session_ledger.py:1473-1476 (provider_census hand-globs codex_root.parent/'archived_sessions')"
native_alternative = "codex app-server JSON-RPC: `thread/list` pages stored thread logs with server-side filters that match every hand-rolled predicate here — `cwd` (exact working-directory match, accepts an array), `archived` (true lists archived threads only), `sourceKinds` (cli/vscode/exec/appServer/subAgent*), `sortKey` = created_at | updated_at | recency_at with `sortDirection`, cursor pagination, and experimental `parentThreadId`/`ancestorThreadId` for root-vs-child. `thread/read` (with includeTurns) and `thread/turns/list` read a stored thread without resuming it. Codex also keeps this metadata in a state DB (`useStateDbOnly`, CODEX_SQLITE_HOME), so listing does not require scanning JSONL at all."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/app-server.md:282 (thread/list filters incl. cwd, archived, sourceKinds, parentThreadId/ancestorThreadId); :630-668 (List threads: sortKey created_at|updated_at|recency_at, sourceKinds value list, cursor pagination, example response); :281 and :590-600 (thread/read without resuming, includeTurns); :526 ('Resuming a thread doesn't update thread.updatedAt ... The timestamp updates when you start a turn.' — the exact activity semantics _codex_activity_timestamp derives by hand); :747 (thread/archive moves the log into the archived sessions directory; archived threads are excluded from thread/list unless archived:true); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/config-file__environment-variables.md:17-18 (CODEX_HOME, CODEX_SQLITE_HOME state DB)"
rationale = "Every selection predicate this module implements by brute-force file scanning is a first-party server-side filter at codex-cli 0.152.1's app-server, and the 'latest turn activity, not creation time' semantics it re-derives by reading every record is exactly what sortKey=updated_at/recency_at means. The O(all sessions x all lines) rescan is the concrete cost. Native does not replace the parse of the selected thread into requirement/promise claims, and adding an app-server dependency puts a daemon in a gate path — so this is refactor (adopt thread/list for discovery + census, keep the JSONL parse for evidence bytes and byte-exact cutoffs, which a JSON-RPC summary cannot provide), not retire."
probe = "codex app-server </dev/null & then send: {\"method\":\"thread/list\",\"id\":1,\"params\":{\"cwd\":\"/Users/rmanaloto/dev/github/ray-manaloto/dotfiles\",\"limit\":5,\"sortKey\":\"updated_at\"}} and compare the returned ids against `uv run --project python python -c \"from pathlib import Path; from dotfiles_setup.session_ledger import discover_codex_transcripts as d; print([p.name for p in d(Path('/Users/rmanaloto/dev/github/ray-manaloto/dotfiles'))])\"`; control arm: repeat thread/list with cwd='/tmp/not-a-repo' and require an empty data array"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_ledger-3"
unit = "mod-session_ledger"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "SelectionCertification.UNCERTIFIED_ACTIVITY_FALLBACK — the ledger cannot prove which Claude session it is reviewing, so it reviews the N most recently-modified project transcripts and records the omission 'active session identity is unverified; latest activity is only a fallback'. CoverageSelection.session_id is routed ONLY to the Codex leg; the Claude leg gets no identity at all."
evidence = "python/src/dotfiles_setup/session_ledger.py:194-200 (SelectionCertification enum); :590-592 (_UNCERTIFIED_ACTIVITY text); :3760 ('codex_session_id = selection.codex_session_id or selection.session_id' — the only consumer of session_id); :3785-3789 (discover_sources receives codex_session_id only; the Claude leg is limit-only); :3838-3846 (require_active_identity adds the uncertified omission); python/src/dotfiles_setup/session_review.py:811-820 (session_review passes require_active_identity=True and only codex_session_id). Repo-wide grep for CLAUDE_CODE_SESSION_ID across python/src/dotfiles_setup/, mise.toml and .claude/settings.json returns 0 hits (control arm: the same grep for CODEX_HOME returns session_ledger.py:1308,1311)."
native_alternative = "Claude Code sets `CLAUDE_CODE_SESSION_ID` automatically in Bash tool subprocesses, hook command subprocesses and stdio MCP subprocesses, to the current session ID, matching the hook JSON `session_id`, updated on /clear, and receiving the resumed ID under `--resume <id>`. `mise run session-review` runs in exactly such a Bash subprocess, so the native identity is already in the process environment."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/env-vars.md:349 (CLAUDE_CODE_SESSION_ID: 'Set automatically to the current session ID in Bash and PowerShell tool subprocesses, hook command subprocesses, and stdio MCP server subprocesses ... Use to correlate scripts and external tools with the Claude Code session that launched them'); live presence probe run in this audit's Bash subprocess printed CLAUDE_CODE_SESSION_ID=SET (value never printed; control arm HOME=SET, and a known-absent name would print ABSENT); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:730 (hook input carries session_id alongside transcript_path)"
rationale = "The module's own worst-case certification state exists because the caller supposedly cannot prove which native task is under review — but the harness has published that identity into every Bash subprocess for many releases, it is measurably SET here, and no code in this repo reads it. Reading os.environ['CLAUDE_CODE_SESSION_ID'] into CoverageSelection and matching it against the Claude transcript's sessionId turns UNCERTIFIED_ACTIVITY_FALLBACK into EXPLICIT_SESSION_ID for the Claude provider, and turns a silent 'most recently modified wins' selection into a checked one. Refactor rather than retire: the env var is absent when session-review runs from a plain shell or CI, so the fallback path must stay, and the Codex side needs its own identity (no equivalent CODEX thread-id env var is documented in config-file__environment-variables.md — only CODEX_HOME/CODEX_SQLITE_HOME)."
probe = "uv run --project python python -c \"import os; print('present' if os.environ.get('CLAUDE_CODE_SESSION_ID') else 'absent')\" run once from the Claude Code Bash tool (expect present) and once from a detached shell with `env -u CLAUDE_CODE_SESSION_ID` (expect absent) — both arms required before wiring it as a certification source"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_ledger-4"
unit = "mod-session_ledger"
kind = "module"
disposition = "refactor"
confidence = 0.65
custom_thing = "codex_root_kind(): classifies a Codex transcript into RootKind (SUBAGENT/GUARDIAN/EXEC_WORKER/PLUGIN_TASK/IMPORTED/INTERACTIVE/UNKNOWN) by json-dumping three metadata fields into one lowercased string and substring-sniffing it for 'subagent', 'guardian', 'review', 'exec', 'plugin', 'import', 'user', 'vscode', 'codex-tui'."
evidence = "python/src/dotfiles_setup/session_ledger.py:1337-1361 (codex_root_kind: `encoded = json.dumps(identity, sort_keys=True).lower()` then `'subagent' in encoded`, `'guardian' in encoded or 'review' in encoded`, ...); python/src/dotfiles_setup/session_ledger.py:141-150 (RootKind enum); python/src/dotfiles_setup/classifier_tables.py:305-311 (this classifier is a registered, table-tested classifier)"
native_alternative = "Codex publishes the authoritative typed vocabulary as `sourceKinds` on thread/list: `cli`, `vscode`, `exec`, `appServer`, `subAgent`, `subAgentReview`, `subAgentCompact`, `subAgentThreadSpawn`, `subAgentOther`, `unknown` — with the server defaulting to interactive sources (`cli` and `vscode`) when the filter is omitted, i.e. the 'is this an interactive root' question answered server-side."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/app-server.md:649-660 (sourceKinds value list); :641 ('sourceKinds - restrict results to specific thread sources. When omitted or [], the server defaults to interactive sources only: cli and vscode')"
rationale = "A substring sniff over a JSON blob is a symptom check against strings this repo does not own (probes-need-a-control-arm rule 9): the day Codex renames 'subagent' to 'sub_agent' in metadata, this classifier silently reclassifies every child thread as INTERACTIVE and grants it root authority, with no alarm. The native enum is the same taxonomy, finer-grained (it distinguishes subAgentReview / subAgentCompact / subAgentThreadSpawn, which this collapses into GUARDIAN and SUBAGENT), and is served rather than inferred. Refactor, not retire: RootKind is also applied to Claude-side sources and to imported history, which sourceKinds does not cover, and the mapping must be explicit and tested rather than a rename."
probe = "Compare, for the same ~/.codex/sessions corpus, codex_root_kind()'s verdict per file against thread/list's sourceKinds for the same thread ids; any disagreement is the finding. Control arm: assert the comparison discriminates by mutating one metadata blob's thread_source to a value neither side has seen and confirming the two verdicts diverge."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_ledger-5"
unit = "mod-session_ledger"
kind = "module"
disposition = "refactor"
confidence = 0.55
custom_thing = "_attested_issue_receipt() requires the recorded receipt argv to start EXACTLY with ['fnox','exec','--','gh'] before a GitHub issue readback is accepted as trusted evidence."
evidence = "python/src/dotfiles_setup/session_ledger.py:3950 (`and argv[:4] == [\"fnox\", \"exec\", \"--\", \"gh\"]`); surrounding checks at :3934-3957 (api_url must start https://api.github.com/repos/, html_url must start https://github.com/, body_sha256 must be 64 hex)"
native_alternative = "gh authenticates natively from the process environment: `GH_TOKEN`, `GITHUB_TOKEN` (in that precedence order) are read as the authentication token, with stored credentials used otherwise — so no launcher wrapper is required for the invocation to be authenticated. The repo's own credential posture makes those variables ambient in every shell (all fnox credentials are `env = true` since 2026-08-02, with one carve-out), which means `fnox exec --` is no longer the confinement boundary this assertion treats it as."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/root/help_topic.go:45 ('GH_TOKEN, GITHUB_TOKEN (in order of precedence): an authentication token that will be used when ...') and :54 (stored credentials used otherwise); .claude/rules/secrets-out-of-the-shell-env.md (rule 2: 'REVERSED — secrets now live in the shell by decision ... fnox exec is no longer a confinement boundary, because the parent shell already has everything')"
rationale = "The check binds the SHAPE of a launcher rather than the property it wants (a live, authenticated GitHub readback of the issue body). Under the current, deliberately-reversed credential posture that shape proves nothing about confinement, while it does hard-fail an otherwise-valid receipt produced by a bare `gh api` — the failure mode is a gate that rejects correct evidence for a stale reason. Refactor: keep the api_url/html_url/body_sha256/attestation checks (those bind the property) and drop or generalise the argv-prefix equality to 'argv invokes gh with api and the recorded api_url'. Not retire, because the receipt attestation as a whole has no native equivalent (finding -6)."
probe = "uv run --project python pytest tests/test_session_ledger.py -k issue_receipt -x -q, then re-run with a fixture receipt whose argv is ['gh','api',<api_url>] (no fnox prefix) and confirm it is rejected — proving the prefix is load-bearing today and quantifying what the refactor changes"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-session_ledger-6"
unit = "mod-session_ledger"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "The claim layer proper: requirement atomization from markdown bullets (atomize_requirement_text), promise extraction from assistant text (_PROMISE), typed semantic dispositions with receipt-kind gates (SemanticDisposition.persistable/.complete requiring issue:/artifact:/test:/commit:/user: refs), the omission census (_classify_omission, CoverageStatus.INCOMPLETE fail-loud), append-safe SourceCutoff byte prefixes with sha256 matching, bounded content-addressed artifact segmentation under MAX_RENDER_BYTES, and the resumable ReviewIteration loop."
evidence = "python/src/dotfiles_setup/session_ledger.py:1580-1592 (atomize_requirement_text); :40-42 (_PROMISE); :288-333 (SemanticDisposition.persistable/.complete); :749-780 (_classify_omission); :805-812 (CoverageStatus fail-loud); :532-556 (SourceCutoff.matches byte-prefix check); :717-746 (_bounded_json_chunks); :4087-4185 (advance_iteration)"
native_alternative = "None found. The nearest first-party surfaces return MESSAGES, not claims: Agent SDK list_sessions/get_session_messages (agent-sdk__python.md:231-322), codex app-server thread/read+thread/turns/list (app-server.md:281-283), Claude Code /export and --output-format json (sessions.md:207-212). The harness-side output-size features shipped since the pin (bashOutputMaxChars/taskOutputMaxChars up to 128K, the 1 GB disk cap on saved tool results) bound what an agent SEES; they do not produce deterministic, content-addressed artifact segments. No linter in the pin set (agnix, claude-code-lint, cclint, agents-lint, ast-grep, zizmor, ghalint) reads transcripts, and /skill-doctor reports unused skills, not unmet user requirements."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sessions.md:207-212 (the complete list of script interfaces for session data: /export, -p --output-format json/stream-json, transcript_path from hooks, the Agent SDK — all message-level); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (2.1.265 1 GB tool-result disk cap; 2.1.261 bashOutputMaxChars/taskOutputMaxChars; 2.1.261 /skill-doctor — none is a requirement/promise ledger); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/app-server.md:281-283 (thread/read, thread/turns/list return turns and items, not claims)"
rationale = "Justification, not a default: no harness, CLI, linter or marketplace plugin at the host's pinned versions models 'what the user asked for' and 'what the agent promised' as typed, individually-dispositionable claims with a receipt-kind gate and a fail-loud omission census. Every native surface stops at delivering messages — which is precisely why findings -1 and -2 propose adopting them as the READ boundary underneath this layer rather than replacing it. The byte-exact SourceCutoff and append-only prefix semantics also have no native counterpart: SDKSessionInfo exposes file_size and last_modified, but nothing that certifies which bytes were reviewed, and the vendor explicitly reserves the right to change the on-disk format (sessions.md:224), which makes this layer's independence from that format the thing worth keeping."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_review-1"
unit = "mod-session_review"
kind = "module"
disposition = "refactor"
confidence = 0.65
custom_thing = "Hand-rolled STRUCTURAL validation of docs/agents/goal-history.md: goal_history_errors() splits the doc on a dated `## ` heading regex and _iteration_errors()/_goal_text() assert per-entry that each of the 9 GOAL_HISTORY_FIELDS bullets appears, that exactly one ```mermaid fence is present, and that `### Current goal` precedes `### Current workflow`."
evidence = "python/src/dotfiles_setup/session_review.py:140 (_GOAL_HISTORY_ENTRY heading regex), :128-139 (GOAL_HISTORY_FIELDS), :382-396 (_goal_text ordered-section check), :398-421 (per-field presence/duplicate counting), :424-425 (`block.count(\"```mermaid\") != 1`), :445-462 (goal_history_errors); wired as a preflight at :681-689 and contract-bound at python/verification/suites.toml:2335."
native_alternative = "mdschema (aqua:jackchuka/mdschema, already pinned at 0.15.2 in mise.toml:41 but wired nowhere) validates Markdown structure declaratively from a .mdschema.yml: `structure[].heading.pattern` + `regex: true` matches a repeating dated entry heading, `count: {min,max}` bounds occurrences, `children` enforces the ordered `### Current goal` / `### Current workflow` subsections, `code_blocks: [{lang: mermaid, min: 1, max: 1}]` replaces the mermaid fence count, `required_text` (regex form) replaces the per-field bullet presence check, and `heading_rules.unique` covers duplicate headings. `mdschema check --schema <file>` is the runner."
alternative_evidence = ".agent/kb/raw/mdschema-0.15.2-capability-probe.md:307 (heading `pattern`+`regex`), :326 (`count` CountConstraint min/max), :350 (`required_text` with regex), :402 (`code_blocks` CodeBlockRule with lang/min/max), :149 (`unique_per_level`), :17-30 (`mdschema check`/`derive`/`init` subcommands). The repo already records this as deferred-for-want-of-a-schema, not as impossible: hk.pkl:361-363 (\"rumdl/mdschema builtin adoption DEFERRED ... mdschema requires authoring a schema file that does not exist yet\") and docs/hk-builtins-audit.md:118."
rationale = "mdschema covers the structural half declaratively, which is the half most likely to rot as the entry contract changes. It does NOT cover three checks in the same functions, so this is a refactor and not a retire: (a) `required_text` has no min/max, so 'exactly once' duplicate-field detection (session_review.py:405-409) is not expressible; (b) the 'current goal must be a Markdown quote' rule (:392-395) has no blockquote rule in the schema surface; (c) the SHA-256 digest chain (:420-437) and duplicate-Iteration-ID check (:410-413) are semantic and stay in python. Correct shape: author `.mdschema/goal-history.yml`, run `mdschema check` as an hk step, and shrink session_review.py to the digest-chain and append-only checks only."
probe = "mise exec -- mdschema derive docs/agents/goal-history.md > /tmp/gh.yml && edit the derived heading list into one regex entry with count.min=1, then: mise exec -- mdschema check --schema /tmp/gh.yml docs/agents/goal-history.md; control arm: delete one `- **Evidence:**` bullet from a copy and confirm a non-zero exit."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_review-2"
unit = "mod-session_review"
kind = "module"
disposition = "keep"
confidence = 0.82
custom_thing = "Git-backed APPEND-ONLY enforcement of the goal history: resolve the fixed origin/main merge-base, walk every first-parent revision from that baseline to HEAD, `git show <rev>:docs/agents/goal-history.md` at each, require every blob to be a byte-prefix extension of its predecessor, require the bootstrap revision to hold exactly one iteration, then check the working tree against committed HEAD — failing closed when the baseline cannot be resolved."
evidence = "python/src/dotfiles_setup/session_review.py:466-473 (_git_bytes), :480-483 (_authorized_goal_history_base merge-base HEAD origin/main), :502-523 (_committed_append_only_error, `rev-list --first-parent --reverse`, `not blob.startswith(previous)`), :525-546 (_append_only_error, fail-closed on unresolved baseline); requires CI full history at .github/workflows/ci.yml:185; contract-bound at python/verification/suites.toml:2335."
native_alternative = "None found. hk ships no builtin of this class — its builtins are linter/formatter wrappers, one pkl file per tool; a control-armed search of the hk source tree for append-only/prefix semantics returns nothing while the same command shape finds markdown_lint.pkl. GitHub rulesets and branch protection gate WHO may push and WHETHER a PR is required, not that a tracked file's new bytes are a prefix-extension of the old ones. mdschema (finding 1) validates a single document snapshot and has no git awareness (.agent/kb/raw/mdschema-0.15.2-capability-probe.md:17-30 lists check/derive/generate/init/schema only)."
alternative_evidence = "Control-armed grep, 2026-09-09: `grep -rlni 'append.only' /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl /Users/.../hk/docs` -> 0 files, while the identical command shape for 'markdownlint' -> /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/markdown_lint.pkl, so the probe discriminates. hk builtin inventory as adopted here: docs/hk-builtins-audit.md."
rationale = "Required justification: the check is a cross-revision byte-prefix invariant over one tracked file, evaluated against an authority (the origin/main merge-base) that the branch under review must not be able to choose for itself. No linter, no hk builtin, and no GitHub platform feature at the host's pins expresses 'this file may only grow at its tail, measured from a baseline the branch cannot nominate'. The one piece that IS delegable — the per-snapshot document shape — is finding 1; what remains here is git plumbing plus the fail-closed baseline rule, and it stays custom."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_review-3"
unit = "mod-session_review"
kind = "module"
disposition = "keep"
confidence = 0.72
custom_thing = "Lane 1: mine this project's Claude transcripts for recurring non-canonical Bash command SHAPES and rank them by session spread, to propose new skills — shape_candidates() over command_audit.iter_bash_commands(), with is_reportable_shape() filtering shell constructs, harness mechanics and pasted literals."
evidence = "python/src/dotfiles_setup/session_review.py:265-283 (is_reportable_shape), :286-318 (shape_candidates, ranks by (sessions, occurrences)), :74 (CANDIDATE_KINDS = one_off|bypass), :711-716 (transcript lane in session_review_main); contract python/verification/suites.toml:2341-2355."
native_alternative = "Closest native features, both non-substitutes: (a) the bundled `/fewer-permission-prompts` skill mines the SAME corpus — 'Scan your transcripts for common read-only Bash and MCP tool calls' — but its verdict is a .claude/settings.json permission allowlist, and it exposes no mined-shape output another consumer could rank; (b) `/skill-doctor` (Claude Code 2.1.261) runs the inverse direction, reporting which ALREADY-LOADED skills go unused and what they cost in context."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/commands.md:89 (`/fewer-permission-prompts` bundled skill, transcript scan -> permission allowlist) and .../whats-new__2026-w16.md:123 (same). `/skill-doctor`: .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md, 2.1.261 bullet 'Added `/skill-doctor`'. Codex-side: nothing in .agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md mines transcripts for workflow candidates (its transcript feature is `/export` to Markdown, 0.148.0)."
rationale = "Required justification: the native transcript miner answers a different question (should this call be pre-approved?) and emits a settings allowlist, not a ranked candidate list; nothing native ranks by SESSION SPREAD, which is this lane's whole thesis (twenty uses in one session is a grind, three across three sessions is a workflow). `/skill-doctor` prunes existing skills and cannot propose one that does not exist. Retiring lane 1 for either would lose the finding class it exists for. Worth revisiting only if `/fewer-permission-prompts` ever gains a machine-readable dump of its mined shapes."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_review-4"
unit = "mod-session_review"
kind = "module"
disposition = "keep"
confidence = 0.78
custom_thing = "Lane 2: regex scan of .agent/notepad.md, the newest session handoff and the tracked goal history for phrases that record manual slog ('by hand', '~N turns', 're-derived', 'cost a cycle'), with a suppressor list for phrasings that FORBID manual work, a bounded tail that preserves real line numbers, and a newest-first glob bound."
evidence = "python/src/dotfiles_setup/session_review.py:144-151 (NARRATIVE_MARKERS), :159-166 (NARRATIVE_SUPPRESSORS), :320-344 (narrative_paths, glob_limit), :346-361 (narrative_hits, one hit per line), :364-375 (tail_text returning the offset), :548-571 (scan_narrative)."
native_alternative = "None found at the host's pins. The repo's markdown linters check structure and style, not phrase semantics: rumdl 0.2.63 and markdownlint-cli2 0.23.2 are rule-based style linters; agnix 0.52.1 validates agent-doc conventions against rule IDs (AGM-*/XP-*, see .agnix.toml:60-75); mdschema validates declared document structure (.agent/kb/raw/mdschema-0.15.2-capability-probe.md:17-30). ast-grep 0.45.3 matches syntax trees of programming languages and has no markdown-prose grammar for this. No Claude Code feature in the 2.1.258-2.1.266 gap digest reads the notepad or handoffs."
alternative_evidence = "agnix scope: .agnix.toml:62 `tools = [\"claude-code\"]` with rule-ID overrides at :67-75. mdschema surface: .agent/kb/raw/mdschema-0.15.2-capability-probe.md:1-30. Claude Code gap: .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 capabilities, none touching agent-authored notepad/handoff prose)."
rationale = "Required justification: this is a project-specific semantic surfacer over a project-specific corpus (.agent/**, which is gitignored — the module docstring at :44-49 records that this is exactly why the issue's `--since <ref>` bound is unimplementable). A generic linter cannot carry this repo's own slog vocabulary or its suppressor set, both of which the module records as measured from a real first run rather than guessed (:159-163). Nothing native reads these files at all."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_review-5"
unit = "mod-session_review"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "Hand-rolled mutual-exclusion and range validation of the lane flags inside the library: _lane_configuration_error() sums transcript_only/narrative_only/requirements_only and refuses >1, plus `sessions >= 1` and `1 <= max_iterations <= 5`."
evidence = "python/src/dotfiles_setup/session_review.py:657-671 (_lane_configuration_error), called from :673-676 (_review_preflight_error) and :705-707 (session_review_main returns 2)."
native_alternative = "argparse's native `add_mutually_exclusive_group()` — already adopted for exactly these three flags one layer up, so the CLI path can never reach the module's `selected_only > 1` branch. Range validation has native argparse forms too (`type=` callable / `choices=range(1, 6)`)."
alternative_evidence = "python/src/dotfiles_setup/main.py:595 `review_lane = review_parser.add_mutually_exclusive_group()` with the three flags added at :596-611; the same native mechanism is used twice more in the file at :384 and :735."
rationale = "Required justification: the native group binds the CLI parser only, and this module is a public library entry point — tests call session_review_main() directly with a hand-built LaneChoice (tests/test_session_review.py:563 'asking_for_both_exclusive_lanes_is_refused_not_resolved', :576, :591, :602), and the module docstring at :701-704 makes refusing-rather-than-resolving a stated contract. Deleting the check would make an impossible-via-CLI combination silently resolve for library callers. Noted as a duplication, not a defect: the correct future move is to keep the library check and NOT to add a second parser-level guess."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_state-1"
unit = "mod-session_state"
kind = "module"
disposition = "keep"
confidence = 0.75
custom_thing = "The unit as a whole: one read-only snapshot (branch + tree cleanliness + recent commits + open PR) rendered as a paste-ready Markdown block for post-/clear handoff reconciliation, with a deliberate three-valued PR outcome (NONE / OPEN / UNVERIFIABLE) so a failed gh lookup can never be read as 'no open PR'."
evidence = "python/src/dotfiles_setup/session_state.py:2-13 (module docstring stating the UNVERIFIABLE contract), :37-42 (PrState), :232-247 (gather), :250-287 (render); consumed by mise.toml:913-916 [tasks.session-state] and .claude/skills/session-resume/SKILL.md:47."
native_alternative = "No single native command produces this composite. The closest natives each answer one question and all of them collapse 'could not ask' into 'nothing found' at the shell level: `gh pr status` / `gh pr list --json` (pull-request lookup), `git status --porcelain`, `git log`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/pr/status/status.go:53-77 (`gh pr status`, `cmdutil.AddJSONFlags(cmd, &opts.Exporter, api.PullRequestFields)`) — a PR-only view; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/pr/checks/checks.go:123 (checks-only JSON). Nothing in the Claude Code corpus offers a session-state/reconciliation surface: control-armed grep over /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/*.md — 'SessionStart' returns 5 files, 'Recent commits' / 'git status at the start' return 0."
rationale = "The load-bearing value is not the data collection (git and gh already do that) but the failure-mode discipline: PrState.UNVERIFIABLE for command failure, timeout, malformed JSON and detached HEAD (session_state.py:157-163, :194-215), which is exactly the false-negative class .claude/rules/probes-need-a-control-arm.md exists to prevent. A shell pipeline of the native commands returns an empty list on both 'no PR' and 'gh is broken', which is the bug this module was written to remove. Keep the module; the sub-findings below are internal adoptions of native mechanics, not replacements of the unit."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_state-2"
unit = "mod-session_state"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "Hand-rolled parsing of `git status --porcelain --no-renames` output: split on lines and slice the path as `line[3:]`, with quoted/special-character filenames documented in-code as an accepted v1 limitation."
evidence = "python/src/dotfiles_setup/session_state.py:120-128 — comment ':122 Quoted filenames with special characters remain a documented v1 limitation.', `_STATUS_PREFIX_LENGTH = 3` at :32, `line[_STATUS_PREFIX_LENGTH:]` at :125."
native_alternative = "git's own machine-parsing format: `git status --porcelain -z`, which prints pathnames as-is with no quoting and terminates records with NUL, removing the core.quotePath escaping the module cannot decode. With `--no-renames` already passed there is no two-path rename record to disambiguate, so the change is a one-line switch from `out.splitlines()` to `out.split(\"\\0\")`."
alternative_evidence = ".agent/kb/raw/git-status-porcelain-z.md (saved verbatim from git-status(1), git 2.50.1 host probe 2026-09-09): 'When the -z option is given, pathnames are printed as is and without any quoting and lines are terminated with a NUL (ASCII 0x00) byte. Without the -z option, pathnames with \"unusual\" characters are quoted as explained for the configuration variable core.quotePath'."
rationale = "The limitation the module documents is not inherent — git ships the exact machine-readable mode that erases it, and the repo's own use-tool-builtins rule makes 'the tool already does this' the default answer. Adopting `-z` also removes the fragile `len(line) > 3` guard, since NUL framing makes the record boundary unambiguous. Refactor rather than retire: the surrounding tri-state gather() still has no native equivalent (finding 1)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_state-3"
unit = "mod-session_state"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`_checks_summary`: a hand-written tally over the `statusCheckRollup` JSON that renders `N/M passing`, with its own passing set {SUCCESS, NEUTRAL, SKIPPED, PASS} read from whichever of conclusion/state/status happens to be present."
evidence = "python/src/dotfiles_setup/session_state.py:166-180 (`passing_values = frozenset({\"SUCCESS\", \"NEUTRAL\", \"SKIPPED\", \"PASS\"})`, `return f\"{passing}/{len(rollup)} passing\"`), fed by the `--json number,title,statusCheckRollup` request at :198-210."
native_alternative = "`gh pr checks` computes and prints this tally natively, and separates the buckets the custom set conflates: 'All checks were successful' / 'Some checks were not successful' plus '%d cancelled, %d failed, %d successful, %d skipped, and %d pending checks'. `gh pr checks --json` exposes the same per-check buckets for programmatic use."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/pr/checks/output.go:70-89 (summary strings and the five-bucket tally format); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/pr/checks/checks.go:123 (`cmdutil.AddJSONFlags(cmd, &opts.Exporter, prCheckFields)`)."
rationale = "Counting SKIPPED and NEUTRAL inside 'passing' makes '8/8 passing' indistinguishable from '8 skipped' in the very block a resuming session reconciles against — the same conflation class as feedback_skipped_check_satisfies_required. gh already owns the bucket vocabulary. Adopt gh's buckets (either by calling `gh pr checks --json` or by mirroring output.go's five categories in the render) rather than keeping a private passing set; a straight retire is wrong because `gh pr checks` costs a second network round-trip and the current summary rides on the single `gh pr list` call the module already makes."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_state-4"
unit = "mod-session_state"
kind = "module"
disposition = "refactor"
confidence = 0.45
custom_thing = "Hand-rolled argument validation in `main()`: a list comprehension that treats any token other than the literal `--no-pr` as unknown, writing its own usage error to stderr and returning rc=2. No `--help`, no declared flag surface."
evidence = "python/src/dotfiles_setup/session_state.py:290-298 (`unknown = [arg for arg in args if arg != \"--no-pr\"]`, `sys.stderr.write(f\"session-state: unknown argument(s): ...\")`, `return 2`); wired at python/src/dotfiles_setup/main.py:1269 and :2361; the mise entrypoint is a bare passthrough at mise.toml:916."
native_alternative = "mise's `usage` field on a TOML task declares flags declaratively, validates them, generates `--help`, and exposes them as `usage_*` env vars / `{{ usage.* }}` template values — e.g. `usage = 'flag \"--no-pr\" help=\"skip the GitHub lookup\"'` on [tasks.session-state]. For the direct `dotfiles-setup session-state` route the stdlib equivalent is `argparse`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-arguments.md:7-29 ('### 1. Usage Field (Preferred)', `flag \"-v --verbose\" help=...`, values exposed as `usage_*`) and :33-42 (env-var exposure); note :479 of /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md deprecates the older Tera `flag()` form in favour of it."
rationale = "Partial coverage, which is why this is refactor and not retire: mise's usage spec only guards the `mise run session-state` route, while `dotfiles-setup session-state` is dispatched directly from main.py:2361 and would still reach the hand-rolled check. The win is a declared, self-documenting flag surface (`mise run session-state --help`) instead of a literal-string comparison that silently accepts `--no-pr` typos as 'unknown argument' with no suggestion. Low confidence because the current code is four lines and correct; this is a hygiene adoption, not a defect."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-session_state-5"
unit = "mod-session_state"
kind = "module"
disposition = "unknown"
confidence = 0.30
custom_thing = "The git half of `gather()` — current branch, tree cleanliness, and the last N commit subjects — collected by three subprocess calls specifically so a post-/clear session can re-orient."
evidence = "python/src/dotfiles_setup/session_state.py:103-141 (`_current_branch`, `_dirty_paths`, `_recent_commits`) and :232-247 (gather); the consumer is the post-/clear step at .claude/skills/session-resume/SKILL.md:47."
native_alternative = "Claude Code appears to inject the same three facts (current branch, main branch, `git status` output, recent commits) into every fresh session as an automatic context block, which would make the git half redundant for the skill's only caller. This is observed in the live harness at host version 2.1.266 but is NOT documented anywhere in the offline corpus, so it is unverified as a stable, relied-upon interface."
alternative_evidence = "No citation available. Control-armed grep over /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/*.md with the same command shape: 'SessionStart' -> 5 files (probe discriminates), 'gitStatus' -> only statusline.md:549-552 (a user-authored example, not the injection), 'Recent commits' / 'git status at the start' / 'snapshot in time' -> 0 files. Not present in .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 new capabilities, none about git context) or .agent/kb/raw/claude-code-llms-index.digest.md."
rationale = "If the harness reliably injects branch/status/recent-commits at session start, `gather()`'s git half duplicates it for the session-resume path and only the PR half plus the paste-ready rendering earn their keep. But an undocumented context block is not an interface: it is absent from the docs corpus and from both gap digests, it says nothing about worktrees or a mid-session refresh, and the module is also invoked from a plain shell (mise.toml:913-916) where no harness context exists. Settle it before touching anything."
probe = "grep -rn \"gitStatus\\|Recent commits\\|Main branch (you will usually\" /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/ /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/claude-code-docs/ ; then, for the live arm, in a scratch dir outside the repo: cd \"$(mktemp -d)\" && git init -q && git commit -q --allow-empty -m probe && claude -p 'Print verbatim any git branch/status/commit context you were given before this message; if none, print NONE' --permission-prompts none"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_store-1"
unit = "mod-session_store"
kind = "module"
disposition = "keep"
confidence = 0.83
custom_thing = "Hand-rolled incremental, content-addressed parse-fact cache: fingerprint-keyed REUSE/APPEND/REBUILD classification over a per-source byte prefix, so a grown transcript is parsed only from its previous newline boundary forward."
evidence = "python/src/dotfiles_setup/session_store.py:274-314 (_decision/lookup), :362-455 (resolve, append_parser vs cold_parser oracle), :316-350 (publish_source); sole caller python/src/dotfiles_setup/session_ledger.py:3650-3672 via mise.toml:1478-1493 [tasks.session-review]"
native_alternative = "mise 2026.9.x experimental task artifact cache — `sources`/`outputs` up-to-date skipping plus `cache = { enabled, env, command_inputs }`, backed by a content-addressed store (and the CAS remote-cache protocol)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/toml-tasks.md:185-191; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/task-configuration.md:624-637 (\"Stores successful task results in a content-addressed local cache\"; requires experimental, at least one matching source, and explicit outputs or `outputs = []`; \"absolute outputs ... are not supported\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/remote-cache-protocol.md:10-18 (CAS + action results)"
rationale = "mise's cache is binary per task invocation: any change to any declared source is a full miss and the whole task re-runs. This unit's entire value is the opposite — a *partially changed* input where the unchanged prefix's facts are reused and only the appended suffix is decoded (CacheStats.decoded_bytes = len(suffix), session_store.py:429-433). The inputs also sit outside the workspace (Claude/Codex transcripts under ~/.claude/projects/<encoded-cwd>/*.jsonl, /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__sessions.md:272), and the live session's own transcript grows during the run, so a task-level cache would miss on every invocation while mise's audit/outputs machinery is scoped beneath the workspace root. No native mechanism in mise, hk, uv, gh, graphify or Claude Code parses an append-only JSONL suffix; keeping the custom store is justified."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_store-2"
unit = "mod-session_store"
kind = "module"
disposition = "refactor"
confidence = 0.72
custom_thing = "Hand-rolled canonical JSON (`json.dumps(..., separators, sort_keys, ensure_ascii=False)`) plus an untyped `json.loads` read and a ~50-line isinstance/KeyError validation ladder that reconstructs `SourceState` field by field."
evidence = "python/src/dotfiles_setup/session_store.py:126-129 (_canonical_json), :194-199 (_read_json), :201-244 (_cached_manifest/_cached_state manual validation), :342-348 and :354-359 (manifest/receipt encoding)"
native_alternative = "The repo's own mandated serialization seam `dotfiles_setup.codec.encode/decode` over msgspec — typed decode validates a dataclass in one call (msgspec raises ValidationError instead of a hand-written ladder), and msgspec's encoders take a native `order='deterministic'|'sorted'` option for canonical byte output."
alternative_evidence = "python/src/dotfiles_setup/codec.py:339-374 (encode/decode); python/AGENTS.md \"Serialization: route every call through codec (#675)\"; python/pyproject.toml:106-117 (TID251 ban on direct msgspec entry points); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/msgspec/docs/changelog.md:107 (\"Add an `order` option to all encoders for enforcing deterministic/sorted\") and :42; proof the on-disk records already decode this way: tests/test_session_ledger.py:832-837 decodes runs/latest.json and RunReceipt with codec.decode"
rationale = "The store's own tests already read its artifacts back through codec.decode into the dataclass, so the write side and the manual validation ladder are the only remaining hand-rolled half. Routing manifests/pointers/receipts through codec (extending it with the msgspec `order` argument, which codec does not yet expose) removes ~50 lines of isinstance/KeyError checking and puts the store on the single serialization seam #675 created. Not a retire: the CAS design, the fingerprint comparison and the fallback-to-REBUILD-on-invalid semantics stay. Byte output changes, which only invalidates the local cache once."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_store-3"
unit = "mod-session_store"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "Private atomic-write helper: mkstemp in the destination directory, fsync the file, `Path.replace`, then fsync the parent directory."
evidence = "python/src/dotfiles_setup/session_store.py:160-178 (_atomic_write)"
native_alternative = "An existing in-repo implementation of the same recipe — `dotfiles_setup.session_gate._atomic_json` (mkstemp + canonical json.dump + fsync + replace) — with two further near-copies in docker.py and sync.py; one shared helper instead of four."
alternative_evidence = "python/src/dotfiles_setup/session_gate.py:94-107; python/src/dotfiles_setup/docker.py:150; python/src/dotfiles_setup/sync.py:371; python/src/dotfiles_setup/lint.py:98 (a fourth os.replace-atomicity site)"
rationale = "No tool ships this as a native feature (stdlib has no atomic-write one-liner and no third-party dep is warranted for ~15 lines), so the supersession here is internal: the same recipe exists four times and only session_store's copy fsyncs the parent directory, so the others are strictly weaker on crash-consistency. Consolidating on the strongest copy is a real, small win; the store keeps its `_publish_immutable` conflict check, which is store-specific."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "mod-session_store-4"
unit = "mod-session_store"
kind = "module"
disposition = "keep"
confidence = 0.78
custom_thing = "Cross-process single-writer serialization of cache lookup+publication via an exclusive `fcntl.flock` on `<root>/store.lock`, held for the whole resolve."
evidence = "python/src/dotfiles_setup/session_store.py:150 (lock_path), :152-158 (locked contextmanager), :377 (resolve holds it across snapshot, parse and publish)"
native_alternative = "hk's `exclusive = true` step option (hk maintains per-file read/write locks and serializes exclusive steps against all others)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/hooks.md:14 (\"`exclusive = true` hook steps will wait until all previous steps finished and block later steps from starting\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/why-hk.md:5 (read/write lock model)"
rationale = "hk's exclusivity is scoped to steps inside one hk hook run, and this store is never invoked from an hk step — it runs under `mise run session-review` / the session-review gate tasks (mise.toml:1478-1523), and two such runs (or a gate plus an ad-hoc run) are exactly the concurrency it must survive. mise has no equivalent cross-invocation task lock in the corpus, and the same flock idiom is already the repo's convention for this class of problem (dag_tick.py:1010, codex_verdict.py:441). Nothing native covers it; the stdlib call is three lines."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-session_store-5"
unit = "mod-session_store"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "A class named `SessionStore` whose public surface is `lookup`/`resolve`/`publish_source`/`publish_run` over Claude and Codex transcript files."
evidence = "python/src/dotfiles_setup/session_store.py:132-158; imported as `SessionStore` in python/src/dotfiles_setup/session_ledger.py:26-30"
native_alternative = "The Claude Agent SDK exports its own `SessionStore` — a Protocol with required `append(key, entries)` / `load(key)` plus optional `list_sessions`, `list_session_summaries`, `delete`, `list_subkeys`, used to mirror session transcripts to an external backend (S3/Redis/DB) for cross-host resume."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__session-storage.md:9 (\"A `SessionStore` adapter lets you mirror those transcripts to your own backend\") and :20-60 (the exported TypeScript/Python `SessionStore` interface, `SessionKey`, `SessionStoreEntry`)"
rationale = "The SDK's SessionStore is a *write/mirror* adapter the SDK calls while producing a transcript, for resume across hosts; it offers no parse-fact caching, no content addressing, no incremental suffix parsing, and it only exists inside a process running the Agent SDK — this repo reads finished transcripts from disk instead. So it does not supersede anything here. Recorded because the identical name is a live confusion hazard for anyone reading `from dotfiles_setup.session_store import SessionStore` and assuming SDK-protocol semantics; the fix is a one-line docstring note (or a rename such as `FactStore`), not a change of mechanism."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-skillopt_provenance-1"
unit = "mod-skillopt_provenance"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "The gate this module exists to be is orphaned. `mise run skillopt-provenance-check` is the module's only entrypoint, and nothing runs it: no hk step, no CI job, no suites.toml contract, no mise `depends`."
evidence = "mise.toml:1518-1520 defines `[tasks.skillopt-provenance-check]` -> `uv run --project python python -m dotfiles_setup.skillopt_provenance`. A whole-repo grep for the task name returns exactly two hits: mise.toml:1518 (the definition) and python/src/dotfiles_setup/skillopt_provenance.py:395 (the module's own log string). CONTROL ARM for the grep shape: the same grep for `pin-actions` returns hits in BOTH hk.pkl:419 and mise.toml:1216, so the command can see cross-file wiring when it exists — the absence is real, not a blind probe. The only other repo reference to the module at all is a prose mention in .github/workflows/ci.yml:186 (a comment about fetch-depth), which does not invoke it."
native_alternative = "mise task dependency wiring (`depends` / `depends_post`), which is how every other gate in this repo is chained, or an hk step in hk.pkl (as `pin-actions` is referenced from) / a `[[suite]]` in python/verification/suites.toml."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/tasks/architecture.md:34-51 — `#### depends - Prerequisites` / `#### depends_post - Cleanup Tasks`, with the `[tasks.test] depends = [\"lint\", \"build\"]` example at lines 38-42."
rationale = "A verification gate that nothing invokes is decoration — it can only ever pass, because it never runs. The custom side is fine; the wiring is missing. mise already has the native mechanism (`depends`) and the repo already uses it elsewhere, so the fix is one line in mise.toml (chain it under `verify` / `verify-local`) plus a suites.toml contract binding that call site so its removal is not silent. Note that `sibling` tasks in the same region of mise.toml (session-review-mutation-*) ARE reachable, being named in session_gate.py's PREVENTIONS table — this task is the odd one out."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-skillopt_provenance-2"
unit = "mod-skillopt_provenance"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "A bespoke mutation-replay harness with its own receipt schema: `replay()` extracts a commit into a tempdir, runs the pinned test clean, applies a hard-coded `old -> new` source mutation, re-runs, and requires positive rc==0 / hostile rc!=0; `_run()` digests argv/stdout/stderr into a receipt; `_verify_receipt()` re-validates the committed receipts against schema `dotfiles.skillopt-present-day-replay.v1`."
evidence = "python/src/dotfiles_setup/skillopt_provenance.py:233-269 (`_run`), :272-299 (`replay`, incl. the `text.count(fix.old) != 1` anchor assert at :290 and the discrimination assert at :294), :302-355 (`_verify_receipt`), :27 (SCHEMA), :30-34 (REPLAY_RECEIPTS)."
native_alternative = "The repo's OWN already-shipped attested mutation-sentinel: `dotfiles_setup.session_gate.mutation_sentinel()` + `run_command()`, which copies the target into an isolated candidate root, applies a registered `mutant_replacement`, runs the registered gate task, requires the gate to REJECT the mutant, and atomically writes a digested, nonce-bound receipt. It is already wired to mise tasks (`session-review-mutation-credential-launcher`, `session-review-mutation-git-hook-contamination`)."
alternative_evidence = "python/src/dotfiles_setup/session_gate.py:178-200 (`run_command`: \"Execute one mutation/control gate and atomically attest its result\", ATTESTED/FAILED status, argv+rc+digest receipt), :261-300 (`_execute_private`, `mutation_sentinel`: \"Mutate an isolated candidate and prove the normal gate rejects it\", `if result.returncode == 0 or before == after: return 2`), :69,:81 (registered mutation task names) and mise.toml:1510-1516 (the two wired mutation tasks). NOTE: this is an IN-REPO existing mechanism, not a corpus-documented external native feature — I found no external native equivalent (no mutation-testing plugin is in the uv lock, and pytest ships none)."
rationale = "Two independent implementations of the same idea (mutate -> prove the check flips -> digest a receipt) with two schemas, two receipt layouts and two tamper checks. `use-tool-builtins.md` makes the existing mechanism the default answer. What session_gate does NOT cover, and would have to be added as a parameter rather than a copy: skillopt replays against a historical commit extracted with `git archive` rather than the working tree, and it pins a pytest node id rather than a mise gate task. So this is a fold-into-one-registry refactor, not a straight retire. Confidence held at 0.6 because the merge is non-trivial and the receipts are already committed under a stable schema."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-skillopt_provenance-3"
unit = "mod-skillopt_provenance"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "`verify_live()` issues nine sequential, uncached REST reads through `gh api` — three per fix (`git/commits/<sha>`, `contents/<path>?ref=<sha>`, `pulls/<n>`) — each a separate `fnox` + `gh` process spawn with a 60s timeout, and treats ANY bytes on stderr as failure."
evidence = "python/src/dotfiles_setup/skillopt_provenance.py:196-230 (`verify_live`, three `_json_fetch` calls per fix over the 3-element FIXES tuple), :165-181 (`github_fetch`, `subprocess.run(fnox_command((\"gh\", \"api\", *argv)) ...)`, and the `if result.returncode != 0 or result.stderr:` rejection at :179)."
native_alternative = "`gh api graphql` — one batched request can resolve all three commits, their trees, the blob objects and the merged-PR bindings in a single round trip; and `gh api --cache <ttl>` gives native response caching for the repeated identity reads."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/api/api.go:83 (\"graphql to access the GitHub API v4\"), :174 and :185 (`$ gh api graphql -F owner='{owner}' ...`, `gh api graphql --paginate -f query=`), :301 (`cmd.Flags().DurationVar(&opts.CacheTTL, \"cache\", 0, \"Cache the response, e.g. \\\"3600s\\\", \\\"60m\\\", \\\"1h\\\"\")`)."
rationale = "The custom side is nine spawns where gh natively supports one. Two secondary points fall out of the same seam and argue for touching it: (a) the `or result.stderr` rejection at :179 makes ANY gh diagnostic on stderr — a deprecation notice, a rate-limit warning — indistinguishable from a provenance failure, so the gate can go red for a reason that has nothing to do with provenance; `gh api --cache` would also reduce how often that surface is hit. (b) A single GraphQL document makes the whole binding atomic against a mid-verification force-push, which nine independent reads are not. Not a retire: the custom shape (which fields must match) is the actual policy and stays."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-skillopt_provenance-4"
unit = "mod-skillopt_provenance"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "A hand-rolled tamper-evidence layer: canonical sorted-key JSON manifest plus a sidecar `.sha256`, re-derived and byte-compared on every verification, with receipt filenames carrying their own content digest as the last `-`-separated segment."
evidence = "python/src/dotfiles_setup/skillopt_provenance.py:124-154 (`manifest_bytes`, `json.dumps(..., sort_keys=True, separators=(\",\",\":\"))`), :157-162 (`export` writing MANIFEST + `.sha256`), :358-377 (`verify_local`: byte-equality against a freshly re-derived manifest, exact filename-set equality against REPLAY_RECEIPTS), :302-305 (filename-digest check)."
native_alternative = "`gh attestation verify` — GitHub's sigstore-backed artifact provenance: a cryptographically signed claim binding a subject artifact to the Actions workflow (actor identity: repository/owner + signer workflow, validated against the certificate's SourceRepository / SourceRepositoryOwner / SAN fields)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/attestation/verify/verify.go:26-60 (`Use: \"verify [<file-path> | oci://<image-uri>] [--owner | --repo]\"`, `Short: \"Verify an artifact's integrity using attestations\"`, and the Long text describing predicate types and the actor-identity fields)."
rationale = "JUSTIFICATION (required): `gh attestation` binds an artifact to the GitHub Actions workflow that PRODUCED it. This manifest is not produced by a workflow — it is a locally authored assertion about three already-landed commits, generated by `export()` on a developer machine and committed. There is no signer workflow for sigstore to attest, so `gh attestation verify` has nothing to verify here, and adopting it would require first inverting the whole flow so CI generates the manifest. The self-hash it does implement is also honest about its own limit: a `.sha256` sidecar written by the same code that writes the manifest detects accidental drift, not an adversary with commit access — which is exactly why `verify_live()` exists alongside it. Flagging the inversion (have CI emit + attest the manifest) as a possible future direction, but at today's pins nothing native covers this, so keep."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-skillopt_provenance-5"
unit = "mod-skillopt_provenance"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "Network-side commit/tree/blob identity checking done over HTTP (`repos/.../git/commits/<sha>` and `contents/<path>?ref=<sha>` + base64-decode + sha256 of the blob + a `def <node>(` substring check), when git itself is content-addressed and the same objects are provably present locally."
evidence = "python/src/dotfiles_setup/skillopt_provenance.py:199-220 (commit/tree compare, base64 decode of `content`, `digest(blob) != fix.blob_sha256`, `f\"def {fix.node}(\" not in blob.decode()`). The same module already proves the objects are locally reachable: :277-282 runs `git archive fix.commit` against the local repo. The test double takes the offline route for the same assertion — tests/test_skillopt_provenance.py:37-41 serves the blob from `git show <commit>:<path>`."
native_alternative = "git's own object database: `git cat-file` / `git rev-parse <commit>^{tree}` verify commit->tree->blob identity offline, with no HTTP call, because a git object id IS the hash of its content."
alternative_evidence = "No corpus doc tree for git was provided in this unit's CORPUS list, so the native side is cited from the repo's own already-working use of it rather than from vendor docs: python/src/dotfiles_setup/skillopt_provenance.py:277-282 (`git archive fix.commit` succeeds locally under `check=True`) and tests/test_skillopt_provenance.py:37-41 (`git show <commit>:<path>` returns the identical blob the live path base64-decodes)."
rationale = "JUSTIFICATION (required): the local route is cheaper but answers a strictly weaker question. `git cat-file` proves 'this content hashes to this id'; it cannot prove 'this commit is reachable from ray-manaloto/dotfiles on GitHub and landed via merged PR #728'. Only the third of the three live reads (`pulls/<n>` — merged, merge_commit_sha, base.repo.full_name, :221-230) strictly requires the network, but dropping the first two would leave the merge-commit assertion unanchored to a tree the checker has independently identified. The remote round trip is the point of the check, not incidental cost, so it stays. The efficiency half of this concern is already carried by finding -3 (batch the same reads into one `gh api graphql` call) — that is the change worth making, not moving the check offline."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-sync-1"
unit = "mod-sync"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "Registry-vs-local-tag staleness detection plus a durable convergence witness: SyncStatus.stale, SyncRecord, _state_file/read_sync_record/write_sync_record (per-arch, per-workspace containers map, atomic tmp+rename)."
evidence = "python/src/dotfiles_setup/sync.py:133 (SyncStatus.stale), :213 (SyncRecord), :259 (_state_file), :264 (read_sync_record), :304 (write_sync_record); wired as `mise run sync` -> `dotfiles-setup docker sync` at mise.toml:939 and asserted by python/verification/suites.toml:1127 (workflow.sync-wiring)."
native_alternative = "@devcontainers/cli 0.89.0 `devcontainer outdated` (the only native 'is my devcontainer out of date' verb), or a pure-docker digest comparison (`docker image inspect --format '{{json .RepoDigests}}'` / the registry per-platform config digest from `docker buildx imagetools inspect --raw`) instead of a state file."
alternative_evidence = ".agent/kb/raw/mod-sync-native-probes.md P1 (live run: `devcontainer outdated --output-format json` returns ONLY {\"features\":{...sshd...}} — no base image, no container state) and P2 (on a CONVERGED workspace the local RepoDigest 043c749… equals the local image Id, not the registry index digest 2741c01d…, and the registry amd64 config digest 3d3794e2… also differs; control arm: the record's registry_digest is byte-identical to the live index digest, so the comparison can report 'same')."
rationale = "Both candidate native routes were probed live and both fail. `devcontainer outdated` is scoped to Feature/Template versions declared in devcontainer.json — it has no notion of the base image digest or the local overlay container, which is the entire subject of this module. And no docker-side digest is stable across the buildkit `--output type=docker` re-export this repo must use instead of a classic pull: on a workspace the record calls converged, RepoDigests and the registry config digest BOTH disagree with the local store, which is exactly why the state record exists. Nothing native carries that witness."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-sync-2"
unit = "mod-sync"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "Shared-state write accepts a lost update: two clones converging concurrently read-modify-write ~/.local/state/dotfiles/sync-<ref>.json with only a tmp+rename for torn reads, and the docstring documents the lost update as an accepted fail-safe."
evidence = "python/src/dotfiles_setup/sync.py:362-373 (\"A lost update (two writers, last wins) is still possible and is fail-safe\"), reading at :329 and writing at :368-373; the file is host-wide by construction (_state_file names it after the image ref only, sync.py:259, called out at sync.py:243)."
native_alternative = "stdlib `fcntl.flock(LOCK_EX)` around the read-modify-write — the mechanism this repo already uses in three other modules for precisely this 'two ticks must not both write one file' case."
alternative_evidence = "python/src/dotfiles_setup/dag_tick.py:997-1010 (`try_acquire_lock`, flock LOCK_EX|LOCK_NB), python/src/dotfiles_setup/codex_verdict.py:441 (flock LOCK_EX, docstring at :415 \"Everything else under an exclusive flock, so two ticks cannot both …\"), python/src/dotfiles_setup/codex_lane.py:311."
rationale = "An existing in-project mechanism (three independent flock implementations already shipped) covers the exact race this module documents and then declines to close. The cost of the lost update is bounded (one spurious rebuild ~20-30 min on this host), so this is not urgent — but three copies of a lock helper plus a fourth site that wanted one and went without is the shape that argues for extracting ONE helper and adopting it here. Not 'retire' because the record itself stays (mod-sync-1)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-sync-3"
unit = "mod-sync"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "Hand-rolled JSON persistence for the SyncRecord model: json.loads + isinstance guards on decode, json.dumps of a hand-built dict on encode."
evidence = "python/src/dotfiles_setup/sync.py:285-299 (json.loads, three isinstance degradations, manual SyncRecord construction) and sync.py:352-361 (json.dumps of a literal dict — note the written shape and the dataclass have already diverged: legacy_container_image_id is parsed but never written)."
native_alternative = "dotfiles_setup.codec (msgspec) — the project's declared single model system, which does typed decode with defaults natively so no isinstance ladder is needed."
alternative_evidence = "python/AGENTS.md:85 (\"## Serialization: route every call through `codec` (#675)\" — msgspec is the project's model system, #669), python/pyproject.toml:107-125 (the TID251 ban list making codec the only route); existing adopters: python/src/dotfiles_setup/session_ledger.py, python/src/dotfiles_setup/graphify.py."
rationale = "The repo declares msgspec-via-codec as the one model system, and this module predates/ignores it for a dataclass that is literally a persisted document. A msgspec Struct would give the encode side for free and kill the encode/decode shape drift already visible above. It is 'refactor', not 'retire', because the decode semantics are deliberately degrade-not-raise (a corrupt field must not crash a converge, sync.py:264-283) whereas msgspec raises ValidationError — so the adoption must wrap the decode and keep the same fallbacks, which is why confidence is only moderate."
probe = "uv run --project python python -c \"import msgspec, dotfiles_setup.codec as c; print(c.decode(b'{\\\"registry_digest\\\":\\\"a\\\",\\\"local_image_id\\\":\\\"b\\\",\\\"containers\\\":5}', type=dict))\" — confirm the wrong-typed `containers` raises ValidationError (i.e. a try/except fallback is required to preserve today's behaviour)."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-sync-4"
unit = "mod-sync"
kind = "module"
disposition = "keep"
confidence = 0.60
custom_thing = "local_platforms(): one `docker image inspect --platform <p>` subprocess PER published target to discover which platforms the local tag already carries."
evidence = "python/src/dotfiles_setup/sync.py:596-624 (loop over published_targets(), one _run per target), consumed by refresh_local_tag at sync.py:653."
native_alternative = "`docker image inspect <ref> --format '{{json .Manifests}}'` — docker-cli 29.7.2 with the containerd snapshotter exposes a per-platform Manifests array, which would enumerate present platforms in ONE call."
alternative_evidence = ".agent/kb/raw/mod-sync-native-probes.md P3: on this host (containerd snapshotter confirmed via `docker info --format '{{.DriverStatus}}'` -> [[driver-type io.containerd.snapshotter.v1]]) `.Manifests` returns `null` for the :dev tag, while the control arm on a bogus key errors (\"map has no entry for key ZzqNoSuchFieldQ\") and `.Id` returns a real digest — so the field exists in the schema but is unpopulated for the image sync cares about."
rationale = "The native single-call route was armed and measured empty for exactly the tag this code inspects, so adopting it today would silently report 'no platforms present' and make refresh_local_tag stop preserving the other architecture's layers — the #800 F2 regression the loop exists to prevent. Keep the N-probe loop until the probe below reports a populated array on a host that genuinely carries both platforms."
probe = "On a host that has synced BOTH architectures: docker image inspect ghcr.io/ray-manaloto/dotfiles-devcontainer:dev --format '{{json .Manifests}}' — if it lists both platform descriptors, replace the loop with one call plus a control arm asserting the field is non-null before trusting it."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-sync-5"
unit = "mod-sync"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "refresh_local_tag(): a synthetic `docker buildx build --pull --platform <union> --output type=docker -t <ref> -` over a one-line `FROM <ref>` to re-anchor the LOCAL tag on the registry's current manifest."
evidence = "python/src/dotfiles_setup/sync.py:627-678; the rejection of the obvious native is recorded at sync.py:49-50 (\"Classic docker pull is never used (it wedges on the ~38GB image)\") and in .claude/rules/mise-tasks-only.md's task map row (`docker pull …dotfiles-devcontainer…` -> `mise run sync`)."
native_alternative = "`docker pull --platform` (the plain native refresh) or `docker buildx imagetools create -t <ref> <ref>`."
alternative_evidence = ".agent/kb/raw/mod-sync-native-probes.md P4: `docker buildx imagetools create --help` -> \"Create a new image based on source images\", with `--dry-run  Show final image instead of pushing` — its action is a registry PUSH; it exposes no local-store output, so it cannot re-anchor a local tag."
rationale = "Two natives exist and neither does the job: imagetools create writes to a registry, not to the local image store (probed above), and classic `docker pull` is the measured-wedge path this repo bans for the ~38GB image. The buildkit `FROM` trick IS the native mechanism being used — the custom part is only the platform-union computation, which no docker flag expresses."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-sync-6"
unit = "mod-sync"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "In-flight CI awareness: inflight_ci_runs() lists 10 ci.yml runs on the branch and filters `status != \"completed\"` client-side; wait_for_run() shells `gh run watch --exit-status` and then re-reads the conclusion through the API."
evidence = "python/src/dotfiles_setup/sync.py:554-576 (client-side filter) and sync.py:579-593 (watch + API cross-check), driven by _report_inflight at sync.py:722-753."
native_alternative = "gh's own server-side filters and watcher: `gh run list --status <s>` and `gh run watch --exit-status`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/run/list/list.go:96 (`cmdutil.StringEnumFlag(cmd, &opts.Status, \"status\", \"s\", \"\", shared.AllStatuses, ...)` — a SINGLE-value enum), /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/run/shared/shared.go:52-64 (AllStatuses: queued, completed, in_progress, requested, waiting, pending, …), /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/run/watch/watch.go:88 and :141 (--exit-status)."
rationale = "This capability already IS the native one, wrapped as thinly as it can be. `--status` takes one value out of five non-terminal statuses, so pushing the filter server-side would cost five calls instead of one call plus a one-line predicate. And the API cross-check after `gh run watch --exit-status` is mandated by .claude/rules/gh-cli-watch.md and feedback_gh_run_watch (watch has reported 0 prematurely), so it is a documented correction to the native tool, not a reinvention of it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-sync-7"
unit = "mod-sync"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The orchestration itself: the decide_action state matrix (stale / overlay-current / container-state -> rebuild|up|verify-only), the --check tri-state exit code (0 current / 1 stale-or-outdated / 2 registry-unreachable), and the tiered post-converge gate (verify_latest smoke vs `mise run verify-local`)."
evidence = "python/src/dotfiles_setup/sync.py:681-696 (decide_action), :837-854 (--check tri-state), :791-808 (_verify), :811-870 (sync_main); consumers: mise.toml:928-939 and python/src/dotfiles_setup/pr.py:906 (ship/land's hard sync --full gate)."
native_alternative = "None found. Surveyed at the host's pins: @devcontainers/cli 0.89.0 (up/build/outdated/upgrade/exec — probed, .agent/kb/raw/mod-sync-native-probes.md P1), docker-cli 29.7.2, mise 2026.9.3 (no new capability since the pin — .agent/kb/raw/mise-releases-since-pin.digest.md), hk 1.57.0 (1.58.1 is docs-only — .agent/kb/raw/hk-releases-since-pin.digest.md), gh, uv, renovate."
alternative_evidence = ".agent/kb/raw/mise-releases-since-pin.digest.md (\"ZERO releases strictly newer than the baseline, so there is no new capability\"), .agent/kb/raw/hk-releases-since-pin.digest.md (\"None. … no changes to hk's runtime behavior\"), .agent/kb/raw/mod-sync-native-probes.md P1 (devcontainer CLI command surface)."
rationale = "Required justification: the decision matrix is repo-specific policy — it composes three facts no single tool holds together (a registry index digest, this repo's #677 two-label container identity, and this repo's smoke/verify tiers) and encodes a fail-safe direction (offline => never take a destructive action) that no generic tool would choose for us. mise gained nothing since the pin and hk's only newer release is documentation, so there is no 2026-09 capability to adopt here. The module is already the thin-caller shape the repo's zero-bash-logic policy asks for, and suites.toml:1127 gates that shape."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-token_audit-1"
unit = "mod-token_audit"
kind = "module"
disposition = "keep"
confidence = 0.92
custom_thing = "Cross-file contract-token uniqueness audit: find_ambiguous() walks every `per_path_tokens` entry in the bespoke manifest python/verification/suites.toml, counts each token's substring occurrences in its declared file, and fails on any count != 1; AMBIGUITY_ALLOWED is a 31-entry allowlist of deliberate multiplicity, each with a written justification, and a stale entry (allowlisted but now unique/gone) also fails."
evidence = "python/src/dotfiles_setup/token_audit.py:358-367 (find_ambiguous), :87-287 (AMBIGUITY_ALLOWED), :389-407 (find_violations, incl. the stale-entry arm); wired as hk step `contract_token_uniqueness` at hk.pkl:354-356 (`check = \"uv run --project python dotfiles-setup token-audit\"`) and asserted by suite `workflow.token-audit-wiring` at python/verification/suites.toml:1658-1690."
native_alternative = "None. hk 1.57's builtin catalogue (145 steps) has no occurrence-counting or token-uniqueness step; the closest TOML steps are `taplo`/`tombi` (syntax + schema lint of one file, no cross-file predicate). No Claude Code hook event, permission rule, agent-frontmatter field, ast-grep rule, zizmor/ghalint/agnix check, or pytest plugin models 'this literal must occur exactly once in that other file'. hk 1.58.1 (the only release newer than the 1.57.0 pin) is documentation-only."
alternative_evidence = "Builtin enumeration: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/ (145 *.pkl files listed; no token/uniqueness/occurrence builtin). Probe: `grep -rni 'uniqueness|occurrence' /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs` -> 0 hits, control-armed with the same command shape on a term known present ('batch' -> hk/docs/configuration.md, hk/docs/builtins.md, hk/docs/public/llms.txt), so the 0 is real. Gap digest .agent/kb/raw/hk-releases-since-pin.digest.md: v1.58.1 is 'A small documentation-only release. There are no changes to hk's runtime behavior.'"
rationale = "The predicate is defined over an artifact no external tool knows exists: this repo's own verification manifest and its handler semantics (`per_path_tokens` -> path -> token list). A native tool would have to be taught the manifest's schema AND the cross-file resolution before it could count anything, at which point it is this module. The allowlist-with-justification + stale-entry-fails shape is likewise repo-specific policy (same shape as bash_budget.py) and has no baseline-file analogue in any pinned linter that can express 'this exception is no longer needed'. Keeping it is justified, not defaulted."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-token_audit-2"
unit = "mod-token_audit"
kind = "module"
disposition = "refactor"
confidence = 0.45
custom_thing = "find_unaudited(): a purely structural rule over suites.toml — a suite with `handler = \"require_tokens\"`, a non-empty bare `tokens` list, and exactly one entry in `paths` is a violation, because the bare form is invisible to the uniqueness audit while meaning the same thing as `per_path_tokens`."
evidence = "python/src/dotfiles_setup/token_audit.py:370-386 (the predicate is `suite.get('handler') == 'require_tokens' and suite.get('tokens') and len(suite.get('paths', [])) == 1` — no file content is read); folded into find_violations at :394."
native_alternative = "taplo (already pinned 0.10.0 and already running over every *.toml including suites.toml) can validate a TOML file against a JSON Schema: `taplo lint --schema <URL>` (CLI flag) or a `[schema] path = ...` block in `.taplo.toml`. The rule is exactly a JSON Schema draft-2020-12 `if`/`then: false` over one `[[suite]]` object (`handler` const, `paths` maxItems 1, `tokens` required), so a schema would enforce it — and would additionally catch handler-name typos and unknown keys, which nothing checks today."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/taplo/crates/taplo-cli/src/args.rs:160-162 (`/// URL to the schema to be used for validation.` -> `pub schema: Option<Url>`); .../taplo/crates/taplo-common/src/config.rs:353-364 (`/// Options for schema validation and completion.` ... `/// A local file path to the schema, overrides url if set.`). Already-wired call site: hk.pkl:167-169 uses `Builtins.taplo` with `batch = true`, and the builtin's glob is `**/*.toml` (.../hk/pkl/builtins/taplo.pkl), so suites.toml is already linted by taplo; no `.taplo.toml` exists in the repo, so no schema is configured (`ls -a | grep -i taplo` -> nothing)."
rationale = "Native covers this one sub-capability fully and the tooling is already wired — the delta is a `.taplo.toml` plus a hand-authored `schemas/suites.json`. Two costs argue against a straight swap and are why this is 'refactor, evaluate' rather than 'retire': (a) the module's value here is largely its remediation prose ('that form is invisible to the uniqueness audit ... Move them (#397)'), which a schema error cannot carry; (b) every file under schemas/ today is a VENDORED upstream schema with sha256 provenance managed by schema_vendor.py + schemas/sources.toml, so a locally-authored schema is a new artifact class for that pipeline to special-case. The win is that a schema also closes gaps this module does not look at (handler typos, unknown keys). Decide with the probe; do not retire find_unaudited before a schema is proven to fail on a real violation."
probe = "printf '%s\\n' '[[suite]]' 'name = \"probe.bare-tokens\"' 'handler = \"require_tokens\"' 'paths = [\"README.md\"]' 'tokens = [\"x\"]' > /tmp/probe-suites.toml && cat > /tmp/probe-suites.schema.json <<'EOF'\n{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"type\":\"object\",\"properties\":{\"suite\":{\"type\":\"array\",\"items\":{\"if\":{\"type\":\"object\",\"required\":[\"handler\",\"paths\",\"tokens\"],\"properties\":{\"handler\":{\"const\":\"require_tokens\"},\"paths\":{\"maxItems\":1}}},\"then\":false}}}}\nEOF\nmise exec -- taplo lint --no-auto-config --schema \"file:///tmp/probe-suites.schema.json\" /tmp/probe-suites.toml; echo \"violating rc=$?\"; sed -i '' 's/^tokens.*$/per_path_tokens = { \"README.md\" = [\"x\"] }/' /tmp/probe-suites.toml && mise exec -- taplo lint --no-auto-config --schema \"file:///tmp/probe-suites.schema.json\" /tmp/probe-suites.toml; echo \"clean rc=$? (must be 0 while violating is non-zero, or the probe does not discriminate)\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-token_audit-3"
unit = "mod-token_audit"
kind = "module"
disposition = "keep"
confidence = 0.85
custom_thing = "count_tokens() + preflight_main(): the same occurrence-count predicate exposed as a one-shot CLI (`mise run token-check -- <file> \"<token>\"... [--expect N]`) that prints BOTH arms (OK / AMBIGUOUS / MISSING) so a candidate contract token can be proven before the contract is written."
evidence = "python/src/dotfiles_setup/token_audit.py:335-355 (count_tokens), :309-332 (TokenCount verdict words), :410-455 (preflight_main); mise.toml:1318-1329 ([tasks.token-check] -> `dotfiles-setup token-audit --check`); dispatch at python/src/dotfiles_setup/main.py:624-647 and :2496-2505; contract `workflow.token-check-preflight` binds the SHARED call site at python/verification/suites.toml:2272-2284 (`\"for counted in count_tokens(root, raw, tokens)\"`)."
native_alternative = "Plain substring counting is natively available — `rg --count-matches --fixed-strings '<token>' <file>` or `grep -cF` — and mise could wrap it directly in [tasks.token-check] with no Python at all."
alternative_evidence = "ripgrep/grep are host builtins, not a pinned repo tool; no repo-relative or corpus citation is claimed for a capability this generic. The load-bearing citation is the counter-argument on the custom side: python/verification/suites.toml:2272 states verbatim that 'the property this contract protects is that the two scopes cannot drift apart, which is why `for counted in count_tokens(root, raw, tokens)` is bound as a token rather than merely `def count_tokens(`: the whole-suite walk was re-inlining `text.count(token)` before this change'."
rationale = "Retiring this to a shell one-liner would re-create exactly the defect #652 closed: the pre-flight and the gate would then ask the question through two different implementations, and the pre-flight's entire value is being BELIEVED before the contract is written — a pre-flight that says OK where the gate later says AMBIGUOUS is worse than none. The shared call site is itself contract-bound (suites.toml:2284), so a shell substitute would fail the repo's own gate. Two further behaviours a `grep -c` does not have and the callers depend on: zero and many are reported with DIFFERENT words because they are different defects, and the PASSING arm is printed because a silent success is indistinguishable from a probe that never ran. Native counting exists; native *shared-predicate* counting does not."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-verify-1"
unit = "mod-verify"
kind = "module"
disposition = "refactor"
confidence = 0.85
custom_thing = "Hand-rolled structural JSON validation of the skill eval corpus: `_skill_eval_id` + `skill_eval_corpus` (required-field set, unexpected-field rejection, positive-int id with an explicit bool guard, non-empty-string fields, array-of-strings `files`, `expectations` min-length, duplicate-id detection, required-id consumption) — ~85 lines of imperative type checks feeding a bespoke `fail()` message per rule."
evidence = "python/src/dotfiles_setup/verify.py:218-259 (`_skill_eval_id`), :262-301 (`skill_eval_corpus`), :600-609 (`_handle_skill_eval_corpus`); the single consumer is python/verification/suites.toml:2307-2318 (`workflow.codex-task-orchestration-evals`, handler `skill_eval_corpus`, `skill_name`, `required_eval_ids = [10..23]`)."
native_alternative = "`check-jsonschema` 0.38.0 (already pinned in this repo) validating the corpus against a JSON Schema 2020-12 file — `required`, `additionalProperties: false`, `{type: integer, exclusiveMinimum: 0}`, `minLength`, `items: {type: string}`, `minItems` cover every per-record rule verbatim, including the bool guard (JSON `true` is not an `integer`). The repo already vendors schemas under `schemas/` with `schemas/sources.toml`, so the seam exists."
alternative_evidence = ".agent/kb/raw/mod-verify-probes.md:3-13 — live control-armed probe run this session: `check-jsonschema --version` -> 0.38.0; PASS ARM on the real `.agents/skills/codex-task-orchestration/evals/evals.json` -> rc=0 `ok -- validation done`; FAIL ARM on a mutation (id=0, extra field `bogus_field`, empty `expectations`) -> rc=1 naming all three at `$.evals[0].id`, `$.evals[1]`, `$.evals[2].expectations`. Tool pin: mise.toml `pipx:check-jsonschema = \"0.38.0\"`."
rationale = "The per-record half is fully covered by a declarative schema and should move there — that deletes ~60 of the 85 lines and gets better error messages (JSON pointers) for free. It is NOT a full retire: a third probe arm (`.agent/kb/raw/mod-verify-probes.md:12`) fed the schema a corpus with two records sharing an id and it returned rc=0, so JSON Schema cannot express cross-item `id` uniqueness, and it cannot express `required_eval_ids` consumption (a named set of ids must all be present) either. Those two checks — the only ones that encode the contract's actual intent, that each named replay is consumed — stay in Python and become ~15 lines over a parsed list of ids."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-verify-2"
unit = "mod-verify"
kind = "module"
disposition = "retire"
confidence = 0.90
custom_thing = "Two handlers registered in `HANDLERS` that no contract dispatches to: `dockerfile_structure` (token-ordering assertion inside a Dockerfile, with its own not-found/ordering failure messages) and the `no_vscode_user` legacy alias that just forwards to `_handle_forbid_tokens`."
evidence = "python/src/dotfiles_setup/verify.py:485-531 (`dockerfile_structure`), :632-643 (`_handle_dockerfile_structure`), :646-648 (`_handle_no_vscode_user`), registered at :667 and :669. Zero of the 152 suites select either: a parse of python/verification/suites.toml gives handler counts require_tokens 111, forbid_tokens 17, regex_forbid 8, regex_match 6, policy_doc 4, require_lines 4, skill_eval_corpus 1, schema_drift 1 — and 0 suites omit an explicit `handler` key, so the name-derived fallback at verify.py:72 cannot reach them either. The one suite the alias was named for, `identity.no-vscode-user`, runs under `forbid_tokens`. Neither symbol appears anywhere in tests/test_verify.py."
native_alternative = "For the Dockerfile-ordering intent: `hadolint` 2.15.1, already the pinned Dockerfile linter and wired as an hk builtin. For the alias: nothing — `forbid_tokens` already is the handler."
alternative_evidence = ".agent/kb/raw/mod-verify-probes.md:15-28 — `hadolint --help` enumerated verbatim this session: the only rule-shaped options are `--error/--warning/--info/--style/--ignore RULECODE`, `--trusted-registry`, `--require-label LABELSCHEMA`, `--strict-labels`. There is no user-defined-rule DSL, so hadolint does NOT cover a bespoke before/after ordering assertion — which is moot, because no contract asks for one."
rationale = "~70 lines of handler code plus two `HANDLERS` entries are unreachable from the manifest and untested. This is dead surface that reads as capability: a future contract author sees `dockerfile_structure` in the map and binds to a code path that has never run against a real suite. Delete both handlers and both registry entries; if Dockerfile ordering is ever wanted, `regex_match` with a multiline pattern already expresses it and is exercised by 6 live suites. Retiring is safe precisely because the count is 0, and the counting grep is control-armed (the same command shape returns 111 for `require_tokens`)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-verify-3"
unit = "mod-verify"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "Token binding by UNANCHORED SUBSTRING over raw file text — `require_tokens` (111 of 152 suites), its `per_path_tokens` variant, and the `#`-comment-stripping/allowlist machinery bolted on around it — plus the whole compensating apparatus that exists only because the binding is textual: the `token_audit` uniqueness CLI, the `contract_token_uniqueness` hk step, and the three-question authoring preamble in the manifest."
evidence = "python/src/dotfiles_setup/verify.py:184-215 (`require_tokens`, combined-text substring), :563-587 (`_handle_require_tokens` per-path substring), :169 (comment stripping), :159/:170 (regex allowlist); the compensations at python/verification/suites.toml:6-42 (authoring preamble: 'Token matching is UNANCHORED SUBSTRING in every handler'; '**33 of 33** rebindings closed a LIVE one … **11** were held up by a COMMENT alone') and hk.pkl:354-355 (`contract_token_uniqueness` -> `dotfiles-setup token-audit`). Declared path extensions across the manifest: py 164, toml 56, md 39, yml 35, Dockerfile/no-ext 32, json 30, pkl 13, hcl 10."
native_alternative = "`ast-grep` 0.45.3 (pinned at mise.toml:47, currently installed but wired into nothing) can express a file-level MUST-CONTAIN assertion syntactically: a rule of `kind: <root node>` with `not: {has: {pattern: <call>, stopBy: end}}` fires exactly when the file lacks a real parsed call site, so a comment or a docstring quoting the same text cannot satisfy it. `sg scan -c sgconfig.yml` returns rc=1 with a per-rule `message`, which is the same gate shape `mise run verify` already needs."
alternative_evidence = ".agent/kb/raw/mod-verify-probes.md:30-46 — live two-arm probe run this session with the rule inlined there: ARM A `ast-grep scan` over python/src/dotfiles_setup/verify.py (which DOES call `schema_vendor.check_drift`) -> rc=0, no findings; ARM B over python/src/dotfiles_setup/lint.py (which does not) -> rc=1, one module-level error. Language coverage limit: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/lib.rs:259-286 — `enum SupportLang` lists 29 languages (Bash, C, Cpp, CSharp, Css, Dart, Go, Elixir, Haskell, Hcl, Html, Java, JavaScript, Json, Kotlin, Lua, Markdown, Nix, Php, Python, Ruby, Rust, Scala, Solidity, Swift, Tsx, TypeScript, Yaml) with NO Toml, NO Dockerfile and NO Pkl."
rationale = "The defect class this module keeps re-fixing (#299, #392, #394, #397: a comment, a docstring or a prefix-rename satisfying a contract about wiring) is a direct consequence of substring binding, and ast-grep removes it *by construction* for the file types it parses — which is the largest share of the manifest (164 python, 30 json, 35 yml, 39 md, 10 hcl declared paths). Refactor, not retire, on two measured limits: ast-grep parses none of toml (56 paths), Pkl (13) or Dockerfile (32), so the substring engine must stay for those; and the `not/has` form reports one diagnostic per missing file rather than naming which token is absent, so per-token failure messages would regress unless one rule is authored per token. Recommend adopting it for the python-target `require_tokens` suites first (where a call site, not a name, is what the contract means) and leaving the rest on the existing engine."
probe = "mkdir -p /tmp/sg/rules && printf 'ruleDirs:\\n  - rules\\n' > /tmp/sg/sgconfig.yml && printf 'id: t\\nlanguage: python\\nseverity: error\\nmessage: missing call\\nrule:\\n  kind: module\\n  not:\\n    has:\\n      pattern: schema_vendor.check_drift($$$)\\n      stopBy: end\\n' > /tmp/sg/rules/t.yml && mise exec -- ast-grep scan -c /tmp/sg/sgconfig.yml python/src/dotfiles_setup/verify.py; echo A=$?; mise exec -- ast-grep scan -c /tmp/sg/sgconfig.yml python/src/dotfiles_setup/lint.py; echo B=$?"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-verify-4"
unit = "mod-verify"
kind = "module"
disposition = "keep"
confidence = 0.80
custom_thing = "The manifest-driven contract engine itself: TOML suite loading, handler dispatch with a name-derived fallback, the default-strict `paths_required` precondition (a declared path that no longer exists FAILS before dispatch), name/category filtering, `--json` output, list mode, and the pass/fail/skip tally driving the exit code."
evidence = "python/src/dotfiles_setup/verify.py:23-35 (`load_manifest`), :38-41 (`_missing_paths`), :44-102 (`run_suite`, strictness at :82-91), :117-127 (`_resolve_paths`, which silently drops missing paths — the reason the precondition lives above it), :674-746 (`main`: filters, list mode, JSON, tally, rc). Wiring: mise.toml:364-373 (`[tasks.verify]` -> `uv run --project python dotfiles-setup verify run`), python/src/dotfiles_setup/main.py:895-918 + :2047-2058 (subparser + `handle_verify`), tests/test_verify.py:53-80 (both arms of the strict default), .claude/rules/verify-before-advancing.md:26 (the gate contract)."
native_alternative = "None found. The four closest were each probed and ruled out: (a) hk `Step` — a step whose `glob` matches no file simply does not run, which is the exact silent-pass failure `paths_required` exists to prevent; (b) ast-grep — 29 languages, none of them TOML/Pkl/Dockerfile, and no cross-file suite/category/JSON-report layer; (c) `gitleaks` custom `[[rules]]` — declarative and allowlist-capable but forbid-only and secret-shaped; (d) `mdschema` — markdown-only."
alternative_evidence = "hk Step glob semantics: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:327-350 (`glob`: 'By default this will only run this step if at least 1 staged file matches … If no patterns are provided, the step will always run'). ast-grep languages: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/lib.rs:259-286. gitleaks custom rules: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gitleaks/README.md:302-345 (`[[rules]]` with `regex`, `path`, `entropy`, `keywords`, `[[rules.allowlists]]`) — a matcher that reports findings, with no 'must be present' form. mdschema deferral already recorded in-repo at hk.pkl:361-363. Version currency: .agent/kb/raw/hk-releases-since-pin.digest.md ('No new capabilities'; v1.58.1 is docs-only) and .agent/kb/raw/mise-releases-since-pin.digest.md ('ZERO releases strictly newer than the baseline')."
rationale = "Required justification: what this engine asserts is cross-file WIRING — 'this hk step calls this CLI which reaches this module and this rule documents it' — and no linter in the toolchain has that shape. Every native candidate is a matcher that reports what it FINDS; this engine's dominant mode (111 of 152 suites) is asserting what must be THERE, plus a precondition that the named files still exist at all. hk's own glob semantics make it strictly worse for that: a renamed target silently removes the step. The gate is also deliberately distinct from hk (python/AGENTS.md:51-56, `.claude/rules/verify-before-advancing.md:26`), runs whole-repo in CI `contract-preflight` rather than on staged files, and its JSON/tally output is consumed as a gate result. Keep the engine; the improvements worth making are the three findings above (schema-ise the eval corpus, delete the dead handlers, move python-target token binding to a syntactic matcher), not a replacement."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-workflow_hooks-1"
unit = "mod-workflow_hooks"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "The module's entire reason for existing: a 970-line per-job policy check (CLI `dotfiles-setup workflow-hooks`, hk step `workflow_hk_skip_hooks`, suite `workflow.adr-0001-wiring`) asserting that every CI job which commits or pushes carries `HK_SKIP_HOOKS: pre-commit,pre-push` in its job env — because, per the ADR quoted in the module header, \"There is no global guard — the two known cases are fixed by name.\""
evidence = "python/src/dotfiles_setup/workflow_hooks.py:19-23 (the ADR's pre-amendment 'There is no global guard' text this module was written to close); python/src/dotfiles_setup/workflow_hooks.py:61-70 (SKIP_ENV/REQUIRED_SKIPS, with the comment 'hk has no CI self-detection (grepped every release v0.7.0->v1.51.0: zero hits)' — the pin is now 1.57.0); python/src/dotfiles_setup/workflow_hooks.py:910-951 (skips_required_hooks / find_violations); hk.pkl:403-414 (the step that shells out to it); mise.toml:128 (`postinstall = \"mise reshim && hk install --mise\"`, the install that puts hooks on the runner)"
native_alternative = "hk 1.57.0's PROJECT config can itself declare the skip, conditioned on the runner environment, giving exactly the global guard the ADR said did not exist: in `hk.pkl`, `skip_hooks = if (read?(\"env:CI\") != null) List(\"pre-commit\",\"pre-push\") else List()`. `skip_hooks` is a first-class Config.pkl setting at precedence tier 5 (project config), all skip sources are unioned, and pkl's `env:` resource reader is on by default."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:175 (`skip_hooks: List<String> = List()`, doc at :169); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:453 (precedence row 5 = 'Project config (hk.pkl)') and :445 (skip_hooks unions across sources); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/hook.rs:1124-1126 (`if settings.skip_hooks.contains(&self.name)` -> hook skipped); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/pkl/docs/modules/language-reference/pages/index.adoc:3098,3103,3131 (`read(\"env:PATH\")`, `env: :: Reads an environment variable`, `read?(\"env:PORT\")`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/pkl/docs/modules/pkl-cli/partials/cli-common-options.adoc:16 (`env:` is in the DEFAULT allowed-resources); LIVE control-armed probe on the pinned 1.57.0 binary saved at .agent/kb/raw/workflow-policy-linters-probe.md (env set + HK_CACHE=0 -> [\"pre-commit\",\"pre-push\"]; env unset + HK_CACHE=0 -> []; literal-List control -> [\"pre-commit\",\"pre-push\"])"
rationale = "The module exists solely because hk offered no repo-wide way to say 'never run these hooks on a runner'. It does: the project hk.pkl is a settings source (tier 5) that can set skip_hooks, and hk's pklr evaluator honours pkl's env: resource reader — both verified live on hk 1.57.0, with a negative arm and a literal control. Adopting that one line moves the guarantee from 'every future job author remembers an env line, policed by 970 lines of derivation' to 'the config says no'. It is NOT a clean retire, for one measured reason: hk's config cache is keyed on file content, not environment, so with the cache at its default the env-set arm returned the STALE `[]` (arm B in the probe table). A fresh GHA runner has a cold cache, but any CI step that restores ~/.cache/hk, and any devcontainer that switches CI state in place, would silently serve the wrong verdict — which is the same class of silent, green failure this module was built to prevent. So: adopt the native global skip as the primary layer, then shrink this module to (at most) the classification-gap half, rather than keep it as the sole guard. Auxiliary fact not citable from the offline corpus: GitHub-hosted runners export CI=true — hk itself relies on that ecosystem fact via the is_ci crate (sources/hk/Cargo.toml:45, sources/hk/src/cli/mod.rs:188), and GITHUB_ACTIONS is the narrower key if CI is judged too broad."
probe = "cd $(mktemp -d) && git init -q && printf 'amends \"package://github.com/jdx/hk/releases/download/v1.57.0/hk@1.57.0#/Config.pkl\"\\nskip_hooks = if (read?(\"env:CI\") != null) List(\"pre-commit\",\"pre-push\") else List()\\n' > hk.pkl && CI=true $(mise which hk) config get skip_hooks && env -u CI $(mise which hk) config get skip_hooks   # then the load-bearing arm: run the SAME two invocations back-to-back WITHOUT HK_CACHE=0 to measure whether the content-hashed config cache serves a stale value across an env flip, and re-run inside a GHA job (or `act`) with a warm ~/.cache/hk restored, to decide whether the native guard can stand alone."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "mod-workflow_hooks-2"
unit = "mod-workflow_hooks"
kind = "module"
disposition = "refactor"
confidence = 0.60
custom_thing = "A hand-rolled mise task-table reader and transitive-dependency solver: `MISE_CONFIGS` globbing `mise.toml` + `.config/mise/conf.d/*.toml`, `_task_entry` decoding both the inline-string and table task shapes and the string-or-list forms of `run`/`depends`/`depends_post`/`wait_for`, `_task_bodies` parsing them with tomllib, and `mise_task_git_writers` iterating a fixed point over that graph to find tasks that transitively reach a git write."
evidence = "python/src/dotfiles_setup/workflow_hooks.py:405-407 (MISE_CONFIGS), :647-696 (_task_entry/_task_bodies), :699-731 (mise_task_git_writers fixed-point loop)"
native_alternative = "`mise tasks ls --json` already emits every RESOLVED task with `run`, `depends`, `depends_post`, `wait_for`, `source` and `config_sources` — the exact fields this code reconstructs — and `mise tasks deps [--dot]` renders the declared dependency graph mise itself will execute. `mise tasks info <task> --json` gives the same per task."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/tasks/ls.md:18 (`-J --json`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/tasks/info.md:26-45 (JSON shape: name, source, config_sources, depends, env, run); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/tasks/deps.md:9-14 (graph built from depends/depends_post/wait_for, and the documented caveat that `{ task = ... }` refs inside a `run` array are execution steps, not graph edges); host probe saved at .agent/kb/raw/workflow-policy-linters-probe.md (87 repo tasks + 5 user-global tasks, each with run/depends)"
rationale = "This is the shape use-tool-builtins.md exists to catch: a second, weaker implementation of mise's own task resolution. The custom reader is authoritative only for the two config globs it names, so a task that arrives any other way mise supports — a file task under a tasks dir, a monorepo/global config, an alias — is invisible, and route 3 goes silently blind exactly where the module's own docstring says it must not ('a NEW task that wraps the same entrypoint was a silent false negative'). Not a straight retire, and the reason is measured, not hypothetical: `mise tasks ls --json` on this host returned 5 tasks sourced from ~/.config/mise/config.toml alongside the 87 repo ones, so a gate consuming it must filter on `source` under the repo root or become host-dependent (and un-reproducible in CI). The refactor is therefore: parse `mise tasks ls --json`, filter by source prefix, keep the fixed-point union over the filtered graph (mise deps gives the edges but not the 'this body invokes a git write' predicate)."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && mise tasks ls --json | python3 -c \"import json,sys;d=json.load(sys.stdin);print(len(d), sorted({t['source'] for t in d}))\" && mise tasks deps --dot | head -20   # confirm every task the tomllib reader finds is present with identical run/depends, and that filtering by source under the repo root reproduces exactly the current mise_task_git_writers() output"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-workflow_hooks-3"
unit = "mod-workflow_hooks"
kind = "module"
disposition = "refactor"
confidence = 0.50
custom_thing = "`_ARGV_GIT_WRITE`, a regex over the raw text of every `python/src/dotfiles_setup/*.py` file looking for a `[\"git\", <write-verb>]` argv literal, with a hand-tuned `[^]]{0,200}?` bound to keep the match inside one list literal — plus the workaround it forces: the module must PARAPHRASE the argv literal in its own comments so it does not match itself."
evidence = "python/src/dotfiles_setup/workflow_hooks.py:424-437 (the regex and its `[^]]{0,200}` bound), :896-907 (_python_git_write_modules scanning source text), :409-420 (the comment explaining the self-match workaround)"
native_alternative = "ast-grep (already pinned in this repo at 0.45.3) does this structurally: Python is a first-class language, so a rule matching a list literal whose first element is `git` and whose members include a write verb has no character bound, no self-match hazard from comments or docstrings, and no false hit across statement boundaries."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/lib.rs:279 and :396 and :519 (Python is a supported language, extensions py/py3/pyi); mise.toml:47 (`\"github:ast-grep/ast-grep\" = \"0.45.3\"` — already installed, no new dependency)"
rationale = "A regex over source text is the homegrown parser use-tool-builtins.md rejects, and this one carries two visible scars: an arbitrary 200-character window standing in for 'the same list literal' (a bounded probe — probes-need-a-control-arm.md rule 3), and a documented need to write about its own subject obliquely so the detector does not fire on its own prose. ast-grep is already a repo pin, so the cost is a rule file, not a dependency. Held below 0.6 rather than higher because the current regex is control-armed by a real test (the derivation once correctly flagged workflow_hooks itself) and because ast-grep would add a subprocess to an hk step that is currently pure-python and hermetic — so the win is precision, not capability."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && mise exec -- ast-grep --lang python --pattern '[\"git\", $$$ARGS]' python/src/dotfiles_setup/ -l | sort -u   # must return exactly {pr.py, session_gate.py} (GIT_WRITING_MODULES), and must NOT return workflow_hooks.py despite its comments discussing the same literal"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-workflow_hooks-4"
unit = "mod-workflow_hooks"
kind = "module"
disposition = "keep"
confidence = 0.70
custom_thing = "The policy engine proper: `parse_jobs` flattening workflow YAML into jobs, `_expand_local` recursively inlining local composite actions (./ and $/ prefixes) with cycle protection and an opaque-action ledger, the `ACTION_RUNS_GIT_LOCALLY` verdict map over third-party actions, and `classification_gaps` failing on unclassified / stale / opaque / unregistered entries so the map cannot rot."
evidence = "python/src/dotfiles_setup/workflow_hooks.py:133-259 (_expand_local + parse_jobs), :327-361 (the action verdict map, incl. the autofix-ci/action REMOTE trap), :734-783 (action_id / committing_actions / unclassified_actions), :831-893 (classification_gaps)"
native_alternative = "None found. The three workflow linters in this repo's pins are fixed-audit tools with no user-defined policy surface: zizmor 1.30.0 exposes only ignores/persona/severity through its config (there is no `--list-audits`, and `-c` loads ignore/severity config), ghalint 1.5.6 ships a fixed policy set with a config that only excludes, and actionlint checks syntax/expressions/shell rather than cross-job semantic policy. None of them can express 'this job reaches a local git write two composite hops down, therefore it must carry this env var'."
alternative_evidence = ".agent/kb/raw/workflow-policy-linters-probe.md (live `zizmor --version` 1.30.0, `zizmor --list-audits` -> 'unexpected argument', full `zizmor --help` showing only --config/--no-config/--persona/--min-severity/--no-ignores as the configurable surface; `ghalint --help` 1.5.6 showing only run / run-action / experiment / version with a `-c` config path)"
rationale = "Required justification: the check is a REPO-SPECIFIC semantic policy over a transitive graph — 'does this job reach a git binary in the runner's checkout', answered across composite-action inlining and a per-vendor behavioural verdict (peter-evans/create-pull-request writes locally; autofix-ci/action does not, despite every surface signal saying it does). No available linter accepts a user-authored rule of that shape; zizmor and ghalint are closed audit sets, and their configs subtract findings rather than add rules. The classification-gaps half additionally has no analogue anywhere — it is this repo's own allowlist-plus-staleness contract, copied deliberately from bash_budget.py. Keeping it is also what survives finding -1: if the native hk global skip is adopted, this is the part still worth having, because it is the part that answers 'which jobs write to git', not 'did someone remember an env line'."
probe = "mise exec -- zizmor --help 2>&1 | grep -i 'custom\\|rule\\|policy'   # control arm: the same grep against `hk --help` (which DOES have user-authored steps) must return hits, proving the grep discriminates"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "mod-workflow_hooks-5"
unit = "mod-workflow_hooks"
kind = "module"
disposition = "keep"
confidence = 0.55
custom_thing = "`shell_tokens` — a shlex-based tokenizer for workflow `run:` blocks configured for command position (posix quoting, punctuation_chars, newline as a separator), plus `_command_starts` / `_git_subcommand` / `_invocation_tokens` / `_strip_runner_prefix`, and the shared `redact_heredoc_bodies` pre-pass that blanks heredoc payloads."
evidence = "python/src/dotfiles_setup/workflow_hooks.py:473-511 (shell_tokens over stdlib shlex), :514-595 (command-position scan and git subcommand parse), :598-621 (_strip_runner_prefix), :54 + :504 (shared heredoc redaction, factored out of hook_guard rather than copied)"
native_alternative = "The nearest candidate is ast-grep's `bash` language (tree-sitter-bash), which parses commands and heredoc bodies as real grammar nodes and so would subsume both the command-position logic and the heredoc pre-pass. It is a replacement for a stdlib parser, not an addition of a missing capability."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/bash.rs (bash is a supported ast-grep language) and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/lib.rs:295 (language list); mise.toml:47 (pinned 0.45.3)"
rationale = "Required justification: this half ALREADY satisfies use-tool-builtins.md — the lexer is stdlib shlex, not a homegrown scanner, and the one thing shlex cannot model (heredoc bodies as inert data) is factored into a single module shared with hook_guard rather than duplicated. Swapping to ast-grep bash would trade a pure-python, dependency-free, in-process parse for a subprocess in a pre-commit step, and would split the heredoc semantics that hook_guard and this module currently share by construction — the sharing is what stops the two guards drifting, and drift between them is a defect class this repo has already paid for (#265). Revisit only if the shlex fallback path (unbalanced quote -> whitespace split) is ever observed producing a wrong verdict, since that is the only place the stdlib parse is knowingly approximate."
probe = "uv run --project python pytest tests/test_workflow_hooks.py -x -q -k 'heredoc or quote or token'   # then re-run with redact_heredoc_bodies neutered to a passthrough: the heredoc arm must FAIL, proving the pre-pass is load-bearing and not decoration before deciding whether a bash grammar would buy anything"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-1"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "`.agent/plans/` is declared as the home for plans and session handoffs (`.claude/rules/agent-artifact-conventions.md:20`, rule 2 at `:65`: \"A handoff is a plan — `.agent/plans/session-{date}.md`\"). It is a prose-only convention: nothing binds Claude Code's own plan-mode writer to it, so native plan files land in `~/.claude/plans` while the rule's plans land in `.agent/plans/`, and the rule never reconciles the two."
evidence = ".claude/rules/agent-artifact-conventions.md:20 and :65; `jq -r 'keys[]' .claude/settings.json` returns $schema, disableClaudeAiConnectors, enabledPlugins, env, extraKnownMarketplaces, fallbackModel, hooks, permissions, prefersReducedMotion, switchModelsOnFlag, teammateMode — no `plansDirectory`; `grep -rn plansDirectory .claude/ mise.toml python/` -> 0 hits (control arm: the same grep for `when_to_use` returns 4 hits in python/)."
native_alternative = "The `plansDirectory` setting redirects plan-mode plan files into the repo, resolved relative to the project root (default unset = `~/.claude/plans`). Setting it to `.agent/plans` makes the rule's convention the harness's actual behaviour instead of a habit, and the native plan file is additionally re-injected after every compaction and exposed to hooks as `planFilePath`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:2709-2719 (`### plansDirectory` … \"resolves the path relative to the project root\", Default: unset, so Claude Code uses `~/.claude/plans`; example `\"plansDirectory\": \"./plans\"`); settings-reference.md:703 (index row); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/claude-directory.md:1527 (`plans/` — Plan files written during plan mode); large-codebases.md:468 (\"Claude Code re-injects the plan file after each compaction\"); hooks.md:1733 (`planFilePath`); changelog.md:4782 (\"Added `plansDirectory` setting to customize where plan files are stored\")."
rationale = "The rule's directory choice is right; its enforcement mechanism (prose) is superseded by a settings key that makes the harness write there itself. Adopt `plansDirectory: \".agent/plans\"` in `.claude/settings.json` and have the rule cite it, so plan-mode output and hand-written handoffs stop living in two places. Note the key is ignored in `-p`/SDK runs (settings-reference.md:2707 context), so the prose rule still carries headless sessions — hence refactor, not retire."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-2"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "retire"
confidence = 0.80
custom_thing = "The table row `.agent/project-memory.json` | Cross-session project knowledge (`.claude/rules/agent-artifact-conventions.md:23`) — a hand-rolled per-project knowledge store for agents."
evidence = ".claude/rules/agent-artifact-conventions.md:23; `ls -la .agent/project-memory.json` -> ABSENT (control arm: the sibling rows `.agent/notepad.md` (431828 B) and `.agent/command-audit.md` (49542 B) both exist in the same `ls` of `.agent/`). `grep -rn '^memory:' .claude/agents/*.md` -> 0 hits across 9 agent files, so no subagent here uses the native store either."
native_alternative = "Subagent persistent memory: the `memory:` frontmatter field on a `.claude/agents/*.md` subagent gives it a durable directory — `project` -> `.claude/agent-memory/<name>/` (version-controlled), `local` -> `.claude/agent-memory-local/<name>/` (gitignored), `user` -> `~/.claude/agent-memory/<name>/`. Claude Code injects the first 200 lines / 25KB of that directory's `MEMORY.md` into the subagent's system prompt and auto-enables Read/Write/Edit for it."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:563-590 (`#### Enable persistent memory`, the scope table at :582-584, and the injection/tooling bullets); sub-agents.md:302 (frontmatter field row); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/claude-directory.md:1513 (`agent-memory/<name>/` — Persistent memory for subagents, Project and global, committable)."
rationale = "The row is a dead pointer (the file does not exist and nothing in the repo writes it), and the capability it names — cross-session knowledge an agent accumulates — is now a first-class harness feature with a documented scope table, automatic prompt injection and its own curation limit. Delete the `.agent/project-memory.json` row and, where a subagent genuinely needs durable knowledge, set `memory: project` on that agent instead of inventing a JSON file. `project` is the doc's own recommended default (sub-agents.md:594-596)."
probe = ""
correction = "**⚠ CITATION CORRECTED (round 2, re-verified round 5):** the `agent-memory/<name>/` table row is at `claude-directory.md:1503`, not `:1513` (`:1513` is the `## Application data` heading). Re-probed this round with `sed -n '1503p;1513p'`: `:1503` is the `| [`agent-memory/<name>/`](#ce-agent-memory) | Project and global | ✓ | Persistent memory for subagents |` row and `:1513` is `## Application data`. The row's other anchors (`sub-agents.md:563`, `:582-584`, `:302`) verified exact, so the claim stands."
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-3"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "Rule 5: \"**Learned skills go in `.claude/skills/<name>/SKILL.md`**, never under `.agent/` — Claude Code's loader does not scan anywhere else. Frontmatter `name` should match the directory name so slash-invocation and auto-loading stay consistent.\" (`.claude/rules/agent-artifact-conventions.md:70-73`)."
evidence = ".claude/rules/agent-artifact-conventions.md:70-73."
native_alternative = "Both halves are stale at 2.1.266. (a) The loader scans several more places: `~/.claude/skills/`, plugin `skills/`, `~/.claude/skills/synced/`, the enterprise managed-settings `.claude/skills/`, every parent directory up to the repo root, `.claude/skills/` inside each `--add-dir`/`/add-dir`/SDK `additionalDirectories` directory, and **nested `.claude/skills/` below the working directory**, which load lazily the first time Claude reads a file in that subdirectory and are then addressed as `/<subdir>:<skill>`. (b) For a personal or project skill the command name comes from the **directory name**, not the frontmatter `name`; the frontmatter `name` replaces the last segment only for *plugin* skills."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:118-120 (personal/project/plugin table), :135 and :165-167 (nested `.claude/skills/` load lazily below the working directory), :175 (`--add-dir` / `/add-dir` / SDK `additionalDirectories` load `.claude/skills/`), :147 and :234 (`~/.claude/skills/synced/`), :266 (enterprise managed-settings skills dir), :384-385 (name comes from the *directory* name; nested clash -> `apps/web:deploy`), :387 and :390 (frontmatter `name` replaces the directory name only in a plugin skill)."
rationale = "The directive (\"put skills in `.claude/skills/`, never in `.agent/`\") stays correct, but the two supporting facts are wrong at this host version and one of them is actively misleading: an agent that trusts \"the loader does not scan anywhere else\" will not consider a package-local `apps/x/.claude/skills/` (which this repo's own skill list already shows as a supported shape via directory-scoped names), and one that trusts \"frontmatter `name` drives invocation\" will chase a rename that has no effect on a project skill. Restate as: directory name is the command for project/personal skills; frontmatter `name` matters for plugin skills; nested and `--add-dir` skill dirs also load."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-4"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "Rule 6: \"a `description` over 1,536 chars is silently truncated, taking the keywords Claude matches on with it\" (`.claude/rules/agent-artifact-conventions.md:89-91`) — stated as a hard, unconditional per-field cliff on `description` alone."
evidence = ".claude/rules/agent-artifact-conventions.md:89-91."
native_alternative = "At 2.1.266 the cap is (i) on `description` **plus `when_to_use` combined**, (ii) applied to the *skill listing*, and (iii) **configurable** — `skillListingMaxDescChars` changes the 1,536 figure, `skillListingBudgetFraction` / `SLASH_COMMAND_TOOL_CHAR_BUDGET` change the whole-listing budget, and `skillOverrides: \"name-only\"` drops a low-priority skill's description instead. `/skill-doctor` (2.1.261) reports which loaded skills go unused and what they cost in context."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:337 (\"the combined `description` and `when_to_use` text is truncated at 1,536 characters in the skill listing\"), :338 (`when_to_use` \"counts toward the 1,536-character cap\"), :1058 (`skillListingBudgetFraction`, `SLASH_COMMAND_TOOL_CHAR_BUDGET`, `skillOverrides` name-only, \"The cap is configurable with `skillListingMaxDescChars`\"); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (capability `/skill-doctor`, version 2.1.261)."
rationale = "The repo's own enforcement already models this correctly — `python/src/dotfiles_setup/listing_budget.py:77` and `:174` measure `description` + `when_to_use` combined — so the eager rule is the only place still asserting the narrower, harder claim, and it is the copy an agent reads before writing a skill. Update the sentence to name the combined field and the `skillListingMaxDescChars` knob; keep the warning, since the truncation is still silent and still removes trigger text (`python/verification/suites.toml:2196` records a real 1,789-char agent description losing its negative example)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-5"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "The rule is the repo's authoritative \"where do I create this artifact\" map (`.claude/rules/agent-artifact-conventions.md:13-35`, rule 1 at `:63-64` \"No ad-hoc directories\"), but its `.claude/**` coverage stops at skills, rules and agents (`:70-73`)."
evidence = ".claude/rules/agent-artifact-conventions.md:13-35 (the two tables) and :70-73 (rule 5's `.claude/` enumeration: skills, rules, agents only)."
native_alternative = "The `.claude/` directory now has a documented, committable file inventory that the rule's map omits: `commands/*.md` (same mechanism as skills), `output-styles/*.md`, `workflows/*.js` (dynamic workflow scripts saved from `/workflows`, each becoming a `/<name>` command), `agent-memory/<name>/`, `agent-memory-local/<name>/`, `.worktreeinclude`, and `settings.local.json`. Large tool outputs also now spill to `~/.claude/projects/<project>/<session>/tool-results/` rather than to any repo path."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/claude-directory.md:1493-1516 (the full \"Configuration you author\" table: `rules/*.md`, `settings.local.json`, `.worktreeinclude`, `skills/<name>/SKILL.md`, `commands/*.md`, `output-styles/*.md`, `agents/*.md`, `workflows/*.js`, `agent-memory/<name>/`), :1525 (`projects/<project>/<session>/tool-results/` — large tool outputs spilled to separate files)."
rationale = "A creation-triggered map that omits half the native artifact classes pushes an agent toward inventing a location for anything it does not find listed — precisely the failure rule 1 exists to prevent. Extend rule 5's enumeration to the documented `.claude/` inventory (or link `claude-directory.md`'s table) rather than growing the rule's own tables. `workflows/*.js` matters most here: it is a native seam that competes with rule 6's skill -> mise task -> python library stack for deterministic multi-step sequences, and the rule currently does not acknowledge it exists."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-6"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "refactor"
confidence = 0.55
custom_thing = "Every artifact path the rule mandates for working state (`.agent/state/`, `.agent/notepad.md`, `.agent/plans/`, `.agent/logs/`, `.agent/kb/raw/`) is deliberately gitignored (`.claude/rules/agent-artifact-conventions.md:3-5, 13-24`; `.gitignore:41-50`), with no provision for worktree-isolated sessions."
evidence = ".claude/rules/agent-artifact-conventions.md:3-5 and :13-24; .gitignore:41-50 (`.agent/state/`, `.agent/notepad.md`, `.agent/command-audit.md`)."
native_alternative = "`.worktreeinclude` at the project root lists gitignored files to copy into every worktree Claude creates — the documented answer for exactly this class (`.env`, local secrets, and any untracked working state). It applies to worktrees Claude creates for `--worktree` sessions and for subagents declaring `isolation: worktree`; it is **not** processed when a `WorktreeCreate` hook replaces the default git behaviour."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/worktrees.md:179-189 (`### Copy gitignored files into worktrees` — \"untracked files … are not present. To copy them automatically when Claude creates a worktree, add a `.worktreeinclude` file to your project root\"), :271 (`.worktreeinclude` is not processed when a `WorktreeCreate` hook is configured); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:269-271 and :305 (`isolation: worktree` gives a subagent an isolated checkout; the worktree is cleaned up when the subagent makes no changes); claude-directory.md:1506 (`.worktreeinclude` — Gitignored files to copy into new worktrees)."
rationale = "Latent today — `grep -rn '^isolation:' .claude/agents/*.md` returns 0 across 9 agents, so nothing in this repo currently runs worktree-isolated. But the rule's whole contract is 'working artifacts are gitignored, durable ones are tracked', and under worktree isolation the gitignored half silently starts empty and is then discarded with the worktree, which is the same total-loss shape `agent-report-persistence.md` was written against. Add `.agent/notepad.md` / `.agent/plans/` to a `.worktreeinclude` (or state in the rule that worktree-isolated lanes must persist to a tracked `docs/` path) before the first agent sets `isolation: worktree`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-7"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "refactor"
confidence = 0.75
custom_thing = "Rule 1 \"No ad-hoc directories. Not `.agent/handoffs/`, `.agent/temp/`, `.agent/output/`. Map your artifact to the closest path above.\" plus the eight-row `.agent/` table (`.claude/rules/agent-artifact-conventions.md:13-24, 63-64`) — a hand-maintained map with no gate behind it."
evidence = "`ls -la .agent/` on this branch returns paths the table does not list: `instructions-loaded/` (19 entries), `telemetry/` (529 entries), `session-review.md` + ~40 `session-review.md.*.json` sidecars at the tree root, `session-review-*-20260813.json`, and `kb/audit/` + `kb/reports/` (the table lists only `.agent/kb/raw/`). In the other direction the listed `.agent/project-memory.json` is absent. Control arm: the listed `.agent/notepad.md`, `.agent/command-audit.md`, `.agent/logs/`, `.agent/plans/`, `.agent/state/` are all present in the same listing, so the probe sees the tree correctly."
native_alternative = "None — this is internal drift, not a superseded mechanism. The nearest harness levers are `.gitignore` (already used) and the `InstructionsLoaded` hook event, which is presumably what writes the undeclared `.agent/instructions-loaded/`; neither validates the rule's map. Enforcement would have to be this repo's own (an hk step / `suites.toml` contract asserting `ls .agent/` ⊆ the rule's table), which is the shape `zero-bash-logic.md`'s `bash_logic_budget` allowlist already uses for `scripts/*.sh`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/changelog.md:4130 (\"Added `InstructionsLoaded` hook event that fires when CLAUDE.md or `.claude/rules/*.md` files are loaded into context\") — the plausible writer of the undeclared `.agent/instructions-loaded/`; .claude/rules/zero-bash-logic.md (the allowlist-plus-budget pattern that gives an equivalent prose policy machine teeth)."
rationale = "Six undeclared top-level artifact locations and one dead row mean the map has already lost to reality, and rule 1 is the one clause in this file that is purely prescriptive — an agent obeying it today cannot tell which of `.agent/telemetry/`, `.agent/kb/audit/`, `.agent/session-review.md.*.json` are sanctioned. Either reconcile the table with the tree (adding rows for telemetry, instructions-loaded, session-review, kb/audit, kb/reports; deleting project-memory.json) or, better, give it the allowlist gate the sibling zero-bash-logic rule uses, so the next divergence is a failing check rather than silent rot."
probe = "ls -1 /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/ | sort > /tmp/actual.txt; grep -o '`\\.agent/[^`]*`' /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.claude/rules/agent-artifact-conventions.md | sort -u; diff <(cat /tmp/actual.txt) -"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-8"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "unknown"
confidence = 0.50
custom_thing = "The rule bans `.agent/temp/` as an ad-hoc directory (`.claude/rules/agent-artifact-conventions.md:63-64`) but names no home for genuinely transient scratch files, so the mapping for 'a temp file I will delete' is unspecified."
evidence = ".claude/rules/agent-artifact-conventions.md:63-64; neither `.agent/` table row covers throwaway scratch (the closest, `.agent/logs/`, is 'Execution logs, pipeline traces')."
native_alternative = "The harness now provisions a per-session scratchpad directory and instructs agents to use it for temporary files instead of `/tmp` or project paths — in this session `/private/tmp/claude-501/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/<session-uuid>/scratchpad`. If confirmed as a stable documented feature, the rule should point at it rather than leaving scratch unmapped."
alternative_evidence = "NOT YET CITABLE — `grep -rn -i scratchpad` over the offline corpus `/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/*.md` returns 0, and over every gap digest in `.agent/kb/raw/*.md` returns 0. Control arm: `grep -c -i plugin .agent/kb/raw/claude-code-llms-index.md` returns 20, so the corpus and the fetch are both readable. The only evidence is this session's own system prompt at 2.1.266, which is not a doc anchor."
rationale = "The feature is observably live at the host version (it is injected into this subagent's environment block) but is documented nowhere the audit is allowed to cite, so it fails the citation gate. Do not amend the rule on the strength of a prompt string: settle it against the published docs first, then either add a 'transient scratch -> harness scratchpad' row or record explicitly that scratch belongs outside the repo."
probe = "curl -sS https://code.claude.com/docs/en/tools-reference.md | grep -n -i 'scratchpad' ; curl -sS https://code.claude.com/docs/en/llms.txt | grep -n -i 'scratch' ; # control arm for the same fetch shape: curl -sS https://code.claude.com/docs/en/tools-reference.md | grep -c -i 'Bash tool'  — save any 200 response under .agent/kb/raw/ before citing it"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-artifact-conventions-9"
unit = "rule-agent-artifact-conventions"
kind = "rule"
disposition = "keep"
confidence = 0.90
custom_thing = "The rule's two load-bearing structural claims: `.agent/` was chosen because it collides with nothing in Claude Code's own surface (`:7-11`), and the rule cannot be `paths:`-scoped because it is creation-triggered (`:99-103`)."
evidence = ".claude/rules/agent-artifact-conventions.md:7-11 (\"`.agent/` → 0 hits in Claude Code's docs, `CLAUDE.md` → 439\") and :99-103; the cited sibling `docs/rules-evidence/agent-artifact-conventions.md` exists (3746 B), so the archaeology pointer is live, and the `md-size-budgets.md` § trigger-test classification still names `agent-artifact-conventions` as creation-triggered."
native_alternative = "None. Re-armed at 2.1.266: `grep -rn '\\.agent/' <corpus>/*.md` (excluding `.agents/`, `agent-memory`, `agent-sdk`) returns 0 files; control arm `grep -rl 'CLAUDE.md' <corpus>/*.md` returns 72 files. Rules still load eagerly unless `paths:`-gated, and `paths:` still fires on *reading* a matching file, which a create-new-artifact decision never does — no native mechanism triggers on 'about to create a file at a path'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/glossary.md:251 (\"A rule can be path-scoped with YAML `paths:` frontmatter so it only loads when Claude reads a matching file\"); claude-directory.md:169 (\"Use `paths:` frontmatter with globs to scope rules\"; subdirectories are discovery, not deferral); agent-sdk__claude-code-features.md:111 and :116 (project and user rule load locations, gated only by `settingSources`)."
rationale = "Required justification: both claims survive a re-probe with a control arm at the host version, and no native feature covers what this half of the rule does. Nothing in Claude Code reserves or writes `.agent/`, so the namespace choice is still safe; and the only native scoping lever (`paths:`) is read-triggered, so scoping this rule would remove it from exactly the sessions that are about to create an artifact. The eager cost is real but there is no mechanism to trade it against — the rule's own answer (moving archaeology to `docs/rules-evidence/`) remains the only available lever, and it has already been applied."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-report-persistence-1"
unit = "rule-agent-report-persistence"
kind = "rule"
disposition = "refactor"
confidence = 0.88
custom_thing = "Rule 1 makes verbatim persistence a MANUAL coordinator habit: 'When a findings-bearing agent's final report arrives, write it verbatim to docs/research/kb/reports/agents/<agent-name>.md in the SAME turn'. Nothing mechanical fires; the coordinator must remember, every time."
evidence = ".claude/rules/agent-report-persistence.md:22-25 ('Persist at receipt ... write it verbatim ... in the SAME turn'); .claude/settings.json declares hooks only for PreToolUse, PostToolUse, SessionStart, SessionEnd, InstructionsLoaded (python -c json dump of .hooks keys) — SubagentStop is ABSENT, so no layer under the prose."
native_alternative = "SubagentStop hook. It fires when a subagent finishes and receives `last_assistant_message` — 'the text content of the subagent's final response, so hooks can access it without parsing the transcript file' — plus `agent_id`, `agent_type` (the matcher value) and `agent_transcript_path`. A SubagentStop hook matched on the findings-bearing agent types can write the verbatim report to docs/research/kb/reports/agents/<agent_type>.md deterministically, exactly the way hook_guard/ask_quality already back clarify-before-acting and mise-tasks-only. The same block can be scoped per-agent instead of session-wide via the subagent frontmatter `hooks` field (a `Stop` hook there is converted to `SubagentStop`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2325 (SubagentStop input: last_assistant_message, agent_transcript_path, agent_type); same file:2319-2321 (event definition, matches on agent type); same file:664 ('Subagent hooks ... Claude Code converts a `Stop` hook here to `SubagentStop`'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:301 (`hooks` frontmatter field, 'Lifecycle hooks scoped to this subagent')."
rationale = "The DESTINATION (a tracked repo path that survives a clone) has no native equivalent, so the rule survives — but the TRIGGER does not need to be a remembered habit any more. This repo's own doctrine (mise-tasks-only.md 'Enforcement layers': markdown alone is 'relying on the LLM, never the only layer') says a prose-only requirement that a hook can carry should be carried by the hook. Refactor: keep rules 1-4 as the policy, add a SubagentStop hook that writes last_assistant_message to the rule's own path, and let the prose describe the exception cases (rule 4's mechanical agents) rather than the copying."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-report-persistence-2"
unit = "rule-agent-report-persistence"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "Rule 1b's remediation for 'A rule nothing pushes into the prompt is not a layer' is two prose habits: put the incremental-persistence line in the agent DEFINITION (.claude/agents/staleness-auditor.md), and 'add one line to any ad-hoc brief'. The second half covers exactly the delegations with no definition to carry it (general-purpose / Explore / an ad-hoc Agent call) and is unenforced — the rule itself records that the line went into none of four briefs on 2026-08-03."
evidence = ".claude/rules/agent-report-persistence.md:38-44; the definition-side half is real (grep -rln PLANNING_DISABLED .claude/agents/ -> 5 codex-* files; .claude/agents/staleness-auditor.md:38 '### 1. Persist findings incrementally, to disk, as you go'), the ad-hoc half has no mechanism."
native_alternative = "SubagentStart hook with `hookSpecificOutput.additionalContext` — 'String added to the subagent's context at the start of its conversation, before its first prompt'. It fires for EVERY subagent spawned via the Agent tool regardless of whether an agent definition exists, and supports matchers on agent type (`general-purpose`, `Explore`, `Plan`, custom names, plugin-scoped `^my-plugin:reviewer$`). That injects the incremental-persistence + file-role contract into ad-hoc briefs mechanically, which is the half no agent file can reach."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2304-2313 (SubagentStart additionalContext, with JSON example); same file:2283-2287 (fires on Agent-tool spawn, matcher = agent type name, plugin-scoped anchoring); same file:314 (matcher values table); same file:1024 (SessionStart/SubagentStart output contract: 'Context only ... adds context for Claude')."
rationale = "The rule already diagnoses its own gap correctly ('a rule nothing pushes into the prompt is not a layer') and then prescribes the weakest available fix for the ad-hoc case. SubagentStart additionalContext IS the thing that pushes it into the prompt, for every delegation including the ones with no definition file. Refactor rule 1b to name the hook as the layer and keep the agent-definition text as the per-agent specialization."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-report-persistence-3"
unit = "rule-agent-report-persistence"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "Rule 1b asserts as a harness fact that an undelivered report is a TOTAL LOSS: 'Pair it with deliver before idle: an agent that *finished* and went idle without sending its report was a total loss in the same run', and the framing rule ('existed only in the session's context window — one /clear from being lost') rests on the same premise."
evidence = ".claude/rules/agent-report-persistence.md:43-44 ('a total loss in the same run'); .claude/rules/agent-report-persistence.md:11-13 ('existed **only** in the session's context window')."
native_alternative = "Claude Code persists every subagent's own transcript to disk independently of delivery: SubagentStop carries `agent_transcript_path` = 'the subagent's own transcript stored in a nested `subagents/` folder', and sub-agents.md states 'Session persistence: subagent transcripts persist within their session. You can resume a subagent after restarting Claude Code by resuming the same session.' Live probe on this host: 409 *.jsonl files under ~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/*/subagents/ (control arm: the same find with a known-absent dirname `qzzxwv` returned 0, so the probe discriminates). An agent that finished and never sent is therefore recoverable, not lost."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2325 (agent_transcript_path, nested subagents/ folder) and :2336-2343 (JSON example showing .../abc123/subagents/agent-def456.jsonl); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:1056 ('subagent transcripts persist within their session'); sub-agents.md:298 (maxTurns: output 'marked as partial' and resumable, v2.1.246+)."
rationale = "The stated fact is now wrong in its strong form and it is load-bearing — it is what makes 'deliver before idle' feel like a last line of defence. What survives is narrower and should be said instead: the native transcript is MACHINE-LOCAL and UNTRACKED (~/.claude/projects/**), swept on the cleanupPeriodDays retention schedule, and invisible to a fresh clone — so it is a recovery path, not the artifact the rule requires. Rewrite 1b's total-loss claim as 'recoverable from agent_transcript_path, but not tracked', which also matches the standing memory hook 'on an idle notification read the report path on disk yourself'."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-agent-report-persistence-4"
unit = "rule-agent-report-persistence"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "The 'Applies to' section scopes the rule to 'All Agent-tool delegations in this repo', which omits agent-team TEAMMATES — a delegation surface this very session uses (sibling roster: main, cold-probe-v3, cold-probe-v3-r2, cold-s1-aslr). Teammates are not spawned by the Agent tool, so neither the rule's scope statement nor a SubagentStop-based layer reaches them."
evidence = ".claude/rules/agent-report-persistence.md:79-83 ('All Agent-tool delegations in this repo — research sweeps, adversarial verification passes, code-review agents, audit agents — regardless of which skill or workflow launched them'); no mention of teammates or agent teams anywhere in the file (grep 'teammate|team' -> 0 hits)."
native_alternative = "TeammateIdle hook: 'Runs when an agent team teammate is about to go idle after finishing its turn. Use this to enforce quality gates before a teammate stops working, such as requiring passing lint checks or **verifying that output files exist**.' It receives `teammate_name`, fires on every occurrence (no matcher needed), and CAN BLOCK — exit code 2 'Prevents the teammate from going idle, so it continues working', with stderr fed back as the next instruction. That is a literal machine implementation of 'deliver before idle' for the teammate surface."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2588-2592 (TeammateIdle definition and stated use case 'verifying that output files exist'); same file:860 (blocking table: 'Prevents the teammate from going idle, so it continues working'); same file:1014 (decision-control contract); same file:55 (event list); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-teams.md:215."
rationale = "The rule's scope predates the teammate surface and now under-covers it, while the harness ships the strongest enforcement point in the whole file for exactly this case — a hook that can refuse the idle until the report file exists. Widen 'Applies to' to name teammates, and wire TeammateIdle as the gate for them (SubagentStop for Agent-tool delegates, per finding 1)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-agent-report-persistence-5"
unit = "rule-agent-report-persistence"
kind = "rule"
disposition = "refactor"
confidence = 0.72
custom_thing = "Rule 5 delegates the coverage audit to a prose step inside a skill: '/session-handoff enumerates the session's agent launches and requires each findings-bearing one to map both its brief and its report to an on-disk artifact'. The enumeration is done by the model at handoff time, from memory/transcript, with no authoritative roster."
evidence = ".claude/rules/agent-report-persistence.md:74-77; .claude/skills/session-handoff/SKILL.md:178 ('At session-handoff, audit coverage: enumerate every agent launched') — a prose instruction, no roster source named."
native_alternative = "The harness now emits an authoritative roster for free, two ways: (a) the per-session `subagents/` directory is a complete on-disk list of every subagent that ran (one <agent_id>.jsonl per launch, probed live above); (b) a SubagentStop hook receives `agent_id` + `agent_type` on every completion and can append the launch to a session ledger as it happens, instead of the model reconstructing the list at the end. Anthropic's own guidance points the same way for post-return parent-side bookkeeping: 'To inject context into the parent session after a subagent returns, use a PostToolUse hook on the Agent tool instead.'"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2325 (agent_id/agent_type on every SubagentStop) and :2346 (PostToolUse on the Agent tool for parent-side post-return work); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:1056 (transcripts persist per session)."
rationale = "The audit's weakest link is the enumeration, not the mapping: a launch the model forgets is invisible to the check, so the gate can only pass. A hook-written ledger (or a read of the subagents/ directory) makes the denominator authoritative and turns rule 5 from a recollection into a diff. Keep the rule, change its input source."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-agent-report-persistence-6"
unit = "rule-agent-report-persistence"
kind = "rule"
disposition = "unknown"
confidence = 0.45
custom_thing = "The rule's founding premise — a large agent report 'existed only in the session's context window' — may itself be stale at 2.1.261+, where Claude Code saves oversized tool results to a file rather than keeping them inline. If the Agent tool's result is covered by taskOutputMaxChars, then every large subagent report is already on disk before any rule fires, which changes what rule 1 is protecting against."
evidence = ".claude/rules/agent-report-persistence.md:11-13 (the 2026-07-05 incident framing)."
native_alternative = "`taskOutputMaxChars` setting (2.1.261) raising 'how much command/background-task output Claude receives inline before it is saved to a file (up to 128K characters)', plus the 2.1.265 '1 GB cap on tool results saved to disk, with the in-conversation preview noting when a saved file was truncated'. Whether 'task output' here means the Agent tool's result or only background Bash tasks is NOT resolvable from the sources available: the term has 0 hits across the entire offline claude-code doc tree (control arm: `cleanupPeriodDays` in the same corpus with the same command shape -> 3 files), so only the changelog digest speaks to it."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (bullets: 'Added `bashOutputMaxChars` and `taskOutputMaxChars` settings', v2.1.261; 'Added a 1 GB cap on tool results saved to disk', v2.1.265)."
rationale = "Precision demands not asserting either way: the changelog wording is ambiguous between the Agent tool and background Bash tasks, and the offline corpus does not carry the setting at all. If it does cover Agent results, rule 1's rationale should be restated (the risk is loss of a TRACKED artifact, not loss from context); if it does not, the premise stands as written. A single live probe settles it."
probe = "grep -rn 'taskOutputMaxChars' /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/ ; curl -s https://code.claude.com/docs/en/settings-reference.md | grep -n -A4 'taskOutputMaxChars'   # then, empirically: launch a subagent whose final report exceeds the configured limit and check whether the Agent tool result in the parent transcript is replaced by a saved-file reference (ls the session dir under ~/.claude/projects/<slug>/) rather than inline text"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-agent-report-persistence-7"
unit = "rule-agent-report-persistence"
kind = "rule"
disposition = "keep"
confidence = 0.90
custom_thing = "The rule's core requirement itself: a findings-bearing report must end up VERBATIM at a TRACKED path (docs/research/kb/reports/agents/<agent-name>.md) that survives a fresh clone, with sources under .agent/kb/raw/<slug>.md, plus rules 2-4 (verbatim means verbatim; notepad is additive; mechanical agents exempt)."
evidence = ".claude/rules/agent-report-persistence.md:22-29 (the ONE path, tracked, survives a fresh clone), :63-73 (rules 2-4). The tracked destination exists and is in use: docs/research/kb/reports/agents/ holds real artifacts (2026-08-27-cold-review-lock-shared.md, 2026-08-29h-codex-implementer-832.md, ...)."
native_alternative = "None. The closest native persistence surfaces are all machine-local and untracked: subagent transcripts under ~/.claude/projects/**/subagents/ (swept by the cleanupPeriodDays retention sweep), and subagent `memory` scope, whose only version-controllable tier is `.claude/agent-memory/<name>/` — documented as the subagent's own cross-session LEARNINGS, not a verbatim third-party report the coordinator files, and keyed by agent name rather than by run."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:582-584 (memory scopes table: user / project `.claude/agent-memory/<name-of-agent>/` 'project-specific and shareable via version control' / local) and :302 (`memory` field: 'Persistent memory scope ... Enables cross-session learning'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-teams.md:251 (retention 'governed by the same cleanupPeriodDays you already control for session transcripts')."
rationale = "Required justification: no native feature writes a verbatim agent report into the repository's tracked tree, and that — not mere durability — is what this rule buys (an artifact a fresh clone, a reviewer, and a later session can all open; docs/rules-evidence and the audit trail depend on it). The native surfaces are either ephemeral and machine-local (transcripts, retention-swept) or semantically different (agent-memory = the agent's own learnings, name-keyed, not run-keyed). So the rule is not retired; findings 1-5 change HOW it is triggered and enforced, not WHETHER it exists."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ai-cli-invocation-1"
unit = "rule-ai-cli-invocation"
kind = "rule"
disposition = "refactor"
confidence = 0.98
custom_thing = "The rule's canonical 'Implementation (with tool execution)' pattern `echo \"prompt\" | codex exec --full-auto --sandbox workspace-write -` (.claude/rules/ai-cli-invocation.md:18). This is the one prescribed write-capable codex invocation in the whole repo's eager instruction corpus."
evidence = ".claude/rules/ai-cli-invocation.md:18 (`codex exec --full-auto --sandbox workspace-write -`); the same file's premise at :8-9 is that 'Incorrect flags waste tokens and cause silent failures'. .claude/agents/codex-operator.md:75-78 already carries the contradiction in-repo: '`--full-auto` is documented in `.claude/rules/ai-cli-invocation.md` and **does not exist** on codex 0.152.0'."
native_alternative = "codex-cli 0.152.1's own exec surface: `-s, --sandbox <SANDBOX_MODE>` with `workspace-write` (the write-capable mode `--full-auto` used to compose), plus `--approve-for-me` ('Route approval requests through automatic review using the workspace-write sandbox') and `--dangerously-bypass-approvals-and-sandbox`. `codex exec` is already non-interactive, so no separate auto-approval flag is needed for the write lane."
alternative_evidence = ".agent/kb/raw/ai-cli-invocation-live-probes.md (live probe, host, 2026-09-09): `codex exec --full-auto --help` -> rc=2 `error: unexpected argument '--full-auto' found`; CONTROL ARM `codex exec --ephemeral --help` -> rc=0 prints help. .agent/kb/raw/codex-exec-help-0.152.1.md lists `-s, --sandbox` (read-only|workspace-write|danger-full-access), `--approve-for-me`, and has 0 occurrences of `full-auto` (control arm: `ephemeral` = 2 occurrences in the same file)."
rationale = "This is a hard, reproduced break, not a style drift: the rule's write-lane invocation exits rc=2 on the pinned CLI. It is the exact failure class the rule exists to prevent, and it is the rule itself producing it. Refactor rather than retire because the guidance is still needed - replace line 18 with `codex exec --sandbox workspace-write -`, and carry over codex-operator.md:72's caveat that `--approve-for-me` is mutually exclusive with `--sandbox` and does not unlock git writes headless."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ai-cli-invocation-2"
unit = "rule-ai-cli-invocation"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "The 'WRONG patterns (will silently fail)' bullet `codex exec \"prompt\"` - positional arg without `-` stdin flag (.claude/rules/ai-cli-invocation.md:29), and the absolute at :32-33 'Always pipe prompts via stdin'."
evidence = ".claude/rules/ai-cli-invocation.md:29 and :32-33."
native_alternative = "codex-cli 0.152.1 documents the positional as a first-class supported form: `[PROMPT] Initial instructions for the agent. If not provided as an argument (or if `-` is used), instructions are read from stdin. If stdin is piped and a prompt is also provided, stdin is appended as a `<stdin>` block`."
alternative_evidence = ".agent/kb/raw/codex-exec-help-0.152.1.md, `Arguments:` block of `codex exec --help` (codex-cli 0.152.1); re-saved in .agent/kb/raw/ai-cli-invocation-live-probes.md."
rationale = "A positional prompt is now documented behaviour, not a silent failure, and stdin+positional now COMPOSE (stdin is appended as a `<stdin>` block) rather than conflict. Listing it under 'will silently fail' teaches a false fact and burns a turn when an agent sees a positional invocation work. The ARG_MAX rationale for preferring stdin survives - keep that as a preference, drop the 'WRONG / silently fail' framing. (The sibling bullets on the same lines are still TRUE and must stay: `-p` is `--profile` in 0.152.1, and `codex --full-context --help` -> rc=2 'unexpected argument', control arm `codex --version` -> 0.152.1.)"
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ai-cli-invocation-3"
unit = "rule-ai-cli-invocation"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "The Gemini 'WRONG patterns' bullet `gemini -p \"prompt\"` - `-p \"\"` triggers headless mode, prompt comes via stdin (.claude/rules/ai-cli-invocation.md:46), and the two prescribed invocations at :39 and :42 that pass an empty `-p \"\"`."
evidence = ".claude/rules/ai-cli-invocation.md:39, :42, :46."
native_alternative = "gemini 0.59.0's own `--help`: `-p, --prompt  Run in non-interactive (headless) mode with the given prompt. Appended to input on stdin (if any).` and the usage banner `Gemini CLI - Defaults to interactive mode. Use -p/--prompt for non-interactive (headless) mode.` So `-p \"prompt\"` IS the documented canonical headless form, and stdin composes with it rather than replacing it."
alternative_evidence = ".agent/kb/raw/ai-cli-invocation-live-probes.md - `gemini --version` = 0.59.0; `gemini --help` line 19 `-p, --prompt ... Appended to input on stdin (if any)`, line 3 usage banner, line 14 `query  Initial prompt. Runs in interactive mode by default`."
rationale = "The rule's second Gemini WRONG-pattern (`gemini \"prompt\"` hangs interactively) is CONFIRMED by the same help text (line 14: the positional `query` runs interactive by default), so the section is half right. But marking `-p \"prompt\"` wrong inverts the vendor's own documented form and pushes agents to the more fragile `-p \"\"` + stdin shape. Rewrite to: `-p <prompt>` is the headless switch, stdin is appended to it. Also unresolved on the same lines: `GEMINI_FORCE_FILE_STORAGE=true` (:42) has 0 hits in `gemini --help` - but --help does not enumerate env vars at all, so that is an unreachable bound, not an absence; see probe field."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ai-cli-invocation-4"
unit = "rule-ai-cli-invocation"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "The rule's Gemini lane is the standalone `gemini` CLI (.claude/rules/ai-cli-invocation.md:35-47), and it carries no invocation patterns at all for `agy` / `antigravity`."
evidence = ".claude/rules/ai-cli-invocation.md:35-47 (whole '## Gemini CLI' section). `which -a gemini` resolves to /Users/rmanaloto/.local/share/mise/installs/gemini/0.59.0/bin/gemini - a USER-GLOBAL mise install; `grep -n -E 'gemini' mise.toml .config/mise/conf.d/shared.toml` returns no pin for it (only the prose at mise.toml:84 describing antigravity-cli as 'Gemini 3.x')."
native_alternative = "This repo's declared Gemini lane is the Antigravity CLI, pinned as `antigravity-cli = \"1.1.24\"` (mise.toml:97), which 'ships `antigravity` + the `agy` symlink the antigravity plugin drives' (mise.toml:83-85), driven by the enabled `antigravity@antigravity-for-claude-code` plugin (0.26.0) whose skills (`antigravity:delegate`, `antigravity:research`, `antigravity:review`) are the sanctioned entry points."
alternative_evidence = "mise.toml:97 `antigravity-cli = \"1.1.24\"`; mise.toml:83-87 (the `agy` symlink + host-only justification); .claude/CLAUDE.md 'Adopted plugins' paragraph naming `antigravity@antigravity-for-claude-code` (Google Antigravity/Gemini 3.x via `agy`); host probe `command -v agy` -> present (.agent/kb/raw/ai-cli-invocation-live-probes.md)."
rationale = "The rule documents an unpinned, unreviewed CLI as the Gemini path while the repo's reviewed, pinned Gemini path (`agy` + the antigravity plugin skills) is absent from it. That inverts `use-tool-builtins.md`/`mise-tasks-only.md`: the sanctioned mechanism should be the one the eager rule names. Refactor to lead with the `antigravity:*` skills / `agy`, and keep the raw `gemini` block only as the unpinned-fallback footnote it actually is (label it as resolving from a user-global mise config outside this repo's review)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-ai-cli-invocation-5"
unit = "rule-ai-cli-invocation"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "The '## Background Mode Warning' section (.claude/rules/ai-cli-invocation.md:59-64): 'Claude Code's `run_in_background` does NOT reliably capture streaming stdout from Codex or Gemini' - an uncited harness-behaviour assertion, whose only remedy offered is `-o /tmp/result.md` or a foreground run."
evidence = ".claude/rules/ai-cli-invocation.md:59-64. The claim carries no doc anchor, no version, and no control arm anywhere in the file or in docs/rules-evidence/ai-cli-invocation.md's cited role."
native_alternative = "Two native mechanisms now cover the capture problem: (1) the unified `TaskOutput` tool - 'The previous name `BashOutput` is still accepted as an alias' - is the documented read path for background task output; (2) since 2.1.261 the `bashOutputMaxChars` and `taskOutputMaxChars` settings raise how much command/background-task output Claude receives inline before it is saved to a file (up to 128K characters), and since 2.1.265 tool results saved to disk are capped at 1 GB with the in-conversation preview noting when a saved file was truncated."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__python.md:3020 (TaskOutput / BashOutput alias); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:175 (background tasks, `/tasks`, foreground-subagent termination semantics); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md bullets for 2.1.261 (`bashOutputMaxChars`/`taskOutputMaxChars`, 128K) and 2.1.265 (1 GB disk cap + truncation notice). Control arm for the corpus grep: `PreToolUse` returns 5 files under the same doc tree while `bashOutputMaxChars` returns 0 - i.e. the setting is genuinely newer than the offline snapshot, which is why it is only in the gap digest."
rationale = "The section states a harness fact with no citation, at a host version 9 releases past what the offline corpus covers, and the harness has since shipped exactly the knobs the concern is about. The `-o` file-capture advice is still correct and is what this repo's own agent definitions do (.claude/agents/codex-advisor.md:87-89) - so refactor, not retire: keep `-o` as the recommended deterministic capture, add `codex exec --json` (JSONL events) as the machine-readable stdout form, name `TaskOutput` as the read path, and either cite or downgrade the blanket 'does NOT reliably capture' claim. Also add the real, cited background hazard the section omits: tools-reference.md:175 - a background command started by a FOREGROUND subagent is ended when that subagent gives its final response."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ai-cli-invocation-6"
unit = "rule-ai-cli-invocation"
kind = "rule"
disposition = "refactor"
confidence = 0.75
custom_thing = "The rule prescribes raw `codex exec` command lines as the reference (.claude/rules/ai-cli-invocation.md:13-25, and :68 'The patterns above ARE the reference'), with no pointer to this repo's canonical wrapper - and the same invocation is separately hand-copied into four agent definitions, one of which now contradicts the rule."
evidence = ".claude/rules/ai-cli-invocation.md:13-25, :66-73. Duplicated copies: .claude/agents/codex-operator.md:51-56, .claude/agents/codex-advisor.md:87-90, .claude/agents/codex-claude-code-expert.md:97-100, .claude/agents/codex-staleness-auditor.md:48-49. The contradiction: .claude/agents/codex-operator.md:75-78."
native_alternative = "In-repo canonical task `mise run codex-lane` (mise.toml:654-669), a thin caller over python/src/dotfiles_setup/codex_lane.py, which builds the invocation once - `exec`, `--ephemeral`, `--sandbox`, `--output-schema`, `-o` (codex_lane.py:382-386) - and is already the shape `mise-tasks-only.md` mandates ('New recurring workflow ships its logic as a python/ module + a mise task'). The codex-side native features it uses and the rule omits: `--output-schema <FILE>` (structured final response) and `--json` (JSONL events to stdout)."
alternative_evidence = "mise.toml:654-669 (`[tasks.codex-lane]`, 'run `codex exec` with BOTH --output-schema and -o'); python/src/dotfiles_setup/codex_lane.py:382-386 (the literal argv); .claude/rules/mise-tasks-only.md (canonical-task doctrine + the enforcement-layer list); .agent/kb/raw/codex-exec-help-0.152.1.md (`--output-schema`, `--json` in the 0.152.1 exec surface)."
rationale = "Five hand-maintained copies of one command line is exactly the drift substrate that produced finding 1 - the rule went stale while codex-operator.md was corrected, so the repo now asserts both that `--full-auto` is the write pattern and that it does not exist. The rule should stop being the argv reference and instead (a) point at `mise run codex-lane` / the agent definitions as the single source for the machine-run lanes, and (b) keep only the judgement content it alone carries: pipe via stdin for ARG_MAX, the known-wrong flag shapes, and 'flags drift between releases - re-probe `codex exec --help`'. Note `mise run codex-agent-parity` (mise.toml:1251-1255) already gates the agent copies but does NOT cover this rule, which is why this file is the one that rotted."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ai-cli-invocation-7"
unit = "rule-ai-cli-invocation"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "The '## OpenCode CLI' section (.claude/rules/ai-cli-invocation.md:49-57): two lines, `echo \"prompt\" | opencode run` and `opencode run -m provider/model`, with no WRONG-pattern list and no machine-capture form."
evidence = ".claude/rules/ai-cli-invocation.md:49-57."
native_alternative = "opencode 1.18.27's own `run --help`: `--format` with `[choices: \"default\", \"json\"]` (raw JSON events) is the deterministic capture form the other two sections get via `-o`/`-o text`; and `-p, --password  basic auth password (defaults to OPENCODE_SERVER_PASSWORD)` - so on this CLI `-p` is neither prompt nor profile, a third meaning for the same letter across the three CLIs the rule covers."
alternative_evidence = ".agent/kb/raw/ai-cli-invocation-live-probes.md (`opencode run --help`, host, 1.18.27): `--format [string] [choices: \"default\", \"json\"] [default: \"default\"]`; `-p, --password`; `-m, --model` (confirms the rule's surviving line); positional `message [array]`."
rationale = "Nothing in this section is wrong, so this is an omission rather than a break - but the omission is in the rule's own load-bearing axis. The file's entire premise is 'the same-looking flag means different things per CLI'; `-p` is `--profile` on codex, `--prompt` on gemini and `--password` on opencode, and only two of those three are recorded. Add the `-p` collision row and `--format json` as the capture form. Lower confidence than the others because no measured failure is attached to it yet."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ai-cli-invocation-8"
unit = "rule-ai-cli-invocation"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "The rule as a unit - an eager .claude/rules ADR that encodes correct invocation flags for third-party AI CLIs, plus the meta-instruction at :68-73 to re-probe `--help` rather than trust a written invocation."
evidence = ".claude/rules/ai-cli-invocation.md:1-9, :66-73."
native_alternative = "None. Every native Claude Code surface that could carry this is scoped to Claude Code's OWN tool calls, not to the argv of a third-party binary: permission rules match tool+command patterns and can deny a shape but cannot rewrite or teach one (permissions.md:133 `Bash(run_in_background:true)`); a PreToolUse hook can deny a Bash call but the repo's own doctrine records that a hook 'fails OPEN on its own errors' and is blind to `$(...)`/`sh -c`/aliases (.claude/rules/mise-tasks-only.md, 'Enforcement layers'); agent frontmatter fields and settings keys carry no CLI-flag semantics."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:133 (permission rules match tool/command shape only); .claude/rules/mise-tasks-only.md 'Enforcement layers (deep-research verified, 2026-07-07)' (hook deny is deterministic but fail-open and syntax-evadable). No page under the claude-code doc tree defines a mechanism that validates a third-party CLI's flags."
rationale = "Required justification: no native harness feature supersedes the UNIT, only individual facts inside it (findings 1-7). The harness can deny a command shape, never teach the correct one, and the correct one is set by three external release trains this repo does not control - which is precisely why the file rotted to rc=2 in finding 1. Keep the rule, but the durable fix is structural: make the content that CAN be machine-checked machine-checked (a flag-probe contract in python/verification/suites.toml or an extension of `mise run codex-agent-parity` to cover this file), and let the prose keep only the judgement half - stdin for ARG_MAX, the per-CLI `-p` collision, and 're-probe `--help`, this file included'."
probe = "uv run --project python dotfiles-setup verify run 2>&1 | grep -i 'ai-cli\\|codex' ; grep -rn 'ai-cli-invocation' python/verification/suites.toml hk.pkl"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ci-local-parity-1"
unit = "rule-ci-local-parity"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "Rule 2's prescribed verification: \"`mise which <tool>` should resolve under `~/.local/share/mise/installs/`\" as the check that a tool is declared in the REPO mise config rather than only in global `~/.config/mise/`."
evidence = ".claude/rules/ci-local-parity.md:33 (\"Verification: `mise which <tool>` should resolve under `~/.local/share/mise/installs/`.\"), stating the rule whose whole point is .claude/rules/ci-local-parity.md:31 (\"Global mise tools are invisible to CI runners.\")."
native_alternative = "`mise ls --current --json` emits, per tool, a `source` object carrying `{type, path}` — the absolute path of the config file that DECLARED the tool. That is the native fact that separates repo-declared from user-global, which `mise which` (an install-path resolver) structurally cannot."
alternative_evidence = "Live probe on the pinned host (mise 2026.9.3, macos-arm64), run from the repo root — POSITIVE ARM: `mise ls --current --json | jq` returns for `actionlint` `source.path = /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.config/mise/conf.d/shared.toml`; the same query lists `bat` and `atuin` with `source.path = /Users/rmanaloto/.config/mise/config.toml`. CONTROL ARM for the rule's own probe: `mise which bat` -> `/Users/rmanaloto/.local/share/mise/installs/bat/0.26.1/.../bat` and `mise which atuin` -> `/Users/rmanaloto/.local/share/mise/installs/atuin/18.21.0/.../atuin`, i.e. two GLOBAL-only tools satisfy the rule's stated verification identically to the repo-declared `mise which actionlint` -> `/Users/rmanaloto/.local/share/mise/installs/actionlint/1.7.12/actionlint`. Grep control arm on the repo config: `grep -n 'bat' mise.toml .config/mise/conf.d/shared.toml` -> 0 hits while `grep -c actionlint .config/mise/conf.d/shared.toml` -> 1, so the absence is real, not a broken grep."
rationale = "This is a probe that can only pass — every mise-managed tool installs under `~/.local/share/mise/installs/` regardless of which config declared it, so the stated verification returns the same answer for the compliant and the non-compliant case alike (`probes-need-a-control-arm.md` rule 1). Rule 2's DIRECTIVE is sound and should stay; only its verification line should be replaced with the native discriminator, e.g. `mise ls --current --json | jq -r '.\"<tool>\"[0].source.path'` and asserting the path is inside the repo. Refactor rather than retire because no native mise feature enforces the policy on its own — it only supplies the fact the check needs."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ci-local-parity-2"
unit = "rule-ci-local-parity"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "Rule 6's manual three-step habit for a new hk step: `hk validate`, then `mise run lint`, then commit — an eyeball check that a newly added step passes, with no replayable violation case. Reinforced by hk.pkl's own deferral of the native mechanism."
evidence = ".claude/rules/ci-local-parity.md:60-65 (Rule 6, the three numbered steps); hk.pkl:697-703 — \"#160 T12: step `tests{}` + the hk_test builtin + `hk test` in CI are DEFERRED with probe evidence: `hk test` executes test cases IN THE REAL PROJECT DIRECTORY (no sandbox ...). Needs upstream sandboxing ... first.\""
native_alternative = "hk's step-defined tests: a `tests { }` mapping on any Step, run by the `hk test` subcommand (`--step`, `--name`, `--list`, `--json`), with an OPT-IN sandbox via the `{{tmp}}` template variable and fixtures cleaned on success. Also available as the `hk_test` builtin step so `hk test` itself runs inside `hk run check`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:931 (\"Define self-contained tests for a step, runnable via `hk test`.\") and :1004 (`tests: Mapping<String, StepTest>`), with `{{tmp}}`: \"sandbox path used to execute the test\" at Config.pkl:954; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:1919 — \"**(test)** make sandbox opt-in via {{tmp}}; clean fixtures on success\" (jdx/hk#182), landed in 1.9.1 (CHANGELOG.md:1915 header), i.e. 48 minor versions BEFORE this repo's 1.57.0 pin; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/builtins/hk_test.pkl:8-14 (the `hk_test` builtin, `command = \"hk test --quiet\"`). LIVE PROBE on the pinned binary (`hk 1.57.0`), in a scratchpad git repo outside this tree, with BOTH ARMS: a `no_lint_skip`-shaped step with two declared tests writing to `{{tmp}}` — violation case `expect { code = 1 }`, clean case `expect { code = 0 }` — returned `ok - no_lint_skip_demo :: violation fails (70ms)` / `ok - no_lint_skip_demo :: clean passes (71ms)`, rc=0, and `ls -a` of the project dir afterwards showed only `.git` and `hk.pkl` — ZERO leaked test files."
rationale = "The blocker hk.pkl:697 records — `hk test` writing into the real project tree — is stale at the current pin: sandboxing shipped in hk 1.9.1 and the live two-arm probe on 1.57.0 left nothing behind. Rule 6's `hk validate` and `mise run lint` steps remain correct and were re-probed current (`hk validate --help` exits 0 on 1.57.0), so this is a refactor, not a retirement: add a step 4 that declares a `tests { }` violation/clean pair for the new step and runs `hk test --step <name>`. That converts the rule's weakest clause — \"verify the step passes\", a one-faced check on clean code that `probes-need-a-control-arm.md` rule 2 refuses — into a native, replayable FAIL arm, and it is the one mechanism that would have caught the #154 bare-`{}` silent-skip class the top of hk.pkl documents."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ci-local-parity-3"
unit = "rule-ci-local-parity"
kind = "rule"
disposition = "refactor"
confidence = 0.75
custom_thing = "Rule 1's manual habit: \"When adding or modifying a `run:` step in ci.yml lint job, verify there is a corresponding step in `hk.pkl` that runs the same check locally.\" — a human-verified correspondence with no expression in either config and no gate."
evidence = ".claude/rules/ci-local-parity.md:16-20. The divergence class is live in the repo: the CI lint job carries four bare `run:` steps with no hk step behind them — `.github/workflows/ci.yml:114` (\"Assert chezmoiignore mise overlay hard gate\"), :134 (`mise outdated || true`), :136 (`mise reshim`), :145 (`mise doctor`)."
native_alternative = "hk profiles: a Step may carry `profiles: List<String>?`, and the run is scoped with `hk run check --profile ci` / `-p '!slow'` / root `profiles = List(\"ci\")`. A CI-only check therefore lives IN hk.pkl as a profiled step instead of as a bare `run:` block, so it has a local invocation by construction and Rule 1's correspondence stops being something a human has to remember."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:156 — \"Profiles to enable or disable. Profiles allow you to group steps that should run only in certain contexts (e.g., CI, slow tests).\" — and the Step-level field at Config.pkl:682 (`profiles: List<String>?`, class Step). CLI surface probed on the pinned binary: `hk --help` on 1.57.0 lists `-p, --profile <PROFILE>  Profiles to enable/disable prefix with ! to disable`. CONTROL-ARMED absence in this repo: `grep -n 'profiles' hk.pkl hk-common.pkl hk-image.pkl mise.toml` -> 0 hits, while `grep -c glob hk.pkl` -> 41 with the same command shape, so the repo genuinely uses zero profiles."
rationale = "Rule 1 states a correspondence but names no mechanism to express it, so it degrades to a habit — and the habit has already slipped four times in the one workflow it governs. hk profiles are the native way to say \"this check runs only in CI\" without leaving hk, which collapses the parity question from \"did a human check?\" to \"is it an hk step?\". Not a retire: profiles cover the CI-only-step case, not the genuinely non-hk steps (`mise reshim`, `mise doctor`, artifact uploads), which are runner housekeeping rather than lint checks and should be explicitly exempted in the rule text instead of silently violating it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ci-local-parity-4"
unit = "rule-ci-local-parity"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "The rule's own `paths:` frontmatter — `.github/workflows/*.yml`, `hk.pkl`, `mise.toml`, `.devcontainer/mise-system.toml` — which decides when the rule is loaded at all."
evidence = ".claude/rules/ci-local-parity.md:1-7 (the frontmatter). The body has since grown to govern files the globs never name: Rule 2 explicitly governs `.config/mise/conf.d/shared.toml` (.claude/rules/ci-local-parity.md:26-27, \"the shared fragment `.config/mise/conf.d/shared.toml` (the host<->image tools; epic #160 T5)\"), and the repo's hk config was split into `hk-common.pkl` / `hk-image.pkl` (AGENTS.md 'Split hk Architecture'), neither of which is in the globs."
native_alternative = "Path-scoped rule loading is a harness feature with documented semantics: a rule with `paths:` loads ONLY when Claude reads a file matching one of its globs, so an unlisted path means the rule is absent exactly when it applies. The fix is native and is a frontmatter edit: add `.config/mise/conf.d/shared.toml`, `hk-common.pkl`, `hk-image.pkl` (and `.devcontainer/mise-runtime.toml`) to the existing `paths:` list."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/claude-directory.md:168 — \"A rule without `paths:` frontmatter loads at session start like CLAUDE.md; a rule with `paths:` loads only when Claude reads a matching file.\"; same file :167 — \"Rules with `paths:` load when a matching file enters context\"; and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/changelog.md:3632 — \"Rules and skills `paths:` frontmatter now accepts a YAML list of globs\" (the list form this file already uses, so no syntax change is needed)."
rationale = "This is a stale harness-fact-in-practice rather than a superseded mechanism: the scoping decision is still correct (this rule is genuinely file-triggered, and `md-size-budgets.md` cites it by name as the canonical safe-to-scope case), but the glob set was frozen before #160 T5 added the shared fragment and before the hk config was split into three pkl files. A session editing `.config/mise/conf.d/shared.toml` — the exact file Rule 2 was amended to cover — never loads the rule telling it what to check, which is the same failure shape that un-scoped `zero-skip-policy` and `clean-git-state` on 2026-07-15."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ci-local-parity-5"
unit = "rule-ci-local-parity"
kind = "rule"
disposition = "refactor"
confidence = 0.65
custom_thing = "Rule 4 (`uv run --project python`, never `--directory`) carried as prose only, while its structural sibling Rule 3 (never `npx`) is backed by a deterministic PreToolUse deny."
evidence = ".claude/rules/ci-local-parity.md:43-50 (Rule 4) vs .claude/rules/ci-local-parity.md:35-41 (Rule 3), whose gate is python/src/dotfiles_setup/hook_guard.py:250-253 (`\"npx\"`, `re.compile(_CMD + r\"npx\\s\")`, \"Do not use npx. Use the mise-installed binary directly ...\"). No corresponding rule exists for `--directory`: `grep -n 'uv run --directory\\|--directory' python/src/dotfiles_setup/hook_guard.py` -> 0 rule hits (only the module docstring at :21 mentions the legacy shell guards), and `jq -r '.permissions | to_entries[] | .value[]? | select(test(\"uv\"))' .claude/settings.json` -> empty."
native_alternative = "A scoped `permissions.deny` Bash rule in `.claude/settings.json`, e.g. `\"deny\": [\"Bash(uv run --directory *)\"]` — the harness's own enforcement layer, which leaves the `Bash` tool available and blocks only matching calls, and which the docs name as the mechanism to reach for when guidance must actually bind."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:68 — \"A scoped rule like `Bash(rm *)` leaves the tool available and blocks matching calls when Claude attempts them.\"; same file :235 — \"A deny or ask rule matches past any leading assignment, so `Bash(rm *)` in deny still matches `FOO=bar rm -rf tmp/`\"; and the doctrinal anchor /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/claude-directory.md:168 — \"rules are guidance Claude reads, not configuration Claude Code enforces. For guaranteed behavior use hooks or permissions.\" Rule 4's underlying fact was re-verified current at the uv 0.12.9 pin: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/uv/crates/uv/tests/it/help.rs:91 — `--directory` is \"Change to the given directory prior to running the command\", so the cwd-changing semantics the rule warns about are unchanged."
rationale = "The rule's fact is correct and current; what is missing is the enforcement layer the harness offers and that the sibling rule already uses. Confidence is held below the others because AGENTS.md records that the permission engine unwraps runners (the reason `pytest` is doc-level only), so the deny pattern must be probed against the real matcher before adoption — a rule that fails to match, or that over-matches the canonical `--project` form, would be worse than the prose. Refactor, not keep: the gap is an unused native mechanism, not the absence of one."
probe = "Add \"Bash(uv run --directory *)\" to .permissions.deny in .claude/settings.local.json, then run BOTH arms in one turn — NEGATIVE (must be denied): `uv run --directory python pytest tests/test_lock_coverage.py -x -q`; POSITIVE (must still run): `uv run --project python pytest tests/test_lock_coverage.py -x -q`. Adopt only if arm 1 is denied AND arm 2 is not; revert the settings edit either way."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-ci-local-parity-6"
unit = "rule-ci-local-parity"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "The rule's remaining load-bearing content that nothing native supersedes: Rule 3's npx prohibition, Rule 4's `--project` directive, Rule 5's retired-but-numbered stub, and Rule 6's `hk validate` step."
evidence = ".claude/rules/ci-local-parity.md:35-41 (Rule 3), :43-50 (Rule 4), :52-58 (Rule 5, \"RETIRED (#160 T12, hk 1.49) ... Kept as a numbered rule so references to 'rule 5' stay valid\"), :62 (`hk validate`)."
native_alternative = "None for the policy itself. Each underlying harness/tool fact was re-probed and holds at the host's pins rather than being superseded: `hk validate` still exists on hk 1.57.0; hk's pkl-eval cache is still content-hashed so Rule 5's retirement remains correct; uv's `--directory` still changes cwd at 0.12.9; and neither mise nor hk ships a feature that decides for you which config a tool must be declared in or which runner flag to use."
alternative_evidence = "Live probes on the pinned binaries: `hk --version` -> `hk 1.57.0`, `hk validate --help` -> \"Validate the config file\", rc=0 (so Rule 6 step 1 is current, not stale). `hk` gap digest .agent/kb/raw/hk-releases-since-pin.digest.md — the only release newer than the 1.57.0 pin is v1.58.1, whose body states verbatim \"A small documentation-only release. There are no changes to hk's runtime behavior\", so nothing in the gap window supersedes any of this. `mise` gap digest .agent/kb/raw/mise-releases-since-pin.digest.md — zero releases newer than the host's 2026.9.3. `uv` gap digest .agent/kb/raw/uv-releases-since-pin.digest.md — the five capabilities in 0.12.10/0.12.11 are all `uv publish` / `pylock.toml` / `uv tree` changes, none touching `--project` or `--directory` semantics."
rationale = "Justified keep, not a default. These four clauses encode a POLICY CHOICE about which of several working invocations this repo standardises on — mise-pinned binary over npx, `--project` over `--directory` — and no tool feature can make that choice; mise will happily run either, and uv documents both flags as supported. Rule 5's stub is 6 lines of correct, still-true archaeology that preserves the stable numbering other docs cite, so deleting it would break references to buy back almost nothing under this file's `rule_scoped` budget (400 lines / 32,000 B; the file is 66 lines). The gap digests for all three tools were read and contain nothing that changes any of it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-clarify-before-acting-1"
unit = "rule-clarify-before-acting"
kind = "rule"
disposition = "refactor"
confidence = 0.97
custom_thing = "All three vendor-doc anchors the rule cites to justify its gate are stale line numbers at 2.1.266. `.claude/rules/clarify-before-acting.md:81` cites `$CC/hooks.md:1394` for 'AskUserQuestion is a documented PreToolUse matcher value'; `:82` cites `$CC/settings.md:177` for 'hooks reload from settings.json without a restart'; `:83` cites `$CC/hooks.md:1544` for 'a deny prevents the call while its reason is shown to Claude'. The same stale `$CC/hooks.md:1544` anchor is duplicated inside the enforcer's docstring at `python/src/dotfiles_setup/ask_quality.py:56`."
evidence = ".claude/rules/clarify-before-acting.md:79-87 (the 'Confirmed against the vendor docs and a live probe (2026-08-02)' paragraph); python/src/dotfiles_setup/ask_quality.py:56"
native_alternative = "All three FACTS still hold at 2.1.266, but at different anchors: PreToolUse matches `AskUserQuestion` -> /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1542; settings (incl. `hooks`) reload without a restart -> .../claude-code/settings.md:646; `permissionDecision: \"deny\"` prevents the tool call and `permissionDecisionReason` for a deny is 'shown to Claude' -> .../claude-code/hooks.md:1744 and :1745. Re-anchor the rule (and ask_quality.py's docstring) to these lines."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1542 ('Matches on any tool name except EndConversation: ... `AskUserQuestion` ...'); .../settings.md:646 ('Claude Code watches your settings files and reloads them when they change, so it applies most edits to the running session without a restart, including edits to `permissions`, `hooks` ...'); .../hooks.md:1744 ('\"deny\" prevents the tool call') and .../hooks.md:1745 ('For \"deny\", shown to Claude'). Control arm on the corpus: `grep -c permissionDecision hooks.md` = 32 while a freshly-invented absent token returned 0, so the grep discriminates. The cited lines today hold unrelated content: hooks.md:1394 is now the UserPromptExpansion decision-control table; settings.md:177 is a JS snippet fragment from a settings-scope diagram."
rationale = "Category (a) staleness: every cited anchor drifted while the underlying facts survived. The rule's whole authority rests on 'Confirmed against the vendor docs, not assumed' — a citation that resolves to unrelated prose is indistinguishable from an invented one, which is exactly the failure md-size-budgets.md was written about. Fix is a re-anchor, not a rewrite; the claims themselves are all still verbatim-true."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-clarify-before-acting-2"
unit = "rule-clarify-before-acting"
kind = "rule"
disposition = "refactor"
confidence = 0.92
custom_thing = "The rule states its AskUserQuestion mandate is UNCONDITIONAL: 'Whenever you need input, use the `AskUserQuestion` TOOL — never options in prose. This is the *mechanism* rule and it is unconditional' (.claude/rules/clarify-before-acting.md:8-12), reinforced by rule 5 'Keep asking until sure'. It names no context in which the tool is unavailable and no fallback for one."
evidence = ".claude/rules/clarify-before-acting.md:8-12; .claude/rules/clarify-before-acting.md:64-66 (rule 5)"
native_alternative = "The harness now removes or denies `AskUserQuestion` in four documented contexts, so the mandate cannot be followed there: (1) every subagent — the first tool filter strips `AskUserQuestion` 'even when listed in the `tools` field'; (2) `dontAsk` permission mode denies it 'even if you've allowed them'; (3) non-interactive `-p` mode blocks it unless a hook returns `allow` + `updatedInput`; (4) `--permission-prompts none` (shipped 2.1.259) auto-denies anything that would prompt. The rule should add a bounded fallback — when the tool is absent or denied, present the options in prose and STOP rather than guessing — which is the behaviour the rule's own rule 1 actually wants."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:382 (in the 'first filter removes these tools, even when listed in the `tools` field' list, .../sub-agents.md:380-382); .../claude-code/permissions.md:86 and .../claude-code/permission-modes.md:469 (dontAsk denies AskUserQuestion even if allow rules match); .../claude-code/hooks.md:1773 ('`AskUserQuestion` and `ExitPlanMode` require user interaction and normally block in non-interactive mode with the `-p` flag'); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md ('`--permission-prompts none` for unattended headless hosts — anything that would prompt is auto-denied', 2.1.259)."
rationale = "Category (c): the rule forbids the only thing that works in the contexts where the harness has made the mandated mechanism unavailable. This is not hypothetical — this audit runs inside a subagent, where AskUserQuestion is stripped by sub-agents.md:382's filter, so an agent obeying the literal text emits a call that cannot land and has no sanctioned way to surface a fork. The corroborating project memory (`AskUserQuestion is STRIPPED from every subagent`, feedback_orchestration_process_lessons / project_session_2026-09-01) now has a vendor-doc anchor, so the caveat can be stated with a citation rather than as folklore."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-clarify-before-acting-3"
unit = "rule-clarify-before-acting"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "Rule 5 — 'Keep asking until sure. A second clarifying round is cheaper than rework' (.claude/rules/clarify-before-acting.md:64-66) — assumes a question stays open until the user answers it, so an unanswered ask is a blocked turn rather than a silent go-ahead."
evidence = ".claude/rules/clarify-before-acting.md:64-66"
native_alternative = "The harness now ships `askUserQuestionTimeout` (`60s` / `5m` / `10m`, user or managed settings, also on the `/config` 'Question auto-continue timeout' row). After the idle window the dialog closes on its own, submits any options already selected, and 'tells Claude you may be away from your keyboard, so Claude proceeds on its own judgment and can re-ask later'. The rule should note that an auto-continued question is NOT an answer — the agent must treat it as unresolved ambiguity and re-ask or stop, not as tacit approval — and that the timeout never applies to permission prompts or plan approval."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:130-137 ('### Question auto-continue timeout ... set the `askUserQuestionTimeout` setting to `60s`, `5m`, or `10m` ... it submits any options you'd already selected and tells Claude you may be away from your keyboard, so Claude proceeds on its own judgment ... The timeout applies only to `AskUserQuestion`'s multiple-choice questions; permission prompts, including plan approval, never auto-resolve on idle'); .../claude-code/settings-reference.md:602 and :2759 (the `askUserQuestionTimeout` setting entry)."
rationale = "Category (c): the harness can now close the question for the user and hand control back with an explicit 'proceed on your own judgment' framing — the exact outcome rule 1 exists to prevent. The rule is not superseded (nothing native decides the ambiguity), but it must name the timeout so an auto-continue is read as 'still unresolved', not as consent. Confidence held below 0.9 because the setting is opt-in and this repo has not set it — I did not read the user settings.json, so whether it is currently active here is unverified."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-clarify-before-acting-4"
unit = "rule-clarify-before-acting"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "The gate itself: `.claude/settings.json:52-62` wires the `Bash|AskUserQuestion|Edit|Write|NotebookEdit` PreToolUse matcher to `scripts/pretooluse-guard.sh`; `hook_guard.decide_payload` dispatches `tool_name == \"AskUserQuestion\"` to `dotfiles_setup.ask_quality`, which denies an ask whose options lack a leading `(Recommended)` label, a `PRO:`/`CON:` pair in every description, or a citation (with the literal `[no prior evidence]` escape)."
evidence = ".claude/settings.json:52-62; python/src/dotfiles_setup/hook_guard.py:799 (`if tool_name == \"AskUserQuestion\": ...`); python/src/dotfiles_setup/ask_quality.py:83-86 (RECOMMENDED_MARKER / PRO_MARKER / CON_MARKER / NO_EVIDENCE_ESCAPE); .claude/rules/clarify-before-acting.md:70-77"
native_alternative = "None at 2.1.266. The `AskUserQuestion` input schema has no recommendation, evidence, or trade-off field — options are `{ label; description; preview? }` — and `preview` is opt-in behind the Agent-SDK `askUserQuestion.previewFormat` option ('When unset, Claude does not emit previews'), so it neither carries a citation nor gates anything. Permission rules (`permissions.deny` / `ask`) match tool names and rule content patterns, not the semantic contents of an option's description, so no allow/deny rule can express 'every option must state a downside'. A PreToolUse command hook returning `permissionDecision: \"deny\"` remains the only mechanism that can inspect the proposed input and reject it, and it still works exactly as the rule describes."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__typescript.md:2490-2496 (`AskUserQuestionInput`: `options: Array<{ label: string; description: string; preview?: string }>` — no recommendation/citation/trade-off field) and .../agent-sdk__typescript.md:1016 (`askUserQuestion.previewFormat` ... 'When unset, Claude does not emit previews'); .../claude-code/hooks.md:1836 ('PreToolUse hooks run before every tool call, whether or not it needs permission') and .../claude-code/hooks.md:1744 ('\"deny\" prevents the tool call'), which together keep the wired mechanism valid at 2.1.266."
rationale = "Required justification: the gate enforces a CONTENT standard on the tool's input, and no native surface at 2.1.266 expresses that — the tool schema has no field for a recommendation, a citation, or a stated downside, and permission rules cannot read an option's description. The one native affordance that could have carried evidence (`preview`) is SDK-gated and emits nothing when unset, so adopting it would weaken the gate rather than replace it. PreToolUse deny remains the documented, still-current mechanism (hooks.md:1744/1836), and it fires regardless of permission mode because PreToolUse runs on every tool call, not only on prompting calls. Confidence is 0.85 rather than higher because I verified the offline corpus and the 2.1.258-2.1.266 gap digest but did not run a live 2.1.266 probe of the AskUserQuestion schema."
probe = "echo '{\"tool_name\":\"AskUserQuestion\",\"tool_input\":{\"questions\":[{\"question\":\"q\",\"header\":\"h\",\"multiSelect\":false,\"options\":[{\"label\":\"A\",\"description\":\"no markers\"},{\"label\":\"B\",\"description\":\"no markers\"}]}]}}' | bash /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/scripts/pretooluse-guard.sh; echo \"rc=$?\"   # FAIL arm: must emit permissionDecision deny naming the missing recommendation/PRO:/CON:/citation. Then re-run with a compliant payload as the PASS arm."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-clean-git-state-1"
unit = "rule-clean-git-state"
kind = "rule"
disposition = "refactor"
confidence = 0.82
custom_thing = "The asserted tool fact \"New files must be `git add`-ed before hk runs, or hk won't check them\" — the rule's justification for manually staging new files before validating."
evidence = ".claude/rules/clean-git-state.md:24 (\"3. New files must be `git add`-ed before hk runs, or hk won't check them\"); the validation command the rule points at is `mise run lint` (.claude/rules/clean-git-state.md:16), which is `hk run check --all` (python/src/dotfiles_setup/lint.py:66 `HK_COMMAND = (\"hk\", \"run\", \"check\", \"--all\")`) — the same command CI runs (.github/workflows/ci.yml:113)."
native_alternative = "hk 1.57's own file selection: under `--all` with no stash, hk unions the git index with the untracked-file set, so untracked/new files ARE checked without staging. The `check` hook declares no `stash`, so stash resolves to None."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/hook.rs:1683-1688 (`} else if opts.all { ... let mut all_files = repo.lock().await.all_files(None)?; if !stash { all_files.extend(git_status.untracked_files.iter().cloned()); }`); stash resolution /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/hook.rs:637-646 + hk.pkl:748-752 (the `check` hook sets no `stash`, unlike `pre-commit` at hk.pkl:712 `stash = \"git\"`); default confirmed at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/environment_variables.md:319-326 (`HK_STASH_UNTRACKED` Default: `true`; only when set to false do \"Untracked files ... not appear in reports or `hk check --all` results\") and /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/env.rs:62."
rationale = "The claim is stale for the path the rule actually prescribes. It remains TRUE for the pre-commit hook path only — `defaults_to_staged_files()` returns true just for `pre-commit` (hook.rs:649-651) and that branch takes `git_status.staged_files` alone (hook.rs:1688-1691). That is the control arm: the same function yields the other answer for the other hook, so the distinction is real, not a mis-read. Refactor line 24 to scope the claim to the commit-time hook and say plainly that `mise run lint` (`hk check --all`) already sees untracked files, so a new file needs no `git add` merely to be linted."
probe = "cd $(mktemp -d) && git init -q . && git commit -q --allow-empty -m base && printf 'x' > new.txt && hk run check --all --dry-run 2>&1 | grep -c new.txt   # control arm: repeat with `hk run pre-commit --all` and with the file staged"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-clean-git-state-2"
unit = "rule-clean-git-state"
kind = "rule"
disposition = "refactor"
confidence = 0.75
custom_thing = "The manual \"Before Every Commit\" checklist — run `git diff --name-only` / `git diff --cached --name-only` by hand to verify \"what hk checked locally matches what CI will see\"."
evidence = ".claude/rules/clean-git-state.md:18-24."
native_alternative = "Two mechanisms already do this without a human diff: (1) hk's own pre-commit stash — `stash = \"git\"` makes hk remove ALL unstaged worktree changes for the duration of the run, so the steps see exactly the index content that will be committed and that CI will check out; (2) `mise run ship`'s preflight, which hard-refuses a non-clean working tree before any gate runs."
alternative_evidence = "hk.pkl:710-712 (`[\"pre-commit\"] { fix = true; stash = \"git\" }`); stash execution /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/hook.rs:1288-1307 (\"Stash ALL unstaged changes in the repository (not only files under consideration) so that unrelated worktree changes do not affect or get affected by fixers\"); ship gate python/src/dotfiles_setup/pr.py:485-501 (`_working_tree_clean` -> \"FAIL  ship: working tree not clean — commit (or stash) first so the gates validate exactly what ships\")."
rationale = "The rule predates both layers and still reads as if the human is the only mechanism. It should keep its directive but delegate the enforcement: name hk's `stash = \"git\"` as what guarantees commit-time local==CI content, and `mise run ship`'s clean-tree refusal as the push-time gate, then drop the two by-hand `git diff` steps to a one-line 'if you want to look, here is how'. Not a full retire: neither mechanism covers the ad-hoc `mise run lint` run that the rule's first section is about (the `check` hook does not stash), so the discipline still has a live window."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && git stash list >/dev/null && echo 'staged-vs-worktree arm: edit a tracked file, stage only part, run `hk run pre-commit` and confirm the step sees index content only'"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-clean-git-state-3"
unit = "rule-clean-git-state"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "The per-file manual instruction \"Stage ALL file deletions: `git add <deleted-file>`\" and \"Stage ALL file modifications you intend to commit\" as separate enumerated human steps."
evidence = ".claude/rules/clean-git-state.md:11-15 and the table row .claude/rules/clean-git-state.md:30 (\"File deleted on disk, not staged | File exists in CI | `git add <deleted-file>`\")."
native_alternative = "git's own `git add -u` / `--update`: \"update tracked files\" — stages every tracked modification AND deletion in one command, and, unlike `-A`/`git add .`, adds nothing untracked, so it does not collide with the repo's ban on bulk-adding phantom `.agent/state/**` files."
alternative_evidence = ".agent/kb/raw/git-add-update-flag-probe.md (saved probe of the host's git 2.50.1: `-u, --[no-]update  update tracked files`; `-A, --[no-]all  add changes from all tracked and untracked files`); the bulk-add ban is .claude/rules/do-not.md #5 (\"Do NOT bulk `git add .`\"), which `-u` does not violate."
rationale = "The rule asks a human to enumerate deletions one by one when git ships the exact primitive — and the enumeration is the step that actually fails (a missed deletion is precisely the 2026-04-05 agnix/dockerfile-reviewer.md incident the rule cites). Replace steps 2+3 with `git add -u` plus explicit `git add <path>` for genuinely new files, and note why `-A`/`.` is still forbidden. Keep the rule; change the mechanism it prescribes."
probe = "cd $(mktemp -d) && git init -q . && printf a > f && git add f && git commit -qm x && rm f && git add -u && git status --porcelain   # expect ' D f' staged; control arm: repeat without -u and confirm the deletion stays unstaged"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-clean-git-state-4"
unit = "rule-clean-git-state"
kind = "rule"
disposition = "retire"
confidence = 0.70
custom_thing = "Divergence-table rows 3 and 4 — \"Global mise tool installed | Tool missing in CI | Add to mise.toml\" and \"npx resolves cached package | npx re-downloads in CI | Use mise binary name\" — carried as manual reminders."
evidence = ".claude/rules/clean-git-state.md:32-33."
native_alternative = "Both are now machine-gated elsewhere and need no eager reminder: the mise tool/lock surface is enforced by the `mise_lock_integrity` and `dependency_ownership` hk steps (which fail the same `hk check --all` the rule tells you to run), and the npx row is covered by the mise-first policy plus the PreToolUse redirect table."
alternative_evidence = "hk.pkl:337-339 (`[\"mise_lock_integrity\"] { check = \"uv run --project python dotfiles-setup lock-check\" }`); hk.pkl:443-447 (`[\"dependency_ownership\"] { glob = List(\"mise.toml\", \"mise.lock\", \"python/pyproject.toml\", \"python/uv.lock\") ... }`); .claude/rules/mise-tasks-only.md:27 (`| \\`npx <tool>\\` | the mise-pinned binary directly |`) and AGENTS.md:146 (\"use mise binaries directly, not npx\")."
rationale = "Retire these two table rows only (not the rule). They are neither git-state facts nor unenforced: a missing/stale lock entry fails the lint gate, and npx is redirected by the guard and banned by two other eager documents. Keeping them here costs eager bytes and duplicates ownership — exactly the trimming lever md-size-budgets.md prescribes for unscoped rules (~88% of the eager corpus). Note the counterweight: md-size-budgets.md:171 records that the 2026-07-28 trimming pass deliberately left this file alone as \"already at the directive-plus-one-example floor\", so retire ONLY these two rows and do not treat the finding as licence for a broader cut."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && uv run --project python dotfiles-setup lock-check; echo rc=$?   # control arm: delete a [[tools.X]] block from mise.lock in a scratch copy and confirm rc!=0"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "rule-clean-git-state-5"
unit = "rule-clean-git-state"
kind = "rule"
disposition = "unknown"
confidence = 0.50
custom_thing = "The premise of divergence row 1 — that a file deleted on disk but not staged is silently SKIPPED locally while CI still has it (the failure mode the whole rule is built on)."
evidence = ".claude/rules/clean-git-state.md:13 and :30."
native_alternative = "hk 1.57 builds the `--all` list from the git INDEX, not the worktree, and its directory filter explicitly KEEPS paths it cannot stat (\"Can't stat it at all (deleted/renamed) - keep it\"). So a deleted-unstaged path is still handed to the steps, which should error loudly on a missing file rather than silently skipping it — inverting the described divergence."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/git.rs:438-455 (`all_files` iterates `repo.index()`, falling back to `git ls-files -z`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/hook.rs:1717-1738 (retain filter; else-branch comment \"Can't stat it at all (deleted/renamed) - keep it\")."
rationale = "The code says the local run should no longer be blind to an unstaged deletion, but whether it FAILS depends on each step: `{{files}}`-templated steps get a nonexistent path (loud), while glob-only or whole-repo steps may not notice. That is a per-step property this audit cannot settle by reading, and the claim is load-bearing for the rule's headline incident — so it needs a live arm before the rule is rewritten around it."
probe = "cd $(mktemp -d) && git init -q . && printf 'x=1\\n' > a.py && git add a.py && git commit -qm x && rm a.py && hk run check --all; echo rc=$?   # armed both ways: expect a loud failure naming a.py; control arm = same run with the deletion staged (`git add -u`), which must be rc=0"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-clean-git-state-6"
unit = "rule-clean-git-state"
kind = "rule"
disposition = "keep"
confidence = 0.80
custom_thing = "The rule itself: an eager, unscoped `.claude/rules` ADR mandating clean/staged git state before validation."
evidence = ".claude/rules/clean-git-state.md:1-7 and :35-41 (\"Why this rule is eager (never `paths:`-scoped)\")."
native_alternative = "None at the harness level. The only adjacent Claude Code capability shipped since the offline corpus is the `/diff` panel (2.1.260), which DISPLAYS uncommitted changes live — it enforces nothing and does not stage anything."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (capability bullet: \"A diff panel opens beside the conversation in fullscreen mode showing uncommitted changes live as Claude edits; toggled with `/diff`\", version 2.1.260); no settings key, hook event, permission rule or agent field in the 2.1.258-2.1.266 digest or .agent/kb/raw/claude-code-llms-index.digest.md addresses index/worktree hygiene. Cross-reference verified live (control-armed): .claude/rules/md-size-budgets.md:140 (\"`clean-git-state` (fires when validation is about to run)\") and :153; control arm = `zero-skip-policy` returns 2 hits in the same file with the same command shape."
rationale = "Required justification: (a) the rule asserts NO harness facts — it names no hook event, settings key, precedence claim or `$CC/...:line` anchor, so nothing in it can have gone stale against 2.1.266; its single cross-reference, `.claude/rules/md-size-budgets.md`, still exists and still classifies `clean-git-state` as behaviour-triggered and therefore un-scopable (md-size-budgets.md, \"Scoping: the trigger test\" — \"clean-git-state (fires when validation is about to run)\"). (b) Its trigger is a decision (\"I am about to validate\"), which no `paths:` glob and no native feature predicts; the enforcing layers that do exist (hk's pre-commit stash, ship's clean-tree refusal) fire only at commit/push time, leaving the ad-hoc `mise run lint` window uncovered. (c) It forbids nothing a native feature has made safe. So the rule stays eager; findings 1-5 are edits WITHIN it, not grounds to retire it."
probe = "grep -rn \"clean-git-state\\|working tree\\|git status\" /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings.md /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md   # control arm: the same grep for \"PreToolUse\" must return many hits"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-do-not-1"
unit = "rule-do-not"
kind = "rule"
disposition = "refactor"
confidence = 0.93
custom_thing = "Entry 11's rationale asserts a harness cost fact: \"A registered server taxes **every** conversation's system prompt with **every** tool's schema, forever\" — used as the reason to avoid native MCP registration for our own lookups."
evidence = ".claude/rules/do-not.md:79-81 (\"A registered server taxes **every** conversation's system prompt with **every** tool's schema, forever\"); the sibling rule already retired the same sentence at .claude/rules/research-doc-sources.md:97-100 (\"the old 'every schema in every conversation, forever' cost is FALSE here ... Do not cite that sentence\")."
native_alternative = "MCP tool search: MCP tools are deferred and discovered on demand by default, so a registered server's schemas are NOT in the system prompt; only tool names are listed and the schema loads via ToolSearch. Per-server opt-out exists (`alwaysLoad`), and the deferral requires Sonnet/Haiku/Opus 4.5+ — satisfied by this host's Opus 5."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/mcp.md:1354 (\"## Scale with MCP tool search\"), :1376 (\"Tool search is enabled by default: MCP tools are deferred and discovered on demand\"), :1393 (unset => \"All MCP tools deferred and loaded on demand\"), :1380 (model requirement: Sonnet/Haiku/Opus 4.5 and later), :1421 (`alwaysLoad` opt-out for the small set that must load upfront)."
rationale = "The DIRECTIVE (prefer the tool's CLI / a plain HTTP API / mcp2cli over registering a server for our own problems) survives on the grounds do-not.md's own sibling gives it — a spawned process, a pin, an auth path and a doctor failure mode — but the stated REASON is a stale harness fact at 2.1.266 and is contradicted, by name, by another eager rule in the same corpus. Two eager rules asserting opposite facts about the same mechanism is exactly the drift this audit exists to catch. Rewrite entry 11's rationale to cite tool search + `alwaysLoad` and the real cost (operational surface, not context tax), or delegate the whole rationale to research-doc-sources.md § 'MCP: two lanes' which already carries the corrected version."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-do-not-2"
unit = "rule-do-not"
kind = "rule"
disposition = "refactor"
confidence = 0.88
custom_thing = "Entry 8's parenthetical harness/tool fact: \"`CLAUDE_CONFIG_DIR` is NOT containment (it redirects the skill dir only; that write is hardcoded)\" — the justification for the blanket 'run every `graphify <platform> install` in a throwaway directory' habit."
evidence = ".claude/rules/do-not.md:34-38 (entry 8, the `CLAUDE_CONFIG_DIR` claim at :36-38)."
native_alternative = "graphify itself now honours `CLAUDE_CONFIG_DIR` for the always-on Claude registration, not just the skill directory: at the repo's pinned graphify 0.9.53 the global branch writes `$CLAUDE_CONFIG_DIR/CLAUDE.md`, and only the no-env-var branch falls through to `~/.claude/CLAUDE.md`. The fix shipped in 0.9.44, i.e. BEFORE the pin."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/graphify/install.py:640-652 (comment: \"Honor CLAUDE_CONFIG_DIR for the global (non-project) case, same as _platform_skill_destination does for the skill copy path (#527) -- this always-on registration path was missed by that fix (#2694)\"; `elif os.environ.get(\"CLAUDE_CONFIG_DIR\")` branch writes `config_dir / \"CLAUDE.md\"`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/CHANGELOG.md:142 (\"`graphify install` (Claude always-on) now writes the CLAUDE.md registration into `$CLAUDE_CONFIG_DIR` ... instead of always mutating the default `~/.claude/CLAUDE.md`\") and :134 (\"the CLAUDE_CONFIG_DIR half shipped in 0.9.44\"); pin confirmed at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/graphify/pyproject.toml:7 (version = \"0.9.53\")."
rationale = "The prohibition itself stays true — a BARE `graphify install` with no `--project` and no `CLAUDE_CONFIG_DIR` still writes `~/.claude/CLAUDE.md` (install.py:651-652) — but the stated reason 'that write is hardcoded' is false at the version this repo pins, and it is the reason the rule escalates to 'throwaway directory outside this repo'. Correct the parenthetical to: `CLAUDE_CONFIG_DIR` redirects both the skill dir and the CLAUDE.md registration since 0.9.44, so it IS containment for the profile write; the residual hazard is the codex/AGENTS.md path (which `--project` does not fix and our size gate rejects), and that is what still justifies the throwaway-dir escalation. Leaving the stale half in place means the next session either distrusts a working containment mechanism or distrusts the whole entry."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-do-not-3"
unit = "rule-do-not"
kind = "rule"
disposition = "refactor"
confidence = 0.72
custom_thing = "Entries 2, 3 and 5 (`mise run build` / `docker buildx bake dev-load` locally; raw `docker run/exec/stop/rm/build` for devcontainer lifecycle; bulk `git add .`) are enforced only by prose in this ADR plus the PreToolUse `hook_guard`, which this repo documents as failing OPEN on its own errors."
evidence = ".claude/rules/do-not.md:13-14 (entry 2), :16-18 (entry 3), :23-24 (entry 5); the fail-open property is stated in .claude/rules/mise-tasks-only.md § 'Enforcement layers' (\"The hook fails OPEN on its own errors ... Hard bans that must never fail open belong in settings.json permission deny rules, not the hook\"); the repo already uses deny rules for exactly this shape at .claude/settings.json:23-39 (`Bash(chezmoi apply:*)`, the plan-attest set)."
native_alternative = "`permissions.deny` Bash pattern rules in `.claude/settings.json` — e.g. `Bash(docker buildx bake dev-load*)`, `Bash(mise run build)`, `Bash(docker run:*)`, `Bash(git add .)`. Deny rules are evaluated before allow rules, block in EVERY permission mode including `bypassPermissions`, and cannot be overridden by a PreToolUse hook returning `allow`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permission-modes.md:30 (\"Deny rules block in every mode, including `bypassPermissions`. Allow rules have no effect in `bypassPermissions`.\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:495 (\"Hook decisions don't bypass permission rules ... a matching deny rule blocks the call\") and :68 (scoped rule semantics, `Bash(rm *)`)."
rationale = "These three entries are hard, never-bend invariants with a stable command shape — precisely the class the repo's own doctrine says belongs in `permissions.deny` rather than in a fail-open hook or in prose. The native mechanism exists, is already used in this settings file for chezmoi and plan-attest, and is strictly stronger (deny-first precedence, survives bypassPermissions, survives a broken guard script). Entry 3 needs a scoped rule, not a bare `Bash` deny, because `docker ps/logs/info` must stay allowed — write it as narrow verb patterns and keep the prose entry as the WHY. This is an adopt-the-native-mechanism refactor, not a retirement: the ADR text still has to explain the invariant."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-do-not-4"
unit = "rule-do-not"
kind = "rule"
disposition = "refactor"
confidence = 0.62
custom_thing = "Entry 8's prescribed habit for containing a third-party installer's writes: 'Run any `graphify <platform> install` in a **throwaway directory outside this repo**, never here' — a remembered human/agent procedure with no machine layer."
evidence = ".claude/rules/do-not.md:40-43."
native_alternative = "Sandbox filesystem isolation: `sandbox.filesystem.denyWrite` (e.g. `[\"~/.claude\"]`) is enforced at the OS level for every sandboxed Bash command AND its child processes — which is the only layer that can stop graphify, since it is a Python subprocess writing files itself and Read/Edit deny rules explicitly do not reach those."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sandboxing.md:190 (\"You can also deny write or read access using `sandbox.filesystem.denyWrite` and `sandbox.filesystem.denyRead`\") and :172 (\"These paths are enforced at the OS level, so all commands running inside the sandbox, including their child processes, respect them\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:319 (\"Read and Edit deny rules ... don't apply to arbitrary subprocesses that read or write files indirectly, like a Python or Node script that opens files itself. For OS-level enforcement that blocks all processes from accessing a path, enable the sandbox\")."
rationale = "The rule's own escalation ('throwaway directory') exists because no layer below it could catch a subprocess write to `~/.claude`. The harness now has exactly that layer, and the docs name this failure mode verbatim. Adopting it converts entry 8 from a remembered habit into a machine-enforced boundary that also covers every OTHER tool that writes outside the repo. It is a refactor rather than a retirement for two reasons: the sandbox only binds sandboxed Bash commands (a session run without `sandbox.enabled`, or a command that falls back to unsandboxed, is uncovered), and the codex/AGENTS.md half of entry 8 is a size-gate problem the sandbox does not address."
probe = "claude --print '/sandbox' is not scriptable; instead check the resolved config with: grep -n 'sandbox' /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.claude/settings.json ~/.claude/settings.json 2>/dev/null; then arm both directions in a scratch dir with sandbox.filesystem.denyWrite=['~/.claude'] — a write to ~/.claude/zz-probe-$(date +%s) must fail while a write to the cwd succeeds."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-do-not-5"
unit = "rule-do-not"
kind = "rule"
disposition = "refactor"
confidence = 0.55
custom_thing = "Entry 9's branch-first enforcement stack — the custom PreToolUse `branch_guard` denying `Edit`/`Write`/`NotebookEdit` on a repo file while on the default branch, plus the manual recovery recipe (`git branch <new> && git reset --hard origin/main`)."
evidence = ".claude/rules/do-not.md:45-66 (entry 9; the guard at :58-62, the recovery at :47-49)."
native_alternative = "Git worktrees are now first-class in the harness: `claude --worktree <name>` starts the session in `.claude/worktrees/<name>/` on a NEW branch `worktree-<name>`, and mid-session Claude can create one itself with the `EnterWorktree` tool. A session started that way structurally cannot write to the default branch's checkout, so 'branch BEFORE the first edit' becomes the session's starting state rather than a deny that fires after the fact."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/worktrees.md:21 (\"Pass `--worktree` or `-w` with a name to create an isolated worktree and start Claude in it ... on a new branch named `worktree-<name>`\"), :41 (\"You can also ask Claude to 'work in a worktree' during a session, and it creates one with the `EnterWorktree` tool\"), :12 (desktop app gives every new session its own worktree automatically)."
rationale = "Native worktrees cover the ENTRY path that entry 9 says keeps failing — the two recorded relapses both happened straight after `mise run land`, which leaves the session on `main`; a `--worktree` launch would have made that state unreachable. They do not cover the whole rule: a session legitimately launched in the main checkout still needs `branch_guard`, and the branch name the harness picks (`worktree-<name>`) is not the repo's `feat/...` convention, so `ship` would need to tolerate or rename it. So: adopt `--worktree` as the documented default launch for implementation work and keep the guard as the backstop — do not retire the guard. One caveat the ADR must carry if it adopts this: hook `${CLAUDE_PROJECT_DIR}` does NOT follow the worktree (worktrees.md:46-49), and every guard in this repo is invoked as `bash \"${CLAUDE_PROJECT_DIR:-.}/scripts/...\"` (.claude/settings.json:56-58) — the #343 fail-open-off-root defect class."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && claude -p --worktree audit-probe 'run: git rev-parse --abbrev-ref HEAD; git rev-parse --show-toplevel; echo CLAUDE_PROJECT_DIR=$CLAUDE_PROJECT_DIR' — confirms the branch is non-default AND whether the PreToolUse guard path still resolves from inside .claude/worktrees/."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-do-not-6"
unit = "rule-do-not"
kind = "rule"
disposition = "keep"
confidence = 0.66
custom_thing = "Entry 10's mechanism for keeping credentials out of artifacts: a prose ban on writing an environment dump into a tracked file, backed by the repo-local `no_env_dump` hk step."
evidence = ".claude/rules/do-not.md:68-74."
native_alternative = "`CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1` strips credentials from Bash-tool, hook and stdio-MCP subprocess environments (Anthropic + cloud-provider credentials, anything Claude Code recognises as a credential, and credentials embedded in registry URLs), so a dump run from a Bash tool call would contain fewer live values; `sandbox.credentials` can unset or mask specific variables for sandboxed commands."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/env-vars.md:366 (`CLAUDE_CODE_SUBPROCESS_ENV_SCRUB` full description); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sandboxing.md:315 (\"`sandbox.credentials` affects sandboxed Bash commands only. To strip credentials from all subprocesses regardless of sandboxing, set `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB`\") and :687."
rationale = "Keep entry 10 as written, and do NOT adopt the native scrub here. Justification, not default: (a) the native feature solves a different problem — it reduces what a subprocess can READ, while entry 10 governs what lands in a TRACKED FILE, which no harness setting inspects; (b) the scrub only removes variables Claude Code recognises as credentials, and this repo's exposure is ~56 fnox/Doppler-provided names of arbitrary spelling (.claude/rules/secrets-out-of-the-shell-env.md), so it would leave most of them in a dump while making the dump look sanitised — the 'clean scanner means ask what it can see' failure mode; (c) enabling it directly contradicts Ray's deliberate 2026-08-02 reversal that credentials be ambient to every terminal and agent. The `__MISE_DIFF` zlib+base64 packing that makes such a dump unscannable is a mise property with no harness counterpart, so `no_env_dump` remains the only layer that sees it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-do-not-7"
unit = "rule-do-not"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "Entries 1 (no dock launch of CLion/VS Code for devcontainer work), 4 (no `2>/dev/null` in the Dockerfile), 6 (do not trust `gh run watch --exit-status`) and 7 (do not switch `docker context` off `desktop-linux`) — four invariants with no proposed native substitute."
evidence = ".claude/rules/do-not.md:7-11 (entry 1), :20-21 (entry 4), :26-27 (entry 6), :29-32 (entry 7)."
native_alternative = "None. Control-armed corpus sweep over /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/*.md: `initializeCommand` -> 0 files, `docker context` -> 0 files, `run watch` -> 0 files. Control arm on the same command shape: `devcontainer` -> 9 files, `worktree` -> 56 files, `EnterWorktree` -> 10 files, so the probe discriminates. The nearest native mechanism, the `Monitor` tool, watches a command's output lines but reads no workflow conclusion and cannot repair `gh`'s exit code."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:39 and :310 (`Monitor` tool: \"Runs a command in the background and feeds each output line back to Claude\" — output lines only, no CI conclusion semantics)."
rationale = "Required justification: these four bind surfaces the harness does not model. Entry 1 is macOS GUI process env inheritance reaching a devcontainer `initializeCommand` — the harness has no launcher-env feature and the string appears nowhere in its docs. Entry 4 is a repo verification contract (`build.no-stderr-suppression`) over Dockerfile bytes. Entry 6 is a defect in the `gh` CLI's own `--exit-status`, which no harness feature can correct; the harness's `Monitor` tool is a line-streaming watcher, not a workflow-conclusion reader, so substituting it would swap a known-flaky signal for an unrelated one. Entry 7 is a Docker Desktop vs Colima runtime property (`/run/host-services/ssh-auth.sock`). All four stay as prose invariants; entries 2/3/5 are the ones that can be hardened natively (see rule-do-not-3)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-gh-cli-watch-1"
unit = "rule-gh-cli-watch"
kind = "rule"
disposition = "refactor"
confidence = 0.88
custom_thing = "The rule's 'Canonical patterns' section prescribes the three commands an agent should run to wait on CI: `gh pr checks 123 --watch --interval 30`, `gh pr checks 123 --watch --fail-fast`, `gh run watch 1234567890 --exit-status`, and `gh pr checks --watch`."
evidence = ".claude/rules/gh-cli-watch.md:31, .claude/rules/gh-cli-watch.md:34, .claude/rules/gh-cli-watch.md:41, .claude/rules/gh-cli-watch.md:49"
native_alternative = "In-repo canonical mise tasks, machine-enforced: the live PreToolUse guard DENIES both `gh run watch` and `gh pr checks ... --watch` and redirects to `mise run ship` (watches PR checks to bucket-verified green) / `mise run land -- <PR#>` (watches main CI via --json buckets), leaving only the one-shot `gh pr checks <n> --json` and `gh run view <id> --json conclusion` reads allowed. The guard's own deny message cites THIS rule as its authority, but the rule never names the mise verbs."
alternative_evidence = "python/src/dotfiles_setup/hook_guard.py:404-412 (Rule \"gh run watch\", deny message names `mise run land -- <PR#>` and cites .claude/rules/gh-cli-watch.md); python/src/dotfiles_setup/hook_guard.py:413-421 (Rule \"gh pr checks --watch\", deny message names `mise run ship`); .claude/rules/mise-tasks-only.md table rows `gh run watch <id>` -> `mise run land -- <PR#>` and `gh pr checks ... --watch` -> ship/land already watch"
rationale = "Every command the eager rule holds up as the canonical thing to do is denied for agents by a guard that cites this rule as its justification. An agent that reads only this rule writes a command the harness rejects, and the deny cancels the whole compound command. The rule should lead with `mise run ship` / `mise run land` for agents, keep `gh ... --watch` explicitly scoped to a human's long-running terminal, and name the allowed one-shot JSON reads. Nothing upstream changed here; the repo's own mechanism moved out from under the rule."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-gh-cli-watch-2"
unit = "rule-gh-cli-watch"
kind = "rule"
disposition = "refactor"
confidence = 0.78
custom_thing = "The rule treats Claude Code's `Monitor` tool as a footnote — a parenthetical pointer into the evidence file — while leading with a blocking foreground `gh ... --watch` as the agent-side mechanism for 'wait until CI is done, tell me when'."
evidence = ".claude/rules/gh-cli-watch.md:21-23 ('when Claude Code's `Monitor` tool is the right tool instead ...: docs/rules-evidence/gh-cli-watch.md'); docs/rules-evidence/gh-cli-watch.md section 'When Claude Code's `Monitor` tool is the right tool instead' restricts it to three narrow cases"
native_alternative = "The harness `Monitor` tool is now a first-class, documented tool whose FIRST-CLASS listed use case is exactly this job: 'Poll a PR or CI job and report when its status changes'. It runs the command in the background, feeds each output line back to Claude as an event, honours the same permission rules as Bash, and is cancellable via TaskStop. Paired with Bash `run_in_background: true` + `/tasks`, it is the harness-native shape for a long wait — and this host's own Bash tool description instructs 'use Monitor with an until-loop to wait on a condition'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:310 (## Monitor tool), :315 ('Poll a PR or CI job and report when its status changes'), :324 (Monitor uses the same permission rules as Bash); tools-reference.md:39 (tool table row for Monitor); tools-reference.md:172-176 (Background commands, run_in_background + /tasks + auto-backgrounding at timeout); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permission-modes.md:433 (`Monitor` allow rules)"
rationale = "When the rule was written, Monitor was an exotic alternative worth three narrow carve-outs. At 2.1.266 it is a documented tool whose canonical example IS 'poll a PR or CI job', it is subject to Bash permission rules (so the repo's existing guard posture still applies to it), and the harness's own Bash guidance points at it for condition-waits. The rule should promote Monitor from a footnote to a named agent-side option alongside the mise verbs, rather than describing it as the exception. Not a 'retire': `gh --watch` remains correct for a human terminal and inside CI scripts, which Monitor does not serve."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "rule-gh-cli-watch-3"
unit = "rule-gh-cli-watch"
kind = "rule"
disposition = "refactor"
confidence = 0.62
custom_thing = "The rule's headline ban on hand-rolled `while ! gh ... ; do sleep 30; done` poll loops, and its anti-pattern block, stated unconditionally for 'all scripts, hooks, skills, agents, and ad-hoc Bash invocations in this repo'."
evidence = ".claude/rules/gh-cli-watch.md:3-5, .claude/rules/gh-cli-watch.md:52-59, .claude/rules/gh-cli-watch.md:64-68"
native_alternative = "Partially covered natively, but NOT by a block: Claude Code never auto-backgrounds a command that starts with `sleep` (it stops it at the timeout instead), so a detached sleep-poll degrades rather than being refused — control-armed live on this host, `sleep 1; echo rc=$?` returned rc=0, so a leading `sleep` is executed, not denied. The genuine native substitute for the wait-on-a-condition shape is the `Monitor` tool (see finding 2). The pipefail half of the ban (`cmd | grep -q PAT` returning 141) has no native cover and stays repo-enforced by hk."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:179-181 ('Claude Code never auto-backgrounds three kinds of command. It stops them at the timeout instead: * A command that starts with `sleep`.'); tools-reference.md:315 (Monitor polls a PR or CI job); hk.pkl:232 (`no_grep_q_under_pipefail` step, still present)"
rationale = "The unconditional ban now contradicts a sibling eager rule: .claude/rules/long-running-command-hangs.md:36-43 PRESCRIBES an in-turn `while [ $SECONDS -lt $deadline ]; do grep -q RC \"$LOG\" && break; sleep 15; done` poll for Mac-side container ops, because backgrounding gets those reaped. Two eager rules, opposite instructions, no carve-out in either. The harness has not made the sleep-poll safe (probe above: it runs, it is merely never auto-backgrounded), so the fix is not to relax the ban but to scope it — 'never poll a GHA run/PR when gh has a --watch flag or a mise verb; for a local condition-wait use Monitor, or the bounded in-turn poll that long-running-command-hangs.md defines' — and cross-link the two rules."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-gh-cli-watch-4"
unit = "rule-gh-cli-watch"
kind = "rule"
disposition = "keep"
confidence = 0.90
custom_thing = "The rule's factual assertions and every anchor it cites: the `gh` flags (`--watch`, `--fail-fast`, `--interval`, `--exit-status`), the `no_grep_q_under_pipefail` hk step, the exit-141-under-pipefail claim, the evidence file, and the four 'See also' pointers."
evidence = ".claude/rules/gh-cli-watch.md:14-16, :18-19, :23, :31-49, :70-78"
native_alternative = "None needed — every anchor still resolves at the host's versions, and no native feature has superseded the facts themselves. Verified: gh 2.100.0 (2026-09-03) still defines `--watch` and `--fail-fast` on `gh pr checks` and `--exit-status` on `gh run watch`; the premature-exit caveat still has no upstream fix (the watch loop returns on `run.Conclusion != Success` with no completion re-verification, so cross-verifying with `gh run view --json conclusion` remains the only mitigation); hk.pkl still carries the step; the evidence file, the workflows AGENTS.md pointer, and all three named auto-memory files exist."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh/pkg/cmd/pr/checks/checks.go:118-119 (--watch, --fail-fast); .../gh/pkg/cmd/run/watch/watch.go:88 (--exit-status), :141 and :205-208 (exit-status returns SilentError on non-success, no re-verification); hk.pkl:232; docs/rules-evidence/gh-cli-watch.md (3191 B, present); .github/workflows/AGENTS.md:146-148 (cites this rule as Authority); /Users/rmanaloto/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/memory/{feedback_gh_run_watch.md,feedback_gh_cli_watch_flag.md,feedback_long_running_tail_logs.md} all present"
rationale = "Required justification for 'keep': this rule asserts no Claude Code harness fact (no hook event, settings key, precedence claim, or $CC anchor), so axis (a) has nothing to go stale against 2.1.266. Its gh-CLI facts were re-derived against the pinned gh source at the installed 2.100.0 and all hold, and the `--exit-status` cross-verify habit has no native replacement — GitHub has shipped nothing that makes a single `gh run watch` exit code trustworthy, and the harness offers no equivalent. The factual layer of the rule stays as written; only the prescriptive layer moves (findings 1-3)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-goal-history-1"
unit = "rule-goal-history"
kind = "rule"
disposition = "refactor"
confidence = 0.82
custom_thing = "The one-writer coordination protocol, declared wholly manual: \"One repository has one implementation writer by default... If a Desktop task restarts while a fallback subagent owns the same repository, stop one writer before either mutates files; never infer disjointness from different task or worktree names.\" plus the closing \"The one-writer restart protocol is manual by design.\""
evidence = ".claude/rules/goal-history.md:19-22 and .claude/rules/goal-history.md:29 (the sentence is pinned as a literal contract token at python/verification/suites.toml:2335, so changing it requires editing that per_path_tokens entry in the same diff). No hook enforces it: .claude/settings.json wires PreToolUse, PostToolUse, SessionStart, SessionEnd and InstructionsLoaded only — no Stop, SubagentStart, WorktreeCreate or TaskCreated handler."
native_alternative = "Claude Code worktree isolation, which makes writer disjointness mechanically enforced rather than inferred: `isolation: worktree` on a subagent's frontmatter; a four-check block set (Edit/Write/NotebookEdit into the main checkout, Bash/PowerShell/Monitor cwd resolving into it, git redirects via `git -C`/`--git-dir`/`GIT_DIR`/`GIT_WORK_TREE`/`cd`, and unverifiable command shapes) applied to the session and every subagent it spawns; a `git worktree lock` held for the whole life of an agent or backgrounded session; per-session automatic worktrees in the desktop app; and a per-task worktree toggle for Desktop scheduled tasks."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:305 (`isolation` frontmatter field); worktrees.md:83-95 (How Claude Code enforces isolation — the four checks; \"The same enforcement covers every subagent Claude spawns from the isolated session\"); worktrees.md:100 (Isolate subagents with worktrees); worktrees.md:132 (\"While an agent is running, Claude Code holds a `git worktree lock` on its worktree\"); desktop.md:323 (\"For Git repositories, each session gets its own isolated copy of your project using Git worktrees\"); desktop-scheduled-tasks.md:34 (\"By default, scheduled tasks run against whatever state your working directory is in, including uncommitted changes. Enable the worktree toggle when creating the task to give each run its own isolated Git worktree\")."
rationale = "The rule states the protocol is manual *by design*, but at 2.1.266 the harness ships the mechanical layer the protocol was hand-simulating, and the rule names none of it. Two concrete adoptions: (i) any fallback subagent that may write should carry `isolation: worktree` (sub-agents.md:305), after which the harness BLOCKS its writes into the main checkout (worktrees.md:89-92) and holds a git lock for its lifetime (worktrees.md:132) — disjointness becomes enforced, not asserted; (ii) the Desktop half is a documented one-toggle fix (desktop-scheduled-tasks.md:34), and unattended scheduled tasks are exactly the restart case the rule describes. What survives, and why this is refactor rather than retire: the harness enforces isolation only for sessions IT isolated, the scheduled-task toggle is OFF by default, and Claude in a desktop session cannot see terminal sessions \"even in worktrees of the same project\" (desktop.md:359) — so the residual clause \"never infer disjointness from different task or worktree names\" stays correct for non-isolated writers. The rule should name the native toggles as the primary control and scope the manual protocol to writers the harness is not isolating. Note the suites.toml token pin above before rewording."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "rule-goal-history-2"
unit = "rule-goal-history"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "The append trigger is a manual habit: \"After an accepted goal change, orchestration-topology change, major milestone, landing, or handoff, append one iteration to `docs/agents/goal-history.md` before advancing.\" Nothing fires at the moment of advancing; the validator only runs when a human invokes `mise run session-review`."
evidence = ".claude/rules/goal-history.md:3-5 (the directive); mise.toml:1478,1493 ([tasks.session-review] -> `dotfiles-setup session-review`, operator-invoked); .claude/skills/session-review/SKILL.md:10-14 (invocation is a manual command list); .claude/settings.json hooks block contains PreToolUse, PostToolUse, SessionStart, SessionEnd, InstructionsLoaded and NO Stop entry, so no per-turn gate exists."
native_alternative = "A `Stop` hook, the only per-turn event with decision control — it can refuse to end the turn and feed a reason back to Claude. For a judgment-shaped trigger (\"did this turn accept a goal change, topology change, landing or handoff?\") the native `type: \"prompt\"` hook form evaluates the condition on a model and returns `ok:false` + `reason`, which is fed back so Claude keeps working; `impossible: true` is the documented escape so the turn can still end."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2458 (### Stop — \"Runs when the main Claude Code agent has finished responding\"); hooks.md:2530 (#### Stop decision control — \"`Stop` and `SubagentStop` hooks can control whether Claude continues\"); hooks-guide.md:841-850 (## Prompt-based hooks — `type: \"prompt\"`, model-evaluated, `ok:false` on Stop feeds the reason back unless `impossible:true`); hooks.md:1025 (SessionEnd has \"No decision control\", so the repo's existing SessionEnd hook could not carry this)."
rationale = "The rule's enforcement story is strong for CONTENT (the byte-level validator, finding 3) and empty for TIMING: `before advancing` is carried by prose alone, and the session's own hook config proves no per-turn gate is wired. `Stop` is the documented event for exactly this — it is the only per-turn hook that can block, and its prompt-based form handles the judgment trigger that no deterministic matcher can express (the same machinery the harness itself uses for `/goal`, per goal.md:120 \"`/goal` is a wrapper around a session-scoped prompt-based Stop hook\"). Confidence is held at 0.6 rather than higher because a naive Stop hook fires on every turn and most turns are not goal changes, so the adoption is a prompt-based hook with a narrow condition (and an `impossible` escape), not a blanket blocker — a design decision, not a mechanical swap."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-goal-history-3"
unit = "rule-goal-history"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "The append-only enforcement mechanism itself: a custom Git walk that resolves the fixed `origin/main` merge-base, replays every first-parent revision in order checking each blob is a byte-prefix extension of the last, then checks the working tree, plus a structural/digest-chain validator over the nine required field labels."
evidence = "python/src/dotfiles_setup/session_review.py:445-470 (goal_history_errors — structural + digest chain), :480-482 (_authorized_goal_history_base — merge-base HEAD origin/main, no caller override parameter), :500-521 (_committed_append_only_error — `rev-list --first-parent --reverse`, `blob.startswith(previous)`), :523-545 (_append_only_error — fails closed with \"cannot resolve the fixed origin/main goal-history baseline\"); asserted as a seam by python/verification/suites.toml:2322-2337."
native_alternative = "None found. Nothing in the Claude Code harness, and nothing in git or the pinned toolchain, validates that a specific tracked file is byte-append-only across a branch's first-parent history. The nearest native harness concept, `/goal`, is a session-scoped completion condition evaluated by a small fast model, restored across resume and recorded only in the transcript — it produces no tracked artifact, no digest chain, and no cross-session record."
alternative_evidence = "Control-armed corpus search over /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/*.md: `grep -rn -i \"append-only|append only|immutable log|audit log\"` returns only unrelated hits — agent-sdk__session-storage.md:101 (delete is a no-op for append-only session backends), claude-apps-gateway-deploy.md:174 (gateway DB migrations), hooks.md:1294 / hooks-guide.md:343 (InstructionsLoaded and PostToolUse used for audit LOGGING, no validation) — and zero hits describing an append-only file gate. Control arm, same command shape: `grep -rln \"isolation: worktree\" *.md` returns 7 files (changelog.md, agent-view.md, claude-directory.md, glossary.md, sub-agents.md, tools-reference.md, worktrees.md), so the probe discriminates. Native `/goal` semantics: goal.md:9 (session completion condition), goal.md:38 (\"One goal can be active per session\"), goal.md:100-104 (restored on resume, turn count reset), goal.md:120-124 (evaluator verdicts recorded in the transcript)."
rationale = "Required justification: the capability is \"prove a tracked markdown file was only ever appended to, relative to a baseline the branch cannot choose for itself\". No harness feature, no git feature, and no pinned tool provides it — GitHub rulesets gate refs and paths, not per-file byte prefixes, and a hook can log a change (hooks.md:1294) but has no decision control at SessionEnd/InstructionsLoaded and no history-walking primitive at any event. The one native feature that shares the word \"goal\" solves a different problem (in-session completion condition, transcript-only). The custom validator also already satisfies this repo's own bar: it ignores caller-supplied baselines and fails closed when the baseline cannot be resolved (session_review.py:526-528), and its FAIL direction is armed by tests/test_session_review.py (rewrite-behind-an-unrelated-commit, branch-appended-entry rewrite, rewritten bootstrap, missing history). Keep it as-is."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-goal-history-4"
unit = "rule-goal-history"
kind = "rule"
disposition = "refactor"
confidence = 0.50
custom_thing = "The rule's unqualified use of \"goal\" — \"an accepted goal change\", \"Prior goal digest\", \"Current goal digest\", \"the current goal text\", and the artifact name `docs/agents/goal-history.md` — written before the harness shipped a command of the same name and with different semantics."
evidence = ".claude/rules/goal-history.md:3, :7-12 (field labels `Prior goal digest` / `Current goal digest`, \"A digest identifies exact goal text\"), :24-27 (\"distinguish an accepted decision from verified delivery\")."
native_alternative = "The native `/goal` command: a session-scoped completion condition (one per session) evaluated after every turn by the small fast model, with `/goal` (status), `/goal clear`, an `◎ /goal active` indicator, restoration across `--continue`/`--resume`, and achieved/failed entries written to the transcript."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/goal.md:9 (\"The `/goal` command sets a completion condition...\"), goal.md:38 (\"One goal can be active per session\"), goal.md:68-86 (status output), goal.md:88-98 (`/goal clear`), goal.md:100-104 (restore on resume), goal.md:120-124 (evaluator verdicts recorded in the transcript)."
rationale = "Not a superseded mechanism — the two things do not overlap in function (finding 3 establishes that) — but a live ambiguity introduced by the harness after this rule was written. An agent that reads \"append one iteration ... after an accepted goal change\" with `/goal` in context can plausibly read the rule as being about session goal state, or conversely can run `/goal` and believe the durable record was updated. The cheap fix is one disambiguating clause in the rule (\"goal here means the repo's tracked objective, not the session-scoped `/goal` completion condition\"), which costs a few bytes against the rule_unscoped budget and removes the collision. Held at 0.5 because this is a clarity finding, not a defect: nothing currently mis-fires, and the rule's own text is internally consistent."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-graphify-first-1"
unit = "rule-graphify-first"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "The rule's headline dated fact: \"**The two installs are aligned as of 2026-08-31 — both 0.9.53.**\" (.claude/rules/graphify-first.md:16-17), with the whole \"Nothing records WHICH graphify built the graph\" section resting on the two installs currently agreeing."
evidence = ".claude/rules/graphify-first.md:16-23; the repo pin it names is python/pyproject.toml:9 `\"graphifyy[all]==0.9.53\"` and python/pyproject.toml:57 `override-dependencies = [\"graphifyy[all]==0.9.53\"]`."
native_alternative = "No native harness feature — this is a stale FACT, not a superseded mechanism. The user-global pin has moved to 0.9.56 while the repo pin stayed at 0.9.53, so the alignment the rule records has ended and the divergence the rule warns about is live today."
alternative_evidence = "/Users/rmanaloto/.config/mise/config.toml:290 `\"pipx:graphifyy\" = { version = \"0.9.56\", extras = [\"all\"], minimum_release_age = \"0s\" }`. Live probe (control-armed): `command -v graphify` -> `/Users/rmanaloto/.local/share/mise/installs/pipx-graphifyy/0.9.56/bin/graphify` (positive arm `command -v mise` -> resolves; negative arm `command -v qzzxwv7` -> ABSENT), so bare-PATH graphify is 0.9.56 vs the repo's 0.9.53. Upstream latest is also 0.9.56 per .agent/kb/raw/graphify-readme-0.9.56.digest.md (0 new capabilities)."
rationale = "The rule explicitly says to treat the alignment as \"a fact with a date on it, not an invariant\" — and it has now expired. Rewrite lines 16-23 to state the CURRENT state (PATH 0.9.56 vs repo 0.9.53, diverged) or bump python/pyproject.toml to 0.9.56 to restore alignment. Leaving it as-is is worse than silence: a reader who trusts the sentence concludes a bare `graphify update .` would at least have built the graph with the same version, which is now false. Note the bump is blocked by finding 5's hardcoded literal."
probe = "grep -n graphify /Users/rmanaloto/.config/mise/config.toml && grep -n graphifyy /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/python/pyproject.toml && command -v graphify"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-graphify-first-2"
unit = "rule-graphify-first"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "The ban on the bare binary is carried by prose alone: \"**Always the mise tasks, never a bare `graphify` on `PATH`.**\" and \"Never run a global Graphify binary or installer as a substitute for the project tasks\" — self-described as \"**procedural, not enforced**\" and enforced \"by convention, not by verification\"."
evidence = ".claude/rules/graphify-first.md:10-12, :36-43. Control-armed: `grep -rn graphify python/src/dotfiles_setup/hook_guard.py` returns 0 hits (the PreToolUse guard that denies `gh pr merge`, `--no-verify`, etc. has no graphify rule), and .claude/settings.json's `permissions.deny` (line 23ff) lists chezmoi/plan-attest entries but nothing for graphify."
native_alternative = "A native `permissions.deny` rule — `\"Bash(graphify *)\"` plus the bare `\"Bash(graphify)\"` — which blocks deterministically in EVERY permission mode, including bypassPermissions, and needs no custom code. The repo already uses exactly this idiom for the same class of ban (`Bash(chezmoi apply:*)`)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permission-modes.md:30 \"Deny rules block in every mode, including `bypassPermissions`.\"; permissions.md:148-151 (wildcard form, `*` after the program/subcommand); permissions.md:235 \"A deny or ask rule matches past any leading assignment, so `Bash(rm *)` in deny still matches `FOO=bar rm -rf tmp/`\"; permissions.md:239-241 confirms environment runners (`mise exec`, `npx`, `uv`-style wrappers) are NOT stripped, so the sanctioned routes `mise run graphify-*` and `uv run --project python dotfiles-setup graphify *` do not match the rule and keep working. Repo precedent: .claude/settings.json:23-25."
rationale = "The rule states its own weakness (\"procedural, not enforced\") but a native, zero-code, mode-proof enforcement layer exists and is already in use elsewhere in this same settings file. Finding 1 makes the gap consequential rather than theoretical: with PATH graphify now at 0.9.56 and the repo at 0.9.53, one bare `graphify update .` silently produces graph bytes from a different builder, and by the rule's own account nothing records it. Add the deny pair to .claude/settings.json and have the rule cite it instead of asserting convention."
probe = "python3 -c \"import json;print(json.load(open('.claude/settings.json'))['permissions']['deny'])\"  # then, after adding the rule, control-arm both directions: a bare `graphify --version` must be denied while `mise run graphify-health` still runs"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-graphify-first-3"
unit = "rule-graphify-first"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "The rule presents its opening directive — \"Before broad source search, run `mise run graphify-health`\" — as a manual habit, and says the guarantee is \"procedural, not enforced\". It never mentions that a PreToolUse hook layer now injects that orientation automatically on every search and read."
evidence = ".claude/rules/graphify-first.md:3-9 and :36-43 (no reference anywhere in the file to a hook, to scripts/graphify-hook-guard.sh, or to strict mode)."
native_alternative = "The native `PreToolUse` hook event with `hookSpecificOutput.additionalContext` — already wired in this repo on the `Bash|Grep` and `Read|Glob` matchers to `scripts/graphify-hook-guard.sh`, which execs graphify's own native `hook-guard` subcommand and rewrites its nudge copy to this repo's mise tasks. Its escalation path is also native and config-only: strict mode via the `GRAPHIFY_HOOK_STRICT`/`_TTL` env vars set in settings.json's `env` block, no code change."
alternative_evidence = ".claude/settings.json:64-68 (`\"matcher\": \"Bash|Grep\"` -> `graphify-hook-guard.sh search`) and :74-78 (`\"matcher\": \"Read|Glob\"` -> `graphify-hook-guard.sh read`); scripts/graphify-hook-guard.sh:1-30 (thin fail-open wrapper; line 16 documents `GRAPHIFY_HOOK_STRICT/_TTL` as an env var, not a code change); python/src/dotfiles_setup/graphify.py:564-575 `rewrite_hook_nudge`. Harness doc: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:972 \"The `additionalContext` field passes a string from your hook into Claude's context window... inserts it into the conversation at the point where the hook fired.\" Observed live this session: every Bash call returned `PreToolUse:Bash hook additional context: MANDATORY: graphify-out/graph.json exists. You MUST run mise run graphify-query...`."
rationale = "The rule is stale about its own enforcement topology: what it describes as an unaided habit is already carried, per-tool-call, by a native hook the repo wires — and a reader following the file alone would not know the layer exists, nor that strict mode is one settings key away. This is the load-bearing half of the rule that IS now mechanized; the un-mechanized half is finding 2's bare-binary ban. Add a short \"Enforcement layers\" paragraph naming the hook and strict mode, so the eager bytes describe the real system."
probe = "sed -n '60,80p' .claude/settings.json && bash scripts/graphify-hook-guard.sh search </dev/null"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-graphify-first-4"
unit = "rule-graphify-first"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "The rule correctly records that the self-authored builder-version stamp was REMOVED (\"An earlier version of this rule claimed a rebuild stamp closed that gap; it did not... It was removed rather than kept as a check that always reports 'fine'\"), but the mise task the rule points readers at still advertises the stamp as present."
evidence = ".claude/rules/graphify-first.md:29-34 (stamp removed) and python/src/dotfiles_setup/graphify.py:145-177 `_receipt_problem` docstring (\"An earlier version of this fix tried to restore (2) with a self-authored stamp written by ``update()``... Removed rather than kept as decoration\") plus graphify.py:536-549 `update()`, whose body is a single `_run([\"graphify\", \"update\", target])` that writes no stamp. Contradicted by mise.toml:738 `description = \"Rebuild the project knowledge graph and stamp the graphify version that built it\"` and mise.toml:744-745 \"...and records the builder version so graphify_health can catch a graph built by the wrong binary.\""
native_alternative = "No native feature is involved — this is stale wiring copy that must follow the code. The native surface that DOES exist and is correctly used is `mise run graphify-update`'s task description, which is what `mise tasks`/`mise run --help` shows the operator; it currently advertises a capability the implementation does not have."
alternative_evidence = "python/src/dotfiles_setup/graphify.py:536-549 (`update()` returns `_run([\"graphify\", \"update\", target], cwd=project_root)` — no stamp write anywhere in the module; `grep -n stamp python/src/dotfiles_setup/graphify.py` matches only the docstrings explaining the removal, never a write)."
rationale = "A rule and the task it sanctions assert opposite things about the same mechanism, and the mise description is the surface an operator reads first (`mise tasks`). By this repo's own tool-currency-and-native-first rule 5, retiring a mechanism means syncing every doc that describes it in the same change; that sync missed mise.toml. Fix the description and comment to say the stamp was deliberately removed and why, so the rule stops being contradicted by its own call site."
probe = "grep -n 'stamp\\|builder version' mise.toml python/src/dotfiles_setup/graphify.py"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-graphify-first-5"
unit = "rule-graphify-first"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "The `version drift` state the rule instructs you to act on (\"`missing`, `stale`, `corrupt`, version drift... say the graph is unavailable and fall back to source\") is decided by a hardcoded string literal duplicated out of pyproject.toml."
evidence = ".claude/rules/graphify-first.md:7-9 and :19-21 (\"`mise run graphify-query`/`graphify-update` resolve **this repo's pinned version** (`python/pyproject.toml`), which is what `graphify_health`'s `version drift` check compares against\"); the check is python/src/dotfiles_setup/graphify.py:235-236 `if runtime != \"0.9.53\": return HealthResult(GraphifyStatus.VERSION_DRIFT, runtime, \"expected 0.9.53\")` — a literal, not a read of the pin."
native_alternative = "Derive the expected version from the distribution's own declared requirement with stdlib `importlib.metadata` — the module already imports from it at graphify.py:20 (`from importlib.metadata import PackageNotFoundError, version`). `importlib.metadata.requires(\"dotfiles-setup\")` returns the pin verbatim, making pyproject.toml the single source of truth."
alternative_evidence = "Live probe (this repo's own venv): `uv run --project python python -c \"from importlib.metadata import requires; print([r for r in requires('dotfiles-setup') if 'graphify' in r])\"` -> `['graphifyy[all]==0.9.53']`, matching python/pyproject.toml:9 exactly. Existing native usage in the same file: graphify.py:81-83 `_runtime_version()` already calls `version(\"graphifyy\")`."
rationale = "The literal makes finding 1's remediation actively unsafe: bumping python/pyproject.toml to 0.9.56 to re-align with the user-global pin would leave graphify.py comparing against `\"0.9.53\"`, so `graphify_health` would report VERSION_DRIFT on the CORRECT version and the rule would then instruct every session to declare the graph unavailable and fall back to source. Reading the pin from package metadata removes the duplicate and is stdlib, already imported. No native harness feature is at stake here, but the fix is a native-library one rather than more custom code."
probe = "uv run --project python python -c \"from importlib.metadata import requires; print([r for r in requires('dotfiles-setup') if 'graphify' in r])\" && grep -n '0.9.53' python/src/dotfiles_setup/graphify.py"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-local-devcontainer-first-1"
unit = "rule-local-devcontainer-first"
kind = "rule"
disposition = "unknown"
confidence = 0.82
custom_thing = "The rule's headline cost figure: \"A CI base rebuild costs ~2.5h\" (.claude/rules/local-devcontainer-first.md:5-6), cited to the auto-memory note `feedback_ci_build_duration_baseline` and used as the whole economic argument for the rule."
evidence = ".claude/rules/local-devcontainer-first.md:5 — \"A CI base rebuild costs ~2.5h (`feedback_ci_build_duration_baseline`)\"; the SAME repo, describing the SAME event in the code this rule spawned, says 37 minutes: python/src/dotfiles_setup/pr.py:362 — \"it exists precisely to fail in 60s instead of after a ~37min CI base build\". mise.toml:817 repeats the rule's figure (\"~2.5h of CI\"). Two of this repo's own artifacts state different magnitudes for one number."
native_alternative = "No native feature — this is an inherited-number defect, settled by GitHub's own run-timing API (`gh run list`/`gh run view --json`), not by any harness capability."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/gh — `gh run view --json` is the first-party timing source; no doc anchor asserts either figure, which is the point: neither 2.5h nor 37min is currently backed by a measurement in-tree."
rationale = "Both numbers are inherited restatements with no re-derivation (probes-need-a-control-arm.md rule 6: an inherited number is not a measurement). They differ by ~4x, and the rule's entire cost/benefit argument (\"60s local vs 2.5h CI\") rests on the larger one. Whichever is right, one of the two in-tree statements is wrong and should be corrected in the same change; the rule should carry the figure WITH its condition (cold vs warm base leg) rather than a bare number."
probe = "gh run list --workflow ci.yml --limit 50 --json databaseId,conclusion,createdAt,updatedAt | jq '.[] | select(.conclusion==\"success\")' ; then for a run whose base-prep leg actually rebuilt: gh run view <id> --json jobs --jq '.jobs[] | select(.name|test(\"base-prep\")) | {name, startedAt, completedAt}' — report cold-base-leg wall time, and label the rule's figure with which leg it measures."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-local-devcontainer-first-2"
unit = "rule-local-devcontainer-first"
kind = "rule"
disposition = "refactor"
confidence = 0.72
custom_thing = "The hand-maintained \"Existing local tiers — use them, don't reinvent\" table (.claude/rules/local-devcontainer-first.md:34-42), a five-row transcription of mise task names + costs kept in sync by hand."
evidence = ".claude/rules/local-devcontainer-first.md:36-42 lists exactly verify-apt-pins, verify-container-latest, verify-local, renovate-dryrun, apt-repo. All five resolve (mise.toml:806, 941, 1048, 844, 679 — control arm: `[tasks.lint]` at mise.toml:207 found by the same grep shape). But two local tiers that answer precisely the rule's step-2 question are ABSENT from the table: `mise run verify-image` (mise.toml:375-397 — a throwaway `docker run --rm --pull=never` spot-check of cookbook paths/toolchains inside `:dev`, i.e. the exact 'ephemeral probe container' pattern the rule prescribes at :52-54) and `mise run smoke` (mise.toml:399 — tier 1-3 checks in the running devcontainer, the mechanism behind the table's own verify-container-latest row)."
native_alternative = "mise's own task registry as the source of truth instead of a transcribed table: `mise tasks ls --json` (and the `description` field each task already carries) enumerates every local tier with its description, so the rule can point at the registry rather than restate a subset that silently drifts."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/tasks/ls.md:2 (`mise tasks ls`), :18 (`-J --json` — Output in JSON format), :28 (sort/display by `description`); the descriptions that would be surfaced already exist, e.g. mise.toml:807 and mise.toml:376."
rationale = "The table's content is correct but incomplete, and incompleteness is the failure mode that matters here: a reader told 'use them, don't reinvent' who does not see `verify-image` will hand-roll exactly the `docker run --rm` the table was meant to prevent. Keep the two rows whose COST and CAVEAT are judgement (verify-apt-pins ~60s vs a base rebuild; verify-local ~20min), and replace the enumeration with a pointer to `mise tasks ls` plus the task descriptions, which cannot drift out of date."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-local-devcontainer-first-3"
unit = "rule-local-devcontainer-first"
kind = "rule"
disposition = "refactor"
confidence = 0.63
custom_thing = "The four-step gate at .claude/rules/local-devcontainer-first.md:19-29 is a manual habit for every image build input EXCEPT apt pins: name the failure mode, pick the container, run both arms, then push. Only the apt-pin tier has an automated trigger; for `.devcontainer/Dockerfile`, `docker-bake.hcl`, `mise-runtime.toml` and `mise-system.toml` there is no edit-time or push-time layer at all — ship deliberately SKIPS container validation for base-image inputs and defers to CI."
evidence = ".claude/rules/local-devcontainer-first.md:19-29 (the prose gate); python/src/dotfiles_setup/pr.py:363-366 — `if changes_apt_pin_inputs(paths): gates.append(Gate(\"verify-apt-pins\", ...))` followed by `if touches_surface(paths) and not changes_base_image_inputs(paths):`, i.e. base-image inputs get NO local container gate; .claude/settings.json wires only PreToolUse, PostToolUse, SessionStart, SessionEnd, InstructionsLoaded — no file-change-triggered hook exists in this repo."
native_alternative = "The `FileChanged` hook event (new since this rule was written), which fires on a filesystem watcher regardless of what wrote the file — an Edit/Write, a Bash command, or a process outside Claude Code — combined with the `watchPaths` output field (returnable from SessionStart/CwdChanged) to arm absolute paths like `.devcontainer/Dockerfile` and `docker-bake.hcl` that a working-directory-literal matcher cannot name."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:60 (\"`FileChanged` | When a watched file changes on disk. The `matcher` field specifies which filenames to watch\"); :2787-2793 (fires \"no matter what changed the file\"; matcher segments are registered as literal filenames in the working directory); :2828 (`watchPaths` for files you cannot name up front, seeded from SessionStart/CwdChanged); :1153 (`watchPaths` — \"Array of absolute paths to watch for FileChanged events\"); :1544 (\"Unlike PreToolUse, Claude Code runs FileChanged hooks after the change, and they have no decision control, so they can't block the write\"); :876 and :1025 (FileChanged: no decision control, stderr shown to user only). Control arm on the corpus: a known-absent token in the same file returns 0."
rationale = "Native covers PART of the gate, and precisely the part the repo lacks. `FileChanged` is an at-the-moment-of-edit trigger that no glob-scoped rule or ship-time gate provides, and by the corpus it cannot block (no decision control), which makes it the right shape for this rule: a nudge naming the cheapest probe for the file just touched ('you edited .devcontainer/Dockerfile — the cheapest arm is `mise run verify-image` / `mise run verify-apt-pins`'), not a new deny. The blocking half stays where it already is (ship's gate matrix, pr.py:363). Do NOT extend the PreToolUse guard to deny pushes for this: the rule itself concedes at :61-66 that base-image inputs cannot be validated locally, so a deny would redirect to nothing — the outage shape mise-tasks-only.md warns about."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-local-devcontainer-first-4"
unit = "rule-local-devcontainer-first"
kind = "rule"
disposition = "keep"
confidence = 0.88
custom_thing = "The rule as a whole: the doctrine 'reproduce the failure mode in a local container before spending a CI round-trip', plus its two judgement clauses — the dirty-container warning (:44-59, #288: the same apt pin failed in the devcontainer and passed in a clean base container) and the non-license clause (:61-66: a green local probe does not replace CI, does not license a local base build, and this arm64 Mac is not the amd64 image)."
evidence = ".claude/rules/local-devcontainer-first.md:1-15, :44-59, :61-73. Every internal citation still resolves: docs/rules-evidence/local-devcontainer-first.md, python/src/dotfiles_setup/apt_pins.py, and the four sibling rules all exist; do-not.md #2 is still 'no local base builds' and #3 is still 'no raw docker for devcontainer lifecycle', matching the numbering the rule cites at :54 and :63. The rule asserts NO Claude Code harness fact — no hook event, no settings key, no precedence claim, no `$CC/` anchor — so audit axis (a) has nothing stale in it. It is contract-asserted end-to-end by `workflow.apt-pins-enforcement` (python/verification/suites.toml:1911-1926, which binds the token 'mise run verify-apt-pins`' inside this very file)."
native_alternative = "None. Neither Claude Code 2.1.266 nor the pinned toolchain provides a substitute for the judgement this rule encodes."
alternative_evidence = "Gap digests read for this unit: .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 new capabilities — plugin-dir hot reload, output-size settings, /skill-doctor, managedMcpServers, --permission-prompts none, glab recognition, plugin validate --json; none touches local-vs-CI validation), .agent/kb/raw/claude-code-llms-index.digest.md (zero new capability pages), .agent/kb/raw/mise-releases-since-pin.digest.md (host already on the newest release; zero newer capabilities), .agent/kb/raw/hk-releases-since-pin.digest.md (only v1.58.1, explicitly documentation-only, zero new capabilities)."
rationale = "Required justification: the thing this rule carries is a cost-aware judgement — which environment can exhibit THIS failure mode, and whether the cheap environment is honest about it — and no harness feature decides that. The #288 measurement (a pin that FAILED in the devcontainer and PASSED in a clean base container, because apt refuses to downgrade) is a false-negative class no linter, hook or permission rule can detect; it is the reason the rule exists and it stays entirely un-automatable. The non-license clause is likewise a bound on evidence, not a mechanism. Keep the rule, with the three amendments above (correct/condition the cost figure, stop transcribing the tier list, add the FileChanged nudge for non-apt image inputs)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-long-running-command-hangs-1"
unit = "rule-long-running-command-hangs"
kind = "rule"
disposition = "refactor"
confidence = 0.88
custom_thing = "Rule 2 prescribes a hand-rolled in-turn polling loop as THE way to watch a long local command: `deadline=$((SECONDS+540)); while [ $SECONDS -lt $deadline ]; do grep -q RC \"$LOG\" && break; sleep 15; done`. The rule never mentions the Monitor tool."
evidence = ".claude/rules/long-running-command-hangs.md:37-43 (\"What works is **in-turn polling** — background the command, then keep the turn engaged reading its log\" + the bash while/sleep loop); .claude/rules/long-running-command-hangs.md:54-59 (the log path the loop is told to poll)"
native_alternative = "The built-in Monitor tool: runs a command in the background and feeds each output line back to Claude mid-conversation, explicitly for \"Tail a log file and flag errors as they appear\", \"Poll a PR or CI job and report when its status changes\" and \"Track output from any long-running script you point it at\", with \"You keep working in the same session and Claude interjects when an event arrives.\" It also inherits the Bash permission rules, so this repo's existing Bash allow/deny surface still applies. Secondarily, the Bash tool's own `run_in_background` re-invokes the session when the command exits, so no loop is needed to notice completion."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:39 (Monitor in the tool table); :310-324 (Monitor tool section, the four bullets above, \"Claude interjects when an event arrives\", and \"it uses the same permission rules as Bash\"); :173-175 (`run_in_background: true`, `/tasks`). The host's own Bash tool contract at 2.1.266 states \"Foreground `sleep` is blocked; use Monitor with an until-loop to wait on a condition\", and the docs corroborate the mechanism: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:179-181 — \"Claude Code never auto-backgrounds three kinds of command. It stops them at the timeout instead: * A command that starts with `sleep`.\""
rationale = "The rule's prescribed mechanism is now both superseded and partly hostile: Monitor does exactly what the loop does (background + line-by-line callback + no turn-blocking), and a foreground `sleep`-driven wait is the one shape the harness refuses to auto-background. Rule 2 should keep its EXCEPTION (Mac-side container ops must not go idle) but name Monitor as the mechanism, demoting the bash loop to a non-Claude-Code fallback (a human terminal, CI)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-long-running-command-hangs-2"
unit = "rule-long-running-command-hangs"
kind = "rule"
disposition = "refactor"
confidence = 0.72
custom_thing = "Rule 2's blanket claim that Mac-side container ops are REAPED when the turn goes idle: \"`mise run ship`/`land`, `verify-local`, `sync`, and image pulls are killed if the turn goes idle waiting on them, so 'background it' is precisely wrong here\"."
evidence = ".claude/rules/long-running-command-hangs.md:33-37"
native_alternative = "The lifetime rule is now documented and CONDITIONAL, and the documented condition contradicts the blanket form: a background command started by a FOREGROUND SUBAGENT ends at that subagent's final response, while \"Commands started by the main conversation or by a background subagent keep running.\" A third case is headless `-p`, where background tasks end shortly after the run's final result."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:175 — \"When a subagent running in the foreground started the command, Claude Code ends it when that subagent gives its final response. Commands started by the main conversation or by a background subagent keep running. In non-interactive mode with the `-p` flag, background tasks end shortly after the run's final result.\"; :177 (a timed-out command moved to the background inherits \"the same lifetime rules\")"
rationale = "As written the rule attributes the reaping to 'the turn going idle', which is not the documented predicate — the predicate is WHO started the command. A main-conversation `mise run ship` in the background is documented to survive; a subagent-launched one is not. Rewriting the exception in the harness's own terms makes it actionable (launch long Mac-side work from the main conversation, never from a foreground subagent) instead of a blanket prohibition. Confidence is short of 0.9 because the rule records a real observed reaping whose launch context it does not state, so the observation may be the foreground-subagent case rather than a doc contradiction."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-long-running-command-hangs-3"
unit = "rule-long-running-command-hangs"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "The rule's founding premise and headline — \"Any command that can block ... MUST be run with a hard time bound\", justified by a `hk` invocation that \"hung at 0% CPU ... for ~7 hours\" because nothing bounded it. The rule presents the out-of-process wrapper in `lint.py` as the only thing standing between an agent and an unbounded wait."
evidence = ".claude/rules/long-running-command-hangs.md:3-14 (headline + the 7-hour incident); :22-29 (rule 1, the wrapper as the bound)"
native_alternative = "Every Bash tool call already runs under a harness-enforced timeout the model cannot exceed — `BASH_DEFAULT_TIMEOUT_MS` (2 min out of the box when Claude passes none) and `BASH_MAX_TIMEOUT_MS` (10 min ceiling) — and on expiry Claude Code moves the command to the background rather than blocking the turn. An unbounded blind foreground wait is therefore unreachable through the Bash tool at 2.1.266."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:153-158 (\"Each command runs under a timeout, and Claude manages it\"; `BASH_DEFAULT_TIMEOUT_MS`; `BASH_MAX_TIMEOUT_MS`); :177 (\"When a command reaches its timeout without finishing, Claude Code moves it to the background instead of stopping it\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/env-vars.md:185,187"
rationale = "The wrapper is NOT redundant — the harness backgrounds a timed-out command, it does not kill hk's process group and it prints no debug-log tail, and the rule also binds CI jobs and a human's terminal where no harness exists. But the rule currently reads as if nothing bounds an agent's command, which is false at this host version and leads agents to over-engineer bounds the harness already supplies. Rule 1 should state the harness bound, then say precisely what it does not do (no process-group kill, no diagnostics, no coverage outside a Bash tool call) as the justification for keeping `lint.py`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-long-running-command-hangs-4"
unit = "rule-long-running-command-hangs"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "Rule 1 tells the reader to raise the lint ceiling with `--timeout <secs>` or `DOTFILES_LINT_TIMEOUT=<secs>` above the 600s default, with no mention that a Bash tool call has its own ceiling. The repo's `.claude/settings.json` `env` block does not set `BASH_MAX_TIMEOUT_MS`."
evidence = ".claude/rules/long-running-command-hangs.md:27-29; python/src/dotfiles_setup/lint.py:59-60 (`DEFAULT_TIMEOUT_SECONDS = 600`, `TIMEOUT_ENV_VAR = \"DOTFILES_LINT_TIMEOUT\"`); .claude/settings.json:3-12 — the `env` block holds 8 `CLAUDE_*`/`OTEL_*` keys and no `BASH_*` key (control arm: the same grep shape that returned rc=1 for `BASH_|TIMEOUT` returned the 8 keys for `\"env\"`, so the miss is real, not a broken probe)"
native_alternative = "`BASH_MAX_TIMEOUT_MS` in the settings `env` block raises the ceiling on what Claude may request for a Bash call; the documented default is 600000 ms, which is exactly the wrapper's 600s default, so any raised `DOTFILES_LINT_TIMEOUT` is unreachable from a Bash tool call until this key is set. `env-vars.md` shows the settings-file `env` form for `BASH_DEFAULT_TIMEOUT_MS`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/env-vars.md:187 — \"`BASH_MAX_TIMEOUT_MS` Maximum timeout the model can set for long-running bash commands (default: 600000, or 10 minutes). The effective ceiling is the larger of this and `BASH_DEFAULT_TIMEOUT_MS`\"; :85 (the settings.json `env` block form); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:158"
rationale = "Two ceilings sit on top of each other and only one is documented in the rule. An agent following rule 1 and raising `DOTFILES_LINT_TIMEOUT` to 1200 gets a run the harness hands back at 600s (auto-backgrounded), which reads as the wrapper failing. The fix is native and one line: add `BASH_MAX_TIMEOUT_MS` to the settings `env` block (per `.claude/CLAUDE.md` § DAG topology pins, `CLAUDE_*`-class pins go in settings `env`, never a shell export) and say so in rule 1."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-long-running-command-hangs-5"
unit = "rule-long-running-command-hangs"
kind = "rule"
disposition = "unknown"
confidence = 0.60
custom_thing = "Rule 3's prescribed capture shape: `cmd > /tmp/out.log 2>&1; echo \"rc=$?\" >> /tmp/out.log`, which the repo also promotes in `mise-tasks-only.md`'s canonical-task table."
evidence = ".claude/rules/long-running-command-hangs.md:66-68"
native_alternative = "Claude Code refuses to auto-background a compound command it cannot parse into simple commands, treating a parameter expansion as unparseable, and STOPS such a command at the timeout instead of backgrounding it — the doc's own example is a trailing `; exit \"${PIPESTATUS[0]}\"`. A `git`-containing command is likewise never auto-backgrounded. If `; echo \"rc=$?\"` falls in that class, the rule's own recommended shape converts a slow gate into a killed one — the exact failure the rule exists to prevent."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:179-183 — \"Claude Code never auto-backgrounds three kinds of command. It stops them at the timeout instead: * A command that starts with `sleep`. * A command that runs `git` anywhere in it. * A compound command Claude Code can't fully parse into simple commands. Claude Code treats a parameter expansion such as `${VAR}` as unparseable, so it stops a command that ends in `; exit \"${PIPESTATUS[0]}\"` at the timeout even when the rest of that command parses.\""
rationale = "The doc names `${VAR}` and `${PIPESTATUS[0]}` explicitly but not the bare special parameter `$?`, so whether rule 3's shape parses is not settled by the corpus. It matters: if it does not parse, every gate an agent runs through the rule's blessed pattern is stopped rather than backgrounded at the ceiling from finding 4, and the rc file is left half-written. Settle it with a live probe before rewriting rule 3."
probe = "Run two Bash tool calls with an explicit short timeout and compare the returned result text for 'moved to the background' vs a stop/kill. Arm A (parseable control, expected: backgrounded): Bash(command='/bin/sh -c \"sleep 30; echo done\" > /tmp/probeA.log 2>&1', timeout=5000). Arm B (the rule's shape): Bash(command='/bin/sh -c \"sleep 30\" > /tmp/probeB.log 2>&1; echo \"rc=$?\" >> /tmp/probeB.log', timeout=5000). Then, after ~40s, `cat /tmp/probeA.log /tmp/probeB.log` — a missing `rc=` line in probeB.log with probeA.log holding `done` proves the compound form was stopped, not backgrounded."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-long-running-command-hangs-6"
unit = "rule-long-running-command-hangs"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "Rule 3's blanket prohibition on `cmd 2>&1 | tail -N`, whose stated motivation is capture ('to capture'), and whose replacement is a manual redirect-to-file."
evidence = ".claude/rules/long-running-command-hangs.md:61-68"
native_alternative = "Claude Code already streams every Bash command's output to a working file and hands Claude the file path plus a preview once a valid result passes ~30,000 characters, with `BASH_MAX_OUTPUT_LENGTH` raising the read-back window to a hard ceiling of 150,000; 2.1.261 added the `bashOutputMaxChars` and `taskOutputMaxChars` settings raising inline command/background-task output up to 128K characters, and 2.1.265 added a 1 GB cap on tool results saved to disk with the preview noting truncation. So the volume reason for piping to `tail` is natively solved and the redirect-to-file half of rule 3's workaround is now duplicative."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:160-171 (\"Claude Code streams a command's output to a working file as the command runs\"; the valid/failure inline table; `BASH_MAX_OUTPUT_LENGTH` 30,000 default, 150,000 ceiling); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — `bashOutputMaxChars`/`taskOutputMaxChars` (2.1.261) and the 1 GB tool-result cap (2.1.265)"
rationale = "The exit-code half of rule 3 is pure POSIX shell semantics with no native replacement — `cmd | tail` still returns tail's status, and the harness's own valid/failure classification would read that as a success — so the prohibition itself KEEPS and the `hook_guard` 'gate command piped to head/tail' rule stays load-bearing. What is superseded is the stated reason and the workaround: the rule should cite the native output-to-file behaviour (so agents stop hand-rolling redirects for volume) and narrow its prohibition to the exit-code argument alone, which is the part nothing native fixes."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-long-running-command-hangs-7"
unit = "rule-long-running-command-hangs"
kind = "rule"
disposition = "refactor"
confidence = 0.65
custom_thing = "Rule 4's remedy for a wedged process — \"Kill it (and its process group), then diagnose from the log tail\" — stated as a purely manual habit."
evidence = ".claude/rules/long-running-command-hangs.md:70-74"
native_alternative = "For anything the harness started, background tasks are listed and stopped natively with `/tasks` (and, for the session's own watches, by asking Claude to cancel a Monitor); a timed-out foreground command is already a background task by the time it looks wedged, so it is reachable there rather than only through `ps`/`kill`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:175 — \"List and stop background tasks with `/tasks`.\"; :177 (a timed-out command becomes a background task); :322 (\"Stop a monitor by asking Claude to cancel it or by ending the session\")"
rationale = "Partial coverage only: `/tasks` reaches harness-started tasks, not a stray hk process group left behind by a previous session or by `lint.py`'s own kill path, and it does not print the debug-log tail the rule wants for diagnosis. So rule 4's manual kill stays as the fallback, but naming `/tasks` first stops agents from reaching for `ps | grep | kill` improvisation, which the `reap` skill already documents as having nearly killed a session's own shell."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-long-running-command-hangs-8"
unit = "rule-long-running-command-hangs"
kind = "rule"
disposition = "keep"
confidence = 0.90
custom_thing = "The rule's core hk-specific claims: rule 1's \"hk has none of its own [timeout]\", rule 5's hk lock/state guidance under `~/.local/state/hk/`, rule 6's `depends` + `fail_fast = false` deadlock diagnosis (blocked by `hk.pkl`'s `no_hk_depends`), and rule 7's 'grep for a `❯ <step>` with no matching `✔ <step>`' wedge locator."
evidence = ".claude/rules/long-running-command-hangs.md:22-29, :76-79, :81-94, :96-98; python/src/dotfiles_setup/lint.py:10-26 (the no-native-timeout finding, recorded against hk 1.46) and :59-60, :67, :86-92 (the `~/.local/state/dotfiles/hk-lint-<hash>[-<pid>].log` paths rule 2 cites — verified accurate)"
native_alternative = "None at the pinned versions. hk 1.57.0 still exposes no step-level `timeout` key and no `--timeout` CLI flag, and hk 1.58.1 (the only release newer than the pin) is documentation-only with \"no changes to hk's runtime behavior\". No hk release since has fixed the `depends`/`fail_fast` deadlock, and Claude Code offers nothing that names a wedged hk step or cleans hk's lock state."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/Cargo.toml:23 (`version = \"1.57.0\"` — the corpus is exactly the pin, so this is a same-version probe); grep for `timeout` across /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/ returns only VitePress site files (stars.data.ts, EndevSponsors.vue, banner.ts), zero configuration/step keys — control arm: `profiles` with the same command shape hits docs/configuration.md, docs/getting_started.md and 3 more; grep for `\"timeout\"` in /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/commands.json returns 0 — control arm: `\"jobs\"` in the same file returns hits. hk CHANGELOG.md `depends`/`fail_fast` mentions are all old (:1792, :2550, :2588, :2910-2911), none a deadlock fix. .agent/kb/raw/hk-releases-since-pin.digest.md — v1.58.1 is \"A small documentation-only release. There are no changes to hk's runtime behavior.\""
rationale = "Required justification: the wrapper in `lint.py` exists precisely because hk ships no bound of its own, and that is still true at the pinned 1.57.0 and at the only newer release. The harness's Bash timeout (finding 3) is not a substitute — it backgrounds rather than killing, so it would leave the wedged hk process group alive and print no diagnostics, and it does not exist at all in CI or a human terminal, which is where `mise run lint` also runs. Rules 5, 6 and 7 are hk-internal forensics with no counterpart in any tool: nothing native names a wedged hk step, cleans `~/.local/state/hk/`, or knows that a failed dependency never releases its dependent. Rule 2's log paths were re-verified against `lint.py` and are accurate as written."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-md-size-budgets-1"
unit = "rule-md-size-budgets"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "The rule quotes Anthropic's memory docs verbatim as \"CLAUDE.md files are loaded in full regardless of length, though shorter files produce better adherence\" and concludes in bold: \"Nothing truncates a CLAUDE.md at any size. The 200-line/25KB hard truncation applies to auto-memory MEMORY.md only.\""
evidence = ".claude/rules/md-size-budgets.md:31-36 (the block quote at :31-32 and the bold conclusion at :34-36)"
native_alternative = "The quoted sentence no longer exists anywhere in the Claude Code docs; the current wording states a real hard cliff for CLAUDE.md: \"This limit applies only to MEMORY.md. Claude Code loads a CLAUDE.md file of up to 4 MiB in full and skips a larger file.\" — and the size guidance section repeats \"Claude Code skips a file over 4 MiB.\" So a CLAUDE.md is not truncated, it is SKIPPED ENTIRELY past 4 MiB."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:405 and :456. Control arm for the absence of the quoted string: `grep -rn \"regardless of length\"` over the whole claude-code doc tree returns 0, while the same command shape for \"reduce adherence\" returns 2 files (claude-directory.md, memory.md) — so the probe discriminates."
rationale = "A load-bearing quotation in a rule whose entire thesis is 'a true fact must travel with its source' has itself gone stale: the sentence is no longer in the corpus and its replacement contradicts the bolded conclusion drawn from it. Re-quote memory.md:405 and change 'Nothing truncates a CLAUDE.md at any size' to 'a CLAUDE.md over 4 MiB is skipped in full, not truncated'. This does not change any budget number (24,000 B is ~175x under 4 MiB) but it removes a false absolute and adds a second real cliff the file's own section 'the only real cliff this repo can hit' currently denies exists."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-md-size-budgets-2"
unit = "rule-md-size-budgets"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "\"Plus one hard limit: a SKILL.md `description` > 1,536 chars is truncated silently, taking the keywords Claude matches on with it — the skill simply stops being discovered. It is the only real cliff this repo can hit.\""
evidence = ".claude/rules/md-size-budgets.md:107-109"
native_alternative = "Three things changed. (a) The cap covers the COMBINED `description` + `when_to_use` text, not `description` alone. (b) It is now a configurable setting, `skillListingMaxDescChars` (type: positive integer, Default: 1536, Scope: Any file) — so 1,536 is a default, not a hard limit. (c) The cap that actually bites first is a listing-wide budget, `skillListingBudgetFraction` (Default: 0.01 = 1% of the context window): when the listing overflows, Claude Code \"keeps every skill's name but drops the descriptions of the least-used skills\" — a whole-description drop that no per-file byte budget can predict or prevent. `skillOverrides` with \"name-only\" is the native lever for freeing that budget without editing any SKILL.md."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:2739-2753 (`skillListingMaxDescChars`, default 1536), :2723-2735 (`skillListingBudgetFraction`, default 0.01), :3885 (`skillOverrides`); skills.md:337-338 (combined description+when_to_use capped at 1,536) and skills.md:1050-1058 (overflow drops descriptions least-used-first; `skillOverrides` \"name-only\"). Control arm: `grep -c skillListingZzqqxxFake skills.md` = 0 while the three real keys hit, so the probe discriminates."
rationale = "The rule states a number that is now a settings default, attributes it to the wrong field, and calls it 'the only real cliff' while the harsher, non-deterministic cliff (whole descriptions dropped under a 1%-of-context listing budget) goes unmentioned. That understates the risk for a repo carrying ~30 skills. The `skill` row of the budget table should reference the two settings by name, and the rule should say the repo deliberately does not set them: control-armed 2026-09-09, `grep -n 'skillListing|skillOverrides|SLASH_COMMAND_TOOL_CHAR_BUDGET' .claude/settings.json` -> ABSENT while `fallbackModel` hits 1."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-md-size-budgets-3"
unit = "rule-md-size-budgets"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "The rule's measurement doctrine is a hand-derived byte census of the repo tree: \"Measured 2026-07-28 — unscoped rules are ~88% of the eager corpus, so this is where the bytes actually are. The pass took it 132,683 -> 105,648 B (-20.4%) across 19 of 21 eager rules\" — a one-off count of files on disk, with no standing way to see what the model actually receives."
evidence = ".claude/rules/md-size-budgets.md:167-172"
native_alternative = "Claude Code now reports actual per-category context spend natively: `/context` lists what loaded under a **Memory files** row (the authoritative check that a CLAUDE.md/rule reached context at all) and a **Skills** row reporting the listing size AFTER the budget is applied, i.e. what the model really receives; `/doctor` gives 'an estimate of the listing's context cost and its biggest contributors'; and `/skill-doctor` (shipped 2.1.261, newer than the offline corpus) 'shows which loaded skills go unused and what they cost in context, so users can prune them'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:69, :145, :421, :437 (`/context` **Memory files**); skills.md:1056 (Skills row is post-budget), skills.md:1054 (`/doctor` estimate); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — capability `/skill-doctor`, version 2.1.261."
rationale = "The gate itself must stay (see finding 6), but the rule's *measurement* half is now partly native and strictly better: a disk byte-count cannot tell you a rule failed to load, or that a skill's description was dropped from the listing, and both are exactly the failure the rule cares about. Add a short 'Measuring' note pointing at `/context` (Memory files + post-budget Skills row), `/doctor` and `/skill-doctor` as the way to verify what actually reached context, keeping the byte census only as the pre-commit gate's input. Confidence is 0.8 rather than higher because these are interactive commands with no machine-readable output probed here, so they supplement rather than replace `kb-setup md-budget`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-md-size-budgets-4"
unit = "rule-md-size-budgets"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "The 'Scoping: the trigger test' section prescribes when to use `paths:` frontmatter and treats a `paths:` list as free — it discusses only the trigger semantics ('trigger when Claude reads files matching the pattern') and never mentions any limit on the pattern list itself."
evidence = ".claude/rules/md-size-budgets.md:132-151 (section body), and this very file's own frontmatter `paths:` list at :1-8"
native_alternative = "Claude Code documents a hard budget on the `paths` list: \"a rule's whole `paths` list shares one budget of 1,000 expanded patterns and 4 MiB, and patterns without braces don't count against it\", and \"Claude Code uses any pattern that would exceed the budget unexpanded, and its literal braces match no files\" — i.e. an over-budget brace pattern silently matches nothing, which for a scoped rule means it silently never loads. (Before v2.1.217 the same case stalled or crashed the CLI at startup.)"
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:242-244"
rationale = "This rule is the repo's single authority on 'how much may an instruction file cost and when may it be scoped', so a documented budget on the scoping mechanism itself belongs in it — especially because the failure mode is silent (an over-budget brace group matches no files, so the rule is absent exactly like the 2026-07-15 zero-skip-policy incident the section is built around). No repo rule is near 1,000 expanded patterns today (this file's own list is 5 brace-free globs), so this is a documentation gap, not a live defect: one sentence in the scoping section, not a new gate. Confidence 0.7 because the risk is latent rather than currently realized."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-md-size-budgets-5"
unit = "rule-md-size-budgets"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "The budget table's `nested` row cites the load semantics as \"'included when Claude reads files in those subdirectories'; not re-injected after `/compact`\", using non-re-injection as part of the justification for the looser 400-line/32,000-byte budget."
evidence = ".claude/rules/md-size-budgets.md:94"
native_alternative = "The docs now state the compaction behaviour positively and pair nested CLAUDE.md with `paths:` rules: \"Project-root CLAUDE.md survives compaction: after `/compact`, Claude re-reads it from disk and re-injects it into the session. Nested CLAUDE.md files in subdirectories and rules with `paths:` frontmatter reload as Claude reads files they apply to.\""
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:462"
rationale = "'Not re-injected after /compact' is a half-truth at 2.1.266: a nested CLAUDE.md does reload, just on a file-read trigger rather than automatically, and the same is true of the `rule_scoped` class which the table justifies differently. The budgets themselves do not change, but the cited semantics should be re-worded to the current sentence so the table's rationale column stays quotable — the whole point of this file is that a quoted fact must be traceable to its source. Low confidence on impact (no number moves), high enough on staleness (the quoted phrasing is not the current phrasing)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-md-size-budgets-6"
unit = "rule-md-size-budgets"
kind = "rule"
disposition = "keep"
confidence = 0.90
custom_thing = "The gate itself: line + byte budgets per load class, enforced pre-commit by the shared `kb-setup md-budget` engine wired as hk step `md_size_budget`, with the AGENTS.md 12,000-char ceiling delegated to agnix AGM-003."
evidence = ".claude/rules/md-size-budgets.md:13-17 and :88-105; hk.pkl:616-617 (`[\"md_size_budget\"] { check = \"uv run --project python kb-setup md-budget\" }`)"
native_alternative = "None. Claude Code has no mechanism that rejects, warns on, or fails a build for an oversize CLAUDE.md / rule / SKILL.md. Its only size behaviours are: load the file anyway and note that adherence degrades ('Files over 200 lines consume more context and may reduce adherence'), skip a CLAUDE.md over 4 MiB, and truncate the MEMORY.md index — none of which this repo can hit and none of which is a gate. The nearest native things (`/context`, `/doctor`, `/skill-doctor`) are interactive reports, not enforcement, and the auto-memory reminder/error path applies only to `MEMORY.md`, a file this repo does not commit."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:456 (advisory wording only), :401-405 (the 200-line/25KB measure-and-error path is scoped to MEMORY.md alone: 'This limit applies only to MEMORY.md'), skills.md:1040 (`claude plugin validate` checks frontmatter PARSING, not size). Control arm: the same corpus does surface real enforcement keys elsewhere (settings-reference.md:2739 `skillListingMaxDescChars`), so a 0-result for a size gate is a real absence, not a blind probe."
rationale = "Required justification: the harness deliberately does not gate instruction-file size — it degrades quietly instead, which is precisely the failure this repo pays a pre-commit gate to make loud. `claude plugin validate --json` (2.1.259) validates manifests and frontmatter parsing, not byte or line counts, so it cannot substitute. The five findings above are edits to the rule's PROSE and cited anchors; the gate, the class taxonomy and every number in the budget table survive the audit unchanged."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-mise-tasks-only-1"
unit = "rule-mise-tasks-only"
kind = "rule"
disposition = "refactor"
confidence = 0.75
custom_thing = "The rule's own hard bans (`git commit --no-verify`/`-n`, `git push --no-verify`, an `HK_SKIP_HOOKS=`/`HK_SKIP_STEPS=` command prefix, and the credential-echo ban) are enforced ONLY by the fail-open PreToolUse guard, even though the rule states in the same file that 'Hard bans that must never fail open belong in settings.json permission deny rules, not the hook'."
evidence = ".claude/rules/mise-tasks-only.md:29-30,33 (the three hard-ban rows) and .claude/rules/mise-tasks-only.md:69-71 (the fail-open warning + the 'belong in permissions deny' sentence); the live deny list in .claude/settings.json `permissions.deny` contains only `Bash(chezmoi apply:*)`, `Bash(chezmoi update:*)`, four `*attest-plan*`/`*set-active-plan*` entries and the `plan-attest` task entries — no `--no-verify`, no `HK_SKIP_HOOKS`, no credential-echo rule; enforcement is python/src/dotfiles_setup/hook_guard.py:788-830 behind scripts/pretooluse-guard.sh:33-41 which `exit 0`s (allows) on any guard error."
native_alternative = "`permissions.deny` rules in settings.json: they block in EVERY permission mode including bypassPermissions, they are evaluated independently of (and after) any hook decision, and Bash deny rules must match each subcommand of a compound command independently."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permission-modes.md:30 ('Deny rules block in every mode, including bypassPermissions'); permissions.md:495 ('Hook decisions don't bypass permission rules. Claude Code evaluates deny and ask rules regardless of what a PreToolUse hook returns'); permissions.md:222 ('A rule must match each subcommand independently', separators `&&`, `||`, `;`, `|`, `|&`, `&`, newlines)."
rationale = "Native deny rules cover exactly the property the rule says these three rows need — cannot fail open, cannot be skipped by a permission mode — and the rule already names the mechanism without routing its own hard bans to it. Refactor, not retire, for two documented reasons: a deny rule's message is generic ('Permission to use X has been denied', permissions.md:455) so it cannot carry the redirect text that is this rule's whole point, and deny patterns are glob-shaped with everything before the first `*` matched literally (permissions.md:151), so they cannot express the guard's flag-position precision (e.g. `git commit -m \"x\" --no-verify`). The correct shape is both layers: deny rules for the bans, the guard for the redirects."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-mise-tasks-only-2"
unit = "rule-mise-tasks-only"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "The claim that command substitution is unavoidably out of reach: 'Still fail-open BY DESIGN: `$(…)`, `sh -c`/`eval`, base64, aliases.' — presented as a property of the enforcement layer rather than of the custom regex guard."
evidence = ".claude/rules/mise-tasks-only.md:71-73; the guard's masking pass deliberately neuters quoted/substituted spans before matching — python/src/dotfiles_setup/hook_guard.py:660-685 (`_inert_masked` docstring) and :106-107 ('Deliberately fail-open beyond this (sh -c, base64, aliases)')."
native_alternative = "The harness's own Bash permission-rule matcher inspects commands inside `$()` and backticks, strips leading `VAR=value` assignments, and checks each `&&`/`||` subcommand — so a pattern expressed as a permission rule (in `permissions.deny` or in a hook `if` condition) sees the substitution shapes the custom regex chooses not to see."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:438-448 (matching table: ``Bash(rm *)`` vs ``echo $(rm -rf /)`` → 'yes … commands inside `$()` and backticks are checked'; ``FOO=bar git push`` → 'leading assignments are stripped'; ``npm test && git push`` → 'each subcommand is checked'); hooks.md:452 ('Because the `if` filter is best-effort, use the permission system rather than a hook to enforce a hard allow or deny'); changelog.md:386 (2.1.243 fixed `if` conditions firing on `$()`/backticks with trailing arguments — the matcher is actively maintained)."
rationale = "The `$(…)` half of the 'fail-open BY DESIGN' list is stale as a statement about what is POSSIBLE: it is true of the repo's regex guard, not of the harness, whose permission matcher descends into command substitution. `sh -c`, base64 and aliases remain genuinely uncovered (the docs' own caveat: when Claude Code cannot determine which commands run, it runs the hook rather than resolving them). Refactor: re-word the line to say 'this GUARD fails open on `$(…)`; a permission rule would not', and move the shapes that matter into deny rules per finding 1."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "rule-mise-tasks-only-3"
unit = "rule-mise-tasks-only"
kind = "rule"
disposition = "refactor"
confidence = 0.55
custom_thing = "The 'Extending' recipe — 'New redirect = new `_RULES` entry in hook_guard.py + a test + a row in the table above' — with the guard wired as a single unconditional handler that spawns a bash wrapper plus a `uv run` python process on EVERY Bash, AskUserQuestion, Edit, Write and NotebookEdit tool call."
evidence = ".claude/rules/mise-tasks-only.md:90-94 (the extending recipe, which never mentions a native pre-filter); .claude/settings.json PreToolUse handler `matcher: \"Bash|AskUserQuestion|Edit|Write|NotebookEdit\"` running `bash \"${CLAUDE_PROJECT_DIR:-.}/scripts/pretooluse-guard.sh\"` with no `if` field; scripts/pretooluse-guard.sh:37-40 (spawns `uv run --project $ROOT/python dotfiles-setup hook pretooluse` on every matched call)."
native_alternative = "The hook `if` field: permission-rule syntax (e.g. `\"Bash(gh *)\"`, `\"Bash(git *)\"`, `\"Bash(mise *)\"`) that gates whether the handler process is spawned at all, evaluated on PreToolUse. Multiple conditions = multiple handlers, one rule each."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:427 (the `if` field, 'The hook command only runs if the tool call matches the pattern'); hooks.md:209 ('If the command had been `npm test`, the `if` check would fail and `block-rm.sh` would never run, avoiding the process spawn overhead'); hooks.md:432 ('The `if` field holds exactly one permission rule … define a separate hook handler for each'); changelog.md:3591 (added in 2.1.85)."
rationale = "A native pre-filter shipped 170+ patch releases before the host version and the rule's extending recipe still doesn't mention it, so every `ls` pays two process spawns. Refactor rather than retire: `if` is documented as best-effort (hooks.md:452), it holds one rule per handler, and the guard's Edit/Write leg keys on git branch state rather than a path pattern, so `if` can only cover the Bash command families (`gh`, `git`, `mise`, `docker`, `chezmoi`, `hk`, `npx`, `pytest`, `echo`/`printf`) — the guard body must stay authoritative for the deny decision."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-mise-tasks-only-4"
unit = "rule-mise-tasks-only"
kind = "rule"
disposition = "refactor"
confidence = 0.55
custom_thing = "The manual compensating habit for a deny that kills a whole compound command: '⚠️ After ANY deny, re-check that the intended side effects actually happened — a deny cancels the entire compound command.'"
evidence = ".claude/rules/mise-tasks-only.md:72-74; the guard only ever emits a deny — python/src/dotfiles_setup/hook_guard.py:826-829 (`hookSpecificOutput` → `permissionDecision: \"deny\"` + `permissionDecisionReason`), with no `updatedInput` anywhere in the module (grep for `updatedInput` in python/src/dotfiles_setup/hook_guard.py returns 0 hits, control-armed against `permissionDecision`, which returns 3)."
native_alternative = "PreToolUse `hookSpecificOutput.updatedInput` — the hook replaces the tool's arguments before it runs, so the guard can rewrite the offending segment into the canonical `mise run …` invocation (or strip it) instead of cancelling the caller's entire compound command. It is accepted alongside `allow` and `ask` decisions."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1029 ('`PreToolUse`: `updatedInput` directly under `hookSpecificOutput` replaces a tool's arguments before it runs'); hooks.md:1073 (the JSON example); changelog.md:4902 (2.1.0, `updatedInput` alongside an `ask` decision — 'enabling hooks to act as middleware'); changelog.md:3597 (2.1.85, PreToolUse `updatedInput` with `allow`)."
rationale = "The lost-side-effects warning is a human habit compensating for a mechanism choice, and the harness has offered the middleware shape since 2.1.0. Refactor, not retire, and deliberately narrow: a silent rewrite of a command Claude asked for is its own hazard, so the candidate rows are the mechanical 1:1 substitutions the table already asserts are equivalent (`hk run check --all` → `mise run lint`, `npx <tool>` → the mise binary) with the rewrite named in `permissionDecisionReason`. The judgement-carrying rows (`gh pr merge` → ship/automerge/land by provenance) must keep denying. Note the caveat that a rewritten input is re-checked against `permissions.deny` (permissions.md:495, changelog.md:3164)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-mise-tasks-only-5"
unit = "rule-mise-tasks-only"
kind = "rule"
disposition = "unknown"
confidence = 0.70
custom_thing = "Enforcement layer 4: 'the self-learning loop `mise run command-audit`, run per session by a `SessionEnd` hook' — asserted as a live layer of the five-layer inventory."
evidence = ".claude/rules/mise-tasks-only.md:63-66; the wiring is .claude/settings.json `SessionEnd` → `mise -C \"${CLAUDE_PROJECT_DIR:-.}\" run command-audit -- --output …/.agent/command-audit.md` with `\"timeout\": 120`."
native_alternative = "None — but the harness caps this event: 'SessionEnd hooks share a 1.5-second budget; if your settings set a longer per-hook `timeout`, Claude Code raises the budget to match, up to 60 seconds.' The configured 120 is therefore capped at 60, and a SessionEnd hook has no decision control and only shows stderr to the user, so an over-budget audit is cancelled with nothing shown to Claude."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:428 (the SessionEnd budget sentence, 'up to 60 seconds'); hooks.md:873 (SessionEnd exit-2 row: 'No — Shows stderr to user only'); hooks.md:1025 (SessionEnd under 'None — No decision control')."
rationale = "The 60-second ceiling is a documented harness fact the rule's layer-4 claim does not account for: the settings ask for 120s and cannot get it. Whether the layer is actually a no-op depends on how long a full transcript mine takes on this machine, which no evidence in-repo measures — hence 'unknown' rather than a disposition on the layer. If it exceeds 60s the fix is native-shaped (`\"async\": true` on the command hook, which the docs say Claude Code does not enforce a timeout on — hooks.md:428, :457), not a rewrite of the audit."
probe = "/usr/bin/time -p mise -C /Users/rmanaloto/dev/github/ray-manaloto/dotfiles run command-audit -- --output /private/tmp/claude-501/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/323f75d8-e472-46fe-85ba-769a82acf97c/scratchpad/audit-probe.md > /private/tmp/claude-501/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/323f75d8-e472-46fe-85ba-769a82acf97c/scratchpad/audit-timing.log 2>&1; echo \"rc=$?\" >> /private/tmp/claude-501/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/323f75d8-e472-46fe-85ba-769a82acf97c/scratchpad/audit-timing.log  # real > 60s means the SessionEnd layer is being cancelled every session"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-mise-tasks-only-6"
unit = "rule-mise-tasks-only"
kind = "rule"
disposition = "refactor"
confidence = 0.50
custom_thing = "'⚠️ The hook fails OPEN on its own errors' stated as a standing property of the layer, with the only remedy offered being 'move it to settings.json permission deny rules'."
evidence = ".claude/rules/mise-tasks-only.md:69-71; scripts/pretooluse-guard.sh:33-41 — `fail_open()` ends in `exit 0`, and the guard's non-zero rc is converted to `fail_open \"guard-error-rc=$?\"`."
native_alternative = "Hook exit code 2: on events that can block (PreToolUse among them) exit 2 blocks the tool call 'whether or not you print JSON', and the docs state plainly that exit 1 is treated as a non-blocking error while 'If your hook is meant to enforce a policy, use `exit 2`'. The error path can therefore fail CLOSED without moving anything to permissions."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:798 ('Exit 2 means a blocking error. On events that can block, exit 2 blocks whether or not you print JSON'); hooks.md:836 ('Claude Code treats exit code 1 as a non-blocking error and proceeds … If your hook is meant to enforce a policy, use `exit 2`'); hooks.md:851-856 (per-event table: PreToolUse blocks on exit 2)."
rationale = "Fail-open here is a choice in the repo's wrapper, not a harness constraint, and the rule reads as though it were the latter. The two branches deserve different answers: `interpreter-absent` must stay `exit 0` (its documented purpose is not bricking a cold Claude-web session before web-setup.sh runs — scripts/pretooluse-guard.sh:6-9), while `guard-error-rc=…` — the guard's interpreter present but crashing — could `exit 2` and block, which is the shape hooks.md:836 prescribes for a policy hook. Refactor the sentence and the wrapper's error branch; keep the recorded-fail-open ledger (#343) either way."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-mise-tasks-only-7"
unit = "rule-mise-tasks-only"
kind = "rule"
disposition = "keep"
confidence = 0.60
custom_thing = "Table row: '`<gate> 2>&1 | tail -40` → `<gate> > /tmp/out.log 2>&1; echo \"rc=$?\" >> /tmp/out.log`, then read the file', enforced by the guard's 'gate command piped to head/tail' rule."
evidence = ".claude/rules/mise-tasks-only.md:22 (the row); the same redirect is repeated as the canonical recipe in .claude/rules/long-running-command-hangs.md rule 3 and .claude/rules/verify-before-advancing.md."
native_alternative = "The size half of the motive is now handled by the harness: Bash output is streamed to a working file, delivered inline up to ~30,000 characters and past that as a saved file path plus preview that Claude reads or searches, with `BASH_MAX_OUTPUT_LENGTH` raising the read-back window to 150,000; 2.1.261 adds `bashOutputMaxChars`/`taskOutputMaxChars` settings and 2.1.265 a 1 GB cap with a truncation note in the preview."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:160-171 (Output limits: streaming to a working file, 30,000-char inline threshold, file path + preview, `BASH_MAX_OUTPUT_LENGTH` up to 150,000); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md:15-17 (`bashOutputMaxChars`/`taskOutputMaxChars`, 2.1.261) and :11-13 (1 GB cap + truncation note, 2.1.265)."
rationale = "Keep the row and the guard rule, but the justification needs narrowing to the half that is still true. The exit-code half is untouched by any native feature: a shell pipeline still returns `tail`'s status, and the Bash tool's own valid/failure classification is derived from that masked exit code (tools-reference.md:169), so `| tail` still converts a red gate into a result Claude reads as green. The size half — the reason anyone reaches for `| tail` — is now the harness's job, so the row should say 'you never need to bound gate output yourself; the harness saves it and hands you the path' rather than leaving the redirect looking like the only way to see a long log."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-mise-tasks-only-8"
unit = "rule-mise-tasks-only"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "The rule as a whole: a repo-specific redirect map (one-off command → canonical `mise run` task) enforced by a PreToolUse guard that returns a deny carrying the redirect target as its reason."
evidence = ".claude/rules/mise-tasks-only.md:10-33 (the map) and :57-67 (the five-layer inventory); python/src/dotfiles_setup/hook_guard.py:826-829 emits `permissionDecision: \"deny\"` with a `permissionDecisionReason` naming the replacement task."
native_alternative = "None for the redirect semantics. `permissions.deny` blocks but its denial message is fixed ('Permission to use mcp__workspace__bash has been denied' is the documented shape), so it cannot name a replacement command; `permissions.ask` prompts the human rather than redirecting the agent; the `if` field only filters when a handler runs. No harness feature maps 'command X in this repo' to 'run task Y instead'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:455 (the fixed denial message shape); permissions.md:66-68 (deny rules are pattern blocks, with no exception or message field); hooks.md:1016 (PreToolUse decision control is `permissionDecision` + `permissionDecisionReason` — the reason string is the only channel that can carry a redirect, and it belongs to a hook, not to a settings rule)."
rationale = "Required justification: the unit's core mechanism has no native equivalent at Claude Code 2.1.266. The harness can block a command and can filter which handler sees it, but nothing native can tell the agent WHICH canonical task to run instead — that text can only reach Claude through a hook's `permissionDecisionReason` (hooks.md:1016), which is exactly what this rule's guard does. Findings 1-4 and 6 narrow the mechanism at its edges (hard bans to deny rules, `$()` framing, `if` pre-filtering, `updatedInput` rewrites, a fail-closed error path); the redirect map itself stays custom."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-notepad-enforcement-1"
unit = "rule-notepad-enforcement"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "Rule 4 ('Research agents especially: any multi-file analysis or research sweep MUST write intermediate findings before proceeding') is carried ONLY as prose in an unscoped .claude/rules file, i.e. it is delivered by the eager-rule loader and by nothing else. There is no per-agent wiring."
evidence = ".claude/rules/notepad-enforcement.md:29-30 (rule 4); .claude/rules/notepad-enforcement.md:8-9 (mechanism is 'the ordinary Write/Edit tools'); control arm on the repo's agent definitions: `grep -rn '^memory:' .claude/agents/*.md` returns 0 hits while `grep -c '^description:' .claude/agents/*.md` returns 1 for each of 9 agent files, so the frontmatter surface is readable and the field is simply unused."
native_alternative = "Two native carriers that reach a research lane the eager-rule loader may not: (a) unscoped rules load 'at launch with the same priority as `.claude/CLAUDE.md`', but the built-in Explore and Plan subagents 'skip your CLAUDE.md files' while 'every other built-in and custom subagent loads both' — so routing research to a NAMED custom subagent (or to `Agent` with an explicit definition) is the documented way to make the instruction reach the lane; (b) `--append-subagent-system-prompt` appends text to the end of every subagent's system prompt, nested subagents included (non-interactive `-p` only, v2.1.205+), with `CLAUDE_CODE_ENABLE_APPEND_SUBAGENT_PROMPT` as its env twin."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:199; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:33; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/cli-reference.md:66; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/env-vars.md:266"
rationale = "The repo's own routing table sends Research to 'a read-only Explore/Agent lane' (.claude/CLAUDE.md, 'There is no grok here' table), and sub-agents.md:33 states Explore skips CLAUDE.md files — so rule 4 is plausibly absent from exactly the lane it names. The sibling rule already records the measured version of this failure ('a rule nothing pushes into the prompt is not a layer' — .claude/rules/agent-report-persistence.md rule 1b: the requirement went into none of four briefs, two agents died and left nothing). The refactor is to stop relying on the eager loader for rule 4 and name the carrier: a custom agent definition body, or the append-subagent-system-prompt flag for headless runs. Confidence is held at 0.6 because sub-agents.md:33 says 'CLAUDE.md files', not '.claude/rules/*.md', so whether Explore also drops the rules directory is inference from memory.md:199's equal-priority statement, not a direct citation."
probe = "claude -p --agents '{\"probe\":{\"description\":\"probe\",\"prompt\":\"reply only with the literal token you were told to echo, or NONE\"}}' 'ask the probe agent to echo the notepad token' ; and separately, in an interactive session, launch an Explore subagent and ask it to quote the first line of .claude/rules/notepad-enforcement.md from its own loaded context WITHOUT reading the file — a NONE answer from Explore alongside a quote from a named custom agent is the discriminating pair."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-notepad-enforcement-2"
unit = "rule-notepad-enforcement"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "The rule's compliance check is a manual human habit: 'Verification: after an agent completes work, check `.agent/notepad.md`. Empty or stale relative to the work performed means it did not comply.' Nothing runs it. The repo wires PreToolUse, PostToolUse, SessionStart, SessionEnd and InstructionsLoaded hooks but has no Stop or SubagentStop hook at all."
evidence = ".claude/rules/notepad-enforcement.md:37-38; .claude/settings.json `hooks` block (keys present: PreToolUse, PostToolUse, SessionStart, SessionEnd, InstructionsLoaded — no Stop/SubagentStop). Control arm on the wiring probe: `grep -c 'command-audit' .claude/settings.json` returns 1, so a SessionEnd-wired mechanism IS visible to the same grep shape that returns nothing for notepad."
native_alternative = "The `SubagentStop` hook event: it fires 'when a subagent finishes', it CAN block ('SubagentStop | Yes | Prevents the subagent from stopping'), it matches on `agent_type`, and its input carries `last_assistant_message` and `agent_transcript_path` so a checker needs no transcript parsing. It accepts all five hook types including `prompt`, whose `{ok,reason,impossible}` contract feeds `reason` back to the subagent as its next instruction when `ok:false` — i.e. 'you have not written your findings to .agent/notepad.md yet' becomes an enforced retry rather than a human's later audit. The docs even ship the worked shape ('Check multiple conditions before stopping')."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:50, :317, :859, :2319, :2325, :2346, :3410, :3495, :3499, :3513"
rationale = "This is the repo's own enforcement-layer doctrine applied to itself: .claude/rules/mise-tasks-only.md states markdown alone is 'relying on the LLM, never the only layer', and this rule is currently markdown-only. SubagentStop is the exact native event for 'did this agent record its findings before it stopped', it is blocking, and the repo already owns the pattern (a PreToolUse guard shelling into python/src/dotfiles_setup/hook_guard.py). Refactor rather than retire because the hook enforces the rule, it does not replace the rule text that says WHAT to record."
probe = "Wire a SubagentStop command hook (matcher: the research agent types) that compares `.agent/notepad.md` mtime/size against the hook's own start stamp and returns decision:block with a reason; then arm both directions — one subagent run that appends to the notepad (must be allowed to stop) and one that does not (must be blocked once)."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-notepad-enforcement-3"
unit = "rule-notepad-enforcement"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "`.agent/notepad.md` as THE running-findings file, mandated by this rule ('The notepad is `.agent/notepad.md`'), with no re-injection mechanism of its own — while the repo simultaneously runs a second, independently-mandated running-findings file."
evidence = ".claude/rules/notepad-enforcement.md:6-9 and rules 1-4; the competing convention is .claude/rules/agent-report-persistence.md rule 1c, whose file-role table assigns 'Research, analysis, evidence, technical findings' to `findings.md`; .gitignore:122-131 ignores `/task_plan.md`, `/findings.md`, `/progress.md`, `/.planning/` for the plugin, calling it 'a deliberate exception to agent-artifact-conventions.md rule 1'; `.agent/notepad.md` is 432,929 bytes and append-only across all sessions (python/src/dotfiles_setup/session_review.py:114 'The notepad is append-only ACROSS sessions')."
native_alternative = "The `planning-with-files@planning-with-files` plugin is ENABLED in this project (`.claude/settings.json` enabledPlugins: `\"planning-with-files@planning-with-files\": true`), pinned at 3.17.2, and its `findings.md` carries a byte-for-byte identical job description — 'findings.md | Research, discoveries | After ANY discovery'. Unlike the notepad it is WIRED: its `hooks/hooks.json` registers SessionStart with matcher `startup|resume|clear|compact` plus UserPromptSubmit, PreToolUse and PostToolUse hooks that re-inject the planning context, and `scripts/inject-plan.sh:1137` states 'task_plan.md, findings.md, progress.md remain on disk and will be re-read after compaction'."
alternative_evidence = "/Users/rmanaloto/.claude/plugins/cache/planning-with-files/planning-with-files/3.17.2/skills/planning-with-files/SKILL.md:103; .../3.17.2/skills/planning-with-files/reference.md:173; .../3.17.2/hooks/hooks.json (SessionStart matcher `startup|resume|clear|compact`, UserPromptSubmit, PreToolUse matcher `Write|Edit|Bash|Read|Glob|Grep`, PostToolUse matcher `Write|Edit`); .../3.17.2/skills/planning-with-files/scripts/inject-plan.sh:1137; .agent/kb/raw/planning-with-files-changelog.digest.md (3.17.2 is the top released entry; nothing newer shipped as of 2026-09-09T09:53Z)"
rationale = "The stated purpose of this rule is context-loss survival ('When context was lost, every finding had to be re-derived' — line 34-35). The plugin's findings.md solves exactly that with lifecycle re-injection on `clear` and `compact`; the notepad has no such mechanism, so on the failure mode the rule names the notepad is the weaker of the two files the repo already maintains. Two rules now mandate two different running-findings files for the same content, which memory.md:91 flags as the contradictory-instruction case ('if two rules contradict each other, Claude may pick one arbitrarily'). Refactor, NOT retire: `.agent/notepad.md` has a real consumer — session_review.py's lane 2 reads it by that exact path (python/src/dotfiles_setup/session_review.py:124) and python/verification/suites.toml:2342 binds that lane as a contract — so deleting the file would break a gate. The correct change is for this rule to name ONE file as the running-findings surface and state, in one line, the division of labour with findings.md (or point at it and keep the notepad purely as session_review's narrative corpus)."
probe = "Confirm the plugin's hooks are actually live in this project rather than merely enabled: run `/status` or `claude doctor` and check the hooks list for the planning-with-files SessionStart entry, with the control arm that the repo's own SessionStart (tool-currency-check + doctor) appears in the same listing."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-notepad-enforcement-4"
unit = "rule-notepad-enforcement"
kind = "rule"
disposition = "refactor"
confidence = 0.65
custom_thing = "Rule 2's 'any context the next agent will need' is served by a hand-maintained, gitignored, unbounded 432KB append-only markdown file that no agent is given a scoped view of; each agent must find its own prior findings by reading a corpus of every session this repo has had."
evidence = ".claude/rules/notepad-enforcement.md:22-23; .gitignore:45 (`.agent/notepad.md` ignored); file size 432,929 bytes; python/src/dotfiles_setup/session_review.py:557 ('the notepad accumulates across every session') — the module has to bound its own read for this reason."
native_alternative = "Subagent persistent memory: the `memory` frontmatter field on a subagent definition ('Persistent memory scope: `user`, `project`, or `local`. Enables cross-session learning'), giving that agent a directory (`.claude/agent-memory/<name-of-agent>/` for `project` scope, which the docs call the recommended default because it is shareable via version control). When enabled, Claude Code injects memory-reading/writing instructions into the agent's system prompt, injects the first 200 lines or 25KB of that directory's `MEMORY.md`, auto-enables Read/Write/Edit, and instructs the agent to curate MEMORY.md past that limit — i.e. the persistence instruction, the scoped view, and the size bound are all provided by the harness rather than by rule prose."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:302 (frontmatter table row) and :563-598 ('Enable persistent memory', scope table, 'When memory is enabled' bullets, 'Persistent memory tips')"
rationale = "For the repo's NAMED, recurring agent lanes (.claude/agents/staleness-auditor.md, codex-advisor.md, dockerfile-reviewer.md, adversarial-critic.md and the rest — 9 definitions, zero of which set `memory:`), the harness already offers a per-agent durable store with automatic prompt injection and an automatic curation bound; the rule currently reimplements that as an instruction plus one giant shared file. This does NOT cover the whole rule: agent memory is per-agent and cross-session, so it is a poor fit for the within-session, cross-lane running log the notepad also serves, and it is gated on auto memory being on (`autoMemoryEnabled` / `CLAUDE_CODE_DISABLE_AUTO_MEMORY`) — which is why this is refactor and not retire. Adopting it for the named lanes is the reviewable diff; note that `project` scope writes into `.claude/agent-memory/`, which is tracked, so it interacts with agent-artifact-conventions.md and needs a deliberate call."
probe = "Add `memory: project` to one agent (e.g. .claude/agents/staleness-auditor.md), launch it, and confirm `.claude/agent-memory/staleness-auditor/` is created and that a second launch quotes its own MEMORY.md without being told the path; control arm: an agent WITHOUT the field must produce no such directory in the same run."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-notepad-enforcement-5"
unit = "rule-notepad-enforcement"
kind = "rule"
disposition = "keep"
confidence = 0.90
custom_thing = "The rule's core directive itself — 'write findings as you go, never batch to session end', plus the four numbered rules and the .agent/notepad.md file location."
evidence = ".claude/rules/notepad-enforcement.md:1-38; docs/rules-evidence/notepad-enforcement.md (archaeology, present and current)."
native_alternative = "None at 2.1.266. Auto memory (`MEMORY.md`) is the nearest native persistence surface but is scoped to durable cross-conversation user/project knowledge, not an in-session running log, and subagent `memory` is per-agent; neither writes an as-you-go findings trail for a mixed-lane session. Claude Code ships no built-in notepad tool and no built-in as-you-go persistence enforcement."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:199 and the auto-memory section; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:563 (memory is per-agent, cross-conversation); .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 new capabilities across 2.1.258-2.1.266: none is a notepad, findings store, or as-you-go persistence feature)."
rationale = "Required justification: (a) audit axis (a) came back CLEAN — this rule asserts no hook event, no settings key, no precedence claim and no `$CC/...` doc anchor, so it has no stale harness fact to correct. Its only external claims are that `.agent/notepad.md` is gitignored (true, .gitignore:45) and that the oh-my-claudecode notepad MCP tools are absent (control-armed 2026-09-09: `oh-my-claudecode` returns 0 hits in both .claude/settings.json and ~/.claude/settings.json while `fable-orchestrator` returns 3 in the same grep shape — the plugin is not merely disabled, it is not registered at all, so the rule's statement is if anything understated). (b) The '0 invocations across 941 transcripts' figure is INHERITED and was not re-derived in this audit — label it unverified per probes-need-a-control-arm.md rule 6 if it is ever restated. (c) The rule also forbids nothing the harness has since made safe: it forbids batching findings to session end, and no 2.1.258-2.1.266 capability persists a session's working findings automatically. The three refactors above change the CARRIER and the ENFORCEMENT of the directive, not the directive, so the file stays."
probe = "Re-derive the 941-transcript / 0-invocation figure before it is restated anywhere: enumerate ~/.claude/projects/*/**.jsonl for this project and grep for the two `mcp__plugin_oh-my-claudecode_t__notepad_write_*` tool names, with a control arm grepping a tool name known to appear (e.g. `\"name\":\"Bash\"`) over the same corpus with the same command shape."
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-persistence-gate-retry-1"
unit = "rule-persistence-gate-retry"
kind = "rule"
disposition = "refactor"
confidence = 0.72
custom_thing = "The whole file is an UNSCOPED .claude/rules/*.md (no `paths:` frontmatter), so all 6,170 B / 115 lines of niche devcontainer failure-triage load eagerly into every session, including sessions that never touch a container. Its trigger is a specific gate's failure text (`verify-local` / `persistence` / `land` / `sync`), not a topic the session is always working on."
evidence = ".claude/rules/persistence-gate-retry.md:1 (file begins with the `# Persistence Gate` H1 — no `---`/`paths:` block; the only two scoped rules in the tree are .claude/rules/ci-local-parity.md and .claude/rules/md-size-budgets.md, `grep -l '^paths:' .claude/rules/*.md`). Size measured: `wc -lc` -> 115 lines / 6170 bytes. Applies-to list is .claude/rules/persistence-gate-retry.md:95-103 (four mise tasks)."
native_alternative = "Claude Code Skills — the harness's own documented mechanism for exactly this class: task-specific instructions that should not sit in context all the time. Only the skill DESCRIPTION is loaded in a regular session; the body loads on invocation or when Claude judges it relevant. The repo already has the landing zone: `.claude/skills/devcontainer-sync/SKILL.md` carries a failure-signature triage table and already points back at this rule."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/memory.md:182 — \"Rules load into context every session or when matching files are opened. For task-specific instructions that don't need to be in context all the time, use skills instead, which only load when you invoke them or when Claude determines they're relevant to your prompt.\"; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:510 — \"skill descriptions are loaded into context so Claude knows what's available, but full skill content only loads when invoked\". Existing host: .claude/skills/devcontainer-sync/SKILL.md:53-60 (triage table incl. `getaddrinfo ENOTFOUND ghcr.io` = transient DNS) and :89 (already cites this rule). Supporting new capability: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — `/skill-doctor` (2.1.261) reports which loaded skills go unused and what they cost in context."
rationale = "Fold the retry heuristic + failure-signature table into `.claude/skills/devcontainer-sync/SKILL.md` (which already duplicates one of its rows) and leave at most a one-line pointer eagerly. Counter-argument, stated honestly: `.claude/rules/md-size-budgets.md` § 'the trigger test' classes behaviour-triggered rules as MUST-stay-eager — but the same section carves out 'behaviour-triggered but NICHE -> a skill, not a rule', and this rule fires only when one of four Mac-local container tasks has already failed, which is precisely a relevance-matchable moment. Confidence is held below 0.8 because the judgement of niche-vs-general is Ray's, not mechanical."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-persistence-gate-retry-2"
unit = "rule-persistence-gate-retry"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "Failure-signature table row `FAIL: in-volume canary missing` — classed 'real defect'. The gate never emits that string. This is a symptom-sniff bound to log text the rule does not own, so an operator grepping the verify-local log for it gets 0 hits on a genuine canary failure and falls through to the table's default posture (retry once)."
evidence = ".claude/rules/persistence-gate-retry.md:49 (`| FAIL: in-volume canary missing | real defect | ...`). What the gate ACTUALLY prints: mise.toml:553 `FAIL: TMPDIR canary lost across restart` and mise.toml:556 `FAIL: TMPDIR canary content mismatch (expected ..., got ...)`. Control arm, same command shape over the same corpus: the sibling row's string `drifted across stop/up` matches at mise.toml:541, so the grep discriminates — the miss is real, not a broken probe."
native_alternative = "No harness/tool feature applies; the in-repo fix is to bind the row to the emitter's real strings and pin them with the repo's existing token-contract mechanism (`require_tokens` / `per_path_tokens` in python/verification/suites.toml, chosen via `mise run token-check`), so a later rewording of the FAIL text fails a contract instead of silently orphaning a triage row."
alternative_evidence = "python/verification/suites.toml:55 (`handler = \"require_tokens\"`) and :57 (`per_path_tokens = { \".devcontainer/Dockerfile\" = [...] }`) — 152 `[[suite]]` blocks exist, so the mechanism is live, not aspirational. Emitter anchors: mise.toml:553, mise.toml:556."
rationale = "A stale fact with a one-line fix. The row is not merely mis-worded: it is the discriminator between 'retry once' and 'stop and triage a home-volume mount regression', so a wrong string pushes a real defect into the environmental bucket."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-persistence-gate-retry-3"
unit = "rule-persistence-gate-retry"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "Failure-signature table row `R[123] ... not works` — classed 'real defect, do NOT retry'. The literal `not works` is emitted nowhere in the repo; `works` appears only in the R-gates' SUCCESS lines, so the row cannot match any failure output."
evidence = ".claude/rules/persistence-gate-retry.md:50. Probe: `grep -rn \"not works\" mise.toml python/src/dotfiles_setup/ scripts/` -> 0 hits; control arm `grep -c \"works\" mise.toml` -> 27, and those hits are success lines (mise.toml:994 `OK: R3 container is ... on all three signals`, mise.toml:1016 `OK: R1 inbound ssh ... works`). Real R-gate failure strings: mise.toml:975 `FAIL: no running devcontainer named ...`, :982 `FAIL: image arch is ..., expected ...`, :987 `FAIL: uname -m is ..., expected ...`, :992 `FAIL: arch is ..., expected ...`."
native_alternative = "Same as finding 2 — no native feature; replace the pseudo-signature with the four real `FAIL:` strings the R-gates emit and pin them via python/verification/suites.toml token contracts."
alternative_evidence = "python/verification/suites.toml:55 (`require_tokens` handler in live use across 152 suites). Emitter anchors: mise.toml:975, 982, 987, 992."
rationale = "Second instance of the same defect class as finding 2, on the row that guards the R1/R2/R3 invariants — the most expensive row to get wrong, since it is the one that says 'do NOT retry without diagnosing'."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-persistence-gate-retry-4"
unit = "rule-persistence-gate-retry"
kind = "rule"
disposition = "refactor"
confidence = 0.78
custom_thing = "The failure-signature table is hand-maintained and INCOMPLETE with respect to its emitter: the persistence gate has seven distinct `FAIL:` exits, and the table covers two of them (one of which is mis-spelled, finding 2). A failure matching none of the rows has no documented class, and the rule's surrounding prose defaults the reader to 'retry once'."
evidence = ".claude/rules/persistence-gate-retry.md:43-51 (the table). Uncovered emitter exits: mise.toml:508 `FAIL: fewer tools installed ($PRE_COUNT) than mise-system.toml declares ($EXPECTED_MIN)`, :559 `FAIL: ~/.bashrc missing after restart`, :560 `~/.zshrc`, :561 `~/.profile`, :562 `FAIL: ~/.ssh missing after restart`, :563 `FAIL: ~/.local/share/mise missing after restart` — all real defects, none in the table."
native_alternative = "No harness native. The in-repo mechanism that closes the class rather than the instances: a `require_tokens`/`per_path_tokens` contract in python/verification/suites.toml asserting that every `FAIL:` string in `[tasks.persistence]` appears in this rule's table (and vice-versa), so adding a gate exit without a triage row fails `mise run verify`."
alternative_evidence = "python/verification/suites.toml:8 (the handler list comment naming `forbid_tokens / require_tokens / per_path_tokens`), :55, :57; token selection tooling: .claude/skills/token-check/SKILL.md (`mise run token-check -- <file> \"<token>\"`)."
rationale = "Findings 2 and 3 are instances; this is the class. Per .claude/rules/probes-need-a-control-arm.md rule 9, a check that sniffs for a symptom string it does not own silently becomes a no-op when the emitter is reworded — here it already has, twice, and nothing reported it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-persistence-gate-retry-5"
unit = "rule-persistence-gate-retry"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "Two cost figures for the SAME operation contradict each other inside this file: step 3 says re-running `dev-rebuild` 'costs ~30 min on this Mac' (line 25-26) while the land-smoke section says the same rebuild is 'a ~21.5GB pull that can take hours' (line 69, repeated line 90). Separately, the image size diverges from two sibling eager rules that say ~38GB."
evidence = ".claude/rules/persistence-gate-retry.md:25-26 ('rebuild costs ~30 min on this Mac'), :69 ('a ~21.5GB pull that can take hours'), :90 ('~21.5GB pull'). Divergent siblings: .claude/rules/verify-before-advancing.md:66 and :76 ('~38GB image' / '~38GB buildkit pull'), .claude/rules/mise-tasks-only.md:15 ('classic pull wedges on ~38GB')."
native_alternative = "No native feature — this is an inherited-number hygiene fix. Re-derive from the docker image store and state the condition with the number."
alternative_evidence = "Live measurement this session (docker CLI 29.7.2, host Docker Desktop): `docker image ls --format '{{.Repository}}:{{.Tag}} {{.Size}}'` -> `ghcr.io/ray-manaloto/dotfiles-devcontainer:dev 21.4GB` (also `vsc-dotfiles-273897ea-amd64:latest 21.4GB`). Control arm: the same command in the same run returned varied sizes for other images (mcp/grafana 209MB, ghcr.io/github/github-mcp-server 69.2MB), so it is not printing a constant. First attempt at this probe was BLIND — `timeout docker image ls` failed with `mise ERROR No version is set for shim: timeout` and produced no docker output at all; re-run without the shim."
rationale = "This rule's ~21.5GB is CONFIRMED current (21.4GB measured); the ~38GB in verify-before-advancing.md and mise-tasks-only.md is the unreconciled figure and should be flagged to the synthesis stage as an out-of-unit item. In-unit, the fix is the ~30 min vs 'can take hours' contradiction: both describe `mise run dev-rebuild`, and the rule uses the cheap figure to argue AGAINST rebuilding and the expensive one to argue against it again — carry each number with its condition (warm local layers vs a cold registry pull) per .claude/rules/probes-need-a-control-arm.md rule 6."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-persistence-gate-retry-6"
unit = "rule-persistence-gate-retry"
kind = "rule"
disposition = "keep"
confidence = 0.88
custom_thing = "The core mechanism itself: a human/agent retry-once heuristic for a network-sensitive gate (`@devcontainers/cli up` re-resolving the sshd feature mid-cycle), plus the two host-health probes in step 2 (`dscacheutil -q host -a name ghcr.io` returning an ip_address; `curl -sI https://ghcr.io/v2/` returning 405)."
evidence = ".claude/rules/persistence-gate-retry.md:3-8 (why the gate is network-sensitive), :12-29 (the four-step heuristic), :63-66 (`mise run smoke` as the discriminating probe for the land-smoke transient). Wiring verified: mise.toml:1048 `[tasks.verify-local]` runs `up -> smoke -> persistence` sequentially; mise.toml:457 `[tasks.persistence]` calls `mise run up` mid-body (mise.toml:525, the network-touching call the rule names); `land` runs post-merge `sync_main(...)` at python/src/dotfiles_setup/pr.py:906, which is why the land-smoke transient surfaces there."
native_alternative = "None found. mise has no task-level retry/backoff: its task schema exposes `depends`, `depends_post`, `wait_for` but no `retry` key; `@devcontainers/cli` 0.89.0 exposes no retry flag for `up`; hk 1.58.1 and mise 2026.9.3 shipped zero new capabilities over the pins."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/schema/mise-task.json:118 — `wait_for` present; `grep -rn '\"retry\"' schema/` -> 0 hits, control arm `grep -rn 'wait_for' schema/` -> hits at schema/mise.json:3193 and schema/mise-task.json:118, so the absence is real. `grep -rn 'retry' --include=\"*.md\" docs` in the mise tree hits only cache/OCI/brew internals (docs/errors.md:73, docs/dev-tools/mise-oci.md:157), never task config. Gap digests: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/raw/mise-releases-since-pin.digest.md ('ZERO releases strictly newer than the baseline'), .agent/kb/raw/hk-releases-since-pin.digest.md ('None' — v1.58.1 is documentation-only). Step-2 probes re-verified LIVE this session: `curl -sI -o /dev/null -w '%{http_code}' https://ghcr.io/v2/` -> 405, control arm on a bogus path `https://ghcr.io/v2/zzqx-nonexistent-probe-path-9f3/` -> 404 (so 405 is a real discriminating answer, not a constant); `dscacheutil -q host -a name ghcr.io` -> `ip_address: 140.82.112.34`."
rationale = "Required justification: nothing native retries or classifies this. mise cannot retry a task, the devcontainers CLI cannot retry `up`, and no Claude Code feature at 2.1.266 re-runs a failed Bash command or triages its stderr — the 2.1.258-2.1.266 digest's only output-related additions are `bashOutputMaxChars`/`taskOutputMaxChars` (2.1.261), which change how much output reaches Claude inline, not whether a wrapper's exit code is truthful. That last point also leaves the rule's warning at :73-75 ('the task notification cannot be trusted here') standing unrefuted at the host version. Both step-2 host probes were re-armed live and still return exactly what the rule says. Keep the mechanism; apply findings 1-5 to where it lives and what strings it cites."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-probes-need-a-control-arm-1"
unit = "rule-probes-need-a-control-arm"
kind = "rule"
disposition = "refactor"
confidence = 0.82
custom_thing = "The rule mandates a MANUAL fail-arm for every hk gate: 'Arm the positive ... reintroduce the bug and confirm it fails' (rule 2), scoped by 'Applies to ... the FAIL direction of every gate added to hk.pkl or suites.toml'. hk.pkl records the repo DEFERRING hk's native step tests, with the stated reason that `hk test` has no sandbox and 'left written test files behind'."
evidence = ".claude/rules/probes-need-a-control-arm.md:54-55 (arm the positive), :146-148 (Applies to ... hk.pkl or suites.toml); hk.pkl:697-703 (#160 T12 deferral: 'step `tests{}` + the hk_test builtin + `hk test` in CI are DEFERRED ... executes test cases IN THE REAL PROJECT DIRECTORY (no sandbox) ... Needs upstream sandboxing'); hk.pkl:232 (no_grep_q_under_pipefail, the one gate whose inverse IS machine-enforced, and it has no declared test)"
native_alternative = "hk's native step tests: a `tests { ... }` mapping on any step, run by `hk test` (--list/--step/--name), with `expect.code` to REQUIRE a non-zero exit on a violating input, `write`/`fixture` to build the violating fixture, and `{{tmp}}`/`tmpdir` to run it in a temporary sandbox. This makes the rule's manual 'reintroduce the bug' arm a declarative, every-run property of the gate."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/pkl/Config.pkl:931-1004 (tests mapping; '`fixture` copies a directory into a temporary sandbox', '{{tmp}}: sandbox path used to execute the test', 'expect supports code (default 0), stdout, stderr, files'); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/src/step_test.rs:22-33 (fixture, write, tmpdir fields) and :44-56 (StepTestExpect.code); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/cli/test.md:3-13; /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:1919 (sandbox opt-in via {{tmp}}, hk 1.9.1) and :916 (tmpdir step-test option, hk 1.36.0) — both far below the pinned hk 1.57.0 (Cargo.toml version = 1.57.0; pin at .config/mise/conf.d/shared.toml:31). LIVE PROBE (this host, rc=0): `mise exec -- hk test --list` => 'total: 94' tests, all from hk BUILTIN steps (actionlint, typos, gitleaks, shellcheck, ...); grep for any repo-authored step name (no_lint_skip|no_grep_q|bash_logic|claude_md|no_env_dump|require_pipefail|md_size) returned rc=1 while the same file yields 28 builtin step names — control-armed, so the absence is real."
rationale = "The deferral premise the repo recorded ('no sandbox') is STALE: sandboxing landed in hk 1.9.1 and the `tmpdir` option in 1.36.0, and the pinned 1.57.0 is already executing 94 builtin step tests against this very config. So the FAIL direction of every custom hk gate can be bound declaratively (expect.code = 1 on a fixture the gate must reject) instead of by a remembered manual mutation. Refactor, not retire: `hk test` covers only hk steps — the rule's ad-hoc probes (find/curl/grep/liveness) and its suites.toml half have no hk equivalent, and hk's `expect.stdout`/`stderr` are SUBSTRING checks, which is exactly the no-op shape rule 2 warns about, so the rule's judgment content still governs how a test is written."
probe = "mise exec -- hk test --list > /tmp/hk-tests.txt 2>&1; echo rc=$?; grep -cE 'no_lint_skip|no_grep_q_under_pipefail|bash_logic_budget' /tmp/hk-tests.txt   # expect rc=0 and count 0 today; after adopting, each custom gate should list a violation-case test"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-probes-need-a-control-arm-2"
unit = "rule-probes-need-a-control-arm"
kind = "rule"
disposition = "refactor"
confidence = 0.72
custom_thing = "The rule's canonical worked example, and the memory it links, prescribe a hand-rolled agent-liveness probe over transcript files on disk ('find ... -name agent-*.jsonl reported AGENT DEAD'; memory feedback_agent_spawn_liveness = 'verify the transcript exists AND grows'). The lesson taught is 'fix your glob', not 'stop globbing transcripts'."
evidence = ".claude/rules/probes-need-a-control-arm.md:16-20 (the find/glob false negative) and :158-159 (See also: Memory feedback_agent_spawn_liveness — 'the liveness probe this rule's headline example broke, now corrected'); memory file present at ~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/memory/feedback_agent_spawn_liveness.md"
native_alternative = "Harness-native agent discovery and liveness: the `ListAgents` tool / `/list-agents` (lists subagents, other sessions AND live teammates, marks Remote Control peers `offline`), and `SendMessage`'s `notify_when_idle` input, which asks a session on this machine to send back one notice when it next goes idle or exits. Crucially, `ListAgents` now REPORTS an incomplete listing instead of silently treating unseen sessions as absent — the native fix for the exact bound-limited-search failure this rule was written about."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/changelog.md:482 (under '## 2.1.239' at :430) '`ListAgents` and `/list-agents` now list your live teammates (previously only subagents and other sessions appeared, so a reachable teammate looked absent)'; :632 (under '## 2.1.234' at :597) '`SendMessage` and `ListAgents` now say when your account's session list was too long to check completely, instead of treating unseen sessions as absent'; :736 ('`ListAgents` now marks disconnected Remote Control sessions as `offline`'); cross-session-messaging.md:92,102,106 (notify_when_idle, 'Requires Claude Code v2.1.236 or later in both sessions'), :127 (offline status). Both are live in THIS session (host 2.1.266): `ListAgents` is in the tool list and the session prompt names four addressable agents."
rationale = "For the class the rule uses as its headline — 'is that agent alive?' — the correct move at 2.1.266 is no longer a better glob but the native tool, which cannot produce the rule's false negative and explicitly distinguishes 'not listed' from 'could not check'. The rule should say so where it tells the story, and the memory it links should point at ListAgents/notify_when_idle. Refactor rather than retire, and the coverage is genuinely partial: sub-agents.md:391 states a BACKGROUND subagent does not keep `ListAgents`, and cross-session-messaging.md:117 states a subagent or teammate setting `notify_when_idle` gets no subscription — so in exactly the delegated lanes this repo runs, an on-disk check is still sometimes the only route, and the rule's arming discipline still binds it."
probe = "In a session with a live delegate: call ListAgents and compare with `ls -lt ~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/*.jsonl` — a disagreement is the cross-check rule 7 asks for and names which route is blind."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-probes-need-a-control-arm-3"
unit = "rule-probes-need-a-control-arm"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "Rule 3's catalogue of 'display bounds' that turn absent into unreachable names only bounds the AGENT introduces itself (`| head`, `| tail`, a bare `ls` of a large dir). It does not name the bound the HARNESS imposes on tool output, which no `head`-free command can avoid and which the agent does not choose."
evidence = ".claude/rules/probes-need-a-control-arm.md:69-71 ('display bounds (| head, | tail, a bare ls of a large dir)'), :99-100 (rule 4: distinguish 'answered no' from 'never asked')"
native_alternative = "Claude Code 2.1.261 added the `bashOutputMaxChars` and `taskOutputMaxChars` settings, which raise how much command / background-task output reaches Claude inline (up to 128K characters) before it is spilled to a file; 2.1.265 added a 1 GB cap on tool results saved to disk with the in-conversation preview noting when a saved file was truncated. Together they make the harness's own display bound both visible and adjustable — the native counterpart to rule 3."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — bullet 'bashOutputMaxChars and taskOutputMaxChars settings raise how much command/background-task output Claude receives inline before it is saved to a file (up to 128K characters)', version 2.1.261; bullet '1 GB cap on tool results saved to disk, with the in-conversation preview noting when a saved file was truncated', version 2.1.265. Control arm on the offline corpus: knowledge-base/sources/agent-harness-docs/docs/claude-code/settings.md yields 0 hits for 'OutputMaxChars'/'truncat' while 'permissions' yields 16 in the same file, so the keys are genuinely newer than the snapshot rather than a blind grep — the digest is the citation."
rationale = "The rule is about probes whose bound the reader cannot see; the harness's inline-output cap is precisely such a bound and is now both nameable and raisable by a settings key. Adding one line to rule 3 ('the harness truncates tool output; bashOutputMaxChars / taskOutputMaxChars raise it, and a truncated save is flagged in the preview') keeps the catalogue current. It is a refactor and not a retire: the settings key mitigates one bound, it does not arm any probe."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-probes-need-a-control-arm-4"
unit = "rule-probes-need-a-control-arm"
kind = "rule"
disposition = "refactor"
confidence = 0.55
custom_thing = "The rule's 'Applies to' names hk.pkl and suites.toml as the places whose FAIL direction must be armed, and rule 2's example is a hand-run mutation. It does not name the repo's own probe/control eval harness, which already encodes an armed pair as data."
evidence = ".claude/rules/probes-need-a-control-arm.md:144-148 ('Applies to ... the FAIL direction of every gate added to hk.pkl or suites.toml'), :54-57"
native_alternative = "The shared `kb_setup.evals` engine (SHA-pinned uv git dependency, kb-setup c70f0f8) exposes a declarative `probe=` / `control=` pair per case, which this repo already uses for five cases — including a control arm built expressly to make the probe fail (`_broken_graph_canary`, `_broken_doctor`)."
alternative_evidence = "python/src/dotfiles_setup/eval_cases.py:35 ('from kb_setup import evals'), :218-231 (`_broken_graph_canary` — 'Control arm: drive the canary against a graph that cannot answer'), :334, :348, :359, :369 (`control=_broken_graph_canary`), :379 (`control=_broken_doctor`); python/src/dotfiles_setup/token_audit.py:114 (the token 'control=' is itself contract-bound)"
rationale = "This is the one place in the repo where 'a probe plus its control' exists as a declared, re-run-on-every-CI pair rather than as a habit — the mechanized form of rules 1, 2 and 5. A rule that catalogues where its discipline binds should point at it, so a new gate's author reaches for the existing `control=` slot instead of re-deriving a manual mutation. Refactor of the 'Applies to' and 'See also' sections only; the harness is repo-local so nothing about the rule's substance retires."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-probes-need-a-control-arm-5"
unit = "rule-probes-need-a-control-arm"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "The rule as a whole: nine judgment rules requiring the reader to prove a probe can produce the opposite answer (arm the negative/positive, distrust bound-limited searches, distinguish 'answered no' from 'never asked', state which arm ran, re-derive inherited numbers, cross-check surprises, arm the fixture, assert capability over symptom)."
evidence = ".claude/rules/probes-need-a-control-arm.md:1-148 in full. Every internal citation it makes still resolves at HEAD: hk.pkl:232 (`no_grep_q_under_pipefail`), tests/AGENTS.md (exists), docs/rules-evidence/probes-need-a-control-arm.md (exists), .claude/rules/verify-before-advancing.md (exists), and both linked memories (feedback_agent_spawn_liveness.md, feedback_pipe_kills_exit_code.md present in the project memory dir). Rule 9's worked case is still live in code: python/src/dotfiles_setup/main.py:665 and hk.pkl:482 both describe the lookahead-canary that asserts the RE2 engine before trusting renovate-config-validator. The rule asserts NO harness anchor (no $CC path, no hook event, no settings key), so there is no stale harness fact to correct."
native_alternative = "None. Searched the 2.1.266-current surface: hooks.md hook events (PreToolUse/PostToolUse/Stop/SessionEnd) can deny or annotate a tool call but evaluate no counterfactual; permission rules gate WHICH command runs, not whether its negative result is meaningful; no settings key, agent field, plugin or slash command in the corpus or in the 2.1.258-2.1.266 gap digest evaluates whether a check can produce the other answer. The only mechanized instances anywhere are repo-local and cover single shapes (hk.pkl:232 for `grep -q` under pipefail; eval_cases.py `control=` for five named cases)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md (full event list; nothing evaluating a probe's discriminating power) and .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 new capabilities, none about verification of a check's fail arm) plus .agent/kb/raw/claude-code-llms-index.digest.md ('zero new-capability bullets'). Control arm for the negative search: the same corpus/grep shape returns 439-scale hits for tokens known present ('PreToolUse' matches 10+ files listed), so a 0-result on a verification feature is a real absence, not a blind probe."
rationale = "KEEP with justification, not by default: this rule governs a JUDGMENT — whether the reader believes a result — and no harness feature at 2.1.266 can observe that decision. Findings 1-4 mechanize or update four specific SHAPES it covers (hk gate fail-arms, agent liveness, the harness's own output bound, the repo's probe/control pairs); the residue — arming an ad-hoc find/curl/grep, noticing a token spelling, re-deriving an inherited number, arming the fixture — has no native counterpart and is behaviour-triggered, so per md-size-budgets.md's trigger test it must also stay EAGER rather than being scoped or demoted to a skill. Nothing in it is forbidden-but-now-safe: every prohibition it carries (bounded searches, symptom sniffing, unarmed negatives) remains as true at 2.1.266 as when written."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-real-integration-evidence-1"
unit = "rule-real-integration-evidence"
kind = "rule"
disposition = "keep"
confidence = 0.88
custom_thing = "The rule's core directive: mocks/synthetic subprocesses/self-authored receipts are never the SOLE evidence an integration works; a completion claim for an external CLI, hook, credential path, Graphify release, devcontainer or cross-repo dependency needs one real invocation through the public project entrypoint plus its real failure/control arm, else mark the capability unverified."
evidence = ".claude/rules/real-integration-evidence.md:3-11 (whole file is 12 lines, 585 B; it asserts NO harness fact - no hook event, no settings key, no precedence claim, no $CC doc anchor - so audit axis (a) has nothing that can be stale). Cited as a live policy from AGENTS.md:111."
native_alternative = "None. Nothing at Claude Code 2.1.266 evaluates the epistemic status of evidence. The complete hook-event list (/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1085-2787: SessionStart, Setup, InstructionsLoaded, UserPromptSubmit, PreToolUse, PermissionRequest, PostToolUse, PostToolUseFailure, PostToolBatch, PermissionDenied, SubagentStart/Stop, TaskCreated/Completed, Stop, StopFailure, ...) gates tool CALLS and their inputs/outputs; no event sees whether a subprocess was real or a fixture."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1085 (## Hook events) through :2787 - enumerated, not asserted; plus .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (all 14 new capabilities in 2.1.258-2.1.266 are plugin-dir hot reload, tool-result disk cap, bashOutputMaxChars/taskOutputMaxChars, --append-subagent-system-prompt-file, /skill-doctor, org-policy line, /diff panel, prompt-cache-miss cause, /reload-plugins headless, /advisor text form, managedMcpServers, --permission-prompts none, glab MR recognition, claude plugin validate --json - none touches evidence quality). Control arm for the negative: the same corpus DOES answer adjacent questions - code-review.md:190 documents a 'Verification bar' - so the grep is not blind."
rationale = "Required justification: the directive is an epistemic standard about what a piece of evidence PROVES, and no harness feature at the host's versions or any repo pin (hk 1.57.0, mise 2026.9.3, pytest, ast-grep 0.45.3) can distinguish a real invocation from a fixture that returns the same bytes - that judgement is made when the claim is written, before any tool call the harness could hook. The rule also asserts nothing version-bound: no doc anchor, no settings key, no hook name, and its one named third-party ('Graphify release') is still current (.agent/kb/raw/graphify-readme-0.9.56.digest.md reports 0 new capabilities vs the 0.9.53 pin). Keep as-is; it is behaviour-triggered, so it correctly stays unscoped/eager per .claude/rules/md-size-budgets.md, and at 585 B it is far inside the rule_unscoped budget."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-real-integration-evidence-2"
unit = "rule-real-integration-evidence"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "The completion bar is delivered ONLY as eager prose read at session start ('a completion claim ... requires at least one real invocation through the public project entrypoint plus its real failure/control arm'), with no surface that re-states it to a reviewer at PR time."
evidence = ".claude/rules/real-integration-evidence.md:6-9; the only pointer to it in the eager corpus is AGENTS.md:111. No hk step, no python/verification/suites.toml contract, and no REVIEW.md carries it (control-armed: `ls REVIEW.md .claude/REVIEW.md` -> both 'No such file or directory' in /Users/rmanaloto/dev/github/ray-manaloto/dotfiles)."
native_alternative = "Claude Code's native `REVIEW.md` surface for Code Review: a repo-root file whose text reaches every finding and verification agent in the review pipeline, with two documented slots that fit this rule exactly - 'Repo-specific checks' (rules flagged on every PR) and 'Verification bar' (require evidence before a class of finding is posted)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/code-review.md:172 (### REVIEW.md), :174 ('agents in the review pipeline that find and verify findings receive its contents as your repository's review instructions'), :188 ('Repo-specific checks ... Because REVIEW.md reaches every finding and verification agent directly, these land more reliably than the same rules in a long CLAUDE.md'), :190 ('Verification bar: require evidence before a class of finding is posted')."
rationale = "Native covers the DELIVERY half, not the standard itself: the rule's judgement content stays (finding 1), but Anthropic's own docs say a repo-specific check of this shape lands more reliably from REVIEW.md than from the eager instruction corpus, and this repo has no REVIEW.md at all. Refactor = keep the rule as the authored standard and mirror one 'Always check' line into a new repo-root REVIEW.md ('a completion claim for an external CLI, hook, credential path, devcontainer or cross-repo dependency must cite a real invocation plus its failure arm, not a mock or a self-authored receipt'), so the bar is applied by the review pipeline rather than only remembered. Not 'retire': REVIEW.md is read by review agents only, so it cannot replace an eager rule that must bind the AUTHOR mid-session."
probe = "gh api repos/ray-manaloto/dotfiles/installation --jq '.app_slug' ; gh pr view 992 --json reviews --jq '[.reviews[].author.login] | unique'   # settles whether Claude Code Review actually runs on this repo's PRs (a REVIEW.md nothing reads would be dead weight)"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "rule-real-integration-evidence-3"
unit = "rule-real-integration-evidence"
kind = "rule"
disposition = "refactor"
confidence = 0.45
custom_thing = "The 'self-authored receipts' clause - banning a prior report / another agent's output as the sole evidence a capability works - is stated only as a habit, with no named lane that enforces it."
evidence = ".claude/rules/real-integration-evidence.md:3-4 ('Do not use mocks, synthetic subprocesses, or self-authored receipts as the sole evidence that an integration works.') - the file names no agent, task or gate that checks this."
native_alternative = "The installed fable-orchestrator plugin (1.21.0) ships `premise-verifier` provenance enforcement: a PREMISES row whose citation is a review report, a reviewer finding, another agent's output, a conversation summary, or 'verified earlier' without a file:line FAILS provenance and is re-settled against the code itself (REFUTED / UNVERIFIABLE / 'CONFIRMED (provenance corrected)')."
alternative_evidence = ".agent/kb/raw/fable-orchestrator-changelog.md:20 (## 1.20.0 - 2026-08-10) and :24 ('Provenance is part of the premise rule, and the verifier enforces it. A row's citation is a code location the architect read fresh this session - never a review report, a reviewer finding, another agent's output ...'); digest at .agent/kb/raw/fable-orchestrator-changelog.digest.md confirms the installed plugin.json version 1.21.0 is the changelog's top entry, so this capability is live on this host. The lane is already routed in .claude/CLAUDE.md ('Premise verification | fable-orchestrator:premise-verifier (Claude, read-only)')."
rationale = "Native covers PART of the clause and only in the spec lane: the premise-verifier mechanically rejects report-sourced citations in a spec's PREMISES table, which is the same laundering pattern this rule forbids, but it says nothing about mocks or synthetic subprocesses and does not run outside a fable-orchestrator dispatch. Refactor = have the receipts clause name the lane ('for a spec's premises this is machine-checked by fable-orchestrator:premise-verifier - see its provenance rule'), so the rule points at the mechanism instead of re-teaching it, while keeping the mock/synthetic half as pure judgement. Not 'retire' and not 'keep silently': a rule that duplicates a live enforced check without naming it is how the two drift apart."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "rule-research-doc-sources-1"
unit = "rule-research-doc-sources"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "Step 00 asserts the offline KB corpus holds '36 source trees / 6,446 markdown files (measured 2026-08-02)' as the reason to grep it first."
evidence = ".claude/rules/research-doc-sources.md:11-15"
native_alternative = "Re-derive the figure from the corpus itself (the tracked per-tree `.manifest` files under knowledge-base/sources/ make it countable), or drop the frozen number and cite the directory. Measured 2026-09-09: 100 source trees, 40,846 markdown files."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ — `ls -d */ | wc -l` -> 100; `find . -name '*.md' -type f | wc -l` -> 40846 (control arm: the five named subtrees agent-harness-docs/docs/{claude-code,codex,cursor,opencode,pi} all still resolve, so the probe is not reading an empty tree)"
rationale = "The inherited number understates the corpus 6.3x in trees and 6.3x in files. It is load-bearing prose — it is the whole argument for making this step 00 — and per probes-need-a-control-arm rule 6 an inherited number restated without re-derivation is not a measurement. The step itself is correct and should stay; only the frozen figure is stale. Replace it with the directory citation plus a dated re-measurement, or delete the count."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-research-doc-sources-2"
unit = "rule-research-doc-sources"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "The chain mandates a strict top-to-bottom order (00 -> 0 -> 1 -> 2 -> 3 ctx7 -> 4 raw HTML) with 'never skip a step that would have worked', and prescribes the ctx7 two-step by hand (`ctx7 library`, then `ctx7 docs`)."
evidence = ".claude/rules/research-doc-sources.md:3-7 and :63-74"
native_alternative = "A repo-tracked auto-loading skill, `.claude/skills/find-docs/SKILL.md`, already covers step 3 and its description instructs the model to enter at ctx7 for *any* library/CLI/SDK question ('Always use for: API syntax questions, configuration options, ... CLI tool usage' / 'Prefer this over web search'), with no mention of steps 00/0/1/2. Skills load on relevance ahead of prose, so the skill silently pre-empts the ordering this rule mandates. It also tells the agent to `npm install -g ctx7@latest` / `npx ctx7@latest`, contradicting the repo's mise pin."
alternative_evidence = ".claude/skills/find-docs/SKILL.md:1-35 (description block + 'npm install -g ctx7@latest' / 'npx ctx7@latest'); mise.toml:49 pins `\"npm:ctx7\" = \"0.5.9\"`; .claude/rules/mise-tasks-only.md table row '`npx <tool>` | the mise-pinned binary directly'. Skill relevance-loading: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md"
rationale = "This is the (c) case: a mechanism the rule prescribes as a late step is now performed by itself, earlier, by a skill the rule never names. The rule is the ADR for doc fetching and must either subordinate find-docs (state that it is step 3 and that 00/0/1/2 come first) or the skill must be edited to defer to the chain and to the mise-pinned `ctx7`. Leaving both in place means the documented preference order is unenforced in exactly the sessions it exists to govern. Confidence is not 0.95 only because relevance-triggering is probabilistic rather than deterministic."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-research-doc-sources-3"
unit = "rule-research-doc-sources"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "Step 4 (last resort) prescribes raw HTML fetch via `curl <url>` or `npx @teng-lin/agent-fetch <url>`, with `defuddle` to clean HTML before parsing."
evidence = ".claude/rules/research-doc-sources.md:76-79"
native_alternative = "The built-in `WebFetch` tool: `WebFetchInput = { url: string; prompt: string }` — 'Fetches content from a URL and processes it with an AI model', returning `{bytes, code, codeText, result, durationMs, url}`. It does the fetch-plus-extract that agent-fetch + defuddle were bolted on for, is governable by `WebFetch(domain:...)` permission rules (which a Bash `curl` is explicitly NOT), and needs no unpinned npm package."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__typescript.md:2671-2681 (WebFetchInput + description) and :3592-3612 (WebFetchOutput); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:410-418 (`WebFetch(domain:...)` rules); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/admin-setup.md:113 ('Denying WebFetch blocks Claude's fetch tool, but if Bash is allowed, `curl` and `wget` can still reach any URL')"
rationale = "Neither `@teng-lin/agent-fetch` nor `defuddle` is pinned anywhere in this repo (control arm: the same grep over mise.toml + .config/mise/conf.d/shared.toml returns the `npm:ctx7` pin at mise.toml:49, so the grep discriminates), so step 4 currently mandates an unpinned `npx` in a mise-first repo. Refactor rather than retire: steps 1-2 must stay `curl`, because llms.txt and `.md` are already clean text and paying WebFetch's model-processing pass over them would be a regression. Only the HTML limb of step 4 is superseded."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-research-doc-sources-4"
unit = "rule-research-doc-sources"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "The MCP section asserts unconditionally that 'Claude Code presents MCP tools deferred — names only, loading a schema on demand via ToolSearch — a 33x difference', and instructs 'never refuse a registration on its strength' of the old always-loaded cost."
evidence = ".claude/rules/research-doc-sources.md:97-104"
native_alternative = "Tool search is on by DEFAULT but is conditional and configurable: it is disabled when `ANTHROPIC_BASE_URL` points at a non-first-party host, when `ENABLE_TOOL_SEARCH=false`, when `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS` is set (which `ENABLE_TOOL_SEARCH` cannot override), on Azure-hosted Microsoft Foundry deployments (server-side rejection), and on pre-4.5-generation models on Google Cloud Agent Platform. Separately, a server (or an individual tool) can opt OUT of deferral with `alwaysLoad: true` / `\"anthropic/alwaysLoad\": true`, restoring the upfront cost the rule says no longer exists."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/mcp.md:1376 ('Tool search is enabled by default: MCP tools are deferred... Claude Code disables it when ANTHROPIC_BASE_URL points to a non-first-party host'), :1378 (CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS), :1380 (model compatibility), :1389-1394 (ENABLE_TOOL_SEARCH table), :1419-1439 ('Exempt a server from deferral' / alwaysLoad); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__tool-search.md:18,32-49"
rationale = "The rule's correction of the old 'schema tax forever' claim is directionally right and must stay, but it states as an invariant something the docs state as a default with five named escape hatches plus a per-server opt-out. A rule that says 'never refuse a registration on that strength' is wrong for any session behind a proxy base URL, with betas disabled, or registering an `alwaysLoad` server. Add the condition and name `ENABLE_TOOL_SEARCH` / `alwaysLoad` as the knobs. This repo currently sets none of them (control arm: .claude/settings.json `env` block reads back 8 keys, none of them ANTHROPIC_BASE_URL / ENABLE_TOOL_SEARCH / CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS), so the rule happens to be true here today — which is exactly why the condition needs writing down."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-research-doc-sources-5"
unit = "rule-research-doc-sources"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "Step 0 claims 'Every repo in `docs/research/mintlify-catalog.md` has both `llms.txt` and `llms-full.txt` pre-fetched under `docs/research/mintlify-cache/<owner>/<repo>/`'."
evidence = ".claude/rules/research-doc-sources.md:37-45"
native_alternative = "The catalog's own banner scopes the guarantee to the VERIFIED TABLE, not to the whole file: 'As of 2026-04-07, every repo in the verified table below has both llms.txt AND llms-full.txt downloaded'. The file also carries a request QUEUE whose rows are uncached. Cite the verified table, not the file."
alternative_evidence = "docs/research/mintlify-catalog.md:3-10 (the 'verified table below' banner) and :143-145 (queue rows: lima-vm/lima, abiosoft/colima, openai/codex). Measured 2026-09-09: the catalog names 17 owner/repo pairs, the cache holds 16 directories, all 16 have both files (loop over every cache dir reported zero MISSING); the one uncached catalog repo is openai/codex, a queue row."
rationale = "The rule restated a bounded guarantee without its bound — the same shape probes-need-a-control-arm rule 3 exists to catch. An agent that follows the rule literally looks for openai/codex in the cache, finds nothing, and loses confidence in step 0 as a whole. Two-line fix: say 'every repo in the catalog's verified table', and note that openai/codex's docs are already covered by step 00 (knowledge-base/sources/codex-docs/ and agent-harness-docs/docs/codex/ both exist on disk), which makes that queue row moot rather than outstanding."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-research-doc-sources-6"
unit = "rule-research-doc-sources"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "The rule's discovery guidance for an unknown docs domain is a manual habit: 'do NOT guess a project's docs domain ... Grep the cache or the catalog for the right URL first', with no step for a target absent from both."
evidence = ".claude/rules/research-doc-sources.md:47-49, :51-56"
native_alternative = "The built-in `WebSearch` tool takes `{ query, allowed_domains?, blocked_domains? }`, so the 'which domain hosts these docs' question has a native answer that can be constrained to trusted hosts instead of guessed at."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__typescript.md:2683-2694 (WebSearchInput with allowed_domains/blocked_domains); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__agent-loop.md:148 ('Web | WebSearch, WebFetch | Search the web, fetch and parse pages')"
rationale = "Partial supersession only, which is why this is refactor and not retire: for a catalogued repo the cache is still strictly cheaper and must stay step 0. The gap is the uncatalogued case, where the rule currently offers only 'check the target site' and a warning not to guess — a warning with no alternative is what produces the guess. Add a bounded discovery step (WebSearch with allowed_domains) between step 0 and step 1, explicitly for targets absent from the cache and catalog."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-research-doc-sources-7"
unit = "rule-research-doc-sources"
kind = "rule"
disposition = "unknown"
confidence = 0.50
custom_thing = "The absolute ban 'Never mcp2cli a per-repo mintlify MCP URL' — justified by a 2026-04-07 probe finding those URLs are GET-only preview descriptors whose POST returns 404, and that an API key does not unlock one."
evidence = ".claude/rules/research-doc-sources.md:81-93; the probe of record is docs/research/mintlify-catalog.md:12-28 (validation-log banner, dated 2026-04-07)"
native_alternative = "Unverified. This is a claim about a third party's product surface (Mintlify), not about the harness, so no Claude Code / codex feature at 2.1.266 supersedes it; but Mintlify may have shipped live per-repo MCP endpoints in the ~5 months since the only probe. The rule forbids a mechanism on evidence that has never been re-armed."
alternative_evidence = "No current citation exists — no doc under the offline corpus and no saved URL under .agent/kb/raw/ speaks to Mintlify's per-repo /mcp endpoints (control arm: grepping the same tree for `mintlify` does return the catalog and skill hits, so the corpus is searchable and the absence is real)."
rationale = "Category (c): a ban that may now forbid something the vendor has made work. It cannot be settled offline and must not be reported as either stale or current without a live probe. If POST still 404s, the ban stands unchanged and should get a re-probe date; if it now returns a protocol response, the ban is stale and mcp2cli is back on the table for catalogued repos."
probe = "curl -s -o /dev/null -w 'GET=%{http_code}\\n' https://mintlify.com/jdx/mise/mcp ; curl -s -o /dev/null -w 'POST=%{http_code}\\n' -X POST -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\"}' https://mintlify.com/jdx/mise/mcp ; curl -s -o /dev/null -w 'CONTROL_LIVE_MCP=%{http_code}\\n' -X POST -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\"}' https://docs.anthropic.com/mcp"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-research-doc-sources-8"
unit = "rule-research-doc-sources"
kind = "rule"
disposition = "keep"
confidence = 0.90
custom_thing = "Step 00 itself — 'for AGENT-HARNESS behaviour, grep the knowledge-base's offline sources FIRST', with the `KB=`/`CC=` shell recipe — and step 3's caveat that ctx7's `skills` subcommands still run despite their absence from `--help`."
evidence = ".claude/rules/research-doc-sources.md:11-35 (step 00) and :72-74 (the ctx7 caveat)"
native_alternative = "None. Claude Code has no built-in index of a local vendor-doc corpus: WebFetch/WebSearch reach the network, ToolSearch indexes TOOLS not documents, and Skills load instructions rather than search a doc tree. Nothing in the harness makes a zero-round-trip grep of an on-disk vendor corpus unnecessary."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__agent-loop.md:148 (the built-in tool table lists only WebSearch/WebFetch under 'Web' — both network calls, no local-corpus tool); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__tool-search.md:9 (tool search discovers TOOLS from a tool catalog, not documents)"
rationale = "Required justification: step 00 is the rule's core and no native feature covers it — the corpus is 100 trees / 40,846 md files on local disk (measured 2026-09-09) and the harness offers no local doc index, so the cheapest harness-behaviour lookup remains a grep. Its cited layout also still holds: all five agent-harness-docs/docs/{claude-code,codex,cursor,opencode,pi} subtrees resolve. The ctx7 caveat is likewise still TRUE at the pinned 0.5.9 and should not be tidied away — live probe: `ctx7 --help` lists no `skills` command, yet `ctx7 skills --help` prints the subcommand's own usage (generate/install/search/list/remove/info/suggest). Control arms: `ctx7 library --help` prints its own usage (known-present) while `ctx7 zzqqwwxx-nope --help` falls back to the ROOT usage (known-absent), so the probe discriminates present from absent subcommands."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-research-repo-enumeration-1"
unit = "rule-research-repo-enumeration"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "The rule's Enforcement section declares itself unenforceable on a premise that is now false: \"An hk grep-check on staged docs/research/runs/*.md would be a no-op — that tree is gitignored, so nothing is ever staged from it\", echoed by the Applies-to bullet calling docs/research/runs/**/*.md \"gitignored by default via per-clone exclude\"."
evidence = ".claude/rules/research-repo-enumeration.md:57-60 (Enforcement) and :39-41 (Applies to). Measured 2026-09-09 on branch feat/orchestration-modernization-audit: `git ls-files docs/research/runs | wc -l` -> 92 tracked files (88 of them .md); `git check-ignore -v docs/research/runs` -> rc=1 (NOT ignored), control arm `git check-ignore -v .agent/notepad.md` -> rc=0 matching `.gitignore:106`, so the probe discriminates. `.git/info/exclude` contains only `.claude/**` runtime entries, no research path. `.claude/rules/agent-artifact-conventions.md` lists `docs/research/runs/` in its \"Tracked, durable — survives a clone\" table, directly contradicting this rule."
native_alternative = "An ordinary hk step over staged `docs/research/**/*.md` is now non-vacuous, because the tree is tracked and staged routinely — which is exactly the condition the rule itself names as the trigger to add one (\"becomes worth adding once tracked research artifacts are routine\"). The concrete declarative form is the mdschema step in finding -2."
alternative_evidence = "hk.pkl:355 shows the repo's existing custom-step shape (`check = \"uv run --project python dotfiles-setup token-audit\"`), i.e. a staged-file gate is a one-entry change. Tracked-ness measured above via `git ls-files`/`git check-ignore` with control arm."
rationale = "This is a stale fact, not merely stale prose: it is the sole stated reason the rule stays reviewer-enforced, so correcting it removes the blocker. 536 tracked .md files live under docs/research today (419 carry the section; control arm: 508 of them contain any '##' heading, so the 0/absent arm is not a broken grep). Rewrite Enforcement to say the tree is tracked and wire the gate; leaving the sentence in place preserves a false justification for having no layer."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-research-repo-enumeration-2"
unit = "rule-research-repo-enumeration"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "The prescribed mechanism is a hand-written markdown convention checked by human reviewers — \"Reviewer-enforced, not machine-enforced\" — for a purely structural requirement (a section titled `## GitHub repos touched`, at the end of the file, whose body is `owner/repo` links or the literal `_None._`)."
evidence = ".claude/rules/research-repo-enumeration.md:55-60 (Enforcement), :17-35 (Format block specifying the exact heading, the link shape, and the `_None._` empty form), :3-6 (\"MUST end with\")."
native_alternative = "mdschema 0.15.2 — already pinned in this repo — validates Markdown *structure* against a declarative `.mdschema.yml`: a `structure` array of `StructureElement`s, each with a `heading` pattern (plain text or regex), `optional`, `count {min,max}`, `severity`, and `required_text` (\"Text that must appear in this section\", with regex support) — i.e. \"this document must contain `## GitHub repos touched`, exactly once, containing github.com/<owner>/<repo> links or `_None._`\", enforced by `mdschema check`."
alternative_evidence = "Pin: mise.toml:41 (`\"aqua:jackchuka/mdschema\" = \"0.15.2\"`). Capability, from the saved live probe of the pinned binary: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/raw/mdschema-0.15.2-capability-probe.md:17 (`check  Validate Markdown files against schema`), :269 (\"Document structure defines required/optional sections and their validation rules\"), :299 (\"Simple heading text (e.g., '## Features')\"), :322-324 (`optional`: \"Section is not required\"), :325-327 (`count`: \"Occurrence constraints {min, max}\"), :374 (`required_text`: \"Text that must appear in this section\"). Deferral record: hk.pkl:361-363 — \"rumdl/mdschema builtin adoption DEFERRED ... mdschema requires authoring a schema file that does not exist yet\" — the blocker was an unwritten schema, not a missing capability; no `.mdschema.yml` is tracked (`git ls-files | grep -i mdschema` -> 0 hits, control arm `grep rumdl mise.toml` -> mise.toml:42)."
rationale = "Native tooling covers the checkable half exactly (heading present, once, at the end via the ordered `structure` array, containing the required link text) — and the ordering half has already failed in practice: docs/specs/eval-harness-design.md:772 records the section being pushed off the end by later appends, the failure mode `structure` ordering catches for free. Two traps for the adopter: (1) hk-common.pkl:51-52 excludes `docs/research/runs/**` and `docs/research/kb/**` from ALL hk builtins (to keep persisted agent reports verbatim per agent-report-persistence.md), so the gate must be a custom `check =` step with its own glob, not a builtin — a builtin would silently skip the very tree the rule governs; (2) the check must exempt ingested corpus (`docs/research/kb/raw/**`, `docs/research/mintlify-cache/**`), which are sources, not artifacts. Refactor, not retire: mdschema enforces the section's SHAPE, never its truth (see finding -4)."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && printf 'structure:\\n  - heading: \"## GitHub repos touched\"\\n    count: {min: 1, max: 1}\\n' > /tmp/mdschema-probe.yml && mise exec -- mdschema check --schema /tmp/mdschema-probe.yml docs/research/runs/research-20260715-md-size-limits/report.md; echo \"compliant rc=$?\"; sed '/## GitHub repos touched/,$d' docs/research/runs/research-20260715-md-size-limits/report.md > /tmp/stripped.md && mise exec -- mdschema check --schema /tmp/mdschema-probe.yml /tmp/stripped.md; echo \"stripped rc=$? (must be non-zero for the gate to discriminate)\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-research-repo-enumeration-3"
unit = "rule-research-repo-enumeration"
kind = "rule"
disposition = "refactor"
confidence = 0.75
custom_thing = "The rule's Applies-to / Not-applies-to lists scope the requirement to `docs/research/**` and explicitly exempt \"Rule files, skill files, CLAUDE.md\", and the rule carries no mechanism for pushing the requirement into an agent's prompt — it is stated only in an eager rule file the delegate may never see."
evidence = ".claude/rules/research-repo-enumeration.md:37-45 (Applies to) and :47-53 (Not applies to, line 53 = \"Rule files, skill files, CLAUDE.md\"). Measured: 55 tracked .md files OUTSIDE docs/research carry a literal `^## GitHub repos touched` heading, including `.claude/skills/memory-index-curation/SKILL.md:210` (an exempted skill file) and every authored receipt (e.g. docs/receipts/448.md:216-219)."
native_alternative = "Claude Code subagent definitions — the markdown body of a `.claude/agents/*.md` file IS the subagent's system prompt, so the requirement rides every delegation instead of depending on the delegate reading a rule file. Six agent definitions already embed the section as their required output template."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:241 (\"Subagent files use YAML frontmatter for configuration, followed by the system prompt in Markdown\") and :225 (the `--agents` JSON `prompt` field is \"the system prompt, equivalent to the markdown body in file-based subagents\"). In-repo adoption: `grep -rln 'GitHub repos touched' .claude/agents` -> 6 files (staleness-auditor.md:164, codex-staleness-auditor.md, adversarial-critic.md, codex-adversarial-critic.md, claude-code-expert.md, codex-claude-code-expert.md). The doctrine is already recorded next door: .claude/rules/agent-report-persistence.md §1b — \"A rule nothing pushes into the prompt is not a layer.\""
rationale = "Two drifts in one place. (a) Practice has outgrown the declared scope: receipts and skill files now carry the section, so \"Not applies to: skill files\" is false as written and a future mdschema glob built from this list would under-cover. (b) The rule's own enforcement story omits the layer that actually works today — the agent-definition system prompt — which is the native Claude Code mechanism for making an instruction reach a delegate. Refactor: restate Applies-to as \"any findings-bearing artifact\" (matching agent-report-persistence.md's language), and name the agent-definition body as the push layer alongside the mdschema gate."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-research-repo-enumeration-4"
unit = "rule-research-repo-enumeration"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "The rule's core directive itself: an agent must enumerate, in its own words, every owner/repo whose source, README, issues, docs or mintlify site it consulted while producing an artifact, one line of reason each."
evidence = ".claude/rules/research-repo-enumeration.md:3-6 (the MUST), :28-33 (what counts as touched), :8-15 (the Why — a cheap-to-grep index feeding docs/research/mintlify-catalog.md, which still exists: docs/research/mintlify-catalog.md, 11,581 bytes)."
native_alternative = "None at Claude Code 2.1.266. No harness feature records or emits the set of external repos a session consulted, and nothing in the 2.1.258-2.1.266 gap window adds one; mdschema (finding -2) can assert the section EXISTS but has no way to know which repos were actually read, so the semantic content stays agent-supplied."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — all 14 new capabilities in the gap window (`--plugin-dir` folders, tool-result disk cap, `bashOutputMaxChars`/`taskOutputMaxChars`, `--append-subagent-system-prompt-file`, `/skill-doctor`, org-policy status line, `/diff` panel, prompt-cache-miss cause, `/reload-plugins` headless, text `/advisor`, `managedMcpServers`, `--permission-prompts none`, `glab` MR recognition, `claude plugin validate --json`); none concerns source or citation enumeration. Corpus probe: `grep -rln \"repos touched\" /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/` -> 0 files, control arm `grep -rln \"PreToolUse\"` over the same directory -> hits (costs.md, agent-sdk__hooks.md, permission-modes.md), so the 0-result arm is a real absence, not a broken grep."
rationale = "Required justification for 'keep': the enumeration is a judgement artifact — which repos a research pass genuinely relied on — that no tool can derive after the fact. The harness does not log external doc fetches per repo, mdschema validates shape not truth, and graphify indexes THIS repo's code, not consulted third parties. Measured compliance shows the habit is live and productive (85 of 88 tracked .md under docs/research/runs carry the section; control arm: 508 of 536 tracked docs/research .md contain any '##' heading, so the miss arm is real). Keep the directive; only its enforcement story (-1, -2) and its scope (-3) need changing."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-secrets-out-of-the-shell-env-1"
unit = "rule-secrets-out-of-the-shell-env"
kind = "rule"
disposition = "refactor"
confidence = 0.72
custom_thing = "Rule 7's load-bearing premise that a probe's stdout is structurally uncovered: \"Every gate above guards a *file write* or a *spawn*; none guards the output of a command an agent runs, and that output lands in the session transcript\" and \"Gap tracked in #474, still OPEN: one shape is now gated (below), every other shape is carried by this rule alone.\" The same claim is baked into the guard's deny text (\"stdout IS the session transcript, and nothing downstream redacts it\"), and the enforcement it ships is a PreToolUse regex over the COMMAND (_PRINTS_SECRET matches echo/printf/print followed by ${CREDENTIAL_NAME}) -- i.e. shape-matching the invocation, never the output."
evidence = ".claude/rules/secrets-out-of-the-shell-env.md:81-89 (rule 7 head), :97-100 (the one gated shape); python/src/dotfiles_setup/hook_guard.py:204-206 (_PRINTS_SECRET regex over the command text), :530-543 (Rule \"secret_value_substitution\", deny text asserting \"nothing downstream redacts it\"); gh issue view 474 -> state OPEN, title \"Nothing stops a secret VALUE reaching the session transcript - every existing layer guards a file write or a spawn\""
native_alternative = "PostToolUse hook `hookSpecificOutput.updatedToolOutput` -- replaces a tool's result before it is sent to Claude. The docs name redaction as the intended use case and the canonical worked example is literally a Bash result rewritten to {\"stdout\": \"[redacted]\", ...}. Unlike _PRINTS_SECRET this operates on the OUTPUT, so it is shape-independent: a PostToolUse redactor can scan the actual stdout for the 56 declared credential VALUES and blank them regardless of which shell construct produced them -- exactly rule 7's \"every other shape\" residue. The repo already wires a PostToolUse hook, so this is a handler change, not new infrastructure."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:1031 (`PostToolUse`: `updatedToolOutput` replaces the tool's result), :1034 (\"For redaction or transformation use cases, intercept at `PreToolUse` for outbound tool inputs and `PostToolUse` for inbound tool results\"), :1967 (field definition), :1977-1978 (example rewriting a Bash result to \"stdout\": \"[redacted]\"); repo wiring: .claude/settings.json hooks events include PostToolUse (probed: keys ['InstructionsLoaded','PostToolUse','PreToolUse','SessionEnd','SessionStart'])"
rationale = "The absolute claim (\"none guards the output\", \"nothing downstream redacts it\") is a stale harness fact at 2.1.266: the harness ships an output-rewrite seam and documents redaction as its purpose. But it does NOT fully retire rule 7, for two documented reasons and one unknown. (1) hooks.md:1988 warns updatedToolOutput \"only changes what Claude sees\" -- the command has already run, and OpenTelemetry tool spans and analytics capture the ORIGINAL output before the hook fires, which matters here because .claude/settings.json sets OTEL_LOG_RAW_API_BODIES. (2) MessageDisplay (hooks.md:1416, \"redact API keys or internal hostnames from Claude's responses\") is the tempting-looking lever and is the WRONG one: it is display-only and \"the transcript and what Claude sees keep the original\". (3) Unknown: whether the session .jsonl transcript persists the original or the replacement -- rule 7's stated harm is the transcript, so that decides retire-vs-refactor of the transcript half. Correct action: keep the doctrine, and add a PostToolUse value-redactor as the layer #474 says does not exist; then re-word rule 7 and the guard's deny text from 'impossible' to 'configured here'."
probe = "Wire a throwaway PostToolUse hook returning {\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"updatedToolOutput\":{\"stdout\":\"[redacted]\",\"stderr\":\"\",\"interrupted\":false,\"isImage\":false}}} for Bash; run `printf 'CANARY-%s\\n' \"$(date +%s)\"`; then grep the session transcript jsonl under ~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/ for the canary string. Control arm: run the same command with the hook disabled and confirm the canary IS present. Hit => transcript keeps the original (refactor stands); miss with a passing control arm => the transcript half of #474 is closable natively."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-secrets-out-of-the-shell-env-2"
unit = "rule-secrets-out-of-the-shell-env"
kind = "rule"
disposition = "refactor"
confidence = 0.62
custom_thing = "The rule's blast-radius claim, stated as a fact about the world: \"There is no blast-radius cap any more. Under `env = true` **all 50** are printable by any probe, wrapped or not\" / \"assume every credential is reachable from any shell\", and rule 2's \"`fnox exec` is no longer a confinement boundary ... Rules 1, 4 and 7 are now the only things between 50 credentials and a transcript or a commit -- there is no second line behind them any more.\""
evidence = ".claude/rules/secrets-out-of-the-shell-env.md:102-107 (no blast-radius cap; all 50 printable), :49-55 (rule 2: no second line behind rules 1/4/7)"
native_alternative = "`sandbox.credentials.envVars` (Claude Code v2.1.187+): `{\"name\":\"X\",\"mode\":\"deny\"}` unsets a variable before each sandboxed Bash command; `{\"name\":\"X\",\"mode\":\"mask\"}` (v2.1.199+) shows the command a per-session sentinel while the sandbox proxy swaps the real value back only for `injectHosts`, so \"the command and anything it logs never hold the real credential, but its requests still authenticate\" -- a value a probe prints is then a sentinel, not a live token. Broader, non-sandbox lever: `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1` strips recognized credentials from Bash-tool, hook and MCP-stdio subprocess environments while the parent Claude process keeps them for API calls."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sandboxing.md:278 (sandbox.credentials, v2.1.187+), :280 (deny unsets env vars before each sandboxed command), :323 (mask, v2.1.199+), :325 (per-session sentinel; \"anything it logs never hold the real credential\"), :315 + :687 (scope limit: sandboxed Bash only; use CLAUDE_CODE_SUBPROCESS_ENV_SCRUB for all subprocesses); env-vars.md:366 (SUBPROCESS_ENV_SCRUB semantics); changelog.md:3670 (introduced). Repo state probed: .claude/settings.json has NO `sandbox` key and its `env` block does not set CLAUDE_CODE_SUBPROCESS_ENV_SCRUB."
rationale = "The rule was written 2026-08-02/03 and its 'there is no cap' sentence is now a statement about THIS repo's configuration, not about the harness. Two native caps exist at 2.1.266 and neither is configured. It is a refactor rather than a retire because adopting either has real, citable costs the rule should record instead of silently inheriting: SUBPROCESS_ENV_SCRUB directly contradicts Ray's stated requirement that credentials be \"available to all terminals and ai/llm agents\" (it strips them from the Bash tool, hooks and MCP stdio servers, which is the entire point of the 2026-08-02 reversal), and `mask` requires sandboxing enabled plus `network.tlsTerminate` and per-host `injectHosts`, is honoured ONLY from user/managed settings (sandboxing.md:362 -- Claude Code ignores mask entries in a repository's .claude/settings.json), which collides with the standing feedback_no_user_level_file_updates rule. So the fix is documentary + a decision: state the two native caps, state which is declined and why, and stop asserting that no cap exists."
probe = "claude doctor (or `/status`) on 2.1.266 to confirm the sandbox credential checks are present, then a scoped trial: add {\"sandbox\":{\"enabled\":true,\"credentials\":{\"envVars\":[{\"name\":\"DOPPLER_TOKEN\",\"mode\":\"deny\"}]}}} to a --settings file, run `printenv DOPPLER_TOKEN >/dev/null; echo rc=$?` inside a sandboxed Bash call and expect rc=1, with the control arm (no settings file) returning rc=0. Presence flags only -- never print the value."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-secrets-out-of-the-shell-env-3"
unit = "rule-secrets-out-of-the-shell-env"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "The credential count the rule reasons with is internally inconsistent and stale: the header says \"56 sanctioned as of 2026-08-29\", but the body repeatedly says 50 -- \"With 50 credentials in every child instead of 4, the blast radius ... is 12.5x larger\" and \"all 50 are printable by any probe\". The 12.5x figure is derived from 50/4, so it moves with the count."
evidence = ".claude/rules/secrets-out-of-the-shell-env.md:3-7 (56 sanctioned), :18-19 (\"50 credentials\", \"12.5x larger\"), :102 (\"all 50\"); ground truth probed: doctor.toml:39-96 `env_true` list contains exactly 56 entries (counted, not asserted)"
native_alternative = "None -- this is a repo-fact drift, not a harness mechanism. The authoritative source already exists in-repo as doctor.toml's `[fnox].env_true` set, which the rule itself designates as the reviewed home for the list (rule 5), so the fix is to stop restating a count the rule cannot keep in sync and cite doctor.toml instead."
alternative_evidence = "doctor.toml:39 (`env_true = [`) through :96 (`]`), 56 entries; .claude/rules/secrets-out-of-the-shell-env.md:60-68 (rule 5: a new secret \"must be added to doctor.toml's env_true set in the same reviewed diff\")"
rationale = "Two different numbers for the same set inside one file, with a derived ratio (12.5x) attached to the stale one, is exactly the misattribution shape md-size-budgets.md records: a real figure that travelled without its condition. The count is verifiable in one place the rule already points at; replace both body figures with a reference to doctor.toml's env_true set (and drop or re-derive the 12.5x), so the next credential addition cannot silently falsify the rule."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-secrets-out-of-the-shell-env-4"
unit = "rule-secrets-out-of-the-shell-env"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "The remainder of the rule: the fnox/Doppler/macOS-keychain diagnosis chain (a keychain authorization dialog hangs non-GUI processes and is NOT a locked keychain; `security show-keychain-info` prompts unconditionally so its hang proves nothing; fnox's doppler provider shells out to the `doppler` CLI so a hung doppler hangs every uncached read), rule 1 (never commit an env dump; __MISE_DIFF is zlib+base64 so scanners see nothing), rule 3 (do not mark a non-secret as a secret), rule 4 (ask what a clean scanner can see), rule 5 (a new secret is a reviewed doctor.toml diff), rule 6 (diagnose by layer; never `fnox get`; MISE_ENV_CACHE serves dead names)."
evidence = ".claude/rules/secrets-out-of-the-shell-env.md:21-38 (keychain/doppler diagnosis), :42-48 (rule 1), :56-59 (rules 3-4), :60-68 (rule 5), :69-80 (rule 6); enforcement + backing artifacts all present: hk.pkl:307 (`no_env_dump` step), python/src/dotfiles_setup/env_blob_scan.py (9754 B), docs/secrets-doppler-fnox-keychain.md (33073 B), docs/rules-evidence/secrets-out-of-the-shell-env.md (36485 B)"
native_alternative = "None. No Claude Code feature at 2.1.266, and nothing in the mise / hk / fnox / doppler gap digests, models a third-party secret manager's provider topology, the macOS keychain authorization-dialog hang, mise's __MISE_DIFF encoding, or the MISE_ENV_CACHE staleness mode. The nearest harness-side settings (sandbox.credentials, CLAUDE_CODE_SUBPROCESS_ENV_SCRUB) act on a child's environment; they say nothing about where a credential is stored, why a provider hangs, or what a scanner can read."
alternative_evidence = "Absence, control-armed against the same corpus that DID surface the two native levers in findings 1-2: grep -inE 'secret|credential|redact|mask' over /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings.md returns only 2 unrelated hits (settings.md:646 apiKeyHelper reload, :805 cloud-environment carryover), while the same command shape over sandboxing.md returned the sandbox.credentials block (sandboxing.md:266,278,280,302,311,321,362,425,477,609,687) -- so the probe discriminates and the null is real. Gap digests read: claude-code-changelog-2.1.258-2.1.266.digest.md (14 capabilities, none credential-related), claude-code-llms-index.digest.md (0 new capability pages), mise-releases-since-pin.digest.md, hk-releases-since-pin.digest.md."
rationale = "Required justification: this is tool-topology and failure-mode knowledge about fnox, Doppler, the macOS keychain and mise's env encoding -- none of which the harness models, and none of which any pinned tool now solves natively. Its enforcement layer (`no_env_dump` in hk.pkl, backed by env_blob_scan.py) is a genuine capability gate rather than a symptom sniff: it exists precisely because gitleaks 8.30.1 and betterleaks 1.8.1 measurably cannot read a compressed env blob, so retiring it would return the repo to a scanner-clean-but-unscanned state. Keep as-is; the only edits this file needs are findings 1-3."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-tool-currency-and-native-first-1"
unit = "rule-tool-currency-and-native-first"
kind = "rule"
disposition = "refactor"
confidence = 0.78
custom_thing = "The rule's scope (\"Applies to\": mise, hk, Renovate, uv/ruff/ty, docker/bake, chezmoi, pinact, agnix) and the config it drives (currency.toml, which deep-tracks only doppler + graphify) exclude the agent-harness layer entirely: the Claude Code CLI itself and the five enabled plugins (fable-orchestrator 1.21.0, planning-with-files 3.17.2, mattpocock-skills 1.2.3, antigravity 0.26.0, codex 1.0.6). No currency lane in this repo can report drift on them, so the rule's own premise ('the built-in you needed may have shipped since you last looked') goes unchecked for the fastest-moving dependency the session actually runs on."
evidence = ".claude/rules/tool-currency-and-native-first.md:70-76 (the Applies-to list, harness/plugins absent); currency.toml:23-49 (only [tool.doppler] and [tool.graphify] are deep-tracked); mise.toml:38 pins only `npm:claude-code-lint`, never Claude Code itself; .claude/settings.json:131-143 lists enabledPlugins with NO version field. Control-armed probe: `grep -rniE '2\\.1\\.2[0-9]{2}|claude_code_version|claude --version' doctor.toml currency.toml mise.toml renovate.json` -> 0 hits, while the same grep shape over the shared fragment returns `.config/mise/conf.d/shared.toml:31 hk = \"1.57.0\"` and `:38 npm:@openai/codex ... 0.152.1`; and `grep -rn '1.21.0\\|fable-orchestrator' currency.toml renovate.json` -> 0 hits with control arms `grep -c graphify currency.toml` -> 11 and `grep -c agnix renovate.json` -> 2. This audit's own gap digests measure the resulting blind spot: 14 new Claude Code capabilities between the offline corpus stop (2.1.257) and the host 2.1.266 (.agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md) and 7 new fable-orchestrator capabilities since the pinned 1.21.0 (.agent/kb/raw/fable-orchestrator-changelog.digest.md)."
native_alternative = "Claude Code's own plugin version/update surface: `claude plugin list` (the full listing, incl. skills-directory and synced plugins), `claude plugin update <plugin> [-s scope]`, and the documented version-as-cache-key resolution (plugin.json version -> marketplace entry version -> source commit SHA -> archive sha256 -> unknown) that decides whether an update exists and drives auto-update. That is the native equivalent of `mise outdated` for the plugin layer, and it is what a currency lane should shell out to instead of tracking plugin versions by hand."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:1108 (`### plugin update`), :1113 (`claude plugin update <plugin> [options]`), :1134 (`### plugin list`), :1139 (`claude plugin list [options]`), :1320 (\"Claude Code uses the plugin's version as the cache key that determines whether an update is available... skips the update if it matches what's already installed\"), :1336 (the three versioning approaches table). Machine-readable adjunct shipped since the host's own baseline: `--json` on `claude plugin validate`, Claude Code 2.1.259 — .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.md:210."
rationale = "Not a stale fact — every harness fact and wiring claim the rule makes still verifies (see finding -3). This is a scope gap the native surface can close cheaply: add the plugin/harness layer to the rule's Applies-to list and give currency.toml (or the tool-currency task) a lane that reads `claude plugin list` rather than leaving the repo's fastest-moving dependencies outside the only rule that exists to keep dependencies current. Refactor rather than retire, because the rule's judgment half stays valuable; it just needs to cover the layer that now moves fastest."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-tool-currency-and-native-first-2"
unit = "rule-tool-currency-and-native-first"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "Rules 2 and 3 (\"periodically re-check ... does the tool now do this natively?\" / \"when a native feature supersedes custom code, RETIRE it\") and rule 5's \"sync the describing docs/skills in the SAME change\" are executed entirely by hand for this repo's own skill corpus: the sibling `.claude/skills/tool-currency-check/SKILL.md` drives `mise run tool-currency` / `kb-setup currency` for tool VERSIONS and has no input at all on which of the repo's ~25 project skills are dead weight or what they cost in context."
evidence = ".claude/rules/tool-currency-and-native-first.md:43-52 (rules 2 and 3), :59-63 (rule 5, \"skill files\"), :89-91 (\"This rule's remaining, un-automatable job\"); .claude/skills/tool-currency-check/SKILL.md:42-44 — the three commands the skill drives are `mise run tool-currency`, `kb-setup currency run --tool graphify --json`, `kb-setup currency apply --tool graphify`, all version-only."
native_alternative = "`/skill-doctor` — shipped in Claude Code 2.1.261, below the host's 2.1.266 — reports which loaded skills go unused and what each costs in context, i.e. a native detector for exactly the \"custom thing that is now dead weight\" class this rule tells you to hunt by hand, for the skill surface."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.md:71 (\"Added `/skill-doctor` to show which loaded skills go unused and what they cost in context, so you can prune them\"), digested at .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (capability entry, version 2.1.261)."
rationale = "Partial coverage only, so refactor not retire: `/skill-doctor` measures usage and context cost of SKILLS, which is one narrow slice of the rule's surface (it says nothing about a superseded python module, a renovate customManager, or a snapshot file). But it is a real, specifically-named native input that the rule's periodic re-check and its sibling skill currently have no equivalent of, and the rule's own rule 3 is the reason to wire it in: naming `/skill-doctor` in rule 2's checklist turns one hand-run judgment into a measured one. Confidence is held below 0.7 because the feature is known only from the changelog line (2.1.261) — the offline corpus predates it, so its exact output shape is unverified here."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-tool-currency-and-native-first-3"
unit = "rule-tool-currency-and-native-first"
kind = "rule"
disposition = "keep"
confidence = 0.88
custom_thing = "The rule as a whole — the native-first RETIREMENT JUDGMENT (\"is this piece of custom code now superseded by a tool feature?\"), plus every wiring fact it asserts: currency.toml, the two mise tasks, the shared kb_setup.currency engine, the refresh.yml daily job, the SessionStart drift check, workflow.tool-currency-wiring, hk_version_parity, and the three named hk gates in rule 5."
evidence = "Axis (a) audited and CLEAN — every asserted artifact exists: currency.toml (2969 B); mise.toml:586 `[tasks.tool-currency]` and :595 `[tasks.tool-currency-check]`; python/pyproject.toml:40 pins `kb-setup @ git+...@c70f0f8` (the SHA in this audit's pin list); .github/workflows/refresh.yml:163 job `tool-currency`, :183 `mise run tool-currency > /tmp/tool-currency.md`, :217/:224 upsert of the standing issue; .claude/settings.json:102 SessionStart hook running `mise ... run tool-currency-check`; python/verification/suites.toml:1816 `workflow.tool-currency-wiring` with per-path tokens at :1840-1860; hk.pkl:523 `[\"hk_version_parity\"]`; hk.pkl contains `md_size_budget`, `claude_agents_md_pairs`, `claude_md_import_stub` (1 each); mise.toml:1260 `agnix . --strict`; docs/rules-evidence/tool-currency-and-native-first.md and .claude/skills/tool-currency-check/SKILL.md both present; .devcontainer/P2996-CACHE.md present; all three cited memory files present (feedback_research_release_notes_native_first.md, feedback_use_tool_builtins.md, feedback_content_hash_must_cover_copy_inputs.md) in /Users/rmanaloto/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/memory/. The rule cites no harness doc anchor (no `$CC/...:NNNN`), so there is no stale doc-anchor claim to correct. Rule 6 is visibly working in the artifact it governs: all 8 surviving renovate.json customManagers carry an in-file justification of the form \"No native/preset manager covers X\" (renovate.json:87, :100, :112, :127, :140, :153, :165, :177)."
native_alternative = "None. The candidates were checked and each covers only version drift, which the rule already delegates: mise's `mise outdated` (already wired through `kb-setup currency daily`), Renovate PRs carrying the upstream changelog (already wired via renovate.json's preset `github>jdx/renovate-config`), and the harness's own `claude plugin update`. No tool at any pinned version decides whether a piece of custom code has become redundant."
alternative_evidence = "Gap digests, all read for this unit: .agent/kb/raw/mise-releases-since-pin.digest.md — ZERO releases newer than the host's 2026.9.3, so no new mise capability exists to supersede anything; .agent/kb/raw/hk-releases-since-pin.digest.md — only v1.58.1, verbatim \"A small documentation-only release. There are no changes to hk's runtime behavior\", zero new capabilities; .agent/kb/raw/uv-releases-since-pin.digest.md — 5 capabilities (publish token revocation, `missing-exclude-newer-package-lock`, `uv tree --invert` cycles, pylock.toml hash backfill/warning), none of them a currency or code-supersession mechanism; .agent/kb/raw/graphify-readme-0.9.56.digest.md — zero new capabilities; .agent/kb/raw/renovate-config-options.digest.md — new pep621 capabilities (PEP 735 dependency-groups, pixi.lock, array-form `tool.uv.sources` in 44.62.2 vs this repo's pinned 44.59.2) none of which overlaps any of the 8 surviving customManagers (hk pkl amends URL, .chezmoiversion, clang-p2996 git SHA, gcc-latest .deb URL, ubuntu digest in bake HCL, MISE_VERSION, dive, [bootstrap.packages] apt pins)."
rationale = "Required justification: the rule's core directive is a judgment, and judgment has no native implementation at any of the host's versions or the repo's pins. The version-currency MECHANICS were already retired into the shared engine (rule lines 78-91), which is precisely this rule applied to itself, so nothing custom is left to delete on that axis. What remains — 'does this custom module/manager/snapshot still need to exist?' — is answered today only by a human reading a changelog; the two closest native surfaces are `mise outdated` (version drift, already delegated) and Renovate's changelog-carrying PRs (already delegated). Keep the rule; the two refactors above widen its scope (finding -1) and give one slice of its manual hunt a measured input (finding -2)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-use-tool-builtins-1"
unit = "rule-use-tool-builtins"
kind = "rule"
disposition = "refactor"
confidence = 0.86
custom_thing = "The rule names mise's built-in os/arch discriminator as the bare facts `os`/`arch`, alongside `chezmoi.os`, `runner.os` and `TARGETARCH` — i.e. it reads as a template VARIABLE an agent would write as `{{ os }}` / `{{ arch }}`."
evidence = ".claude/rules/use-tool-builtins.md:41-42 — \"Check whether a built-in fact already discriminates your cases — `chezmoi.os`, mise's `os`/`arch`, `runner.os`, `TARGETARCH`\"."
native_alternative = "mise exposes os/arch only as template FUNCTIONS — `os()`, `arch()`, `os_family()` — never as variables. The canonical form is `{% if os() == \"linux\" %}`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/templates.md:295 (`arch() -> String`), :296 (`os() -> String`), :298 (`os_family() -> String`), :575 (`{% if os() == \"linux\" %}`). The `### Variables` list at templates.md:178-247 enumerates env, vars, cwd, config_root, config_source, mise_bin, mise_pid, mise_env, xdg_*, tools, usage — and contains no `os` or `arch` entry. Control arm: the same section read verbatim does contain `config_root` (templates.md:187), so the absence is real, not a blind read. (An earlier regex probe `\\{\\{ *X *\\}\\}` was discarded because its control arm on `config_root` also returned 0 — the docs list variables as bullets, not as brace expressions.) mise host/pin is 2026.9.3, already the newest published release (.agent/kb/raw/mise-releases-since-pin.digest.md)."
rationale = "The underlying claim (mise ships a built-in os/arch fact, so do not hand-roll uname parsing) is TRUE and the rule should keep making it; only the invocation form named is wrong. An agent obeying the rule literally writes `{{ os }}`, which is not a defined mise template variable. Not yet a live defect — repo configs use no bare `{{ os }}`/`{{ arch }}` (control arm: `{{ env.` matches 3 sites incl. mise.toml:153), so this is doc accuracy, not a bug. Fix is a two-word edit: cite `os()`/`arch()`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-use-tool-builtins-2"
unit = "rule-use-tool-builtins"
kind = "rule"
disposition = "refactor"
confidence = 0.72
custom_thing = "The rule's step 3 mandates a written justification for any surviving custom code (\"record *why* in the code comment or PR body\") and states the default answer is deletion without it — but the rule documents no enforcement layer at all, presenting itself as prose-only guidance."
evidence = ".claude/rules/use-tool-builtins.md:25-29 (step 3) and :62-66 (\"Justify any custom solution in writing\"); the file's `## See also` (:75-82) lists only two memory files, a commit and an upstream URL — no gate, no contract, no task. `grep -n \"use.tool.builtins|native_first\" hk.pkl hk-common.pkl` returns 0 hits (control arm: `bash_logic_budget|no_lint_skip` returns 5 hits in hk.pkl), so no hk step binds it."
native_alternative = "A layer already exists and the rule does not know about it: three `python/verification/suites.toml` contracts — run by `mise run verify` — cite `.claude/rules/use-tool-builtins.md` by path as the standard the custom code in question must satisfy, and bind the site where that justification is written. `build.renovate-config-validate` records the four-clause argument; `workflow.env-blob-scan` records the measured-blindness justification; the codex-lane contract states the argument \"lives ONCE, in the `codex_lane` module docstring — the place a reader of the code lands\"."
alternative_evidence = "python/verification/suites.toml:1535 (\"which `.claude/rules/use-tool-builtins.md` refuses as a hard gate\"), :1696 (\"That measured blindness is what justifies custom code here at all (`use-tool-builtins.md`)\"), :2183 (\"the full four-clause argument `use-tool-builtins.md` requires in writing lives ONCE, in the `codex_lane` module docstring\"). Doctrine that markdown alone is never the only layer: .claude/rules/mise-tasks-only.md § \"Enforcement layers\" (\"markdown alone is 'relying on the LLM', never the only layer\")."
rationale = "Not superseded, but under-wired in a way that is already half-fixed elsewhere. Every sibling judgment rule in this repo names its layer (`zero-bash-logic` -> `bash_logic_budget`; `clarify-before-acting` -> `ask_quality`; `mise-tasks-only` -> `hook_guard`); this one names none, so a reader cannot tell the justification requirement is actually asserted anywhere. The refactor is documentation of an existing seam plus the convention it implies — the written justification belongs in the module docstring (the codex_lane precedent, suites.toml:2183) and the contract binds that site — not new code. Deliberately NOT proposing a PreToolUse hook: no deterministic matcher can tell \"custom code that duplicates a built-in\" from custom code that does not, which is exactly why finding 4 is a keep."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-use-tool-builtins-3"
unit = "rule-use-tool-builtins"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "The rule links to no sibling rule, while five eager rules and three skills declare it their parent — the reference graph is entirely one-way into this file."
evidence = ".claude/rules/use-tool-builtins.md — `grep -n \"\\.claude/rules/|\\[\\[\"` returns rc=1, zero matches (control arm: the same grep against .claude/rules/verify-before-advancing.md returns 3). Inbound: .claude/rules/tool-currency-and-native-first.md, zero-bash-logic.md, md-size-budgets.md, secrets-out-of-the-shell-env.md, research-doc-sources.md, do-not.md, plus .claude/skills/{tool-currency-check,chezmoi-check,devcontainer-workflow,session-handoff,memory-index-curation}/SKILL.md."
native_alternative = "`.claude/rules/tool-currency-and-native-first.md` is the currency-over-time half of this exact rule and already back-links: its `## See also` opens \"`use-tool-builtins.md` — the point-in-time sibling: prefer built-ins over inventing custom logic now.\" Its operational surface is the `tool-currency-check` skill plus `mise run tool-currency` / `mise run tool-currency-check` (the shared `kb_setup.currency` engine), which mechanises the \"has the built-in shipped since?\" half this rule states only as a habit."
alternative_evidence = ".claude/rules/tool-currency-and-native-first.md § \"Why this rule exists\" (self-declared sibling) and § \"How currency is checked now\" (`mise run tool-currency` -> `kb-setup currency daily`; `mise run tool-currency-check` -> the SessionStart drift check); .claude/skills/tool-currency-check/SKILL.md."
rationale = "A reader who lands on this rule is told to \"research existing solutions FIRST\" and is given a doc-fetch chain, but is never pointed at the repo's own mechanised answer to the same question — the currency engine that reports, every session, whether a native feature has landed since a pin. Two `## See also` lines close it. Low blast radius, and cheap: the rule is at 82 lines / 4,477 B against the rule_unscoped budget of 200 lines / 24,000 B, so there is room."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-use-tool-builtins-4"
unit = "rule-use-tool-builtins"
kind = "rule"
disposition = "keep"
confidence = 0.88
custom_thing = "The rule's core directive and hard gate: name the capability, research existing tools/platform features first, prefer them, and treat homegrown code as the last resort with a written justification."
evidence = ".claude/rules/use-tool-builtins.md:3-33 (the standing rule + the four-step hard gate); the worked chezmoi failure at :45-56, whose anchors all still resolve — commit bd40767 is present (`refactor(chezmoi): use built-in chezmoi.os instead of custom is_container`), home/.chezmoiignore:32-40 carries the built-in `chezmoi.os` gate and records the retired `is_container` variable, docs/rules-evidence/use-tool-builtins.md exists (3,947 B), and both cited memory files (feedback_use_tool_builtins.md, feedback_devcontainer_only_mise_overlay.md) are on disk."
native_alternative = "None. The nearest native mechanism at 2.1.266 is the `/doctor` skill, which since v2.1.206 trims checked-in CLAUDE.md files by cutting content Claude could derive from the codebase and \"migrates the always-loaded guidance that remains into skills and nested CLAUDE.md files that load on demand\" — a context-cost pass over instruction files, not a prefer-native-over-custom gate."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/commands.md:83 (`/doctor` capability list, incl. the v2.1.206 CLAUDE.md trim requirement) and best-practices.md:186. Corpus probe for a native equivalent: `grep -rniE \"prefer (the )?(built-in|native)\"` over the whole claude-code doc tree returns 0 hits; control arm `grep -rn \"PreToolUse\"` over the same tree returns 239, so the search is not blind. The 2.1.258-2.1.266 gap digest (.agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md) lists 14 new capabilities — --plugin-dir folders, bashOutputMaxChars/taskOutputMaxChars, --append-subagent-system-prompt-file, /skill-doctor, /diff, prompt-cache-miss causes, /reload-plugins headless, text /advisor, managedMcpServers, --permission-prompts none, glab recognition, `claude plugin validate --json`, the 1 GB tool-result cap — none of which is a research-first or prefer-native gate. The llms-index digest reports zero new capability pages."
rationale = "Required justification for the keep: this is a judgment gate over an open-ended decision (\"does something already do this?\"), and no harness surface can decide it. A permission rule matches command strings, a hook matches tool inputs, a settings key toggles a fixed behaviour, an agent field scopes a delegate — none can tell custom code that duplicates a built-in from custom code that does not, because the answer lives in a third party's changelog, not in the tool call. `/doctor` is the only native feature that acts on this class of file and it acts on the wrong axis (context cost, not correctness). One caution follows from that: `/doctor` would propose migrating this always-loaded rule into a skill, and per .claude/rules/md-size-budgets.md § \"Scoping: the trigger test\" that proposal must be declined — the rule is behaviour-triggered (it fires when custom code is about to be written, which no glob and no relevance match predicts), so a skill or a `paths:` scope would make it absent exactly when it is needed. It stays eager and stays prose; findings 1-3 sharpen it rather than replace it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-verify-before-advancing-1"
unit = "rule-verify-before-advancing"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "The rule's third ALWAYS gate is written as the bare command `dotfiles-setup verify run` (.claude/rules/verify-before-advancing.md:26), a form that is not on PATH."
evidence = ".claude/rules/verify-before-advancing.md:26 ('- `dotfiles-setup verify run` — `0 failed`.'); the same broken form is repeated in the repo at CONTEXT.md:49, python/AGENTS.md:20 and .claude/skills/session-handoff/SKILL.md:194."
native_alternative = "The repo's own canonical mise task `mise run verify`, created specifically because the bare console script returns rc=127 outside the uv venv; `mise-tasks-only.md` requires the task form for every recurring workflow."
alternative_evidence = "mise.toml:364-373 — `[tasks.verify]` with the comment 'AGENTS.md previously told agents to run a bare `dotfiles-setup verify run`, which is NOT on PATH and returns rc=127 (hit 2026-07-20 while running the pre-commit gates)' and `run = 'uv run --project python dotfiles-setup verify run'`; AGENTS.md:137 already prescribes `mise run verify`."
rationale = "An eager rule whose ALWAYS matrix names a command that exits 127 teaches the failure the task was created to remove, and contradicts AGENTS.md's own 'Validate before committing' block. Replace line 26 with `mise run verify` (keeping the '0 failed' evidence requirement)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-verify-before-advancing-2"
unit = "rule-verify-before-advancing"
kind = "rule"
disposition = "refactor"
confidence = 0.95
custom_thing = "The 'Opened a PR' matrix row MANDATES `gh pr checks <n> --watch` until terminal (.claude/rules/verify-before-advancing.md:37) — a command the repo's own PreToolUse guard hard-denies."
evidence = ".claude/rules/verify-before-advancing.md:37 ('| Opened a PR | `gh pr checks <n> --watch` until terminal …')."
native_alternative = "The guard rule 'gh pr checks --watch' in hook_guard.py denies it and redirects to `mise run ship` (watches PR checks to bucket-verified green) / `mise run land` (watches main CI); a one-shot `gh pr checks <n> --json` read stays allowed."
alternative_evidence = "python/src/dotfiles_setup/hook_guard.py:413-420 (Rule('gh pr checks --watch', re.compile(_CMD + r\"gh\\s+pr\\s+checks\\b[^;&|\\n]*--watch\\b\"), …, _V2)); asserted as a DENY case at python/src/dotfiles_setup/eval_cases.py:144 ('gh pr checks 123 --watch', _DENY, 'ship/land already watch'); the redirect table is .claude/rules/mise-tasks-only.md:'gh pr checks … --watch' row."
rationale = "Two eager rules give opposite instructions and the machine layer sides against this one: following line 37 produces a deterministic PreToolUse deny (which also cancels the whole compound command). Rewrite the row as `mise run ship` (which already watches to terminal) with a one-shot `gh pr checks <n> --json` read for evidence."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-verify-before-advancing-3"
unit = "rule-verify-before-advancing"
kind = "rule"
disposition = "refactor"
confidence = 0.80
custom_thing = "'The gate' section (lines 96-101) is a prose-only, model-honoured stop condition: 'Only after every applicable check above is green do you: commit, push, merge, start the next task, or report completion.' Nothing in the harness enforces it — .claude/settings.json wires PreToolUse, PostToolUse, SessionStart, SessionEnd and InstructionsLoaded, but no turn-end or task-completion event."
evidence = ".claude/rules/verify-before-advancing.md:96-101; .claude/settings.json hooks block contains only PreToolUse (line 52), PostToolUse, SessionStart, SessionEnd (line 108) and InstructionsLoaded — control-armed: grep for 'Stop' in .claude/settings.json returns no hook-event key."
native_alternative = "The `TaskCompleted` hook event — documented for exactly this purpose ('Use this to enforce completion criteria like passing tests or lint checks before a task can close'), blocking on exit code 2 — and the `Stop` hook's `decision: \"block\"` (prevents Claude from stopping, continues the conversation), with `/goal` as the session-scoped prompt-based shortcut."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2402-2406 (TaskCompleted), :862 ('TaskCompleted | Yes | Prevents the task from being marked as completed'), :1014 (exit code 2 blocks the action with stderr feedback), :858 ('Stop | Yes | Prevents Claude from stopping, continues the conversation'), :2458-2545 (Stop input + Stop decision control); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/goal.md:9,26,120 ('/goal is a wrapper around a session-scoped prompt-based Stop hook')."
rationale = "The repo's own doctrine (mise-tasks-only.md 'Enforcement layers': markdown alone is 'relying on the LLM, never the only layer') applies to this rule and is unmet — it is the one judgment gate with zero machine layer. The check MATRIX is repo-specific and must stay in prose, but the STOP condition is now a native event: a TaskCompleted (and/or Stop) hook running the applicable gates would make 'done means verified done' deterministic instead of remembered."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-verify-before-advancing-4"
unit = "rule-verify-before-advancing"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "The delegation clause — 'agent-delegated work (the delegating context is responsible for confirming the delegate's checks actually passed)' (lines 105-107) — is a manual obligation on the parent session with no mechanism."
evidence = ".claude/rules/verify-before-advancing.md:105-107."
native_alternative = "`SubagentStop` hooks: same decision-control format as Stop, `decision: \"block\"` keeps the subagent running and delivers the reason as its next instruction, and `last_assistant_message` gives the delegate's final text without parsing the transcript; subagent-scoped hooks are converted from `Stop` to `SubagentStop` automatically."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2319-2346 (SubagentStop input + decision control), :859 ('SubagentStop | Yes | Prevents the subagent from stopping'), :664 ('Claude Code converts a `Stop` hook here to `SubagentStop`')."
rationale = "The parent-confirms-delegate obligation is precisely what SubagentStop was built to hold, and the repo already delegates heavily (codex/Opus lanes, agent teams). Nothing native reads the delegate's gate results by itself, so the rule stays — but it should name SubagentStop as the enforcement seam rather than leaving the obligation purely narrative."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-verify-before-advancing-5"
unit = "rule-verify-before-advancing"
kind = "rule"
disposition = "refactor"
confidence = 0.65
custom_thing = "'Evidence discipline' bans `cmd 2>&1 | tail -N` and prescribes the redirect-to-file + `echo rc=$?` ritual as the only way to keep a gate's output readable (lines 85-88)."
evidence = ".claude/rules/verify-before-advancing.md:85-88; the sibling prescription in .claude/rules/long-running-command-hangs.md rule 3 and the mise-tasks-only.md redirect row."
native_alternative = "`bashOutputMaxChars` and `taskOutputMaxChars` settings (Claude Code 2.1.261) raise how much command / background-task output reaches the model inline before it is spilled to a file (up to 128K chars), and 2.1.265 added a 1 GB cap on tool results saved to disk with the in-conversation preview flagging truncation — so output volume, the original reason to pipe into `tail`, is now a settings key. Neither is set in this repo (control-armed: grep of .claude/settings.json returns ABSENT while the sibling key \"env\" is found at line 3)."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — 'capability: `bashOutputMaxChars` and `taskOutputMaxChars` settings raise how much command/background-task output Claude receives inline before it is saved to a file (up to 128K characters). version: 2.1.261' and 'capability: 1 GB cap on tool results saved to disk … version: 2.1.265' (both newer than the offline corpus, which stops at 2.1.257 and contains neither key)."
rationale = "The PROHIBITION is still correct and must stay — a pipe returns tail's exit code, and no setting changes that. What is now stale is the implied premise that truncating output is the only way to keep a long gate readable: the rule should point at `bashOutputMaxChars`/`taskOutputMaxChars` (and note the harness already saves oversized results to disk) so the file-redirect ritual is about the rc, not about volume."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 2
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-verify-before-advancing-6"
unit = "rule-verify-before-advancing"
kind = "rule"
disposition = "keep"
confidence = 0.85
custom_thing = "'never a background-task \"completed\" notification's exit code' — the rule requires reading a file-based rc or the API `conclusion` field instead of trusting the harness's own background-task completion summary (line 85-88)."
evidence = ".claude/rules/verify-before-advancing.md:85-88; corroborated by auto-memory feedback_background_task_notification_can_lie ('the harness's own \"exit code 0\" summary was wrong TWICE in one session') and .claude/rules/persistence-gate-retry.md:'The task notification cannot be trusted here'."
native_alternative = "None. The only native surface that exposes background-task state to a gate is the `background_tasks` array on Stop/SubagentStop input, whose per-entry fields are id, type, status, description, command, agent_type, server, tool, name — there is NO exit-code field, so a hook cannot read a background task's real rc either."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2474-2490 (background_tasks purpose + full field table; no exit code), :2499-2520 (example entry: status 'running' only); the 2.1.258-2.1.266 digest lists no changelog entry touching background-task exit-code reporting (.agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md)."
rationale = "Justified keep: at 2.1.266 the harness still surfaces background-task completion as a status label, not an exit code, and the field table proves a hook could not substitute for the file-based rc. The rule is the only layer covering this failure mode, and it has fired at least twice in real sessions."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-verify-before-advancing-7"
unit = "rule-verify-before-advancing"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "The base-pull section states the image as '~38GB' twice and calls the transfer 'the ~38GB buildkit pull' (lines 64-66, 76-77)."
evidence = ".claude/rules/verify-before-advancing.md:66 ('~38GB image') and :76 ('~38GB buildkit pull can take hours')."
native_alternative = "The repo's other eager rule sizes the same pull at ~21.5GB, and the research report that produced 38 GB scopes it to UNCOMPRESSED image bytes, not transferred layers — i.e. the figure travelled without its condition, which is the exact failure this file's own closing postscript warns about."
alternative_evidence = ".claude/rules/persistence-gate-retry.md:69 and :90 ('a ~21.5GB pull that can take hours'); docs/research/runs/research-20260711-cicd-image-pipeline/report.md:126 ('### Where the bulk is (~38 GB uncompressed)'); docs/research/kb/reports/agents/cold-review-800-20260828.md:40 ('linux/amd64/v2 21.5GB + linux/arm64 19.9GB')."
rationale = "Two eager rules give a caller two different magnitudes for the same wait, and the larger one is an uncompressed-size figure repurposed as a transfer size. Per this file's own 'carry a number with its CONDITION' postscript, state it as '~21.5GB compressed transfer (~38GB uncompressed on disk)' and cite the manifest, or drop the figure and say 'hours on a slow link'."
probe = "docker buildx imagetools inspect ghcr.io/ray-manaloto/dotfiles-devcontainer:dev --raw | jq '[.manifests[] | {platform: .platform, size: .size}]'  # then sum the amd64 manifest's layer sizes to get the real transferred bytes"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-zero-bash-logic-1"
unit = "rule-zero-bash-logic"
kind = "rule"
disposition = "keep"
confidence = 0.87
custom_thing = "The allowlist + per-file `max_lines` growth budget itself: `ALLOWLIST: dict[str, BashAllowance]` (11 entries) and the three violation kinds `unlisted` / `growth` / `stale`, run as the `bash_logic_budget` hk step via a thin `dotfiles-setup bash-budget` wrapper."
evidence = ".claude/rules/zero-bash-logic.md:11-24 (the two mechanisms); python/src/dotfiles_setup/bash_budget.py:66 (`ALLOWLIST: dict[str, BashAllowance] = {`), :148-206 (`find_violations` — unlisted/growth/stale); hk.pkl:247-250 (`[\"bash_logic_budget\"] { glob = List(\"scripts/*.sh\", \".devcontainer/scripts/*.sh\"); check = \"uv run --project python dotfiles-setup bash-budget\" }`); python/verification/suites.toml:1388-1407 (`workflow.bash-logic-enforcement` binds the chain)."
native_alternative = "None. hk 1.57.0 (and 1.58.1, the only newer release) ships no allowlist/line-budget builtin, and Claude Code 2.1.266 permission rules are per-path boolean allow/ask/deny — they carry no line-count predicate and no new-file-vs-existing-file predicate, so neither can express `growth past baseline` or `stale entry`."
alternative_evidence = "hk docs, control-armed grep over /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs (--include='*.md'): `allowlist|max_lines|line budget|line count` -> 0 hits, control term `glob` -> 75 hits in the same corpus with the same command shape. Newer-than-pin gap digest: .agent/kb/raw/hk-releases-since-pin.digest.md ('New capabilities: None' — v1.58.1 is documentation-only). Claude Code path-rule surface: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:389 (Edit(src/**) as deny/ask matches a path) and :64 (deny -> ask -> allow ordering) — the whole vocabulary is path matching, nothing numeric. .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md lists 14 new capabilities through 2.1.266, none of which is a file-size/allowlist gate."
rationale = "Required justification: the gate's two teeth are a per-file numeric budget and a bidirectional allowlist/tracked-set reconciliation (`stale`). No native mechanism in any pinned tool expresses either — hk has no such builtin (control-armed 0 vs 75) and the harness's permission grammar is path-shaped, not count-shaped. The python enforcer is also the only form that does not itself violate the policy (bash_budget.py:28-31). Keep as-is."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-zero-bash-logic-2"
unit = "rule-zero-bash-logic"
kind = "rule"
disposition = "refactor"
confidence = 0.60
custom_thing = "The rule is creation-triggered (md-size-budgets.md classifies it: 'zero-bash-logic governs *new* .sh files ... you never read the file first'), yet its only enforcement point is a git-hook step that fires at commit time. Nothing stops an agent from authoring `scripts/foo.sh` and only learning it is unlisted several turns later at `mise run lint`."
evidence = ".claude/rules/zero-bash-logic.md:6-12 ('until the `bash_logic_budget` hk step gave it machine teeth' — hk is a git-hook runner); hk.pkl:247-250 (the sole enforcement seam); .claude/settings.json — grep for `Edit(` / `Write(` returns no permission path rules at all (only `Bash(*attest-plan.sh)`-style entries at :30-35), and the PreToolUse matcher `Bash|AskUserQuestion|Edit|Write|NotebookEdit` routes file-modifying tools to `branch_guard` only (python/src/dotfiles_setup/hook_guard.py:28-33)."
native_alternative = "Native `permissions.ask` (or `deny`) path rules on the Edit tool, e.g. `\"ask\": [\"Edit(scripts/**)\", \"Edit(.devcontainer/scripts/**)\"]` in .claude/settings.json — zero custom code, evaluated before any hook, with documented deny -> ask -> allow precedence. Note the required spelling: a `Write(path)` rule is accepted but never consulted (and warns at startup); path rules for Write/NotebookEdit must be written as `Edit(...)`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:316 ('Claude Code checks file permissions against `Edit(path)` and `Read(path)` rules only ... Use `Edit(docs/**)` in place of `Write(docs/**)` ... Requires Claude Code v2.1.210 or later' — host is 2.1.266); :389 (table row: `Edit(src/**)` as a deny or ask rule matches at any depth); :64 ('Rules are evaluated in order: deny, then ask, then allow'); :495 ('Hook decisions don't bypass permission rules')."
rationale = "Native covers only the creation-time half, and imperfectly: an `Edit(scripts/**)` ask rule cannot distinguish a new file from a legitimate shrink-edit of an allowlisted script, so it prompts on both, and it cannot carry the rule's reason text the way a hook deny can. That is exactly a 'refactor', not a 'retire': adopt the native ask rule as the cheap early layer that makes the policy visible at authoring time, and keep bash_budget.py as the commit-time backstop that actually decides. The version precondition (2.1.210) is satisfied by the host, and the `Write(...)`-never-consulted footgun must be respected when writing it."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-zero-bash-logic-3"
unit = "rule-zero-bash-logic"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "The rule's stated scope. Title and framing are whole-repo ('No New Bash'), and the only exemption it names is `plugins/** is out of scope (vendored / third-party)' — which reads as 'everything else is covered'. The enforcer's actual scope is two hard-coded directories: SCOPE_PATHSPECS = ('scripts/*.sh', '.devcontainer/scripts/*.sh'). A new `.sh` at the repo root, in `home/`, `.github/`, `python/` or `tests/` is silently outside the gate, and `plugins/**` was never in scope to exempt."
evidence = ".claude/rules/zero-bash-logic.md:26 ('`plugins/**` is out of scope (vendored / third-party)'), :37-41 ('Applies to: Every `scripts/*.sh` and `.devcontainer/scripts/*.sh`'); python/src/dotfiles_setup/bash_budget.py:48-51 (SCOPE_PATHSPECS, the only scope the enforcer has); hk.pkl:248 (same two globs). Latent, not yet actual: `git ls-files '*.sh' | grep -v '^plugins/'` returns 11 files, all 11 of which are in scope today."
native_alternative = "git's own pathspec exclude magic — `git ls-files -- '*.sh' ':(exclude)plugins/**'` — makes the scope whole-repo-minus-vendored in one string, with no new custom code, replacing the two hard-coded directory pathspecs the module already passes to `git ls-files`."
alternative_evidence = "Live probe in this repo, control-armed (git 2.50.1, Apple Git-155): `git ls-files -- '*.sh' ':(exclude)plugins/**'` -> 11; the same command without the exclude -> 13. The pair discriminates, so the exclude magic is really applied and the 11 is not an empty-probe artifact. Consumer already present: bash_budget.py:160-166 shells out to `git ls-files --` with SCOPE_PATHSPECS, so the change is one constant."
rationale = "The rule asserts a scope its enforcer does not have; the `plugins/**` sentence is the tell, since plugins could never have been in scope under SCOPE_PATHSPECS. Today the gap is latent (11/11 tracked non-plugin scripts are in scope), which is precisely why it can rot unnoticed — the first `.sh` someone adds at the repo root or under `home/` is unenforced and the doc says it is covered. The fix is a tool builtin, not new code: widen SCOPE_PATHSPECS to `('*.sh', ':(exclude)plugins/**')` and align the rule's 'Applies to' section, or narrow the rule's prose to the two directories it actually governs. Refactor rather than keep, because one of the two sides is wrong today either way."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "rule-zero-skip-policy-1"
unit = "rule-zero-skip-policy"
kind = "rule"
disposition = "refactor"
confidence = 0.90
custom_thing = "Rule 3 mandates ONE escalation channel: \"ask the user via AskUserQuestion\" (and the Local Validation Gate step 3 repeats it: \"Only escalate to user via AskUserQuestion after 2 failed fix attempts\"). The rule names no fallback for a context where that tool does not exist."
evidence = ".claude/rules/zero-skip-policy.md:18-20; .claude/rules/zero-skip-policy.md:35"
native_alternative = "The harness structurally removes AskUserQuestion in exactly the contexts this repo now runs most: every subagent (first tool filter, applied even when the tool is listed in the agent's `tools` field), `dontAsk` permission mode, and the new `--permission-prompts none` headless flag (2.1.259). The native escalation path in those contexts is the SubagentStop/Stop decision channel (`decision: \"block\"` with `reason`, or `hookSpecificOutput.additionalContext`) and the subagent's own final report back to the caller, which then asks."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:379 and :382 (\"The first filter removes these tools, even when listed in the `tools` field: * `AskUserQuestion`\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permission-modes.md:40 and :469 (dontAsk \"denies the built-in `AskUserQuestion` tool even if your allow rules match it\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:86; .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (capability `--permission-prompts none`, 2.1.259, \"anything that would prompt is auto-denied\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:2141 and :2346 (Stop/SubagentStop `decision: \"block\"` returns the reason into the conversation)."
rationale = "This is a live stale-fact, not a hypothetical: the rule is eager, so it loads into every subagent lane (this audit included), where it instructs an action the harness has already stripped from the tool pool. The failure mode is precisely the one the rule exists to prevent — a lane that cannot ask has no sanctioned move for an unresolved diagnostic and silently defers. Fix is one clause: in a subagent / dontAsk / `--permission-prompts none` context, return the unresolved diagnostic verbatim to the caller (or block via SubagentStop) instead of calling AskUserQuestion; the caller performs the ask. The sibling rule `clarify-before-acting.md` carries the same unqualified \"use the AskUserQuestion TOOL\" mandate and would need the same clause."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-zero-skip-policy-2"
unit = "rule-zero-skip-policy"
kind = "rule"
disposition = "refactor"
confidence = 0.85
custom_thing = "Rule 1's ban on `# noqa` / `# type: ignore` is machine-enforced by a hand-written grep step, `no_lint_skip`, whose pattern is maintained by hand and has already had to be widened once for new suppression syntaxes (`ruff:ignore`, `ty:ignore`)."
evidence = "hk.pkl:110-127 (step `no_lint_skip`, `check = \"! grep -rn --include='*.py' 'noqa\\\\|type: ignore\\\\|ty: ignore\\\\|ty:ignore\\\\|ruff:ignore\\\\|ruff:file-ignore\\\\|ruff:disable\\\\|pylint: disable\\\\|nosec' python/src/ tests/ plugins/\"'; comment at :121-124 records the hand-widening); .claude/rules/zero-skip-policy.md:11-13"
native_alternative = "ruff and ty ship the blanket-suppression half of this ban as first-class lint rules, and this repo already runs them: PGH004 `blanket-noqa` (bare `# noqa`), PGH003 `blanket-type-ignore` (bare `# type: ignore`), RUF100 `unused-noqa` (a suppression that no longer suppresses anything), and ty's `blanket-ignore-comment` for bare `ty: ignore`. `python/pyproject.toml` sets `select = [\"ALL\"]`, so PGH003/PGH004/RUF100 are enabled today."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ruff/crates/ruff_linter/src/codes.rs:909 (`(PygrepHooks, \"003\") => ... BlanketTypeIgnore`) and :910 (`(PygrepHooks, \"004\") => ... BlanketNOQA`); .../ruff/crates/ruff_linter/src/codes.rs:1217 (`(Ruff, \"100\") => ... UnusedNOQA`); .../ruff/crates/ruff_linter/src/rules/pygrep_hooks/rules/blanket_noqa.rs:11-16 (\"Check for `noqa` annotations that suppress all diagnostics... Suppressing all diagnostics can hide issues\"); .../ruff/crates/ty_python_semantic/src/suppression.rs:66 (`blanket-ignore-comment`); repo side: python/pyproject.toml:64 (`select = [\"ALL\"]`)."
rationale = "Native does NOT fully cover the rule — PGH003/PGH004/ty only reject BLANKET suppressions; a coded `# noqa: S310` is legal to them and is exactly the case that escaped enforcement in a plugin script (hk.pkl:117-120). So the grep stays. What is stale is that neither the rule nor the step says which half is native: a reader (or a future 'tidy-up') cannot tell that the grep's only load-bearing surface is coded suppressions plus the non-ruff tokens `nosec` and `pylint: disable`. Refactor = name PGH003/PGH004/RUF100/blanket-ignore-comment as the native layer in the rule, and scope the grep's justification comment to the residue it alone catches, so the two layers cannot silently duplicate or drift."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-zero-skip-policy-3"
unit = "rule-zero-skip-policy"
kind = "rule"
disposition = "refactor"
confidence = 0.70
custom_thing = "Rule 1 bans `--no-verify` and `HK_SKIP_HOOKS=`, and the Local Validation Gate mandates a manual habit (\"Before ANY git commit, you MUST run local validation... Do NOT commit until all hk checks pass\"). The machine layer behind the flag ban is the custom PreToolUse `hook_guard` regex, which the repo's own doctrine records as FAIL-OPEN on its own errors."
evidence = ".claude/rules/zero-skip-policy.md:11-13 and :29-37; python/src/dotfiles_setup/hook_guard.py:493-500 (`git --no-verify` rule) and :517-522 (`HK_SKIP_HOOKS prefix` rule); .claude/rules/mise-tasks-only.md § \"Enforcement layers\" (\"The hook fails OPEN on its own errors... Hard bans that must never fail open belong in settings.json permission deny rules, not the hook\")"
native_alternative = "`.claude/settings.json` `permissions.deny` Bash rules. Deny rules are evaluated before allow rules and across every settings scope, and a PreToolUse hook's decision cannot bypass them — so a deny entry cannot fail open the way a hook script can. The current deny list contains no git/hk-skip entry (it covers only chezmoi, mcp filesystem writes, and plan-attest)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:495 (\"Hook decisions don't bypass permission rules... a matching deny rule blocks the call\") and :596 (\"deny rules from any scope are evaluated before allow rules\"); repo side: .claude/settings.json `permissions.deny` enumerated 2026-09-09 — 16 entries, none matching `git`, `--no-verify`, or `HK_SKIP`."
rationale = "The judgment half of the gate (research the failure, don't defer) has no native equivalent and stays. What is superseded is the ENFORCEMENT choice for the two flag bans: a fail-open regex hook is carrying a ban the harness offers as a deterministic, fail-closed permission rule, and the repo's own doctrine already says that class belongs in `permissions.deny`. Refactor = add `Bash(git commit --no-verify:*)`, `Bash(git commit -n:*)`, `Bash(git push --no-verify:*)` and an `HK_SKIP_HOOKS`/`HK_SKIP_STEPS` prefix deny to settings.json and demote the hook_guard rules to advisory, keeping the hook for the shapes a glob cannot express. Confidence is 0.7 rather than higher because Bash deny patterns are prefix/glob-shaped and may not match every flag position the regex catches — that placement question is for the settings owner, not this audit."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "rule-zero-skip-policy-4"
unit = "rule-zero-skip-policy"
kind = "rule"
disposition = "unknown"
confidence = 0.40
custom_thing = "The rule lists \"Adding `continue-on-error: true` to a CI step to mask failures\" as a violation, and rule 1 bans the flag without approval — but nothing machine-checks it. The repo has four workflow sites using it (one literal `true`, six expression-gated), none of which any gate inspects."
evidence = ".claude/rules/zero-skip-policy.md:11-13 and :44; .github/workflows/refresh.yml:337 (`continue-on-error: true`); .github/workflows/build-publish.yml:148,286,442,554,837,1070 (`continue-on-error: ${{ !matrix.target.blocking }}`). Control arm for the 0-result gate grep: the same `grep -rn` shape over hk.pkl for a token known present (`no_lint_skip`) returns hk.pkl, while `continue-on-error` over hk.pkl/hk-common.pkl/suites.toml returns only a suites.toml token STRING (python/verification/suites.toml:848), not a gate."
native_alternative = "Possibly a first-class audit in one of the three GHA linters already pinned — zizmor 1.30.0, ghalint 1.5.6, actionlint 1.7.12 — which would replace the manual habit with a policy toggle. Not settled offline: none of these three has a source or doc tree in the corpus, so no anchor can be cited either way."
alternative_evidence = "No citation available — the corpus has no zizmor/ghalint/actionlint tree (verified against the per-tool corpus map in the task prompt). Per rule 4 of the audit contract this is therefore 'unknown', not 'keep'."
rationale = "A live probe settles it in seconds and would either retire the manual clause into a linter policy or confirm the clause is the only layer. Do not assume absence: the three linters ship dozens of audits and at least ghalint is policy-file driven, so a matching policy may already exist unset."
probe = "mise exec -- zizmor --list-audits 2>&1 | grep -i 'continue\\|error' ; mise exec -- ghalint run --help 2>&1 | head -20 ; mise exec -- ghalint --version && printf 'jobs:\\n  a:\\n    runs-on: ubuntu-latest\\n    steps:\\n      - run: false\\n        continue-on-error: true\\n' > /tmp/coe.yml && mise exec -- actionlint /tmp/coe.yml; echo \"rc=$?\"   # control arm: the same three commands against a token they DO flag (e.g. actionlint on a workflow with an undefined `runs-on`) must report a finding, or the probe is blind"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "rule-zero-skip-policy-5"
unit = "rule-zero-skip-policy"
kind = "rule"
disposition = "keep"
confidence = 0.90
custom_thing = "The rule's core directive — every warning/error/diagnostic must be investigated and resolved, research before deferring, and \"a passing CI pipeline with suppressed warnings is not 'green'\" — as an always-loaded judgment constraint."
evidence = ".claude/rules/zero-skip-policy.md:5-7, :15-16, :26-27, :59-65 (the eager-on-purpose justification)"
native_alternative = "None. The nearest native mechanisms gate COMMANDS and OUTCOMES, not judgment: permission allow/deny/ask rules and PreToolUse hooks decide whether a tool call runs (permissions.md:495, :596); GitHub required status checks / rulesets decide whether a branch merges. Neither can see the decision this rule governs — a diagnostic that a human or agent chose to dismiss inside a pipeline that then reports green. Claude Code's own guidance directs exactly this content (pitfalls and conventions that differ from tool defaults) to memory/rules rather than to a mechanism."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/permissions.md:495 (permission rules decide tool calls, and a hook cannot override a deny) and :565 (\"Permission deny rules block Claude from even attempting to access restricted resources\") — the whole surface is tool-call gating; nothing in hooks.md's event table (hooks.md:1085-1017) exposes a 'diagnostic dismissed' event to hook. Gap digests for 2.1.258-2.1.266 and the llms index add no such event: .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (14 capabilities, none judgment-related), .agent/kb/raw/claude-code-llms-index.digest.md (\"zero new-capability bullets\")."
rationale = "Required justification: the rule's trigger is a decision, not a file read and not a tool call, so no hook event, permission rule, agent field, or settings key can fire on it — the harness has no 'about to dismiss a warning' surface at 2.1.266, and the changelog and index gap digests confirm none shipped in the version window. Its own § \"Why this rule is eager\" already records the measured cost of the alternative (scoped until 2026-07-15, therefore absent from exactly the sessions it governs). Keep the directive eager and unscoped; findings 1-3 trim what has since become mechanically enforced, which is the only lever available."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-1"
unit = "shell-scripts"
kind = "scripts"
disposition = "retire"
confidence = 0.75
custom_thing = "scripts/benchmark-docker.sh — 171-line docker runtime benchmark harness (cold/warm bake build timings, startup, FS I/O, memory, tool validation, JSON emit)."
evidence = "scripts/benchmark-docker.sh:1-171; local base build at scripts/benchmark-docker.sh:63 and :68 (`docker buildx bake dev-load`); PLATFORM shadow bug: scripts/benchmark-docker.sh:16 sets PLATFORM from `dotfiles-setup platform triple`, scripts/benchmark-docker.sh:46 overwrites it with `uname -m`, so every `--platform ${PLATFORM}` at :77/:82/:94/:100/:108/:120/:125/:131 gets the HOST arch, not the target triple. No invoker: `git grep benchmark-docker` outside the file returns only docs, python/src/dotfiles_setup/bash_budget.py:71, python/src/dotfiles_setup/platform_target.py:6 and a mise.toml:134 comment — no mise task, no hk step, no workflow. Control arm, same command shape: `git grep devcontainer-smoke` returns real call sites mise.toml:411 and .devcontainer/devcontainer.json:237, so the 0-invoker result is real, not a broken grep."
native_alternative = "`dotfiles-setup image benchmark --image-ref … [--platform …] [--output-path …]` — the python benchmark command (smoke + size report + tool count + modeled pull time, JSON payload schema 3), which is the path CI actually runs. Local cold/warm bake timings are not a loss: building the base locally is a standing project ban."
alternative_evidence = "python/src/dotfiles_setup/main.py:1097-1112 (`image benchmark` subparser); python/src/dotfiles_setup/image.py:1455-1499 (`benchmark()` writes the JSON payload); wired live at .github/workflows/image-analysis.yml:134. Ban on the shell script's core action: .claude/rules/do-not.md item 2 (\"Do NOT `mise run build` or `docker buildx bake dev-load` locally\")."
rationale = "Dead code that is also policy-illegal and silently wrong: nothing calls it, its two build phases are exactly what do-not.md #2 forbids running locally, and its platform parameter is shadowed so the AMD64-correctness assertion at :85 compares the host's own arch. Retiring it also removes 171 lines from the bash budget. The one surviving use case — the deferred Colima-vs-Docker-Desktop runtime comparison (issue #78) — belongs as flags on `dotfiles-setup image benchmark`, not as a resurrected shell harness."
probe = "git grep -n benchmark-docker -- ':!scripts/benchmark-docker.sh' ':!docs/**'   # confirm no live caller before deleting"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-2"
unit = "shell-scripts"
kind = "scripts"
disposition = "refactor"
confidence = 0.85
custom_thing = "scripts/check-claude-md-stub.sh — a 34-line embedded awk state machine (HTML-comment stripping across lines, whitespace trim, content accumulation) plus its own `git ls-files | grep -E | grep -v` corpus enumeration, run as an hk `check`."
evidence = "scripts/check-claude-md-stub.sh:21-54 (the awk program) and :55 (`git ls-files | grep -E '(^|/)CLAUDE\\.md$' | grep -v '^\\.claude/'`); wired at hk.pkl:684 (`check = \"scripts/check-claude-md-stub.sh\"`) with NO `glob`, so it re-enumerates the whole repo on every hk run."
native_alternative = "Two mechanisms, both already used in this repo: (a) hk steps invoke the python CLI directly — `check = \"uv run --project python dotfiles-setup <cmd>\"` — no shell wrapper; (b) hk's native `glob` + `{{files}}` templating passes the matched/staged file set to the step instead of the script re-deriving it from git."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:53-57 (`glob` filters the staged files; `check = \"eslint {{files}}\"`) and :166 (a standalone `{{files}}` expands to one argument per file). In-repo precedent for the python-direct step: hk.pkl:249, 269, 308, 338, 346, 355, 413, 445, 487, 516, 639, 776."
rationale = "An awk state machine is a logic body, not a thin wrapper — the same argument python/src/dotfiles_setup/bash_budget.py:27-31 makes for its own enforcer (\"The check logic lives here (not in a big inline-bash hk step) so the enforcer does not itself violate the policy it enforces\"). The comment-stripping parser is the exact kind of code that wants unit tests, and no native linter enforces this repo-specific `@AGENTS.md`-only invariant, so the fix is to move the body into a `dotfiles-setup` subcommand and add `glob = List(\"**/CLAUDE.md\")` + `{{files}}` so the step stops scanning the whole tree on every commit."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-3"
unit = "shell-scripts"
kind = "scripts"
disposition = "refactor"
confidence = 0.80
custom_thing = "scripts/check-claude-agents-md-pairs.sh — a bash function + two `git ls-files | grep` loops implementing the CLAUDE.md/AGENTS.md sibling-pair invariant."
evidence = "scripts/check-claude-agents-md-pairs.sh:21-43 (`check_pair()` with dirname/sibling construction, two enumeration loops); wired at hk.pkl:694 with NO `glob`."
native_alternative = "Same as shell-scripts-2: the invariant body becomes a `dotfiles-setup` subcommand called directly by the hk step, with hk's `glob` + `{{files}}` supplying the file set."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:53-57 and :166; in-repo precedent hk.pkl:516 (`check = \"uv run --project python dotfiles-setup check-doc-refs\"`), whose module python/src/dotfiles_setup/doc_refs.py:71 already reasons about this very invariant in python."
rationale = "Pure path logic with no CLI being wrapped — bash buys nothing here, and doc_refs.py already carries adjacent knowledge of the same rule, so the pair check is a natural sibling function with tests. Folding it (and shell-scripts-2) into python removes two of the eleven allowlisted scripts from python/src/dotfiles_setup/bash_budget.py rather than merely re-budgeting them."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-4"
unit = "shell-scripts"
kind = "scripts"
disposition = "refactor"
confidence = 0.80
custom_thing = "scripts/validate-devcontainer-json.sh layer 3 — `check-jsonschema --schemafile <raw.githubusercontent.com URL>` fetched live on every pre-commit that touches devcontainer.json."
evidence = "scripts/validate-devcontainer-json.sh:32 (`SCHEMA_URL=https://raw.githubusercontent.com/devcontainers/spec/main/schemas/devContainer.schema.json`, pinned to `main`, not a tag) and :71; wired at hk.pkl:151."
native_alternative = "This repo's own vendored-schema mechanism: a `[[schema]]` record in `schemas/sources.toml` (tool/file/version/source/sha256) + the vendored JSON under `schemas/`, with offline drift checking in `mise run verify` and network refresh in CI (`mise run schema-vendor-refresh`, the `schema-refresh` job)."
alternative_evidence = "schemas/sources.toml:1-16 (provenance format) and its three existing records; python/src/dotfiles_setup/schema_vendor.py:2-33 — the docstring states the exact reason this mechanism exists: \"A remote `#:schema` would make `mise run lint` a per-run network dependency and a red gate whenever the schema host is unreachable\", with `check_drift` offline and `refresh` run by .github/workflows/refresh.yml."
rationale = "The repo already solved this problem for mise/ruff/typos and wrote down why; the devcontainer spec schema is the one schema still fetched from the network inside the lint gate, and it is pinned to a moving `main` branch, so the same commit can pass today and fail tomorrow with no local change. Vendoring it under schemas/ with a sha256 makes the pre-commit hermetic and puts spec drift on the reviewed refresh path instead of surprising a commit. Layers 1 and 2 (biome, `devcontainer read-configuration`) stay — they are genuine thin CLI wrapping."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-5"
unit = "shell-scripts"
kind = "scripts"
disposition = "refactor"
confidence = 0.80
custom_thing = "scripts/ensure-docker-up.sh — hand-rolled Docker Desktop start: background `docker desktop start &`, a 60s `date`-arithmetic deadline loop polling `docker info` every 2s, a PID cleanup trap and a log tail."
evidence = "scripts/ensure-docker-up.sh:20 (`ENSURE_DOCKER_TIMEOUT:-60`), :51-58 (backgrounded start + log file + cleanup trap), :60-68 (the poll loop); called from hk.pkl:137 (`docker_bake_check`) and scripts/validate-devcontainer-json.sh:48."
native_alternative = "`docker desktop start` is SYNCHRONOUS by default and takes a native timeout: `-d, --detach  Do not synchronously wait for the requested operation to complete` and `--timeout seconds  Terminate the running command after the specified timeout with a non-zero exit code`. `docker desktop status --format json` gives a machine-readable engine state, replacing the `docker info` liveness poll."
alternative_evidence = ".agent/kb/raw/docker-desktop-start-flags-probe.md (live probe, docker-cli 29.7.2, 2026-09-09): `docker desktop start --help` shows `-d, --detach` and `--timeout seconds`; `docker desktop --help` lists the `status` command; `docker desktop status --help` shows `--format string ... pretty, json`."
rationale = "The script's 40 lines of backgrounding, deadline arithmetic, polling and PID cleanup exist only because the author assumed `docker desktop start` returns immediately — the probe shows it blocks until the operation completes and carries its own timeout with a non-zero exit. `docker desktop start --timeout \"${ENSURE_DOCKER_TIMEOUT:-60}\"` collapses lines 51-73 to one command with a real exit code, which is exactly the .claude/rules/use-tool-builtins.md gate (prefer the CLI's own flag over a hand-rolled poller). The `open -a Docker` cold-launch branch at :42-47 has no CLI equivalent and stays."
probe = "docker desktop stop && time docker desktop start --timeout 60; echo \"rc=$?\"   # confirm it blocks until the engine answers `docker info` and returns non-zero on expiry"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-6"
unit = "shell-scripts"
kind = "scripts"
disposition = "refactor"
confidence = 0.70
custom_thing = ".devcontainer/scripts/on-create.sh — 74 lines of container-create logic: chezmoi apply, a `sudo find -not -user | xargs chown -h` ownership repair with a mismatch count, `mise install -y`/`reshim` plus a `mise ls --installed --json | jq length` post-condition assertion, and a 30-day atime TMPDIR sweep + empty-dir prune."
evidence = ".devcontainer/scripts/on-create.sh:40-72; invoked from .devcontainer/devcontainer.json:236 (`\"onCreateCommand\": \"bash /workspaces/${localWorkspaceFolderBasename}/.devcontainer/scripts/on-create.sh …\"`)."
native_alternative = "The devcontainer lifecycle hook can call the python CLI directly — this repo already does exactly that for the host-side hook: `\"initializeCommand\": \"uv run --project python dotfiles-setup docker initialize-host\"`, whose body lives in python/src/dotfiles_setup/docker.py. The image ships uv + python 3.14, so `onCreateCommand` can call `uv run --project python dotfiles-setup container on-create` the same way."
alternative_evidence = ".devcontainer/devcontainer.json:235 (the initializeCommand→python precedent); python/src/dotfiles_setup/docker.py:213 `initialize_host_ssh_runtime()` (the python body behind it); python/src/dotfiles_setup/container.py:129 `verify_latest()` (existing container-side module to host the new command)."
rationale = "Ownership repair, a JSON-parsed tool-count post-condition and a two-phase filesystem sweep are logic, not thin wrapping — .claude/rules/zero-bash-logic.md's default answer for exactly this shape is python/, and the sibling lifecycle hook in the same file already takes that route. The chown/find pipeline is also the highest-risk code in the unit (it runs under sudo over all of $HOME), which is the strongest argument for putting it somewhere unit-testable. No devcontainer-spec feature covers it: `updateRemoteUserUID` is set false at .devcontainer/devcontainer.json:228 and addresses UID remapping, not repairing root-owned files on a persistent home volume."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-7"
unit = "shell-scripts"
kind = "scripts"
disposition = "refactor"
confidence = 0.55
custom_thing = "scripts/pretooluse-guard.sh — a fail-open shim whose stated purpose is to stop the harness \"failing closed\" and blocking EVERY Bash call when Python >=3.14 is absent (the \"web brick\")."
evidence = "scripts/pretooluse-guard.sh:5-8 (\"FAILS OPEN (exit 0 = allow the Bash call) when it cannot run — so a cold Claude-web session … is not bricked by every Bash call being denied\") and :36-41; the same premise drives scripts/web-setup.sh:7-10 (\"the guard errors on startup and the harness fails closed, blocking EVERY Bash call\"); wired at .claude/settings.json:58."
native_alternative = "Claude Code's documented hook-failure semantics already fail open: a hook that cannot start (e.g. exit 127) or exits non-zero-but-not-2 is a NON-BLOCKING error and the action proceeds; only exit code 2 (or a JSON `permissionDecision: \"deny\"`) blocks a PreToolUse tool call."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:833 (\"A hook that can't start lands in the same non-blocking bucket. When the script path doesn't exist or isn't executable, the shell exits with a code like 127 … For most hook events, the action proceeds\") and :836 (\"exit code 2 is the only exit code that blocks through the code alone … Claude Code treats exit code 1 as a non-blocking error and proceeds\"); PreToolUse decision control at the same file :1738-1750 lists no failure-implies-deny path."
rationale = "The shim's headline justification is not the documented behaviour at this host's version (2.1.266): an absent interpreter yields a non-blocking hook error and the Bash call proceeds regardless of the wrapper. What DOES survive is the other half — recording every fail-open (#343's finding that an unrecorded fail-open is indistinguishable from enforcement), which the harness does not do, and anchoring `$ROOT` so `uv run --project` resolves off-repo. So the file should stay but shrink to the recorder + anchor, and the brick rationale in its header and in web-setup.sh:7-10 should be corrected rather than propagated. Confidence is mid because the original claim may rest on an observed pre-2.1.x behaviour or on a guard that printed a deny-shaped JSON on its error path; the probe settles it."
probe = "Temporarily point .claude/settings.json's PreToolUse command at a script that does `exit 127`, run one Bash tool call, and read the transcript notice: documented expectation is `Failed with non-blocking status code:` and the call PROCEEDS. Control arm: the same script doing `exit 2` must DENY the call."
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-8"
unit = "shell-scripts"
kind = "scripts"
disposition = "refactor"
confidence = 0.60
custom_thing = "scripts/devcontainer-smoke.sh — residual bash assertions in tiers 2 and 3 (doppler canary-key counting via `env | grep -c`, home-volume owner via `stat -c '%U'`, seeded-file/dir existence loops, TMPDIR equality, SSH_AUTH_SOCK + `ssh-add -L` + `ssh -T git@github.com` parsing)."
evidence = "scripts/devcontainer-smoke.sh:51-64 (doppler), :87-124 (home volume, seed survivors, TMPDIR), :126-152 (SSH/R2); the tier-1 and tier-3 CORES are already python-generated and eval'd at :34-35 and :84-85. Invoked from mise.toml:411 and .devcontainer/devcontainer.json:237."
native_alternative = "The repo's own #223 pattern: emit the assertion body from python so the in-container path and the CI no-mount smoke run byte-identical logic — `dotfiles-setup image smoke-script --tier N` (`build_tier1_script` / `build_tier3_script`), extended to the mount/SSH-dependent assertions, with the shell file left as the `bash -lc` launcher."
alternative_evidence = "python/src/dotfiles_setup/image.py:868 (`build_tier1_script`) and :833 (`build_tier3_script`), emitted by the `smoke-script` handler at python/src/dotfiles_setup/image.py:2146/2176 and dispatched at :2286; the same-file rationale at :468 and :519 (\"the two smoke paths cannot diverge\")."
rationale = "Half of this file has already been migrated for exactly the reason that applies to the other half: assertions written twice drift. The residual tier-2/tier-3 checks are the mount- and SSH-dependent ones, which genuinely cannot run in the CI no-mount smoke — but that argues for generating them from python and skipping them in the no-mount context, not for keeping them as hand-written bash. A concrete drift already exists in this unit: both skill files document `scripts/devcontainer-smoke.sh --include-up` (.claude/skills/devcontainer-workflow/SKILL.md:85, .agents/skills/devcontainer-workflow/SKILL.md:73) while the script parses no arguments at all — `$1` appears nowhere in scripts/devcontainer-smoke.sh, so the flag is silently ignored."
probe = "scripts/devcontainer-smoke.sh --include-up  # inside the container: confirm the documented flag is silently ignored (no `devcontainer up` is attempted) before deciding whether to implement or delete it from the two SKILL.md files"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-9"
unit = "shell-scripts"
kind = "scripts"
disposition = "refactor"
confidence = 0.60
custom_thing = "scripts/check-chezmoi-templates.sh — a hermetic-HOME setup plus a `git ls-files 'home/**/*.tmpl'` loop feeding each template to `chezmoi execute-template` on stdin."
evidence = "scripts/check-chezmoi-templates.sh:23-34 (temp HOME + mise symlink + rendered config) and :36-47 (the per-file loop); wired at hk.pkl:454 with `glob = List(\"home/**/*.tmpl\", \"home/.chezmoi.toml.tmpl\")`."
native_alternative = "`chezmoi execute-template` natively accepts many templates and filenames: `chezmoi execute-template [template]...` with `-f, --file  Treat arguments as filenames` — so the whole file set can be passed in one invocation; and hk already computes that set, exposing it as `{{files}}` (the step declares the glob but the script ignores it and re-derives the corpus from `git ls-files`)."
alternative_evidence = ".agent/kb/raw/chezmoi-execute-template-multifile-probe.md (live probe, chezmoi 2.72.1, 2026-09-09: `Usage: chezmoi execute-template [template]... [flags]`, `-f, --file  Treat arguments as filenames`); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/docs/configuration.md:166 (a standalone `{{files}}` expands to one argument per file)."
rationale = "Two independent redundancies: the script re-enumerates a corpus hk already selected (so a one-file commit still renders every template), and it loops because the author did not use `--file`, which the CLI has. The honest caveat is failure attribution — a single multi-file invocation reports one non-zero exit without naming the offending template — so the refactor is `{{files}}` into a python subcommand that calls `chezmoi execute-template --file` per file (or in one shot then bisects), with the hermetic temp-HOME construction (lines 23-34, the part with real reasoning behind it) moving into that module rather than staying in bash."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-10"
unit = "shell-scripts"
kind = "scripts"
disposition = "keep"
confidence = 0.80
custom_thing = "scripts/graphify-hook-guard.sh — 31-line fail-open exec wrapper that resolves this repo's pinned graphify and execs `dotfiles-setup graphify hook-guard <kind>`."
evidence = "scripts/graphify-hook-guard.sh:24-31 (the entire body: one variable, one `command -v uv` guard, one exec, `exit 0`); wired at .claude/settings.json:68 and :78; all nudge logic lives in python/src/dotfiles_setup/graphify.py (`hook_guard_main` / `rewrite_hook_nudge`)."
native_alternative = "None that removes it: `.claude/settings.json` hook entries take a single `command` string, and `${CLAUDE_PROJECT_DIR}` is already used there, so the only alternative is inlining the same `command -v uv && uv run --project … || true` into settings.json — moving the bash rather than eliminating it, and losing the `2>/dev/null || true` advisory containment."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:833 (a hook that cannot start is a non-blocking error — which is why this shim's `exit 0` is belt-and-braces rather than load-bearing) and the settings-level `command` shape used at .claude/settings.json:68."
rationale = "Justification, not default: this is the shape the zero-bash-logic rule explicitly sanctions — a resolve-and-exec shim with zero decision logic, whose entire body is a uv presence check and one exec, and whose nudge-rewriting core already lives in python and is unit-tested. Nothing native can replace it because a settings.json hook is a single command string and the alternative is the identical bash inlined into JSON, where it is harder to read, shellcheck-invisible, and outside the bash budget. Its one questionable line is the interpreter check, which shell-scripts-7's probe also covers."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "shell-scripts-11"
unit = "shell-scripts"
kind = "scripts"
disposition = "keep"
confidence = 0.75
custom_thing = "scripts/web-setup.sh — Claude Code web/cloud bootstrap: installs mise, runs `mise install`, installs Python 3.14 via uv, `uv sync`, and appends PATH to `$CLAUDE_ENV_FILE`."
evidence = "scripts/web-setup.sh:41-73; invoked from the SessionStart hook at .claude/settings.json:102, gated on `CLAUDE_CODE_REMOTE=true`, and pasted verbatim into the claude.ai/code environment setup-script field per scripts/web-setup.sh:15-22."
native_alternative = "None for the bootstrap itself — it must run before uv/python exist, and Claude Code's own native mechanism for this is precisely a shell setup script plus `$CLAUDE_ENV_FILE` PATH export, which this file uses rather than reinvents."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:833 (documents the SessionStart/hook command surface and its non-blocking failure handling; no declarative provisioning primitive exists that would replace a setup script)."
rationale = "Required justification: this is the one script that CANNOT be python — it is the code that installs python. Every other line is a documented native mechanism being invoked (`mise install`, `uv python install`, `uv sync`, `$CLAUDE_ENV_FILE`), so there is no homegrown logic to migrate. One correction rides along from shell-scripts-7: its header claim that an absent interpreter makes the harness \"fail closed, blocking EVERY Bash call\" (scripts/web-setup.sh:7-10) contradicts hooks.md:833/836 — the script is still worth having (the gates genuinely need the toolchain), but the urgency framing should be fixed rather than repeated."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-1"
unit = "skills-1"
kind = "skills"
disposition = "refactor"
confidence = 0.90
custom_thing = "devcontainer-feature-schema-probe hand-rolls a feature-schema lookup out of three `curl https://raw.githubusercontent.com/devcontainers/features/main/src/<name>/{devcontainer-feature.json,README.md,install.sh}` calls piped through jq."
evidence = ".claude/skills/devcontainer-feature-schema-probe/SKILL.md:26-52 (the three curl recipes); :75 (the worked `curl ... | jq '.options'` verification example); :96-99 (\"applies to all devcontainer features ... fetch their devcontainer-feature.json from the source repo\")."
native_alternative = "`devcontainer features info verbose <feature>` in the ALREADY-PINNED @devcontainers/cli 0.89.0 (mise.toml \"npm:@devcontainers/cli\" = 0.89.0). It fetches the published OCI manifest and returns the exact `options` schema inside `annotations[\"dev.containers.metadata\"]`, plus canonical digest and published tags, with `--output-format json`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/raw/devcontainer-features-info-probe.md:5-22 (`devcontainer features info <mode> <feature>`, modes manifest|tags|dependencies|verbose, `--output-format json`); :24-32 (verbose returns the sshd `options` map with exactly `version` + `gatewayPorts`, matching the skill's hand-curled example); :44-47 (control arm: a bogus feature returns \"No manifest found!\", so the probe discriminates)."
rationale = "The skill's JUDGEMENT is still load-bearing and native tooling does not carry it: devcontainer features silently drop unknown option keys, which is why the sshd misconfiguration survived ~3 weeks (:66-70). But its MECHANISM is now strictly worse than the pinned CLI in a way that matters: `raw.githubusercontent.com/.../main/...` reads the feature repo's main branch, whereas the build resolves a PUBLISHED OCI tag — those can differ, so the hand-rolled probe can certify a schema the build never uses. `features info verbose` reads the same artifact the resolver reads, works for third-party/private registries the raw-GitHub URL shape cannot express, and needs no per-feature URL guessing. Rewrite the Procedure section around `devcontainer features info verbose <feature> --output-format json`, keeping the anti-pattern and the silent-drop warning."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-2"
unit = "skills-1"
kind = "skills"
disposition = "retire"
confidence = 0.85
custom_thing = "find-docs is a second, independent ctx7 wrapper that duplicates context7-cli, and it instructs the agent to install and run ctx7 via npm/npx rather than the mise-pinned binary."
evidence = ".claude/skills/find-docs/SKILL.md:27 (`npm install -g ctx7@latest`); :33 (`npx ctx7@latest <command>`); :41-45 (the same `ctx7 library` -> `ctx7 docs` two-step context7-cli/SKILL.md:13-15 already documents); :3-17 (827-char description asserting \"Always use for: API syntax questions ... Prefer this over web search\"). Duplicate surface: .claude/skills/context7-cli/SKILL.md:10 (\"In this repo `ctx7` is mise-pinned — do NOT `npm install -g` or `npx` it\")."
native_alternative = "The repo already has the correct wrapper (context7-cli) plus the pinned binary (mise.toml:49 `\"npm:ctx7\" = \"0.5.9\"`); Claude Code's native lever for retiring a redundant skill without editing a shared repo file is `skillOverrides` (`\"off\"` / `\"name-only\"`), and `/skill-doctor` is the native measurement of which skills go unused and what they cost."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:768-790 (skillOverrides four states: on / name-only / user-invocable-only / off); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:766 (skillOverrides setting row); /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md:11 (`/skill-doctor` added in 2.1.261: \"shows which loaded skills go unused and their context cost, to help prune skills\")."
rationale = "find-docs is a verbatim upstream Context7 skill dropped in unmodified, and every line of it that differs from context7-cli differs by VIOLATING a repo rule. (a) `npm install -g ctx7@latest` / `npx ctx7@latest` is exactly what .claude/rules/ci-local-parity.md:35-41 (\"Rule 3: Use mise binary names, never npx ... npx bypasses mise, re-downloads in CI, and may resolve a different version\") forbids, and context7-cli:10 cites that rule by name. (b) Its \"Always use for ... Prefer this over web search\" instruction routes the agent straight to ctx7, which is step 3 of .claude/rules/research-doc-sources.md — skipping step 00 (the offline KB harness corpus), step 0 (the mintlify cache) and steps 1-2 (llms.txt / per-page .md), i.e. the three cheapest steps the eager rule mandates first. (c) It burns 827 chars of the skill-listing budget (measured; the largest in this unit) on that duplicate. context7-cli covers the same CLI correctly and cites both rules. Delete .claude/skills/find-docs/ and its .agents/skills/find-docs/ twin; if deletion needs a review cycle, `skillOverrides: {\"find-docs\": \"off\"}` removes it from context immediately."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-3"
unit = "skills-1"
kind = "skills"
disposition = "refactor"
confidence = 0.85
custom_thing = "git-branch-commit-push-workflow carries a hand-written branch -> commit -> push -> `gh pr create` procedure, a stacked-PR recipe, a `git worktree add/remove` isolation recipe, and a GitButler post-mortem."
evidence = ".claude/skills/git-branch-commit-push-workflow/SKILL.md:74 (`gh pr create --base main --head <feature-branch> ...`); :89 (`gh pr create --base feature-a --head feature-b ...`); :152 (\"Use `gh pr create --base <base>`\"); :100-111 (the `git worktree add ../<repo>-<branch>` / `git worktree remove` recipe); :55-75 (the branch-off-main procedure)."
native_alternative = "Three separate mechanisms already own every part of this: (1) the PR half is guard-DENIED and redirected to `mise run ship` by the repo's own PreToolUse guard; (2) the branch-first invariant is carried eagerly by do-not.md #9 plus the `branch_guard` PreToolUse hook; (3) worktree isolation is native Claude Code — the `--worktree`/`-w` launch flag, the `EnterWorktree`/`ExitWorktree` tools, and `/batch` (one worktree per unit)."
alternative_evidence = "python/src/dotfiles_setup/hook_guard.py:350-354 (rule \"gh pr create\" -> \"Use `mise run ship` — it runs the path-aware gate matrix\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/worktrees.md:21 (\"Pass `--worktree` or `-w` with a name to create an isolated worktree and start Claude in it\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-sdk__typescript.md:2857-2881 (EnterWorktree / ExitWorktree tool contracts); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/commands.md:60 (`/batch` \"spawns one background subagent per unit in an isolated git worktree\")."
rationale = "Three of the skill's four sections are dead or denied in this repo. Its step 5 (`gh pr create`) is denied by the repo's own guard, so a user who follows the skill hits a deny — a documented procedure whose command cannot run. Its worktree section predates native worktrees entirely. Its branch-first rule duplicates an eager rule that already has four enforcement layers. What survives and is worth keeping is the GitButler composition post-mortem (:20-43): a real, still-uncited failure class with no native equivalent. Note the skill costs ZERO listing context — `disable-model-invocation: true` keeps it out of the skill index (context-window.md:57: \"Skills with `disable-model-invocation: true` are not in this list. They stay completely out of context until you invoke them with `/name`\") — so this is a correctness problem, not a budget one. Rewrite to: the GitButler invariant, a pointer to `mise run ship` / the pr-workflow skill for the PR half, and a pointer to `claude --worktree` for isolation. Do NOT remove `disable-model-invocation: true` (:5, and the HTML comment at :7-15 records that agnix --strict requires it for a state-mutating skill)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-4"
unit = "skills-1"
kind = "skills"
disposition = "refactor"
confidence = 0.85
custom_thing = "ci-warning-investigator's capture step tells the agent to run a LOCAL base-image build and pipe it into tee, and its whole triage/classify/document/file-an-issue loop is prose procedure with no task or library behind it."
evidence = ".claude/skills/ci-warning-investigator/SKILL.md:27 (`docker buildx bake dev-load 2>&1 | tee /tmp/build.log`); :24 (`gh run view <run-id> --log 2>&1 | grep -i \"warn|error|fatal\" | sort -u`); :50-58 (the hand-maintained fixable/unfixable classification table); :74-79 (a hand-written `gh issue create` invocation)."
native_alternative = "The repo's own do-not invariant plus the local-first tier table: local base builds are CI-only, and `mise run verify-apt-pins` / a throwaway `docker run --rm` on the pinned base are the sanctioned local probes. For exit-code-safe capture, the canonical form is `<gate> > /tmp/out.log 2>&1; echo \"rc=$?\" >> /tmp/out.log`."
alternative_evidence = ".claude/rules/do-not.md:14-16 (invariant #2: \"Do NOT `mise run build` or `docker buildx bake dev-load` locally. CI-only.\"); .claude/rules/local-devcontainer-first.md § \"Existing local tiers\" (`mise run verify-apt-pins` ~60s; \"Predicting the base build? A throwaway `docker run --rm` on the digest-pinned BASE_IMAGE\"); .claude/rules/long-running-command-hangs.md rule 3 (\"never `cmd 2>&1 | tail -N` to capture ... Bash returns the last pipeline command's exit code\"); .claude/rules/mise-tasks-only.md table row `<gate> 2>&1 | tail -40` -> redirect-to-file."
rationale = "Line 27 instructs an action the repo's authoritative invariant list forbids outright — the skill is not merely stale, it is actively wrong, and it is the kind of wrong that costs ~2.5h of local build before anyone notices. The `| tee` and `| grep | sort -u` shapes also discard the build's real exit code, the exact failure long-running-command-hangs.md rule 3 exists to prevent. Separately, the skill is pure hand-carried procedure: there is no `[tasks.ci-warning*]` in mise.toml and no `dotfiles_setup` module behind it, so it violates the three-layer stack (skill -> mise task -> python library) that .claude/rules/agent-artifact-conventions.md rule 6 mandates. Replace the capture step with `mise run verify-apt-pins` / a throwaway `docker run --rm` on the pinned base plus redirect-to-file-and-read-rc, and keep the research-first triage judgement, which nothing native supplies."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-5"
unit = "skills-1"
kind = "skills"
disposition = "refactor"
confidence = 0.80
custom_thing = "chezmoi-check carries its entire validation workflow as inline bash — a `for f in home/*.tmpl` loop, three greps, and a `chezmoi verify --verbose 2>&1 | head -20` — with no mise task and no python module behind it."
evidence = ".claude/skills/chezmoi-check/SKILL.md:68-86 (the four-step bash workflow incl. the `for f in home/*.tmpl` loop at :69-72 and the `grep -rn 'is_container' home/ && echo FAIL || echo OK` gate at :82); :17 (`chezmoi verify --verbose 2>&1 | head -20`); :20 (bare `chezmoi diff`, no target)."
native_alternative = "The repo's own three-layer stack — a `mise run <task>` wrapping a `python/src/dotfiles_setup/` module — which every other operational skill in this unit already uses (devcontainer-sync -> `[tasks.sync]` -> sync.py; blast-radius -> `[tasks.graphify-affected]` -> graphify.py). Control arm: `grep -n chezmoi mise.toml` returns ZERO rows, while the same grep shape for `sync`/`graphify` returns `[tasks.sync]` (mise.toml:928) and `[tasks.graphify-affected]` (mise.toml:760), so the absence is real, not a broken probe."
alternative_evidence = ".claude/rules/agent-artifact-conventions.md rule 6 (\"A skill is the TOP of a three-layer stack, never the whole thing ... Build downward: skill -> mise task -> python library ... every mechanic lives in the library, and the task is the seam. No bash\"); .claude/rules/zero-bash-logic.md § \"Applies to\" (\"A new recurring workflow ships its logic as a `python/` module + a mise task, not as a new shell script\"); .claude/skills/devcontainer-sync/SKILL.md:9-12 (the worked in-repo example of the correct shape)."
rationale = "This is the clearest three-layer-stack violation in the unit: a recurring validation workflow whose every mechanic lives in the skill body. Two of those mechanics are also independently broken. (1) `:17` `chezmoi verify --verbose 2>&1 | head -20` returns head's exit code, so a failing verify reads as rc=0 — the exact pipe-kills-exit-code trap. (2) `:20` `chezmoi diff` with no target is the recorded false negative: a relative or absent target prints \"not managed\" and exits rc=0 (memory `feedback_chezmoi_diff_needs_absolute_target`), so the documented command CANNOT report a difference. Ship the loop and the is_container gate as `dotfiles_setup.chezmoi_check` behind `mise run chezmoi-check`, reading real rcs; leave the `chezmoi.os` built-in-fact judgement (:23-37, :51-52) in the skill, which is genuinely non-obvious and has no native carrier. Note `chezmoi apply`/`update` are already host-denied (.claude/settings.json:24-25) while `execute-template`/`verify`/`diff` are read-only and allowed, so the refactor is not blocked on the host."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-6"
unit = "skills-1"
kind = "skills"
disposition = "refactor"
confidence = 0.85
custom_thing = "devcontainer-workflow's task table advertises two entrypoints the repo's own rules forbid or redirect: `mise run build` (a local base-image build) and `mise run pre-commit` (raw unbounded `hk run pre-commit --all`)."
evidence = ".claude/skills/devcontainer-workflow/SKILL.md:22 (\"`mise run build   # docker buildx bake dev-load (build base image locally)`\"); :30 (\"`mise run pre-commit    # hk run pre-commit --all`\"). Task bodies confirm both: mise.toml:1200-1202 (`[tasks.build]` run = \"docker buildx bake dev-load\"); mise.toml:203-205 (`[tasks.pre-commit]` run = \"hk run pre-commit --all\", no timeout)."
native_alternative = "`mise run lint` — the read-only, CI-identical gate wrapped in a hard out-of-process timeout, which mise.toml:207-219 documents as \"the preferred manual/agent entrypoint\" precisely because hk has no timeout of its own; and for the image, the CI-published `:dev` base reached via `mise run sync` / `mise run dev-rebuild`, never a local bake."
alternative_evidence = ".claude/rules/do-not.md:14-16 (invariant #2: no local `mise run build` / `docker buildx bake dev-load`); .claude/rules/mise-tasks-only.md canonical task map row (\"`hk run check --all` / `hk run pre-commit --all`\" -> \"`mise run lint` (read-only ≡ CI, hard timeout + log-tail diagnostics)\"); .claude/rules/long-running-command-hangs.md rule 1 (\"Use `mise run lint`, not raw `hk run check`/`hk run pre-commit` ... because hk has none of its own\", citing the 7-hour hang)."
rationale = "A skill whose job is to be the operator's task table is the single worst place for a forbidden command, because it is read as the menu. `mise run build` is invariant #2 verbatim, listed with no prohibition note. `mise run pre-commit` is the raw unbounded hk invocation that mise-tasks-only redirects and that long-running-command-hangs.md documents wedging for ~7 hours at 0% CPU. The rest of the skill is sound and genuinely non-derivable — the arch-scoped `--id-label` identity trap (:66-79), the ≤89-line host-user overlay cap, the chezmoiignore hard gate. Fix the two rows: annotate `mise run build` as CI-only (or drop it and point at `mise run sync`), and replace `mise run pre-commit` with `mise run lint`. Also worth correcting while in there: `:91` hard-codes \"pytest 190/190\", an inherited count with no control arm that rots on every test added."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-7"
unit = "skills-1"
kind = "skills"
disposition = "refactor"
confidence = 0.85
custom_thing = "Six of this unit's eleven SKILL.md files have a STALE, hand-maintained duplicate under `.agents/skills/<name>/SKILL.md`, and one of the duplicates carries a mechanically-rewritten dead link."
evidence = "Measured with `cmp -s` across all 11 members: DIVERGED = adversarial-review (20 diff lines), blast-radius (8), chezmoi-check (4), context7-cli (8), devcontainer-sync (26), devcontainer-workflow (33, and 12 lines SHORTER at 135 vs 147); identical = bake-action-set-precedence-expertise, ci-warning-investigator, devcontainer-feature-schema-probe, find-docs, git-branch-commit-push-workflow. Sample drift: .agents/skills/devcontainer-sync/SKILL.md:25 still says \"dry-run staleness report (rc 1 if stale)\" where .claude/skills/devcontainer-sync/SKILL.md:25 says \"(rc 1 if stale or this architecture's container is outdated)\" (the #677 arch-scoping); .agents/skills/devcontainer-sync/SKILL.md:56 points at `.Codex/rules/persistence-gate-retry.md`. Only ONE pair has a parity gate: hk.pkl:644-646 `session_review_skill_parity` (`cmp -s .claude/skills/session-review/SKILL.md .agents/skills/session-review/SKILL.md`)."
native_alternative = "The pinned `skills` CLI (mise.toml:51 `\"npm:skills\" = \"1.5.23\"`) owns cross-agent skill distribution natively: `skills add -a <agents>` symlinks (or `--copy`) one source into every agent directory, and `skills experimental_sync` re-syncs them — so `.claude/skills/` and `.agents/skills/` become one source with N links instead of N hand-edited copies. In-repo, the same problem is already solved by copy-from-one-source in `dotfiles_setup.graphify_skill` behind `mise run graphify-skill-install -- <platform>`."
alternative_evidence = "Live probe of the pinned binary, `mise exec -- skills --help` (rc=0): \"Project: experimental_sync   Sync skills from node_modules into agent directories\"; \"Add Options: -a, --agent <agents>   Specify agents to install to (use '*' for all agents)\"; \"--copy   Copy files instead of symlinking to agent directories\". Control arm for the dead link: `ls .claude/rules/persistence-gate-retry.md` succeeds while `ls .codex/rules/persistence-gate-retry.md` returns \"No such file or directory\" (`.codex/` exists and holds only agents/, config.toml, hooks.json, skills/), so `.Codex/rules/...` names nothing."
rationale = "This is the highest-value finding in the unit and nothing currently flags it. The Codex-facing surface silently disagrees with the Claude-facing one for 6 of 11 skills, so a Codex lane reading `.agents/skills/devcontainer-sync/SKILL.md` gets the PRE-#677 semantics of `mise run sync -- --check` — a stale operational contract, in exactly the file the implementer lane reads. The `.Codex/rules/` pointer proves the mirror was produced by a mechanical `.claude` -> `.Codex` substitution that nobody re-ran, and it resolves to nothing (macOS case-insensitivity makes `ls -d .Codex` succeed against `.codex/`, which is why a naive existence check would have passed it — the file underneath does not exist). hk.pkl already encodes the fix pattern for exactly one pair (`session_review_skill_parity`); either generalise that `cmp -s` contract across the mirrored set, or better, stop maintaining copies and use the pinned `skills` CLI's agent-directory linking so there is one source of truth."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-8"
unit = "skills-1"
kind = "skills"
disposition = "refactor"
confidence = 0.70
custom_thing = "Four narrowly-scoped skills in this unit rely on description-keyword matching alone for activation, with no `paths:` frontmatter, and one of them stores its provenance in ad-hoc top-level frontmatter keys."
evidence = ".claude/skills/bake-action-set-precedence-expertise/SKILL.md:1-4 (frontmatter = name + description only; the skill is only ever relevant to docker/bake-action in .github/workflows/**); .claude/skills/chezmoi-check/SKILL.md:1-4 (only relevant to home/**.tmpl); .claude/skills/devcontainer-feature-schema-probe/SKILL.md:1-7 (only relevant to .devcontainer/devcontainer.json; and carries non-standard keys `type: learned-skill`, `extracted-from:`, `applicability:`); .claude/skills/ci-warning-investigator/SKILL.md:1-4. Measured frontmatter keys across the unit: only `name`, `description`, `user-invocable`, `disable-model-invocation` are used anywhere, plus those three ad-hoc keys."
native_alternative = "Two documented frontmatter fields: `paths:` (glob patterns that limit when a skill is auto-activated, same format as path-specific rules) and `metadata:` (a free-form YAML map explicitly provided for the author's own key-value data)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:351 (\"`paths` ... Glob patterns that limit when this skill is activated. Accepts a comma-separated string or a YAML list. When set, Claude loads the skill automatically only when working with files matching the patterns.\"); skills.md:353 (\"`metadata` ... Free-form YAML map for your own key-value data ... Claude Code doesn't act on its contents, and drops a value that isn't a map. Don't reuse frontmatter field names such as `paths` as keys.\")."
rationale = "`paths:` is the native, documented mechanism for exactly the shape these four skills have: file-triggered expertise whose relevance is decidable from the path being edited. This is the skills-side twin of the trigger test the repo already applies to rules (.claude/rules/md-size-budgets.md § \"Scoping: the trigger test\": file-triggered -> safe to scope; behaviour-triggered -> must stay eager). Adding `paths:` to the four file-triggered members makes their activation deterministic instead of description-keyword-lucky, and frees listing budget for the behaviour-triggered ones (adversarial-review, blast-radius, devcontainer-sync) that must stay unscoped. Confidence is 0.7 rather than higher because `paths:` narrows auto-activation and could in principle suppress a legitimate cross-file invocation (e.g. asking about bake precedence while reading docker-bake.hcl rather than the workflow) — so each glob needs to be chosen to cover every real trigger file, not just the obvious one. The `metadata:` half is unambiguous: move `type`/`extracted-from`/`applicability` under `metadata:` so the frontmatter stops carrying keys outside the documented schema."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "skills-1-9"
unit = "skills-1"
kind = "skills"
disposition = "keep"
confidence = 0.95
custom_thing = "Description-length audit of all eleven members against the 1,536-character skill-listing truncation cliff."
evidence = "Measured with a yaml.safe_load of each frontmatter over `description` + `when_to_use` (the two fields that share the cap): find-docs 827, adversarial-review 574, blast-radius 548, devcontainer-sync 323, context7-cli 266, git-branch-commit-push-workflow 237, devcontainer-feature-schema-probe 207, ci-warning-investigator 204, devcontainer-workflow 191, bake-action-set-precedence-expertise 178, chezmoi-check 152. Maximum = 827, i.e. 54% of the cap; no member is within 700 chars of it. No member uses `when_to_use` at all."
native_alternative = "The cap itself is native and configurable — `skillListingMaxDescChars` (default 1,536) and `skillListingBudgetFraction` (default 1% of the context window) — and `/skill-doctor` (new in 2.1.261) is the native way to measure which listed skills actually get used and what they cost."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:337 (\"the combined `description` and `when_to_use` text is truncated at 1,536 characters in the skill listing\"); skills.md:1052 (\"if you have many skills, Claude Code shortens descriptions to fit the listing's character budget ... The budget scales at 1% of the model's context window\"); skills.md:1058 (skillListingBudgetFraction / SLASH_COMMAND_TOOL_CHAR_BUDGET / skillListingMaxDescChars); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings-reference.md:2739-2753 (skillListingMaxDescChars)."
rationale = "Required negative answer to the unit's explicit question, stated with its measurement rather than assumed: NO description in this unit exceeds the 1,536-char cap, so no skill in skills-1 is being silently truncated out of discoverability. Keep the descriptions as they are. The caveat worth recording is that the cap is not the only pressure — skills.md:1052 says the per-entry cap applies \"regardless of budget\", and the LISTING budget (1% of context, shared across all 35 skills in .claude/skills/ plus every enabled plugin's) can shorten descriptions before any individual one hits 1,536. That is a whole-repo question, not a skills-1 question; the native probe for it is `/skill-doctor`, and the native lever is `skillOverrides: \"name-only\"` on low-priority entries (find-docs, per skills-1-2, is the obvious first candidate)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-10"
unit = "skills-1"
kind = "skills"
disposition = "keep"
confidence = 0.80
custom_thing = "adversarial-review — a 330-line skill that teaches how to WRITE and BOUND a review round: the finite-co-domain test, the cardinality grep, the completion-not-emptiness stop condition, Q-FRESH/Q-SCOPE/Q-CLAIM, and the ticket escape hatch."
evidence = ".claude/skills/adversarial-review/SKILL.md:17-37 (the finite-co-domain property); :68-90 (the bounded test: \"A round is bounded iff its brief states a domain with a CARDINALITY\", control-armed across seven verbatim #601 briefs, v1-v6 zero hits vs v7 twice); :92-141 (the stop condition, with both rejected severity-keyed variants replayed); :154-201 (Q-FRESH/Q-SCOPE/Q-CLAIM); :213-290 (the three brief templates)."
native_alternative = "The bundled `/code-review` skill (and its ultra multi-agent cloud tier), plus `/security-review`, `/simplify`, and the `mattpocock-skills:code-review` marketplace skill — all of which RUN a review. None of them writes a brief, states a stop condition, or decides whether a review LOOP should end."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:23 (\"Claude Code includes a set of bundled skills, such as `/doctor`, `/code-review`, `/batch`, `/debug`, `/loop`, and `/claude-api`\"); skills.md:126-127 (a project skill of the same name overrides a bundled one — not applicable here: no member of this unit collides with a bundled name); skills.md:347-348 (`context: fork` + `agent` — the native way for a skill to dispatch its own subagent round)."
rationale = "Required justification: the native review surface is an ENGINE, this skill is a CONTROLLER, and no native feature occupies the controller slot. `/code-review` at any effort level answers \"what is wrong with this diff\" — which is precisely the unbounded round-1 question this skill measures as non-terminating (:22-26, six inconclusive rounds on #601). Nothing bundled or in a marketplace supplies a cardinality test, a completion-over-emptiness stop condition, or the re-open clause; those are derived from a measurement of this repo's own review history and are not reconstructible from a vendor doc. Two adoption notes rather than a supersession: (1) `/code-review` is a legitimate ENGINE for the round-1 open-hunting slot the skill already sanctions at :53-54, so the round-1 template could name it instead of implying a hand-briefed agent; (2) `context: fork` + `agent:` frontmatter (skills.md:347-348) would let the skill dispatch its own bounded round in a forked subagent rather than instructing the caller to do it by hand — worth considering, but it changes the skill from advisory to executing, which is a deliberate design choice and not an audit finding."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-11"
unit = "skills-1"
kind = "skills"
disposition = "keep"
confidence = 0.90
custom_thing = "blast-radius and devcontainer-sync — two skills that are pure judgement layers over `mise run <task>` -> `python/src/dotfiles_setup/` modules, with all mechanics in the library."
evidence = ".claude/skills/blast-radius/SKILL.md:16-19 (\"Both are thin `mise` callers over `python/src/dotfiles_setup/graphify.py` (`affected`/`affected_main`, `prs`/`prs_main`) ... All mechanics live there; this file is judgement only\"), backed by mise.toml:760 `[tasks.graphify-affected]` and mise.toml:770 `[tasks.graphify-prs]`; .claude/skills/devcontainer-sync/SKILL.md:9-12 (\"Logic lives in `python/src/dotfiles_setup/sync.py` (zero-bash-logic); the mise task is a thin caller of `dotfiles-setup docker sync`\"), backed by mise.toml:928 `[tasks.sync]` and the `workflow.sync-wiring` contract in python/verification/suites.toml referenced at :68-70."
native_alternative = "Native candidates were checked and none reaches these: `/blast-radius`-style reverse traversal has no bundled equivalent (graphify is the substrate, and its gap digest reports zero new capabilities since the 0.9.53 pin); devcontainer convergence has no Claude Code surface at all; the generic native levers (`Monitor`, `/loop`, `/schedule`, agent teams) address WAITING and REPETITION, not graph traversal or image-digest convergence."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/raw/graphify-readme-0.9.56.digest.md (\"0 new capabilities\"; control-armed: all 14 command terms from the fetch already present in the offline README, so the null is a real answer); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:23 (the bundled-skill roster — `/doctor`, `/code-review`, `/batch`, `/debug`, `/loop`, `/claude-api` — contains no graph-traversal or container-convergence skill)."
rationale = "Required justification: these two are the reference implementation of the shape this audit is looking for, and there is nothing native to move them onto. Each is the top of the mandated three-layer stack (skill -> mise task -> python library, .claude/rules/agent-artifact-conventions.md rule 6), each carries only the non-derivable judgement, and both non-obvious failure modes they document are the kind a native tool would not tell you: blast-radius:35-39 (graphify prints \"No unique node match\" at rc 0, which is a DIFFERENT answer from \"No affected nodes found\" — read the message, not the exit code) and devcontainer-sync:53 (`registry: UNREACHABLE` means staleness is UNKNOWN, so a green run is not base-currency evidence). Both are exactly the control-arm discipline the repo's rules demand, encoded where it gets read. One nit, not a disposition change: blast-radius:67-73 self-flags that it was hand-written rather than authored via `/skill-creator:skill-creator` because a codex lane cannot invoke that slash command — that disclosure is correct and should stay."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-1-12"
unit = "skills-1"
kind = "skills"
disposition = "keep"
confidence = 0.80
custom_thing = "bake-action-set-precedence-expertise (38 lines) and the deprecated-command caveat inside context7-cli — two single-fact expertise notes with no task, library or gate behind them."
evidence = ".claude/skills/bake-action-set-precedence-expertise/SKILL.md:9 (\"the `set:` parameter has higher precedence than the `push:` parameter ... it overrides `push: true`\"); :12 (the #34-#36 three-PR diagnosis cost); :22-27 (the conditional-override pattern). .claude/skills/context7-cli/SKILL.md:19-23 (the `ctx7 skills …` deprecation caveat, previously verified on 0.5.5)."
native_alternative = "For bake-action: none — neither actionlint 1.7.12, zizmor 1.30.0, ghalint 1.5.6 nor any Claude Code feature encodes third-party action PARAMETER precedence; the fact lives only in docker/bake-action's own behaviour. For the ctx7 caveat: the pinned binary is now 0.5.9, not the 0.5.5 the skill cites, so the caveat needed re-arming rather than replacing."
alternative_evidence = "Live control-armed re-probe on the current pin (mise.toml:49 `\"npm:ctx7\" = \"0.5.9\"`; `mise exec -- ctx7 --version` -> `0.5.9`): `mise exec -- ctx7 --help` (rc=0) lists ONLY `setup`, `remove`, `library` (line 17), `docs` (line 19) — `skills` is absent from help; yet `mise exec -- ctx7 skills list` still EXECUTES, printing \"Warning: Skill commands are deprecated and will stop working in the next major release.\" and then the skill listing. Control arm: `library` and `docs` appear in `--help` AND execute, so the probe discriminates \"hidden but live\" from \"gone\"."
rationale = "Required justification, one per skill. (a) bake-action-set-precedence-expertise: nothing native or pinned knows that `set:` beats `push:` in docker/bake-action — it is a third-party composite action's internal precedence, invisible to actionlint/zizmor/ghalint, and it cost three PRs (#34-#36) of green-CI-but-no-image to diagnose. A 38-line note is the right size for it and there is no cheaper carrier. The only strengthening available is a machine gate (an ast-grep or hk pattern rejecting an unconditional `*.output=type=cacheonly` in a bake-action `set:` block), which would be an ADDITION, not a replacement — the skill would still carry the why. (b) context7-cli: keep as the repo's single ctx7 wrapper (it is the survivor in skills-1-2), and update one number — the caveat says \"verified on the pinned 0.5.5\" while the pin is 0.5.9. I re-armed it rather than repeating the inherited claim: the deprecation is STILL true on 0.5.9, hidden from `--help` but still executing at rc 0. Change `0.5.5` to `0.5.9` and keep the \"absence from --help is NOT proof they are gone\" warning, which the re-probe confirms is still the correct reading."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-1"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.90
custom_thing = "`.claude/skills/graphify/SKILL.md` is the vendor-generated graphify bundle (stamped `.claude/skills/graphify/.graphify_version` = 0.9.53) and instructs the agent to invoke the bare `graphify` binary and to self-install/upgrade it."
evidence = ".claude/skills/graphify/SKILL.md:53 (\"Run `graphify query \\\"<question>\\\"` immediately\"), :689 (bare ```graphify query \"<question>\"``` fenced block), :65 (\"### Step 1 - Ensure graphify is installed\"), :88 (`uv tool install --upgrade graphifyy -q 2>&1 | tail -3`), :92-93 (`pip install graphifyy -q --break-system-packages ... | tail -3`). This contradicts .claude/rules/graphify-first.md (\"Always the mise tasks, never a bare `graphify` on `PATH`\") and .claude/rules/do-not.md #8, and the repo pins graphify in python/pyproject.toml (0.9.53) so a self-upgrade path silently drifts the pin. The `| tail -3` on the install commands also violates .claude/rules/long-running-command-hangs.md rule 3 (pipe masks the real rc)."
native_alternative = "The repo's own thin-layer tasks already exist and are what the PreToolUse hook forces: `mise run graphify-query` (mise.toml:724), `mise run graphify-update` (mise.toml:737), `mise run graphify-affected` (mise.toml:760); refreshing the bundle is `mise run graphify-skill-install -- claude` (mise.toml:749). Claude Code's own skill mechanism for keeping the file authoritative is the frontmatter reference set in the skills doc."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:311 (\"Keep the body itself concise... State what to do\") and :336-355 (frontmatter reference); repo side mise.toml:724, mise.toml:737, mise.toml:760, mise.toml:749. Gap digest .agent/kb/raw/graphify-readme-0.9.56.digest.md reports zero new graphify capabilities since the pin, so nothing upstream has fixed this."
rationale = "The skill is not superseded (nothing native builds a knowledge graph), but its imperative half is actively wrong for this repo: it tells an agent to do three things the repo hard-bans. `.claude/skills/graphify-skill-install/SKILL.md` exists precisely to regenerate this file from the vendor package, so the vendor text keeps coming back. Refactor to the same shape `.agents/skills/graphify/SKILL.md` already uses (a DELIBERATE STUB redirecting to the mise tasks), or post-process the vendor bundle on install to rewrite bare `graphify <verb>` into `mise run graphify-<verb> --`; the PreToolUse hook is currently the only thing standing between this text and a banned call."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "skills-2-2"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.85
custom_thing = "`.claude/skills/graphify/SKILL.md` is 714 lines / 41,300 bytes of inline build procedure (shell blocks for Steps 0-9), loaded whole into the main conversation context whenever the skill triggers, even though a `references/` sidecar already exists next to it."
evidence = ".claude/skills/graphify/SKILL.md — 714 lines, 41300 bytes (`wc -c`); headings at :61-:599 are the inline pipeline (Step 0 through Step 9) with embedded bash; `.claude/skills/graphify/references/` already holds 8 offloaded files (query.md 13456 B, update.md 10425 B, extraction-spec.md 7960 B, …), so the progressive-disclosure mechanism is wired but the build steps were never moved."
native_alternative = "Claude Code's documented skill sizing rule plus two frontmatter mechanisms the file does not use: `context: fork` (run the skill in its own forked subagent context so its body never enters the main window) and `background`/`agent`; plus `/skill-doctor` to measure what unused loaded skills actually cost."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:472 (\"Keep `SKILL.md` under 500 lines. Move detailed reference material to separate files.\"), :347 (`context` frontmatter: \"Set to `fork` to run in a forked subagent context\"), :349 (`background`), :519 (invoked skills are re-attached after compaction, 5,000-token slice each, 25,000-token shared budget); `/skill-doctor` shipped 2.1.261 per .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (host is 2.1.266)."
rationale = "Nothing native replaces graphify, but the harness has an explicit answer for a skill this large and it is unused: control-armed grep shows `context: fork` appears in 0 of the 33 project skills (`grep -rn '^context: fork' .claude/skills/` = 0 while `^name:` = 33, so the probe discriminates). The build pipeline is a whole-turn operation whose 41 KB is pure overhead for the far more common query path — fork it, or move Steps 0-9 into `references/build.md` behind the existing sidecar the way query/update/exports already are."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "skills-2-3"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.95
custom_thing = "`.claude/skills/mcp2cli/SKILL.md` justifies the whole skill on the claim that registered MCP servers \"inject every tool's JSON schema into Claude's system prompt for **every turn of every conversation, forever**\" — \"easily several thousand tokens of permanent context tax per conversation\"."
evidence = ".claude/skills/mcp2cli/SKILL.md:139-143 (the claim), :150-152 (the rule of thumb derived from it), :9-12 (\"nothing is registered with Claude Code, so **no tool schemas land in Claude's system prompt**\"). The repo's own eager rule already flags this: .claude/rules/research-doc-sources.md § \"MCP: two lanes\" — \"the old 'every schema in every conversation, forever' cost is FALSE here (measured 2026-07-30) ... a 33× difference. Do not cite that sentence\"."
native_alternative = "MCP tool search: MCP tools are deferred by default and discovered on demand via the `ToolSearch` tool, so only tool NAMES are presented, not schemas."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/mcp.md:1376 (\"Tool search is enabled by default: MCP tools are deferred and discovered on demand.\"), :1393 (unset ENABLE_TOOL_SEARCH → \"All MCP tools deferred and loaded on demand\"), :1354 (\"## Scale with MCP tool search\"); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/tools-reference.md:60 (`ToolSearch` — \"Searches for and loads deferred tools\"). Control arm: the same grep shape returns 140 hits for \"MCP\" in mcp.md, so the 0-result risk is excluded."
rationale = "The skill's stated reason to exist is a fact the harness invalidated, and the repo's own rule explicitly forbids citing it — yet the skill is where the sentence still lives, so anything reading the skill instead of the rule inherits the wrong model. mcp2cli still has a real (smaller) case — a one-shot call with no registration, no auth plumbing, no server lifecycle — so this is a rewrite of the justification and the 'rule of thumb', not a retirement of the tool."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-4"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.93
custom_thing = "`.claude/skills/mintlify/SKILL.md` repeats the same superseded context-tax claim in its closing section: \"Registering an MCP server via `claude mcp add` injects every tool's schema into Claude's system prompt for every conversation forever\"."
evidence = ".claude/skills/mintlify/SKILL.md:252-260 (\"## Prefer `mcp2cli` over `claude mcp add` for mintlify servers\"), duplicating .claude/skills/mcp2cli/SKILL.md:139-143. Both are contradicted by .claude/rules/research-doc-sources.md § \"MCP: two lanes\"."
native_alternative = "Same as skills-2-3: default-on MCP tool search defers tool schemas and loads them via `ToolSearch`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/mcp.md:1376, :1393; tools-reference.md:60."
rationale = "Second copy of a claim the harness disproved. It also does not need to be here at all: the section adds nothing beyond a pointer, and the skill's own load-bearing findings (per-repo `/mcp` URLs are GET-only descriptors; POST → 404) are independent of the cost argument and stay true. Delete the section and link `.claude/rules/research-doc-sources.md` instead of restating a superseded premise in a second place."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-5"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.97
custom_thing = "`.claude/skills/mcp2cli/SKILL.md` restates the tool's pin in prose: \"Pinned in `mise.toml` as `\\\"pipx:mcp2cli\\\" = \\\"2.6.0\\\"`\"."
evidence = ".claude/skills/mcp2cli/SKILL.md:156 says `2.6.0`; the actual pin is mise.toml:31 `\"pipx:mcp2cli\" = \"3.7.0\"` — a full major version behind, and 2.6.0 predates the argparse/flag-order behaviour the same file documents at :94-97 and :103-117 as current."
native_alternative = "mise's own config is the single source of truth and is already queryable at runtime (`mise ls pipx:mcp2cli`, `mise.toml` itself); the harness's skill format also supports inline bash injection so a skill can print the live pin rather than freeze a copy of it."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:352 (`shell` frontmatter governs `` !`command` `` and ` ```! ` blocks in a skill — i.e. inline shell injection is a first-class skill feature); repo side mise.toml:31."
rationale = "A transcribed version number in a doc has no gate behind it and has already drifted 2.6.0 → 3.7.0. The line adds nothing a reader cannot get from mise.toml, and a stale one actively misleads anyone reasoning about whether a documented flag exists at the installed version. Drop the literal, or replace it with an inline `` !`mise ls pipx:mcp2cli` `` so it cannot rot."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-6"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.82
custom_thing = "`.claude/skills/handoff/SKILL.md` exists on the premise that no cross-surface continuity mechanism exists: \"Claude Code sessions do NOT transfer across surfaces ... there is no built-in cross-device resume (confirmed against the official docs). The only state that crosses the boundary is what you **commit to the branch**.\""
evidence = ".claude/skills/handoff/SKILL.md:10-16 (the premise), :47-52 (step 4, commit+push as \"the cross-surface part\"), :59-68 (step 5, the pasted resume line). The whole procedure is hand-carried — doc sync, a tracked markdown file, a plain `git push`, and a printed instruction for the human to run on the other machine."
native_alternative = "Three shipped harness features cross exactly this boundary: `/teleport` (pull a Claude Code on the web session into this terminal, fetching the branch AND the conversation), Remote Control (drive a running local session from claude.ai or the mobile app on another device), and cross-session messaging (message your own sessions on other machines, `SendMessage` by name)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/commands.md:148 (`/teleport` — \"Pull a Claude Code on the web session into this terminal. Opens a picker, then fetches the branch and conversation\"), commands.md:29 (\"`/teleport` pulls a web session into this terminal, and `/remote-control` lets you continue this local session from another device\"), overview.md:225 (\"Continue a local session from my phone or another device | Remote Control\"), remote-control.md:144-146 (\"### Connect from another device\"), remote-control.md:173 + changelog.md:812 (cross-session messaging / `SendMessage` to sessions on other machines, v2.1.224+)."
rationale = "The skill's foundational sentence is now false, and it is stated as verified (\"confirmed against the official docs\"), so nothing prompts a re-check. Not a full retirement: `/teleport` is web→terminal only and Remote Control needs the origin session still running, so a durable tracked handoff is still the right answer for terminal→terminal on a different machine, or for a session that has ended. But the skill must say which case it covers and route the other cases to the native verb — as written it would have an agent hand-build a markdown handoff for a web→terminal move that `/teleport` does in one command."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-7"
unit = "skills-2"
kind = "skills"
disposition = "unknown"
confidence = 0.55
custom_thing = "`.claude/skills/handoff/SKILL.md` step 5 tells the user to paste `/resume docs/handoffs/session-{date}.md` on the receiving surface, relying on the project skill `.claude/skills/resume/SKILL.md` to claim that command name."
evidence = ".claude/skills/handoff/SKILL.md:59-68 (\"Print exactly (single line the user pastes on the receiving surface)\" → ```/resume docs/handoffs/session-{date}.md```); the project skill it targets is .claude/skills/resume/SKILL.md (frontmatter `name: resume`, `disable-model-invocation: true`, `argument-hint: \"[path to a specific handoff file, optional]\"`)."
native_alternative = "`/resume [session]` is a Claude Code built-in command (\"Resume a conversation by ID or name, or open the session picker\", alias `/continue`) — a different meaning for the same token, and the docs are explicit that built-in command names are reserved in at least some resolution paths."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/commands.md:127 (`/resume [session]` built-in, alias `/continue`); skills.md:122-132 (name resolution: a local skill overrides a *bundled* skill, and a skill beats a `.claude/commands/` file — neither clause states the outcome against a *built-in command*), skills.md:240 (\"Claude Code also reserves the names of its own built-in commands and bundled skills ... so it skips a synced skill with one of those names too\")."
rationale = "The documented precedence table covers skill-vs-bundled-skill and skill-vs-command-file but never states what a *project* skill does against a reserved built-in command name; skills.md:240 shows the reservation is real for at least synced skills. If the built-in wins, handoff's step-5 output is a broken instruction that opens a session picker on the receiving machine instead of reading the handoff — the exact surface where the user has no context to debug it. Cheap to settle, and if it resolves against the skill the fix is a rename (e.g. `/session-resume-handoff`), not a redesign."
probe = "cd /Users/rmanaloto/dev/github/ray-manaloto/dotfiles && claude -p '/resume docs/handoffs/nonexistent.md' --permission-prompts none 2>&1 | head -30   # then compare with: claude -p '/help' 2>&1 | grep -n 'resume' — does the project skill body run, or does the built-in session picker/ID lookup answer?"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-8"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.80
custom_thing = "`.claude/skills/pr-workflow/SKILL.md` prescribes a hand-rolled shell poll loop as the sanctioned way to wait for a PR to auto-merge: \"`land` has no `--wait` and `gh pr checks --watch` is guard-denied, so poll the blessed one-shot read, keeping the turn engaged: `until [ \\\"$(gh pr view <N> --json state --jq .state)\\\" = MERGED ]; do sleep 60; done`\"."
evidence = ".claude/skills/pr-workflow/SKILL.md:128 (the `land: PR #N is OPEN` failure-mode row). This is the exact anti-pattern .claude/rules/gh-cli-watch.md names (\"Never hand-roll a `while ! gh ... ; do sleep; done` polling loop\"), and the harness's own Bash tool contract on this host states foreground `sleep` is blocked and directs the caller to `Monitor` with an until-loop instead — so the prescribed recovery cannot execute as written."
native_alternative = "Two shipped mechanisms: the bundled `/loop [interval] [prompt]` skill (run a prompt repeatedly while the session stays open; omit the interval and Claude self-paces), and the `Monitor` tool, which the Bash tool contract explicitly designates for waiting on a condition instead of a foreground `sleep`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/commands.md:106 (\"`/loop [interval] [prompt]` — **Skill.** Run a prompt repeatedly while the session stays open ... Example: `/loop 5m check if the deploy finished`\"); Bash tool description in this session's tool contract: \"Foreground `sleep` is blocked; use Monitor with an until-loop to wait on a condition.\" `Monitor` is present in this host's deferred-tool list."
rationale = "This is a documented recovery step that the current harness blocks, in the one skill agents reach for whenever a ship stalls — the failure lands at the worst moment and looks like a `gh` problem. `/loop 2m mise run land -- <PR#>` (or a Monitor until-loop on the same one-shot `gh pr view --json state` read the row already blesses) gives the same bounded wait using a mechanism that runs, and it stops the skill from contradicting `.claude/rules/gh-cli-watch.md` while citing it three lines later. The rest of pr-workflow (ship/automerge/land, `--match-head-commit`, the provenance split) is not superseded."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-9"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.75
custom_thing = "`.claude/skills/mintlify/SKILL.md` and `.claude/skills/mcp2cli/SKILL.md` carry two identical reference sections — the mcp2cli tool-name `_ → -` normalization gotcha and the pre-subcommand flag-order gotcha — as duplicated prose in both files."
evidence = ".claude/skills/mintlify/SKILL.md:196-216 (\"## Tool-name normalization gotcha\") and :218-231 (\"## Flag-order gotcha\") duplicate .claude/skills/mcp2cli/SKILL.md:103-117 and :85-101, including the same WRONG/RIGHT examples and the same `unrecognized arguments: --head` error string. mintlify's body is 271 lines; mcp2cli's is 174."
native_alternative = "The skill format's documented answer for shared/detailed reference material is a sidecar file loaded on demand (`${CLAUDE_SKILL_DIR}` resolves it regardless of cwd), which the sibling `.claude/skills/graphify/references/` and `.claude/skills/context7-cli/references/` already use in this repo."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:472 (\"Keep `SKILL.md` under 500 lines. Move detailed reference material to separate files.\"), :311 (\"every line is a recurring token cost\"), :408 (`${CLAUDE_SKILL_DIR}` — \"The directory containing the skill's `SKILL.md` file ... reference files bundled with the skill, regardless of the current working directory\")."
rationale = "A duplicated gotcha is two things to fix when mcp2cli's CLI changes — and it already went stale once in one copy (the pin, skills-2-5), which is the failure this shape produces. mintlify's own section even ends \"see 'Tool-name normalization' below\" in a block copied from mcp2cli, so the cross-reference is already confused. The mcp2cli file owns these facts; mintlify should link them."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-10"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.70
custom_thing = "Every skill in this unit activates purely on description matching — none uses the `paths:` frontmatter to scope activation to the file surface it is about, even where the surface is a single named file: `lock-image` (`.devcontainer/mise-system.toml`, `mise-runtime.toml`), `lock-shared` (`.config/mise/conf.d/shared.toml`), `pkl-import-hyphen-alias-expertise` (`*.pkl`), `lint-delta` (`python/pyproject.toml` / `python/uv.lock`)."
evidence = "Control-armed measurement across all 33 project skills: `grep -rl '^paths:' .claude/skills/` → 0 files, while `grep -rl '^name:' .claude/skills/` → 33 files, so the probe discriminates. The four skills' own frontmatter carries only `name`/`description`/`user-invocable` (.claude/skills/lock-image/SKILL.md:1-5, .claude/skills/lock-shared/SKILL.md:1-5, .claude/skills/lint-delta/SKILL.md:1-5, .claude/skills/pkl-import-hyphen-alias-expertise/SKILL.md:1-4). Their descriptions are among the longest in the repo (lock-image 685 chars, lock-shared 674, lint-delta 525) and every one of those characters sits in the skill listing every session."
native_alternative = "`paths:` frontmatter — \"Glob patterns that limit when this skill is activated ... Claude loads the skill automatically only when working with files matching the patterns\"; plus `skillOverrides` `\"name-only\"` and `skillListingBudgetFraction` for listing-budget control, and `/skill-doctor` to measure which loaded skills go unused and what they cost."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:351 (`paths` frontmatter), :1058 (\"set low-priority entries to `\\\"name-only\\\"` in `skillOverrides` so they list without a description ... configurable with `skillListingMaxDescChars`\"), :342 (`user-invocable: false` for background knowledge); `/skill-doctor` shipped 2.1.261 per .agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (host 2.1.266)."
rationale = "These are read-triggered skills in the repo's own taxonomy (.claude/rules/md-size-budgets.md § 'the trigger test': `paths:` fires on READ, which is right when the trigger is editing a named config file, and wrong for judgment-shaped rules) — you edit `shared.toml`, THEN you need `lock-shared`. `paths:` is the documented mechanism for exactly that and is unused repo-wide, so all 33 descriptions compete for one listing budget every session. Note the truncation axis is currently clean: the longest description in this unit is 685 chars against the 1,536-char cap (skills.md:337), so nothing here is being silently cut — the cost is budget pressure, not truncation. Start with `/skill-doctor` to rank, then scope the file-surface skills."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-11"
unit = "skills-2"
kind = "skills"
disposition = "refactor"
confidence = 0.65
custom_thing = "`.claude/skills/pkl-import-hyphen-alias-expertise/SKILL.md` is a 37-line always-listed skill whose body is roughly half retired history — the pklr backend limitation and the `HK_PKL_BACKEND=pkl` override it recommends were both removed from the repo."
evidence = ".claude/skills/pkl-import-hyphen-alias-expertise/SKILL.md:3 and :9 both label the pklr half \"Historical\" / \"verified FIXED by hk 1.49 — the `HK_PKL_BACKEND=pkl` override was retired in #160 T12\"; :23 repeats it as \"(Retired)\"; yet :31 and :35 still ship `# HK_PKL_BACKEND=pklr` / `# HK_PKL_BACKEND=pkl` inside the RIGHT/WRONG example block, so the retired override is presented as part of the correct pattern. AGENTS.md § 'Split hk Architecture' states the same retirement (host pin is hk 1.57.0). The live half — hyphenated pkl imports need an `as` alias — is a Pkl language fact, not repo logic."
native_alternative = "The surviving fact is enforced by the toolchain itself (pkl/pklr refuses a hyphenated implicit identifier at evaluation, and `mise run lint`'s hk pkl steps run on every hk.pkl edit), and the harness's mechanism for narrow background knowledge is `paths:` + `user-invocable: false` rather than a permanent listing entry."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/skills.md:351 (`paths` — auto-load only for matching files), :342 (`user-invocable: false` — \"Use for background knowledge users shouldn't invoke directly\"), :311 (\"State what to do rather than narrating how or why\"). Gap digest .agent/kb/raw/hk-releases-since-pin.digest.md shows nothing since the hk pin reopens the pklr question."
rationale = "Keeping it is defensible — the alias requirement is a real trap with a cryptic error and `hk-common.pkl` is hyphenated, so the repo hits it. But two-thirds of the body documents a mechanism that no longer exists and the code example still shows the retired env var as the RIGHT answer, which is how a stale skill becomes a wrong instruction. Cut to the live fact, add `paths: ['**/*.pkl', 'hk*.pkl']` and `user-invocable: false`, and move the #160 T12 archaeology to `docs/rules-evidence/` per .claude/rules/agent-artifact-conventions.md."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-2-12"
unit = "skills-2"
kind = "skills"
disposition = "keep"
confidence = 0.88
custom_thing = "The five task-wrapping skills in this unit — `lock-image`, `lock-shared`, `lint-delta`, `graphify-skill-install`, `memory-index-curation` — plus `pr-workflow`'s ship/automerge/land core."
evidence = "Each is the thin top of the required three-layer stack and names its own layers: lock-image SKILL.md:9-13 → mise.toml:1331 `[tasks.lock-image]` → python/src/dotfiles_setup/image_lock.py; lock-shared SKILL.md:9-13 → mise.toml:1299 → lock_shared.py; lint-delta SKILL.md:16 → mise.toml:1526 → lint_delta.py; graphify-skill-install SKILL.md:9-12 → mise.toml:749 → graphify_skill.py; memory-index-curation SKILL.md:18-22 → mise.toml:713 `[tasks.memory-index]`; pr-workflow SKILL.md:9-13 → mise.toml:887 `[tasks.ship]` → pr.py. Bodies carry judgement (which artifact, which failure mode, measured control arms) rather than transcribed procedure — e.g. lock-shared SKILL.md:41-61 (the macOS-vs-linux uv asset divergence, with its bun/hk/pixi/yq control arm) and lock-image SKILL.md:52-59 (the 131→64 linux-x64 truncation at an unchanged tool count)."
native_alternative = "None. Nothing bundled or in an installed plugin regenerates mise lockfiles per-platform, partitions linter violations across two pinned tool versions, audits the auto-memory index budget, places a graphify skill surface without the banned vendor installer, or drives this repo's gated ship/automerge/land loop. `/skill-doctor` (2.1.261) can rank their context cost but does not replace any of them; `/loop` and `Monitor` address only pr-workflow's wait step (skills-2-8), not the verbs."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/commands.md — the full bundled-skill and built-in command table (`/loop`:106, `/resume`:127, `/teleport`:148) contains no lockfile, linter-delta, memory-index, or repo-PR-loop entry; skills.md:23 enumerates the bundled set (`/doctor`, `/code-review`, `/batch`, `/debug`, `/loop`, `/claude-api`) and none applies. Control arm for the negative: the same table search DOES surface the three native features this audit used against other members of this unit (skills-2-6, skills-2-8), so the probe discriminates rather than returning a blanket 'nothing found'."
rationale = "Required justification: these are repo-specific mechanics with no native counterpart at the host's versions, and each already satisfies the three-layer contract (skill carries judgement → mise task is the seam → python module holds the logic), so there is no hand-carried procedure to lift out. Their descriptions also clear the truncation axis with room — longest is lock-image at 685 chars against the 1,536-char cap (skills.md:337) — so no keyword the matcher needs is being silently cut. The only changes they warrant are the activation-scoping in skills-2-10 and pr-workflow's blocked wait step in skills-2-8."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-3-1"
unit = "skills-3"
kind = "skills"
disposition = "refactor"
confidence = 0.85
custom_thing = "`resume` skill: a hand-rolled cross-surface resume protocol (fetch branch -> read tracked docs/handoffs/session-*.md -> restate plan), justified by the explicit premise that no native cross-device resume exists."
evidence = ".claude/skills/resume/SKILL.md:11-12 — \"Reconstructs working context on a fresh surface from the tracked handoff on the branch (Claude Code has no built-in cross-device session resume — the branch is the only carrier)\"; steps 1-2 at :17-35 hand-roll `git fetch/checkout/pull` + newest-handoff selection. The same false premise is duplicated in the sibling sending half, .claude/skills/handoff/SKILL.md:9-11 (\"there is no built-in cross-device resume (confirmed against the official docs)\")."
native_alternative = "Claude Code teleport + Remote Control + Desktop \"Continue in\". `claude --teleport` / `claude --teleport <session-id>` pulls a cloud session (conversation history AND its branch) into the terminal; `/teleport` or `/tp` does it inside a running session; `/tasks` then `t` teleports into a background session; the Desktop app's \"Continue in\" menu pushes a local session to the web; `/remote-control` drives an in-progress local session from another device."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/claude-code-on-the-web.md:174 (`--teleport` picker / direct id), :175 (`/teleport`, `/tp`), :176 (`/tasks` press `t`), :180 (\"Claude verifies you're in the correct repository, fetches and checks out the branch from the cloud session, and loads the full conversation history into your terminal\"), :182 (`--teleport` vs `--resume` distinction), :63 (\"From the CLI, session handoff is one-way ... The Desktop app provides a Continue in menu that can send a local session to the web\"); .../remote-control.md:144 (\"### Connect from another device\"), :284."
rationale = "Native now covers the web<->terminal direction end-to-end, including the branch checkout the skill hand-rolls, so the skill's stated justification is false at the host's version (2.1.266; teleport requires >=2.1.223 per claude-code-on-the-web.md:178). It is not fully retired: the CLI cannot push a terminal session to the web (claude-code-on-the-web.md:63), teleport requires claude.ai auth and is unavailable under Zero Data Retention (:55, :197), and terminal-on-machine-A -> terminal-on-machine-B is still uncovered. Refactor to route to `/teleport` / `/tp` / `/remote-control` first and keep the branch-carrier path only for those residual cases; correct the premise sentence in both this skill and .claude/skills/handoff/SKILL.md."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-3-2"
unit = "skills-3"
kind = "skills"
disposition = "retire"
confidence = 0.80
custom_thing = "`tmux-extended-keys` skill: 135 lines carrying the tmux CSI-u recipe for Shift+Enter, plus the claim that Claude Code's `/terminal-setup` is \"misdirection\" / \"a red herring\" inside tmux."
evidence = ".claude/skills/tmux-extended-keys/SKILL.md:54-59 (the two-line config: `set -s extended-keys on`, `set -as terminal-features 'xterm*:extkeys'`), :33-36 (\"`/terminal-setup` ... refuses to run under tmux and points you to the source terminal — but that's misdirection\"), :98-100 (\"`/terminal-setup` is a red herring inside tmux\")."
native_alternative = "The vendor now documents this exact procedure in the Claude Code terminal-config page, under a `## Configure tmux` heading, with a superset of the skill's config block and a corrected account of `/terminal-setup`."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/terminal-config.md:116 (\"## Configure tmux\"), :118-126 (the block `set -g allow-passthrough on` / `set -s extended-keys on` / `set -as terminal-features 'xterm*:extkeys'` + `tmux source-file ~/.tmux.conf`, and the explanation of each line), :33 (\"Run `/terminal-setup` directly in the host terminal rather than inside tmux or screen, since it needs to write to the host terminal's configuration\"), :42 (\"If you are running inside tmux, Shift+Enter also requires the tmux configuration below even when the outer terminal supports it\")."
rationale = "The documented procedure is a strict superset of the skill: it adds `set -g allow-passthrough on`, which the skill omits entirely and without which desktop notifications and the progress bar never reach the outer terminal (terminal-config.md:126). The skill is also wrong on the point it makes loudest — the doc says `/terminal-setup` is not misdirection but a host-terminal step that tmux config is *additional* to (:42). A 135-line skill that is both redundant with and less complete than the vendor page is net-negative; retire it, keeping at most a one-line pointer to terminal-config.md#configure-tmux wherever the repo's tmux config lives."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "skills-3-3"
unit = "skills-3"
kind = "skills"
disposition = "refactor"
confidence = 0.70
custom_thing = "`ssh-ignoreunknown-cross-platform` skill: 103 lines on tolerating macOS-only ssh_config directives inside a Linux container, premised on the host `~/.ssh/config` being bind-mounted into the devcontainer."
evidence = ".claude/skills/ssh-ignoreunknown-cross-platform/SKILL.md:13-14 (\"You bind-mount the host `~/.ssh/config` into a Linux container\"), :6 (`applicability: any time the host ~/.ssh/config is bind-mounted into a Linux container`), :44-61 (the `IgnoreUnknown` fix). The repo does not bind-mount it: .devcontainer/devcontainer.json:122-129 lists exactly three mounts (`~/.local/state/dotfiles`, `/run/host-services/ssh-auth.sock`, the arch-scoped home volume) and no `.ssh` source; :237 `postCreateCommand` builds `~/.ssh` fresh (`install -d -m 700 ~/.ssh` + authorized_keys + `ssh-keyscan` known_hosts). Control-armed: `git grep -c IgnoreUnknown -- .claude/skills .agents/skills` returns hits in both trees (rc=0), so the zero hits elsewhere in the repo is a real absence — the fix this skill teaches is applied nowhere here."
native_alternative = "OpenSSH's own `IgnoreUnknown` is the built-in the skill correctly names (skill:87, `man ssh_config`), so there is no *harness* feature to adopt; the repo-side alternative is the R2 design already shipped — Docker Desktop's native `/run/host-services/ssh-auth.sock` magic socket, which forwards the agent without sharing the host ssh_config at all."
alternative_evidence = ".devcontainer/devcontainer.json:38-42 (\"(R2) OUTBOUND agent: `ssh -T git@github.com` inside the container reaches the host's launchd SSH agent via Docker Desktop's native `/run/host-services/ssh-auth.sock` magic socket\"), :124 (the socket bind), :221 (`SSH_AUTH_SOCK`); AGENTS.md \"Docker Runtimes\" section (Docker Desktop supported since 2026-04-09, exposes the socket natively)."
rationale = "No Claude Code, codex or pinned-tool feature supersedes `IgnoreUnknown` — the content is accurate OpenSSH knowledge. What is stale is its applicability: the repo's devcontainer stopped bind-mounting `~/.ssh/config` when it moved to the Docker Desktop magic socket, so the motivating failure cannot occur here, and the skill sits in the always-matchable skill index for a condition this repo does not have. Refactor: move the content to a tracked reference (docs/rules-evidence/ or .devcontainer/AGENTS.md's SSH section) and drop the SKILL.md, or narrow the description to say it applies only if a host ssh_config mount is ever reintroduced. Do not simply delete the knowledge — it is a correct built-in-first case study cited by use-tool-builtins.md."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-3-4"
unit = "skills-3"
kind = "skills"
disposition = "refactor"
confidence = 0.85
custom_thing = "`uv-project-vs-directory-expertise` step 3 and its recognition pattern instruct clearing hk's pkl config cache by hand after editing hk.pkl."
evidence = ".claude/skills/uv-project-vs-directory-expertise/SKILL.md:23 (\"3. After editing hk.pkl, clear the cache: `rm -rf ~/Library/Caches/hk/configs/`\"), :19 (\"hk config changes appear to be ignored (stale cache)\"), :12 (\"Debugging was further complicated by hk's pkl config cache at `~/Library/Caches/hk/configs/` which showed stale commands\")."
native_alternative = "hk now keys its resolved-config cache by content, so an edited hk.pkl invalidates itself and no manual clearing is needed."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/hk/CHANGELOG.md:367 (\"**(config)** share resolved config cache by content by @jdx in #1044\") under the release header at :346 (\"## [1.50.0](https://github.com/jdx/hk/compare/v1.49.0..v1.50.0) - 2026-07-06\"); repo pins hk 1.57.0 (mise.toml / .config/mise/conf.d/shared.toml), i.e. seven minors past the fix. The repo already states this: root AGENTS.md \"Split hk Architecture\" (\"The pkl-eval cache is content-hashed since hk 1.47 — no manual cache clearing after edits\") and .claude/rules/long-running-command-hangs.md rule 5 (\"the old 'clear the pkl config cache' guidance is retired\")."
rationale = "The skill's core insight is still correct and native-grounded — `--directory` changes the working directory while `--project` does not (uv corpus: docs/concepts/projects/init.md:23, \"The working directory can be modified with the `--directory` option\") — so this is not a retire. But two of its four Approach steps and one recognition bullet teach a cache-clearing ritual the tool made obsolete, and the repo's own eager rules already contradict it, so a session that loads this skill gets advice its rules forbid. Delete step 3 and the stale-cache recognition bullet, and trim the #43 narrative's cache aside to the `--project`/`--directory` fact."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-3-5"
unit = "skills-3"
kind = "skills"
disposition = "refactor"
confidence = 0.75
custom_thing = "`research-with-verification-gap-fill`: a hand-written 4-phase parallel-research protocol (decompose -> fan out N scientists -> one opus verifier -> persist), still frontmattered as a draft, with its own model-routing table and persist paths."
evidence = ".claude/skills/research-with-verification-gap-fill/SKILL.md:4-6 (`type: skill-draft`, `status: draft`, `inspired-by: oh-my-claudecode:sciomc`), :12-16 (Status: \"Draft ... until validated across at least one more session\"), :45-94 (the four phases), :89 (persist to `docs/research/runs/<session-id>/`), :115-120 (See also: `oh-my-claudecode:sciomc`, `.agent/wiki/sciomc-verification-stage.md`), :123-131 (promotion criteria, never met since 2026-04-07). Two of its three see-also targets are dead: `.agent/wiki/` does not exist (`ls -d .agent/wiki` -> No such file or directory; control arm in the same invocation: `ls -d .claude/skills/handoff` returned a real directory), and the oh-my-claudecode plugin is recorded as disabled with 0 invocations across 941 transcripts (.claude/rules/notepad-enforcement.md, \"The notepad is a file, not a tool\")."
native_alternative = "Claude Code agent teams for the parallel-research-plus-cross-challenge shape, subagents for the plain fan-out, and the repo's own `adversarial-review` skill for bounding the verifier's brief; `docs/research/kb/` replaced the persist path."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/agent-teams.md:25 (\"**Research and review**: multiple teammates can investigate different aspects of a problem simultaneously, then share and challenge each other's findings\"), :32 (\"### Compare with subagents\"), :10 (experimental, gated on `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`), :16 (\"Before you set up a team, check whether a lighter option does the job\"). Repo side: .claude/skills/adversarial-review/SKILL.md:1-6 and its thesis at :13-15; .claude/rules/agent-report-persistence.md rule 1 (\"**ONE path.** `docs/research/kb/` is tracked and survives a fresh clone; the old `docs/research/runs/<topic>/agents/` does not\")."
rationale = "Native agent teams cover the fan-out-and-cross-challenge half of this protocol, but they are experimental and off by default (agent-teams.md:10), so this is not a clean retire. What is definitely wrong is the skill's current state: it has sat at `status: draft` for five months with unmet promotion criteria, it cites a disabled plugin and a directory that does not exist, and its Phase 4 persist path was superseded by agent-report-persistence.md's single `docs/research/kb/` path — a session following it verbatim would write research where nothing survives a clone. Refactor: fix the persist path, drop the two dead references, express the fan-out in terms of subagents/agent teams, and either promote it or fold the surviving judgement (the mandatory verification stage) into `adversarial-review`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-3-6"
unit = "skills-3"
kind = "skills"
disposition = "refactor"
confidence = 0.60
custom_thing = "`session-handoff` (281 lines) + `session-resume` (106 lines): a hand-rolled `/clear` survival protocol — snapshot state, sync docs, hand-write a durable memory file plus a gitignored handoff, then emit a resume prompt that a second skill reconciles against live repo state."
evidence = ".claude/skills/session-handoff/SKILL.md:10-11 (\"Run this **before** `/clear` to ... persist recovery context that survives the clear\"), :150-158 (step 3a: hand-write a `project_*`/`feedback_*` file under `~/.claude/projects/-Users-rmanaloto-.../memory/` and add a `MEMORY.md` pointer), :160-169 (step 3b local handoff), :233-261 (step 6 resume prompt); .claude/skills/session-resume/SKILL.md:9-10, :22-40."
native_alternative = "Two native mechanisms cover parts of this: (a) `/clear` does not destroy the conversation — sessions are saved continuously and reopenable with `claude --continue` / `claude --resume` / `/resume`, including a summary dialog for large stale sessions; (b) auto memory writes and re-reads the same `~/.claude/projects/<project>/memory/` files by itself, and Claude Code measures `MEMORY.md` against the 200-line/25KB read limits and errors when it is over."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sessions.md:15 (\"Sessions are saved continuously to local transcript files as you work, so you can return to one after exiting or running `/clear`\"), :13-24 (the `--continue` / `--resume` / `--resume <name>` / `--from-pr` / `/resume` entry-point table), :75 (\"### Resume from a summary\"); .../memory.md:12 (\"**Auto memory**: notes Claude writes itself based on your corrections and preferences\"), :369 (per-project memory directory path), :403 (Claude Code measures MEMORY.md against the 200-line and 25KB read limits and returns an error when over), :411 (\"Claude reads and writes memory files during your session\")."
rationale = "The skill's framing — that context must be persisted because `/clear` destroys it — is now only half true: the pre-clear session remains resumable by id or name (sessions.md:15), which is a cheaper recovery route than reconstructing from a paraphrased handoff, and the handoff itself warns that paraphrase gets details wrong (SKILL.md:220-231). Step 3a likewise hand-rolls what auto memory does natively, including the size discipline Claude Code now enforces itself. What native does NOT do, and what justifies keeping the skill: the doc-sync sweep (step 2), the gate-verification block (step 4), the agent-report coverage audit (step 3c), and the self-triggered-run cold-review gate (:203-218). Refactor to lean on `--resume`/auto memory for continuity and keep the skill for the repo-specific discipline; record the session id in the handoff so the next session can reopen rather than only re-read."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-3-7"
unit = "skills-3"
kind = "skills"
disposition = "refactor"
confidence = 0.80
custom_thing = "A second, tracked copy of every skill in this unit under `.agents/skills/**`, produced by a `claude` -> `Codex` string substitution rather than a verbatim copy."
evidence = "`diff .claude/skills/resume/SKILL.md .agents/skills/resume/SKILL.md` yields, among others: line 11 \"Claude Code has no built-in cross-device session resume\" -> \"Codex has no built-in cross-device session resume\"; line 55 \"(`.claude/rules/verify-before-advancing.md`)\" -> \"(`.Codex/rules/verify-before-advancing.md`)\"; line 72 \"`.claude/skills/handoff/SKILL.md`\" -> \"`.Codex/skills/handoff/SKILL.md`\". `.Codex/` does not exist in the repo. Measured across this unit: 7 of 11 members differ between the two tracked trees (reap, session-review, token-check, uv-project-vs-directory-expertise are byte-identical; research-with-verification-gap-fill, resume, session-handoff, session-resume, ssh-ignoreunknown-cross-platform, tmux-extended-keys, tool-currency-check DIFFER). Both trees are tracked (`git ls-files .claude/skills` -> 44 files; `git grep -c IgnoreUnknown -- .claude/skills .agents/skills` -> hits in both, rc=0)."
native_alternative = "Codex's documented repo skill root is `$REPO_ROOT/.agents/skills` read as-is — nothing in Codex asks for or benefits from rewriting `.claude` path references, and Codex ships an import flow for moving Claude skill folders across rather than transforming them."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/build-skills.md:135 (\"For repositories, Codex scans `.agents/skills` in every directory from your current working directory up to the repository root\"), :139-141 (the REPO/USER discovery table, incl. `$REPO_ROOT/.agents/skills`), :45 (\"A skill is a directory with a `SKILL.md` file plus optional scripts and references\"); .../codex/import.md:5 and :54 (import flow brings skills across); .../codex/app-server.md:1648 (\"Copy skill folders from /Users/me/.claude/skills to /Users/me/.agents/skills\") — a copy, not a rewrite."
rationale = "The mirror itself is correct and required — Codex will not see `.claude/skills` (build-skills.md:135). The transform is the defect: substituting `claude` -> `Codex` rewrites live repo paths into `.Codex/rules/...` (which resolve to nothing, so a Codex lane following the skill finds no rule file) and rewrites a vendor fact into a claim about the wrong vendor. Refactor the mirroring step to a verbatim copy or symlink, exactly as Codex's own copy operation does, and add a check that the two trees agree byte-for-byte; the 7-of-11 divergence measured here is the drift that check would have caught."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-3-8"
unit = "skills-3"
kind = "skills"
disposition = "keep"
confidence = 0.80
custom_thing = "The four task-backed skills in this unit — `reap`, `session-review`, `token-check`, `tool-currency-check` — each a judgement layer over a mise task over a python library."
evidence = ".claude/skills/reap/SKILL.md:10-13 (all invocations are `mise run reap -- ...`) backed by mise.toml:1538 `[tasks.reap]`; .claude/skills/token-check/SKILL.md:10-11 + :16-17 (\"`python/src/dotfiles_setup/token_audit.py` holds both scopes; the task is a thin caller\") backed by mise.toml:1318 `[tasks.token-check]`; .claude/skills/session-review/SKILL.md:10-21 + :30 (`python/src/dotfiles_setup/session_review.py`) + :184-187 (\"Use only the durable invocation chain shown above: skill -> mise task -> `uv run --project python` -> the Python library\") backed by mise.toml:1478/:1495/:1506; .claude/skills/tool-currency-check/SKILL.md:41-44 backed by mise.toml:586 `[tasks.tool-currency]` (which drives the shared `kb_setup.currency` engine). Description lengths measured for all 11 members: max 645 chars (`reap`), all far under the 1,536-char truncation cliff; sizes max 281 lines / 15,973 B (`session-handoff`), all under the `skill` class budget of 500 lines / 32,000 B in .claude/rules/md-size-budgets.md."
native_alternative = "None found. Searched the Claude Code corpus and the 2.1.258-2.1.266 gap digest for a native process reaper, a transcript-mining requirement/automation-candidate review, a verification-contract token uniqueness pre-flight, and a pinned-tool currency/native-supersession auditor; the nearest native surfaces are `/tasks` and `TaskStop` (harness-owned tasks only, not OS processes), `/skill-doctor` (skill context cost, not tool pins) and Renovate (version bumps, not custom-code retirement)."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md (15 new capabilities enumerated; none is a process reaper, transcript miner, contract-token checker or tool-currency auditor — the only skills-adjacent entry is \"New `/skill-doctor` command shows which loaded skills go unused and their context cost, to help prune skills\", 2.1.261); .agent/kb/raw/claude-code-llms-index.digest.md (\"no page-level gap found\", control-armed with an injected fake slug); .agent/kb/raw/mise-releases-since-pin.digest.md:49-53 (`mise skills ls`/`skills sync` links a *tool's* vendored Agent Skills into `.claude/skills` — it does not author or supersede repo-authored skills)."
rationale = "Required justification: all four are repo-specific judgement layers over repo-specific python libraries, and each names a cost native tooling cannot address — `reap` guards the ancestor chain of the session's own shell (SKILL.md:32-36, where a hand-rolled `pkill -f` would kill the shell mid-command); `token-check` asks the `contract_token_uniqueness` predicate before the contract exists, which the gate structurally cannot (SKILL.md:16-17); `session-review` mines Claude and Codex transcripts for typed requirement coverage with a fail-closed state machine; `tool-currency-check` carries the one thing the shared currency engine explicitly does not automate — whether a piece of custom code is now superseded by a native tool feature (SKILL.md:36-38). All conform to the three-layer stack required by agent-artifact-conventions.md rule 6, and no description in this unit is anywhere near the 1,536-char truncation limit."
probe = "claude -p '/skill-doctor' 2>&1 | head -60   # then, per member: grep the report for reap / session-review / token-check / tool-currency-check to see which of the 11 have gone unused and what each costs in context"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "skills-3-9"
unit = "skills-3"
kind = "skills"
disposition = "refactor"
confidence = 0.60
custom_thing = "`tool-currency-check` Guardrails: a hand-maintained account of which mise backends are acceptable, naming only ubi (deprecated), github: and vfox."
evidence = ".claude/skills/tool-currency-check/SKILL.md:115-119 — \"**Never introduce a `ubi:` pin.** The ubi backend is deprecated (warns since mise 2026.4.0, REMOVED in 2027.1.0); its niche (prebuilt GitHub-release binary) is the `github:` backend — vfox is the *plugin-system* recommendation, not a ubi replacement.\" No mention of `packslip:`."
native_alternative = "mise's `packslip:` backend went GA in v2026.9.2 — installs from a vendor-signed release manifest with sigstore verification, signer pinning recorded in `mise.lock` / `pins.toml`, a `packslip.stampers` trust setting, and host-requirement checks; v2026.9.3 extended it to vfox plugin archives."
alternative_evidence = ".agent/kb/raw/mise-releases-since-pin.digest.md:32-37 (\"**`packslip:` backend is now GA (was experimental)** ... Shipped v2026.9.2\"), :38-43 (signer pinning, `mise packslip pins` / `forget`), :44-48 (`packslip.stampers`, host-requirement checks), and the v2026.9.3 section (\"vfox plugins installable from signed packslip archives\"). Anchors in the verbatim fetch .agent/kb/raw/mise-releases-since-pin.md (`\"tag_name\":\"v2026.9.2\"`, `\"tag_name\":\"v2026.9.3\"`)."
rationale = "The skill's whole purpose is to keep the repo current with tool features, so a stale backend table inside it is self-defeating: an agent following step 5's report template would propose a `github:` pin where a signed `packslip:` pin is now the stronger option, and would not know signer pinning writes into `mise.lock`. Add a `packslip:` row to the Guardrails with its verification properties, and keep the ubi ban as-is. Flagged at moderate confidence because the digest is a release-notes summary, not the mise backend docs page the skill's own guardrail says to verify against."
probe = "curl -s https://mise.jdx.dev/dev-tools/backends/packslip.html | head -80   # confirm GA status and the pins.toml/mise.lock signer-pinning behaviour against the live backend doc before editing the guardrail"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-1-1"
unit = "suites-1"
kind = "suites"
disposition = "refactor"
confidence = 0.70
custom_thing = "build.image-architecture-integrity binds three pytest tests by SUBSTRING of their def lines (`def test_every_stage_that_reads_targetarch_also_declares_it(`, `def test_the_image_config_locks_every_published_architecture(`, `def test_a_required_platform_absent_from_the_lock_is_still_locked(`) inside tests/test_image_arch.py, tests/test_platform_target.py, tests/test_image_lock.py."
evidence = "python/verification/suites.toml:377-419 (per_path_tokens entries for the three tests/ files). The binding is comment-blind: python/src/dotfiles_setup/verify.py:563-586 `_handle_require_tokens` never passes a strip_comments flag and `require_tokens` (verify.py:184-215) has no such parameter, while the sibling `_handle_forbid_tokens` (verify.py:552-560) explicitly passes `strip_comments=entry.get(\"strip_comments\", True)`. So a commented-out or narrated `def test_...(` line satisfies this contract after the real test is deleted."
native_alternative = "pytest node-id selection: naming the test as `tests/test_image_arch.py::test_every_stage_that_reads_targetarch_also_declares_it` makes pytest itself fail collection with `ERROR: not found` and rc=4 when the test is gone or renamed. It reads the collected AST, not the file text, so a comment cannot satisfy it."
alternative_evidence = "Live control-armed probe (this session, saved to the session scratchpad pytest-probe.log): present arm -> `1 test collected in 0.02s`, rc_present=0; absent arm `tests/test_image_arch.py::test_this_name_does_not_exist_qqzz` -> `ERROR: not found: .../tests/test_image_arch.py::test_this_name_does_not_exist_qqzz`, `no tests collected`, rc_absent=4. The probe therefore discriminates in both directions. Corpus: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/pytest (pinned pytest available via `uv run --project python pytest`)."
rationale = "The Dockerfile / mise-system.toml half of this contract has no native substitute and must stay as require_tokens. Only the three tests/ per-path entries are supersedable: they assert 'this test still exists', which pytest expresses natively and comment-proof. Refactor = move those three entries to a check_type that runs the three node ids (or add them to the CI pytest invocation as explicit node ids), keeping the rest of the contract intact."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "suites-1-2"
unit = "suites-1"
kind = "suites"
disposition = "refactor"
confidence = 0.70
custom_thing = "build.no-warning-suppression forbids MISE_QUIET / MISE_LOG_LEVEL=warn / MISE_LOG_LEVEL=error in exactly two files: `.devcontainer/Dockerfile` and `.github/workflows/ci.yml`."
evidence = "python/verification/suites.toml:86-93 (`paths = [\".devcontainer/Dockerfile\", \".github/workflows/ci.yml\"]`). But ci.yml is a thin caller — AGENTS.md describes `ci.yml (thin caller) -> ... -> reusable build-publish.yml`, and the image build with its own mise surface lives in `.github/workflows/build-publish.yml`: that file carries 25 `mise` references and its own MISE_ env block at `.github/workflows/build-publish.yml:68-69` (`MISE_LOG_FILE`, `MISE_LOG_FILE_LEVEL`), and is NOT in this contract's paths. A `MISE_QUIET` added there passes. Control arm for the scan: MISE_QUIET count is 0 in all eight workflows today, while `2>/dev/null` returns 2 in build-publish.yml and 1 in ci.yml with the same command shape, so the 0 is a real absence, not a broken grep."
native_alternative = "ast-grep (pinned `github:ast-grep/ast-grep` 0.45.3) has first-class YAML support, so a rule matching workflow `env:` mappings structurally can be run glob-lessly across `.github/workflows/**` — the same shape hk already uses for `no_platform_literals` (a glob-less wrapper that enumerates the tracked tree rather than a hand-predicted path list)."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/lib.rs:404 `Yaml => &[\"yaml\", \"yml\"]` (plus lib.rs:44, :253, :287). hk.pkl:260-270 documents the glob-less rationale for `no_platform_literals`: \"Glob-less on purpose — a literal can arrive in any tracked config or source file, so the scan enumerates the tracked tree rather than a set of paths someone predicted.\""
rationale = "The contract is not superseded — nothing native forbids MISE_QUIET — but its path list encodes a CI topology that has since changed (ci.yml became a thin caller). Minimum fix is adding build-publish.yml; the durable fix is the glob-less shape the repo already uses for the sibling platform-literal rule, which is what makes the path list stop being a thing someone must remember to update."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-1-3"
unit = "suites-1"
kind = "suites"
disposition = "refactor"
confidence = 0.45
custom_thing = "build.path-includes-mise-shims declares three `paths` (.devcontainer/Dockerfile.host-user, .devcontainer/devcontainer.json, home/.chezmoi.toml.tmpl) but binds per_path_tokens for only one of them."
evidence = "python/verification/suites.toml:265-272. Measured occurrences of `mise/shims`: `.devcontainer/Dockerfile.host-user` = 1 (the bound site), `.devcontainer/devcontainer.json` = 0, `home/.chezmoi.toml.tmpl` = 0 — the non-zero arm is the control that proves the grep works. Because `_handle_require_tokens` calls `_resolve_paths` after the per-path pass (verify.py:582-587) and the bare `tokens` list is empty, the two unbound paths reduce to pure file-existence assertions with nothing shims-related left in them."
native_alternative = "None claimed as a tool feature. The native-shaped fix is the handler's own `per_path_tokens` semantics: declare only the paths the contract actually binds, so `dotfiles-setup token-audit` (hk step `contract_token_uniqueness`, hk.pkl:354-357) sees the whole declared surface. A separate existence-only contract would state the residual intent honestly."
alternative_evidence = "python/src/dotfiles_setup/verify.py:563-587 (`per_path` loop, then `_resolve_paths`); hk.pkl:345-357 (`hk_audit`, `contract_token_uniqueness`); suites.toml:33-42 documents that `find_unaudited` refuses the single-path bare form precisely so declarations cannot assert less than they look like."
rationale = "Low confidence deliberately: the contract's own description (suites.toml:267) records that a prior review REJECTED deleting these paths and says the scope is left visible on purpose, so this is a re-litigation flag rather than a defect. The audit-relevant fact is that the declaration now names two files with zero shims content, which is exactly the shape a future reader mistakes for coverage."
probe = "uv run --project python dotfiles-setup token-audit && grep -c 'mise/shims' .devcontainer/devcontainer.json home/.chezmoi.toml.tmpl .devcontainer/Dockerfile.host-user"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "suites-1-4"
unit = "suites-1"
kind = "suites"
disposition = "keep"
confidence = 0.75
custom_thing = "The Dockerfile ENV/mount-shape contracts: build.no-custom-mise-data-dir, build.mise-install-path-system, build.no-custom-mise-cache-dir, build.home-not-root, build.no-stderr-suppression, build.cache-mount-sharing-locked, build.no-mise-data-cache-mount, build.buildkit-syntax, build.openssh-not-in-base, build.uv-link-mode-not-global, build.base-image-arg, build.apt-via-mise-bootstrap, build.no-custom-cargo-home, build.no-custom-rustup-home, build.no-custom-gopath, build.no-custom-npm-prefix, build.home-opt-home, build.no-xdg-in-tools-stage, build.gcc-latest-deb-checksum, build.gcc-latest-reflection, build.clang-p2996-reflection, build.no-retired-indirection-args — all text-level (token/regex) assertions over `.devcontainer/Dockerfile`."
evidence = "python/verification/suites.toml:50-138, :215-263, :274-376, :420-479. Residual fragility is real but small: `build.no-custom-cargo-home` / `build.no-custom-rustup-home` rely on a LEADING SPACE to avoid matching MISE_CARGO_HOME/MISE_RUSTUP_HOME (suites.toml:280-284, :291-295), which a tab-indented continuation line would slip past."
native_alternative = "None available. ast-grep 0.45.3 — the repo's only structural matcher — has NO Dockerfile language, so these cannot be lifted to an AST rule; hadolint (pinned 2.15.1, already wired at hk.pkl:131-133 over both Dockerfiles) exposes only rule ignores / trusted-registry / label-schema configuration, not a custom-rule DSL that could express 'ENV HOME must not be /root' or 'every cache mount carries sharing=locked'."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/language/src/lib.rs:395-405 — the extension map ends at `Yaml => &[\"yaml\", \"yml\"]` with no Dockerfile entry; case-insensitive grep for 'dockerfile' in that file returns 0 while the control arm 'Yaml' returns 5 hits (lib.rs:44, 253, 287, 296, 404) with the same command shape, so the absence is real. hk.pkl:129-133 shows hadolint is already the only Dockerfile linter in the gate."
rationale = "Required justification: there is no native mechanism at the repo's pins that can assert Dockerfile-level policy. hadolint enforces its own fixed DL/SC rule set and cannot be taught these; ast-grep cannot parse Dockerfiles at all. Each of these contracts binds the ENV/RUN/mount line that IS the mechanism (an `ENV X=Y` line is the call site, not a definition prose can carry), so the substring form is as tight as the substrate allows. Keep as-is; the leading-space anchors are the one place worth a comment rather than a rewrite."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-1-5"
unit = "suites-1"
kind = "suites"
disposition = "refactor"
confidence = 0.50
custom_thing = "The devcontainer.json contracts — build.amd64-platform-wired, build.ssh-r1-inbound-wired, build.ssh-r2-outbound-native-docker-desktop-wired, build.devcontainer-names-arch-scoped — assert wiring by matching raw substrings against a JSONC file that narrates its own mounts, ports and env in comment blocks."
evidence = "python/verification/suites.toml:141-205. The narration hazard is live, not hypothetical: `.devcontainer/devcontainer.json:40` is a COMMENT containing `/run/host-services/ssh-auth.sock` and `:94` is a COMMENT containing `DOTFILES_PLATFORM`, i.e. the same identifiers the contracts bind. Today every bound token still matches exactly once (measured: each of the 8 tokens across the four contracts returns count 1), so there is no live hole — but require_tokens does not strip comments (verify.py:184-215, :563-587, contrast `_handle_forbid_tokens` at verify.py:552-560 which does), so the anchors are the only thing standing between a deleted mount and a green contract. suites.toml:24-31 records that this already happened at repo scale: of 33 rebindings in #397, 11 were held up by a COMMENT alone."
native_alternative = "check-jsonschema 0.38.0 (pinned `pipx:check-jsonschema`) is ALREADY wired as layer 3 of the devcontainer.json gate. A second, local overlay schema — `const` on `name` / `containerEnv.SSH_AUTH_SOCK`, `contains` on `mounts` / `runArgs` / `appPort` / `features` — would bind these four contracts positionally instead of by substring, and a JSON Schema cannot be satisfied by a comment."
alternative_evidence = "scripts/validate-devcontainer-json.sh:60-71 (`check-jsonschema --schemafile \"${SCHEMA_URL}\" \"${tmp}\"`, described in the header as 'Layer 3/3 ... Catches unknown fields, wrong types, missing required fields'); wired at hk.pkl:149-152 (`devcontainer_json_validate`). The repo already vendors local schemas under `schemas/` (mise.json, ruff.json, typos.json, sources.toml), so the overlay-schema pattern exists here."
rationale = "Native covers part of it and is already installed and wired, which is the strongest form of 'refactor'. The caveat that keeps confidence at 0.5: layer 2 of the existing script pipes the config through `devcontainer read-configuration`, which RESOLVES `${localEnv:...}` — and resolution is exactly what these contracts must NOT see (they assert the interpolation, not a literal). So the overlay schema has to run against the raw JSONC, which needs check-jsonschema to accept comments/trailing commas on this file. That is the open question the probe settles."
probe = "printf '{\\n  // c\\n  \"name\": \"x\",\\n}\\n' > /tmp/jsonc-probe.json && printf '{\"type\":\"object\",\"properties\":{\"name\":{\"const\":\"x\"}},\"required\":[\"name\"]}' > /tmp/jsonc-schema.json && uv run --project python -c \"pass\"; check-jsonschema --force-filetype json5 --schemafile /tmp/jsonc-schema.json /tmp/jsonc-probe.json > /tmp/cjs.log 2>&1; echo \"rc_valid=$?\" >> /tmp/cjs.log; printf '{\\n  // c\\n  \"name\": \"WRONG\",\\n}\\n' > /tmp/jsonc-probe-bad.json; check-jsonschema --force-filetype json5 --schemafile /tmp/jsonc-schema.json /tmp/jsonc-probe-bad.json >> /tmp/cjs.log 2>&1; echo \"rc_invalid=$?\" >> /tmp/cjs.log; cat /tmp/cjs.log"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 3
survived = false
schedule = ""

[[finding]]
id = "suites-1-6"
unit = "suites-1"
kind = "suites"
disposition = "refactor"
confidence = 0.60
custom_thing = "build.devcontainer-names-not-recomposed is a regex_forbid whose whole pattern is one literal composition shape: `dotfiles-\\$\\{localWorkspaceFolderBasename\\}`."
evidence = "python/verification/suites.toml:206-214. The pattern matches only the exact prefix `dotfiles-` followed by that one substitution. Any other recomposition of an arch-scoped resource name — `${localWorkspaceFolderBasename}-dev`, a hand-built volume name, a port composed from a hash — passes untouched, while the contract's own description (suites.toml:209) states the regression it exists to prevent is 'a name assembled from ${localWorkspaceFolderBasename} and a hash [that] simply omits the architecture', i.e. a class, not this one string."
native_alternative = "The same local check-jsonschema overlay schema as suites-1-5: a JSON Schema `pattern`/`not` constraint applied POSITIONALLY to the name/mounts/appPort members says 'no member of this set may contain ${localWorkspaceFolderBasename}', which covers the class rather than one spelling, and cannot be dodged by a new key."
alternative_evidence = "scripts/validate-devcontainer-json.sh:60-71 and hk.pkl:149-152 (check-jsonschema already the third layer of the devcontainer.json gate); schemas/ already holds four locally vendored schema files, so the pattern is established in-repo."
rationale = "This is the classic 'fix the instance, not the class' shape the repo has been bitten by three times (feedback_fix_the_class_not_the_instance): the regex closes exactly the input shape the original defect used. It is not superseded — nothing native knows about devcontainer resource naming — but a positional schema constraint expresses the intended invariant, whereas a single literal regex expresses only the last bug."
probe = "printf '%s\\n' 'control arm: the pattern must FAIL on a plausible variant' && python3 -c \"import re;p=re.compile(r'dotfiles-\\\\\\$\\\\{localWorkspaceFolderBasename\\\\}');print('variant matched:',bool(p.search('\\\"name\\\": \\\"\\${localWorkspaceFolderBasename}-amd64\\\"')));print('original matched:',bool(p.search('dotfiles-\\${localWorkspaceFolderBasename}')))\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 3
survived = false
schedule = ""

[[finding]]
id = "suites-1-7"
unit = "suites-1"
kind = "suites"
disposition = "refactor"
confidence = 0.55
custom_thing = "build.manpage-restore-sed-syntax pins the exact TEXT of a shell command — `sed -i '/^path-exclude=\\/usr\\/share\\/man\\/\\*$/d' /etc/dpkg/dpkg.cfg.d/excludes` — as the contract for 'manpages are restored in the image'."
evidence = "python/verification/suites.toml:332-340; the bound line is `.devcontainer/Dockerfile:101`. The contract asserts a spelling, not an outcome: re-quoting the sed expression, switching to `sed -Ei`, or replacing it with an equivalent `grep -v` rewrite all FAIL the contract while preserving the effect, and conversely a sed that silently matches nothing (an upstream change to the excludes file's line format) PASSES it. It also duplicates what the build itself is in the best position to know."
native_alternative = "Docker's own layer semantics: a post-condition in the SAME RUN — the image asserts the exclusion is gone after removing it, and a non-zero rc fails the build. This is the repo's established `feedback_build_time_self_check` convention ('no-op tools need a post-condition test in the same RUN'), and it carries its own control arm on every build, which is what .claude/rules/probes-need-a-control-arm.md rule 9 asks for ('assert the capability — never sniff for a symptom of its absence')."
alternative_evidence = ".claude/rules/probes-need-a-control-arm.md (rule 9, 'When you must BUILD a check, assert the capability — never sniff for a symptom of its absence ... feed the tool an input it must fail on and require the failure'); memory index entry `feedback_build_time_self_check` ('no-op tools need a post-condition `test` in the same RUN'); the same pattern is already used in this Dockerfile for the gcc-latest reflection smoke and the ARCH_EXEC_PROBES loop bound by build.gcc-latest-reflection / build.image-architecture-integrity (suites.toml:362-419)."
rationale = "Not superseded by a third-party tool — the alternative is a mechanism (a failing RUN) rather than a product, which is why this is refactor and not retire. The contract as written binds the most renamable thing in the line (its exact quoting) while leaving the outcome unverified in both directions; moving the assertion into the build and keeping only a thin token for the post-condition's failure message inverts that."
probe = "docker run --rm $(grep -m1 '^ARG BASE_IMAGE' .devcontainer/Dockerfile | cut -d= -f2) sh -c 'grep -c \"path-exclude=/usr/share/man\" /etc/dpkg/dpkg.cfg.d/excludes' ; echo rc=$?   # control arm: the pinned sed's target line must EXIST in the base, or the sed is already a no-op"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-1-8"
unit = "suites-1"
kind = "suites"
disposition = "keep"
confidence = 0.60
custom_thing = "The mise-config value contracts — build.gnupg-installed (`\"apt:gnupg\"`), build.npm-no-update-notifier (`npm_config_update_notifier = \"false\"`), build.rustup-skip-path-check (`RUSTUP_INIT_SKIP_PATH_CHECK = \"yes\"`), and build.image-architecture-integrity's `lockfile_platforms = [\"linux-x64\", \"linux-arm64\"]` — assert TOML key/value pairs in `.devcontainer/mise-system.toml` by substring."
evidence = "python/verification/suites.toml:235-242, :256-264, :341-349, :400-402 (the mise-system.toml per_path_tokens block of image-architecture-integrity)."
native_alternative = "None that can carry the policy. taplo 0.10.0 is pinned and wired (hk.pkl:167-170) and supports the `#:schema` directive, but the only schema available for a mise config is the VENDORED upstream one, which is machine-regenerated: `mise run schema-vendor-refresh` re-downloads `schemas/mise.json` at the pinned tag and rewrites both it and its `version` in `schemas/sources.toml`, so a locally added `const`/`contains` policy constraint would be overwritten on the next refresh. Orthogonal (non-superseding) upside worth noting: `#:schema ./schemas/mise.json` is declared on `mise.toml:1` only — `.devcontainer/mise-system.toml` carries no schema directive at all, so it currently gets no type validation."
alternative_evidence = "mise.toml:1 `#:schema ./schemas/mise.json`; grep for `#:schema` across `.devcontainer/*.toml`, `mise.toml`, `.config/mise/conf.d/*.toml` returns that one hit only (control arm: the same command shape returns the mise.toml line, so the zero for mise-system.toml is real). AGENTS.md / .claude/rules/mise-tasks-only.md: 'hand-editing `schemas/*.json` or its `version` in `schemas/sources.toml`' -> use `mise run schema-vendor-refresh`, which 're-downloads at the pinned tag and rewrites both'. hk.pkl:167-170 (`taplo` builtin, lint only)."
rationale = "Required justification: a JSON Schema over a vendored, machine-refreshed upstream schema file cannot hold repo-policy constants — any local `const` would be destroyed by the next `schema-vendor-refresh`, and a separate overlay schema for one TOML file buys type checking the contract does not need while still not expressing 'gnupg must be in the apt set'. Each of these tokens binds a declaration that IS the mechanism (mise reads the key), so substring binding is honest here. Keep."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-1-9"
unit = "suites-1"
kind = "suites"
disposition = "keep"
confidence = 0.80
custom_thing = "The remaining members of this unit that no finding above touches: build.amd64-platform-wired-mise, build.devcontainer-names-arch-scoped's mise.toml half, build.clang-p2996-ref-in-bake, build.p2996-cache-target, build.base-cache-target, build.no-retired-indirection-args — token contracts over mise.toml and docker-bake.hcl."
evidence = "python/verification/suites.toml:153-163, :191-205 (mise.toml per-path half), :433-479. Each binds an interpolation or a bake target/variable declaration that is itself the wiring: `DOCKER_DEFAULT_PLATFORM = \"{{ env.DOTFILES_PLATFORM }}\"`, `target \"p2996-cache\"` + `target   = \"p2996-export\"`, `variable \"CLANG_P2996_REF\" {` + `CLANG_P2996_REF = CLANG_P2996_REF`, and the forbid of the retired `P2996_SOURCE` / `DEVCONTAINER_BASE_REF` indirection args."
native_alternative = "None. `docker buildx bake` has no schema or lint mode that can assert 'target X must exist and be backed by stage Y'; hk's `docker_bake_check` (hk.pkl:134-140) only runs `devcontainer read-configuration`, which validates the devcontainer config, not the HCL's target graph. mise has no config-assertion surface, and the mise gap digest confirms zero releases newer than the installed 2026.9.3, so no new mechanism has shipped."
alternative_evidence = "hk.pkl:134-140 (`docker_bake_check` = `scripts/ensure-docker-up.sh && devcontainer read-configuration --workspace-folder .` — no bake target assertion); .agent/kb/raw/mise-releases-since-pin.digest.md ('the newest release in the fetched list is `v2026.9.3` ... There are ZERO releases strictly newer than the baseline'); .agent/kb/raw/hk-releases-since-pin.digest.md ('New capabilities: None. ... a small documentation-only release'). hk's own token-uniqueness audit already polices these bindings: `uv run --project python dotfiles-setup token-audit` ran clean (rc=0, no output) this session, wired at hk.pkl:354-357."
rationale = "Required justification: neither of this unit's two upstream tools shipped a capability since the pin (both gap digests report zero new capabilities), and neither docker bake nor mise exposes any declarative assertion surface a contract could delegate to. Every token here extends past the renamable identifier (`target \"p2996-cache\"` carries its quote, `variable \"CLANG_P2996_REF\" {` carries its brace, the platform token carries its full interpolation), and `contract_token_uniqueness` already proves each binds one site. Keep."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-2-1"
unit = "suites-2"
kind = "suites"
disposition = "refactor"
confidence = 0.80
custom_thing = "ci.mise-task-pre-commit-named asserts the mise task exists by substring-matching the literal TOML section header `[tasks.pre-commit]` inside mise.toml."
evidence = "python/verification/suites.toml:929-941, token at python/verification/suites.toml:936 (`per_path_tokens = { \"mise.toml\" = [\"[tasks.pre-commit]\"] }`). The handler is a raw `token in combined` with NO comment stripping: python/src/dotfiles_setup/verify.py:209-213 (`combined = \"\\n\".join(...)`; `missing = [t for t in tokens if t not in combined]`) — contrast forbid_tokens, which does strip comments at python/src/dotfiles_setup/verify.py:164."
native_alternative = "`mise tasks info <name>` — mise's own task resolver. It exits 0 for a task that really resolves and 1 for one that does not, and it resolves across the whole merged config (mise.toml + .config/mise/conf.d/*.toml + file tasks), printing the winning `Source:`."
alternative_evidence = ".agent/kb/raw/suites-2-native-probes.md § P1 — live probe: `mise tasks info pre-commit` -> rc=0 with `Source: ~/dev/github/ray-manaloto/dotfiles/mise.toml`; control arm `mise tasks info zzqq-not-a-task-9931` -> rc=1 `mise ERROR Task not found`. Host mise 2026.9.3, which .agent/kb/raw/mise-releases-since-pin.digest.md confirms is the latest published release."
rationale = "The substring is wrong in BOTH directions. False pass: `# [tasks.pre-commit] was renamed to lint` in mise.toml satisfies the contract with the real task deleted — require_tokens does not strip comments, and suites.toml's own header rule (python/verification/suites.toml:10-12, '#397 ... 11 were held up by a COMMENT alone') names this exact failure class. False fail: moving the task into `.config/mise/conf.d/shared.toml` or a `mise-tasks/` file leaves `mise run pre-commit` working while the contract reddens. `mise tasks info pre-commit` binds the resolved task — the thing the contract actually means — and carries its own control arm on every run via its exit code. Refactor rather than retire because verify.py has no command-executing handler today, so adopting this needs a small `command_rc` handler (the seam); the native mechanism is mise's, not homegrown."
probe = "uv run --project python dotfiles-setup verify run --json | jq '.results[]|select(.name==\"ci.mise-task-pre-commit-named\")' ; mise tasks info pre-commit >/dev/null 2>&1; echo rc=$?"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-2-2"
unit = "suites-2"
kind = "suites"
disposition = "refactor"
confidence = 0.70
custom_thing = "Fourteen ci.* suites assert GitHub-workflow STRUCTURE by whitespace-, indentation- and key-order-exact multi-line substrings of the YAML source: ci.ci-gate-aggregator-exists, ci.explicit-packages-write, ci.push-to-main-skips-build, ci.promote-job-exists, ci.p2996-prep-job-exists, ci.dev-prep-gate-exists, ci.smoke-test-survives-a-skipped-dev-prep, ci.smoke-test-lowers-aslr-entropy-for-tsan, ci.build-publish-matrix, ci.bootstrap-gap-report-wired, ci.refresh-uses-app-token, ci.image-lock-pr-wired, ci.lock-refresh-wired, ci.unconditional-ghcr-login."
evidence = "python/verification/suites.toml:697 binds `\"  ci-gate:\\n    needs: [lint, contract-preflight, changes, build-publish]\\n    if: always()\\n\"` — two leading spaces, four leading spaces, and the FLOW-sequence form of `needs`. python/verification/suites.toml:677 binds `\"\\npermissions:\\n  contents: read\\n  packages: write\\n\"` — i.e. the key ORDER and the 2-space indent of a mapping, not the permission set. python/verification/suites.toml:651-659 is a file-wide `regex_forbid` for a job-scoped invariant, and python/verification/suites.toml:836-856's own description concedes the limitation verbatim: 'No job-scoped negative check is expressible with this schema; that invariant rests on review, not a gate.'"
native_alternative = "`yq` v4 path expressions over the workflow file — `.jobs[\"ci-gate\"].needs`, `.jobs[\"ci-gate\"].if`, `.permissions`, `.jobs[\"smoke-test\"].if` — which read the parsed YAML document, so they are blind to indentation, flow-vs-block style and key order, and are naturally job-scoped (closing the negative-check gap the image-lock-pr description documents)."
alternative_evidence = ".agent/kb/raw/suites-2-native-probes.md § P2 — live probe: `yq '.jobs[\"ci-gate\"].needs' .github/workflows/ci.yml` -> `[lint, contract-preflight, changes, build-publish]`; `yq '.permissions' .github/workflows/ci.yml` -> `contents: read / packages: write`; control arm `yq '.jobs[\"no-such-job\"].needs'` -> `null`. yq is ALREADY a repo pin: .config/mise/conf.d/shared.toml:51 (`yq = \"4.53.6\"`), so this adopts a tool the repo installs rather than adding one."
rationale = "These contracts bind FORMATTING where they mean SEMANTICS, and that cuts both ways: reformatting `needs:` to block style, or reordering `contents`/`packages`, reddens a gate with zero behavioural change (a false fail that trains people to edit the contract), while a byte-identical block sitting in a `#` comment or a `run:` heredoc satisfies it (a false pass — the class suites.toml:10-12 already documents). yq answers the same questions off the parsed document. Deliberately REFACTOR, not retire: yq cannot see inside a `run:` script, which is an opaque scalar to the parser — so ci.smoke-test-lowers-aslr-entropy-for-tsan's contiguous `exit 1` blocks (python/verification/suites.toml:775-779) and ci.bootstrap-gap-report-wired's command tokens must stay substring assertions on the extracted `.run` string. The honest shape is a `yaml_path` handler for the job/needs/if/permissions half, with require_tokens retained for script bodies. No handler for this exists today (python/src/dotfiles_setup/verify.py:660-671 lists all ten)."
probe = "yq '.jobs[\"ci-gate\"] | {\"needs\": .needs, \"if\": .if}' .github/workflows/ci.yml && yq '.jobs[\"smoke-test\"].if' .github/workflows/build-publish.yml"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "suites-2-3"
unit = "suites-2"
kind = "suites"
disposition = "refactor"
confidence = 0.75
custom_thing = "ci.build-publish-matrix's per_path_tokens for python/src/dotfiles_setup/platform_target.py are two DEFINITION tokens — `PUBLISHED_ARCHES = (` and `def publish_matrix_json() -> str:`."
evidence = "python/verification/suites.toml:803-806 (`], \"python/src/dotfiles_setup/platform_target.py\" = [ \"PUBLISHED_ARCHES = (\", \"def publish_matrix_json() -> str:\", ]`). suites.toml's own first authoring question forbids exactly this: python/verification/suites.toml:10-12 — '1. Does it bind an INVOCATION, not a definition or a bare name? A `def foo` or a bare `foo` proves the part exists somewhere in the file'."
native_alternative = "pytest — the existing test suite already exercises both symbols by CALLING them, which is strictly stronger than asserting the `def` line exists: tests/test_platform_target.py:375 (`assert [t.arch for t in targets] == list(platform_target.PUBLISHED_ARCHES)`), tests/test_platform_target.py:442 (`def test_publish_matrix_json_is_parseable_and_complete`), plus monkeypatch-based discrimination arms at tests/test_platform_target.py:542 and :571."
alternative_evidence = "tests/test_platform_target.py:375,417,442,539-542,555-560,569-571 — these are run by `uv run --project python pytest tests/ -x -q`, which AGENTS.md § 'Validate before committing' makes a mandatory gate alongside `mise run verify`, so the coverage is not optional."
rationale = "A `def publish_matrix_json() -> str:` token cannot tell a live function from a dead one, and cannot survive a signature change that is semantically identical (`-> str` to `-> LiteralString`) — it will red-flag a harmless annotation edit while staying green if the function is never called. The tests already prove existence, callability, completeness AND discrimination (the monkeypatch arms). Drop the two python-file tokens and the `python/src/dotfiles_setup/platform_target.py` path from this suite; the eleven workflow-side tokens (python/verification/suites.toml:791-802) are the part that binds wiring nothing else checks — in particular `matrix=$(uv run --project python dotfiles-setup platform-matrix)`, which IS the call site. Keeping the path also has a side cost: run_suite's default-strict missing-path rule (python/src/dotfiles_setup/verify.py:81-92) makes this CI contract fail on a pure module rename that the tests would follow automatically."
probe = "uv run --project python pytest tests/test_platform_target.py -q && grep -n 'PUBLISHED_ARCHES = (\\|def publish_matrix_json' python/src/dotfiles_setup/platform_target.py"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-2-4"
unit = "suites-2"
kind = "suites"
disposition = "retire"
confidence = 0.60
custom_thing = "ci.no-mise-data-in-cache-map — a `regex_forbid` of the literal `\"mise-data\"` in .github/workflows/ci.yml, described as 'mise-data must not be in buildkit-cache-dance cache-map'."
evidence = "python/verification/suites.toml:621-628 (description at :623, `pattern = '\"mise-data\"'` at :628). Its sibling ci.no-actions-cache-workaround forbids the containing mechanism outright in the SAME file: python/verification/suites.toml:642-649, `tokens = [\"actions/cache/restore\", \"actions/cache/save\", \"buildkit-cache-dance\"]`, `paths = [\".github/workflows/ci.yml\"]`."
native_alternative = "None needed — the invariant is already fully covered by the sibling suite ci.no-actions-cache-workaround. A `cache-map` is an input to the `buildkit-cache-dance` action and cannot exist in a workflow that never invokes it, so forbidding the action subsumes forbidding one of its map keys."
alternative_evidence = "python/verification/suites.toml:649 forbids `buildkit-cache-dance` across the identical path set. .agent/kb/raw/suites-2-native-probes.md § P4 — `grep -c \"mise-data\\|buildkit-cache-dance\" .github/workflows/ci.yml` -> 0, control-armed with the same command shape against a known-present term (`grep -c \"ci-gate\"` -> 5), so the zero is a real absence and not a blind probe."
rationale = "Strictly subsumed: it is impossible to violate ci.no-mise-data-in-cache-map without also violating ci.no-actions-cache-workaround, so the second gate adds no detection and costs a suite entry, a description that names a mechanism no longer present anywhere in the repo, and a false-positive surface — an unrelated future use of the string `\"mise-data\"` in ci.yml (a cache key, a volume name, a comment) fails a contract that was only ever about a cache-map. Confidence is 0.6 rather than higher because retiring a defence-in-depth negative contract is a judgement call: if the intent is to also block a hand-rolled cache map with no action involved, the honest fix is to widen the description and rename the suite rather than delete it. Flagged for the operator's call, not a unilateral delete."
probe = "grep -n 'buildkit-cache-dance\\|cache-map\\|\"mise-data\"' .github/workflows/*.yml .github/actions/*/action.yml"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "suites-2-5"
unit = "suites-2"
kind = "suites"
disposition = "refactor"
confidence = 0.80
custom_thing = "ci.dive-config-present asserts only that the string `lowestEfficiency` appears in `.dive-ci`, and its description justifies that by a wiring claim that is now false on both counts: '.dive-ci config must exist when ci.yml references the wagoodman/dive action'."
evidence = "python/verification/suites.toml:907-914 (description at :909, `per_path_tokens = { \".dive-ci\" = [\"lowestEfficiency\"] }` at :914). The suite's `paths` list is `[\".dive-ci\"]` only — it names no consumer, so it binds no call site."
native_alternative = "Bind dive's actual invocation, which lives in a different workflow and does not use the action at all: `.github/workflows/image-analysis.yml:126` runs `dive --ci --ci-config .dive-ci \"$REF\"`, with the binary installed directly at `.github/workflows/image-analysis.yml:122-125` under an explicit comment at `:108` reading 'Direct binary install (NOT `uses: wagoodman/dive@<sha>`)'. Adding that `--ci-config .dive-ci` call site as a per_path_token on image-analysis.yml is the same shape ci.trivy-scan-wired already uses for its sibling scanner (python/verification/suites.toml:916-928)."
alternative_evidence = ".agent/kb/raw/suites-2-native-probes.md § P5 — `grep -rn \"dive\" .github/workflows/ci.yml` returns only a path-filter comment (`:250`) and a `changes` filter entry (`:291`); every dive invocation is in image-analysis.yml. Control arm: the same grep against image-analysis.yml returns the install and run lines :103,:108,:122-126."
rationale = "This is the stale contract in the unit. It still PASSES (verify run: passed), which is precisely why the rot is invisible — the assertion it makes is true, but the reason it gives for making it has been retired: dive moved out of ci.yml into image-analysis.yml, and stopped being an action at all. As written it cannot notice the failure it exists to prevent in either direction: delete the `--ci-config .dive-ci` flag from image-analysis.yml and the contract stays green while dive silently runs on defaults; delete the dive step entirely and the contract still guards an orphan config file. Fix is two lines — repoint the description at image-analysis.yml and add the invocation token so the config and its consumer are bound together, per suites.toml's own question 1 (:10-12)."
probe = "grep -n 'dive --ci --ci-config' .github/workflows/image-analysis.yml && uv run --project python dotfiles-setup token-audit"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-2-6"
unit = "suites-2"
kind = "suites"
disposition = "keep"
confidence = 0.80
custom_thing = "The Dockerfile / devcontainer.json / bake contracts in this unit — build.no-system-config-file-pin, build.dockerfile-section-markers, build.p2996-cache-dockerfile-stages, build.locked-install-with-conda-sha, build.base-image-digest-pinned, build.runtime-tier-stage, build.dev-target-publishes-runtime-stage, build.doppler-secrets-wired, ci.sbom-attestation, ci.provenance-attestation, ci.gha-cache-enabled, identity.no-vscode-user, identity.user-env-set — assert Dockerfile/HCL/JSONC structure by substring and regex."
evidence = "python/verification/suites.toml:480-589 (the build.* block), :630-641 (ci.gha-cache-enabled), :942-966 (identity.no-vscode-user, identity.user-env-set). Example: identity.user-env-set is `regex_match` on `USER=\\$\\{?DEVCONTAINER_USER\\}?` (python/verification/suites.toml:958-966) — text, not the ENV instruction."
native_alternative = "None available at these pins. ast-grep 0.45.3 has NO Dockerfile grammar, so the structural rewrite that works for YAML is unavailable for these files. hadolint 2.15.1 is already wired over both Dockerfiles (hk.pkl:130-132) but its rule set asserts shell/instruction hygiene, not that a named build stage, an ENV VALUE, a digest pin or a sentinel comment is present. `devcontainer read-configuration` is wired (hk.pkl:135-138) and validates devcontainer.json semantics, but says nothing about which runArgs are present. The vendored-schema layer (schemas/sources.toml, check-jsonschema 0.38.0) covers mise/ruff/typos configs, none of these files."
alternative_evidence = ".agent/kb/raw/suites-2-native-probes.md § P3 — live probe: `ast-grep run -l dockerfile -p 'ENV USER=$X' .devcontainer/Dockerfile.host-user` -> `error: invalid value 'dockerfile' for '--lang <LANG>': dockerfile is not supported!`; POSITIVE control arm with the same command shape on a supported language, `ast-grep run -l yaml -p 'always()' .github/workflows/ci.yml` -> 2 hits, so the refusal is a real capability gap and not a broken invocation. hk.pkl:130-132 (hadolint glob) and hk.pkl:135-138 (devcontainer_json_validate) show what IS wired."
rationale = "Required justification: no native or structural tool available to this repo can express 'stage devcontainer-runtime exists', 'BASE_IMAGE is digest-pinned', 'these four hash sentinels are present', or 'ENV USER is DEVCONTAINER_USER'. Dockerfile has no parser in the toolchain (ast-grep control-armed above), hadolint has no value-assertion rules, and Renovate's pinDigests is explicitly DISABLED for the adjacent feature refs (renovate.json:26-30) so it is not a substitute gate for build.base-image-digest-pinned even where it maintains the pin. The residual weakness is real and worth stating: these substrings and regexes are comment-satisfiable exactly like the others (verify.py:209-213 does not strip comments in require_tokens, and regex_match scans raw text), so a commented-out `# ENV USER=${DEVCONTAINER_USER}` would pass. That is mitigated, not solved, by the contract_token_uniqueness gate (hk.pkl:354-356) which does run clean here."
probe = "docker buildx build --call=outline --target devcontainer-runtime -f .devcontainer/Dockerfile . 2>&1 | head -40   # does BuildKit's frontend expose a parsed stage/ENV outline that a structural handler could assert against? if yes, build.runtime-tier-stage and identity.user-env-set upgrade from keep to refactor"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-2-7"
unit = "suites-2"
kind = "suites"
disposition = "keep"
confidence = 0.85
custom_thing = "ci.regex-managers-enabled requires the substring `custom.regex` in renovate.json, and ci.renovate-extends-jdx-preset requires `github>jdx/renovate-config`."
evidence = "python/verification/suites.toml:889-897 and :898-906. Both are single-token per_path_tokens on renovate.json; each token occurs exactly once in the target today (`grep -c` -> 1 for both)."
native_alternative = "None. `renovate-config-validator` (npm:renovate 44.59.2, wired as `dotfiles-setup renovate-validate`) is the tool that would be the native gate, and it validates schema legality and RE2 regex compilability only — a renovate.json that omits `custom.regex` from `enabledManagers`, or drops the jdx preset from `extends`, is a fully VALID config the validator exits 0 on."
alternative_evidence = "python/src/dotfiles_setup/renovate_validate.py:4-35 (the validator's documented scope: RE2 regex compilation) and :72 (`VALIDATOR = \"renovate-config-validator\"`), :84-91 (the RE2 canary config). .agent/kb/raw/renovate-config-options.digest.md — the fresh 2026-09-09 fetch of the Renovate config-options page surfaces no validator capability for asserting preset or manager membership; the new options it does list (dependency-groups/PEP 735, pixi.lock in pep621) are unrelated to this unit."
rationale = "Required justification: the failure both contracts exist to catch is a SILENTLY VALID config — the description at python/verification/suites.toml:891 records the real incident ('the existing CLANG_P2996_REF tracker hit this in 2026-04'), where every customManager block was inert because `custom.regex` was missing from enabledManagers and nothing complained. Renovate's own validator is structurally incapable of flagging that, because it is a legality checker, not a policy checker. The usual comment-satisfaction hole does not apply here either: renovate.json is strict JSON with no comment syntax, so the only way a token can be satisfied off-site is inside another string value — narrow enough that the substring form is honest. A `jq -e '.extends | index(\"github>jdx/renovate-config\")'` rewrite would be marginally more precise but adds a handler for no measured defect class; not worth the change."
probe = "uv run --project python dotfiles-setup renovate-validate; jq -e '.enabledManagers | index(\"custom.regex\")' renovate.json; jq -e '.extends | index(\"github>jdx/renovate-config\")' renovate.json"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-2-8"
unit = "suites-2"
kind = "suites"
disposition = "keep"
confidence = 0.85
custom_thing = "The suites.toml substring/regex contract machinery itself (handlers require_tokens, forbid_tokens, regex_match, regex_forbid) as the vehicle for every remaining member of this unit — ci.image-name-dotfiles-devcontainer, ci.lint-locked-installs, ci.refresh-no-dispatch-hack, ci.no-actions-cache-workaround, ci.trivy-scan-wired, ci.p2996-ref-dispatch-wired, build.locked-install-with-conda-sha, identity.no-vscode-user."
evidence = "python/src/dotfiles_setup/verify.py:660-671 — the complete HANDLERS map (ten entries, all text-matching or policy_doc). python/verification/suites.toml:871-879, :880-888, :660-668, :916-928 for the members listed. Current state: `dotfiles-setup verify run --json` -> rc=0, passed 148 / failed 0 / skipped 4, all 38 unit members `passed`; `dotfiles-setup token-audit` -> rc=0, silent."
native_alternative = "None at the host's pins. hk 1.57.0 has no step type that expresses a file-content contract beyond running a shell check, and the only newer release is documentation-only. mise 2026.9.3 is already the latest release. The nearest generic alternative — a JSON-Schema / structural assertion layer — cannot address these targets: they are Dockerfiles, HCL, lockfiles and workflow `run:` script bodies."
alternative_evidence = ".agent/kb/raw/hk-releases-since-pin.digest.md — 'Releases newer than the pinned 1.57.0: only v1.58.1 ... A small documentation-only release. There are no changes to hk's runtime behavior', i.e. zero new hk capability. .agent/kb/raw/mise-releases-since-pin.digest.md — 'the newest release in the fetched list is v2026.9.3 ... identical to the host's installed version ... ZERO releases strictly newer'. .agent/kb/raw/uv-releases-since-pin.digest.md — 0.12.10/0.12.11 changes are `uv publish` token revocation, `pylock.toml` hashes and `uv tree --invert`; nothing touching verification."
rationale = "Required justification: these members assert the presence or absence of specific literal wiring (an image name, `MISE_LOCKED: \"1\"`, the removed `gh workflow run ci.yml` dispatch hack, the trivy action + SARIF upload, a conda-checksum table in a generated lockfile) in file types no structural tool in this repo parses. The two known weaknesses of the mechanism are ALREADY mitigated in-repo rather than left open: multiplicity (a token satisfied by a stand-in) is gated by `contract_token_uniqueness` -> `dotfiles-setup token-audit` (hk.pkl:354-356), which runs clean; and partial path loss is gated default-strict before dispatch (python/src/dotfiles_setup/verify.py:81-92). One overlap is worth recording without acting on it: build.locked-install-with-conda-sha's lock-side token `[conda-packages.linux-x64` is partly covered by lock_integrity's conda-platform-loss check over the same file (python/src/dotfiles_setup/lock_integrity.py:66, :101-102, :147-151, run as hk step `mise_lock_integrity` at hk.pkl:337-339) — but that is a DELTA gate comparing worktree against HEAD, so it cannot see a lock that has been wrong since the day it was committed. The suite is the absolute floor underneath it; keep both."
probe = "uv run --project python dotfiles-setup lock-check; uv run --project python dotfiles-setup token-audit; echo rc=$?"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-1"
unit = "suites-3"
kind = "suites"
disposition = "retire"
confidence = 0.80
custom_thing = "The four `policy.*` members (policy.zero-warning, policy.research-before-fixing, policy.no-suppression-without-approval, policy.worktree-pr-workflow) are `handler = \"policy_doc\"` entries with no `paths` and no tokens."
evidence = "python/verification/suites.toml:1095-1125 declares all four with `check_type = \"policy\"`, `handler = \"policy_doc\"`, `reference = ...` and zero paths. The handler is python/src/dotfiles_setup/verify.py:534-545: it reads `entry.get(\"reference\", \"unknown\")` and unconditionally returns `{\"status\": \"skipped\", \"reason\": ...}` — it never opens the referenced file, never checks it exists, and has no failing branch. Three of the four point at the SAME doc (.claude/rules/zero-skip-policy.md). Measured token tally for this unit: these four contribute 0 of the 386 per_path tokens."
native_alternative = "The mechanical half of these policies is already machine-enforced by hk steps, and the judgment half is already delivered by Claude Code's own eager rule loading of `.claude/rules/*.md`: hk's `no_lint_skip` step bans noqa/type:ignore/pylint:disable/nosec in Python source, and the `agnix . --strict` step lints the rule docs themselves. Claude Code loads `.claude/rules/*.md` into context natively and fires `InstructionsLoaded` when it does."
alternative_evidence = "hk.pkl:110 (`[\"no_lint_skip\"]`), hk.pkl:491-505 (`[\"agnix\"]` with `check = \"agnix . --strict\"` and glob covering `.claude/**/*.md`). Native rule loading: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/hooks.md:56 — \"`InstructionsLoaded` | When a CLAUDE.md or `.claude/rules/*.md` file is loaded into context.\""
rationale = "A contract that can only report `skipped` is decoration: it cannot fail, it does not even assert its own reference resolves, and it inflates the verify roster with four rows that carry no signal. `probes-need-a-control-arm.md` rule 2 is exactly this — a gate verified only in the passing direction. Retiring them loses nothing that hk's `no_lint_skip`/`agnix` steps and the rule docs' own native loading do not already carry. If a roster entry is wanted at all, one entry with a real existence check on `reference` would assert strictly more than these four combined."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-2"
unit = "suites-3"
kind = "suites"
disposition = "refactor"
confidence = 0.85
custom_thing = "`policy.worktree-pr-workflow` names `reference = \"CLAUDE.md\"` for the policy \"Verify on feature branch, never merge to main for testing\"."
evidence = "python/verification/suites.toml:1119-1125. The referenced file is dead for this purpose: CLAUDE.md is byte-exactly `@AGENTS.md` plus an HTML comment (enforced by the `claude_md_import_stub` hk step, AGENTS.md \"Key Files\" row), and contains no worktree or PR-workflow content. Control-armed grep: `grep -rln \"worktree\" .claude/rules/ AGENTS.md` returns exactly one file, `.claude/rules/goal-history.md`, whose \"worktree\" mention is about ownership handoffs, not this policy — so the concept the contract names lives in neither the file it cites nor anywhere else under that name."
native_alternative = "The repo already owns the exact check this needs: the `doc_refs` hk step validates that every backtick-span path reference in AGENTS.md / rules / skills resolves. Its glob simply does not include `python/verification/suites.toml`, so suite `reference` fields are parsed by nothing — the same blind spot `doc_refs` was created to close for markdown."
alternative_evidence = "hk.pkl:515-518 — `[\"doc_refs\"]` with `glob = List(\"AGENTS.md\", \"**/AGENTS.md\", \".claude/rules/*.md\", \".claude/skills/**/*.md\")` and `check = \"uv run --project python dotfiles-setup check-doc-refs\"`; the preceding comment states backtick citations \"were parsed by NOTHING\"."
rationale = "This is the stale-reference case the unit brief asks for, and it is invisible today because `policy_doc` (verify.py:534-545) never resolves the reference. Either retire the entry with the rest of the `policy.*` group (suites-3-1) or, if kept, repoint it at the surfaces that actually carry the policy (`.claude/rules/do-not.md` #9, `.claude/skills/pr-workflow/SKILL.md`) AND extend `doc_refs`' glob to suites.toml so a `reference` can never again name a file that does not carry the thing."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-3"
unit = "suites-3"
kind = "suites"
disposition = "refactor"
confidence = 0.85
custom_thing = "`identity.username-validation` claims to assert three guards but binds one. Its description is \"Username validation must check empty, root, and invalid chars\"; its `pattern` is `DEVCONTAINER_USER.*=.*\"root\"`."
evidence = "python/verification/suites.toml:995-1002. Against .devcontainer/Dockerfile.host-user the pattern matches only line 18 (`if [ \"${DEVCONTAINER_USER}\" = \"root\" ]`). The empty guard at .devcontainer/Dockerfile.host-user:15-16 (`if [ -z \"${DEVCONTAINER_USER}\" ]; then echo \"ERROR: DEVCONTAINER_USER is empty\"`) and the invalid-character guard at :21-22 (`grep -qE '[^A-Za-z0-9._-]'`) are unbound — delete either and the contract stays green while the description still claims coverage."
native_alternative = "None covers it. hadolint is wired over exactly this file and shellcheck lints the RUN bodies, but both check Dockerfile/shell FORM, not the presence of a specific guard; neither can express \"this RUN must contain an emptiness check on this ARG\". The fix is inside the existing mechanism: three `regex_match` patterns (or one `require_tokens` with three anchored tokens), one per guard."
alternative_evidence = "hk.pkl:131-133 — `[\"hadolint\"] = (Builtins.hadolint) { glob = List(\".devcontainer/Dockerfile\", \".devcontainer/Dockerfile.host-user\") }`; hk.pkl:195 — `[\"shellcheck\"] = Builtins.shellcheck`."
rationale = "A description that over-states what the pattern binds is worse than a narrower description, because a reviewer reads the description and stops. Two of the three named guards are unprotected today. `probes-need-a-control-arm.md` rule 2 applies: the contract has only ever been exercised in the direction where the root guard survives."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-4"
unit = "suites-3"
kind = "suites"
disposition = "refactor"
confidence = 0.70
custom_thing = "101 of this unit's 386 `per_path_tokens` (26%) are test-function-name substrings under `tests/*.py` — asserting that a named control-arm test EXISTS as text. `workflow.dag-tick-wiring` alone carries 43 of them; `workflow.dag-projection-wiring` 13, `workflow.doctor-wiring` 10, `workflow.mise-tasks-enforcement` 9."
evidence = "Tallied from python/verification/suites.toml by parsing `per_path_tokens` and counting entries whose path starts with `tests/`. See e.g. python/verification/suites.toml:1549-1562 (workflow.dag-tick-wiring's `tests/test_dag_tick.py` list, 43 `def test_...(` substrings). The handler is a plain substring test with no comment stripping: python/src/dotfiles_setup/verify.py:210 `combined = \"\\n\".join(p.read_text() for p in paths if p.exists())` then `t not in combined`. So `def test_x(` is satisfied by a test that is skipped at module level, disabled under `if False:`, shadowed by a later redefinition of the same name, or quoted in a docstring."
native_alternative = "pytest's own collection with explicit node IDs: `pytest --collect-only tests/test_dag_tick.py::test_terminal_predicate_arms` exits non-zero when the node does not resolve, and it binds the collected test object rather than a byte sequence — so a shadowed or uncollectable test fails where the substring passes. pytest is already the repo's gate runner."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/pytest/doc/en/reference/reference.rst:2956 — `.. option:: --collect-only, --co`; node-ID selection at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/pytest/doc/en/example/markers.rst:98-101 (\"Selecting tests based on their node ID … You can provide one or more node IDs as positional arguments\")."
rationale = "The intent — \"this control arm must not be deleted\" — is legitimate and nothing else asserts it, so this is not a retirement. But the chosen mechanism asserts strictly less than the native one at the same cost, and it is 26% of the unit's maintenance surface: every ordinary test rename breaks a contract with no behaviour change, which trains reviewers to edit the contract reflexively. Move the `tests/` half of these lists to a `--collect-only` node-ID gate and the contracts get stronger and shorter at once."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "suites-3-5"
unit = "suites-3"
kind = "suites"
disposition = "refactor"
confidence = 0.65
custom_thing = "`require_tokens` — the handler behind 34 of this unit's 38 members — matches unanchored substrings over raw file text with NO comment stripping, while its sibling `forbid_tokens` strips comments by default."
evidence = "python/src/dotfiles_setup/verify.py:210 (`combined = \"\\n\".join(p.read_text() ...)`, then `missing = [t for t in tokens if t not in combined]`) versus python/src/dotfiles_setup/verify.py:141/168-169 (`strip_comments: bool = True`; `content = line.split(\"#\", 1)[0] if strip_comments else line`) and verify.py:559. suites.toml's own header records the consequence at python/verification/suites.toml:20-31: of 33 live holes found in #397, \"11 were held up by a COMMENT alone, 10 by a comment plus code\"."
native_alternative = "ast-grep, already pinned in this repo at 0.45.3 but wired to nothing — no hk step, no mise task, no sgconfig.yml. Its YAML rules bind syntax nodes (`pattern`, `kind`) with relational composition (`inside`, `has`), so a comment or docstring cannot satisfy a rule and a match is provably code."
alternative_evidence = "Pin: mise.toml:47 `\"github:ast-grep/ast-grep\" = \"0.45.3\"`. Rule surface: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/config/src/rule/mod.rs:47-77 — `pub struct SerializableRule` with `pub pattern`, `pub kind`, `pub inside: Maybe<Box<Relation>>`, `pub has: Maybe<Box<Relation>>`. Control arm on \"wired to nothing\": `grep -rn \"ast-grep\" --include=\"*.toml\" --include=\"*.pkl\" --include=\"*.yml\" .` returns only the mise.toml pin plus research prose — the same command returns hk steps for every tool that IS wired."
rationale = "Partial, not full, supersession — and the partiality is the point. Only the `.py` tokens can move to ast-grep; the tokens over mise.toml, .claude/settings.json, hk.pkl, .codex/*.toml and markdown cannot, and those are the majority of the wiring seams. So this is 'adopt the native mechanism for the Python half', not 'retire require_tokens'. Doing so also retires a class the existing `contract_token_uniqueness` gate cannot reach: token_audit answers HOW MANY places match, never whether the one place that matches is executable code."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-6"
unit = "suites-3"
kind = "suites"
disposition = "refactor"
confidence = 0.75
custom_thing = "Definition-only bindings that survive deletion of the wiring they claim to protect: `workflow.sync-wiring` binds `def decide_action(`; `workflow.bash-logic-enforcement` binds `def find_violations(`; `workflow.instructions-observer-wiring` binds `def build_record(`, `def build_report(`, `def scoped_rules_on_disk(`, `def run_report(`."
evidence = "python/verification/suites.toml:1144-1151 (sync-wiring), :1388-1409 (bash-logic-enforcement), :1410-1432 (instructions-observer-wiring). Each `def` token is a definition; the call site is a different line in the same file and is unbound — sync.py:681 def vs sync.py:856 call (`action = decide_action(status, force=opts.force)`); bash_budget.py:152 def vs :206 call; instructions_observer.py:180 def vs :302 call; instructions_report.py:110/190/277 defs vs :289/:297/:389 calls. Delete the call, keep the def, and every one of these contracts stays green — precisely the failure mode suites.toml's own header warns about at python/verification/suites.toml:10-13 (\"Does it bind an INVOCATION, not a definition or a bare name?\")."
native_alternative = "ast-grep relational rules express 'this function is CALLED here' directly: a `pattern: decide_action($$$)` rule constrained with `inside: {kind: function_definition, has: {pattern: sync_main}}` binds the invocation node, not the `def` line."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/config/src/rule/mod.rs:47-77 (`pattern`, `kind`, `inside`, `has` on `SerializableRule`); pin at mise.toml:47."
rationale = "These are live instances of the #299 defect class inside contracts written after #299 — the rule was stated in the file header and then not applied to these members. The `contract_token_uniqueness` gate does not catch it (it audits multiplicity, not call-vs-definition), so nothing today would notice. Minimum fix without new tooling: rebind each to its call-site line with its delimiter (`decide_action(status, force=opts.force)`); better fix: an ast-grep rule that cannot be satisfied by a `def`."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-7"
unit = "suites-3"
kind = "suites"
disposition = "refactor"
confidence = 0.55
custom_thing = "`workflow.doctor-wiring` pins the project doctor's MCP-registration source model — the '`four places`' model — by binding `def claude_json_servers(`, `servers.extend(claude_json_servers(home, repo_root))`, `def parse_mcp_list(`, `(\"mcp-scope\", check_mcp_scope),` and `(\"mcp-duplicate\", check_mcp_duplicate),`."
evidence = "python/verification/suites.toml:1597-1657 (the `python/src/dotfiles_setup/doctor.py` token list). The four-source model is stated in .claude/CLAUDE.md (\"MCP registrations come from FOUR places — `.mcp.json`, each enabled plugin, and `~/.claude.json`'s user-global *and* per-project blocks\"). Control-armed probe of the enumerated sources: `grep -rn \"managed\" python/src/dotfiles_setup/doctor.py` returns 0 hits, while the same command shape for a known-present term (`grep -c \"mcpServers\" python/src/dotfiles_setup/doctor.py`) returns 1 — so the absence is real, not a blind grep."
native_alternative = "Claude Code 2.1.259 added `managedMcpServers` as a managed setting that provides HTTP/SSE MCP servers to every user, with the same entry shape as `.mcp.json` — a fifth registration source the doctor cannot see and the contract does not require it to read."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — \"capability: `managedMcpServers` managed setting lets organizations provide HTTP/SSE MCP servers to every user (same entry shape as `.mcp.json`); command-running entries are skipped. version: 2.1.259\". The offline corpus predates the CLI form and knows only the Desktop-only variant: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/desktop.md:735."
rationale = "The contract is not stale in the sense of naming a dead token — every token still binds. It is stale in the sense that matters more: it certifies a source model the platform has since widened, so `check_mcp_scope`/`check_mcp_duplicate` can report clean on a host whose MCP surface includes servers they never enumerated, and the contract's green is what makes that look settled. `probes-need-a-control-arm.md`: 'when a scanner reports clean, ask what it can see'. This host is not under an org managed policy today, which is why confidence is 0.55 rather than higher — the gap is latent, not currently biting."
probe = "cat \"$HOME/Library/Application Support/ClaudeCode/managed-settings.json\" 2>/dev/null | jq 'has(\"managedMcpServers\")'; grep -rn \"managedMcpServers\" /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings.md"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-8"
unit = "suites-3"
kind = "suites"
disposition = "keep"
confidence = 0.80
custom_thing = "Five members assert VALUES inside the JSONC file `.devcontainer/devcontainer.json` by exact-substring / regex matching: `arch.devcontainer-remote-user-dynamic` (`\"remoteUser\": \"${localEnv:USER}\"`), `arch.devcontainer-json-base-image-localenv` (`${localEnv:BASE_IMAGE}`), `arch.update-remote-user-uid-disabled` (`\"updateRemoteUserUID\": false`), `arch.devcontainer-json-no-bare-image-tag` (regex_forbid `\"dotfiles-devcontainer:`), `arch.no-mise-home-volume` (forbid_tokens `mise-home`)."
evidence = "python/verification/suites.toml:1008-1062. Each is whitespace- and quoting-sensitive: a reformat that changes the space after the colon breaks `\"remoteUser\": \"${localEnv:USER}\"` with no semantic change. The same file already gets three-layer structural validation on every commit that touches it: hk.pkl:149-152 `[\"devcontainer_json_validate\"]` → scripts/validate-devcontainer-json.sh (layer 1 biome lint, layer 2 `devcontainer read-configuration`, layer 3 check-jsonschema against the official spec schema)."
native_alternative = "None reaches these assertions. Probed, both arms: `check-jsonschema --help` on the pinned 0.38.0 reports `--force-filetype [json|toml|yaml]` — there is NO json5/jsonc mode, so a custom `const`-bearing schema cannot be run against the raw JSONC and see the literal `${localEnv:USER}`. The one JSON that check-jsonschema does see is layer 3's input, which is `jq .configuration` of `devcontainer read-configuration` output — i.e. POST-substitution, where `${localEnv:USER}` has already become the concrete username and the assertion is unexpressible by construction (scripts/validate-devcontainer-json.sh:47-58 stubs `BASE_IMAGE=validate` etc. precisely to make substitution succeed)."
alternative_evidence = "Probe output (this session): `mise exec -- check-jsonschema --help | grep force-filetype` → `--force-filetype [json|toml|yaml]`. Control arm on the same probe: the adjacent `--regex-variant [default|nonunicode|python]` line came back in the same grep, so the help text was really read. Repo side: scripts/validate-devcontainer-json.sh:16-21 (layer-3 description) and :47-58 (the substituting layer-2 call whose output feeds it)."
rationale = "Required justification: the official devcontainer schema constrains STRUCTURE (unknown fields, types, required keys) and the pinned validator cannot parse JSONC at all, so nothing native can assert that `remoteUser` holds the un-substituted `${localEnv:USER}` literal or that `updateRemoteUserUID` is specifically `false` rather than merely a boolean. These five suites assert exactly the value-level invariants the schema layer is structurally unable to reach, so they are complementary to `devcontainer_json_validate`, not redundant with it. One caveat worth fixing in place, not a reason to retire: `arch.no-mise-home-volume` runs `forbid_tokens` with the default `strip_comments=True`, which strips after `#` — the wrong comment marker for a `//`-commented JSONC file (verify.py:168-169), so a `// mise-home ...` narration line would trip it as a false positive."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-9"
unit = "suites-3"
kind = "suites"
disposition = "refactor"
confidence = 0.80
custom_thing = "`arch.npm-package-manager-bun`, `arch.pipx-uses-uvx` and `arch.cargo-uses-binstall` assert three mise settings in `.devcontainer/mise-system.toml` by matching the rendered TOML text: `npm.package_manager = \"bun\"`, `pipx.uvx = true`, `cargo.binstall = true`."
evidence = "python/verification/suites.toml:1064-1091. All three depend on taplo's chosen rendering of the assignment — spacing around `=`, and whether the key stays dotted (`npm.package_manager = \"bun\"`) or is written as a `[settings.npm]` table with `package_manager = \"bun\"`. Either is valid TOML for the same setting; only one satisfies the contract."
native_alternative = "mise's own read-only config reader against a named file: `mise config get -f <FILE> <KEY>`, documented as \"Display the value of a setting in a mise.toml file\", effect read-only, with `-f --file <FILE>` to target a specific config instead of the nearest one. It parses the TOML, so it is indifferent to formatting and to dotted-vs-table style."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/mise/docs/cli/config/get.md:1-22 — `mise config get [FLAGS] [KEY]`, \"**Effect:** read-only\", `-f --file <FILE>`. Live probe with both arms on the pinned host mise 2026.9.3: `mise config get -f .devcontainer/mise-system.toml settings.npm.package_manager` → stdout `bun`, rc=0; the same command with a bogus key `settings.npm.zzqfrobnicate` → `mise ERROR Key not found: ... in .../.devcontainer/mise-system.toml`, rc=1. The gate therefore discriminates on exit code, not on text."
rationale = "This is `use-tool-builtins.md`'s exact shape: the tool that owns the file ships a read-only accessor for precisely this question, and the repo is instead matching the tool's serialisation. Rewriting the three as `test \"$(mise config get -f .devcontainer/mise-system.toml settings.npm.package_manager)\" = bun` makes them survive any reformat by taplo (which already runs over this file at hk.pkl:167) and makes them fail loudly on a genuinely-changed value. Confidence is 0.8 rather than higher only because it requires a new handler shape (a command-exit check) that suites.toml's static handlers do not currently offer — the assertion itself is proven."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-10"
unit = "suites-3"
kind = "suites"
disposition = "refactor"
confidence = 0.60
custom_thing = "The `contract_token_uniqueness` gate that backstops this unit audits only `require_tokens` suites via `per_path_tokens`, so the five non-`require_tokens` members here — `identity.logname-env-set`, `identity.workdir-set`, `identity.username-validation` (regex_match), `arch.devcontainer-json-no-bare-image-tag` (regex_forbid), `arch.no-mise-home-volume` (forbid_tokens) — are outside it entirely."
evidence = "python/src/dotfiles_setup/token_audit.py:33-38 — \"`find_ambiguous` reads **only** `per_path_tokens`. A bare `tokens` list was therefore invisible to it\"; and :56-59 — `find_unaudited` covers \"a `require_tokens` suite naming exactly one path\". Neither function has a branch for `regex_match`, `regex_forbid` or `forbid_tokens`. Confirmed live: `uv run --project python dotfiles-setup token-audit` exits rc=0 with zero output on the current tree, which says nothing about these five. The exposure is the same class #397 measured: `identity.workdir-set`'s pattern `WORKDIR\\s+/home/\\$\\{?DEVCONTAINER_USER\\}?` would be satisfied by any second occurrence, including one inside a comment, since regex_match does no comment stripping either."
native_alternative = "None external — this is a coverage extension of the repo's own gate. The nearest structural expression is again ast-grep, which for Dockerfiles can bind `kind` nodes rather than lines, but ast-grep's Dockerfile support is not the point: the cheap fix is to teach `find_ambiguous`/`find_unaudited` to count `pattern` matches for regex handlers with the same multiplicity rule."
alternative_evidence = "python/src/dotfiles_setup/token_audit.py:56-59 (the stated scope of `find_unaudited`); ast-grep rule surface at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/config/src/rule/mod.rs:47-77."
rationale = "The uniqueness gate is the reason the require_tokens mechanism is defensible at all (see suites-3-11), and five members of this unit sit outside it with no marker saying so. That is the #343 shape — a guard that is fine wherever it runs, and simply does not run over part of its nominal domain. Extending the audit to regex handlers is a small, self-contained change to `token_audit.py`; leaving it means the regex members carry no multiplicity discipline while reading, in the manifest, exactly like the ones that do."
probe = "uv run --project python python -c \"from dotfiles_setup import token_audit, verify; import inspect; print('regex' in inspect.getsource(token_audit.find_ambiguous), 'regex' in inspect.getsource(token_audit.find_unaudited))\""
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-3-11"
unit = "suites-3"
kind = "suites"
disposition = "keep"
confidence = 0.75
custom_thing = "The unit as a whole: 34 cross-file `require_tokens` wiring contracts (workflow.*, eval.*, arch.*, identity.*) binding 386 tokens across mise.toml, .claude/settings.json, hk.pkl, python modules, .codex/*.toml, tests and markdown rules."
evidence = "python/verification/suites.toml:959-1694 for this unit's members; tallied 386 `per_path_tokens` across the 38 members. Representative: workflow.mise-tasks-enforcement (python/verification/suites.toml:1271-1295) binds a chain that crosses .claude/settings.json → scripts/pretooluse-guard.sh → python/src/dotfiles_setup/hook_guard.py → hook_selfcheck.py → two test files → a rule doc. The multiplicity hole that made this mechanism unsound is machine-closed: hk.pkl:354 wires `[\"contract_token_uniqueness\"]` to `dotfiles-setup token-audit`, and that gate ran clean on this tree (rc=0, no output)."
native_alternative = "Nothing native asserts cross-file, cross-language wiring. Every candidate is single-language and single-file: agnix lints agent markdown (hk.pkl:491), check-jsonschema validates one JSON document against one schema, taplo validates TOML, hadolint/shellcheck one Dockerfile or script, ast-grep one language's syntax tree. None can express \"the hk step in hk.pkl invokes the subcommand main.py registers, which calls the function in this module, whose control-arm test exists, and whose policy the rule doc states\"."
alternative_evidence = "hk.pkl:491-505 (agnix scope: markdown + settings.json only), hk.pkl:167 (taplo: TOML only), hk.pkl:131-133 (hadolint: two Dockerfiles), scripts/validate-devcontainer-json.sh:16-21 (check-jsonschema: one document, one schema). ast-grep's rule model is per-language by construction — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep/crates/config/src/rule/mod.rs:47-77 keys every rule on `pattern`/`kind` within one parsed tree."
rationale = "Required justification: the invariant these contracts protect — a policy stated in a rule doc is wired through a task, a CLI dispatch, an implementation and a control-arm test, in five different file formats — has no native expression in any tool this repo pins, and the failure it catches (#343's off-root guard, #354's declared-but-unresolvable lane, #391's pin predating its handler) is a real, repeated, expensive class. The mechanism's known weakness (a token satisfied by a stand-in) is already gated by `contract_token_uniqueness`. What should change is not the existence of these contracts but three specific things named separately above: the test-name half moves to pytest node IDs (suites-3-4), the definition-only bindings move to call sites (suites-3-6), and the regex members come under the uniqueness audit (suites-3-10)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-4-1"
unit = "suites-4"
kind = "suites"
disposition = "refactor"
confidence = 0.70
custom_thing = "All 30 `require_tokens` members of this unit (workflow.env-dump-enforcement through workflow.prune-scoping-wiring) assert Python/JSON/YAML call sites by RAW SUBSTRING over the whole file. `_handle_require_tokens` does `tok not in text` with no comment or docstring awareness, and `strip_comments` is a `forbid_tokens`-only option that never reaches this handler."
evidence = "python/src/dotfiles_setup/verify.py:561-585 (`per_path` loop: `text = target.read_text() ...`, `f\"{raw}: missing '{tok}'\" for tok in tokens if tok not in text`); python/src/dotfiles_setup/verify.py:552-560 (`strip_comments=entry.get(\"strip_comments\", True)` passed only by `_handle_forbid_tokens`); python/src/dotfiles_setup/verify.py:141,150,168-169 (the stripper lives in `forbid_tokens`); the file's own header states the consequence: python/verification/suites.toml:22-29 — \"**11** were held up by a COMMENT alone, 10 by a comment plus code, 12 by code alone.\""
native_alternative = "ast-grep (`github:ast-grep/ast-grep` 0.45.3, already pinned in this repo) — structural, parser-backed pattern matching that cannot be satisfied by a comment, a docstring, or a quoted mention. A `handler = \"ast_grep\"` for the Python paths would let a contract say `regressions($A, $B, families=$C)` as an AST pattern instead of a string, closing the class the header documents rather than restating the writing rules."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/ast-grep (pinned per this audit's REPO PINS: \"github:ast-grep/ast-grep\":\"0.45.3\"); the tool is declared in the repo's own toolchain, so adopting it adds no new dependency."
rationale = "Not a retire: `require_tokens` is still the right handler for markdown, TOML and YAML paths where prose IS the artifact, and for asserting a file exists at all. But for the ~20 Python call-site tokens in this unit the substring form is provably weaker than the check it claims to be, and the repo already ships the structural tool. Refactor = add an AST-backed handler and migrate the Python-path tokens; keep substring for the doc/config paths."
probe = "mise exec -- ast-grep run --lang python --pattern 'regressions($$$ARGS)' python/src/dotfiles_setup/lock_refresh.py  # control arm: the same pattern against a file where the call appears only in a docstring must return 0 matches while grep returns 1"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-4-2"
unit = "suites-4"
kind = "suites"
disposition = "refactor"
confidence = 0.80
custom_thing = "`orchestration.plugins-enabled` asserts the two orchestration plugins are ON by substring-matching `\"fable-orchestrator@fable-orchestrator\": true` inside the committed `.claude/settings.json`."
evidence = "python/verification/suites.toml:310-321 (suite body, `per_path_tokens = { \".claude/settings.json\" = [ \"\\\"fable-orchestrator@fable-orchestrator\\\": true\", ... ] }`); the asserted line is live at .claude/settings.json:147."
native_alternative = "`claude plugin list --json` (Claude Code 2.1.266 on this host) emits one record per plugin with `id`, `enabled` and `scope` AFTER settings precedence has been resolved — the authoritative answer to \"is this lane actually available\", which a substring in one settings file cannot give."
alternative_evidence = "Live probe on the host, both arms: `claude plugin list --json | jq` → `fable-orchestrator@fable-orchestrator enabled=true scope=project` and `antigravity@antigravity-for-claude-code enabled=true scope=project`, while sibling entries in the same output read `\"enabled\": false` (e.g. `accessibility-compliance@claude-code-workflows`), so the probe discriminates. Docs: /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/plugins-reference.md:1139 (`claude plugin list [options]`), :423 (`claude plugin disable` writes `\"<name>@synced\": false` into `enabledPlugins`); precedence order at /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/settings.md:49 — \"managed settings, command line, project local, shared project, user. A key set at a higher level overrides the same key set lower down.\""
rationale = "The hole is live, not hypothetical: `.claude/settings.local.json` (project-LOCAL, higher precedence than the shared project file this contract reads) already exists in this clone and already carries an `enabledPlugins` block (`{\"ponytail@ponytail\": false}`). Adding `\"fable-orchestrator@fable-orchestrator\": false` there would disable the implementation lane while this contract, `orchestration.trigger-armed` and `orchestration.mode-line-declared` all stay green — the exact inert-declaration class #354 was opened for, reproduced one precedence tier up. Keep the committed-file token (CI has no host state and the repo must still DECLARE the plugins), and add the resolved check where host state exists: a `doctor.toml` check shelling `claude plugin list --json`, the same reasoning `workflow.listing-budget-enforcement` already uses for host-only inputs."
probe = "claude plugin list --json | jq -r '.[] | select(.id|test(\"fable-orchestrator|antigravity\")) | \"\\(.id) \\(.enabled) \\(.scope)\"'  # then re-run with a temporary `\"fable-orchestrator@fable-orchestrator\": false` in .claude/settings.local.json and confirm it flips to false while `mise run verify` stays 0 failed"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-4-3"
unit = "suites-4"
kind = "suites"
disposition = "refactor"
confidence = 0.75
custom_thing = "`eval.skill-refs-resolve` binds `doc_refs.find_unresolved_skill_refs` / `declared_skills`, which enumerate skills from exactly one root."
evidence = "python/verification/suites.toml:360-384 (suite body); python/src/dotfiles_setup/doc_refs.py:79 — `\".claude/skills/*/SKILL.md\",` is the sole skill glob (control arm: `grep -n 'agents/skills' python/src/dotfiles_setup/doc_refs.py` → 0 hits, while the same command shape for `.claude/skills` → 3 hits)."
native_alternative = "Codex's documented repository skill locations, `$CWD/.agents/skills`, `$CWD/../.agents/skills` and `$REPO_ROOT/.agents/skills` — a second live skill root this repo already populates and which another member of this same unit (`workflow.codex-task-orchestration`) already binds."
alternative_evidence = "/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/build-skills.md:135 (\"Codex scans `.agents/skills` in every directory from your current working directory up to the repository root\") and :139-142 (the REPO/USER location table); /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/codex/customization__overview.md:126. Repo state: `git ls-files .agents | head` lists 37 tracked `.agents/skills/*/SKILL.md`; `diff -rq .claude/skills .agents/skills` reports 26 differing SKILL.md files plus `Only in .agents/skills: clear-prep` and `Only in .agents/skills: codex-task-orchestration`; python/verification/suites.toml:171 binds `.agents/skills/codex-task-orchestration/SKILL.md`."
rationale = "The resolver's own contract says every skill cited by name in an agent doc must exist on disk, but it can only see the Claude-side half of a two-harness skill surface. Two consequences, both live: a doc citing `codex-task-orchestration` (a real skill, Codex-loadable, bound by a sibling contract) resolves against nothing, and the 26-file drift between the two trees is asserted by no contract at all. The fix is native-shaped, not new machinery: parameterise the skill root over the documented Claude and Codex locations (`.claude/skills/*/SKILL.md` + `.agents/skills/*/SKILL.md`) so the resolver matches the harnesses that actually load them."
probe = "uv run --project python python -c \"from pathlib import Path; from dotfiles_setup.doc_refs import declared_skills; s=declared_skills(Path('.')); print('codex-task-orchestration' in s, len(s))\"  # expect False -> the Codex-only skill is invisible; control arm: 'session-review' in s must be True"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-4-4"
unit = "suites-4"
kind = "suites"
disposition = "refactor"
confidence = 0.55
custom_thing = "`workflow.codex-task-orchestration` pins a hand-rolled cross-session handoff protocol — \"canonical handoff bytes\", a frozen SHA-256 of a pre-freeze inventory, \"Read-only acknowledgment\", \"Relinquishment\", a separate \"Start signal\", and a `spawn_agent` agent-thread-limit branch — as 13 prose tokens in a skill plus 9 in an archived research report."
evidence = "python/verification/suites.toml:163-179 (`per_path_tokens` for `.agents/skills/codex-task-orchestration/SKILL.md` and `docs/research/runs/research-20260813-codex-session-communication/report.md`, incl. `\"cc4411af0b12b75daa23b82dbee63f646f31e3b354742d7961f08f3ecae81c30\"`); the report slug dates the underlying research to 2026-08-13."
native_alternative = "codex CLI native session communication shipped AFTER that research: `codex queue` — send messages to existing local or remote sessions (0.149.0); the interactive `codex agents` dashboard — search/start/open/rename/stop tasks (0.149.0); `@` mentions to reference other Codex tasks, and asking agents to read/create/message tasks from the terminal (0.150.0). Host runs codex-cli 0.152.1."
alternative_evidence = ".agent/kb/raw/codex-cli-releases-0.148-0.152.digest.md — \"capability: `codex queue` — send messages to existing local or remote sessions | version: 0.149.0\"; \"capability: Interactive `codex agents` dashboard … | version: 0.149.0\"; \"capability: `@` mentions to reference other Codex tasks; ask agents to read/create/message tasks from the terminal | version: 0.150.0\"."
rationale = "Not a retire — the digest is a release-note summary, so it does not establish that `codex queue` carries the ownership/relinquishment semantics this protocol needs, and the contract itself is honest that synthetic evals never certify live transport. But the protocol was designed against a transport snapshot three minor versions stale, and `use-tool-builtins.md` requires re-checking a native mechanism before KEEPING custom code, not only before writing it. Re-probe the three native verbs against 0.152.1 and either adopt them or record in the skill why they are insufficient; the contract's token set then binds whichever survives."
probe = "mise exec -- codex queue --help; mise exec -- codex agents --help  # then, if a target-session id and a message land, compare against the skill's Start signal / Relinquishment steps in .agents/skills/codex-task-orchestration/SKILL.md"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 2
survived = false
schedule = ""

[[finding]]
id = "suites-4-5"
unit = "suites-4"
kind = "suites"
disposition = "refactor"
confidence = 0.50
custom_thing = "`workflow.listing-budget-enforcement` binds a bespoke measurement of the skill + agent LISTING context cost (`listing_budget.collect_listing`, a `doctor.toml` `max_chars = 34000` ceiling, and a `doctor.py` CHECKS entry), built because `kb-setup md-budget` sweeps one repo's `git ls-files` and is blind to enabled third-party plugins and to `.claude/agents/*.md`."
evidence = "python/verification/suites.toml:68-84 (suite body and `per_path_tokens`, incl. `\"doctor.toml\" = [\"[listing]\", \"max_chars = 34000\"]` and `'(\"listing-budget\", check_listing_budget),'`)."
native_alternative = "`/skill-doctor` — Claude Code 2.1.261 — \"shows which loaded skills go unused and what they cost in context, so users can prune them.\" The host runs 2.1.266, so it is available now; it measures the same quantity (loaded-skill context cost, plugins included) from inside the harness rather than by re-deriving the load set from `~/.claude/plugins` on disk."
alternative_evidence = ".agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.digest.md — \"capability: `/skill-doctor` shows which loaded skills go unused and what they cost in context, so users can prune them. version: 2.1.261, anchor: `<Update label=\\\"2.1.261\\\" ...>` bullet \\\"Added `/skill-doctor`\\\"\". Control arm on the offline corpus: `grep -rn \"skill-doctor\" .../docs/claude-code/` → 0 hits, i.e. the capability postdates the offline snapshot and is known only from the gap digest."
rationale = "Partial coverage, not full. `/skill-doctor` is an interactive slash command with no documented machine-readable output and no ceiling, so it cannot BE the doctor check — the custom module still owns the numeric gate and the two defects it was built to catch (the 1,536-char description truncation and the mtime version-resolution bug). What it does supersede is the hardest half to keep honest: deriving which skills are actually LOADED. Adopt `/skill-doctor` (or its underlying data, if a non-interactive form exists at 2.1.266) as the load-set source and keep `collect_listing` as the ceiling; at minimum cross-check the two once, because two independent measurements of the same number is the cheapest bug detector available."
probe = "claude --help 2>&1 | grep -i 'skill-doctor'; claude -p '/skill-doctor' --output-format json 2>&1 | head -40  # establish whether a machine-readable form exists at 2.1.266 before wiring it"
correction = ""
correction_custom = ""
verify_votes = 3
refuted_votes = 1
survived = true
schedule = ""

[[finding]]
id = "suites-4-6"
unit = "suites-4"
kind = "suites"
disposition = "keep"
confidence = 0.80
custom_thing = "`eval.mise-task-refs-resolve` binds `doc_refs.declared_mise_tasks`, which hand-parses `mise.toml` + `.config/mise/conf.d/*.toml` with `tomllib` and hand-resolves the `alias` key, deliberately NOT calling mise."
evidence = "python/verification/suites.toml:334-358 (suite body); python/src/dotfiles_setup/doc_refs.py:374-401 (`declared_mise_tasks`: `configs = [repo_root / \"mise.toml\", *sorted(conf_d.glob(\"*.toml\"))]`, then `alias = body.get(\"alias\") ...`)."
native_alternative = "`mise tasks ls --json` — mise's own task enumerator, which natively reports an `aliases` array per task (verified: `stop <- [\"down\"]`, `sh <- [\"shell\"]`), i.e. it already does the alias resolution this module reimplements."
alternative_evidence = "Live probe on mise 2026.9.3, both arms. Positive: `mise tasks ls --json | jq '.[] | select(.aliases != null and .aliases != []) | \"\\(.name) <- \\(.aliases)\"'` → `stop <- [\"down\"]`, `sh <- [\"shell\"]`. Negative (the reason the module cannot switch wholesale): with the global config redirected — `MISE_GLOBAL_CONFIG_FILE=$T/none.toml mise tasks ls --json | jq '[.[].name] | length, (map(select(startswith(\"update:\"))) | length)'` → `92` and `5`, so the invoking user's five `update:*` tasks are STILL merged in. The contract's stated reason is therefore re-measured on the current mise, not inherited."
rationale = "Justified keep, and the justification was re-derived rather than trusted: the native enumerator cannot be scoped to this repo's tracked config on mise 2026.9.3 (`MISE_GLOBAL_CONFIG_FILE` does not suppress the user's global tasks — 5 `update:*` names survive), so using it would make a doc citing a task no file here defines resolve as valid, which is the precise failure the contract exists to prevent. It also keeps the check offline so it can ride the existing pre-commit step. The one native piece worth borrowing is the `aliases` field, but that is a convenience, not a correctness gap: the hand-rolled `alias` read is control-armed by `test_task_scanner_catches_a_planted_bad_ref` and the whole suite is green (`mise run verify` → 148 passed, 0 failed)."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-4-7"
unit = "suites-4"
kind = "suites"
disposition = "keep"
confidence = 0.60
custom_thing = "`ci.git-default-branch-env` enumerates seven workflow files by hand and requires the literal `GIT_CONFIG_KEY_0: init.defaultBranch` in each, with its own description recording the gap: \"this is an explicit list, so a NEWLY ADDED workflow is not covered\"."
evidence = "python/verification/suites.toml:241-257 (suite body: seven-entry `paths` list and the identical seven-entry `per_path_tokens`)."
native_alternative = "None found at the platform level. GitHub Actions has no org- or repo-scoped `env` that crosses `workflow_call` (the contract's own reason for being per-file), `actions/checkout` exposes no option to set `init.defaultBranch` for the `git init` it runs, and neither pinned GHA linter can express a cross-file 'every workflow must set X' rule: actionlint 1.7.12 has no custom-rule surface and zizmor 1.30.0's audits are fixed. The `GIT_CONFIG_KEY_0`/`GIT_CONFIG_VALUE_0` pair IS the native git mechanism and is already what the repo uses."
alternative_evidence = "No doc path or saved URL establishes a native alternative — this is a negative claim, so per rule 4 the settling probe is stated below rather than asserted. What IS established: the repo already adopted the native git-side mechanism (the token asserted is `GIT_CONFIG_KEY_0: init.defaultBranch`, git's documented env form), so only the ENUMERATION half is custom."
rationale = "Keep, because the mechanism being asserted is already the native one and nothing in the pinned toolchain can assert it across an open set of files. The residual risk is the one the description names: the coverage list is static while `.github/workflows/` is not, so a new workflow is silently uncovered — exactly the shape `bash_budget` closed with a tracked-set comparison (`.claude/rules/zero-bash-logic.md`, \"A stale entry (an allowlisted path no longer tracked) also fails, so the map can't rot\"). That is a repo-internal refactor, not a native adoption, so it does not change the disposition; it is the follow-up worth filing."
probe = "comm -23 <(git ls-files '.github/workflows/*.yml' | sort) <(uv run --project python python -c \"import tomllib,pathlib;d=tomllib.loads(pathlib.Path('python/verification/suites.toml').read_text());print('\\n'.join(sorted(p for s in d['suite'] if s['name']=='ci.git-default-branch-env' for p in s['paths'])))\")  # any output is a workflow the contract cannot see"
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-4-8"
unit = "suites-4"
kind = "suites"
disposition = "keep"
confidence = 0.65
custom_thing = "The `config.schema-vendor-drift` / `config.schema-vendor-directives-bound` pair: a bespoke `schema_drift` handler comparing recorded version+sha256 against local pins, plus a second suite that re-binds the three `#:schema` directive lines because \"its `schema_drift` handler never reads that field … so that listing reads as coverage of the `#:schema` directives while being purely decorative\"."
evidence = "python/verification/suites.toml:332-366 (both suite bodies); python/src/dotfiles_setup/verify.py:651-658 (`_handle_schema_drift`) and :660-670 (HANDLERS map)."
native_alternative = "None for the drift comparison: taplo 0.10.0 validates a TOML file against the schema a `#:schema` directive names but has no notion of whether that vendored schema matches the tool version this repo pins, and `check-jsonschema` 0.38.0 validates instances against a schema rather than a schema against a pin. The repo's stated reason for vendoring at all (taplo does not cache schemas between runs, so a remote directive makes `mise run lint` a per-run network dependency) is a taplo property with no native workaround."
alternative_evidence = "python/verification/suites.toml:334 records the taplo caching constraint that motivates vendoring; the two pinned JSON-schema tools are named in this audit's REPO PINS (`taplo`:`0.10.0`, `pipx:check-jsonschema`:`0.38.0`) and neither validates a vendored-schema-vs-tool-pin relation."
rationale = "Keep: version-and-hash drift between a vendored schema and the pin that produced it is a repo-specific relation no linter models, and the sibling suite closes the one hole the pair had. One correction owed to the record rather than a new defect: `config.schema-vendor-directives-bound`'s description calls the drift suite's `paths` listing \"purely decorative\", which overstates it — `run_suite` existence-checks every listed path for EVERY handler by default (`if entry.get(\"paths_required\", True):`, python/src/dotfiles_setup/verify.py:82), so those paths do assert the three config files still exist; what they do not assert is the directive CONTENT, which is precisely what the sibling suite now binds. Worth fixing the wording so a later reader does not delete the listing as dead weight."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""

[[finding]]
id = "suites-4-9"
unit = "suites-4"
kind = "suites"
disposition = "keep"
confidence = 0.85
custom_thing = "The unit as a whole — 39 contracts spanning gate wiring (env-dump, hk-builtins, branch-guard, ADR-0001, apt-pins, command-audit, memory-index, tool-currency), orchestration declarations, the codex lane producer/consumer, and the #650-#654 skill/task/library stacks."
evidence = "python/verification/suites.toml:1694-2492 (the members of this unit); `mise run verify` on this branch → `148 passed, 0 failed, 4 skipped`, rc=0, with the only SKIPs being the four `policy.*` human-only entries (verify log lines 91-94, 155-156)."
native_alternative = "No native mechanism covers the class these contracts occupy: asserting that a DECLARATION in one file is still OBSERVED by a mechanism in another (an hk step still shells the CLI, a settings.json hook still names the task, a truth table still binds its axes to the registry). hk gates content per file, pytest gates behaviour within a module, and neither can see a cross-file wiring edge — which is the #354 defect class these were built for."
alternative_evidence = ".agent/kb/raw/hk-releases-since-pin.digest.md — the only release newer than the 1.57.0 pin is v1.58.1, whose body reads verbatim \"A small documentation-only release. There are no changes to hk's runtime behavior\", so hk has shipped nothing that would absorb these checks. .agent/kb/raw/mise-releases-since-pin.digest.md — host mise 2026.9.3 is the newest published release, zero newer capabilities. .agent/kb/raw/graphify-readme-0.9.56.digest.md — 0 new capabilities."
rationale = "Required justification for the group: nothing native asserts a cross-file wiring edge, and the two tool families that could plausibly have grown into it (hk, mise) shipped no such capability since their pins. Equally important for this audit's stale-contract question: the whole suite is green with 0 failures, so no member of this unit is asserting a retired path or token — the answer is measured, not assumed. The specific weaknesses that remain are the four scoped above (substring vs AST for Python call sites, the settings-precedence hole under `orchestration.plugins-enabled`, the single-root skill resolver, and the static workflow enumeration), not the existence of the contracts."
probe = ""
correction = ""
correction_custom = ""
verify_votes = 0
refuted_votes = 0
survived = true
schedule = ""
