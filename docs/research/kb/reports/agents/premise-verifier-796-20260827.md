# premise-verifier report — spec-796 (retire the Codex-side hook only), 2026-08-27, session ad30e818 — SUPERSEDED

Brief: verify the PREMISES rows of `spec-796.md` (brief: `briefs-20260827/spec-796.md`) against the tree at `fix/791-writer-lease-recovery` HEAD `6755fd5`, with `.codex/hooks.json` still present. **Superseded before dispatch**: Ray then decided to retire the writer lease entirely (PR #797), so the spec this verifies was never implemented. Persisted verbatim per `.claude/rules/agent-report-persistence.md`.

---

PREMISE REPORT
ROWS: 12 checked — 9 CONFIRMED (0 provenance corrected) / 1 REFUTED / 0 UNVERIFIABLE / 2 ASSUMED (2 checkable)

L1 — CONFIRMED — `.codex/hooks.json:1-31`: exactly two blocks (`PreToolUse` :4-16, `PostToolUse` :17-29), both `"matcher": "^(Bash|Edit|Write)$"`, both an inline `/usr/bin/python3 -I -S -c 'exec("""…""")'` ancestor walk over `pathlib.Path.cwd().resolve()` that exec's the located `scripts/writer-lease-hook-runner.py`; the only other keys are `description`, `timeout`, `statusMessage`. Pre and Post command strings are byte-identical.
L2 — CONFIRMED — `.gitignore:53-55`: `# Codex CLI temporary state` / `.codex/*` / `!.codex/hooks.json`.
L3 — CONFIRMED — every anchor read fresh: spec `:119`, `:129`, `:160`, `:167` (`T["Codex or Claude task"]`), `:168` (`ROOT[…]`), `:255`, `:256`, `:260`, `:273`; rule `:85`, `:96`; agents doc `:71`, `:74`; SKILL.md `:180`, `:184`.
I1 — CONFIRMED — `scripts/writer-lease-hook-runner.py`: `_deny` :25-36 (Post → `{"continue": false, "stopReason": …}`; else `hookSpecificOutput/permissionDecision: deny`); `_runtime` :83-123 walks `(current, *current.parents)` of `payload["cwd"]` (:87, :89), `_open_root` :43-54, `_open_relative` :57-80, `.git`/runner/hook checks :94-104, messages :121/:122; `main` :173-194 → `_deny(event, f"Writer lease enforcement failed closed: {exc}.")` :190, `return 0` :191; `_invoke` :133-167, venv candidates :137-140, hook path :147, `env={"LC_ALL": "C", "PATH": "/usr/bin:/bin"}` :159.
I2 — CONFIRMED — `.claude/settings.json:40-50`, `:82-92`, `:94-104`: three identical `"command": "/usr/bin/python3 -I -S \"${CLAUDE_PROJECT_DIR:-.}/scripts/writer-lease-hook-runner.py\""`, matcher `Bash|Edit|Write|NotebookEdit`, `"timeout": 10`.
I3 — CONFIRMED — `.codex/hooks.json` reads at tests/test_writer_lease.py:1229, 1303, 1358, 1420, 1505, 1595-1597, 1683; every run is `subprocess.run(["/bin/sh", "-c", command], cwd=<nested>, input=…)` (:1246, :1323, :1370, :1432, :1516, :1609, :1694); :1231 reads the Post command and :1232 asserts `pre_command == post_command`.
I4 — CONFIRMED — required by `_runtime` :100-104, executed at :147; tokens at suites.toml:2148 and in the :2155 map.
P1 — CONFIRMED — tests assert `returncode == 2`, `stdout == b""`, `stderr == b"Writer lease tracked runtime is ambiguous.\n"` (:1440-1442) and `…unavailable.\n"` (:1703-1705); the literals come from `.codex/hooks.json:10/23` (`message="Writer lease tracked runtime is ambiguous." if roots else "…unavailable."`, `raise SystemExit(2)`). Data match holds: same fixture dirs, same payloads.
P2 — REFUTED — the token list is right, the COUNT is not. `python/verification/suites.toml:2155` binds **ten** `def test_…(` names for `tests/test_writer_lease.py`, not nine (adds `def test_drain_clears_a_leaked_entry_by_id_and_unblocks_recovery(` alongside the nine). All other per-path token sets in the row verified verbatim at :2139-2155, including the two `.codex/hooks.json` `statusMessage` tokens at :2143/:2155. T3's five-name list is correct.
E1 — CONFIRMED — no new emission: the tests assert `_deny`'s existing JSON (I1) and docs are prose only.
A1 — ASSUMED (checkable) — I diffed both walks: `.codex/hooks.json:10` and `_runtime` :83-123 apply identical admission (`O_NOFOLLOW`, inode rebind, `.git` + runner + hook entrypoint, exactly-one). Caveat named below.
A2 — ASSUMED (checkable) — `_runtime` uses `payload["cwd"]` (:87), the locator used `Path.cwd()`; all seven tests set `subprocess cwd` and payload `cwd` to the same `nested` (pairs cited under I3). Retargeting cannot change coverage on that axis. Consequence for hunt (3): the runner locates independently of its own path, so the two-complete-runtimes fixture (:1407-1419) still yields the ambiguous branch.

MISSING:
- **The rule file's `PostToolUseFailure` contract token sits INSIDE the paragraph T4 orders rewritten.** T4 asserts the three `.claude/rules/writer-lease.md` tokens "are elsewhere — do not disturb them", but `` `PostToolUseFailure` drains failed tools `` is at `.claude/rules/writer-lease.md:98`, inside the `:96-104` block T4 directs to be reworded (other two at :1 and :108). Architect must state the token survives verbatim at :98, or `mise run verify` fails at the lane's commit.
- **T2's runner failure shape — verified, with one un-stated consequence.** For an ambiguous/absent runtime, `_runtime` raises `RunnerError` before `_invoke`, `main` catches it (:183-190) and emits exit 0, stderr empty, stdout one line: `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"Writer lease enforcement failed closed: hook cwd has ambiguous complete writer-hook runtimes."}}` (`json.dumps(..., separators=(",", ":"), sort_keys=True)` :36) — the `RunnerError` text reaches `permissionDecisionReason` verbatim with a trailing `.`. Consequence not stated: because the fail-closed path never reaches `_invoke`, the "decoy hook entrypoint never executed" marker assertion T2 prescribes **can only pass in the fail-closed arm** — it is a check with one face there; its only real arm is the existing control (decoy deleted). Architect should say so, or bind the marker assertion to the control arm only.
- **Two of the five "retarget cleanly" tests lose a property, and nothing else asserts it.** Today `selects_inner_runner_beneath_unrelated_outer_repo` (:1337) and `symlink_components_cannot_redirect_pre_post_lifecycle` (:1531) also prove *which runner file's bytes execute* (the locator exec'd the admitted inner copy). With the runner named in argv that becomes true by construction. The docstring-level property (the decision comes from the inner runtime's venv + hook + lease state, `_invoke` :134-158) survives. T1's "without changing what they prove" needs that caveat, and the doc sentences it backs (spec :134-135, rule :91-92) become unasserted.
- **Untruthful Codex-hook prose outside T4's edit list.** `docs/specs/codex-writer-lease.md:151-153` ("Codex 0.147's unified-exec handler emits no early PostToolUse…the later `write_stdin` completion emits the original Bash tool-use ID") and `:302-304` ("The final acceptance arm launches real subscription-authenticated Codex from an independent clone: a hostile session must be denied before bytes change") both assert Codex hook enforcement and are not in T4. Same shape in `.claude/rules/writer-lease.md:102-104` (Codex `write_stdin` publishes the ID) — T4 only names :85-94 and :96.
- **Doc contract tokens T4 does not name (hunt 4).** The complete suites.toml:2155 sets are: spec doc **4** tokens (T4 names 2; `fcntl.flock(fd, LOCK_EX | LOCK_NB)` and `Immutable content-addressed 64-event chunks` at :180 are unnamed but outside the edit regions), agents doc **2** (T4 names 1; the mermaid edge `G --> WL["Successor acquires Git-common-dir writer lease"]` unnamed, outside :69-75), SKILL.md **3** (all named), rule **3** (see first item). Architect should record the full four sets so the lane checks all of them.
- **Hunt (5): no other consumer.** Grep over the tree (excluding `docs/research/**`): `.codex/hooks.json` appears only in `.gitignore:55`, `suites.toml:2143/:2155`, the seven test reads, and `docs/specs/codex-writer-lease.md:119`. **Nothing in `hk.pkl`, `.github/**`, `mise.toml`, or `tests/test_codex_lane_e2e.py`.** `mise run token-check` exists (`mise.toml:1173`). Non-blocking; recorded so the architect need not re-probe.
- **Hunt (2): `/bin/sh -c` semantics lose nothing.** Both forms inherit the pytest env and set `cwd=nested`; the command is a single pre-quoted argv, `-I -S` isolates the interpreter either way; `_invoke`'s env is fixed at :159 and its venv lookup uses `pwd.getpwuid` (:136), not `$HOME`. Non-blocking residual.
- **T4's `hook_decision` bootstrap claim is true and unrowed.** `writer_lease.py:2183-2198` exempts a matching bootstrap `Bash` command purely from payload fields — caller-agnostic, so "any caller of the runner" holds. Non-blocking.

VERDICT: correct the spec first — P2's row miscounts the contract-bound test names (ten, not nine), and T4's "the rule's three tokens are elsewhere" is false for `` `PostToolUseFailure` drains failed tools `` at `.claude/rules/writer-lease.md:98`, which sits inside the paragraph the lane is told to rewrite; both are cheap edits. Residuals I judge non-blocking if accepted on the record: A1/A2 (both settled by reading, with the byte-admission caveat named), the hunt-2 and hunt-5 findings, and the `hook_decision` item.

---

Architect disposition: superseded — the whole subsystem was retired instead (PR #797); no correction was applied. Kept because hunt (5) is a complete consumer inventory of `.codex/hooks.json` at that commit.

## GitHub repos touched

_None._ (local tree only)
