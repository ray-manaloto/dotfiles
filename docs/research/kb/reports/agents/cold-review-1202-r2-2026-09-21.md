# Cold review — `b9bae81..54798c5` (round 2)

Resolved refs (full SHAs):

| role | SHA | subject |
|---|---|---|
| base (PRIOR) | `b9bae811e409b4b2720cd5e770772d4ffff12632` | `chore(claude-code): pin 2.1.278 (#1224)` |
| mid | `b7a3b1df` | `fix(claude-doctor): refresh enforcing verdicts in-session` |
| head (NEW) | `54798c574d4a7d1146310b10e35840bf3179875a` | `fix(claude-doctor): preserve established deny verdicts` |

Worktree tracked files == `54798c5` (`git diff --stat 54798c57 -- .` empty); only untracked
agent reports differ. Baselines on scratchpad copies: bun harness `{"arms":40}` rc=0,
`pytest tests/test_claude_doctor.py` 74 passed rc=0.

One `mise run graphify-query` attempt was made and failed (`mise ERROR No version is set
for shim: timeout` — the repo's `timeout` shim is broken); the rest is read from source.

`memory: local` was available and consulted (`.claude/agent-memory-local/cold-reviewer/`).

## HookBudget — NOT re-raised

`claude-code.d.ts:4173-4175` states the clock "stops while a `next(e)` call or any `$` call
of the hook's is in flight (a `$.clock` wait excepted)". The 90 s `timeoutMs` on
`$.process.run` (`register.ts:128`) is therefore not charged against `HookBudget.ms`
(10 000, `claude-code.d.ts:4180-4189`). My own round-1 finding on that axis was wrong and is
corrected in memory. Not raised again here.

## Findings

| # | Sev | Claim | file:line | Evidence |
|---|---|---|---|---|
| F1 | MEDIUM | `parseDoctorReport`'s four shape checks have **zero** guarding tests; three of them are load-bearing. All four can be deleted and the 40-arm bun harness stays rc=0. | `.claude/skills/claude-doctor/hooks/register.ts:81-117` | CITED — mutation rows R2–R7 all SURVIVED the full harness (below). Cause: every malformed payload in the fixture (`tests/fixtures/claude_doctor_hook/harness.ts:463-474`) is also **non-establishing**, so `isEstablishedRefresh` (`register.ts:259-266`) answers first. Right-answer/wrong-reason. Probe arm A/B on malformed-**and**-establishing payloads flips R3/R4/R5 from DENIED to CLEARED. |
| F2 | MEDIUM | The validator is inconsistent in the permissive direction: it REJECTS a non-string `baseline_path` and a non-boolean `disabled_by_baseline`, but silently coerces a non-boolean `enforcement_eligible` — the one field that decides enforcement. | `register.ts:112` vs `:101-108` | CITED — refresh probe: `{verdict:"ok", enforcement_eligible:1}` → `pass (deny CLEARED)`, while `{verdict:"ok", disabled_by_baseline:"yes"}` → `DENY (deny held)`. |
| F3 | LOW | Two of the four validator clauses are dead code. `Array.isArray(value)` (`:82`) is subsumed by the verdict allow-list (an array's `.verdict` is `undefined`); the `disabled_by_baseline` type check (`:104-108`) is subsumed by the `=== true` constructor at `:113`. | `register.ts:82`, `:104-108` | CITED — probe arm B: removing either changes **no** cell of the 8-payload probe. |
| F4 | MEDIUM | New enforcing state whose direct repair is not permitted: widening the pin `except` to `KeyError`/`TypeError` turns a malformed `schemas/sources.toml` into `_PinState.UNKNOWN` → `Verdict.INVALID` → deny-everything, and `Edit`/`Write` of `schemas/sources.toml` is **not** on the permit list (only `doctor.toml` is). | `python/src/dotfiles_setup/claude_doctor.py:310`; permit at `register.ts:216-250` | CITED — `load_sources` does `row["tool"]` (KeyError) and iterates `data.get("schema", [])` (TypeError if `[schema]` is a table), `python/src/dotfiles_setup/schema_vendor.py:469-484`. At `b9bae81` those propagated → CLI crash → hook cached `null` → nothing enforced. Escape hatches remain: Edit `doctor.toml` → `enabled=false`, or any Bash token whose basename is `claude`/`mise`/`uv`. |
| F5 | LOW | The baseline permit is steerable: an arbitrary absolute path in the subprocess JSON permits `Edit`/`Write` of **that** file while the gate denies everything else, and simultaneously refuses the real session-root `doctor.toml`. | `register.ts:225` | CITED — path probe: `baseline_path steered to an unrelated file` → PERMITTED; `baseline_path steered, root doctor.toml now refused` → DENIED. **Not an escalation**: the same JSON could emit `verdict:"ok"` and disarm the gate outright, which is strictly more power. |
| F6 | LOW | A `doctor.toml` that is a symlink makes the documented off-switch un-editable in-session — the only `Edit` escape hatch closes, and the module contract does not say so. | `register.ts:243-244`; contract at `:275-303` | CITED — path probe: `baseline_path is a symlink to another file` → DENIED, and `… target is its real file` → DENIED. Deliberate (harness arm `:294-311` pins it), but undocumented in the permit contract. |
| F7 | LOW | The refresh is one-directional: it can CONFIRM or CLEAR a deny, never ESTABLISH one, because `refreshCachedReport` sits **below** the `if (!isEnforcementEligible(cachedReport)) return result;` guard. A session whose SessionStart check failed transiently is unguarded for its whole life. | `register.ts:364-387` | CITED — **PRE-EXISTING**: base had no refresh at all. Named because commit 1's subject ("refresh enforcing verdicts in-session") describes only half the axis. |
| F8 | LOW | A rejecting `$.clock` removes the refresh rate limit entirely: the catch arm refreshes with no reuse-window bookkeeping, so every subsequent deny-path call re-spawns `uv run … claude-doctor`. | `register.ts:385-387` | CITED — `harness.ts:498-508` asserts `spawned()==3` across two calls on the catch arm. Not a budget risk (see HookBudget note); an unbounded spawn rate. |
| F9 | LOW | `UnicodeDecodeError` escapes `load_baseline`: a non-UTF-8 `doctor.toml` raises a `ValueError` subclass that `except OSError, tomllib.TOMLDecodeError` does not catch, so the CLI exits with a traceback and empty stdout → at SessionStart the hook caches `null` and the gate is off for the session. | `claude_doctor.py:576-579` | CITED — measured on a scratch copy: `UTF8-BAD RAISED: UnicodeDecodeError`. Control arms on the same function: a directory `doctor.toml` → `(True,'native',…)` (IsADirectoryError is an OSError, caught); `enabled=false` → `(False,'native',…)`. The `except` line is **PRE-EXISTING** (`13104cd9`, #1047); this range moved the `read_text()` onto the new `baseline_path` and left the gap. |
| F10 | INFO | `placed()` never inspects `kind`, so a directory satisfies the permit; the production `HookServices.fs.stat` return type drops `kind`/`size`/`mtimeMs` from `FsStat`, so the information is not even available. | `register.ts:52-55`, `:188-214`; `claude-code.d.ts:4071-4102` | CITED — path probe: `baseline_path is a directory` → PERMITTED. Harmless in practice (Edit of a directory errors). |
| F11 | INFO | fs fake fidelity: the harness fake now matches `FsStat` on the dangling-symlink case my round-1 review flagged. One documented case remains unmodelled — `realPath` absent **while the path leads somewhere** ("a hook above withheld it"). | `harness.ts:106-128` vs `claude-code.d.ts:4084-4101` | CITED — fake: `lstat` throws ENOENT for a missing path (matches "rejects `ENOENT`"); dangling link → `{kind:"other", isLink:true, realPath:undefined}` (matches `:4082-4084`, `:4093-4095`), named explicitly at `harness.ts:313-332`. Production handles the withheld-`realPath` case by denying; nothing tests it. |
| F12 | INFO (positive) | The validator fixes a real base defect: at `b9bae81` a malformed report **threw** out of the SessionStart handler. | `register.ts@b9bae81:202` | CITED — matrix: `malformed: 42` reads `THROW` at PRIOR (`TypeError: Spread syntax requires ...iterable not be null or undefined`) and `pass` + an honest "check could not run" line at NEW. |
| F13 | INFO (positive) | `.agents` ↔ `.claude` register.ts parity is now machine-enforced; nothing enforced it before. | `tests/test_claude_doctor_hook.py:42-44` | CITED — `cmp` confirms byte-identical today. `python/verification/suites.toml:2773` still names only the `.claude` copy. |

## PRIOR (`b9bae81`) vs NEW (`54798c5`) — every deny/pass state

Measured by running both `register.ts` versions under the same injected services
(`scratchpad/mut/matrix.ts`), SessionStart then one PreToolUse, refresh re-serving the same
report so the table reads steady state. `THROW` = the handler threw (fails open, hook absent).

| cached report | Read | AskUserQuestion | Bash `git status` | Bash `claude install latest` | Edit root `doctor.toml` | Edit `other.toml` | Task |
|---|---|---|---|---|---|---|---|
| `null` (check could not run) | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass |
| `ok` | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass |
| `unknown` | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass |
| `drift` | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass |
| `invalid` | pass / pass | pass / pass | **DENY / DENY** | pass / pass | **DENY / pass** ⬅ | **DENY / DENY** | **DENY / DENY** |
| `invalid` + `eligible:false` | pass / pass | pass / pass | **DENY / DENY** | pass / pass | **DENY / pass** ⬅ | **DENY / DENY** | **DENY / DENY** |
| `invalid`, no `eligible` key | pass / pass | pass / pass | **DENY / DENY** | pass / pass | **DENY / pass** ⬅ | **DENY / DENY** | **DENY / DENY** |
| `unknown` + `eligible:true` | pass / pass | pass / pass | **pass / DENY** ⬅ | pass / pass | pass / pass | **pass / DENY** ⬅ | **pass / DENY** ⬅ |
| `unknown` + `disabled_by_baseline` | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass |
| malformed `42` | **THROW / pass** ⬅ | **THROW / pass** ⬅ | **THROW / pass** ⬅ | **THROW / pass** ⬅ | **THROW / pass** ⬅ | **THROW / pass** ⬅ | **THROW / pass** ⬅ |
| malformed, unknown verdict string | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass | pass / pass |

### Refresh path — what clears an established `invalid` deny (NEW only; PRIOR has no refresh, so every cell is DENY)

| refresh payload | outcome |
|---|---|
| `null` / spawn failed | DENY (held) |
| `ok` | **pass (CLEARED)** |
| `drift` | **pass (CLEARED)** |
| `unknown` | DENY (held) |
| `unknown` + `disabled_by_baseline:true` | **pass (CLEARED)** |
| `invalid` | DENY (held) |
| `invalid` + `eligible:false` | DENY (held) |
| `42` | DENY (held) |
| `[]` | DENY (held) |
| bad verdict string | DENY (held) |
| bad verdict + `disabled:true` | DENY (held) |
| `ok` + non-string finding | DENY (held) |
| `ok` + numeric `baseline_path` | DENY (held) |
| `ok` + `disabled_by_baseline:"yes"` | DENY (held) |
| **`ok` + `enforcement_eligible:1`** | **pass (CLEARED)** ⬅ F2 |
| bare `{}` | DENY (held) |
| `ok`, `findings` absent | DENY (held) |

## Enforcing → non-enforcing flips, BY NAME

1. **`Verdict.DRIFT` (stale `schemas/sources.toml` pin).** At `b9bae81`, `pin_findings` fed
   `failed_assertion` → `INVALID` → **deny**. Now a positively-established stale pin is
   `DRIFT`, `enforcement_eligible` False → **pass**. Introduced by commit 1
   (`claude_doctor.py:513-528`), attributed in-diff to "the 2026-09-21 operator ruling".
   Pinned by mutation row `P8` (CAUGHT).
2. **`Verdict.UNKNOWN` beats `DRIFT` for a non-version running value.** Commit 2 reordered
   so `not running_is_version` wins over pin `DRIFT` (`claude_doctor.py:520-527`). Both
   states are non-enforcing, so **no enforcement change** — prose only. Row `P2` (CAUGHT).
3. **`invalid` + `Edit`/`Write` of the baseline `doctor.toml`.** DENY at PRIOR, pass at NEW,
   for all three `invalid` shapes. This is `isDoctorTomlRepair` (`register.ts:216-250`), new
   in commit 1 and retargeted onto python's `baseline_path` in commit 2.
4. **Malformed report at SessionStart:** `THROW` → `pass`. A throw already failed open, so
   the permitted set did not widen; the honesty of the reported state did improve (F12).

## Widenings of what is PERMITTED while denying, BY NAME

1. **`isDoctorTomlRepair`** (`register.ts:216-250`) — `Edit`/`Write` of the baseline
   `doctor.toml`. Because `enabled = false` now clears the deny on the very next refresh,
   this is also a **new in-session disarm**: at `b9bae81` the documented off-switch could
   not take effect without a session restart.
2. **`cachedReport?.baseline_path`** (`register.ts:225`) — moves that permit's target from
   the session root onto untrusted subprocess output (F5).
3. **`isEstablishedRefresh`'s `disabled_by_baseline` arm** (`register.ts:264`) — a report may
   clear an established deny purely on that flag, independent of its verdict.

## New DENY states (widening in the other direction)

1. **`ok` / `unknown` / `drift` + `enforcement_eligible: true` now denies**
   (`register.ts:255`). At PRIOR only `verdict === "invalid"` denied. Unreachable from
   today's python — `DoctorVerdict.enforcement_eligible` is `verdict is Verdict.INVALID`
   (`claude_doctor.py:166-178`), pinned by row `P8` (CAUGHT) — but it is a hook-side
   contract widening one python edit away from live.
2. **`_PinState.UNKNOWN` from `KeyError`/`TypeError` → `INVALID`** (F4).

## Mutation results — scratchpad copies only, repo tree never touched

`register.ts` rows run the production bun harness (`tests/test_claude_doctor_hook.py::test_claude_doctor_hook_behaviour_under_bun`); `claude_doctor.py` rows run `tests/test_claude_doctor.py`. Anchor uniqueness asserted (`ORIG.count(old) == 1`); no row was skipped.

| row | mutation | result | guarding test |
|---|---|---|---|
| R1 | revert `parseDoctorReport(JSON.parse(...))` to base `JSON.parse(...) as DoctorReport` | CAUGHT | bun harness, malformed-SessionStart arm `harness.ts:477-483` (throws) |
| R2 | drop `\|\| Array.isArray(value)` | **SURVIVED** | none (dead code, F3) |
| R3 | drop the verdict allow-list | **SURVIVED** | none — load-bearing per probe (F1) |
| R4 | drop the findings element-type check | **SURVIVED** | none — load-bearing per probe (F1) |
| R5 | drop the `baseline_path` type check | **SURVIVED** | none — load-bearing per probe (F1) |
| R6 | drop the `disabled_by_baseline` type check | **SURVIVED** | none (dead code, F3) |
| R7 | `record.enforcement_eligible === true` → `Boolean(...)` | **SURVIVED** | none (F2) |
| R8 | ignore python's `baseline_path` (commit-1 behaviour) | CAUGHT | `harness.ts:366-400` "Python's reported baseline path wins" |
| R9 | drop the INVALID floor from `isEnforcementEligible` | CAUGHT | `harness.ts:485-494` "INVALID cannot be talked down" |
| R10 | `isEstablishedRefresh` no longer accepts `ok` | CAUGHT | `harness.ts:427-438` positive-answer loop |
| R11 | … no longer accepts `drift` | CAUGHT | `harness.ts:427-438` |
| R12 | … no longer accepts `disabled_by_baseline` | CAUGHT | `harness.ts:427-438` |
| R13 | drop the `isEstablishedRefresh` guard (commit-1 bug) | CAUGHT | `harness.ts:440-449` `unknown()` case |
| R14 | drop the `now < lastRefreshAtMs` backward-clock clause | CAUGHT | `harness.ts:510-521` |
| R15 | catch arm no longer refreshes | CAUGHT | `harness.ts:496-508` |
| R16 | deny message drops `cachedReport?.findings` | **SURVIVED** | none — `assertDenied` (`harness.ts:199-202`) never asserts deny CONTENT |
| P1 | narrow pin `except` back to `(ValueError, OSError)` | CAUGHT | `tests/test_claude_doctor.py` |
| P2 | revert the verdict ordering to commit-1 | CAUGHT | `tests/test_claude_doctor.py` |
| P3 | unreadable pin no longer `INVALID` | CAUGHT | `tests/test_claude_doctor.py` |
| P4 | `baseline_path` points elsewhere | CAUGHT | `tests/test_claude_doctor.py` |
| P5 | disabled report drops `disabled_by_baseline` | CAUGHT | `tests/test_claude_doctor.py` |
| P6 | disabled report drops `baseline_path` | CAUGHT | `tests/test_claude_doctor.py` |
| P7 | drop the `replace(verdict, baseline_path=…)` stamp | CAUGHT | `tests/test_claude_doctor.py` |
| P8 | make `DRIFT` enforcement-eligible | CAUGHT | `tests/test_claude_doctor.py` |

Score: python **8/8 caught**; register.ts **10/16 caught, 6 survived** — and every survivor is
inside `parseDoctorReport` or the deny message, i.e. exactly the surface commit 2 added.

### Probe control arms

Validator probe, unmutated arm — both controls discriminate: well-formed `ok` → CLEARED,
well-formed `invalid` → DENIED. Mutated arm, `CLEARED` sets:

```
R2 -> [ok control]                     (no change; dead code)
R3 -> [bad verdict + disabled, ...]    (load-bearing)
R4 -> [ok + non-string finding, ...]   (load-bearing)
R5 -> [ok + numeric baseline_path,...] (load-bearing)
R6 -> [ok control]                     (no change; dead code)
R7 -> [ok control only]                (strict === true is the MORE permissive choice)
```

Path probe control arms: `absent baseline_path -> session root doctor.toml` → PERMITTED and
`absent baseline_path -> nested doctor.toml` → DENIED, so the probe discriminates on placement.

## Recommendations

1. Add harness arms for malformed-**and-establishing** payloads (`{verdict:"ok", findings:["a",7]}`,
   `{verdict:"garbage", disabled_by_baseline:true}`, `{verdict:"ok", baseline_path:7}`).
   Without them `parseDoctorReport` is untested and R3/R4/R5 are silent no-ops waiting to happen.
2. Make `enforcement_eligible` validation consistent with its siblings: reject a non-boolean
   rather than coercing it (F2).
3. Either delete the two dead clauses or give them a payload that reaches them (F3).
4. Decide whether a malformed `schemas/sources.toml` should deny-everything; if yes, add it to
   the permit list alongside `doctor.toml` (F4).
5. Assert deny-message content in `assertDenied` so R16 cannot survive (the findings are the
   operator's only diagnostic on the deny path).

## GitHub repos touched

_None._
