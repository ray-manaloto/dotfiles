# Audit: overlooked/deferred/unhandled issues in this session (2026-09-14)

Scope: live session transcript `~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/6c26be81-8260-48af-b7c8-58eee272c87c.jsonl`
(STILL BEING WRITTEN at audit time — this is a snapshot, not a final record),
`findings.md` §C1–C28, `progress.md` tail, all `docs/research/kb/reports/agents/goalrev-*`,
`res-*-2026-09-1[34].md`, `goal-synthesis-2026-09-13.md`, current git status/diff, and
live `gh issue view` for every ticket named below. Read-only throughout; no source
edited, no git/gh mutated, `.agentsview/config.toml` not read, no credential value
printed.

## Method note

Most items on the operator's seed list turned out to be **findings the session's
own multi-round review already caught and corrected in `findings.md`** (that file
is itself a chain of self-corrections — C14 corrects C4/C5, C16 corrects C1, C17
corrects C16, C20 corrects C15, C23 corrects the brief that spawned it). So "HANDLED"
below means *diagnosed, documented, and — where actionable — fixed in the working
tree*; it does **not** always mean *committed* or *closed as a ticket*. That gap
(diagnosed-but-not-closed) is itself the largest class of unhandled risk found.

## Findings table

| # | Finding | Evidence | Status | Recurrence risk |
|---|---|---|---|---|
| 1 | `agentsview --version 2>&1 \| head -3` printed rc=0 while real rc=1 (`head` supplied the exit code) | `findings.md:4684` (C2) | HANDLED (caught same-turn, re-run without pipe) — but **no gate change**: `long-running-command-hangs.md` rule 3 only denies pipe-to-tail on GATE commands via `hook_guard`; a diagnostic probe like this one is explicitly uncovered | **UNHANDLED as a class** — will recur on the next ad-hoc diagnostic pipe; no issue filed |
| 2 | `findings.md` C1 claimed the azure-cli `uvx_args` arm "no longer reproduces" | C1 (`findings.md:4665`), refuted by C16 (`findings.md:5137`): "C1 is hereby corrected: it did not reproduce only because mise never got that far. A masked failure is not a fixed one." | HANDLED (self-corrected 2 rounds later, root cause eventually traced to mise's own registry in C17) | Low — traced to source, fix applied (uncommitted, see #13 below) |
| 3 | agentsview secrets count was a `--limit 500` cap artifact (353 → 649) | C8 (`findings.md:4846`) | HANDLED — re-paginated, corrected, 2 rule classes recovered (openai-key 25, anthropic-key 24) | Low for this session; **no client-side dedup module shipped** (see #14) so the undercounting risk recurs on the next ad-hoc `secrets list` without `--cursor` |
| 4 | `.agent/telemetry/` reported "dead"; actually the writer relocated to `~/.agentsview/sessions.db` | C14 (`findings.md:5077`) self-corrects C4/C5's "dead instrument" framing | HANDLED (diagnosis) | **UNHANDLED (prevention)** — no doctor check reads `~/.agentsview/sessions.db` liveness; `.agent/telemetry/`-reading code (if any exists) still points at a dead branch. res-instruments proposed a doctor check; not implemented (see #14) |
| 5 | Claim that `locked_scopes` could narrow the `lockfile` setting | C23 (`findings.md:5347`): "`locked_scopes` is scoped to the `locked` key, not `lockfile`. Global-or-nothing." | HANDLED — self-corrected before it reached an implementation | None |
| 6 | Nobody checked WHEN the failing mise code landed | C20 (`findings.md:5285`): shipped in mise **2026.9.7**, ~24h before this session hit it; **2026.9.6 predates it entirely** | HANDLED (diagnosis, retroactively) — but **the zero-workaround alternative (pin mise back to 2026.9.6) was never put to the operator**, per C20's own text: "the choice was never put to the operator because nobody checked the release date." Three `uvx = false` patches were applied instead, without that alternative ever being weighed | **UNHANDLED decision gap** — a real fork-in-the-road was resolved by default, not by choice |
| 7 | `timeout` is a broken shim on this host, silently breaks probes | C18 (`findings.md:5206`): `timeout 300 mise run …` → rc=1 "No version is set for shim: timeout" | HANDLED per-occurrence (re-run without `timeout`) each time it bit | **UNHANDLED as a class** — no issue filed, no doctor check for a broken `timeout` shim; will bite the next session that reaches for it (feedback memory doesn't exist for this specific shim per grep below) |
| 8 | Background-task notifications reported "exit code 0" over a real failing rc — at least 3 times this session | C24 (`findings.md:5368`): "The background-task notification LIED for the third time this session"; `progress.md` tail: "task notification lied THREE times this session" (ship attempt 1, land attempt 1, and a third) | HANDLED per-occurrence (real log always read) | **UNHANDLED as a class** — this is a recurring, cross-session pattern (memory `feedback_background_task_notification_can_lie` already exists) with no mechanical fix; purely procedural discipline is the only guard |
| 9 | `graphify-skill-install -- agents` overwrote a SKILL.md the repo deliberately keeps as a stub | C24 (`findings.md:5368`): caught by `test_skills_mirror.py` failures, SKILL.md reverted, version stamp kept | **HANDLED** (reverted, tests green) | Low for this instance, but the lesson ("`graphify-skill-install` is NOT safe to run for every platform") lives only in `findings.md`, not yet promoted to the `graphify-skill-install` skill/rule itself — next session can repeat it |
| 10 | One graphify version fact lives in 5 files / ~26 occurrences | C19 (`findings.md:5241`) | HANDLED for this bump (each site found via its own failing gate, all green after) — **not committed** (see #13); the underlying 5-site duplication itself is unaddressed (documented as "by design", not simplified) | Moderate — every future bump repeats this multi-file hunt; acknowledged as intentional but still a manual-hunt tax |
| 11 | Issues #1049 and #1050 are fixed in code but still open as tickets | C9 (`findings.md:4893`), confirmed live via `gh issue view`: both **OPEN**, `updatedAt` 2026-09-13T22:27 (before this audit, so no comment/close since) | **UNHANDLED** — findings.md explicitly says "should close or narrow"; neither ticket has been touched | High — stale tickets will mislead the next triage pass into re-investigating already-closed work |
| 12 | 159 guard fail-opens recorded, most recent `2026-09-14T02:13:05Z` (today), nothing reads that number | C5 (`findings.md:4750`): ranked as the corrected "runner-up" candidate, should rank HIGHER than the lane placed it | **UNHANDLED** — verified via `gh issue list --search "fail-open"`: no ticket exists for a fail-open counter/dashboard (closest is #1031, "detect a hook that stopped running" — a different, more general liveness proposal, not this specific counter). No doctor check reads `command-audit.md`'s fail-open count | High — this is a live, silently-accruing security/reliability gap the session itself flagged as under-ranked, and it is still completely unactioned |
| 13 | Phase 1 fix (three `uvx = false` patches + azure-cli lockfile re-derivation + graphify 0.9.53→0.9.61 bump across 5 files) is complete in the working tree but **uncommitted** | `git status --short`: `mise.lock`, `mise.toml`, `python/pyproject.toml`, `python/src/dotfiles_setup/graphify.py`, `python/uv.lock`, `tests/test_graphify.py`, two `.graphify_version` stamps all modified, zero commits since `5f96509` | **UNHANDLED / in-flight** | Moderate — work is on a branch (`feat/enable-research-plugins`, satisfies `do-not.md` #9) so not at risk of loss to a `main`-write, but a large multi-file fix sitting uncommitted across a session boundary is exactly the shape `verify-before-advancing.md` warns against advancing past |
| 14 | Operator's grilling decision (C10): "Instruments: proceed with 3 phases, but research fix + prevention proposals **AND wire detection into this project's doctor**" | `res-instruments-2026-09-13.md` delivered 3 fix+prevention+doctor-check proposals (session-review contamination refuse-to-run fix + doctor check; telemetry-relocation doctor check; secrets client-side dedup module). Checked `doctor.py`'s `CHECKS`/`def check_*` list — only `check_plugin_health` and `check_dependency_currency` exist (from unrelated commit `decf0aa`); none of the three instrument checks were added | **UNHANDLED** — the operator's explicit instruction to wire doctor detection was not carried out this session | High — `.agent/session-review.md` is **currently, right now, still contaminated** (verified live: `Recorded cwd: .../pytest-of-rmanaloto/pytest-5965/.../unmatched`, `Sources: 0 · events: 0`), and nothing will catch the next test-writes-to-prod-path instance |
| 15 | `.agent/session-review.md` contamination itself (the artifact, not just the finding about it) | Same as #14; root cause identified at `main.py:2803` (`res-instruments-2026-09-13.md`): `project_root` conflated with CLI default output path when `--source-repo-root` differs and `--output` is omitted | **UNHANDLED** — root-cause fix not applied, artifact not regenerated | High — every session/tool trusting `.agent/session-review.md` as a coverage ledger is currently reading a dead file |
| 16 | mise WARN: `.config/mise/mise.lock uses lockfile format version 0; run \`mise lock --upgrade\`` (2026.9.7's new v2 format) | C24 (`findings.md:5368`) | **Deliberately deferred, correctly** — explicit "Do NOT run it — a whole-file lock operation is the destructive shape (#370)" | Low (correct call, but the WARN itself will keep firing every session until someone does the scoped migration) — no issue filed to track doing the scoped/safe version |
| 17 | `mise lock --bump <tool>` (new in 2026.9.7) may be the supported replacement for C17's manual lockfile-block deletion | C20 (`findings.md:5285`): "flagged by that lane, **unverified by me**" | **UNHANDLED** — explicitly flagged as unverified, never followed up | Low-moderate — next lockfile-surgery session may hand-roll the C17 manual-deletion approach again instead of the (possibly) safer native flag |
| 18 | `extract`'s free `claude-cli` backend (C28) resolves the whole cost-cap objection raised in C27 | C27 → C28, self-corrected within 2 findings | HANDLED | None — but Phase 3 `extract` itself was **not yet run** as of the last findings entry (C28 stops at "preconditions verified", no execution recorded) — worth noting as in-flight, not a defect |
| 19 | Two npm tools failing to resolve / `aube install failed` lines mentioned in the audit brief's "also hunt for" list | Searched `findings.md`, `progress.md`, transcript grep for "aube" and "npm.*fail" | **NOT FOUND in this session's corpus** — control arm: search for `aube` → 0 hits anywhere in `findings.md`/`progress.md`; likely refers to a DIFFERENT/earlier session's events, not this one. Flagging as a brief-seed item that does not verify against this session | N/A — false lead in the seed list, not a real gap in this session |

## Ranked list — UNHANDLED items only (highest risk first)

1. **#14/#15 — `.agent/session-review.md` is contaminated right now**, with a fully-diagnosed root cause and a written fix proposal that the operator explicitly asked to be wired into the doctor, and none of it shipped. This is the single most concrete, currently-live defect in the corpus.
2. **#12 — 159 guard fail-opens, most recent today, with zero tracking.** The session's own review process flagged this as under-ranked and still did not close the gap by filing anything.
3. **#11 — #1049 and #1050 sit open despite being confirmed fixed in code.** Cheap to close (a `gh issue close` with the evidence already in `findings.md`), not done.
4. **#6 — the mise-2026.9.6-rollback alternative was never presented to the operator** as a real fork before three config patches were applied instead. A retroactive diagnosis, not a prospective decision.
5. **#13 — a large, gate-verified fix (Phase 1 + graphify bump) sits uncommitted** across what may become a session boundary.
6. **#1/#7/#8 — three recurring probe-reliability classes** (diagnostic pipes masking rc, the broken `timeout` shim, lying background-task notifications) each bit multiple times this session and each has zero mechanical prevention — every fix was manual, in-the-moment, and will need to be manually caught again next time.
7. **#17 — `mise lock --bump` as the possibly-correct native replacement for manual lockfile surgery** was flagged and dropped.
8. **#16 — the mise.lock v0→v2 format WARN** has no tracked follow-up for the safe/scoped migration path.
9. **#3/#4 (partial) — no client-side dedup or liveness-doctor-check shipped** for the agentsview secrets cap and telemetry relocation findings, despite both being fully diagnosed with a proposed implementation shape already in hand (res-agentsview-usage, res-instruments).

## GitHub repos touched

- [jdx/mise](https://github.com/jdx/mise) — verified `has_issues:false`, the 2026.9.7 release/commit that introduced the lockfile behavior this session hit, and open issue metadata during the audit (findings, not new reads by me).
- [ray-manaloto/dotfiles](https://github.com/ray-manaloto/dotfiles) — `gh issue view` on #1046, #1049, #1050, #1053 to confirm live state; `gh issue list --search` for fail-open/session-review tracking (0 hits, confirming #12/#15 are untracked).
