# Prioritised Docker image / devcontainer plan review

Scope: `.devcontainer/`, `docker-bake.hcl`, `.github/workflows/build-publish.yml`, the R1/R2/R3 criteria, and image-touching phases of `task_plan.md`.

Evidence rule used below: repository claims carry `path:line` citations; anything not currently established from the repository is marked **UNVERIFIED**.

## 1. Ranked list

1. **Cold-review #822's exact head `da84cc3` now, before auto-merge, with explicit attention to the unintended `BUILDER_IMAGE` coupling and lock removals.** This is the highest-value use of the in-flight build window because the standing gate requires another-lane cold review in addition to green verification (`task_plan.md:35-43`), but the only `da84cc3` record says merely “auto-merge armed, CI running” (`task_plan.md:1458`). Absence control `rg -n 'da84cc3|[Cc]old review' task_plan.md` found the plan's many known cold-review entries at `task_plan.md:41,73,83,113-119,137,213,550`, while `da84cc3` appears only at `task_plan.md:1458` with no review receipt. The exact head changes the builder digest even though that input is declared manual/rare and rebuilds P2996 (`renovate/all:.devcontainer/Dockerfile:16-23`; `renovate/all:docker-bake.hcl:88-95`). Also require the review/CI receipt to settle the unchanged runtime lock under the new mise: the head says lock formats are version-coupled and bumps `MISE_VERSION` to 2026.9.0 (`renovate/all:.devcontainer/Dockerfile:106-118`), while the documented regeneration path writes system and runtime locks together (`renovate/all:.devcontainer/mise-runtime.toml:14-17`; `renovate/all:.github/actions/lock-refresh/action.yml:38-62`). Absence control `git diff --name-only main...renovate/all | rg -n 'mise-system\.lock|mise-runtime\.lock'` matched `.devcontainer/mise-system.lock` and no `.devcontainer/mise-runtime.lock`; whether the unchanged runtime lock is accepted by 2026.9.0 is **UNVERIFIED** until the in-flight build or an equivalent exact-version probe proves it.

2. **If that exact-head review is clean, let #822 finish and then run `mise run land -- 822`, retaining its full local R1/R2/R3 result.** The plan records #822 with auto-merge armed and CI running (`task_plan.md:1458`), CI intentionally validates the published image but not the Mac-only host-user overlay (`.github/workflows/build-publish.yml:974-980`), and `land` is the post-merge gate that fast-forwards `main` and drives full `verify-local` for a devcontainer/image-surface change (`python/src/dotfiles_setup/pr.py:870-900`). `verify-local` covers R3, smoke (including R2), persistence, R1, and secrets (`mise.toml:1026-1048`). **UNVERIFIED:** live GitHub status could not be refreshed because `api.github.com` was unreachable; the current-state premise comes from the operator and `task_plan.md:1458`.

3. **Before the next Renovate cycle, scope the custom Ubuntu regex to `BASE_IMAGE` only and add a regression test proving it cannot match `BUILDER_IMAGE`.** The manager describes a base-image-only dependency but its pattern matches every `ubuntu:<version>@sha256:<digest>` in both Dockerfile and bake files (`renovate.json:137-147`). That contradicts the image design: `BUILDER_IMAGE` is explicitly manual/rare and a change forces the roughly two-hour P2996 rebuild (`.devcontainer/Dockerfile:16-23`; `docker-bake.hcl:88-95`; `python/src/dotfiles_setup/p2996_hash.py:7-15`). The exact #822 head demonstrates the blast radius by changing both values (`renovate/all:.devcontainer/Dockerfile:14,23`; `renovate/all:docker-bake.hcl:81,95`). The plan collapses them into one generic Ubuntu row (`task_plan.md:450-455`) and never names `BUILDER_IMAGE`: absence control was `rg -n 'Dockerfile|BUILDER_IMAGE' task_plan.md`, where `Dockerfile` matched at `task_plan.md:454,479` but `BUILDER_IMAGE` did not. Test coverage is also missing: control `rg -n 'matchStrings|BUILDER_IMAGE' tests/test_renovate_validate.py` matched `matchStrings` at `tests/test_renovate_validate.py:42,57` and found no `BUILDER_IMAGE` guard.

4. **Delete the obsolete “restore the aws-cli blake3 checksum” merge blocker instead of restarting #822 to satisfy it.** Phase 12 calls the removed local checksum a mandatory precondition (`task_plan.md:464-475,482-485`), but Phase 17 later records that upstream exposes no checksum mise can consume, that the sole blake3 was locally computed and dropped by Linux Renovate, and the explicit decision was “do not chase it” (`task_plan.md:1428-1430`). The exact #822 head therefore correctly contains the 2.36.36 macOS URL without that local checksum (`renovate/all:mise.lock:5009-5038`). Absence control: `git show renovate/all:mise.lock | rg -n 'platforms\.macos-arm64|checksum = "blake3:'` matched the known-present macOS-arm64 platform entries, including aws-cli at `renovate/all:mise.lock:5031`, and returned no blake3 line.

5. **After #822 lands, fix the recurring Renovate-pin/image-lock split rather than treating `da84cc3` as the durable solution.** Phase 17 says only #822's `mise-system.lock` drift was repaired and that the failure is recurrent (`task_plan.md:1435,1458`); Phase 12 correctly identifies the deeper problem as two independent PRs that cannot see each other (`task_plan.md:672-690`). Current `refresh.yml` runs only on schedule or manual dispatch and always writes `chore/lock-refresh` (`.github/workflows/refresh.yml:32-41,101-138`), while hosted Renovate cannot run the Linux lock regeneration (`.github/actions/lock-refresh/action.yml:3-13`). Absence controls: `rg -n '"automerge"|"postUpgradeTasks"' renovate.json` matched `automerge` at `renovate.json:38,47` and no `postUpgradeTasks`; `rg -n 'lock-refresh|workflow_run' .github/workflows/refresh.yml` matched the existing refresh job at `.github/workflows/refresh.yml:2-3,57,102,129` and no `workflow_run` trigger. Preserve the plan's Linux-x64 lock path, but make it update the Renovate branch (or explicitly adopt manual landing); do not mislabel the one repaired head as recurrence prevention.

6. **Close Phase 16 as shipped and reconcile its status/net-state prose plus `.devcontainer/AGENTS.md` with #896.** The phase still presents one HIGH as confirmed and its final table still lists `doppler.env` HIGH plus sync state MED-HIGH (`task_plan.md:1146-1149,1400-1414`), while Phase 17 says #896 merged and `land` passed (`task_plan.md:1452-1455`). The live implementation resolves a workspace+architecture env filename and atomically writes it (`python/src/dotfiles_setup/devcontainer_names.py:309-321`; `python/src/dotfiles_setup/docker.py:101-153`), and sync records use a workspace-hash-plus-arch key with an atomic file replacement (`python/src/dotfiles_setup/sync.py:239-256,335-373`); both collision arms exist (`tests/test_docker.py:278-308`; `tests/test_sync.py:218-259`). The directory instructions are now stale: they still describe `initializeCommand` itself downloading to the single bare `~/.local/state/dotfiles/doppler.env` (`.devcontainer/AGENTS.md:31-33,43-48`), whereas current config delegates to Python and supplies `${localEnv:DEVCONTAINER_ENV_FILE}` (`.devcontainer/devcontainer.json:109-117,230-235`).

7. **Retire the “one PR including Python” batching instruction; do not amend the in-flight image PR just to recover that now-missed batching opportunity.** Phase 12 explicitly allows non-image Python items to ship separately but then says the full sweep must be one PR (`task_plan.md:515-526,548-552`); Phase 17 now records both that Python remains undone and that #822 is already in CI (`task_plan.md:1437,1458`). The exact #822 head has no Python path: absence control `git diff --name-only main...renovate/all | rg -n 'Dockerfile|^python/'` matched `.devcontainer/Dockerfile` and no `python/` entry. A new push would also cancel the older branch run by workflow policy (`.github/workflows/AGENTS.md:117-121`), so finish the expensive image lane, then treat pydantic/ty as a cheap non-image follow-up outside this theme.

8. **Rebase and finish the #723 feature-lock reconciliation after the higher-priority image path is green.** This is a live tree contradiction, not a recommendation based only on an open ticket: the devcontainer feature lock is committed and pins `sshd` 1.1.0 by digest (`.devcontainer/devcontainer-lock.json:1-9`), but `.gitignore` still says it is per-machine churn that must stay out of the repository (`.gitignore:15-20`). The refresh workflow already regenerates and commits that path (`.github/workflows/refresh.yml:101-138`), while no local feature-lock task exists: control `rg -n 'lock-image|lock-devcontainer-features' mise.toml` matched the known image-lock task at `mise.toml:1286,1300-1302` and no `lock-devcontainer-features`. Phase 12 already identifies the completed reconciliation branch and keeps it (`task_plan.md:586-591`); re-evaluate it against current #896/#897 before applying because those commits moved overlapping devcontainer and workflow surfaces.

## 2. What is already done but still presented as outstanding

- **Phase 16's HIGH Doppler collision is fixed and regression-armed.** The plan's live status/table still present it as open (`task_plan.md:1146-1149,1400-1405`), but the runtime path is now workspace+architecture-specific and atomically replaced (`python/src/dotfiles_setup/docker.py:64-81,101-153`), its two-clone arm asserts distinct files and contents (`tests/test_docker.py:278-308`), and Phase 17 records #896 merged and landed (`task_plan.md:1452-1455`).
- **Phase 16's MED-HIGH sync-record collision is fixed and regression-armed.** The plan still lists the arch-only state defect (`task_plan.md:1317-1343,1402-1405`), while current code keys entries as `<workspace-hash>:<arch>` and atomically replaces the shared record (`python/src/dotfiles_setup/sync.py:239-256,335-373`); the same-architecture/two-clone test proves both writes survive (`tests/test_sync.py:218-259`).
- **Phase 16's host-side `hk-lint.log` collision is fixed by #897.** The plan's net table still calls it MED (`task_plan.md:1345-1381,1404-1406`), while current code derives per-workspace/per-process files, maintains an atomic stable link, and removes the legacy fixed-path decoy (`python/src/dotfiles_setup/lint.py:29-43,85-87,100-186`); tests cover two workspace scopes, link replacement, legacy removal, concurrent sibling preservation, and cross-workspace pruning (`tests/test_lint.py:130-216,234-278,314-330`). Phase 17 records #897 merged and landed (`task_plan.md:1452-1454`).
- **The per-PR `mise-system.lock` mismatch named for #822 is repaired at its exact head; only merge, review, landing, and recurrence prevention remain.** The head aligns shared codex/uv pins (`renovate/all:.config/mise/conf.d/shared.toml:38,48`) with the image lock (`renovate/all:.devcontainer/mise-system.lock:5049-5050,5514-5515`), and Phase 17 records `da84cc3` as the repair (`task_plan.md:1458`). This supersedes Phase 12's claim that lock-lane repair must happen before #822 can enter a sweep (`task_plan.md:528-534`), but it does **not** supersede the durable automation item in ranked action 5.
- **R1/R2/R3 enforcement itself is already wired; no new success criterion needs inventing.** The durable criteria and owners are explicit (`AGENTS.md:155-162`), `verify-local` sequences R3, smoke/R2, persistence, R1, and secrets (`mise.toml:1026-1048`), and `land` mandates full local validation for this surface (`python/src/dotfiles_setup/pr.py:94-109,870-900`). Therefore #896's recorded successful land is completion evidence for the Phase 16 runtime-verification checklist (`task_plan.md:1218-1229,1455`), not a reason to design another gate.

## 3. What the plan is missing for this theme

- **An exact-head review/receipt slot for #822 before auto-merge.** The standing gate requires it (`task_plan.md:35-43`), yet `da84cc3` is recorded only as repaired plus in CI (`task_plan.md:1458`). This slot must explicitly decide the builder-digest coupling and verify runtime-lock compatibility; green CI alone does not satisfy the plan's own completion definition.
- **A first-class `BASE_IMAGE` versus `BUILDER_IMAGE` dependency boundary.** Current comments and hash code deliberately separate the ~30-minute base cache from the 80–120-minute P2996 cache (`docker-bake.hcl:76-95`; `python/src/dotfiles_setup/p2996_hash.py:4-17`), but neither Phase 12's Ubuntu row nor its rebuild estimate names the builder dimension (`task_plan.md:450-455,479`). The missing acceptance arm is: a Renovate Ubuntu base-digest update changes both `BASE_IMAGE` sites, changes neither `BUILDER_IMAGE` site, and leaves `p2996-hash` unchanged.
- **An accurate inventory of the current #822 head.** Phase 12 calls it seven bumps and pins older targets (`task_plan.md:444-458`), while the exact head has newer Renovate/MISE targets and additional agent-browser/antigravity changes (`renovate/all:mise.toml:31,51,69-70,96`; `renovate/all:.github/actions/setup-mise/action.yml:32-45`). Review must use `main...da84cc3`, not the stale table.
- **A recorded answer for why `mise-runtime.lock` is unchanged under MISE_VERSION 2026.9.0.** The runtime tier says it is regenerated together with the other image locks (`.devcontainer/mise-runtime.toml:14-17`), and the canonical action runs `MISE_ENV=runtime` specifically to write both image locks (`.github/actions/lock-refresh/action.yml:38-62`), but #822 changes only the system lock. The build may prove the old runtime lock remains compatible, but until that exact evidence exists the result is **UNVERIFIED**, not implicitly safe.
- **A documentation closeout tied to #896.** `.devcontainer/AGENTS.md` is the mechanism authority for the durable R1/R2/R3 contract (`AGENTS.md:155-162`), yet its Doppler flow still names the removed bare file (`.devcontainer/AGENTS.md:31-33,43-48`). The plan records shipped code without a paired instruction-surface update (`task_plan.md:1452-1455`).
- **A coherent current-status header.** The top still says `main` is `d4f8436`, Phase 12 has executed nothing, and Phase 12 is next (`task_plan.md:51-70`), while the same file records #896/#897 landed and #822 in CI (`task_plan.md:1452-1458`); Phase 17 itself is headed “NOT STARTED” immediately above those shipped results (`task_plan.md:1418,1452-1458`). That contradiction can dispatch duplicate Phase 16 work and hide the actual #822 review/land critical path.

## 4. Single highest-value next action

**Cold-review #822 exact head `da84cc3` now—specifically the unintended `BUILDER_IMAGE` bump (`renovate/all:.devcontainer/Dockerfile:16-23`), the unchanged runtime lock under mise 2026.9.0 (`renovate/all:.devcontainer/Dockerfile:106-118`; `.devcontainer/mise-runtime.toml:14-17`), and all lock removals—before the recorded auto-merge is allowed to complete (`task_plan.md:35-43,1458`).**
