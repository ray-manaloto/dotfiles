# Cold review — `ecc93fd..5893720` (`fix/lint-log-scope-895`)

Reviewer: cold lane. No spec, issue, or design doc read before forming the view.

## What the change does (my reading of the code alone)

`python/src/dotfiles_setup/lint.py` previously wrote every `mise run lint` hk
debug log to one literal path, `~/.local/state/dotfiles/hk-lint.log`, truncated
at the top of each run. The change replaces that with:

- `LOG_DIR` + a per-run path `hk-lint-<workspace hash>-<pid>.log`
  (`lint.py:66`, `lint.py:83-85`);
- a stable per-workspace symlink `hk-lint-<hash>.log`, repointed by a
  pid-scoped tmp symlink + `Path.replace` (`lint.py:93-113`);
- a pruner deleting this workspace's per-run logs older than 24h
  (`lint.py:116-141`);
- `run_guarded(log_file=None, project_root=None)` — an explicit `log_file` is
  still honoured verbatim, only the default derives (`lint.py:192-222`);
- `main.py:2233-2238` threads `project_root` in;
- doc updates in `.claude/rules/long-running-command-hangs.md` and
  `docs/rules-evidence/long-running-command-hangs.md`;
- four new tests in `tests/test_lint.py`.

Findings below, most severe first.
