# Session-resume tooling inventory — 2026-09-23 (Explore delegate, verbatim)

Persisted at receipt per `.claude/rules/agent-report-persistence.md`.

**Brief (verbatim, abridged header):** "Read-only fact-finding for a design grilling… Questions: 1. `session-state`: exactly what does it check…? 2. `handoff-check`: list every check… 3. `session-agentsview-pass`: what does it compute…? 4. The agentsview CLI/daemon features available… 5. `session-resume` and `session-handoff` SKILL.md step lists… 6. Is there any existing mechanism that reports main-branch CI red at session start?"

Harness banner on receipt: "subagent output matched instruction-shaped pattern(s): settings-json" (relayed; nothing in the report was acted on as an instruction).

---

No existing mechanism reports main-branch CI state at session start. `session-state` checks only the current branch's own PR, and neither the SessionStart hook nor `doctor.toml` touches CI. Read-only throughout: no files written (including no `findings.md`/`progress.md` update) — please persist this report to `docs/research/kb/reports/agents/`. graphify-first hook reminder noted; not run because every target was known by path.

## 1. `session-state`
Task at `mise.toml:991-994`; code `python/src/dotfiles_setup/session_state.py`. `gather()` (`:232-247`) collects exactly four things:
- **Branch**: `git symbolic-ref`; detached HEAD → `None` (`:103-117`).
- **Tree**: `git status --porcelain --no-renames` paths (`:120-128`).
- **Recent commits**: `git log -n 8` (`:131-141`, default `:34`).
- **Open PR for the CURRENT BRANCH only**: `gh pr list --head <branch> --state open --json number,title,statusCheckRollup` (`:194-229`), first row only (`:219`). `_checks_summary` (`:166-180`) reduces the rollup to `N/M passing` (SUCCESS/NEUTRAL/SKIPPED/PASS count as passing); it does not name failing checks. gh failure/timeout/bad JSON → `UNVERIFIABLE`, never "none" (`:9-12`, `:211-215`).
- **Not checked**: main's CI conclusion (no `gh run` call), any other open PR (filter is `--head <branch>`), doctor drift. Only flag is `--no-pr` (`:290-295`).

## 2. `handoff-check`
Task `mise.toml:1006-1009`; `python/src/dotfiles_setup/handoff_check.py`, `check()` `:262-269`. Findings enum `:42-53`:
1. Handoff selection: newest `.agent/plans/session-YYYY-MM-DD[letter].md` if no path given (`:65-81`).
2. `FORBIDDEN_TASK_CARRIER` — "Next task" heading or `next:` / `next task:` line outside fences (`:169-203`, regexes `:35-39`).
3. `UNCLOSED_FENCE` — fence open at EOF (`:204-211`).
4. `MISSING_ACTIVE_PLAN` — no `##` heading containing `NEXT SESSION` in `task_plan.md` (`:221-229`).
5. `MISSING_PLAN_POINTER` / `STALE_PLAN_POINTER` — pointer absent/unreadable, or `plan_sha256` / `active_phase` disagree with `task_plan.md` (`:231-259`).
6. `MISSING_PATH` / `BAD_LINE_RANGE` — every repo-relative `file:line[-end]` citation must exist and be in range (`:84-115`).
7. `UNKNOWN_TASK` — every `mise run <name>` must appear in `mise tasks ls`; `kb-*` names skipped (`:118-166`).
Plan checks skipped (info) when `task_plan.md` absent (`:313-317`); exit 1 on any finding (`:319`). Docstring: does NOT certify completeness (`:4-6`).

## 3. `session-agentsview-pass`
Task `mise.toml:1598-1601`; `python/src/dotfiles_setup/agentsview_pass.py`; flags `--session` (repeatable), `--limit` (default 3), `--skill` (`main.py:1604-1618`).
- Reads server URL + token-file path from the installed skill's `# install-remote:` line (`:84-114`, regex `:24`).
- Session selection (unless `--session`): `agentsview session list --agent claude --project <repo-dir-name> --include-children --limit 3 --json --server … --server-token-file …` (`:301-319`) — i.e. the 3 newest Claude sessions for this project, children included.
- Per session: `agentsview session tool-calls <id> --json` with the same server flags (`:321-332`).
- `census` (`:231-262`, classification `:197-228`): unbounded wait loops in Bash (reuses the guard's check, `:212-215`), `AskUserQuestion` count, position of the `session-handoff` Skill call vs the first `git commit`, Write/Edit/Bash-redirect writes under `docs/research/kb/reports/agents/`, unparsable rows.
- Exit 1 only on an unbounded wait (`:335-336`); exit 2 + `UNVERIFIABLE` on daemon/archive failure (`:352-360`). Does NOT look at tool failures, errors, or outcome.

## 4. AgentsView CLI + skill on this machine
- Only skill: `~/.claude/skills/agentsview-finding-history/SKILL.md`, generated by agentsview v0.43.0 (`:2`), daemon `http://127.0.0.1:8080` (`:3`). Control arm: `~/.claude/plugins/cache` → 0 hits for "agentsview"; `.claude/skills` hits are only mentions in the `verify`, `session-handoff`, `session-review` skills. Installed binary is v0.44.0 (`agentsview --help`) — one version ahead of the skill.
- Skill-documented commands:
  - `session search "<q>"` — `--hybrid`/`--fts`/`--semantic` search MESSAGES only (`:21-28`); plain search with `--in tool_input,tool_result` for IDs/paths/commands/errors (`:29,33,99`); `--exclude-session`, `--context N`, `--limit`, `--json` (`:24,32-33`); `--since` (`:40-42`); `--scope top` (`:79-80`).
  - `session messages <id> --around <ord> --before N --after N --role user,assistant` — window around a hit (`:49-51`).
  - Always pass `--server` + `--server-token-file`, otherwise it silently searches the local SQLite (`:36-37`).
  - 501/503 fallbacks for semantic search (`:64-74`).
- Additional commands (from `agentsview --help` / subcommand help, NOT in the skill): `session list` (filters incl. `--min-tool-failures`, `--outcome success,failure,…`, `--health-grade`, `--resume` [active in last 15m], `--sort failures|retries|health|…`, `--include-children`, `--since`), `session get <id>` (metadata + signals), `session tool-calls <id>`, `session usage` (tokens/cost), `session watch` (live SSE), `session export` (raw JSONL, local only), `health [id]` (grade + signal counts), `recall list|query|brief` (curated knowledge extracted from past sessions), `mcp` (read-only MCP server).
- **Prior-session failures / unfinished work** (probed read-only against the live daemon): `session get` / `session list` rows carry `tool_failure_signal_count`, `consecutive_failure_max`, `final_failure_streak`, `tool_retry_count`, `outcome`, `outcome_confidence`, `ended_with_role`, `health_grade`, `mid_task_compaction_count` — these CAN flag a failing or unfinished prior session. `session tool-calls` rows have only `category, input_json, ordinal, result_length, timestamp, tool_name, tool_use_id` — no error flag and no result text, so they CANNOT list which specific calls failed; error text needs `session search "<error string>" --in tool_result` then `session messages --around <ordinal>`. Nothing reports "unfinished work" directly; `outcome` + `ended_with_role` are the closest proxies.

## 5. Skill step lists
**session-resume** (`.claude/skills/session-resume/SKILL.md`):
1. Find handoff: path arg, else newest `.agent/plans/session-*.md`; fresh-clone fallback to `git log` / `gh issue list` (`:22-45`).
2. `mise run session-state`; plus `mise run handoff-check` for citation-heavy handoffs (`:47-66`).
3. Reconcile; disagreements first; read the active phase from `task_plan.md`; fixed header/DISAGREEMENT/PLAN/OWED/TRAPS shape (`:68-94`).
4. Offer to begin the active phase (`:96-100`).
Explicitly does NOT write/commit/ship or certify coverage (`:102-109`).

**session-handoff** (`.claude/skills/session-handoff/SKILL.md`):
0. Resolve active-plan ambiguity via AskUserQuestion; rulings only in `task_plan.md` (`:17-29`).
1. Snapshot (`git status/branch/log`, `gh pr list --head`, PR CI via `gh pr checks`, `:31-44`); `mise run plan-pointer` (`:46-53`); `mise run session-orphans` (`:89-102`); inventory (don't wait on) GHA runs / Renovate (`:104-119`).
1b. `mise run session-agentsview-pass` (`:121-135`).
2. Doc sync; doc-refs covered by `mise run lint` (`:137-175`).
3. Persist: memory (a), local handoff (b), verbatim agent reports (c) (`:206-250`).
4. Validate (lint/pytest/verify) then commit (`:252-284`).
5. `mise run handoff-check -- <handoff>` (`:286-297`).
6. Print exactly `Run /session-resume` (`:299-305`). Checklist `:307-325`.

## 6. Existing main-CI-red reporting at session start?
None.
- Project SessionStart hook (`.claude/settings.json:132-143`, matcher `startup|resume`) runs only `mise run tool-currency-check` and `mise run doctor` (or `scripts/web-setup.sh` remotely).
- User-level SessionStart hook (`~/.claude/settings.json`) runs only `herdr-agent-state.sh session`.
- `doctor.toml`: case-insensitive grep for `ci\.yml|conclusion|\bmain\b|gh run|workflow|statusCheck` → **0 hits**. Control arm: `^\[` → 8 section headers, and `fnox`/`env_true` hit at `:11,16,39`. Sections: `[fnox] [mcp] [mcp.mutating_tools] [listing] [graphify] [path_drift] [claude] [codex]` (`:16,98,136,138,189,224,243,273`).
- `doctor.py`: same grep → only a docstring mention of the `workflow.tool-currency-wiring` contract (`:824`). Control arm: `doctor.toml` → 4 hits in the same file.
- SessionStart scripts (`scripts/web-setup.sh` 69 lines, `herdr-agent-state.sh` 90 lines): 0 hits for `ci.yml|conclusion|gh run`; control arm = the files' non-empty line counts.
- The only `gh run` readers in the package are in `sync.py`, not on any session-start path: `inflight_ci_runs(branch)` at `:554` (in-progress `ci.yml` runs only) and `wait_for_run` at `:579` (reads a run's `conclusion`). Control arm: `"pr",` found in `sync.py`, `session_state.py`, `workflow_hooks.py`.

## GitHub repos touched

_None (local repo, local agentsview daemon and skill only)._
