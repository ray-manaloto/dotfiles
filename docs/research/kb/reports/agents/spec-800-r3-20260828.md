# SPEC — #800 round 3 (final, bounded — Ray's ruling 2026-08-28): close cold-review-r2 H1/H2/H3, M3, L1, L4

Repo: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles — branch `fix/800-sync-id-labels` at HEAD 6309e53 (clean).
Prior specs (binding for everything not changed here): spec-800.md, spec-800-r2.md in this directory.
Review being closed: /private/tmp/claude-501/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/58dab3e3-be77-4148-81b3-2725b6962da0/scratchpad/cold-review-800-r2.md

## 1. Objective

The two mechanisms #800 exists for — the stopped-container id probe (F1) and `local_platforms` (F2) — must each be pinned by a test that FAILS when the mechanism is deleted (the reviewer deleted both with a green suite). `dotfiles-setup docker down` must degrade gracefully when the daemon is unreachable (it did before this branch). `--check` must not print `current` when a real sync would rebuild on overlay-currency grounds. An empty `--platform` must never reach buildx. Nothing else changes; the residuals M2/M4/M5/M6/L2/L3/L5 are deliberately NOT in scope (they go into the PR body).

## 2. Files

- `python/src/dotfiles_setup/sync.py`, `tests/test_sync.py`
- `python/src/dotfiles_setup/docker.py`, `tests/test_docker.py`
- `mise.toml` — the comment block at `[tasks.prune]` :1002-1006 (L4) AND the `--check` help text at :850 (M3 rc contract)
- `.claude/skills/devcontainer-sync/SKILL.md` :25 and :59 — the `--check` rc-1 wording only (M3); this pulls `mise run lint-docs` into the gate
- `python/src/dotfiles_setup/main.py` :780 — the argparse help `(rc 1 if stale)` → `(rc 1 if stale or this architecture's container is outdated)`

## 3. Interfaces

No signature changes. `sync_main`'s check mode gains one outcome (below).

## 4. Constraints and invariants

**H1 — test the `ps -q` → `ps -aq` fallback at the probe level.** In `tests/test_sync.py`, monkeypatch `sync._run` with a fake that returns `""` for a `docker ps -q …` argv, a container id for `docker ps -aq …`, and an overlay id for `docker inspect …`; assert `container_image_id(names)` returns the overlay id AND that the `-q` call happened before the `-aq` call. Second arm: when `-q` returns an id, `-aq` is never called (assert absence). Deleting the fallback lines (sync.py:452-453) must turn the first test red — verify that by mutation before committing and say so in the report.

**H2 — test `local_platforms` itself.** Monkeypatch `sync._run`: rc 0 for `--platform <A>` and rc 1 for `--platform <B>` (use the two `published_targets()` triples — deriving is preferred, but literals are ALLOWED in `tests/**`: `_SCAN_EXCLUDED_PREFIXES` includes `tests/`, platform_target.py:133-138, and test_sync.py:22/:25 already hold literal triples) → returns `frozenset({A})`; all rc 1 → `frozenset()`; assert every `published_targets()` triple was probed with `--format {{.Id}}`. Replacing the body with `return frozenset()` must turn the first test red.

**H3 — `DevContainerManager.down()` must not raise when docker is unreachable.** `teardown_container_ids` → `_docker_ps_ids` uses `check=True` (devcontainer_names.py:561-568) and `mise run stop` relies on that loudness, so do NOT change devcontainer_names. In `docker.py down()` (:279) catch `subprocess.CalledProcessError` from the lookup, `logger.error("could not list devcontainers: %s", exc)` and return — the pre-branch behaviour was a silent fall-through to "No active or exited devcontainers"; an error-level log is the honest version. Test: stub `docker.shutil.which` as the two existing `down()` tests do (`_get_bin` runs at :276, BEFORE the lookup at :279), `teardown_container_ids` stubbed to raise `CalledProcessError(1, "docker")` → `down()` returns without raising and issues no `docker stop/rm`. `subprocess` is already imported in docker.py:19.

**M3 — `--check` must report an overlay-stale container.** sync.py:783-793: after the `registry_digest is None → 2` branch, compute `stale = status.stale`, `outdated = not status.container_current`; print `check: STALE — sync would rebuild` when either, else `current`; return `1 if (stale or outdated) else 0`. Keep the existing wording for the stale case; for the overlay-only case print `check: OUTDATED — sync would rebuild this architecture's container`. Update the check-mode tests (`test_sync_check_mode_*`) and add one: not stale + running + `container_image_id` set (via `dataclasses.replace` — do NOT grow `_status()` past ruff's PLR0913 limit) + record lacking this arch with legacy None → rc 1 with the OUTDATED line. Also update the three prose sites in §2 (mise.toml:850, SKILL.md:25/:59, main.py:780) so the documented rc contract matches; run `mise run lint-docs` too (`.claude/skills/**` changed). Real-arm consequence the architect will observe: on this host `--check` will now exit 1 on both arches (legacy record) — expected.

**L1 — never emit `--platform ""`.** sync.py:608-611: if `platforms` is empty after the `published_targets()` filter, `refresh_local_tag` raises `ValueError(f"{resolve_platform()!r} is not a published platform; refresh cannot target it")`, and `_converge` catches `ValueError` around the `refresh_local_tag` call and returns `(False, str(exc))` so `mise run sync` / `mise run land` (pr.py:882) print a `FAIL  converge:` line, never a traceback. This is DEFENSIVE — `resolve_platform()` is a published triple unless an operator sets an unpublished `DOTFILES_PLATFORM` spelling (host_platform() is published by construction, platform_target.py:333-336). Test: `monkeypatch.setenv("DOTFILES_PLATFORM", "linux/amd64")` (literal is fine in tests) + `local_platforms` stubbed empty → `refresh_local_tag` raises `ValueError`; and `_converge` with `refresh_local_tag` stubbed to raise → `(False, <message>)`.

**L4 — prune comment.** mise.toml:1005-1006: replace the "happens to carry both" justification with: `sort -u` guards the ONE case that would abort prune under `set -e` — the same id listed twice — which the two label sets should never produce, but a duplicate would cost the whole volume/image/cache tail.

Repo conventions as before: zero inline suppressions; `mise run lint` is the gate (do not run `hk` directly); no docker/mise lifecycle commands; do not push. One commit.

## 5. Verification

```
uv run --project python pytest tests/test_sync.py tests/test_docker.py tests/test_devcontainer_names.py -x -q
uv run --project python ruff check python/src/dotfiles_setup/sync.py python/src/dotfiles_setup/docker.py tests/test_sync.py tests/test_docker.py
uv run --project python ruff format --check python/src/dotfiles_setup/sync.py python/src/dotfiles_setup/docker.py tests/test_sync.py tests/test_docker.py
uv run --project python ty check python/src/dotfiles_setup/sync.py python/src/dotfiles_setup/docker.py
```
Plus `mise run lint-docs` (skill file changed) and the two mutation checks named in H1/H2 (revert the mutation before committing).

## 6. Commit

`COMMIT: lane` — `test(sync): pin the stopped-container probe and local_platforms; graceful down(); --check reports an outdated overlay (#800)`. Do not push.

## 7. PREMISES

- I `container_image_id` fallback — sync.py:451-453: `cid = _run(["docker","ps","-q",*filters])…; if not cid: cid = _run(["docker","ps","-aq",*filters])…`.
- I `local_platforms(image_ref) -> frozenset[str]` — sync.py:552-580: loops `published_targets()`, `_run(["docker","image","inspect","--platform",target.platform,image_ref,"--format","{{.Id}}"])`, rc 0 → present.
- I `refresh_local_tag` union — sync.py:608-611: `wanted = {resolve_platform()} | local_platforms(image_ref)`; `platforms = ",".join(t.platform for t in published_targets() if t.platform in wanted)`.
- I check mode — sync.py:783-793: `registry_digest is None → "check: UNKNOWN — registry unreachable", return 2`; else prints `check: {'STALE — sync would rebuild' if status.stale else 'current'}` and returns `1 if status.stale else 0`; `decide_action` is consulted only after (:795).
- I `DevContainerManager.down()` — docker.py:263-287: `container_ids = teardown_container_ids(resolve_names(workspace=abs_root))` at :279; empty → `logger.info("No active or exited devcontainers found for this project.")`; else `docker stop` + `docker rm -f` per id via `subprocess.run(check=False)`.
- I `_docker_ps_ids` — devcontainer_names.py:561-568: `subprocess.run([... "docker","ps","-aq", *filters], check=True)` — raises `CalledProcessError` when the daemon is unreachable (reviewer probed with `DOCKER_HOST=unix:///nonexistent.sock`: rc 1 → raised).
- L existing tests — tests/test_sync.py:220 `test_stopped_container_with_stale_overlay_is_rebuilt_not_reused`, :234 `…matching_the_record_is_reused_via_up`, :323 `test_container_state_filters_on_both_id_labels_not_local_folder`, :504/:528/:547 the three `test_refresh_local_tag_*` (all stub `local_platforms`); tests/test_docker.py:154 and :189 the two `down()` tests (both stub `teardown_container_ids`).
- L prune comment — mise.toml:1002-1006 (the "happens to carry both" sentence at :1005-1006).
- L `no_platform_literals` scans `python/src/**/*.py` prose (a codex-r2 docstring failed it) but EXCLUDES `tests/` — platform_target.py:133-138 `_SCAN_EXCLUDED_PREFIXES = ("docs/", "missions/", "plugins/", "tests/")`, applied at :439; control: test_sync.py:22/:25 hold literal triples and the 21 literal-gate tests pass.
- L `--check` rc contract is documented at mise.toml:850, `.claude/skills/devcontainer-sync/SKILL.md`:25 and :59, main.py:780 — all say "rc 1 if stale"; the skill is bound by `workflow.sync-wiring` (suites.toml:1076-1106).
- I `_status()` — tests/test_sync.py:55-73, 5 kwargs, no `container_image_id` (ruff PLR0913 limit already hit — codex r1 report).
- I `_converge` refresh call — sync.py:~717 `if status.stale and not refresh_local_tag(status.image_ref): return False, f"buildkit tag refresh failed …"`; every other failure returns `(ok, detail)`, nothing raises through `sync_main`.
- I `DevContainerManager._get_bin("docker")` — docker.py:276, before the lookup; existing tests stub `docker.shutil.which` (tests/test_docker.py:154-200).
- E stdout: one new check-mode line `check: OUTDATED — sync would rebuild this architecture's container` (text only) and a changed return code (1) in that case — consumers: `mise run sync -- --check` callers (documented in mise.toml:850 as "rc 1 if stale"); `pr.py:882` calls `sync_main` WITHOUT `check_only`, unaffected.
- E log: `logger.error("could not list devcontainers: %s", exc)` in `down()` — exception text from docker, non-secret.
- E exception: `ValueError` from `refresh_local_tag` naming the resolved platform string (non-secret env value).
