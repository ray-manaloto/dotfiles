# Agent briefs (part 2) — session 2026-09-23 (`f643887b`), Phase 11 grilling

Persisted per `.claude/rules/agent-report-persistence.md` / session-handoff §3c. These are the prompts
handed TO the four findings-bearing delegates in the Phase 11 `/grilling`, verbatim. Part 1 (the #1296 and
#1306 research briefs) is `session-2026-09-23-agent-briefs.md`.

| Delegate | Report |
|---|---|
| Explore: session-resume tooling inventory | `session-resume-tooling-inventory-2026-09-23.md` (brief abridged in its header; full text below) |
| general-purpose: agentsview resume triage | `agentsview-resume-triage-2026-09-23.md` |
| general-purpose: codegen + logger standards gap | `codegen-logger-standards-gap-2026-09-23.md` |
| general-purpose: context injection via hooks/rules | `context-injection-hooks-2026-09-23.md` |

Consumers: all four feed `task_plan.md` Phase 11 and spec dotfiles#1326 (tickets #1327–#1342). The triage
report governs ticket #1339; the standards-gap report governs #1329–#1333; the injection report governs
#1328 and #1334–#1336. None is pointed to from a rule or skill yet, deliberately: the rule and skill text
they motivate is itself Phase 11 work (#1333, #1340).

## Brief 1 — Explore: session-resume tooling inventory

````text
Read-only fact-finding for a design grilling in /Users/rmanaloto/dev/github/ray-manaloto/dotfiles. Do NOT edit anything. Return a concise, cited report (file:line for every claim). Thoroughness: medium.

Questions:
1. `session-state` (python/src/dotfiles_setup, the `session-state` CLI subcommand behind `mise run session-state`): exactly what does it check? Does it check main's CI conclusion, open PRs' check buckets, doctor drift, or only branch/tree/commits/PR list? Cite the function.
2. `handoff-check`: list every check it performs (cite).
3. `session-agentsview-pass`: what does it compute, which agentsview daemon/CLI calls does it make, and which sessions does it inspect? Cite.
4. The agentsview CLI/daemon features available on this machine: read the installed AgentsView skill(s) (search `~/.claude/skills`, `~/.claude/plugins/cache`, `.claude/skills` for "agentsview") and the `agentsview-finding-history` skill. List the commands/subcommands and flags (search, tool-calls, sessions, messages, errors, etc.) with a one-line purpose each, citing the skill file lines. Note which can list a PRIOR session's failed tool calls, errors, or unfinished work.
5. `.claude/skills/session-resume/SKILL.md` and `.claude/skills/session-handoff/SKILL.md`: summarize the step list of each in 5-8 lines (cite).
6. Is there any existing mechanism that reports main-branch CI red at session start (SessionStart hook, doctor.toml check)? Grep for `ci.yml` / `conclusion` / `main` in doctor.toml and the SessionStart hook scripts. State the control arm you used for any 0-hit search.

Report inline in your final message (do not write files).
````

## Brief 2 — general-purpose: agentsview features for resume triage

````text
Research task (read-only except for your own report file) for ray-manaloto/dotfiles. Invoke the `mattpocock-skills:research` skill and follow it.

## Question
Which AgentsView features — CLI subcommands, the daemon's HTTP API, the MCP server (`agentsview mcp`), `recall` (extract/query/brief), `insight` (Activity Insights), `health`, `session list/get` signals, `export digest`, and anything else — should `/session-resume` use to (a) review the PREVIOUS session(s) for failures, unfinished work, errors and owed items, and (b) research how such issues were resolved before? Also: is there an SDK or documented HTTP API a python module in `python/src/dotfiles_setup/` should call instead of shelling out to the CLI?

## Sources, in order
1. Prior reports already in this repo: `docs/research/kb/reports/agents/2026-09-12-agentsview-field-research.md`, `res-agentsview-usage-2026-09-13.md`, `agentsview-history-2026-09-21.md`, `agentsview-codex-latest-2026-09-22.md`, `agentsview-session-review-2026-09-16.md`, `2026-09-14g-agentsview-watcher-research.md`, and `session-resume-tooling-inventory-2026-09-23.md` (today's inventory — do not repeat it, extend it).
2. The installed binary: `agentsview --version` (v0.44.0) and `--help` for every relevant subcommand (`recall *`, `insight *`, `health`, `session *`, `export digest`, `mcp`).
3. Upstream source/docs at the INSTALLED tag v0.44.0: github.com/kenn-io/agentsview (README, docs/, the HTTP API routes, MCP tool list). Use `gh api` / raw.githubusercontent.com. Record the tag/commit you read.
4. The existing callers: `python/src/dotfiles_setup/agentsview_pass.py` and `~/.claude/skills/agentsview-finding-history/SKILL.md` (note: skill generated by v0.43.0 vs binary v0.44.0 — check what changed).

## Deliverable
A report with: a feature table (feature | what it returns | can it flag prior-session failures/unfinished work/owed items? | cost/latency | stability: CLI vs API vs MCP | evidence), a recommended minimal set for resume triage with the exact commands/endpoints, whether `recall brief` or `insight` can replace hand-written handoff prose, and the skill-version-drift note. Every probe states its control arm (`.claude/rules/probes-need-a-control-arm.md`). Always pass `--server http://127.0.0.1:8080 --server-token-file "$(the token file named in the skill's install-remote line)"` — without them the CLI silently reads the local SQLite. NOTE: semantic search is unavailable right now (vector index building) — use `--fts`, and `--role` is NOT a valid search flag.

## Hard constraints
- Do not modify repo files other than your report. Do not run `agentsview prune`, `db *`, `embeddings *` mutations, `recall import`, or anything that writes the archive. `recall extract` only if it has a dry-run/no-write mode — otherwise skip and say so.
- Persist INCREMENTALLY: create `docs/research/kb/reports/agents/agentsview-resume-triage-2026-09-23.md` early and update it as you go. End with a `## GitHub repos touched` section. Never print credential values (the token file path is fine; its contents are not).
- Do not commit.

## Final message
The full report verbatim plus a 5-line recommendation.
````

## Brief 3 — general-purpose: codegen + universal-logger standards gap

````text
Research task for ray-manaloto/dotfiles. Invoke the `mattpocock-skills:research` skill and follow it. Read-only EXCEPT your own report file.

## Context
The repo owner (Ray) says he has REPEATEDLY required, for ALL python code: (1) a universal logger, including capturing stdout/stderr into it; (2) datamodel-code-generator (and other codegen) for models/enums/request/response types, with STRICT enums for return and error types, ruff formatting + typing extensions enabled on the generator; (3) research into what else can be autogenerated, enforced via hk and other agent enforcements. He asks: "why haven't we been doing this and why do I keep having to ask for it?" Known so far (verify, don't trust): logging migration program #921-#926 + #943 all OPEN; `kb_setup.events`/`sinks` exist in knowledge-base; ruff TID251 decided as enforcement (D18) but live only for msgspec at `python/pyproject.toml:94-120`; decision R16 in `docs/specs/devcontainer-gcc162-dual-arch.md` ("models must be code-generated from datamodel-code-generator, ONLY generated"); #1111 OPEN (generated result models + return/error enums); datamodel-code-generator is not a dependency; memory file `~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/memory/project_session_2026-09-12b.md` records "five retrieval misses".

## Questions
1. **History**: every time Ray stated or re-stated these requirements — dates, sessions, verbatim quotes, and what was done each time (filed issue? spec decision? nothing?). Use AgentsView (`agentsview-finding-history` skill; ALWAYS pass `--server http://127.0.0.1:8080 --server-token-file "<path from the skill's install-remote line>"`; semantic/hybrid search is STALLED — use `--fts` for prose and plain `--in tool_input,tool_result` for identifiers; `--role` is not a flag). Also grep `task_plan.md`, `docs/specs/`, `docs/research/kb/reports/agents/`, memory files, and `gh` issues (use `gh api '/search/issues?q=repo:ray-manaloto/dotfiles+<term>'`, NOT `gh search issues --repo`, which silently returns 0).
2. **How knowledge-base does it**: `~/dev/github/ray-manaloto/knowledge-base` — its logger (`kb_setup.events`/`sinks`), whether stdout/stderr are captured, any codegen (datamodel-code-generator config, generated models, `--check` drift gates, hk steps), enforcement (ruff TID251, hk, contracts, rules). Cite file:line.
3. **Root cause in dotfiles**: why did decided standards not bind new code? Check what an agent actually sees when writing python here: eager rules (`.claude/rules/*`), `AGENTS.md`/`python/AGENTS.md`, spec/ticket templates, the orchestration skill's seven-part spec, hk steps, ruff config, verify contracts. Is the standard written anywhere an implementer loads? Is anything machine-enforced beyond msgspec TID251? Give the causal chain with evidence.
4. **Enforcement design** for Ray's new ruling "any new and/or updated work must always follow this; add follow-up tasks for a full project sweep and cleanup": which mechanisms would make NEW/CHANGED python code comply by construction (e.g. ruff TID251 banning `print`/`logging`/hand-written `msgspec.Struct` outside generated modules; a datamodel-codegen `--check` hk step; a diff-scoped gate that only binds changed files so the legacy backlog does not block; rule text in an eager rule / `python/AGENTS.md`; spec-template field). Evaluate diff-scoped vs whole-tree. Research what else can be autogenerated (CLI parsers, mise task signatures, suites.toml contract types, hk step metadata, agent-report schemas, the agentsview OpenAPI client) — cite tools and their docs (datamodel-code-generator docs incl. its ruff formatter + `--use-annotated`/typing options; walk `.claude/rules/research-doc-sources.md`).

## Deliverable and constraints
- Write your report INCREMENTALLY to `/private/tmp/claude-501/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/f643887b-5a48-4eee-88ad-ceeb7b17d4d9/scratchpad/docswt/docs/research/kb/reports/agents/codegen-logger-standards-gap-2026-09-23.md` (that path is a git worktree on branch `docs/session-resume-research`; the main checkout is busy — do NOT write under `/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/` and do NOT switch branches anywhere). Create it early, update as you go, end with `## GitHub repos touched`.
- Every probe states its control arm (`.claude/rules/probes-need-a-control-arm.md`); a 0-hit search needs a known-present term run the same way.
- Do not commit, push, comment on issues, or edit any other file. Never print credential values.
- Final message: the full report verbatim plus a 6-line answer: (a) how many times Ray asked, (b) why it didn't stick, (c) how KB differs, (d) the recommended enforcement for new/changed code, (e) the sweep follow-ups, (f) other codegen candidates.
````

## Brief 4 — general-purpose: context injection via hooks and rules

````text
Research task for ray-manaloto/dotfiles. Invoke the `mattpocock-skills:research` skill and follow it. Read-only EXCEPT your own report file.

## Question
Ray wants the python standards (universal logger incl. stdout/stderr capture; every model/enum generated via datamodel-code-generator, only business logic hand-coded; ruff TID251 bans + ratchet + `hk check --pr`) AND the new fix-first `/session-resume` behaviour made stronger by **injecting them into agent context** through (a) Claude Code **function hooks**, (b) **codex hooks**, and (c) Claude and codex **rules/instruction files**. He says "we've reviewed this already, especially how pwf (planning-with-files) does it" — so history FIRST, then design.

Answer:
1. **History**: every prior session/report/issue/memory about injecting context via hooks (SessionStart / UserPromptSubmit / SubagentStart / PreToolUse additionalContext, function hooks `@skills-dir`, codex hooks + `/hooks` trust), with what was decided and what was measured. Known leads (verify, don't trust): memory files `~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/memory/project_session_2026-09-11-d.md` (function hooks measured end-to-end; runtime failures fail OPEN and SILENT), `project_session_2026-09-22c.md` (pwf injected the WRONG plan via a codex lane's `.active_plan`), `feedback_stop_hooks_force_a_turn.md`; reports `docs/research/kb/reports/agents/pwf-claude-codex-2026-09-22.md`, `pwf-setup-2026-09-22.md`, `fable-pwf-shared-plan-proposal-2026-09-22.md`, `claudex-loop-full-research-2026-09-22.md`; rules `.claude/rules/notepad-enforcement.md`, `.claude/rules/agent-report-persistence.md` (SubagentStart injection, the NO-SubagentStop decision); `task_plan.md` Phase 10 rulings on "Hook parity: full, pwf first" and Phase 9 item 9.13 hooks; issue #1024 (session-start known-defect register on a function hook); #1302 (codex hook deny under exec with persisted trust). Use AgentsView (`agentsview-finding-history` skill; ALWAYS `--server http://127.0.0.1:8080 --server-token-file "<path from the skill's install-remote line>"`; semantic/hybrid search is stalled — use `--fts` for prose and plain `--in tool_input,tool_result` for identifiers; there is NO `--role` flag). For issues use `gh api '/search/issues?q=repo:ray-manaloto/dotfiles+<term>'` (NOT `gh search issues --repo`).
2. **How pwf injects**: read the installed planning-with-files plugin (find it under `~/.claude/plugins/cache/`), its hooks.json and scripts: which events, the nonce-framed DATA block, attestation/tamper check, size/truncation limits, how it avoids being treated as instructions, and its codex-side equivalent if any. Cite file:line.
3. **Mechanisms available today** (cite vendor docs on disk: `~/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/{claude-code,codex}/`): Claude Code hook events that can add context (and their per-event size caps / truncation), function hooks (`plugin-authoring` skill, `@skills-dir`), `.claude/rules` eager vs `paths:`-scoped loading; codex hooks (events, `additionalContext` support, trust hashing), codex AGENTS.md discovery (root→cwd), codex rules/execpolicy. Which of them can (i) inject a standard reminder when python is about to be edited, (ii) DENY a hand-written model/print before it lands, (iii) inject resume's issue list at session start.
4. **Design recommendation**: a minimal, non-duplicated layer set (hook vs rule vs gate) with the failure mode of each (fail-open, token cost per turn, decay), aligned with this repo's stance that a hard gate beats a per-turn reminder (`.claude/rules/mise-tasks-only.md` "We deliberately do NOT re-inject a per-turn reminder"). Say where that stance should bend and why.

## Constraints
- Write the report INCREMENTALLY to `/private/tmp/claude-501/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/f643887b-5a48-4eee-88ad-ceeb7b17d4d9/scratchpad/docswt/docs/research/kb/reports/agents/context-injection-hooks-2026-09-23.md` (a git worktree on branch `docs/session-resume-research`; do NOT write under `/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/`, do NOT switch branches anywhere). Create early, update as you go, end with `## GitHub repos touched`.
- Every probe states its control arm (`.claude/rules/probes-need-a-control-arm.md`). Do not run `agentsview` commands lacking `--server` (e.g. `health`, `stats`) — that autostarts a stray daemon.
- No commits, pushes, issue comments, plugin installs, or hook trust changes. Never print credential values.
- Final message: the full report verbatim plus a 6-line recommendation.
````

## GitHub repos touched

_None directly by this file (briefs only); the reports enumerate theirs._
