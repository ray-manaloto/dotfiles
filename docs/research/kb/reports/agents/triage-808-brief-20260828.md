# Brief — triage-808 (IN FLIGHT at clear-prep; report NOT received)

Persisted per `.claude/rules/agent-report-persistence.md` and clear-prep §3c:
this agent was launched and had **not** delivered when the session was cleared,
so only its brief survives. **Re-run it** (cheap read-only agent, `Explore`) if
its answers are still wanted — nothing depends on the original instance.

Its whole job is question 3 below: whether `test_shell_integration.py` is
parametrized such that some parameters would pass on a runner. If they would, a
module-level marker silently discards CI coverage that could have run — which is
the same "you think you're covered and aren't" failure that created #808.

---

## The brief, verbatim

Bounded classification task in `/Users/rmanaloto/dev/github/ray-manaloto/dotfiles`.
A verdict per test file, nothing else — no fixes, no edits.

23 tests fail when the pytest suite runs on a GitHub ubuntu runner (job installs
ONLY `python` + `uv` via mise, checkout is the default shallow `actions/checkout`).
Which of three buckets does each fall in?

- **TOOLCHAIN** — fails only because a mise-managed tool is absent; a full
  `mise install` would fix it. (Confirmed mise-managed: `hk`, `chezmoi`, `pixi`,
  `codex`.)
- **GIT-DEPTH** — fails only because the checkout is shallow / `origin/main` is
  unresolvable; `fetch-depth: 0` would fix it.
- **HOST-ONLY** — cannot pass on any runner: needs a login shell (`zsh`), a
  host-only CLI (`claude`, `gemini` — NOT in any mise config), host session state
  under `~/.claude`, or similar.

The failing tests, with error text captured from the real CI run
(run `33208281690`):

- `tests/test_bootstrap.py::test_tool_installed[chezmoi]` / `[pixi]` — "chezmoi is not installed or not in PATH"
- `tests/test_hk_builtins_audit.py` — 5 tests, all `FileNotFoundError: [Errno 2] No such file or directory: 'hk'`
- `tests/test_lint_delta.py::test_the_baseline_revision_is_the_previous_lockfile_change`
- `tests/test_session_review.py` — 5 tests; "invalid goal history: cannot resolve the fixed origin/main goal-history baseline", and "assert 'requires --source-repo-root' in 'invalid goal history: ...'"
- `tests/test_shell_integration.py` — 9 tests: `test_tool_execution_in_login_shell[claude|codex|gemini|pixi]`, `test_tool_reachable_in_login_shell[claude|codex|gemini|pixi]`, `test_zshenv_path_injection`. Errors include `FileNotFoundError: 'zsh'` and "claude not found in login shell PATH"
- `tests/test_skillopt_provenance.py::test_manifest_is_stable_and_all_live_objects_bind`

Six failures across the run reported a bare `assert 2 == 1` and could not be
attributed to a test. Work out which ones they belong to.

For each of the 6 test FILES report:
1. bucket (TOOLCHAIN / GIT-DEPTH / HOST-ONLY / MIXED — and if MIXED, which
   individual tests fall where),
2. the specific line(s) that make it so, cited `file:line`,
3. **whether the file is parametrized such that SOME parameters would pass in CI
   while others cannot** — this matters most: a file-level marker would then drop
   coverage that could have run.

Read the test files and any helper/fixture they use. Do not run the suite.

## GitHub repos touched

- [ray-manaloto/dotfiles](https://github.com/ray-manaloto/dotfiles) — the repo triaged.
