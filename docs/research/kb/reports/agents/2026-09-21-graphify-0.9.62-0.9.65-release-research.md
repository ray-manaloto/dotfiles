# graphify 0.9.62 → 0.9.65 release research

**Agent**: research lane (read-only w.r.t. repo source; writes only this file)
**Date**: 2026-09-21
**Pinned here**: `graphifyy[all]==0.9.61` (`python/pyproject.toml`)
**PyPI latest**: 0.9.65

_Status: IN PROGRESS — appended incrementally as each source is read._

## Probe log (chronological)

- `gh api repos/Graphify-Labs/graphify/releases --jq '.[] | .tag_name'` → returns
  v0.9.65 (2026-09-20T17:47:20Z), v0.9.64 (2026-09-18T21:16:28Z),
  v0.9.63 (2026-09-16T16:31:13Z), v0.9.62 (2026-09-15T17:58:20Z), and older.
  Control arm: the same command returns v0.9.61 (2026-09-12T21:24:20Z), the
  version this repo pins — so the probe can see the versions we already know
  exist.
- Installed version confirmed:
  `python/.venv/lib/python3.14/site-packages/graphifyy-0.9.61.dist-info/METADATA:3`
  → `Version: 0.9.61`.
- `curl https://pypi.org/pypi/graphifyy/0.9.65/json` → wheel
  `https://files.pythonhosted.org/packages/1f/9e/b2c3f6704306fa3c724cdaaa19391e3be14953394436013865569015eb74/graphifyy-0.9.65-py3-none-any.whl`,
  unpacked to the scratchpad at `…/scratchpad/gfy/w65/graphify/`. **Not installed
  into the project venv** — the pinned 0.9.61 tree is untouched.

## Q1 — Release notes for 0.9.62 / 0.9.63 / 0.9.64 / 0.9.65

**Verdict: four releases, ~40 changes, and NONE of them touch any surface this
repo's automation depends on.** Every change is an extractor/resolver fix, a
graph-correctness fix, or a docs/security bump. Source:
`gh api repos/Graphify-Labs/graphify/releases/tags/v0.9.6N --jq .body` (full
bodies transcribed below verbatim).

### Flagged-surface matrix

| Surface the automation touches | Touched in 0.9.62-0.9.65? | Evidence |
|---|---|---|
| `graphify update` (the command `graphify-update` runs) | **Indirectly, 3×** — all graph-correctness, no CLI/contract change | 0.9.62 atomic-write of `graph.json` on the watch/rebuild path (#3555); 0.9.64 incremental `update` reconciles Markdown-family links (#3655); 0.9.65 incremental rebuild no longer purges AST nodes it reported as fail-closed "kept" (#3697/#3695) |
| `install` / skill files | **No** | zero release-note mentions; confirmed by byte-diff in Q2 |
| `_PLATFORM_CONFIG` | **No** | see Q2 diff (identical) |
| `skill.md` / `skill-codex.md` / `skill-agents.md` filenames | **No** | all three present in the 0.9.65 wheel under the same names |
| `references/` sidecars | **No** | see Q2 |
| `.graphify_version` | **No** | see Q2 — the stamp-writing code is byte-identical apart from nothing; `install.py`'s ONLY diff is a hook `timeout` |
| `built_at_commit` in the export | **No** | see Q5 |
| `query` token budget / truncation | **No** | see Q5 |
| `label` (LLM vs local) | **No** | see Q3 |
| data-loss / shrink guards | **YES — 0.9.65 #3697/#3695 is a SHRINK-GUARD DEADLOCK FIX** | "the eviction pass re-checks the kept set, so a moved-file/symlink layout can't deadlock the shrink guard into refusing every update" |
| Python 3.14 support | **Not mentioned in any of the four**; the 0.9.65 security note discusses Python **3.10** glibc wheels only | 0.9.65 body, Pillow CVE-2026-54058 item |

**The one item that matters operationally is 0.9.65 #3697/#3695.** This repo's
shrink guard is the reason the claude SKILL.md carries a hand-added
`raise SystemExit(1)` (see Q4) — and 0.9.65 fixes an upstream bug where the
shrink guard could refuse *every* update on a moved-file/symlink layout. That is
a reason to take the bump, not a reason to fear it.

### v0.9.62 (2026-09-15T17:58:20Z) — verbatim

- Feature: Terraform module calls with a literal local `source` (`./…` or `../…`) now resolve to a directory-scoped module node, exposing the caller→implementation topology (e.g. environment → application → base); remote and registry sources and source expressions are left unresolved and never fabricate a target. After upgrading an existing graph, run `graphify update .` once to regenerate Terraform ids and topology (#3571, thanks @vstepko).
- Feature: a bare implicit-self call in a Ruby method now resolves up the inheritance chain to a method defined on a superclass, including across files. The promotion is conservative — it only upgrades an existing inferred edge when a single owning class, a matching method kind, and one unambiguous ancestry chain all agree, and it bails on mixins, reopened classes, and dynamic superclasses rather than guess (#3569, thanks @oleksii-tumanov).
- Feature: an inline code span in a Markdown file (e.g. `` `Widget.render` ``) now emits a `references` edge to the symbol it names, but only when exactly one corpus callable matches — generic words and ambiguous names link to nothing, so docs join the graph without a false-edge firehose (#3562, thanks @AstroMined).
- Fix: a prose file whose stem is the bare plural "tokens" (e.g. `TOKENS.md`) is no longer excluded as a credential dump; singular `token.md` and other keywords' bare plurals still exclude, and content-based secret detection is unchanged (#3527, thanks @HARSHAVARDHAN-RAJU5).
- Fix: a call whose only resolution target is a sourceless external stub no longer emits a spurious `calls` edge, so an unresolved external name stops accruing a phantom god-node (#3156, thanks @DevChiniwala).
- Fix: calls made inside a module-level anonymous closure (an IIFE or a top-level callback) are now attributed to the file node instead of being dropped (#3124, thanks @ayushcodes10).
- Fix: cross-repo merge now respects C# namespace/`using` scoping — a parked member call binds to a same-named type in another repo only when the caller's namespace or an in-scope `using` resolves it, so a third-party receiver no longer binds to an unrelated repo-local type (#3360, thanks @DevChiniwala).
- Fix: a bare Lua `require("mod")` statement (no assignment) now emits an `imports` edge, including the chained `require("lazy").setup({})` LazyVim idiom, so Neovim configs graph correctly instead of coming out as disconnected files (#3320, thanks @A-Levin).
- Fix: the Claude/CodeBuddy PreToolUse guard hooks now carry a timeout, so a wedged filesystem stat can no longer stall a tool call indefinitely (#3314, thanks @ayushcodes10).
- Fix: id-prefix reconstruction is now Unicode-aware — a non-Latin path segment is slugified with the canonical normalizer instead of being dropped, so files under non-ASCII directories keep stable ids (#3559, thanks @ayushcodes10).
- Fix: `cohesion_score` now excludes self-loops (e.g. a recursive call) from the edge count, so a community's score stays within 0..1 instead of being inflated above 1 (#3558, thanks @jordanalexanderp18-rgb).
- Fix: the incremental watch and rebuild paths now route the `graph.json` replace through the shared atomic-write fallback at both write sites, so a cross-drive or locked-file replace can no longer corrupt the graph mid-update (#3555, thanks @ayushcodes10).
- Fix: a semantic-cache group whose stored `source_file` is malformed is now recovered by basename instead of being dropped, with the recovered group's `source_file` corrected before it is written back; an ambiguous basename stays skipped (#3553, thanks @ayushcodes10).
- Fix: an exported destructuring declaration (`export const { a, b } = obj`) now emits one node per exported name instead of a single combined node, so each name resolves and cross-file imports of a split export link correctly (#3552, #2604, thanks @ayushcodes10).
- Fix: a community made up entirely of file nodes is now counted as thin rather than invisible in the report, so it is no longer silently dropped from the community summary (#3549, #3548, thanks @ayushcodes10).
- Fix: the git guard-hook shell gates now resolve the output directory from `GRAPHIFY_OUT` instead of a hardcoded `graphify-out/`, so a renamed output directory no longer makes the hooks silently no-op (#3546, thanks @breken-ai).

### v0.9.63 (2026-09-16T16:31:13Z) — verbatim

- Feature: Elixir `alias`/`import`/`require`/`use` targets now resolve onto the module's `defmodule` node across files, so the internal module dependency graph is no longer dropped as dangling. Only top-level modules are indexed (a nested `defmodule`, labeled with its bare inner name, cannot capture an unrelated `use <Name>` from another file), and a same-file reference is left unresolved so it cannot clobber the structural `contains` edge (#3603, thanks @ayushcodes10).
- Feature: a Rust `self.method()` call now resolves to a method defined on the same type in another file (the common split-`impl`-block layout), pooling methods across every `impl` of one type and refusing to link when two unrelated types share a bare name (#3602, thanks @ayushcodes10).
- Feature: a Ruby member call `obj.foo` on a known-type receiver now resolves to a method `foo` inherited from a superclass, including across files, using the same conservative promotion as the implicit-self resolver — a single owning class, matching method kind, and one unambiguous ancestry chain, or it stays dangling (#3585, thanks @oleksii-tumanov).
- Fix: every edge endpoint written to `graph.json` is now a declared node. An `imports`/`imports_from`/`re_exports` edge to an external module (stdlib, a third-party dependency) mints a typed external stub node instead of leaving a dangling endpoint that loaders materialise as an attribute-less phantom, and a cross-repo merge unifies the same external module into one global node instead of fragmenting it per repo; sourceless external call targets stay suppressed (#2873, #2878, thanks @AromalBiju1).
- Fix: a Markdown code-span mention edge now survives an incremental rebuild instead of being pruned as an unauthored link, and a dotted span (`Foo.bar`) resolves using its qualifiers as evidence, rejecting misleading matches such as `time.sleep` or `pyproject.toml` (#3587, thanks @AstroMined).

### v0.9.64 (2026-09-18T21:16:28Z) — verbatim

- Feature: Terraform block attributes (`ami`, `instance_type`, `cidr_block`, tags, and the like) are now preserved on the resource/data/module node and are queryable and searchable, with typed values (bool/number/list/map) and nested blocks kept separate from direct attributes. Secret-named attribute values (`password`, `*secret*`, `*token*`, `*_key`, connection strings) are redacted before they reach `graph.json` or the model, so a hardcoded credential in a `.tf` file does not leak (#3644, thanks @hopstreax).
- Feature: JavaScript inside an inline `<script>` block of a PHP file is now indexed as JS (functions and calls) under the PHP file node, mirroring the Vue/Svelte embedded-script handling, with source lines mapped back to the real file positions (#3627, #2320, thanks @ayushcodes10).
- Feature: a Rust `self.method()` call now resolves across files for simple generic impls (`impl<T> Foo<T>`), extending the split-`impl` resolution to generic types while staying fail-closed on bounded, `where`, trait, and concrete-instantiation shapes (#3653, thanks @oleksii-tumanov).
- Feature: a Kotlin `Receiver.method()` call now resolves across files when the receiver's `object`/class (or its `companion object`) is declared in another file (#3598, #1698, thanks @ayushcodes10).
- Feature: a C++ scope-qualified static call `Foo::bar()` now resolves to a definition in another translation unit even when it survived extraction only as a qualified-label node (#3613, #2348, thanks @ayushcodes10).
- Fix: an npm package subpath import (`import x from "pkg/sub"`) now resolves to the same node as the bare package import, so a dependency no longer fragments into separate external nodes (#3601, thanks @ayushcodes10).
- Fix: `export` now detects a stale `.graphify_analysis.json` sidecar and reconstructs communities from `graph.json`, comparing partition structure rather than just the node-id set, so a stale sidecar can no longer override fresh `update` data (#3557, #2386, thanks @ayushcodes10).
- Fix: incremental `update` now reconciles Markdown-family links across `.md`/`.mdx`/`.qmd`/`.skill`, and a document whose parse fails no longer has its authored links pruned (#3655, thanks @oleksii-tumanov).
- Fix: extraction diagnostics now separate external references (out-of-corpus `$ref`/import targets) from genuinely dangling edges, so an expected external reference is no longer reported as a broken endpoint (#3590, thanks @DevChiniwala).

### v0.9.65 (2026-09-20T17:47:20Z) — verbatim

- Security: the `svg`/`all` extras now floor Pillow at `>=12.3.0` for CVE-2026-54058 (Pillow was pulled in transitively via matplotlib). Note: Pillow 12.3.0 dropped its glibc<2.27 cp310 Linux wheel, so a very old-glibc Python 3.10 host with the `svg`/`all` extra builds Pillow from sdist (#3698, thanks @viral-antuit).
- Feature: a Go interface's method requirements (`type Foo interface { Bar() }`) now attach to the interface node, so calls resolve to them (#3672, thanks @rajatnagda45).
- Feature: a Swift protocol's method requirements (`protocol P { func f() }`) now attach to the protocol node (#3673, thanks @rajatnagda45).
- Fix: Java enum body members (methods/constructor/fields after the constants) now attach to the enum, not the file, and intra-enum calls resolve (#3674, thanks @rajatnagda45).
- Fix: a Verilog module instantiation now links to the module's local definition instead of a phantom duplicate; a genuinely external module stays a sourceless stub (#3675, thanks @rajatnagda45).
- Fix: JS/TS `let`/`const` bindings are now scoped to their own block rather than the whole function, so a block-local binding no longer suppresses a genuine `indirect_call` edge elsewhere in the function; `var` stays function-scoped (#3688, thanks @ayushcodes10).
- Fix: the incremental rebuild no longer purges AST nodes it just reported as fail-closed "kept" — the eviction pass re-checks the kept set, so a moved-file/symlink layout can't deadlock the shrink guard into refusing every update (#3697, #3695, thanks @hopstreax).
- Fix: `graph.html` no longer crashes vis-network with a stack overflow on large graphs — nodes are seeded on a spiral before physics runs so overlap-avoidance can't blow the layout recursion (#3699, thanks @sanjaiyan-dev).
- Fix: node and edge tooltips now show special characters literally (C++ templates like `vector<int>`, generics, `&`, quotes) instead of raw HTML entities, while the HTML sinks that need escaping keep it (#3686, #3664, thanks @hopstreax).
- Docs: repository links now point at `Graphify-Labs/graphify` instead of the old account (including in generated wiki output), translated READMEs use the current logo, GitHub issue/PR templates were added, and the Enterprise link was corrected (#3692, #3694, #3693, thanks @Abdul535).

**Note on the 0.9.62 Terraform item**: "After upgrading an existing graph, run
`graphify update .` once to regenerate Terraform ids and topology." This repo
has no `.tf` files (`git ls-files '*.tf'` → 0), so that migration note is inert
here — but a full `graphify-update` after the bump satisfies it regardless.

> **Self-correction (recorded rather than silently edited).** An earlier draft of
> the matrix above asserted `.graphify_version` is "OUR file, not graphify's".
> That was written ahead of the probe and is **WRONG** — it is graphify's own
> file, written by `graphify/install.py::_write_version_stamp`. The row has been
> corrected. The probe that caught it is in Q2.

## Q2 — Does graphify NATIVELY refresh installed skill surfaces, or check `.graphify_version` staleness? (0.9.61 and 0.9.65)

**Verdict: NO native refresh, in either version. There IS a native staleness
WARNING — and it is structurally blind to this repo's project-scoped skills.**
Nothing here changed between 0.9.61 and 0.9.65.

### 2a. `graphify update` never touches skills — both versions

```
grep -n "from graphify.install import|import install|_copy_skill_file|install_skill|install_for_platform" cli.py build.py watch.py
  → 0 hits (0.9.65)
```
Control arm: the same grep shape finds `_PLATFORM_CONFIG` in `install.py` and
`__main__.py`, so the probe discriminates. The `update` command branch is
`graphify/cli.py:2403` (`elif cmd == "update":`) and its whole argument surface
is `--force` / `--no-cluster` / one optional path — no install/refresh flag.

**There is no graphify command or flag that refreshes an already-installed skill
surface other than `install` itself**, which this repo bans (`do-not.md` #8).

### 2b. The native staleness check: warn-only, and USER-SCOPE only

`graphify/__main__.py:163` defines `_check_skill_version(skill_dst, platform_names=None)`.
Its single call site is `__main__.py:526`, inside `_run_cli()`:

```python
_silent_cmds = {"install", "uninstall", "hook-check", "hook-guard"}
if not any(arg in _silent_cmds for arg in sys.argv):
    for skill_dst in {_platform_skill_destination(name) for name in _PLATFORM_CONFIG}:
        _check_skill_version(skill_dst)
```

Three facts, each load-bearing for the automation:

1. **It only prints to stderr.** Three messages exist (`__main__.py:196-234`):
   "skill dir exists but SKILL.md is missing", "skill `references/` sidecar is
   missing", and the version mismatch — the last of which tells you to run
   `graphify install --platform <p>`. **It never copies anything.**
2. **It resolves USER scope.** `_platform_skill_destination` is declared
   `def _platform_skill_destination(platform_name, *, project: bool = False, project_dir=None)`
   (`install.py:81`) and every non-project branch returns a `Path.home()`
   location. The call site at `__main__.py:526` passes **no** `project=`, so
   every path it checks is under `~`. This repo's skills live at
   `<repo>/.claude/skills/graphify/` and `<repo>/.codex/skills/graphify/`, which
   that loop **never visits**. The native warning therefore cannot fire for us —
   it is a check that, for this repo, can only stay silent.
3. **`update` is NOT in `_silent_cmds`**, so the check does run on a
   `graphify update` — it just looks in the wrong place for us.

### 2c. 0.9.61 → 0.9.65 diff of the install/skill surface

`diff -rq` over the two package trees (`__pycache__` excluded) lists 26 differing
`.py` files and one new file (`markdown_resolution.py`). **Not one skill markdown
file and not one `skills/**/references/*.md` appears in that list** — i.e. every
one is byte-identical. Explicit checksum confirmation for the three this repo
copies:

| file | 0.9.61 sha256 | 0.9.65 | verdict |
|---|---|---|---|
| `skill.md` (claude) | `3e4d30df5a936965acaa25117230a6f3ff44353c137e63030e85e3bc3a391fb7` | identical | **SAME** |
| `skill-codex.md` | `3898b1042721cad27d6f87ca44f638415a7f763203aefd86b90f9358ffea3a43` | identical | **SAME** |
| `skill-agents.md` | `7a6d60d8c3fd4384252c23c69c1c82643c6a86dee967f5dfa9f23169fea2cb1a` | identical | **SAME** |

Control arm for that comparison: the same `shasum` pair on `install.py` reports
DIFF, so the probe can produce the other answer.

`skills/` platform dirs are identical in both trees (14 entries: agents, amp,
claude, claw, codex, copilot, droid, kilo, kiro, opencode, pi, trae, vscode,
windows). `skills/claude/references/` and `skills/codex/references/` each hold
the same 8 files at the same byte sizes in both (`add-watch.md` 2486,
`exports.md` 3362, `extraction-spec.md` 7960, `github-and-merge.md` 2177,
`hooks.md` 1267, `query.md` 13456, `transcribe.md` 3173, `update.md` 10425).

### 2d. `install.py`'s ENTIRE diff, 0.9.61 → 0.9.65

One hunk. `diff -u` output in full:

```diff
@@ -338,6 +338,14 @@
     var can force it on or off at runtime without a reinstall.
+
+    Each entry carries a ``timeout`` (#3314): unset, Claude Code defaults a
+    command hook to 600s, so a single wedged guard ... stalls the surrounding
+    tool call for ten minutes ... The guard itself measures ~170ms warm; 10s is
+    generous headroom ...
     """
@@ -345,9 +353,9 @@
     return [
         {"matcher": "Bash|Grep",
-         "hooks": [{"type": "command", "command": f"{exe} hook-guard search"}]},
+         "hooks": [{"type": "command", "command": f"{exe} hook-guard search", "timeout": 10}]},
         {"matcher": "Read|Glob",
-         "hooks": [{"type": "command", "command": read_cmd}]},
+         "hooks": [{"type": "command", "command": read_cmd, "timeout": 10}]},
     ]
```

That is it: 2367 → 2375 lines, +8. **`_PLATFORM_CONFIG` is unchanged**, and
`__main__.py` is byte-identical between the two versions (`diff -u` → empty,
rc=0).

`_PLATFORM_CONFIG` entries for the three platforms this repo cares about, read
from the 0.9.65 tree (identical in 0.9.61 by the diff above):

```python
"claude": {"skill_file": "skill.md",        "skill_dst": .claude/skills/graphify/SKILL.md, "claude_md": True,  "skill_refs": "claude"},
"codex":  {"skill_file": "skill-codex.md",  "skill_dst": .codex/skills/graphify/SKILL.md,  "claude_md": False, "skill_refs": "codex"},
"agents": {"skill_file": "skill-agents.md", "skill_dst": .agents/skills/graphify/SKILL.md, "claude_md": False, "skill_refs": "agents"},
```

**Consequence for the planned automation: the bump to 0.9.65 changes ZERO bytes
in what `graphify_skill.py::install_skill()` copies.** The only thing a refresh
after the bump changes on disk is the `.graphify_version` stamp (0.9.61 → 0.9.65)
and, for the claude platform, whatever hand-edits this repo has layered on top
of the packaged `skill.md` (see Q4 — that is the real risk, and it is OURS, not
graphify's).

## Incident — an accidental `graphify label` run (disclosed, not buried)

At 18:47 I ran `graphify label` against this repo's graph **by accident**: I
grepped for the literal string ``Run `graphify label` `` inside a double-quoted
zsh string, and the backticks command-substituted. Mine, not a tool defect;
every probe since uses single quotes.

Verified aftermath — no data loss:

| check | result |
|---|---|
| `mise run graphify-health` | `fresh (runtime=0.9.61)`, rc=0 |
| `built_at_commit` vs HEAD | `10a17128…` == `git rev-parse HEAD`. Match. |
| labels before / after | 1952 entries / 1952 entries, **0 placeholders both** |
| named → placeholder regressions | **0** |
| `git status` | clean but for the two untracked agent reports; `graphify-out/` is gitignored |
| tokens spent | Gemini free-tier API quota only (4 of 20 batches 429'd and retried). **Zero Claude/agent tokens.** |

Pre-run state is recoverable at `graphify-out/2026-09-21/` (mtime 18:29,
predates the run).

**It produced real evidence for Q3, so it is reported rather than discarded.**
The pre-run labels were graphify's deterministic no-LLM fallback; the post-run
ones are LLM names. 1540 of 1952 changed:

| cid | before (no-LLM hub label) | after (LLM) |
|---|---|---|
| 0 | `properties` | `API and Model Configuration` |
| 1 | `claude-code.d.ts` | `Claude Code Agent Types` |
| 2 | `ContainerConfig` | `Mise Configuration Management` |
| 3 | `MonkeyPatch` | `Graphify CLI Entrypoints` |
| 4 | `main.py` | `Autofix and CLI Commands` |

## Q3 — Does `graphify label` have a no-LLM / local mode in 0.9.65?

**Verdict: there is no "local model" mode built in, but there IS a fully
deterministic LLM-free labeler, and it is already the default in the path this
repo uses. `llm.py` is byte-identical between 0.9.61 and 0.9.65** (`diff -q` →
identical; control arm: the same `diff -q` on `cluster.py` reports DIFFERS), so
nothing about labeling changed across the four releases.

### The three real options, from the source

**(i) Never label with an LLM — `graphify/cluster.py:184-208`,
`label_communities_by_hub`.** Not a placeholder: it names each community after
its **highest-degree member** ("the structural hub"), ties broken by node id
"for run-to-run stability". Its own docstring: *"Used as the default
(no-backend) labeler; an LLM naming pass, when configured, overrides these."*

- PRO: zero tokens, zero network, zero cost, deterministic and reproducible.
  Readable — `auth` / `log_action`, never `Community 70`.
- CON: names are **identifiers, not concepts**. Measured on this repo above:
  `properties`, `main.py`, `ContainerConfig`, `MonkeyPatch`. A community whose
  hub is a generic name gets a generic label, and `main.py` tells a querying
  agent nothing about what the community *does*.
- Reach it with `--no-label`… **but do not.** `cli.py:2233-2235` shows
  `--no-label` assigns literal `Community {cid}` placeholders — it is NOT the
  hub labeler. The hub labeler is what runs in the `else` branch
  (`cli.py:2237-2244`) and in the reuse branch (`cli.py:2223`) when a community
  changed. `--no-label`'s placeholders were bad enough to need their own bug
  (#2073: persisting them "made the reuse branch treat them as fresh forever,
  permanently blocking real labeling").

**(ii) Label with a local model — supported, via `ollama` or an
OpenAI-compatible local server.** `llm.py:127-129` declares an `ollama` backend
at `http://localhost:11434/v1` (`OLLAMA_BASE_URL`/`OLLAMA_HOST` honored,
`llm.py:61-75`); `llm.py:149-155` notes `OPENAI_BASE_URL` "points the backend at
any OpenAI-compatible server (llama.cpp, vLLM, LM Studio, ...)". Select with
`--backend ollama`.

- PRO: semantic names with **zero API cost and zero agent tokens** — the only
  option that gets concept-level labels for free.
- CON: needs a local server running; `__main__.py:574` documents that
  `--max-concurrency` is **forced to 1 for ollama/claude-cli**, so 1952
  communities at `--batch-size` 100 is 20 serial calls — slow. Quality depends
  entirely on the local model. Not something a `mise run graphify-update` can
  assume is up.

**(iii) Label via an API — what actually ran here.** `detect_backend()` is
key-based; this machine has a Google key, so it chose Gemini and 4 of 20 batches
429'd on the free tier before retrying through.

- PRO: best names, no local infrastructure.
- CON: per-run API cost, rate limits, and non-determinism (re-labeling renames
  communities, churning `GRAPH_REPORT.md` and `graph.json`).

### ⚠️ The trap that matters for a zero-agent-token automation

`graphify/llm.py:3512-3520`:

```python
if not backend and _claude_cli_available():
    # `detect_backend` is key-based, and claude-cli is the one backend with no
    # key to find, so it can never be detected there ...
    backend = "claude-cli"
```

**With no API key configured, `graphify label` silently escalates to the
`claude-cli` backend** (declared at `llm.py:209`) if the `claude` CLI is on PATH
— i.e. it spends Claude subscription tokens without being asked. That is exactly
the outcome "zero agent tokens" is meant to prevent, and it is reached by
*removing* configuration, not by adding it. Any automation that may invoke
labeling must pin `--backend` explicitly rather than rely on detection.

Only when there is **no** key AND **no** `claude` CLI does it print
`"[graphify label] no LLM backend configured; keeping Community N
placeholders."` and return placeholders (`llm.py:3521-3528`).

### Recommendation for Q3

**Do not put labeling in the automation at all.** `graphify update` already
produces good-enough labels with zero tokens: the reuse branch keeps earned
labels whose membership signature still matches, and re-hubs only the
communities that actually changed (`cli.py:2196-2231`). Labeling stays a
deliberate, operator-initiated act. If richer names are wanted later, `--backend
ollama` is the only option that is both semantic and free.

## Q4 — Does 0.9.65's packaged claude skill already `raise SystemExit(1)` at the shrink guard?

**Verdict: NO. It does not, and this repo's hand-added line is still load-bearing.
An unconditional skill refresh would silently revert it.**

Packaged `skill.md` (0.9.65), lines 521-524 — Step 5's re-export guard:

```python
wrote = to_json(G, communities, 'graphify-out/graph.json', community_labels=labels)
if not wrote:
    print('ERROR: refused to shrink graphify-out/graph.json (existing graph has more nodes; #479).')
    print('If this shrink is intentional (you deleted files), re-run a full build with --force.')
print('Report updated with community labels')      # <-- falls straight through
```

Two prints, then execution **continues** and the script exits 0. The repo added
the missing exit at `.claude/skills/graphify/SKILL.md:524`.

The `diff -u` of the packaged 0.9.65 `skill.md` against the repo's live
`.claude/skills/graphify/SKILL.md` is **exactly one hunk, one added line**:

```diff
@@ -521,6 +521,7 @@
 if not wrote:
     print('ERROR: refused to shrink graphify-out/graph.json (existing graph has more nodes; #479).')
     print('If this shrink is intentional (you deleted files), re-run a full build with --force.')
+    raise SystemExit(1)
 print('Report updated with community labels')
```

Control arm for the diff: the same `diff -u` of packaged `skill-codex.md`
against `.codex/skills/graphify/SKILL.md` returns **rc=0, byte-identical** — so
the probe can report "no difference" and did not here.

Note the packaged file *does* carry `raise SystemExit(1)` at lines 419 and 437
(Step 4's guard). Only the **Step 5** site is missing it, in both 0.9.61 and
0.9.65 (the two files are byte-identical per Q2).

### Consequence for the planned automation — the one real hazard

| platform | repo copy vs packaged 0.9.65 | effect of an auto-refresh |
|---|---|---|
| **codex** (`.codex/skills/graphify/SKILL.md`) | byte-identical | **no-op, safe** |
| **claude** (`.claude/skills/graphify/SKILL.md`) | +1 line (`raise SystemExit(1)`) | **SILENTLY REVERTS the fix** |

`graphify_skill.py::install_skill()` does write `SKILL.md.bak` before replacing a
differing destination (`python/src/dotfiles_setup/graphify_skill.py`, the
`.bak` branch), so the line is recoverable — but the LIVE skill would lose it,
and nothing would fail. This needs an explicit decision before the automation
ships; see the recommendation section.

## Q5 — Does the repo's `graphify.py` / `graphify_skill.py` rely on anything 0.9.62-0.9.65 renamed or removed?

**Verdict: NO. Every symbol, CLI surface, output string and JSON field the two
repo modules depend on survives unchanged in 0.9.65.** The bump is API-safe. But
there are **six hard-coded `0.9.61` sites** that must move in the same diff, one
of which is a live gate.

### Every dependency, checked against the 0.9.65 source

| What the repo depends on | Repo site | 0.9.65 status |
|---|---|---|
| `graphify.install._PLATFORM_CONFIG` (keys `skill_file`, `skill_dst`, `skill_refs`) | `graphify_skill.py::_platform_config` | **unchanged** — `install.py` diff is one hook-`timeout` hunk (Q2d) |
| `graphify.install.__file__` → package root, `skills/<bundle>/references/` layout | `graphify_skill.py::_package_root`, `resolve_placement` | **unchanged** — 14 platform dirs, same 8 reference files, same bytes |
| `built_at_commit` in `graph.json` | `graphify.py::_staleness_problem` | **unchanged** — `export.py` is **byte-identical** (`diff -q` → IDENTICAL; control arm: `diff -q` on `cluster.py` → DIFFERS) |
| `graphify.export._git_head` | cited in the same docstring | present, `export.py:202` |
| `node_link_data(G, edges="links")` → `"links"` key | `graphify.py::_edges_field` | present, `export.py:331` |
| `graphify.export.prune_dangling_edges` (the fallback's justification) | `_edges_field` docstring | present, `export.py:419` |
| `graphify query "<q>" --budget N [--context C] [--dfs] --graph P` | `build_query_args` | all four flags present, `__main__.py` help block |
| `graphify affected <node> …` | `affected_args` | `cli.py` branch present |
| `"No unique node match for …"` at rc 0 | `graphify.py:520` comment + handling | present, `affected.py:269` |
| `[!] TRUNCATED:` output prefix | `graphify.py:472` | present, `serve.py:1247` and `:1276` |
| `graphify prs` | `graphify.py:580` | `cli.py` branch present |
| `graphify update <target>` | `graphify.py:656` | `cli.py:2403` branch present |
| `graphify hook-guard <kind>` stdout | `graphify.py:706` | `cli.py` branch present |
| distribution name `graphifyy` | `_runtime_version`, `_DIST_NAME` | unchanged on PyPI |

⚠️ One latent coupling worth knowing, though **not live today**:
`graphify.py:458-459` raises `GraphifyIncompleteError` **whenever `graphify query`
writes anything at all to stderr**. Per Q2b, `_check_skill_version` writes a
stale-stamp warning to stderr on every non-silent command — including `query`.
If a USER-SCOPE graphify skill ever gets installed, a version-stamp mismatch
would turn every `graphify-query` into an `incomplete` error. Probed all nine
user-scope destinations: **all absent** (control arm: `~/.claude` reports
PRESENT with the same probe), so the hazard is dormant. Do-not.md #8 is what
keeps it dormant.

### The `0.9.61` sites that must move together

| # | Site | Kind |
|---|---|---|
| 1 | `python/pyproject.toml:9` — `"graphifyy[all]==0.9.61"` | the pin |
| 2 | `python/pyproject.toml:57` — `override-dependencies = ["graphifyy[all]==0.9.61"]` | the pin (second) |
| 3 | **`python/src/dotfiles_setup/graphify.py:335-336`** — `if runtime != "0.9.61": return VERSION_DRIFT` | **LIVE GATE** |
| 4 | `tests/test_graphify.py:63` — `assert dependency == "graphifyy[all]==0.9.61"` | test |
| 5 | `python/uv.lock` — 5 occurrences incl. sdist/wheel sha256 | lock |
| 6 | prose: `mise.toml:808`, `graphify.py:170`/`:680`, `bash_budget.py:111`, `.claude/rules/graphify-first.md:47` | docs |

**#3 is the one that bites.** `graphify_health` hard-codes the expected version
as a string literal; bump the pin without it and every `mise run graphify-query`
returns `version_drift`, which `graphify-first.md` tells agents means "the graph
is unavailable, fall back to source".

### ⚠️ Discovered while probing: the two graphify installs have ALREADY drifted

`.claude/rules/graphify-first.md:47` states: *"The two installs are aligned as of
2026-09-14 — both 0.9.61."* **That is now false.**

```
$ which -a graphify
/Users/rmanaloto/.local/share/mise/installs/pipx-graphifyy/0.9.65/bin/graphify
$ graphify --version                        → graphify 0.9.65
$ uv run --project python graphify --version → graphify 0.9.61
$ grep graphify ~/.config/mise/config.toml
199:"pipx:graphifyy" = { version = "0.9.65", extras = ["all"], minimum_release_age = "0s" }
```

The user-global mise pin is already on **0.9.65**; this repo is on **0.9.61**.
Two consequences:

1. **The rule file is stale and should be corrected in the bump diff** — it
   currently tells a reader the alignment is a present-tense fact.
2. **My accidental `graphify label` ran under 0.9.65**, because a bare `graphify`
   resolves the user-global pin. `graphify_health` still reports `fresh`, because
   it checks the *installed* (0.9.61) package and `built_at_commit` — and,
   exactly as `graphify-first.md` says, *"nothing records who built it"*. The
   accident is an unintended live demonstration of that documented blind spot.

**`graphify` is NOT in `pin-parity.toml`.** That registry tracks `chezmoi`, `hk`,
`claude-code`, `mise` only (control arm: the same grep prints those four and
returns nothing for graphify). So the drift above had no gate to catch it, which
is precisely the class `pin-parity` exists for — a graphify entry is worth adding
in the same change.

## Q6 — What is renovate.json's "2-doc determinism probe" for graphify bumps?

**Verdict: it is a STALE REFERENCE. The thing it names was retired on
2026-07-21, and its corpus no longer exists on disk. There is currently no
automated gate on a graphify bump.**

The claim lives at `renovate.json:54`, in the `graphifyy` package rule:

> "…That is exactly the class where a green build hides a corrupted graph, which
> no CI check can see - so `automerge: false` stays, **and the 2-doc determinism
> probe gates the merge.**"

Tracing it:

- `docs/research/kb/reports/bake-off-extraction-models.md:23` — *"Extraction
  model bake-off — graphify 0.9.22 on the 2-doc probe corpus. Corpus:
  `docs/research/kb/probe/` — 2 documents."*
- The same file's header, lines 1-21: **"⚠️ SUPERSEDED — do not rank models on
  this document. Retired 2026-07-21 as a ranking corpus."** It explains why: n=1
  per arm, no noise floor, model attribution unverifiable, and the `x-doc` metric
  degenerate on two topically disjoint documents.
- Line 15-18: *"The 2-doc probe survives only as a fast smoke test that the
  harness runs at all. Ranking now happens against `tests/fixtures/graphify-gold/`
  with an answer key, 3 repeats, and a null arm. See
  `python/src/dotfiles_setup/graph_bakeoff.py`."*
- **`ls docs/research/kb/probe/` → "No such file or directory".** The corpus is
  gone. Control arm: `ls tests/fixtures/graphify-gold/` lists 8 files including
  `ANSWER_KEY.json`, so the probe can report a directory that exists.

### What actually exists to validate a bump

| Mechanism | Status |
|---|---|
| `mise run bakeoff` → `dotfiles_setup.graphify.bakeoff` → `graph_bakeoff.py` (32 KB, present) | **exists, but is NOT a gate** — `mise.toml:847-870` says it "writes OUTSIDE the repo" to `~/dev/graphify-bakeoff`; nothing in CI calls it |
| `mise run graphify-health` | exists; would catch `version_drift` and a stale graph — **but would FAIL immediately on a bump until `graphify.py:335` is updated** |
| `python/verification/suites.toml` | the only graphify rows are `[tool.graphify]` / `python_package = "graphifyy"` assertions (`:2074`, `:2095`) — no bump contract |
| `renovate.json` `automerge: false` for `graphifyy` | **real and effective** — a human must read the notes. This is the actual gate. |

`mise run bakeoff -- --repeats 3` against `tests/fixtures/graphify-gold/` is the
successor the retired doc names, and it is what a determinism check on this bump
*should* run. But it is an operator action today, not an automated one.

### Recommended correction

Fix `renovate.json:54` in the bump PR: replace "the 2-doc determinism probe gates
the merge" with what is true — `automerge: false` plus a human read of the
release notes — and point at `mise run bakeoff` (gold corpus, 3 repeats, null
arm) as the optional determinism check. Leaving a gate named in a config that
cannot run is how a reviewer comes to believe a check happened.

## Recommendation for the automation

**NATIVE-FIRST VERDICT: graphify offers nothing native to adopt here. Keep the
custom `graphify_skill.py`.** Per `.claude/rules/tool-currency-and-native-first.md`
rule 6, the written justification:

1. **The only native refresh mechanism is `graphify install`**, and it is banned
   for cause (`do-not.md` #8 — it writes `~/.claude`, appends the root
   `AGENTS.md`, patches `.codex/hooks.json`). Nothing in 0.9.62-0.9.65 adds a
   containable alternative: `update` never imports `install` (Q2a), and
   `install.py` gained exactly one hook-`timeout` hunk (Q2d).
2. **The native staleness check is warn-only and user-scope-only** (Q2b), so it
   is structurally incapable of seeing this repo's project-scoped skills. It is
   not a gate we can adopt.

### The bump itself

**Take 0.9.65.** It is low-risk and carries one fix this repo specifically
wants: #3697/#3695 stops the incremental rebuild deadlocking the shrink guard
into refusing every update. It also **re-aligns the repo with the user-global
pin, which is already on 0.9.65** (Q5). Move all six `0.9.61` sites together —
`graphify.py:335` above all, or health hard-fails.

### The skill-refresh automation — one decision is required first

The refresh is byte-safe for codex and byte-**destructive** for claude:

| platform | effect |
|---|---|
| codex | repo copy is byte-identical to packaged 0.9.65 → **no-op** |
| claude | repo copy = packaged + `raise SystemExit(1)` (Q4) → **silently reverted** |

Since the packaged skill bytes did not change at all between 0.9.61 and 0.9.65
(Q2c), an unconditional refresh on this bump buys **only the `.graphify_version`
stamp** — and pays for it by reverting a real fix. Recommended shape:

1. **Refresh, but re-apply the repo's delta.** Keep the one-line patch as tracked
   data next to the installer and re-apply after the copy, so the stamp advances
   and the fix survives. `install_skill()` already writes `SKILL.md.bak`, so this
   is belt-and-braces, not the only protection.
2. **Fail loudly on an unexpected delta.** If the post-copy diff against the
   packaged file is anything other than the known one-line patch, exit non-zero
   rather than overwrite — this is the `zero-skip-policy` shape, and it converts
   a silent revert into a reviewable failure.
3. **Do NOT put labeling anywhere near it** (Q3). `graphify update` already
   produces zero-token labels, and `graphify label` silently escalates to the
   `claude-cli` backend when no API key is present — the exact opposite of the
   zero-agent-token goal.
4. **Add `graphify` to `pin-parity.toml`** in the same change; the drift in Q5
   had no gate.
5. **Correct two stale prose claims** in the same diff: `graphify-first.md:47`
   ("both 0.9.61") and `renovate.json:54` (the retired 2-doc probe).

### Upstream worth filing

The Step 5 fall-through in the packaged `skill.md` (Q4) is a real upstream defect
— the guard prints an ERROR and exits 0 — and this repo has been carrying the
one-line fix locally since 2026-09-14. Sending it to `Graphify-Labs/graphify`
would delete the delta permanently and make every future refresh a clean no-op.

## GitHub repos touched

- [Graphify-Labs/graphify](https://github.com/Graphify-Labs/graphify) — release
  notes for v0.9.62-v0.9.65 via `gh api`, and the 0.9.65 wheel from PyPI read as
  source (`install.py`, `__main__.py`, `cli.py`, `llm.py`, `cluster.py`,
  `export.py`, `serve.py`, `affected.py`, `hooks.py`, `skill*.md`,
  `skills/*/references/`).
- [ray-manaloto/dotfiles](https://github.com/ray-manaloto/dotfiles) — this repo:
  `python/src/dotfiles_setup/graphify.py`, `graphify_skill.py`, `pin_parity.py`,
  `python/pyproject.toml`, `python/uv.lock`, `mise.toml`, `renovate.json`,
  `pin-parity.toml`, `.claude/skills/graphify/SKILL.md`,
  `.codex/skills/graphify/SKILL.md`, `.claude/rules/graphify-first.md`,
  `tests/test_graphify.py`, `docs/research/kb/reports/bake-off-extraction-models.md`.

_Status: COMPLETE._
