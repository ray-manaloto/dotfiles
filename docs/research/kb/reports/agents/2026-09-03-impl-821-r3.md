# Implementation report — #821 wiring call-site binding, round 3

## Scope

- Change only `tests/test_lock_refresh.py` plus this requested evidence report.
- Bind parsed `mise.toml` and composite-action values to their named call sites.
- Exercise both required mutation arms, run the four required gates, and commit
  without pushing.

## Baseline

- Branch: `fix/820-task-scoped-lock-tools`
- HEAD: `201c7f6d141874c0c5961435a6723c2bdb0753a1`
- Pre-existing untracked reports were present and are excluded from this task.

## Evidence log

### Graphify orientation

Both required repository tasks were attempted before source inspection:

```text
mise run graphify-health
error: Failed to initialize cache at `/Users/rmanaloto/Library/Caches/uv`
  Caused by: failed to open file `/Users/rmanaloto/Library/Caches/uv/sdists-v9/.git`: Operation not permitted (os error 1)

mise run graphify-query -- "How does test_lock_refresh_wiring_calls_the_task_and_cli currently relate tests/test_lock_refresh.py to tasks.lock-refresh-root in mise.toml and the Regenerate root mise.lock step in .github/actions/lock-refresh/action.yml, including parsed config precedents?"
error: Failed to initialize cache at `/Users/rmanaloto/Library/Caches/uv`
  Caused by: failed to open file `/Users/rmanaloto/Library/Caches/uv/sdists-v9/.git`: Operation not permitted (os error 1)
```

Graph evidence is unavailable in this sandbox; source inspection is the fallback
authority.

### Reconciliation with active planning state

The live branch and HEAD supersede the stale branch identity embedded in the
attested root plan. The current notepad records the preceding implementation at
`201c7f6` as complete and explicitly identifies this parsed-value wiring test as
the outstanding next round. No source premise has been accepted without direct
inspection in this round.

### Errors

- Initial combined report/notepad append: `apply_patch verification failed`
  because a display-wrapped notepad line was not the exact on-disk anchor. No
  file changed; the append was retried with the verified final line.

### Source premise check

Direct inspection found no contradiction with the specification:

- `tests/test_lock_refresh.py` imports `tomllib` already and contains the three
  quoted independent substring assertions.
- `mise.toml` parses the `lock-refresh-root` task's `run` as
  `uv run --project python dotfiles-setup lock-refresh-root`.
- `.github/actions/lock-refresh/action.yml` has one step named `Regenerate root
  mise.lock`; that step owns `run: mise run lock-refresh-root` and both token
  environment entries.
- `tests/test_image_manifest.py` and `tests/test_workflow_hooks.py` establish
  PyYAML as an existing test-suite dependency.

Implementation choice: require exactly one parsed step with the requested name,
then assert its parsed `run`, `GITHUB_TOKEN`, and `MISE_GITHUB_TOKEN` values.

### Focused baseline

```text
$ uv run --project python pytest tests/test_lock_refresh.py::test_lock_refresh_wiring_calls_the_task_and_cli -q
.                                                                        [100%]
1 passed in 0.20s
rc=0
```

Log: `/tmp/821-r3-focused-baseline.log`.

### Mutation arm 1 — parsed TOML task value

Exact mutation, confined to the existing task `run` value:

```diff
-run = 'uv run --project python dotfiles-setup lock-refresh-root'
+run = 'uv run --project python dotfiles-setup lock-collect # dotfiles-setup lock-refresh-root'
```

The shell comment preserves the old CLI substring while the command actually
selects a different subcommand. Result:

```text
FAILED tests/test_lock_refresh.py::test_lock_refresh_wiring_calls_the_task_and_cli
E       AssertionError: assert 'uv run --pro...-refresh-root' == 'uv run --pro...-refresh-root'
E         - etup lock-refresh-root
E         + etup lock-collect # dotfiles-setup lock-refresh-root
1 failed in 0.19s
rc=1
legacy substring predicates=(True, True, True); all=True
```

Log: `/tmp/821-r3-mutation-toml.log`. Restoration check:
`git diff --exit-code -- mise.toml` returned 0.

### Mutation arm 2 — parsed composite-action step value

Exact mutation:

```diff
-      run: mise run lock-refresh-root
+      # Mutation control: run: mise run lock-refresh-root
+      run: mise run lock
```

The old raw-file assertion still sees its literal in the comment, but the named
step's parsed command is wrong. Result:

```text
FAILED tests/test_lock_refresh.py::test_lock_refresh_wiring_calls_the_task_and_cli
E       AssertionError: assert 'mise run lock' == 'mise run lock-refresh-root'
E         - mise run lock-refresh-root
E         + mise run lock
1 failed in 0.22s
rc=1
legacy substring predicates=(True, True, True); all=True
```

Log: `/tmp/821-r3-mutation-action.log`. Restoration check:
`git diff --exit-code -- .github/actions/lock-refresh/action.yml` returned 0.

### Gate execution notes

- `mise run lint`, attempt 1: sandbox denied creation of
  `/Users/rmanaloto/.local/state/dotfiles/hk-lint-273897ea-42524.log` before hk
  ran. The captured log ends in `PermissionError: [Errno 1] Operation not
  permitted` and `rc=1`. A retry redirects only `XDG_STATE_HOME` to `/tmp` and
  keeps the real repository task as the command under test.
- `mise run lint`, attempt 2: same denial and rc=1. Inspection found the path is
  fixed as `Path.home() / ".local" / "state" / "dotfiles"` in `lint.py`; the
  command-line handler does not expose the library's explicit-log-path seam.
  `XDG_STATE_HOME` therefore cannot redirect it, and changing `HOME` is
  prohibited. Lint remains sandbox-unverified. Retry log:
  `/tmp/821-r3-lint-retry.log`.
- Full pytest, attempt 1: rc=1 after `673 passed`; the unrelated
  `tests/test_dag_tick.py::test_read_proc_start_reads_a_stable_value_for_a_live_process`
  received `None` from `read_proc_start(os.getpid())`. Full captured summary:
  `1 failed, 673 passed, 11 deselected in 33.73s`. Log:
  `/tmp/821-r3-pytest.log`. Classification pending a focused diagnostic.
- One incremental report/notepad append failed before changing files because
  the notepad's multi-line context did not exactly match its on-disk wrapping.
- Pytest classification: `read_proc_start` invokes `ps -o lstart=`. Direct
  probes against both the current shell PID and PID 1 returned
  `zsh: operation not permitted: ps` with rc=127. The suite failure is therefore
  a sandbox denial on an unrelated live-process test, not evidence against this
  change. The required full pytest gate remains sandbox-unverified.
- `mise run verify`: rc=0; `146 passed, 0 failed, 4 skipped`. Log:
  `/tmp/821-r3-verify.log`. The sandbox's mise purgatory-cleanup warning remains
  visible in the log.
- `mise run pin-actions`: rc=3. `pinact run --verify` attempted its real GitHub
  resolution path, but every lookup failed with `dial tcp: lookup
  api.github.com: no such host`. Log: `/tmp/821-r3-pin-actions.log`. The gate is
  network-unverified in this sandbox.

## Final verification record

| Command | Captured result | Classification |
|---|---|---|
| `mise run lint` | rc=1; `PermissionError` writing `~/.local/state/dotfiles/hk-lint-...log` | Sandbox denied; unverified |
| `uv run --project python pytest tests/ -x -q` | rc=1; 673 passed, then live-process test failed because `ps` is denied | Sandbox denied; unverified |
| `mise run verify` | rc=0; 146 passed, 0 failed, 4 skipped | Passed |
| `mise run pin-actions` | rc=3; GitHub API DNS lookup denied | Network denied; unverified |
| `uv run --project python pytest tests/test_lock_refresh.py -q` | rc=0; 24 passed in 2.40s | Supplemental changed-module pass |

Each command wrote a standalone log and appended its own shell exit code; no
gate was piped through a truncating command.

## Diff and restoration summary

- `tests/test_lock_refresh.py`: imports PyYAML, parses both configuration files,
  binds the task command to `tasks.lock-refresh-root`, binds the action command
  and token aliases to the unique `Regenerate root mise.lock` step.
- This report: incremental premise, mutation, restoration, and gate evidence.
- Production/configuration residue: none. Both
  `git diff --exit-code -- mise.toml` and
  `git diff --exit-code -- .github/actions/lock-refresh/action.yml` returned 0.
- `git diff --check`: rc=0.
- HEAD remained `201c7f6d141874c0c5961435a6723c2bdb0753a1` throughout the run.
- The four agent reports that were untracked at baseline were not modified or
  staged by this round.

## Commit attempt

Only the two task-owned paths were named explicitly:

```text
$ git add tests/test_lock_refresh.py docs/research/kb/reports/agents/2026-09-03-impl-821-r3.md
fatal: Unable to create '/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.git/index.lock': Operation not permitted
rc=128
```

The sandbox exposes `.git` read-only. Nothing was staged and no commit was
created. Log: `/tmp/821-r3-git-add.log`.
