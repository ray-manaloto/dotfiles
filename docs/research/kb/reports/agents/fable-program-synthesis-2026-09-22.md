# Fable synthesis — program review across all research lanes (2026-09-22)

> **CORRECTIONS (session review, 2026-09-22d): `daemon version` JSON keys are camelCase (`cliVersion`, `appServerVersion`, `managedCodexVersion`), not snake_case (CHANGE 3); `graphify_native_extract.py:298` is `DEFAULT_BACKEND` (openai-cli refs at 502/580/800); D1 'updater OFF' was rejected (kept ON); the order in §E is superseded by `task_plan.md` Phase 10.**

> Persisted verbatim at receipt by the coordinator (session 2026-09-22d), from the
> `fable-orchestrator:fable-advisor` final message. Inputs: firecrawl-alexandria,
> exa, context7 and last30days program reviews plus the seven earlier deep reports
> in this directory. HTML entities introduced in transit restored.

Read-only lane: nothing written to `findings.md`/`progress.md`; coordinator persists this. Graph is stale (`agentsview` §"Current facts"), so I read source directly.

# A. Verdicts per item

**a. KB corpus — KEEP ruling 1, with one CHANGE in scope.** All three lanes that used `gh api` agree upstream 0.9.65 has no `openai-cli` and #3073 is OPEN (exa §a: `llm.py` 0 hits vs 26 `claude-cli`; firecrawl §a; graphify-features §0). Ray has accepted that loss, but the KB currency engine will **fail closed the day the fork is dropped**: `backend_probes = ["claude-cli","openai-cli"]` (`/Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/currency.toml:49`) and the `[tool.graphify.fork]` block (`:208-214`, whose `clears_when` already spells out the unfork recipe) must go in the same PR, along with KB `CLAUDE.md:66-72` "claude-cli **or** openai-cli" and `graphify_native_extract.py:298` (graphify-features §2 Constraints). Two facts sharpen "full extraction" for pwf/mattpocock: `kb-build --code-only` indexes **0** `.md` nodes (graphify-features G1, measured), so "free AST now, docs later" is exactly right; and pwf carries 18 `SKILL.md` copies with no per-manifest `--exclude` (G4) — the same-name-collision trap (exa §a, terrylica, **unverified at 0.9.65**) needs probe C1 before the pwf/mattpocock manifests are added. `mods/` at v2.1.280 is unverified by every lane (context7 §a) — one `gh api contents` call. Strongest lane: graphify-features (read the pinned source); context7 could not see 0.9.58+ at all (§a control arm).

**b. Codex — KEEP native installer; CHANGE three things; NEEDS-RULING on auto-update and Desktop.**
- Native standalone is the only daemon-capable layout — three independent routes (codex-daemon-history §2 `managed_install.rs:22-28`; codex-desktop §1A; firecrawl §b #46468 `unsupported` for npm). `aqua:openai/codex` cannot serve the daemon either (exa §b caveat; `/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/mise.toml:119-122` records why it was abandoned). `codex update` on an npm install runs `npm install -g`, bypassing mise (context7 §b; codex-desktop §1I). Ruling stands.
- CHANGE 1 — removal breaks two sites the plan doesn't name: `codex_schema.py:28` (`mise exec -- codex --version`) and `schema_vendor.py:119` (reads the version **from the shared.toml npm pin** — this is the current "recorded version" producer). PATH-resolved sites survive (`codex_lane.py:113`, `sdlc_team.py:724`). `ai-cli-invocation.md`'s "only hand-kept argv block" uses `mise exec -- codex`. Renovate coverage of codex ends; the doctor must be what bumps `schemas/sources.toml`.
- CHANGE 2 — install order: remove the mise pin **before** running `install.sh`, else `classify_existing_codex` sees mise's node wrapper and prompts an `npm uninstall -g` (codex-desktop §1D); run with `CODEX_NON_INTERACTIVE=1`; the installer rewrites `~/.local/bin/codex` and appends a PATH block to `.zshrc`/`.zprofile` (exa §b lines 597-628) — chezmoi-templated files.
- CHANGE 3 — codex-doctor must parse `daemon version` JSON `LifecycleOutput` (`cli_version`, `app_server_version`, `managed_codex_version`; context7 §b), never `codex doctor`, which reports healthy on CLI/daemon skew (firecrawl §b #32983); "latest" from `gh api releases/latest` (exa §b: `version.json` is a TUI-only cache, stale since Sep 9). `daemon version` errors on a dead socket — a ready failing arm.
- Image: `install.sh --release <recorded>` exists and clears the auto-update marker (exa §b; codex-desktop §1D) — good for a pinned image. Adjacent: pin the script download + checksum (#34692, `no-stderr-suppression`).
- Desktop: **BLOCKED pending probe C2** — exa §b: #41112/#41014 OPEN, Desktop 26.820.60940 ignores the var because the bundled `codex_app` MCP override makes `configOverrides` non-empty. Single-source (exa) but mechanism-consistent with codex-desktop §1E ("any `-c` override disables daemon reuse"). Host is 26.915 with 26.917 published today (codex-desktop §H), newer than the reporting build.

**c. pwf — KEEP interim; provisional design NEEDS-RULING (see D5).** Bump 3.17.2→3.20.5 is clean: Codex side already 3.20.5 and the cache copies are byte-identical (pwf-claude-codex §1a), so no Codex hook re-trust for this step. Archive to `.planning/.archive/` (hidden dirs are skipped by the resolver; KB precedent — pwf-setup §1). Pin: `PWF_PLAN_ROOT` must be a **descendant of cwd on the Codex route** (P3, pwf-claude-codex §1b) — repo root satisfies both routes as long as lanes keep `-C repo_root`. Two things the interim should add: `sdlc_team.py` sets no `PLANNING_DISABLED` and lanes are already receiving `PLAN TAMPERED` (pwf-claude-codex P2) — decide now; and the hook fires at 11-12s against a 10s timeout (pwf-setup §2) — the bump does not fix that, shrinking the 90KB plan does.

**d. Wrappers — mechanism KEEP (Ray measured it), intent NEEDS-RULING (D3).** Native routes are closed by docs verbatim, three lanes agree (firecrawl §d `$CC/settings-reference.md:4066-4090`; exa §d #22345; context7 §d). Upstream still open (claude-code#92769/#78523, mattpocock#1055). Three lanes independently flag that wrapping inverts documented author intent (exa §d `$CC/changelog.md:1565`; context7 §d `implement.md`; last30days §d), as does repo memory `feedback_protocol_verbs_are_user_invoked_only`. The 2.1.280 "clickable skill state" lead is unverifiable offline — the KB corpus is at 2.1.278 and has no 2.1.280 entry (my grep of `$CC/changelog.md`) — and a UI for existing states cannot add an un-hide state the docs deny.

**e. Latest everything — KEEP; two CHANGEs.** hk 2.0: `hk fix` no longer stages (exa §e, **single-source** via a newreleases.io mirror; context7's index stops at 1.56.1, last30days nothing) → `mise run fmt` and the "git add BEFORE fmt" doctrine change in the same PR; the `amends`/`import` URLs are pinned at 1.57.0 in all three pkl files (`hk.pkl:1,8,11`, `hk-image.pkl:11,14`, `hk-common.pkl:17-18`) and `pin_parity` holds them in lockstep. hk still has no timeout (exa and context7 agree) — `lint.py` stays. `group:all` blocks the codex bump inside #1063 today (codex-desktop §2) and jdxcode warns one failing bump blocks the group (last30days §e) — add `packageRules` splitting majors out; Ray's "hk 2.0 separate PR" already implies this.

# B. Native features vs planned custom code

| Feature | Verdict | Why |
|---|---|---|
| `aqua:openai/codex` | **Reject** | No daemon layout (exa §b; mise.toml:119-122) |
| `daemon version` JSON `LifecycleOutput` | **Adopt** | Replaces all version scraping; `codex doctor` lies on skew (context7 §b; firecrawl §b) |
| `install.sh --release` / `CODEX_RELEASE` | **Adopt** (image only) | Clears marker = pinned, no updater (codex-desktop §1D) |
| `settings.json updater.autoUpdateEnabled` + `daemon update` on demand | **Adopt** — see D1 | Gives "latest at a checkpoint" without #40969 (context7 §b; codex-daemon-history §2) |
| Codex permission profile `task_plan.md = "read"` | **Probe-first (C3)** | Beats PreToolUse deny (#27833 `perl -pi` bypass), no trust step; but incompatible with `-s` and full-access (exa §c) |
| pwf ledger `ledger-append.sh` | **Adopt** | Upstream's own worker channel (pwf-claude-codex §1c) |
| `mise upgrade --bump` | **Adopt, scoped per tool, operator-only** | Bare form is destructive (memory); Renovate stays the automatic path |
| `mise upgrade --dry-run-code` | **Probe-first** | Could replace `dependency-currency`'s mise half; unknown whether it reads lock or config (context7 §e) |
| `uv upgrade` (hidden) | **Probe-first** | Unverified whether it writes at 0.12.17 (exa §e) |
| `vercel-labs/skills` CLI | **Reject** as wrapper replacement | `skills update` restores DMI (exa §d); breaks single-source |
| graphify `clone`/`global`/`merge-graphs` | **Reject** for KB | `clone` cannot carry SHA pins (context7 §a, unverified but no counter-evidence); KB already uses `merge-graphs` internally (graphify-features §1b) |
| 2.1.280 `/plugin` clickable state | **Probe-first (C6)**, low expectation | Docs deny plugin overrides; corpus cannot confirm |
| Alexandria providers | **Reject** | All metadata/paid duplicates of free `gh`/PyPI (firecrawl §Bottom line) |

# C. Contradictions to settle by probe

1. **Same-name `SKILL.md` collision at 0.9.65** (exa §a vs graphify-features G4 silence). Extract the pwf clone at 0.9.65; count nodes per `SKILL.md` path — expect 18 distinct `source_file`s. Control: a uniquely named file yields nodes; fail arm: pre-0.9.65 pin drops colliders.
2. **Desktop `CODEX_APP_SERVER_USE_LOCAL_DAEMON=1`** (exa #41112 vs codex-desktop §H static asar read). With a running daemon ≥0.141, export the var, fully restart the app, `lsof -U` for a connection on `app-server-control.sock` and **no** stdio `app-server` child (today's pid-51467 shape). Control: unset → stdio child; negative: `CODEX_APP_SERVER_FORCE_CLI=1` → stdio child.
3. **Per-file `read` profile on macOS Seatbelt** (exa §c unverified vs fable §1 hook deny). `codex exec` under the profile prompted to append to `task_plan.md` via `apply_patch` **and** via `printf >>`: expect both refused, file unchanged. Control: `scratch.md` changes; negative: default profile → `task_plan.md` changes.
4. **Codex hook deny under `exec` with persisted trust** (KB `093aab0c` "bypass required" vs P1 trust honoured at 0.154; fable §5 arms already specified). Add one arm: edit `hook_guard.py` bytes only, re-run — determines whether trust hashes the hooks.json entry or the script (fable §4 "material").
5. **`daemon update` with `autoUpdateEnabled=false`** (context7 §b "selects latest even with auto disabled" vs codex-daemon-history D `unsupported`). Both are consistent if the marker is present. After a fresh latest-channel install: expect `updated`/`noUpdate`; control: a `--release` install → `unsupported`.
6. **`mods/` exists at `anthropics/claude-code` v2.1.280** — `gh api repos/anthropics/claude-code/contents/mods?ref=v2.1.280`; control: `CHANGELOG.md`.

# D. Rulings to revisit

1. **Daemon updater ON hourly, or OFF with doctor-driven `daemon update` at SessionStart?** Recommend **OFF + doctor-driven**. #40969 OPEN: hourly updater SIGKILLs in-flight turns after a 60s drain, max 300s (exa §b; codex-desktop §1B) — an xhigh lane dies mid-turn. OFF + on-demand keeps "always latest", honours the 09-16 "safe checkpoint" ruling, and dissolves the still-open conflict with the user-global exact-pin track in `macos-development-environment` (agentsview D4/D7; codex-daemon-history TL;DR) — one daemon per `CODEX_HOME` cannot follow two authorities. That track must be re-ruled to match, or retired.
2. **Desktop: keep the env var plan?** Recommend: probe C2 on 26.917 first; if broken, `CODEX_CLI_PATH=$HOME/.codex/packages/standalone/current/bin/codex` (codex-desktop §3.5b) — same outcome (stable binary), no daemon dependency, no custom code.
3. **Wrappers: who invokes `/to-spec`/`/to-tickets`/`/implement`?** Ruling 6 lists them as slash commands Ray runs. If Ray invokes, wrappers are dead code that contradicts author intent and repo memory. Recommend: build wrappers **only** for verbs that must run inside an unattended chain (`/gated-implementation`), with the intent override justified in the wrapper body (use-tool-builtins rule 3).
4. **Codex-side plan enforcement: PreToolUse deny or permission profile?** Recommend the profile if C3 passes: a project hook deny is silently absent under `exec` until the user config trusts its hash and every edit re-requires TUI trust (#46210, P0 arm A; context7 §b) — the fail-open shape this repo bans. Hooks stay for injection (SubagentStart contract). Never `--dangerously-bypass-hook-trust` in launchers.
5. **"Codex is sometimes coordinator."** Recommend defining coordinator as *the process behind the operator's attestation*, and ruling that `sdlc_team`/`codex_lane` lanes never are. Otherwise the Claude `agent_id`-absent discriminator (fable §1) and the profile's read-only carve-out cannot be applied per-launcher.
6. **Removing codex from mise breaks mise-first.** Recommend writing the exception exactly as `doctor.toml:265` does for Claude (`expected_install_method = "native"`), same PR. KB main and three worktrees also pin 0.154.0 (agentsview S1 @9199) — "both repos" includes those.

# E. Revised order

**Unblocked now:** (1) dotfiles pwf interim PR — plugin bump, archive, root pin, `PLANNING_DISABLED` decision for `sdlc_team`, gitignore cleanup; operator: `claude plugin update … -s project`, restart. (2) KB deps-latest PR (no hk). (3) KB graphify unfork PR — `==0.9.65`, delete fork block, drop `openai-cli` from `backend_probes`/`CLAUDE.md`/native-extract. (4) KB hk 2.0 PR; (5) dotfiles hk 2.0 PR — both with the fmt-doctrine change. (6) dotfiles deps-latest + `packageRules` splitting majors. (7) Operator: remove mise codex pin locally, run latest-channel `install.sh` with `CODEX_NON_INTERACTIVE=1`, `daemon start`; then dotfiles codex-A PR — drop `npm:@openai/codex` (shared.toml + mise-runtime), rewire `codex_schema.py:28`/`schema_vendor.py:119` to PATH + `sources.toml`, `ai-cli-invocation.md`, `doctor.toml [codex] native`, Dockerfile `--release <recorded>`; mirror in KB.

**After D1:** (8) codex-doctor PR (SessionStart report + PreToolUse deny fn hook, cloned from claude-doctor; `daemon version` JSON; optional `daemon update`). **After C6/C1:** (9) KB manifests PR (claude-code kind=code v2.1.280, mattpocock main, pwf + `source_only` row) then (10) `kb-update` resync. **After D3:** (11) wrapper-skills PR. **After C2:** Desktop (user-global, no PR). **After pwf KB extraction + C3/C4:** (12) A-enforced design PR.
