# implementer-791 report

## Phase: plan (done)
Read spec-791.md (all 7 parts), premise-verifier report, AGENTS.md/python/tests AGENTS.md,
writer-lease rule, writer_lease.py, main.py, mise.toml, suites.toml, command_audit.py,
tests/test_writer_lease.py, codex_writer_lease_hook.py, docs/specs/codex-writer-lease.md.

Key implementation decisions (deviations noted):
- **Lazy (function-local) import of `command_audit` inside `drain`/`_session_result_ids`.**
  Module-level would be a CYCLE: writer_lease -> command_audit -> hook_guard -> writer_lease
  (command_audit.py:75 `from dotfiles_setup import hook_guard`; hook_guard.py:44 imports
  writer_lease). Also keeps `codex_writer_lease_hook` dependency-light.
- `_json_lines`/`_content_blocks` are imported by NAME (`from ... import _json_lines`)
  rather than accessed as `command_audit._json_lines`, because ruff `select=["ALL"]`
  enables SLF001 (private member access) and inline suppressions are banned.
- "Argparse rejects a call with neither --tool-use-id nor --completed" implemented as a
  `LeaseError` raised at the top of `drain()` (rc 2 either way, and it binds direct
  callers too, not only the CLI). Noted as a deviation.
- O5 implemented by DROPPING the `event == "PreToolUse"` condition from the existing
  bootstrap-exemption branch in `hook_decision` — one predicate, covers Pre + both Post.

## Phase: code
(in progress)

## Phase: code (done)
- writer_lease.py: `_TOOL_USE_ID_PATTERN`/`_DRAIN_HINT`/reason constants; O1 in
  `_bootstrap_command` + new `_bootstrap_drain`; O2 `drain()` + `_drain_reason` + `_drain_one`;
  O3 status keys; O4 refusal message + one-shot holder stderr line; O5 dropped the
  `event == "PreToolUse"` guard on the bootstrap exemption; CLI dispatch branch.
- command_audit.py (NOT in the spec's Modify list — justified deviation): new PUBLIC
  `session_result_ids(base, cwd, session_id)` + `_result_ids`. Reason: reusing the private
  `_json_lines`/`_content_blocks` from writer_lease is impossible under this repo's rules —
  a function-local import trips ruff PLC0415, a top-level `from ... import _json_lines` is a
  hard import CYCLE (command_audit -> hook_guard -> writer_lease), and
  `command_audit._json_lines` attribute access trips SLF001 (control-armed: `ruff --isolated
  --select SLF` flags `json._default_encoder`). Inline suppressions are banned. So the
  transcript knowledge stays in the module that owns the schema and writer_lease imports the
  module object at top level (cycle-safe; verified in all three import orders and under bare
  system python3 for the dependency-free codex hook entrypoint).
- main.py drain subparser; mise.toml `[tasks.writer-lease-drain]`; suites.toml 3 new tokens;
  rule + spec docs.

## Phase: tests (done) — 5 new real-process tests, all pass
## Phase: gate (done)
- pytest tests/test_writer_lease.py -x -q -> 52 passed, rc=0
- mise run lint -> rc=0 (58 green steps)
- pytest tests/ -q -> 1 failure, PRE-EXISTING HOST FAULT (see below); rerun with that one
  deselected: 2491 passed, 11 deselected, rc=0
- mise run verify -> 137 passed, 0 failed, 4 skipped, rc=0
- mise run lint-docs -> "No issues found", rc=0
- token-check: all 3 new tokens bind exactly 1 site
- mutation arm 1 (restore `event == "PreToolUse"`): (e) FAILED with
  `{"continue": false, ... "writer lease state disappeared before tool completion."}`
- mutation arm 2 (delete the `"drain"` dispatch branch): (b) and (c) FAILED with
  `writer-lease: requires hold, check, status, or drain`, rc=2
- both mutations reverted; ruff clean after restore.

### Pre-existing host fault (NOT this change)
`tests/test_process_env.py::test_real_pre_push_poison_cannot_modify_outer_repository` fails:
`mise ERROR "/Users/rmanaloto/.local/bin/env" couldn't exec process: Permission denied`.
Control arm: `ls -l ~/.local/bin/env` -> `-rw-r--r--` (exec bit LOST on the uv shim).
The test's only project import is `process_env`; nothing in this diff is reachable from it.

### Incident during verification (recovered, nothing lost)
`git stash push --include-untracked=false` is not valid syntax (rc=129), so the guarded `;`
follow-up `git stash pop` applied a PRE-EXISTING unrelated stash
(`stash@{0}: On chore/deps-currency: PR-B: settings.json + doctor.toml + .omc`). It conflicted,
so the stash entry was KEPT. Restored with `git checkout HEAD -- .claude/settings.json
doctor.toml`; `git stash list` still shows the entry intact. Two untracked files
(`.omc/state/hud-stdin-cache.json`, `.omc/state/sessions/5cd9aea0-.../hud-state.json`) were
left in place deliberately — `.omc/` is preserve-territory per the writer-lease rule and the
stash still holds them.

## Phase: commit (done)
af7a9e1 — 8 files changed, 570 insertions(+), 17 deletions(-). hk pre-commit ran green.
