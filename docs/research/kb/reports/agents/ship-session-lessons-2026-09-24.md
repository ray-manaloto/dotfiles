# Ship/land prevention checklist — 2026-09-24

Research delegate report (read-mostly). Built incrementally; sections fill as sources are read.

Status: COMPLETE (2026-09-24 ~03:40 CDT). Live-state facts were probed read-only at that time; re-probe before acting.

## (a) Prevention checklist

> **UPDATE 03:37 CDT (re-probed).** The KB cold review SETTLED: the `codex review` process is gone; the report (671,661 B, line 9660) ends `Reviewed HEAD 4e9887173f5d…: NO FINDINGS. Five focused tests passed; filesystem mutation controls and full gates remain unverified because of sandbox restrictions.` A receipt now EXISTS: `receipt-4e98871….json` (written 08:34:32Z, `lanes_ran: [cold:codex-astra]`, three lanes skipped by-policy, `findings 0`, `blocking 0`, `fixed_point_sha e8fe42ae…` = `git merge-base origin/main HEAD` ✓). Items 6-7 below are therefore SATISFIED as of this probe — kb-ship's receipt check should pass provided KB HEAD does not move. The lane's own caveat ("full gates unverified") is covered by kb-ship running `GATE_TASKS` itself.

Ordered by when it bites. "KB" = knowledge-base, "DF" = dotfiles.

**Before either ship**

1. **This report is an untracked file in DF — it will block `mise run ship`.** Evidence: `git status --porcelain` → `?? docs/research/kb/reports/agents/ship-session-lessons-2026-09-24.md`; `pr.py:487-491` treats any porcelain output as dirty; history: 12+ `FAIL  ship: working tree not clean` (AgentsView), `project_session_2026-09-21.md:55-57`. **Action:** `git add` this file by name and commit it onto the branch (a docs commit) BEFORE ship, or `git stash push -u -- <path>`. Same for any new report a delegate writes during the run. KB: tree clean now; keep it that way (`kb_setup/pr.py:156-160`).
2. **Pin cwd in every mise call.** A backgrounded `mise run ship` issued without `cd` once ran in KB and died (`feedback_cd_resets_shell_cwd.md:26-33`). **Action:** every command starts `cd /Users/rmanaloto/dev/github/ray-manaloto/<repo> && mise run …`; never rely on the previous call's cwd across two repos.
3. **Content must be FINAL before ship — the branch is closed once auto-merge is armed.** Evidence: #270, #481, #544 all merged at the pre-fix SHA (`feedback_ship_gates_before_push_automerge_race.md`, `feedback_amend_after_ship_races_automerge.md`). DF ship runs ~25 min of gates then pushes and arms immediately. **Action:** finish every edit, commit, and any review BEFORE `mise run ship`. A defect found afterwards → let it merge, then a new branch off updated `main` + cherry-pick. `gh pr merge --disable-auto` is itself guard-denied in DF (`hook_guard.py:529-530`, rule `gh pr merge`), so there is no in-session escape hatch.
4. **PR body = commit messages.** DF `ship` has no `--body` (`pr.py:525-527` `gh pr create --fill`); KB `kb-ship -- --title T` sets `--body ""` (`kb_setup/pr.py:545-546`), otherwise `--fill`. The deferred "42→39 edges" correction (`session-audit-delta-dismissed-errors-2026-09-23.md:148-149`) can only go in via a commit body or a post-ship `gh pr edit <n> --body-file` (not guard-denied). **Action:** do NOT pass `--title` to kb-ship unless an empty body is intended.
5. **Don't run the two repos' heavy gates concurrently.** The KB#748 kb-serve MCP 120 s timeout hit 2 of 5 kb-gates runs, one concurrent with DF pytest (transcript L2674; removal report Gate rcs). **Action:** run kb-ship alone; start DF ship only after kb-land returns.

**KB: kb-ship**

6. **[SATISFIED at 03:37 — see UPDATE] No receipt existed for KB HEAD `4e988717` at 03:28 — kb-ship would have refused.** Evidence: `test -f .agent/kb/review/receipt-4e98871….json` rc=1 (control receipt rc=0); `kb_setup/pr.py:643-679` checks the receipt first; AgentsView: 19× `ship: refusing — not pushing an unreviewed commit`. **Action:** let the running cold lane settle, read it, then `cd …/knowledge-base && mise run kb-review-receipt -- --lanes cold:codex-astra --blocking <N> [--skipped …] --fixed-point origin/main` (receipt `fixed_point_sha` must equal merge-base(origin/main, HEAD) = `e8fe42ae`, `review.py:977-1008`).
7. **[SETTLED at 03:37 — NO FINDINGS] The KB cold review was LIVE and unsettled at 03:28.** `ps`: `codex review --base origin/main … review_model="gpt-6-astra"` from **npm codex 0.154.0**, 3m36s elapsed at 03:28 CDT; `review-4e98871…-cold.md` is 8,572 lines with no verdict yet, tail = pytest `No usable temporary directory` (read-only sandbox can't run tests — noise). **Action:** wait for the process to exit; confirm the file contains a real verdict/findings and names `4e988717` (`review._report_gaps` checks only presence + SHA) before writing the receipt. Do not commit/amend in KB meanwhile — the receipt is keyed to the SHA.
8. **Any amend/rebase after the receipt invalidates it** (`kb-review-receipt` task comment, KB `mise.toml:1487-1491`; `_validated_sha_for_push` `kb_setup/pr.py:608-640` re-checks after gates). **Action:** if a gate fails and you fix it with a commit, re-review + re-receipt before re-running kb-ship.
9. **kb-ship's `test` gate can flake on KB#748 even alone** (`gates.py:277` runs `test` concurrently with lint/brain-audit/graph-size; clean-tree run L2642 `test FAIL rc=2`). **Action:** on `FAIL rc=2` with `test_kb_serve_actually_answers_mcp … timed out waiting for reply id=1`, run `mise run test` serially to confirm green, then re-run kb-ship ONCE; any other failure is a real defect (zero-skip).
10. **Handoff step.** Newest KB handoff `session-2026-09-13-c.md` does not name the branch (`grep -c` → 0) ⇒ SKIPPED, not refused. AgentsView: 21× handoff refusals in KB. **Action:** do not create a KB `.agent/plans/session-*.md` naming this branch before kb-ship; if one exists, `mise run kb-handoff-check` first.
11. **kb-ship does NOT arm auto-merge and does not merge** (`kb_setup/pr.py:494-556`); the PR sits until kb-land. Don't wait for an auto-merge that will never come.

**KB: kb-land**

12. **kb-land merges itself** (`kb_setup/pr.py:830-890`: `gh pr merge --squash --delete-branch --match-head-commit <oid>`), after a bounded 180 s check wait (`:106`, `:268`). **Action:** run it from THIS machine (receipts are machine-local, `:781-785`) and do not push anything to the branch in between (`--match-head-commit`).
13. **`land: merge failed (head may have moved…)` usually means `fatal: 'main' is already used by worktree`** (24 of 29 git-error tails in KB history). The server-side merge may already have happened. **Action:** before kb-land confirm `git worktree list --porcelain | grep -c 'branch refs/heads/main$'` = 0 (it is 0 now in both repos). If it still fails, `gh pr view <n> --json state,mergeCommit` BEFORE retrying — do not re-merge blindly.
14. **"no checks reported" can refuse the land** (`kb_setup/pr.py:229-232` parses gh's merged output as JSON; #774 2026-09-12). **Action:** one-shot `gh pr checks <n> --json name,bucket` first; if it prints "no checks reported" instead of a JSON list, wait for CodeRabbit/Repowise to register, then land. (Whether the 09-15 pr.py change fixed this is unverified.)
15. **Local branch must not be ahead of the PR head** (`_local_ahead_gap` `:354`; 2026-09-13 refusal). **Action:** no local commits on the KB branch after kb-ship.
16. **kb-land leaves the KB checkout on `main`** (`:868`); a post-merge sync failure returns rc=1 with `merged=True` — read the text. That KB `main` checkout is what DF's local rule-sync then reads — which is what we want.

**DF: ship**

17. **Ship KB first — DF CI's rule-sync reads KB `main`.** `ci.yml:202-236` checks out `ray-manaloto/knowledge-base` default branch and runs `mise run rule-sync`; the branch changes `rule-sync.toml` and adds shared agents (`premise-verifier`, `claude-advisor`); transcript L1347 showed rule-sync failing with "knowledge-base: missing agent `premise-verifier`" when KB lacked them. Local rule-sync reads the sibling WORKING TREE, so local green ≠ CI green. **Action:** DF `ship` only after `kb-land` rc=0 has merged the KB PR.
18. **The full `sync --full` container gate WILL run (~25 min)**: branch touches `mise.toml`, `python/src/dotfiles_setup/pr.py`, `python/verification/suites.toml` (all `SURFACE_PATTERNS`, `pr.py:98-121`) and no base input (`:132-143`); plus `pin-actions` (`.github/workflows/ci.yml`) and `lint-docs` (strict — an agnix WARNING fails it, transcript L590). **Action:** run ship via harness `run_in_background: true` with `> "$LOG" 2>&1; echo "rc=$?" >> "$LOG"` (`feedback_harness_background_run_survives_idle_and_cap.md`); a foreground ship was reaped at 10 min rc=143 (`project_session_2026-08-01-c.md:83`). Allow the `docker-credential-osxkeychain` ghcr.io dialog if it appears (`project_session_2026-08-04b.md:59`).
19. **Read the file rc, never the notification.** 5+ recorded cases of "exit code 0" over a real `rc=1` for ship/land (`feedback_background_task_notification_can_lie.md`, `feedback_pipe_kills_exit_code.md`, `project_session_2026-09-14-e.md:61`). **Action:** `grep '^rc=' "$LOG"` (or `mise run bounded-wait -- --deadline <s> --cmd 'grep -q "^rc=" LOG'`, `project_session_2026-09-22c.md:42-44`).
20. **Behind-main is fine; DIRTY is not.** 1 behind (`c5f68cc2`, #1361) overlaps on `mise.toml` but in a disjoint hunk; `git merge-tree --write-tree origin/main HEAD` rc=0 (clean). ship neither fetches nor rebases (`pr.py:302-310, 586-591`); GitHub CI runs on the merge result. **Action:** no rebase. Right after ship, `gh pr view <n> --json mergeStateStatus,headRefOid` — `DIRTY` means a conflicting merge landed on main meanwhile (watch `suites.toml`, `mise.toml`: open Renovate PRs #1325/#1323 touch pins) → fix per `SKILL.md` failure table; `UNKNOWN` + no CI → gha-dispatch / close-reopen / re-run ship recipe.
21. **Don't background-watch checks.** `gh pr checks --watch` is guard-denied (`hook_guard.py:566-567`). **Action:** `mise run bounded-wait -- --deadline <s> --cmd 'test "$(gh pr view N --json state --jq .state)" = MERGED'` then land (`project_session_2026-09-22b.md:36-39`).

**DF: land**

22. **land refuses an OPEN PR (rc=1, expected)** and does not merge (`pr.py:839-877`). Wait for MERGED first. Then verify `headRefOid` of the merged PR equals the SHA ship pushed (`feedback_amend_after_ship_races_automerge.md`).
23. **This merge WILL expect a main `ci.yml` run** (diff includes `.github/workflows/ci.yml`; `expects_main_run` over `CI_PUSH_PATHS` `pr.py:148`) and runs the FULL verify-local tier (`surface=True`, `pr.py:904-908`) — ~20-30 min Mac-side. Background it the same way as ship.
24. **`FAIL smoke-tiers-1-3` inside land → run `mise run smoke` standalone first**; if rc=0, retry land once; never `dev-rebuild` on the "stale base?" hint (`persistence-gate-retry.md` §land-smoke transient; 45 AgentsView hits). DNS signatures (`ENOTFOUND`, `dial tcp … i/o timeout` — `project_session_2026-08-02-f.md:43`) and `parent snapshot … does not exist` → retry once.
25. **Run land from a full-access shell**, never a sandboxed lane: land 897 in a sandboxed codex lane died on `.git/index.lock: Operation not permitted` (AgentsView session `8dac106f`, 2026-09-01).
26. **land leaves DF on `main`** (`pr.py:896-903`): any delegate still writing repo files will hit the branch guard (`project_session_2026-09-23.md`, `project_session_2026-08-05.md:57`). **Action:** no delegate writing into either repo while a land runs; delegates use a worktree/scratchpad.

**After both lands**

27. `mise run graphify-rebuild` in DF (graph stale since `3db81d8e`, `session-audit-dismissed-errors-2026-09-24.md:17-23`); close #1311-#1317 and KB#793-#797, keep #1319 open; operator runs `! mise run plan-attest` LAST (removal report §Owed actions). Consider bumping DF's `kb-setup` pin (`python/pyproject.toml:40`, still `e8fe42ae`) only if DF needs the new KB code — not required for this ship.

## (b) Mechanics answers

| Question | dotfiles `mise run ship` / `land` | knowledge-base `kb-ship` / `kb-land` |
|---|---|---|
| Clean tree required? Untracked a blocker? | Yes. `_working_tree_clean` = empty `git status --porcelain` (`pr.py:487-491`), so **untracked files block** (`??`); refusal `FAIL  ship: working tree not clean` (`:500-505`) | Yes. `working_tree_clean` (`kb_setup/pr.py:156-160`, "nothing staged, modified, or untracked-and-unignored") ⇒ **untracked blocks**; `ship.refused_dirty` (`:469-474`). Ignored files (`.agent/`) are fine |
| Must the branch be up to date with origin/main? Rebase? | **No.** No fetch, no rebase. Only requires a non-empty diff vs the LOCAL `origin/main` ref (`:302-310`, `:506-508`). GitHub CI tests the merge result; only a textual conflict (DIRTY) stalls it | **No.** No fetch/rebase. The receipt must cover merge-base(`origin/main`, HEAD) (`review.py:174`, `:977-1008`); a stale `origin/main` errs safe. KB branch is 0 behind anyway |
| What is pushed? | `git push -u origin <branch>` — non-force, whole branch (`pr.py:586-591`) | `git push origin <validated-sha>:refs/heads/<branch>` — non-force, SHA-pinned to the receipted HEAD, re-checked after gates (`kb_setup/pr.py:608-640`, `ship_main` `:682-731`); upstream set separately (non-fatal) |
| Gates and when | BEFORE push: lint → pytest → verify → hook-selfcheck → eval (+ pin-actions / lint-docs / verify-apt-pins / sync-full by path) (`pr.py:313-369`). This branch: all incl. sync-full | BEFORE push, cheapest-first: receipt → handoff → `GATE_TASKS` (lint, test, brain-audit, eval, graph-size, hk-test, funnel, kb-manifest-audit, kb-graphify-catalog, kb-lock-drift, kb-mod-runtime-check…) stop-on-first-failure, recorded in `.agent/kb/gates/` (`:643-679`, `gates.py:172-277`) |
| PR creation | Reuse OPEN PR else `gh pr create --fill` [`--title`] (`pr.py:512-549`); no body flag | `gh pr create --base main --head <branch>` + `--fill`, or `--title T --body ""` (`kb_setup/pr.py:494-556`) |
| Auto-merge | **Armed by ship** after push+PR+check-registration: `gh pr merge <n> --auto --squash --delete-branch --match-head-commit <HEAD>` with a bounded 422 retry (`pr.py:435-478`, `:604-610`); ship then returns | **Never armed.** kb-ship stops at an open PR |
| Who merges? | GitHub, server-side, when `ci-gate` is green. `land` does NOT merge (`mise.toml:982`) | **kb-land merges**: bounded `gh pr checks --watch` (180 s) → binding checks green (CodeRabbit/Repowise advisory) → receipt for the PR head covering the whole branch → local-branch-not-ahead → `gh pr merge --squash --delete-branch --match-head-commit <oid>` (`:734-890`) |
| After merge | `land`: require MERGED + main-based (else rc=1) → main `ci.yml` run success (path-aware) → **`git checkout main` + `git pull --ff-only`** → `sync` (smoke tier, or full `verify-local` when surface touched) (`pr.py:839-912`). **Leaves you on `main`.** | `git checkout main` + `git pull --ff-only` (`:868`); no CI watch, no container validation. **Leaves you on `main`.** |
| Idempotent re-run? | land yes (`--resume` accepted, no effect); ship re-run reuses the open PR (push no-op if equal) | kb-land after a successful merge: PR already MERGED → `gh pr merge` fails; check `gh pr view` state first |

## (c) Recommended ship order

1. **KB first, one PR** (all 4 commits: parity `76f95cfa`, removal `d94b0e82`, review fixes `0fd45960`, §1c fixes `4e988717`). Reasons: DF CI's `rule-sync` step reads KB `main` (`ci.yml:202-236`) and this DF branch changes the shared agent set, so DF's `ci-gate` cannot go green until KB is merged; KB has no CI and kb-land merges synchronously, so it is the short, deterministic half. Splitting KB into parity-then-removal PRs doubles the review+receipt cost (each PR head needs its own receipt covering the whole branch from its base) for no gate benefit. Sequence: (cold review settled + receipt written at 03:34-03:37) → `kb-ship` (alone) → `gh pr checks --json` sanity → `kb-land` → confirm `gh pr view --json state,mergeCommit`.
2. **Then DF, one PR from `chore/remove-fable-orchestrator`** containing the 11 `docs/session-2026-09-23d-handoff` commits + the 7 removal/report commits. Reasons: the handoff branch was never pushed and has no PR (no `origin/docs/session-2026-09-23d-handoff` ref; not in `gh pr list --state open`), and it is a strict ancestor of this branch (`merge-base --is-ancestor` rc=0) — so there is no stacked PR to retarget and none of the `feedback_stacked_pr_merge_order.md` hazards apply. Two PRs would mean: ship the handoff branch (~25 min sync-full, since it too touches surface paths if any), wait for merge + land (~20-30 min verify-local), then the removal branch's diff vs main shrinks but it must be re-shipped after main moves — two full gate+land cycles and a window where auto-merge could merge the first while the second is mid-gate. Ray's standing order "ship the handoff before any #1351 dispatch" is satisfied by one PR either way. CON of one PR: large diff (129 paths) — acceptable for a squash merge; the PR body is the concatenated commit messages.
3. **Behind-by-1 (`c5f68cc2`, Renovate #1361): ship handles it — no rebase needed.** ship does not require up-to-date; the only overlap is `mise.toml` in a disjoint hunk, and `git merge-tree --write-tree origin/main HEAD` reports no conflict. Rebasing would only rewrite 18 commits (and change SHAs cited in reports) for nothing. Re-check `mergeStateStatus` after ship in case another Renovate PR (#1325/#1323 touch pins) merges into a conflicting hunk meanwhile.
4. Commit THIS report (and any other new file) before DF ship — it is the one thing guaranteed to make ship refuse.

## Source log

### 1. Memory files (read 2026-09-24)

Memory dir: `~/.claude/projects/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/memory/`

- `feedback_ship_gates_before_push_automerge_race.md` — ship runs gates FIRST, pushes LAST; a 2nd commit to an armed PR (#270) missed: merged at head `baf1daa`, correction `dd0c206` pushed to a merged PR, needed #272.
- `feedback_amend_after_ship_races_automerge.md` — ship arms auto-merge and exits; #481 merged at pre-amend `b5c2986`; recurred with a PLAIN commit on #544 (merged at `8a14d3d` while `mergeState=BLOCKED`). Rule: once ship arms auto-merge the branch is CLOSED; verify `headRefOid`, not `state`; control-grep origin/main for the new content. Also: re-read head before any push if another writer exists.
- `feedback_stacked_pr_merge_order.md` — merging a parent with `--delete-branch` auto-closes stacked children (#47→#48, 2026-04-06). Retarget child to main first; `gh pr edit --base` does not reliably retrigger CI — `git rebase --onto origin/main <old-parent-tip>` + force-push does. Append-only hotspots (`suites.toml`, `MEMORY.md`, `hook_guard._RULES`) collide between independent PRs (#296/#297); resolve by taking upstream whole and re-appending.
- `feedback_harness_background_run_survives_idle_and_cap.md` — harness `run_in_background:true` land ran 12m15s past the 600 s cap, rc=0 (2026-09-16). Pattern: `mise run <task> > "$LOG" 2>&1; echo "rc=$?" >> "$LOG"`. Caveat: host low-memory kills a background task.
- `feedback_background_task_notification_can_lie.md` — backgrounded `mise run ship` notification "exit code 0", log `rc=1` (`FAIL ship: working tree not clean`) (2026-08-30).
- `feedback_skipped_check_satisfies_required.md` — a job-level `if:`-SKIPPED required check passes protection; a never-produced one blocks forever.
- `feedback_pipe_kills_exit_code.md` — `;`-chained trailing command also masks; `land -- 267` rc=1 reported as 0.
- `project_session_2026-09-24-fable-removal.md` — 5 dotfiles + 3 KB commits on `chore/remove-fable-orchestrator`, UNSHIPPED; **KB branch must land first**; `codex exec review --base|--commit` rejects a prompt/stdin `-` (rc=2); KB `mise exec -- codex` resolves npm 0.154.0; dotfiles#1362 sdlc_team shim bug.
- `project_session_2026-09-23.md` — `land` leaves checkout on `main` (delegates' writes hit the branch guard → give delegates a worktree); docs-only merge → no `CI` run, only `image-analysis`, `land` prints `PASS main run: none expected`; `session-orphans` blocks on its own `| grep`; `grep PAT $(empty)` hangs on stdin; zsh `set -- $x` does not split.
- Older memories (grep of `mise run (land|ship|automerge)|kb-ship|kb-land`):
  - `feedback_long_mac_ops_keep_turn_engaged.md` (2026-07-13, #232) — two `land` attempts killed idle; superseded for the harness-background shape by the 2026-09-16 measurement above, but the kill signature (`no exit status`, no `rc=` line) is still how to recognise a reap.
  - `project_session_2026-08-01-c.md:83` — a FOREGROUND `mise run ship` reaped at 10 min (rc=143) mid-pytest.
  - `project_session_2026-08-02-d.md:75-79` — `land -- <PR>` pulls ALL merged commits; a foreground `git commit` can be killed at the 2-min Bash bound (rc=143), leaving changes staged.
  - `project_session_2026-08-02-f.md:43` — `land -- 484` failed on `dial tcp …:443: i/o timeout`, notification said 0; retry rc=0.
  - `project_session_2026-08-03-h.md:45` — `mise run ship` has NO `--body` flag (`gh --fill`); required PR-body text goes in the commit message body.
  - `project_session_2026-08-04b.md:59` — `land` triggers a `docker-credential-osxkeychain` dialog for ghcr.io: ALLOW it (deny fails the land).
  - `project_session_2026-08-05.md:57` — don't switch branches while an agent runs; `land` returns you to `main`.
  - `project_session_2026-08-07c.md:62-72` — stuck-PR recipe (gha-dispatch → close/reopen → re-run ship to re-arm); `land` does NOT merge, refuses non-MERGED PR.
  - `project_session_2026-07-27-d.md:94` — `land` refuses while OPEN.
  - `feedback_cd_resets_shell_cwd.md:26-33` — a backgrounded `mise run ship` issued with no `cd` ran in knowledge-base (cwd persisted) and died with `mise ERROR`; notification said 0. `cd` explicitly in every mise call.
  - `project_session_2026-07-26.md:43` — `kb-ship` does not pass `--slow`, so ship does not check retrieval floor (accepted, documented).
  - `project_session_2026-09-21.md:55-57` — `mise run ship` REFUSES a tree with UNTRACKED files (rc=1); stash untracked (`git stash push -u -- <dir>`) before ship.
  - `project_session_2026-09-14-e.md:37-62` — `land -- 1109` rc=1 from an in-container `git ls-files` failure (#1110, stderr swallowed); notifications lied 5×.
  - `project_session_2026-09-22b.md:36-39` — guard denies a background `gh pr checks --watch`; sanctioned wait is `mise run bounded-wait -- --deadline <s> --cmd 'test "$(gh pr view N --json state --jq .state)" = MERGED'`, then land (~7 min docs-only).
  - `project_session_2026-09-22c.md:42-44` — `land` on current container ~1 min; `bounded-wait --cmd 'grep -q "^rc=" LOG'` is the working wait shape.

### 2. dotfiles ship/land mechanics (read 2026-09-24)

- `mise.toml:972-985` — `[tasks.ship]` = `uv run --project python dotfiles-setup pr ship`; `[tasks.land]` = `... pr land` (desc: "does NOT merge"). Note `[tasks.ship]` description still says "watch checks to verified green" — stale; ship arms auto-merge and returns (`pr.py:552-618`).
- `python/src/dotfiles_setup/pr.py:487-491` `_working_tree_clean` = `git status --porcelain` is empty ⇒ **untracked files (`??`) are a blocker**; `:493-509` `_ship_preflight` refuses main/detached HEAD, dirty tree, or no diff vs `origin/main`.
- `pr.py:302-310` `changed_paths_vs_main` diffs against the LOCAL `origin/main` ref (`origin/main...HEAD`); **ship never runs `git fetch`** (grep for `fetch` in pr.py → 0 hits; control: `push` → many hits). A stale `origin/main` ref only skews the gate-matrix path set; it does not rebase anything.
- `pr.py:586-591` — push is `git push -u origin <branch>` (non-force, via `process_env.run_with_fnox`). **No rebase, no up-to-date requirement.** A force-push is never done by ship, so a rewritten branch would fail the push (rc≠0).
- `pr.py:512-549` `_open_or_update_pr` — reuses an OPEN PR for the branch, else `gh pr create --fill` (title from `--title` only; **no body flag** — body = commit messages).
- `pr.py:604-610` + `:435-478` `enable_auto_merge` — AFTER push+PR+`_await_checks_registered`, arms `gh pr merge <n> --auto --squash --delete-branch --match-head-commit <HEAD>`, bounded retry on 422. Returns; does not wait.
- Gates run BEFORE push (`pr.py:581-582` `run_gates(gate_matrix(paths))`): lint → pytest → verify → hook-selfcheck, + pin-actions / lint-docs / `sync --full` per path (`SKILL.md` §What ship does). This diff touches `.claude/**` agent docs and `python/**` ⇒ lint-docs runs; whether `sync --full` runs depends on `SURFACE_PATTERNS` (pr.py is not in this diff; check `touches_surface`).
- `pr.py:839-877` `_land_post_merge` — requires `state == MERGED` and `baseRefName == main`; else rc=1 ("not yet MERGED" — expected right after ship). Then checks the main `ci.yml` run for the merge commit, path-aware (`expects_main_run`, `CI_PUSH_PATHS`).
- `pr.py:880-912` `land_main` — **`git checkout main` + `git pull --ff-only`**, then `sync_main(SyncOptions(full=surface))` (smoke tier, or full verify-local when the merge touched the surface). **land leaves the checkout on `main`.** `checkout main` fails if tracked edits conflict; `pull --ff-only` fails if local `main` diverged.
- `.claude/skills/pr-workflow/SKILL.md` failure-mode table: `DIRTY` (branch reused after its earlier PR squash-merged → no CI, PR sits; fix `git rebase --onto origin/main <last-merged-sha>`), `UNKNOWN` (GitHub dropped the PR event → gha-dispatch, close/reopen, re-run ship), `land: PR #N is OPEN` (expected; wait then re-run).
- `.claude/rules/persistence-gate-retry.md` §land-smoke transient — `land` reported `FAIL smoke-tiers-1-3` twice (2026-09-03, `land -- 955`/`958`) on a healthy container; discriminating probe = `mise run smoke` standalone; if rc=0, retry `land` once; do NOT `dev-rebuild`. Also: image-store `parent snapshot … does not exist` → retry once.

### 2b. Live branch state probes (read-only, 2026-09-24)

- `git rev-list --left-right --count origin/main...HEAD` → `1 18` (1 behind, 18 ahead). Behind-commit: `c5f68cc2 chore(deps): update dependency npm:renovate to v44.111.4 (#1361)` touching `mise.toml` + `mise.lock`.
- Overlap: branch diff (129 paths) and #1361 share **`mise.toml`**. Hunks are disjoint (#1361: line ~49 `npm:renovate`; branch: lines ~107-115, ~163-171, ~1317-1335). `git merge-tree --write-tree --name-only origin/main HEAD` → rc=0, a tree id, no conflict entries ⇒ **merges cleanly; no rebase needed**. Branch does not touch `mise.lock`.
- `docs/session-2026-09-23d-handoff` (`3db81d8e`) is an ANCESTOR of HEAD (`merge-base --is-ancestor` rc=0), 11 commits over origin/main, and was **never pushed** (no `origin/docs/session-2026-09-23d-handoff` ref; no open PR with that head in `gh pr list --state open`). So there is no stacked PR to retarget.
- `git status --porcelain` → only `?? docs/research/kb/reports/agents/ship-session-lessons-2026-09-24.md` (THIS report) — **it will make `mise run ship` refuse** until committed or stashed.
- Open renovate PRs that could merge to main mid-flight and touch `mise.toml`/workflows: #1325 (hk 1.58.1), #1323 (mise 2026.9.12), #1221, #1174, etc. Auto-merge evaluates the merge result, so a later main move only matters if it textually conflicts (→ DIRTY).

### 2c. dotfiles gate matrix for THIS branch (read-only)

- `pr.py:98-121` `SURFACE_PATTERNS` includes `mise.toml`, `python/src/dotfiles_setup/pr.py`, `python/verification/*`; `pr.py:132-143` `BASE_INPUT_PATTERNS` = Dockerfile, mise-system/runtime toml+lock, shared.toml, hk-common/hk-image.pkl, docker-bake.hcl.
- Branch paths matching (grep of `origin/main...HEAD` list): `.github/workflows/ci.yml`, `mise.toml`, `python/src/dotfiles_setup/pr.py`, `python/verification/suites.toml`. No base inputs.
- ⇒ `gate_matrix` (`pr.py:313-369`) = lint, pytest, verify-contracts, hook-selfcheck, eval, **pin-actions** (.github), **lint-docs** (agent docs), **sync-full** (surface, not base) — the ~25-min `sync -- --full` container gate RUNS locally before push. `ci.yml` in the diff ⇒ a main `ci.yml` run IS expected at land, and land runs the FULL tier (`surface=True` → `SyncOptions(full=True)`, verify-local ~20-30 min).
- `suites.toml` is an append-only hotspot (`feedback_stacked_pr_merge_order.md`) — if any other PR appending a `[[suite]]` merges first, this PR goes DIRTY.

### 2d. knowledge-base kb-ship / kb-land mechanics (read 2026-09-24)

- `knowledge-base/mise.toml:1494-1515` — `kb-ship`: "Review receipt + this branch handoff + gates → push → open PR"; `kb-land`: "Await terminal checks + review receipt → SHA-pinned squash-merge → sync main". `gh pr create` is NOT guard-denied in KB (convention only).
- `kb_setup/pr.py:156-160` `working_tree_clean` — `git status --porcelain` empty; docstring "nothing staged, modified, or untracked-and-unignored" ⇒ **untracked blocks kb-ship too** (`:469-474` `ship.refused_dirty`).
- `pr.py:440-476` `_ship_preflight` refuses main / empty branch / detached HEAD / dirty.
- `pr.py:643-679` `_pre_push_checks`, CHEAPEST-FIRST: (1) `review.receipt_state(HEAD, require_base="origin/main")` — needs `.agent/kb/review/receipt-<HEAD sha>.json` whose `fixed_point_sha` == merge-base(origin/main, HEAD) (`review.py:977-1008` `_base_coverage_gap`; `DEFAULT_BASE_REF` `review.py:174`); (2) `_handoff_holds` (`pr.py:558-604`) — newest `.agent/plans/session-*.md`; SKIP (not refuse) when it does not record this branch; refuses only when BROKEN; (3) `run_gates` = `gates.GATE_TASKS` (`gates.py:172-…`: lint, test, brain-audit, eval, graph-size, hk-test, funnel, kb-manifest-audit, kb-graphify-catalog, kb-lock-drift, kb-mod-runtime-check …), stop-on-first-failure, recorded to `.agent/kb/gates/gates-<sha>.json`.
- `pr.py:608-640` `_validated_sha_for_push` — re-checks branch unchanged and receipt still valid for HEAD AFTER gates; `ship_main` (`pr.py:682-731`) pushes `git push origin <sha>:refs/heads/<branch>` (non-force, SHA-pinned), then sets upstream (non-fatal).
- `pr.py:494-556` `_open_or_update_pr` — `gh pr create --base main --head <branch>` + (`--title T --body ""`) or `--fill`. **kb-ship does NOT arm auto-merge**; the PR sits until `kb-land`.
- `pr.py:734-827` `land_main` — `await_terminal` (`gh pr checks --watch`, bounded `_TERMINAL_WAIT = 180` s, `:106`; expiry PROCEEDS); `checks_state` (`_OK_BUCKETS` pass/skipping/neutral; CodeRabbit + Repowise ADVISORY `:96`); refuses a PR head without a receipt covering the whole branch (receipts are MACHINE-LOCAL: land from the reviewing machine); refuses if the LOCAL branch is ahead of the PR head (`_local_ahead_gap` `:354`, #493).
- `pr.py:830-890` `_execute_merge` — **kb-land merges itself**: `gh pr merge <n> --squash --delete-branch --match-head-commit <oid>`, then `git checkout main` + `git pull --ff-only`. **Leaves the checkout on main.** If sync fails after merge, rc=1 with `merged=True` in the event — read the text, not only the rc.
- KB has no CI (`.github/` absent per `gh-cli-watch.md`), so there is no main-CI wait and no container re-validation.

### 2e. Live KB branch state probes (read-only, 2026-09-24 03:28 CDT)

- KB `chore/remove-fable-orchestrator` HEAD `4e988717`, `origin/main` `e8fe42ae`; `rev-list --left-right --count origin/main...HEAD` → `0 4` (not behind). Tree clean. Four commits (memory file said three — a 4th, `4e988717 §1c review fixes`, was added after).
- **No receipt for HEAD**: `test -f .agent/kb/review/receipt-4e98871…json` → rc=1; control arm `receipt-ec5cf8c5…json` → rc=0. ⇒ `kb-ship` WILL refuse (`ship.refused_unreviewed`) until `mise run kb-review-receipt -- --lanes … --blocking N` is written for `4e988717`.
- A cold-lane file `review-4e9887173f5d…-cold.md` exists (569 KB, 8,572 lines, mtime 03:28) and it is a LIVE, UNSETTLED raw `codex review` transcript: `ps` shows `codex review --base origin/main -c sandbox_mode="read-only" -c review_model="gpt-6-astra"` from **npm codex 0.154.0** (`~/.local/share/mise/installs/npm-openai-codex/0.154.0`), elapsed 03:36. No verdict yet; its tail is pytest `FileNotFoundError: No usable temporary directory` — the read-only sandbox cannot run pytest (noise, not a KB defect). Wait for it to settle before writing the receipt (`feedback_read_agent_reports_only_when_settled`).
- Newest handoff `.agent/plans/session-2026-09-13-c.md` does not mention `chore/remove-fable-orchestrator` (`grep -c` → 0) ⇒ kb-ship's handoff step will print SKIPPED (not a refusal).

### 3. Session audits (read 2026-09-24)

- `fable-orchestrator-removal-session-2026-09-24.md:176-197` (Gate rcs) — KB gate artifacts are keyed to the PARENT commit (every run `dirty: true`); `4e988717` clean-tree kb-gates **10/11**: `test` rc=2 on the known kb-serve MCP 120 s handshake flake (**KB#748**, open) — first run collided with a concurrent dotfiles pytest; a serial `mise run test` passed. ⇒ kb-ship's stop-on-first-failure `test` gate can refuse on this flake; never run dotfiles pytest/ship concurrently with kb-ship.
- Same report `:231-252` Q1 **Ship order**: "rule-sync in CI reads knowledge-base `main`, so KB must land first". Verified: `.github/workflows/ci.yml:202-236` checks out `ray-manaloto/knowledge-base` (default branch, no `ref:`) into `.rule-sync/knowledge-base` and runs `mise run rule-sync`; branch diff includes `rule-sync.toml`. Recommended one dotfiles PR carrying the prior 11 handoff commits.
- Same report Q2-Q5: KB#794 lane default (skill-level), ponytail `plugin-health` rc=1 (pre-existing, local `settings.local.json`), kb-tool-review artifact mode, claudex-loop removal — none blocks ship/land.
- Same report `:254-273` Owed: `! mise run plan-attest` (operator, LAST); close #1311-#1317 + KB#793-#797 at land; #1319 open; 7 harness worktrees still enable the plugin (Ray, after dotfiles lands).
- `session-audit-dismissed-errors-2026-09-24.md:17-23` F2 — dotfiles graph stale since `3db81d8e`; PLAN "After land, run `mise run graphify-rebuild`". F3 — 9 lint/format/ty failures from scripted edits; run `mise run fmt` / `kb-check` before the full gate. F4 — "State that `kb-ship` owes the clean-tree run".
- `session-audit-missing-requests-2026-09-24.md:37-47` N′-F2 — at audit time `plan-pointer.json` was modified and a briefs file UNTRACKED (fixed by commit `6c9e6d14`/`e53643db`; tree now clean apart from this report).
- `session-audit-delta-dismissed-errors-2026-09-23.md:140-149` — a count correction (42→39 blocked_by edges) was deferred to "the PR body at ship". **`mise run ship` has no `--body` flag** (`pr.py:525-527` `gh pr create --fill`; memory `project_session_2026-08-03-h.md:45`) ⇒ the body = the commit messages; a correction must be applied afterwards with `gh pr edit <n> --body-file` (read-only `gh pr edit` is not guard-denied as far as recorded — verify) or accepted as unfixable.
- `session-audit-codex-review-2026-09-24.md:20-23` — two P2s in KB artifact-review mode (fixed in `4e988717` per the removal report). One consequence worth arming: an artifact review could overwrite `review-<HEAD>-cold.md`; the receipt gate checks only presence + SHA (`review._report_gaps`). Before writing the KB receipt, READ the cold report and confirm it is a real diff review with a verdict.
- `codex-call-audit-2026-09-23.md` — wrapper reported "timed out/output empty" while codex still ran; check `ps` + the `-o` file before believing a wrapper (also `project_session_2026-09-23.md` 4th pass).

### 4. Removal session transcript `94aea797…jsonl` (grep-only, 2,775 records)

Extraction: parsed tool_use/tool_result blocks matching `mise run (ship|land|kb-ship|kb-land|kb-review-receipt|kb-gates)|denied|FAIL |rc=[1-9]|refus|git push|gh pr (create|merge)` → 143 hits (`/tmp/rm_tx.txt`). Control arm: the same pattern DID match the known `kb-gates` runs (L937, L1252, L1529, L2190, L2589), so zero ship/land hits is real — **the headless session never ran ship or land** (brief: "Commit; no push or ship", `session-audit-missing-requests-2026-09-24.md` row 10).

- **L2674 (KB#748 comment draft)** — `test_mcp_serve.py::test_kb_serve_actually_answers_mcp` "timed out waiting for reply id=1 (120 s) in **2 of 5** full `mise run kb-gates` runs": one concurrent with dotfiles pytest; the other on the CLEAN `4e988717` tree, "concurrent only with kb-gates' own parallel gates". `gates.py:277` `CONCURRENT_SAFE = {lint, test, brain-audit, graph-size}` — kb-ship uses the same runner (`pr.py:162-192` → `gates.run_and_record(..., stop_on_failure=True)`), so **kb-ship's `test` gate can flake with nothing else running**. L2642: final clean-tree run `test FAIL rc=2`, others PASS.
- **L1347** — `rule-sync` armed rc=1: "knowledge-base: missing agent `premise-verifier`". Local rule-sync reads the SIBLING WORKING TREE (`KB_REPO_PATH` or `../knowledge-base`, L262 docstring), CI reads KB `main` ⇒ local green ≠ CI green until KB is merged.
- **L1851** — `codex exec review --base` + `[PROMPT]` → rc=2 (`error: the argument '--base <BRANCH>' cannot be used with '[PROMPT]'`).
- **L1209 / L1219 / L1261 / L1270 / L2176 / L2185 / L2263 / L1509** — repeated lint failures (rumdl fmt, ruff_format, E501, agnix) after scripted edits; also `kb-check` pointed at a nonexistent `tests/test_cli.py`. Every one was caught by a gate before commit.
- **L590/L596** — agnix rc=1 on a WARNING (`.claude/CLAUDE.md` ~1588 tokens > 1500): `lint-docs` is `--strict`, so a warning is a ship-blocking failure.
- **L2312** — guard sanity: `gh pr create --fill` → `permissionDecision: deny` (dotfiles) — reaffirms: only `mise run ship` opens a dotfiles PR.
- **L2336** — `plugin-health: rc=1` (ponytail disabled in `settings.local.json`) — not a ship gate (not in `gate_matrix`); do not chase it mid-ship.

### 5. AgentsView history (v0.44.0, `--server http://127.0.0.1:8080` + token file; `agentsview health` NOT run)

Query: `agentsview session search '<RE2>' --regex --in tool_result --include-children --include-automated --limit 300 --json`, pattern = `FAIL +(ship|land)|ship: working tree not clean|could not enable auto-merge|land: .*not yet MERGED|FAIL +smoke-tiers|land: merge failed|ship: refusing|land: refusing` → 300 matches (page full, `next_cursor` 300 — so counts below are a FLOOR, not a census), 206 from `Bash` results (the rest are `Read`s of pr.py source). Control arm: the same search matched known source strings (`could not enable auto-merge` in a `Read` of `pr.py:475`, 2026-09-24), so the probe sees the index. Counts include report/doc greps that quote the strings, so they are occurrence counts, not incident counts.

Real output signatures, most frequent first (count · first examples):

| Count | Signature | Repo | Meaning for this run |
|---|---|---|---|
| 21 | `ship: refusing — the handoff for this branch cites something that is not there` (`… 1 broken (only broken exits 1)`) | KB (2026-09-09/10/12) | KB kb-ship handoff step; newest KB handoff today does not name this branch ⇒ SKIP. Do not write a KB `.agent/plans/session-*.md` naming the branch before kb-ship, or run `mise run kb-handoff-check` first |
| 21+ | `land: merge failed (head may have moved since the check)` | KB (2026-08-30/31, 09-02, 09-15) | Real cause in 24 of 29 git-error tails (`session search 'failed to run git' --project knowledge_base`): **`fatal: 'main' is already used by worktree at …`** — `gh pr merge --delete-branch` tries to check out `main` locally after the server-side merge. Message is misleading; the PR may already be MERGED. Also 3× `not a git repository`, 1× `Your local changes … would be overwritten` |
| 19 | `ship: refusing — not pushing an unreviewed commit` | KB mostly (+1 dotfiles 09-15) | missing/stale `kb-review` receipt — **exactly the current KB state** |
| 4 | `land: refusing — PR #N's head is unreviewed` | KB (2026-08-30 #621/#623) | receipt for PR head missing (amend/rebase after review, or landing from another machine) |
| 12+4 | `FAIL  ship: working tree not clean` / `ship: refusing — working tree is dirty` | dotfiles (09-09 … 09-21) + KB | untracked/uncommitted files; incl. harness notification "exit 0" (memory) |
| 14+16+15 | `FAIL  smoke-tiers-1-3` inside `mise run land` (`=== FAILURES ===`, `src/git.rs:193`, "stale base?") | dotfiles (09-03 … 09-17) | the land-smoke transient; `mise run smoke` standalone first, retry land once, never `dev-rebuild` first |
| 5 | `FAIL  ship: gh pr create rc=1` after all gates PASSED | dotfiles 2026-08-27 | gates (incl. sync-full) passed then PR create failed; historic cause seen once: `GraphQL: No commits between main and <branch>` (2026-04-02). Re-running ship reuses/creates the PR — gates re-run (cost) |
| 2 | `land: refusing — PR #N is not green` with `no checks reported on the '<branch>' branch` | KB #774 2026-09-12 | `checks_state` parses gh's merged stdout+stderr as JSON (`kb_setup/pr.py:229-232`); gh's "no checks reported" text is not JSON ⇒ refusal, although `:243-244` intends "no checks" = green. pr.py last changed 2026-09-15 (`e8fe42ae`); whether that path was fixed is UNVERIFIED — before kb-land, `gh pr checks <n> --json name,bucket` and confirm it returns a JSON list |
| 1 | `FAIL  land: could not fast-forward local main` ← `fatal: Unable to create '.git/index.lock': Operation not permitted` | dotfiles 2026-09-01 (session `8dac106f`, land -- 897 run in a sandboxed codex lane) | run land from a full-access shell (this session's Bash, or a `codex-*-operator` which runs at full access), never a sandboxed lane; main CI had already passed |
| 1 | `land: refusing — local branch 'feat/worktree-add' is 1 commit(s) ahead of` | KB 2026-09-13 | `_local_ahead_gap`: never commit locally after kb-ship without pushing through kb-ship again |

Live check for the worktree trap (read-only, now): `git worktree list --porcelain | grep -c 'branch refs/heads/main$'` → **0** in both repos (control: the same grep for `chore/remove-fable-orchestrator` → 1 in each). Local `main` is an ancestor of `origin/main` in both (KB 0 behind; dotfiles 1 behind) ⇒ `pull --ff-only` will succeed if nothing changes.

## GitHub repos touched

- [ray-manaloto/dotfiles](https://github.com/ray-manaloto/dotfiles) — pr.py ship/land, ci.yml rule-sync checkout, hook_guard gh rules, open PR list (`gh pr list --state open`), branch/merge-tree probes, session reports
- [ray-manaloto/knowledge-base](https://github.com/ray-manaloto/knowledge-base) — kb_setup pr.py/review.py/gates.py kb-ship/kb-land, receipts, worktree list, branch state
