# CORRECTED SPEC — #884 respec round 2 (FINAL) on top of c293c4d

This is the last round under the two-round bound. Everything below I confirmed
against the code myself; where the cold review's proposed FIX was wrong I say so,
so you do not implement it as given.

## 1. Objective

The gate you built in `c293c4d` cannot catch the defect it exists to catch. Fix
the gate's detection strategy, close the seam where its production entry point is
untested, and extend it to the half it currently ignores.

## 2. Files

    python/src/dotfiles_setup/codex_agent_parity.py
    tests/test_codex_agent_parity.py
    .claude/agents/codex-claude-code-expert.md      (G4)
    .claude/agents/codex-adversarial-critic.md      (G6)
    .claude/agents/codex-staleness-auditor.md       (G6)
    .claude/agents/codex-advisor.md                 (G7 nit)
    .codex/agents/codex-*.toml                       (G1 sentinel, G6 if bodies change)
    python/verification/suites.toml                 (G5)

Do not touch `.gitignore`, `.claude/CLAUDE.md`, `hk-common.pkl`, `hk.pkl`, or the
four Claude-backed originals.

## 3. The fixes

### G1 — replace symptom-sniffing with a capability assertion (the P1)

Measured by me on the four exported mirrors:

    marker            adversarial  claude-code-expert  staleness  dockerfile
    "Codex Code"           0              0                0          0
    ".Codex/"              2              5                0          0
    "Codex mcp add"        0              0                0          0

So **two of your three markers are dead code that can never fire**, and
`.codex/agents/staleness-auditor.toml` is unambiguously corrupted — it carries
`"does Codex do X"` at :124 and `docs/Codex` at :126 — while scoring **zero on
all three**. A gate blind to a live on-disk instance of its own target is
`.claude/rules/probes-need-a-control-arm.md` rule 9: you bound the check to
strings you do not own, and the exporter's phrasing moved.

⚠️ **The cold review's proposed fix is ALSO wrong — do not implement it as
given.** It proposed "hand-authored tomls have 5-18 case-insensitive `claude`
hits, every mirror has 0". I measured: `.codex/agents/claude-code-expert.toml` is
a mirror and has **18**. A positive-only "claude must appear" test would not flag
it.

Do this instead — assert a capability you OWN, so upstream phrasing cannot drift
the check into a no-op:

Give every hand-authored `.codex/agents/codex-*.toml` an explicit **sentinel** —
a short, distinctive line that identifies it as repo-authored and that a blind
`claude`→`Codex` substitution of an exported body cannot reproduce. Put it in a
stable field (a leading comment line, or a dedicated key). The gate REQUIRES the
sentinel in every tracked `codex-*.toml`. An exporter overwrite destroys it, so
the gate fails loudly regardless of what strings the exporter emits that day.

Keep the negative markers as a secondary signal, and broaden them with the two
shapes I measured (`does Codex`, `docs/Codex`) — but the sentinel is the primary
check, because it is the one whose failure mode is "our file was replaced"
rather than "we guessed the corruption's wording correctly".

Pin the sentinel's own load-bearingness: add a test asserting the sentinel is
absent from all four EXPORTED mirrors, so a later tidy-up that makes the sentinel
something the exporter also emits fails immediately.

### G2 — test the production entry point in the FAILING direction (P2)

The review mutated `codex_agent_parity_main` to `return 0` and the whole suite —
18 targeted tests plus 2577 total — still passed. `find_violations` is well
covered; the seam between it and the CLI is not, and that seam is what the hk
step actually runs.

Add a test that drives the real entry point (`codex_agent_parity_main`, or the
CLI through `main.py`) against a fixture with a known violation and asserts a
NON-ZERO return. Arm it: confirm it fails when the entry point is stubbed to
`return 0`, then restore.

### G3 — the gate ignores the `.md` half entirely (P2)

Zero bytes of any `.claude/agents/codex-*.md` are read; a 0-byte `.md` passes
rc=0. The step is called "parity" and enforces none of what makes these lanes
work. Extend it to check, per `.md`:

- frontmatter `name` equals the filename stem;
- a `model:` line is present (do not pin the value — that is a tuning decision);
- the body contains BOTH `--model gpt-5.6-sol` and an xhigh
  `model_reasoning_effort`, since either missing silently inherits from
  `~/.codex/config.toml`, which this repo does not own;
- the never-substitute-your-own-reasoning prohibition is present.

Each with a control arm proving it fails when violated.

### G4 — raise effort, not model, on `codex-claude-code-expert`

Add `effort: high` to its frontmatter, keeping `model: haiku`. You flagged the
step correctly (enumerate-by-SHAPE is a judgement call); this is the lever that
addresses it without paying for a bigger model. `effort:` is a valid frontmatter
key.

### G5 — add the `suites.toml` contract you flagged as not-done

Follow the `workflow.bash-logic-enforcement` precedent: assert the whole chain
exists — hk step, CLI subcommand, module, tests, mise task.

⚠️ **Use `mise run token-check` while CHOOSING the tokens, not after.** A token
matching more than once can be satisfied by a stand-in, which is how a deleted
registration once stayed green. Report each token's match count.

### G6 — the deliver-before-idle rule is in `codex-advisor` only

The other three lanes have the persist-first ordering but not the
deliver-before-idle / report-last rule. Add it to all three. An agent that
finishes and goes idle without delivering is a total loss, and that has happened
here before.

### G7 — two nits

- `codex_agent_parity.py`: a non-UTF-8 toml raises an uncaught
  `UnicodeDecodeError`. It fails closed via the catch-all, but reports
  "Unexpected command failure" instead of naming the file. Read with
  `errors="replace"` or catch and report it as a violation naming the path.
- `codex-advisor.md:104-105`: stray double blank line.

## 4. Constraints

- Do not weaken anything from `1aca600` or `c293c4d` that this spec does not
  fault. In particular the `hk-common.pkl` enumerated exclude is CORRECT and I
  armed it myself: I planted a typo (verifying the term was 0-count tree-wide
  first) and `mise run lint` went rc=1 with `typos` naming the file by path.
- Do not act on the review's claim that `.codex/agents/` has no consumer beyond
  adding a comment. The Codex Desktop app demonstrably WRITES that directory —
  it produced the four exported mirrors at a single 18:52:09 batch mtime — so a
  consumer exists. Add one comment line in `codex_agent_parity.py` naming it, and
  say the `codex exec` CLI path does not read these files.

## 5. Verification

    mise run lint-docs
    mise run lint
    mise run verify
    mise exec -- uv run --project python pytest tests/ -q
    mise run token-check -- python/verification/suites.toml "<each new token>"

Use `mise exec --` on pytest. Deselect nothing.

Report, per gate check you add, both arms' actual output — the violating input and
its message, and the clean pass. A check verified only on clean input is
decoration.

## 6. Commit

COMMIT: lane — follow-up commits on `feat/codex-agent-lanes-884`. Do not push, no
PR, no other branch.

## 7. PREMISES

- L1 `codex_agent_parity.py:75-79` defines `CORRUPTION_MARKERS = ("Codex Code", ".Codex/", "Codex mcp add")` — read this session.
- L2 Marker counts I measured this session across the four exported mirrors: `Codex Code` 0/0/0/0; `.Codex/` 2/5/0/0; `Codex mcp add` 0/0/0/0 (order: adversarial-critic, claude-code-expert, staleness-auditor, dockerfile-reviewer).
- L3 `.codex/agents/staleness-auditor.toml:124` contains `"does Codex` and `:126` contains `docs/Codex` — read this session; it is corrupted while scoring 0 on all three markers.
- L4 `.codex/agents/dockerfile-reviewer.toml` contains the string `Codex` 0 times — measured this session; it is not corrupted, so the gate missing it is not a defect.
- L5 Case-insensitive `claude` counts this session — mirrors: adversarial-critic 0, claude-code-expert **18**, staleness-auditor 0, dockerfile-reviewer 0; hand-authored: 7, 5, 18, 6. This REFUTES "every mirror has 0" and rules out a positive-only `claude`-presence test as sufficient.
- L6 The four exported mirrors share mtime `18:52:09` exactly — `stat` this session; a single batch write by the Codex Desktop app, which establishes that a consumer of `.codex/agents/` exists.
- L7 `mise run lint` returns rc=1, with `typos` naming `.codex/agents/codex-advisor.toml` by path, when a misspelling is planted in that tracked file; the term was verified 0-count tree-wide before planting, and the tree was restored clean afterwards — run by me this session. The enumerated exclude works.
- L8 The gate's current behaviour, run by me this session: clean -> rc=0; a `name` field changed away from its filename stem -> rc=1 naming the file and both values; restored -> rc=0.
- A1 ASSUMPTION: `effort:` is accepted alongside `model:` in `.claude/agents/*.md` frontmatter. Held without a code read because the loader's schema is not in this repo; the round-1 report cited it, and a citation from a report is not a code read. If the loader rejects it, report that rather than working around it.
