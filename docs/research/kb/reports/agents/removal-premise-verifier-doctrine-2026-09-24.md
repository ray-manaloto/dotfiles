# Premise verification — orchestration doctrine after fable-orchestrator removal (2026-09-24)

Lane: a general-purpose Opus agent following `.claude/agents/premise-verifier.md`. The agent could not be spawned by type because it was added after the session started. It edited nothing except this report. Graph: `mise run graphify-health` returned rc=3 `stale` (built at 3db81d8e, HEAD d4b0b5c4), so every check below reads source directly.

```
PREMISE REPORT
ROWS: 20 checked — 15 CONFIRMED (0 provenance corrected) / 2 REFUTED / 3 UNVERIFIABLE / 0 ASSUMED
D1 grok not installed — CONFIRMED — `command -v grok` rc=1; control `command -v codex` rc=0
D2 lanes exist (codex-{sol,astra}-implementer/advisor/critic/auditor/expert, cold-reviewer, premise-verifier, claude-advisor, gate-runner…spec-scribe) — CONFIRMED — `ls .claude/agents/`
D3 `exec review` has --commit/--base/--ignore-rules — CONFIRMED — codex-cli 0.156.1 `exec review --help` lines 27/21/66
D4 `codex exec -s read-only … review` enforces read-only — UNVERIFIABLE — `-s` exists on `exec` (exec help :55) but not in `review --help`. My argv probe did not discriminate: `-s bogusmode review --help` also returned rc=0. The doctrine already marks this "pending #1297", and #1297 is OPEN. Non-blocking because the text itself flags it.
D5 `exec review` ignores --output-schema — UNVERIFIABLE — the source is an issue report, not code: #1296 (CLOSED) comment says "accepted and IGNORED for reviews". The flag is still listed in `review --help` :69. Non-blocking.
D6 #1310/#1315 OPEN, #1294 CLOSED, #1297 OPEN — CONFIRMED — each title matches how the doctrine uses it (`gh issue view`)
D7 claude-advisor: fable, xhigh, no memory — CONFIRMED — claude-advisor.md:4-5, no `memory:` key
D8 "the codex advisor … returned an empty `-o` file" — CONFIRMED — codex-sol-advisor.md:107 uses `-o "$OUT"`
D9 agent files link token-routing — CONFIRMED — codex-sol-advisor.md:223, claude-advisor.md:15
D10 "CLIs pinned host-only in `mise.toml` (`codex`, `antigravity-cli`)" — REFUTED — mise.toml:171 `disable_tools = ["npm:@openai/codex"]`, so the host runs the native install. The codex pin lives in the shared file (shared.toml:44), not a host-only one. The antigravity-cli half holds (mise.toml:128).
D11 fable-orchestrator no longer enabled; antigravity is — CONFIRMED — `jq .enabledPlugins` on settings.json
D12 workflows /gated-implementation, /graphify-refresh — CONFIRMED — .claude/workflows/*.js
D13 astra lanes generated by `codex-lane-mirror` — CONFIRMED — mise.toml:1362
D14 trigger rule-synced byte-for-byte — CONFIRMED — the two trigger lines compare identical; rule-sync.toml:39
K1 kb-codex-implementer, claude-advisor (fable/xhigh), kb-codex-advisor (sol), kb-codex-astra-advisor — CONFIRMED — KB agents dir; claude-advisor.md:4-5; kb-codex-advisor.md:96
K2 `cold:codex-astra` = `kb-codex-astra-reviewer` / `mise run kb-codex -- --review`, `--model` — CONFIRMED — KB mise.toml:1190; codex_run.py:659, :103
K3 "default since knowledge-base#794" — UNVERIFIABLE — KB#794 is still OPEN ("Make cold:codex-astra the default review lane"). The claim holds only if this branch closes #794.
K4 `_lane_prefix` at review.py:200-202, "called at review.py:620" — REFUTED — the definition is at :200. The call is at :644, inside `report_path` (:634). Line :620 is unrelated docstring text.
K5 `antigravity:review` exists — CONFIRMED — present in this session's skill list (harness state; I did not read a file for it)
K6 bundled `/code-review` and `/mattpocock-skills:code-review` exist — CONFIRMED — skill list (harness state)
MISSING:
- KB .claude/CLAUDE.md:78 says "five-part spec", but orchestrator-routing SKILL.md:80 and the dotfiles skill both say seven-part. Reconcile.
- orchestrator-routing SKILL.md:4,14,17,73 still frame a "Fable-5 architect", while the synced trigger says any model (the default is Opus 5). Decide which framing holds.
- The dotfiles CLAUDE.md lane table gives the codex-diff cold lane as "an Opus subagent (`Agent`, model opus)", while the skill's Review tiers name `cold-reviewer`. Name one.
- claude-advisor is called "read-only", but its tools include Bash (claude-advisor.md:6). Read-only is an instruction at :21, not enforced by the tool list. State this if the claim matters.
VERDICT: correct the spec first — D10 (the codex host pin) and K4 (a stale line cite) are false, and K3 cites an OPEN issue as if it had landed. All three are cheap text fixes. D4 and D5 are non-blocking because the doctrine already flags #1297/#1296.
```

## GitHub repos touched

- [ray-manaloto/dotfiles](https://github.com/ray-manaloto/dotfiles) — issues #1294, #1296, #1297, #1310, #1315; checked the doctrine sources
- [ray-manaloto/knowledge-base](https://github.com/ray-manaloto/knowledge-base) — issue #794; checked kb-review, CLAUDE.md, review.py
