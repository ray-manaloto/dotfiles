# Cold review: cddc27e..b03de55

Resolved SHAs:
- b75fa3b — chore(deps): clear stale v-prefixed lock entries; bump aws-cli 2.36.35
- dd23829 — chore(deps): bump graphifyy 0.9.42 -> 0.9.53
- b03de55 — docs(graphify): retire the two-version split the 0.9.53 bump closed

## Findings

MEDIUM | agnix's linux-x64 asset silently lost its verified-attestation record during the "cosmetic" relock | mise.lock:6619 (pre-image, `git show cddc27e:mise.lock` line 6619) vs mise.lock post-b75fa3b (line removed)
Control command: `git diff cddc27e..b75fa3b -- mise.lock | grep -E "^[+-](version|checksum|provenance)"` -> only 4 hunks changed; one is a bare `-provenance_verified = true` with no replacement, attached to `[tools."github:agent-sh/agnix"."platforms.linux-x64"]` (checksum/url unchanged either side, confirmed via `git show cddc27e:mise.lock | grep -n -A25 agnix`).
Failure scenario: agnix is declared "host-only" in mise.toml:493 but CI actually runs `agnix . --strict` on GitHub Actions ubuntu (linux-x64) runners (mise.toml:1195-1196 comment: "parity with ci.yml"). Before this commit, that linux-x64 asset had `provenance_verified = true` — a real GitHub-attestation verification result, presumably captured when the lock was previously regenerated from a linux host (devcontainer/CI). The commit deleted the whole agnix lock block and let mise "resolve fresh against the bare mise.toml pins" from THIS host (macOS ARM), per the commit's own description. mise can only set provenance_verified=true for the platform it can actually verify from the executing host: the new lock now carries provenance_verified=true ONLY for macos-arm64 (mise.lock:6644, matching this Mac), while linux-x64, linux-arm64, linux-x64-baseline, linux-arm64-musl, linux-x64-musl all carry `provenance = "github-attestations"` with no `_verified` flag. So the asset CI will actually download and run is now recorded as attestation-attempted-but-unverified where it was previously verified — a real integrity-tracking regression, invisible in the commit message (which describes only version-string cosmetics + the aws-cli bump).
Control arm for "clean" claim: rumdl (same commit, same treatment — v-prefix stripped) never had `provenance_verified=true` anywhere before OR after (checked via `git show cddc27e:mise.lock | grep -n -A25 '\[\[tools.rumdl\]\]'`), so this is NOT a universal side effect of the relock — it is specific to agnix, which is the one entry that actually had a verified record to lose.
This is exactly the failure mode the repo's own `lock-shared` skill warns about ("macOS picks a different release asset... writing an entry that is wrong only on the platform no local gate exercises") — except here it's not a wrong asset, it's a silently downgraded verification guarantee for the platform CI depends on.
Note: agnix's mise.toml declaration is a bare host tool (mise.toml:28), not in `.config/mise/conf.d/shared.toml`, so the `lock-shared` skill/task doesn't apply to it directly — but the same class of hazard (macOS-run `mise lock`/relock silently degrading a linux-platform artifact) applies to any tool whose linux entry a prior linux-run lock had verified.
Recommendation: re-run the agnix lock step from a linux host (devcontainer) to restore `provenance_verified` for the linux platforms, or confirm mise intentionally never persists that flag across a relock and downgrade the concern to LOW/informational — I did not find mise's own docs/changelog on this in-repo, so treat this as unconfirmed-by-vendor-docs, evidenced only by the lockfile behavior itself.

LOW | mise.lock v-prefix cleanup itself is clean — no version/checksum mismatch found | mise.lock (agnix, rumdl blocks)
Control: `git diff cddc27e..b75fa3b -- mise.lock | grep -E "^[+-](version|checksum|provenance)"` shows only version-string reformatting (`v0.52.1`->`0.52.1`, `v0.2.62`->`0.2.62`), the one provenance_verified drop above, and the aws-cli version+checksum bump. No checksum for an unchanged version changed, no platform block was dropped entirely (all platform stanzas still present for both tools, confirmed by counting `[tools.rumdl."platforms` and `[tools."github:agent-sh/agnix"."platforms` stanzas before/after — same count, 8 each). aws-cli's macos-arm64 platform gained a `checksum = "blake3:..."` line it previously lacked — that's additive (a stronger check), not a coverage loss.

CLEAN (with control) | `[tool.uv] override-dependencies` does not currently collide with kb-setup's real code path | python/pyproject.toml:51-57, dd23829
Traced: kb-setup (SHA-pinned at c70f0f81 in python/pyproject.toml:40) itself declares `graphifyy[all]==0.9.42` in its own pyproject (confirmed: `git -C ../knowledge-base show c70f0f81:pyproject.toml` line 26). Its only module that imports the graphifyy *internal* API (`from graphify.build import build, ...`, `from graphify.detect import detect, ...` etc.) is `kb_setup/graphify_sdk.py`, and it is imported lazily (function-local `from kb_setup import graphify_sdk`, not at module top) only from inside `kb_setup/graph.py`'s build/detect functions. dotfiles' own `python/src/dotfiles_setup/graphify.py:23` imports only `from kb_setup.graph import GraphifyBuildReceipt` — a msgspec.Struct type, and `kb_setup/graph.py`'s own top-level imports (`atomic, graph_checks, graphify_health, graphify_ops`) do not touch graphify_sdk (confirmed via grep — none of those four modules import `graphify`/`graphifyy` at module level). The kb-setup subcommands dotfiles actually invokes (`kb-setup currency daily`, `kb-setup currency check`, `kb-setup cc`, `kb-setup md-budget`) do not import `kb_setup.graph`'s build/detect path either (grepped currency/run.py, currency/__init__.py, launch.py, check.py at the pinned SHA — no `graphify_sdk`/`kb_setup.graph` hits).
So TODAY, nothing invoked from this repo's venv actually calls kb-setup code written against graphifyy 0.9.42's internal `build`/`detect`/`observe_detect` API while that API is actually resolved at 0.9.53. The override is scoped to exactly one package/extras/version (uv.lock manifest: `overrides = [{ name = "graphifyy", extras = ["all"], specifier = "==0.9.53" }]`) — no silent widening to other packages or platforms.
Residual (not a live bug, a latent risk worth naming): the override means ANY future kb-setup code path that reaches `kb_setup.graph`'s build/detect functions (or a future kb-setup CLI subcommand added to a dotfiles mise task) would silently run kb-setup's 0.9.42-authored code against an untested 0.9.53 API, with no test in this repo asserting that boundary stays inert. The commit's claim "without touching the out-of-bounds knowledge-base repo" is true for the repo's *files*, but not strictly true for kb-setup's *runtime dependency resolution* inside THIS project's shared venv — it is a live override of what kb-setup will actually import if its build/detect code path is ever reached from here.

CLEAN (with control) | version-drift test correctly re-targeted, no tautological/dead test introduced | tests/test_graphify.py:78,227,242-243,320-343,360,389,417,463,469,493
Control: `grep -n "0\.9\.4\|0\.9\.5" tests/test_graphify.py` — every FRESH-status test now asserts against "0.9.53" (the real new pin), and `test_graphify_health_reports_version_drift` (tests/test_graphify.py:305-326) monkeypatches `_runtime_version` to the now-*wrong* "0.9.42" to simulate drift, i.e. it moved from a value that used to be a false-drift/no-drift ambiguity risk (drift sentinel must differ from real pin) to one that still differs from the real pin (0.9.53). This is the correct direction — the sentinel was NOT left on the value that now equals the real pin, which is exactly the trap the brief asked to check for.

CLEAN (with control) | `0.9.42` version-string replacement is complete across all live (non-docs-archive) copies | repo-wide grep
Control: `grep -rn "0\.9\.42" --include="*.py" --include="*.toml" --include="*.md" --include="*.sh" --include="*.pkl" .` (excluding .git). Every hit outside `docs/research/kb/reports/agents/**` (persisted verbatim historical agent reports — correctly frozen per agent-report-persistence.md) and `docs/agents/goal-history.md` (append-only per goal-history.md) is either: (a) `python/pyproject.toml:52`, a comment describing kb-setup's OWN (still-true) 0.9.42 pin, not this repo's — correct to keep; or (b) the deliberate drift-sentinel in tests/test_graphify.py (see above). `.agents/skills/graphify/.graphify_version` is the only actual version-stamp file on disk (searched `find . -name ".graphify_version"`) and it reads 0.9.53. No stale copy found. Negative control for the grep shape: the same command finds the known-present "0.9.53" string in mise.toml/pyproject.toml/graphify.py at dozens of lines, so a 0-result would have been meaningful; instead it truthfully found only the two justified survivors.

CLEAN | b03de55 prose changes state nothing false, and stay within the bash_budget line cap | .claude/rules/graphify-first.md, scripts/graphify-hook-guard.sh
scripts/graphify-hook-guard.sh is exactly 31 lines (`wc -l`) against its bash_budget.py allowance of 31 (python/src/dotfiles_setup/bash_budget.py:106) — at the ceiling, not over. graphify-first.md's rewritten section correctly states the current pin (0.9.53, matches python/pyproject.toml:9) and dates the alignment ("as of 2026-08-31") rather than asserting it as a standing invariant, consistent with the commit's own stated goal. No caveat was deleted: the "nothing records which graphify built the graph on disk" claim, the rebuild-stamp dead-end explanation, and the "always the mise tasks, never bare graphify" guidance are all preserved verbatim in the diff.

## GitHub repos touched
- Local clone of [ray-manaloto/knowledge-base](https://github.com/ray-manaloto/knowledge-base) (sibling working dir), read at commit c70f0f81378ff55522496b57a828645fc002602e (the SHA dotfiles' `python/pyproject.toml` pins) — to trace kb-setup's own graphifyy pin and which of its modules import graphify's internal API, for the override-dependencies finding.
