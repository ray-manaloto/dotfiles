# Premise verification — spec A-2 (fable-orchestrator:premise-verifier, 2026-09-10 00:2xZ) — verbatim parts

## PART 1/4 — L-row verdicts
Repo at feat/orchestration-modernization-audit 241737d (matches). Read every file fresh; edited nothing.

L1 CONFIRMED. `.claude/rules/clarify-before-acting.md:8-9` — "use the `AskUserQuestion` TOOL — never options in prose ... it is unconditional". KB `sub-agents.md:379` states the first filter "removes these tools, even when listed in the `tools` field"; `AskUserQuestion` is the entry at `:382`. Spec's 378-384 spans it; the load-bearing lines are 379 and 382. Caveat the refactor must carry: `:379` also says forks skip both filters and get the main conversation's exact tool pool, so a fork is the one subagent shape that CAN ask.

L2 CONFIRMED. `agent-report-persistence.md:31` is rule 1b; `:45-55` is rule 1c, "coordinator ONLY" at `:51`, "A delegate never writes `task_plan.md`" at `:55`.

L3 CONFIRMED. `agent-artifact-conventions.md:7` is the `.omc/` rename block; `:23` is the `.agent/project-memory.json` row.

L4 CONFIRMED. `notepad-enforcement.md:11-13`; "0 invocations across 941 transcripts" lands at `:13`.

L5 CONFIRMED, anchors DRIFTED. `ai-cli-invocation.md:18` (`--full-auto`) ✓, `:28` (`-p` is `--profile`) ✓, `:47` (`gemini "prompt"` hangs) ✓. But in `.claude/agents/codex-operator.md` the `--full-auto` note is at :76-79, not 74-78, and the `--approve-for-me` exclusivity note is at :73-74, not 72-73.

L6 CONFIRMED, one truncation. `md-size-budgets.md:40-47` holds the misattribution, but the Windsurf block quote runs through `:50` (the source URL is on :50), so cite :38-50. `:93` is the `rule_unscoped` row, 200 lines / 24,000 bytes ✓.

L7 CONFIRMED exactly. `docs/rules-evidence/` holds 17 notes: the four named present, 13 others, NO `clarify-before-acting.md` and NO `md-size-budgets.md`.

L8 CONFIRMED, anchor drifted. KB `hooks.md:2304` is "SubagentStart hooks can't block subagent creation, but they can inject context into the subagent"; the `additionalContext` table row is `:2308`; the JSON example is `:2310-2317`. Cite 2304-2317.

L9 CONFIRMED exactly, all six headings start on the named lines.

Method note: `mise run graphify-health` returned fresh (runtime 0.9.53), but the query truncated at 53 of 173 nodes, so the graph was treated as unavailable and source was used for every repo claim.

## PART 2/4 — A rows and harness premises M1-M5
A1 ASSUMED, status unchanged (four mechanisms confirmed at their anchors; the 23 citation corrections not re-read).
A2 CONFIRMED with two caveats: `plansDirectory` is documented (`settings-reference.md:2709-2721`, indexed :703, example `settings-example.md:164`; added 2.1.9 per `changelog.md:4782`; unchanged through 2.1.266). (a) :2711 resolves relative to the project root, default `~/.claude/plans` (:2715); `.agent/plans` is valid. (b) Adopting it moves plan files into a `git clean -xdf` sweep (`agent-artifact-conventions.md:13`; memory `feedback_plan_files_persist` says `~/.claude/plans` survives) — a durability regression; make it an explicit decision.
M1 — version drift: `claude --version` 2.1.267; `codex --version` codex-cli 0.152.1 (not the 0.152.0 at codex-operator.md:77).
M2 — `askUserQuestionTimeout` EXISTS but is not project-settable: `settings-reference.md:2759-2774`, scope User or managed (:2763), values "60s"/"5m"/"10m"/"never" (:2764), default "never" (:2765), requires v2.1.200 (:2761); when it fires it submits the options already selected (:2761) — an auto-continue is a PARTIAL ANSWER; `CLAUDE_AFK_TIMEOUT_MS` overrides per session (:2766).
M3 — SubagentStop `additionalContext` reaches the SUBAGENT, not the coordinator (`hooks.md:2346`: parent-side injection = PostToolUse hook on the `Agent` tool); `decision: "block"` + `reason` keeps the subagent running; `hooks.md:664`: a hook inside an agent definition has its Stop converted to SubagentStop.
M4 — a broken SubagentStart hook fails silently (`hooks.md:870` "No — Shows stderr to user only"; :888 exit-2 stderr renders in the subagent's own transcript) ⇒ the selfcheck row and a both-arms test are load-bearing.
M5 — SubagentStart matchers filter on agent TYPE (`hooks.md:314`); to reach every subagent the registration must be unscoped (matcher None), per `hook_selfcheck.py:105-108`.

## PART 3/4 — selfcheck wiring and a REFUTED budget premise
M6 — `_SETTINGS_WIRING` (`hook_selfcheck.py:87-131`) declares five rows (PreToolUse, SessionStart, SessionEnd, InstructionsLoaded, PostToolUse); `check_settings_wiring` (:186-215) iterates only declared rows — no unknown-event rejection, no row-count pin (both greps 0, control-armed). A new `hooks.SubagentStart` is mechanically safe but UNPROTECTED until a row exists; :199-215 requires ONE SINGLE settings entry to carry every required substring AND matcher token (pooling rejected).
M7 — `.claude/settings.json` wires exactly five events today; no SubagentStart/SubagentStop, no `plansDirectory`, no `askUserQuestionTimeout`.
M8 — REFUTED: `md-size-budgets.md` carries `paths:` frontmatter (:1-8) ⇒ reclassified `rule_scoped` (`kb_setup/md_budget.py:475`), budget row :95 (400 lines / 32,000 bytes), not :93. The other five are unscoped (each frontmatter checked; clarify-before-acting as the control).
M9 — lines bind before bytes: clarify-before-acting 116/200 (5,961 B); agent-report-persistence 93/200 (5,063); agent-artifact-conventions 103/200 (5,745); notepad-enforcement 44/200 (1,867); ai-cli-invocation 73/200 (2,578); md-size-budgets (SCOPED) 190/400 (10,132/32,000).

## PART 4/4 — live codex probes, gate ownership, spec fixes
M10 — the md_size_budget gate is `kb-setup md-budget` (hk.pkl:617); constants live in knowledge-base `kb_setup/md_budget.py:95` (`EAGER_BYTE_BACKSTOP = 24_000`) and :182 — moving a number needs a knowledge-base PR. `listing_budget.py` governs the skill listing, a different concern.
M11 — §2 does not authorize the `plansDirectory` settings edit.
M12 — §5.6 probed live on codex-cli 0.152.1: `grep -c -- '--full-auto'` → 0; `grep -c -- 'workspace-write'` → 2; positional prompt DOCUMENTED (`Usage: codex exec [OPTIONS] [PROMPT]`; stdin appended as a `<stdin>` block) — refutes `ai-cli-invocation.md:29`; `-p, --profile` stays true (:28); `-s, --sandbox` takes read-only|workspace-write|danger-full-access; `--approve-for-me` exclusivity reproduced: `error: the argument '--approve-for-me' cannot be used with '--sandbox <SANDBOX_MODE>'`. `agy` 1.1.24 and `gemini` on PATH. Probe outputs: scratchpad/codex-help.txt, scratchpad/excl.txt.
Three edits recommended before dispatch: correct the four drifted anchors; per-file budget classes (md-size-budgets against :95); make `plansDirectory` an explicit decision naming the `git clean -xdf` cost.
