# Spec — #796: retire the Codex-side writer-lease hook (`.codex/hooks.json`), keep the repo truthful

Repo /Users/rmanaloto/dev/github/ray-manaloto/dotfiles, branch `fix/791-writer-lease-recovery`. Preceding commits on this branch: `6755fd5` (#759 cherry-pick), `af7a9e1` (#791 round 0), the #791 round-1 commit, and the architect's small commit that DELETED `.codex/hooks.json`, removed the dead `!.codex/hooks.json` un-ignore line from `.gitignore`, and dropped the file's path + its two `statusMessage` tokens from the `workflow.writer-lease` contract in `python/verification/suites.toml`. Your job is everything that commit deliberately left for a lane: the tests that read the deleted file, and the prose/contract lines that now describe an enforcement that no longer exists.

## 1. Objective

Ray's decision (2026-08-27): Codex sessions are no longer lease-enforced by a native hook — `.codex/hooks.json` held nothing but the two writer-lease hooks and the codex-implementer lane could not operate under a Claude-held lease (issue #796: codex runs under its own session id, so the hook denied every codex shell call; its sandbox also broke the admitted bootstrap). Claude's wiring is untouched: `.claude/settings.json` still runs `/usr/bin/python3 -I -S "${CLAUDE_PROJECT_DIR:-.}/scripts/writer-lease-hook-runner.py"` on PreToolUse / PostToolUse / PostToolUseFailure, and that runner still executes `python/src/dotfiles_setup/codex_writer_lease_hook.py` (misnamed, load-bearing — it stays).

Outcomes that must become true:

- **T1 — the seven real-process tests keep proving the runner's properties, driven through the Claude command shape.** Every test that read `.codex/hooks.json` for the inline locator command must instead invoke `/usr/bin/python3 -I -S <root>/scripts/writer-lease-hook-runner.py` with the same stdin payload (`cwd` = the nested directory, as today). The runner's own `_runtime()` ancestor walk (`scripts/writer-lease-hook-runner.py:83-123`) is the mechanism under test now. Five retarget without changing what they prove: `test_pinned_system_runner_uses_real_project_runtime_and_drains_posttooluse` (:1207-1297 — replace the Pre==Post byte-equality assertion with the equivalent fact about `.claude/settings.json`: the PreToolUse, PostToolUse and PostToolUseFailure runner commands are byte-identical), `test_codex_hook_command_reaches_the_tracked_runner_from_nested_cwd` (:1300-1334 — the argv-shape block :1306-1312 describes the deleted inline string and has no successor; delete it, keep the behavioural half), `test_codex_hook_selects_inner_runner_beneath_unrelated_outer_repo` (:1337-1381), `test_codex_hook_ignores_incomplete_outer_runtime_candidates` (:1462-1528), `test_codex_hook_symlink_components_cannot_redirect_pre_post_lifecycle` (:1531-1624).
- **T2 — two tests change their failure-shape assertions, not their property.** `test_codex_hook_blocks_ambiguous_complete_outer_runtime` (:1384-1459) and `test_codex_hook_blocks_when_no_complete_runtime_candidate_exists` (:1627-1706) asserted the inline locator's `exit 2` + fixed stderr. The runner surfaces the same two conditions as a fail-closed decision: exit 0, empty stderr, and a structured stdout JSON whose `permissionDecisionReason` (PreToolUse) contains `Writer lease enforcement failed closed:` followed by `hook cwd has ambiguous complete writer-hook runtimes` / `hook cwd has no complete tracked writer-hook runtime` (`scripts/writer-lease-hook-runner.py:121-122`, `:183-191`, `_deny` :25-36). Assert that shape. The "decoy never executed" half now means the decoy HOOK ENTRYPOINT (`codex_writer_lease_hook.py` in the outer/decoy runtime) is never executed — keep a marker-writing decoy hook and assert the marker is absent; the runner named in argv is by construction the real one. Keep the existing control arms (delete the decoy → deny-not-fail-closed).
- **T3 — keep every contract-bound test name.** `workflow.writer-lease` (`python/verification/suites.toml:2155`) binds five of these function names verbatim (`def test_codex_hook_command_reaches_the_tracked_runner_from_nested_cwd(`, `…selects_inner_runner_beneath_unrelated_outer_repo(`, `…blocks_ambiguous_complete_outer_runtime(`, `…symlink_components_cannot_redirect_pre_post_lifecycle(`, `def test_pinned_system_runner_uses_real_project_runtime_and_drains_posttooluse(`). Either keep the names, or rename AND update the tokens with `mise run token-check -- tests/test_writer_lease.py "<token>"` proving 1 match each. Keeping the names is the cheaper, acceptable choice; `codex_hook` in a name is then historical.
- **T4 — prose tells the truth, tokens survive.** Rewrite only the sentences that assert Codex hook enforcement:
  - `docs/specs/codex-writer-lease.md`: :119-121 ("Both `.codex/hooks.json` and `.claude/settings.json` use the same tracked runner" → Claude only); the Codex-locator paragraph :126-136 (drop; keep the runner's descriptor-walk + fail-closed sentences :136-140); :142-147 (the pre-lease bootstrap shape is still honoured by `hook_decision`'s exemption for any caller of the runner, but no Codex hook enforces it — say so); :157-161 (the `--dangerously-bypass-hook-trust` certification paragraph — the phrase `--dangerously-bypass-hook-trust` is a CONTRACT TOKEN at suites.toml:2155: keep the token in a truthful sentence, e.g. that this replay arm is retired/historical, or replace the token in suites.toml with token-check); evidence items 6, 7, 9 (:255-267) — reword to the runner-driven, Claude-command arms the tests now are; the architecture mermaid node `ROOT["Pinned Python: exactly one complete runtime"]` (:168) is ALSO a contract token — keep the node text (it remains true of the runner's `_runtime()`), and make `T["Codex or Claude task"]` (:167) say Claude. Add ONE paragraph stating the new posture: Codex sessions are cooperative, not hook-enforced (they still run `writer-lease-hold/check/status/drain` by rule); a Codex lane launched by a Claude architect mutates under the architect's lease and is single-writer by the one-live-lane orchestration rule, not by a hook (#796).
  - `.claude/rules/writer-lease.md`: the Codex-locator paragraph :85-94 (drop; keep the runner clause :92-94) and "Codex and Claude `PreToolUse` register…" :96 → Claude. The file's three contract tokens (`# Repository writer lease`, `immutable, content-addressed 64-event chunks`, `` `PostToolUseFailure` drains failed tools ``) are elsewhere — do not disturb them. Unscoped rule: stay ≤200 lines / 24,000 bytes.
  - `docs/agents/codex-task-orchestration.md` :69-75: "Codex and Claude `PreToolUse` hooks" → Claude; drop the "Review changed Codex hook hashes with `/hooks`" sentence (:74-75). The sentence at :71 carries the contract token `` `PostToolUse` hooks drain those exact IDs `` — keep it verbatim inside the reworded sentence.
  - `.agents/skills/codex-task-orchestration/SKILL.md` :179-185: :180 carries the token `` `PostToolUse` hook must drain `` — keep it verbatim (the Claude hook still must); drop :183-185 (the Codex hook-hash trust sentence). `native writer-lease task` (:176) and `must acquire that Git-common-dir` (:177) stay.
- **T5 — nothing else moves.** Do not touch `scripts/writer-lease-hook-runner.py`, `python/src/dotfiles_setup/codex_writer_lease_hook.py`, `python/src/dotfiles_setup/writer_lease.py`, `.claude/settings.json`, `mise.toml`, or `AGENTS.md` (at its 12,000-char cap). Do not re-add `.codex/hooks.json`.

## 2. Files

- `tests/test_writer_lease.py` (the seven tests above; add a small helper for the runner argv if it removes duplication)
- `docs/specs/codex-writer-lease.md`, `.claude/rules/writer-lease.md`, `docs/agents/codex-task-orchestration.md`, `.agents/skills/codex-task-orchestration/SKILL.md`
- `python/verification/suites.toml` ONLY if you rename a test or move a token (T3/T4); every changed token proven with `mise run token-check`.

## 3. Interfaces

- Runner invocation under test: `[ "/usr/bin/python3", "-I", "-S", str(root / "scripts" / "writer-lease-hook-runner.py") ]`, stdin = the same JSON payload the tests build today (`hook_event_name`, `session_id`, `cwd`, `tool_name`, `tool_use_id`, `tool_input`), `cwd=<nested>`.
- Fail-closed decision shape (PreToolUse): `{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "Writer lease enforcement failed closed: <reason>."}}` on stdout, exit 0 (`scripts/writer-lease-hook-runner.py:25-36`, `:183-191`).

## 4. Constraints and invariants

- Repo conventions bind (`AGENTS.md`, `python/AGENTS.md`, `tests/AGENTS.md`): zero inline suppressions; `uv run --project python`; `mise run lint` only; real-process tests with isolated `tmp_path` fixtures; every assertion fails if its change is reverted; no new dependencies.
- The Claude hook path must be provably unchanged: the retargeted tests are the proof. A test that "passes" because it no longer exercises the runner is a regression — each must still drive a real subprocess of the runner.
- Contract tokens (suites.toml:2155) are load-bearing; `mise run verify` must be green at your commit (`workflow.writer-lease` had `.codex/hooks.json` removed already — do not re-add it).
- Docs edits are minimal and truthful; do not rewrite unrelated paragraphs; keep the mermaid diagrams valid.
- Commit: one commit, conventional `refactor(agents): …` or `fix(agents): …` subject, body naming T1–T5 and citing #796 and #791.

## 5. Verification

- `uv run --project python pytest tests/test_writer_lease.py -x -q` (all pass; ~2–3 min).
- Full gate, rc read from files (never `| tail` a gate): `mise run lint`, `uv run --project python pytest tests/ -x -q` (known host fault: `tests/test_process_env.py::test_real_pre_push_poison_cannot_modify_outer_repository` fails on this host because `~/.local/bin/env` is not executable, #792 — report it, then re-run deselecting only it), `mise run verify`, `mise run lint-docs`.
- Control arms to report: (i) point one retargeted test's argv at a nonexistent runner path → it must FAIL (proves the subprocess is real); (ii) in the ambiguous-runtime test, remove the second complete runtime → the fail-closed assertion must FAIL and the control arm pass (the existing control).
- `mise run token-check -- <file> "<token>"` for every token you move or add.

## 6. Commit

lane.

## 7. PREMISES

- L1 `.codex/hooks.json` contained exactly two hook blocks (`PreToolUse`, `PostToolUse`, matcher `^(Bash|Edit|Write)$`), each an inline `/usr/bin/python3 -I -S -c '…'` ancestor-walk locator that execs `scripts/writer-lease-hook-runner.py`; nothing else — read in full this session before deletion
- L2 `.gitignore:54-55` = `.codex/*` then `!.codex/hooks.json` — read this session; the un-ignore line is removed by the architect's commit
- I1 `scripts/writer-lease-hook-runner.py`: `_deny(event, reason)` :25-36 emits `{"continue": false, "stopReason": …}` for Post events and the `hookSpecificOutput … permissionDecision: deny` JSON for Pre; `_runtime(cwd)` :83-123 walks `(current, *current.parents)` of `payload["cwd"]`, admits only candidates with `.git` + `scripts/writer-lease-hook-runner.py` + `python/src/dotfiles_setup/codex_writer_lease_hook.py` opened `O_NOFOLLOW` (`_open_root` :43-54, `_open_relative` :57-80), fails with `"hook cwd has ambiguous complete writer-hook runtimes"` (:121) or `"hook cwd has no complete tracked writer-hook runtime"` (:122); `main()` :173-194 turns `RunnerError`/`OSError`/JSON errors into `_deny(event, f"Writer lease enforcement failed closed: {exc}.")` and returns 0; `_invoke` :133-167 runs `python/.venv/bin/python` (or `~/.venvs/dotfiles-python/bin/python`) on `codex_writer_lease_hook.py` (:147) with `env={"LC_ALL": "C", "PATH": "/usr/bin:/bin"}`
- I2 `.claude/settings.json:40-50` (PreToolUse), `:82-92` (PostToolUse), `:94-104` (PostToolUseFailure): each `"command": "/usr/bin/python3 -I -S \"${CLAUDE_PROJECT_DIR:-.}/scripts/writer-lease-hook-runner.py\""`, matcher `Bash|Edit|Write|NotebookEdit`, timeout 10 — read this session
- I3 The seven tests read `json.loads((project_root / ".codex/hooks.json").read_text(...))["hooks"]["PreToolUse"][0]["hooks"][0]["command"]` at tests/test_writer_lease.py:1229, :1303, :1358, :1420, :1505, :1596, :1683 (grep this session) and run it as `subprocess.run(["/bin/sh", "-c", command], cwd=<nested>, input=<payload>, …)`; :1229's test also reads the PostToolUse command and asserts byte-equality (per the consumer map at docs/research/kb/reports/agents/explore-codex-hooks-consumers-20260827.md §1 — the implementer re-reads each test body; the map's per-test property table is the starting point, not the contract)
- P1 The two exit-2 tests (:1384-1459, :1627-1706) assert `returncode == 2`, `stdout == b""`, `stderr == b"Writer lease tracked runtime is ambiguous.\n"` / `…unavailable.\n"` — those literals come from the deleted inline locator (`.codex/hooks.json`, read this session: `message="Writer lease tracked runtime is ambiguous." if roots else "Writer lease tracked runtime is unavailable."`, `raise SystemExit(2)`); the runner's equivalents are the I1 messages, surfaced via `_deny` with exit 0. DATA match: same fixture directories (outer/inner runtimes, decoys, symlinks), same payloads — only the process under test and the failure shape differ
- P2 `python/verification/suites.toml:2132-2155` `workflow.writer-lease`: `per_path_tokens` for `docs/specs/codex-writer-lease.md` = `fcntl.flock(fd, LOCK_EX | LOCK_NB)`, `Pinned Python: exactly one complete runtime`, `Immutable content-addressed 64-event chunks`, `--dangerously-bypass-hook-trust`; for `docs/agents/codex-task-orchestration.md` = `G --> WL["Successor acquires Git-common-dir writer lease"]`, `` `PostToolUse` hooks drain those exact IDs ``; for `.agents/skills/codex-task-orchestration/SKILL.md` = `native writer-lease task`, `must acquire that Git-common-dir`, `` `PostToolUse` hook must drain ``; for `tests/test_writer_lease.py` the nine `def test_…(` names incl. the five in T3 — read this session (before the architect's commit removed the `.codex/hooks.json` entries)
- L3 Doc anchors, grep this session: spec :119 ("Both `.codex/hooks.json` and `.claude/settings.json` use the same tracked"), :129 ("Codex exposes no documented project-root variable…"), :160 (`--dangerously-bypass-hook-trust`), :168 (mermaid `ROOT["Pinned Python: exactly one complete runtime"]`), :255 (item 6), :256 (item 7), :260 (item 9), :273 (item 11); rule :85 ("Codex runs hook commands from the session working directory…"), :96 ("Codex and Claude `PreToolUse` register…"); agents doc :71 (token sentence), :74 ("Review changed Codex hook hashes with `/hooks`…"); SKILL.md :180 (token sentence), :184 ("native Codex hook hash is trusted…")
- I4 `python/src/dotfiles_setup/codex_writer_lease_hook.py` is executed by the runner for Claude (`scripts/writer-lease-hook-runner.py:147`) and required as a runtime component by `_runtime()` (:100-104); it carries contract tokens at suites.toml:2148/2155 — it stays
- E1 No new values are emitted by this change; the tests assert existing runner output shapes (I1); docs prose only
- A1 The five "retarget cleanly" tests (T1) prove their property through `_runtime()`'s walk exactly as through the inline locator — held on the consumer map's line-by-line comparison of the two implementations (`_open_root`/`_open_relative`/candidate completeness), which the implementer must confirm by the tests actually passing AND failing under the spec's control arm (i)
- A2 `_runtime()` walks `payload["cwd"]` where the locator walked `Path.cwd()`; every test sets both to the same directory (consumer map §1 note) — no test distinguishes them, so retargeting does not change coverage on that axis
