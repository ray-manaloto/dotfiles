# Compaction audit — measurements/numbers/code (sections 2, 3, 8) — 2026-09-04

Auditing summary at .../compaction-summary.md against raw transcript
077f170f-51b3-488e-a1b4-bea7a57fdb14.jsonl (2333 lines, 5.3MB).

Status: IN PROGRESS

## Verdicts table (partial, in progress)

| # | Verdict | Anchor | Claim | Probe |
|---|---|---|---|---|
| 1 | REFUTED (correct) | `.devcontainer/mise-system.toml:159` | gnupg pin "2.4.8-4ubuntu3.1" was "2.4.8-4ubuntu3" | `grep -n 'apt:gnupg'` -> line 159, current value matches; `git show 1610779` shows before/after exactly as summarized. Commit 1610779 = PR #969 fixing #962. |
| 2 | CONFIRMED-STALE (partial) | `platform_target.py:196-212` | Summary bundles `PUBLISHED_ARCHES = ("amd64","arm64")` under this single anchor along with `_RUNNER_LABELS`/`_VALIDATE_*`/`UBUNTU_26_04_ARM_RUNNER_BLOCKING` | `grep -n` shows `PUBLISHED_ARCHES` at **line 188** (outside 196-212), `_RUNNER_LABELS` at 196, `_VALIDATE_ARCH`/`_VALIDATE_RUNNER`/`_VALIDATE_TAG_SUFFIX` at 205-207, `UBUNTU_26_04_ARM_RUNNER_BLOCKING` at 212 — all correctly within range. Cross-checked against the ORIGINAL in-session assistant text (transcript line 1861, tool_use input) which quotes only `_RUNNER_LABELS`, `_VALIDATE_RUNNER`, `UBUNTU_26_04_ARM_RUNNER_BLOCKING` under `platform_target.py:196-212` — it never mentions `PUBLISHED_ARCHES` there. The compaction summary INTRODUCED the PUBLISHED_ARCHES line into that anchor's bundle; the transcript separately cites `PUBLISHED_ARCHES` at line **186** (grep of transcript: 4 hits for `platform_target.py:186`), which itself has drifted 2 lines from the file's CURRENT line 188 (file has grown since that citation, or citation predates a later edit). Net: the individual constant VALUES quoted are all correct in today's file, but the anchor as written misattributes PUBLISHED_ARCHES's line. |
| 3 | REFUTED (correct) | `.github/workflows/ci.yml` `probe-aslr-tsan` job | matrix/timeout/permissions/body/ci-gate wiring as summarized | Read `.github/workflows/ci.yml:378-462` directly: matrix `[ubuntu-26.04-arm, ubuntu-24.04-arm]`, `timeout-minutes: 20`, `permissions: contents: read`, `set +e` then `set -uo pipefail`, heredoc `/tmp/tsan.c` race, `run_tsan()` docker invocation with `--no-install-recommends clang`, `sudo sysctl -w vm.mmap_rnd_bits=28` (runs on HOST runner, not in container — summary doesn't contradict this), unconditional `exit 0`. `ci-gate: needs: [lint, contract-preflight, changes, build-publish, probe-aslr-tsan]` + `if: always()` confirmed verbatim at ci.yml:463-464. All match. |
| 4 | REFUTED (correct) | `python/verification/suites.toml:697`, `tests/test_workflow_hooks.py:105` | ci-gate-aggregator-exists token; EXPECTED_JOBS frozenset entry | Both anchors verified byte-exact: suites.toml:697 is literally the `per_path_tokens` string containing `needs: [..., probe-aslr-tsan]`; test_workflow_hooks.py:105 is literally `(".github/workflows/ci.yml", "probe-aslr-tsan"),`. |
| 5 | REFUTED (correct) | `home/dot_config/mise/config.toml.tmpl:42`, "28 latest pins" | zellij = "latest" at line 42; 28 total `= "latest"` pins in file | `grep -n zellij` -> line 42 exact match. `grep -c '= "latest"'` -> 28, matches exactly. |

(continuing: items 6-9 below)

| 6 | REFUTED (correct) | Section 8 result table | ubuntu-24.04-arm kernel 6.17.0-1022-azure rc 1/1; ubuntu-26.04-arm kernel 7.0.0-1012-azure rc 1/1; mmap_rnd_bits=unknown both | Raw CI log tool_result (transcript line 2199): `runner=ubuntu-24.04-arm kernel=6.17.0-1022-azure mmap_rnd_bits=unknown / as_is_rc=1 / lowered_rc=1` and `runner=ubuntu-26.04-arm kernel=7.0.0-1012-azure mmap_rnd_bits=unknown / as_is_rc=1 / lowered_rc=1`. Exact match, both legs, both kernels, both rc pairs. |
| 7 | REFUTED (correct) | "`apt:` appears 0x in all four lockfiles while `pkl` appears 26x in mise-system.lock" | Re-derived independently: found the tracked mise lockfiles (`mise.lock`, `.config/mise/mise.lock`, `.devcontainer/mise-runtime.lock`, `.devcontainer/mise-system.lock`). `grep -c 'apt:' <file>` = 0 in ALL FOUR. `grep -c 'pkl' .devcontainer/mise-system.lock` = 26 exactly. Control arm: `grep -c 'version' .devcontainer/mise-system.lock` = 51 (nonzero, proves the file/grep aren't silently broken); fresh nonsense string `zzqqxx7788` -> 0 hits, rc=1 (proves grep discriminates absence correctly). |
| 8 | REFUTED (correct, all spot-checked) | Issue/PR numbers #962/#969/#973/#975/#961/#974/#971/#972/#840/#849/#13505/#14062/#394 | `gh issue view`/`gh pr view` on this repo + `-R actions/runner-images` for the two external ones. All titles match their claimed context: #962 (gnupg pin red), #969 (MERGED, the fix), #973 (MERGED, three-image audit docs — matches "PR #973 merged with a red probe" claim), #975 (MERGED — see note below), #961 (still OPEN, "re-resolve latest pins with --bump" — matches claim it "needs main merged"), #974 (OPEN, SHELVED macOS ticket), #971/#972 (OPEN, the two filed follow-ups), #840 (CLOSED, "non-blocking arm64/ubuntu-26.04-arm validation leg" — matches #840 citation for `UBUNTU_26_04_ARM_RUNNER_BLOCKING`), #849 (CLOSED, "three devcontainer images" — matches "now superseded" claim). External: **#13505** title "Support the hypervisor.framework on Apple-silicon" matches summary's "Hypervisor.framework unavailable ... actions/runner-images#13505" exactly. **#14062** title "Please support KVM on ARM runners" matches the summary's verbatim quote exactly, confirming the "wrong subsystem" correction is itself correctly cited. #394 CLOSED, title about `suites.toml` per_path_tokens identifier-boundary audit — matches "Contract tokens ... #394" context. **Note (not an error, a time-drift observation):** summary section 8 states "PR #975 state: OPEN, BLOCKED, ... auto-merge armed" — this was accurate as of the session's last CI check (~09:56Z 2026-09-04); `gh pr view 975` now reports **MERGED**, consistent with auto-merge having landed it after the session ended. This is expected drift, not a summary defect. |
| 9 | one confirmed drop (outside sections 2/3/8, noted for completeness) | — | zizmor run measured during the pipe-guard-deny incident (section 4 item 7) | Raw transcript: `5 findings (2 suppressed): 3 informational, 0 low, 0 medium, 0 high\nziz_rc=11`. This specific number is NOT reproduced anywhere in the summary (section 4's prose only says a pipe-guard deny cancelled the compound command, no counts). It belongs to section 4 (Errors and Fixes), which is outside this audit's assigned slice (2/3/8) — flagging so the section-4 auditor can pick it up. |

## Additional cross-checks performed

- `.github/actionlint.yaml` self-hosted-runner labels: confirmed exactly `ubuntu-26.04`, `ubuntu-26.04-arm`, `xcode-27` (file read directly, matches summary section 3 bullet verbatim).
- All 21 committed research-artifact filenames listed in summary section 3 (`docs/research/kb/reports/agents/2026-09-03-three-image-audit-*`, `2026-09-03-handoff-staleness-critic{-brief,}`, `2026-09-04-*`) verified present on disk with exact matching names via `ls`.
- The linker-error quote in section 8 (`/usr/bin/aarch64-linux-gnu-ld.bfd: cannot find /usr/lib/llvm-21/lib/clang/21/lib/aarch64-unknown-linux-gnu/libclang_rt.tsan.a: No such file or directory` + `clang: error: linker command failed with exit code 1`) verified verbatim against the raw CI log tool_result in the transcript.

## Summary of findings

- **7 of 9 assigned checks: fully REFUTED (summary is correct)** — items 1, 3, 4, 5, 6, 7, 8.
- **1 finding: CONFIRMED-STALE (minor)** — item 2, the `platform_target.py:196-212` anchor. The individual constant *values* quoted are all correct in the file today, but the summary bundles `PUBLISHED_ARCHES = ("amd64", "arm64")` under that single line-range anchor even though `PUBLISHED_ARCHES` lives at line 188 (outside 196-212) — and the original in-session assistant message never attributed `PUBLISHED_ARCHES` to that anchor at all (it was cited separately, at line 186, itself now 2 lines short of the current 188). This is a compaction-introduced misattribution, low-impact because all cited values remain factually true, but the anchor as written would mislead someone jumping straight to lines 196-212 expecting to find `PUBLISHED_ARCHES` there.
- **1 finding: dropped detail outside the assigned slice** — item 9, the zizmor finding counts (section 4, not 2/3/8) — noted for the section-4 auditor.
- No fabricated numbers, no wrong issue/PR attributions, no incorrect kernel/rc values, no incorrect job/wiring description found anywhere in sections 2, 3, or 8.

## GitHub repos touched

- [ray-manaloto/dotfiles](https://github.com/ray-manaloto/dotfiles) — verified all in-repo issue/PR numbers, file/line anchors, and lockfile contents.
- [actions/runner-images](https://github.com/actions/runner-images) — verified #13505 and #14062 titles to confirm the "right verdict, wrong subsystem, correct citation is #13505" claim in section 2.
