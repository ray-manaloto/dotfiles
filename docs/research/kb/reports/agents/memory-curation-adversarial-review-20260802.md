# Cold adversarial review — MEMORY.md curation, 2026-08-02

Persisted verbatim per `.claude/rules/agent-report-persistence.md`. Two independent cold
lenses over a 76-entry removal set, neither told the rationale. Produced by the
`memory-index-curation` workflow (11 agents, 0 errors). The removal set was amended in full
response to these objections — see `docs/handoffs/` / the session handoff for the disposition.

**Objections: 16** — 1 critical, 8 major, 7 minor.

## Objections

### [major] project_session_2026-07-31-b.md

Line 43-45 is the ONLY record anywhere that `codex adversarial-review --wait` DIES: "The foreground call returns its streamed log early and the child is killed; job record vanishes, zero findings. Always launch it with Claude Code's background Bash." Control-armed grep: `adversarial-review` returns 0 hits in `.claude/**` and `docs/rules-evidence/**`; the only other hits are the vendored plugin README (`docs/research/kb/raw/codex-plugin-cc-README.md:89,101,119`) which documents `--wait` as a SUPPORTED flag and says nothing about it dying. Adversarial review is standing practice, not history — `docs/issue-tracker.md:80-82` makes "the adversarial review result" a required receipt field for every `wayfinder:*` ticket, and the index keeps `project_session_2026-08-01-b` (87 findings) as a live entry. Un-indexing this leaves the vendored README's `--wait` as the only findable guidance, and the failure is silent (rc looks fine, zero findings returned).

### [major] project_session_2026-07-31-b.md

Line 40-42 is the only record that `issue_dependencies_summary` is EVENTUALLY CONSISTENT — "read `/dependencies/blocked_by` directly after a write; the summary gives a false negative." `docs/issue-tracker.md:72` and `:77` build the /wayfinder **frontier query** on exactly that field ("drop any with an open blocker (`issue_dependencies_summary.blocked_by > 0`)") with no staleness caveat. This is the dangerous shape: un-indexing the correction leaves the naive usage as the only findable claim, and the failure mode is a blocked ticket presenting as the next ready ticket right after the blocking edge was written. Map #431 is still open (`project_session_2026-08-01-c`, kept), so the loop is live.

### [major] feedback_mise_strict_fake_env.md

The load-bearing correction — "a `MISE_*` var absent from upstream mise is NOT necessarily fake; check the project's own Pydantic `BaseSettings(env_prefix="MISE_")` first" — exists nowhere else, and the trap it guards is STILL LIVE: `python/src/dotfiles_setup/config.py:19` is `SettingsConfigDict(env_prefix="MISE_")` (and `:28` `env_prefix="DOTFILES_"`), so any `MISE_*`/`DOTFILES_*` string is still project-definable. Worse, the wrong claim outlives the correction in tracked files: `docs/ultrapowers/specs/2026-03-29-devcontainer-host-user-migration-design.md:246` still shows `"MISE_STRICT": "1"` in a devcontainer.json block and `docs/research/trail/findings/gap-analysis-discovery-2026-03-28.yaml:206` still lists "MISE_STRICT=1 enforcement", while the record that the whole system was deleted 2026-05-01 (config.py field, audit.py, main.py validator, two tests, the `arch.mise-strict-in-container` contract) lives only in this memory. It also carries an unclosed follow-up: "full-repo audit for fake/unused env vars — not yet investigated."

### [minor] reference_mcp_access.md

Records an unresolved, actively harmful state plus its fix and its decision status: six `mde-mcp-*` wrappers still registered in `~/.claude.json` and failing 4 months after being reported removed; `filesystem` and `context7` are user-global entries that SHADOW this repo's `.mcp.json` server and the working `plugin:context7:context7`; removal is `claude mcp remove <name> -s user`; "Pending Ray's go-ahead as of 2026-07-30". Mitigated (hence minor, not major): `python/src/dotfiles_setup/doctor.py:900-917` re-measures exactly this on every SessionStart and `.claude/CLAUDE.md:69` records the shadowing mechanism — so the symptom resurfaces automatically. What is genuinely lost is the pending-decision state and the removal command.

### [minor] project_session_2026-07-27-d.md

Holds an unactioned security item recorded nowhere else: the 2026-07-27 relay that a knowledge-base session found live cleartext credentials (AWS access key + secret, API tokens, app passwords) inside `__MISE_DIFF`, with values reaching that session's transcript — "Relayed to Ray 2026-07-27; **not actioned** (rotation is Ray's call)." `.claude/rules/secrets-out-of-the-shell-env.md:164` documents a DIFFERENT, later rotation (four `env = true` opt-in values printed by a probe on 2026-08-02), so nothing in the repo records whether the July 27 set was ever rotated. Severity held to minor only because the index hook for this entry never advertised the fact, so un-indexing changes little findability that existed.

### [minor] feedback_adversarial_review_convention.md

Two durable traps live only here: (1) reviewer CLIs honour `.gitignore`/`.git/info/exclude`, so a plan staged in `.agent/plans/**` is INVISIBLE to the reviewer and must be copied to `/tmp` first — `.claude/skills/handoff/SKILL.md:14` notes `.agent/plans/` is gitignored but never draws the reviewer-blindness consequence; (2) a reviewer rubber-stamps unless the prompt MANDATES source introspection with file:line citations and an explicit "verified via source vs verified via plan reasoning alone" receipt — the kept `feedback_codex_review_protocol.md` covers only the opposite failure (Codex reading too much disk). Minor because the specific invocation it documents (`codex-companion.mjs` at plugin 1.0.3, Gemini) is superseded by the `codex@openai-codex` and `antigravity` plugins in `.claude/settings.json:99,102`.

### [minor] feedback_jasonmorganson_pattern.md

`grep -rl jasonmorganson` over the whole repo returns ZERO hits, so this is the only surviving record of why `.devcontainer/Dockerfile` + `.devcontainer/Dockerfile.host-user` are split the way they are: the user-agnostic `/opt/home` staging it replaced "caused rustup HOME mismatch errors", and the published image must stay tools-only with user renaming confined to the overlay. `.claude/agents/dockerfile-reviewer.md` reviews the Dockerfile without carrying this rationale. Minor: the shape is embodied in the two live files, so the risk is only that a future refactor re-derives the HOME/euid class the hard way.

### [minor] feedback_mise_run_masks_digits.md

`.claude/rules/secrets-out-of-the-shell-env.md` records the CAUSE (two one-character all-digit fnox telemetry flags marked redacted) and that "It returns if `mde-py` re-bootstraps the config" — but not the operational recipe this memory carries: a `mise run` log is trustworthy for pass/fail and NEVER for a figure (`16 deny, 18 allow` printed as `[redacted]6 deny, [redacted]8 allow`; `~102105 bytes` as `~[redacted]02[redacted]05`), so re-run under `uv run` to read any number. The recurrence trigger is `mde-secret-add`, a documented happy path Ray runs by hand, and this repo does a lot of measurement work whose figures come out of `mise run eval` / `mise run lint` logs.

### [minor] project_session_2026-07-24-g.md

Records a NEXT item that was never done and is tracked nowhere else: transcript-audit is cwd-scoped (`_project_transcripts(base, cwd)`), so the knowledge-base SessionEnd hook is blind to dotfiles orchestration sessions, and the fix was "NEXT: dotfiles SessionEnd hook invoking `kb-setup brain transcript-audit`". `.claude/settings.json:73-83` still wires only `command-audit` on SessionEnd. Minor: a six-day-old stale intention with no issue behind it, and nothing since has referenced the brain seam.

### [critical] REMOVED project_session_2026-07-24-l.md (MEMORY.md:36) — "a control arm that returns SKIP is NOT armed; check `precondition` first"

This is a uniquely-held fact governing a live gate, and nothing that survives carries it. Probe: `grep -rn "precondition" .claude/rules/` → **0 hits**; control arm, same command shape: `grep -rln "control arm" .claude/rules/` → 4 files (probes-need-a-control-arm.md, secrets-out-of-the-shell-env.md, md-size-budgets.md, ai-cli-invocation.md), so the grep discriminates and the zero is real. The gate it governs is live: `mise.toml:923` `[tasks.eval]`, and `tests/test_eval_cases.py:165` is literally `test_the_precondition_skips_when_graphify_is_absent()` — i.e. the harness can and does emit SKIP. `.claude/rules/probes-need-a-control-arm.md` enumerates seven arming failure modes (bounds, token spelling, own-parser, display bounds, inherited numbers, cross-check, rigged fixture) and not this one; the nearest tracked text, `docs/rules-evidence/probes-need-a-control-arm.md:35`, is about a SKIPPED **CI job**, a different mechanism. Four probe-discipline memories survive (MEMORY.md:73,74,91,92) while the one covering the repo's own eval runner is cut — the surviving set therefore looks armed on this theme and is not.

### [major] KEPT feedback_omc_phantom_teammatemode.md / _launch_required / _plugin_dist_build / _hud_shim_discovery / _autoresearch_tmux (MEMORY.md:159-163) vs REMOVED user_omc_preferences.md (MEMORY.md:6) and feedback_gh_cli_watch_flag.md (MEMORY.md:109)

Five entries about `oh-my-claudecode` survive a byte-constrained trim for a plugin that **is not enabled and is not even listed**: `.claude/settings.json:85-107` enumerates 21 plugins and OMC is absent, and two eager rules say so outright — `.claude/rules/agent-artifact-conventions.md:68` ("the disabled `oh-my-claudecode` plugin ... absent from every session") and `.claude/rules/notepad-enforcement.md:11`. That is 5 index lines / 12,704 B of linked files that can never be actionable. Simultaneously the trim removes `user_omc_preferences.md` (1,981 B) — the only entry that explains why those five exist — and `feedback_gh_cli_watch_flag.md`, which `.claude/rules/gh-cli-watch.md` names in its own See-also and which backs `do-not.md` #6. The keep/cut decision is inverted on applicability: the survivors are dead, the casualties govern live workflow. (The gh removals are defensible on redundancy grounds alone; the objection is that the OMC five were not held to the same test.)

### [major] THEME WIPED — the #354 verification-contracts / suites.toml program: project_session_2026-07-24-i/-k/-l, 2026-07-25, 2026-07-25-b, 2026-07-29-c, 2026-07-30 all removed (MEMORY.md:33,35,36,37,38,53,54)

After these removals **no index entry mentions suites.toml, verification contracts, tiers, or the eval runner**, yet `mise run verify` is a hard gate named three times in AGENTS.md (Quick Start, Testing, "Validate before committing") and `.claude/rules/verify-before-advancing.md` treats contract-preflight as always-required. The nearest survivor, `feedback_forbid_tokens_substring_fragile.md` (MEMORY.md:96), teaches token anchoring but never names the harness, so it is not a route in. Facts lost with no eager or tracked home: "a single-direction table is a hard FAIL" (07-25), "tier-2 fixtures found 2 live false positives" (07-25), "**A contract is not a test**" (07-30), "a `--dry-run` is not a control arm for a WRITE" (07-29-c). A future session that hits a red contract has an index that leads it to nothing about the harness that produced it.

### [major] KEPT project_session_2026-07-27-b.md (MEMORY.md:42) vs REMOVED project_session_2026-07-31-b.md (MEMORY.md:62)

Plainly inverted value. 07-27-b is `tmux -e PATH` is DISCARDED / `defaultMode:auto` IGNORED — artifacts of the autonomous-queue experiment, which the index itself brackets as a runbook (MEMORY.md:23) and whose companion session 07-27-c ("mirroring a task is not evidence it runs") is being cut anyway, leaving a survivor with no thread. 07-31-b carries three facts on live surfaces: it is where **#448 (chezmoi vs mise) was opened** — an open decision the kept 08-01-c (MEMORY.md:68) presumes without stating its origin; `chezmoi [hooks].pre` aborts and sees `--source`; and **`--wait` codex review dies**, an operational trap on the codex implementer lane that `.claude/CLAUDE.md` declares active. If exactly one of the two must go, it is 07-27-b.

### [major] REMOVED feedback_adversarial_review_convention.md (MEMORY.md:155, 4,742 B)

The convention is being unindexed while adversarial review is the repo's dominant recent activity and while its *results* are kept. `grep -rln "adversarial" .claude/rules/ .claude/skills/` returns only `agent-report-persistence.md`, which lists adversarial passes as an artifact class and says nothing about how to construct one — so the mechanics (standalone plan file + code-introspection prompt, to stop the reviewer reading the implementation first) live only in this file. The kept `feedback_refuters_and_cold_review_find_disjoint_defects.md` (MEMORY.md:74) says run BOTH lenses but not how to build either; the kept `project_session_2026-08-01-b.md` (MEMORY.md:67) cites "87 findings/1 refuted ⇒ receipts under-defended" — a ratio whose methodology is exactly what is being cut, and whose companion 07-31-d ("42 findings, 0 refuted") goes too. This very review is an instance of the convention being removed.

### [minor] KEPT project_session_2026-07-27.md (MEMORY.md:41) — "RRF **costs** a pair" — with its whole ladder removed (07-25-b, 07-25-c, 07-26 at MEMORY.md:38,39,40)

The surviving line is the terminal NEGATIVE result of a five-session measurement arc whose baseline, metric and progression are all being unindexed. A future session reads "costs a pair" with no way to know what 8 pairs are, that the ladder ran 1/8 → 3/8 → 5/8, or that the drop rule is by `_origin` and not `file_type`. Mitigated, not eliminated: the ladder is tracked at `knowledge-base/docs/research/reports/peer-tool-synthesis.md:294` and `code-review-graph-retrieval-gap.md:194`, and `_origin=ast` at `knowledge-base/CLAUDE.md:169` — so the facts survive a clone, only the pointer dies. Cheapest fix is not restoring an entry but re-captioning MEMORY.md:41 with the baseline and a path to those two reports.

### [major] SYSTEMIC BIAS — the cut axis is "already carried by an eager rule", applied to principles but not to mechanics

The axis is not age (07-31-b/c/d are cut, 07-24-c is kept), and where it was applied it is correct: I verified coverage for feedback_probe_fixture_can_be_rigged (rule §8 "Arm the FIXTURE too", landed as 919af74), feedback_apt_snapshot_removed (`.claude/agents/dockerfile-reviewer.md:12`, `.devcontainer/mise-system.toml:116`), reference_devcontainer_docs (`research-doc-sources.md` "containers.dev/llms.txt → 404"), reference_codex_invocation (`.claude/rules/ai-cli-invocation.md`), and 07-31-c's squash-reuse DIRTY-PR trap (`.claude/skills/pr-workflow/SKILL.md:130`). All good cuts. But the same test was never run on the mechanics: precondition/SKIP (0 hits), the adversarial convention (0 hits), "`zsh -l -c` is NON-interactive" from 07-27-h, "`pushed_at` lies" from 07-29, and the `rm` recovery half of feedback_hk_stash_phantom_deleted (clean-git-state.md tells you to stage deletions, never that they resurrect untracked) all leave with no home. Net effect: the index keeps duplicated principles' *neighbours* and drops un-derivable mechanics — the inverse of what an index whose principles are already eagerly loaded should do.

## Reviewer overall verdicts

### Lens 1

Mostly defensible — but not entirely, and one file should be pulled out of the set. I ran the repo's own gate first (`mise run memory-index`, rc=0: no index-only distinctive fact, bytes 24,164/25,000 = 97% and binding), then coverage-grepped each candidate's load-bearing claim against `.claude/rules/**`, `.claude/skills/**`, `docs/**`, `hk.pkl` and `python/**`. The large majority of the 76 are safely superseded: the policy `feedback_*` entries (zero-skip, verify-before-advancing, never-suppress, local-validation, research-before-fixing, notepad, use-tool-builtins, release-notes-native-first) each have a full `.claude/rules/*.md` ADR; the session memories from 07-16-b through 07-31-d record epics that shipped and whose lessons were promoted into rules, `suites.toml` contracts and `docs/rules-evidence/`; and several are actively stale or wrong, so un-indexing is a positive good — `project_dotfiles_overview` still names `ghcr.io/sortakool/...`, `install.sh` and 65 tests, `project_two_build_types` names `ghcr.io/ray-manaloto/cpp-devcontainer`, every OMC/octo/GitButler entry describes plugins absent from `.claude/settings.json:85-106`, `home/.chezmoiscripts/` no longer exists, and `feedback_hk_stash_phantom_deleted` is self-labelled OBSOLETE since hk 1.46. I specifically verified and cleared five that looked risky: renovate validator semantics (fully in `hk.pkl:402-421`), the devcontainer-only mise overlay (`home/.chezmoiignore:42-53`), apt snapshot rejection (`.devcontainer/mise-system.toml:116`, `.claude/agents/dockerfile-reviewer.md:12`), minimal resume prompts (`.claude/skills/clear-prep/SKILL.md:213`), and the whole eager-context measurement incl. the `InstructionsLoaded` blindness and the ≈157 KB figure (`docs/rules-evidence/agent-artifact-conventions.md:44-65`). The real problem is `project_session_2026-07-31-b.md`, which carries two live operational gotchas that exist nowhere else and that bear on workflows running right now (the codex adversarial-review lane and the /wayfinder frontier query) — I would keep that one entry, or migrate those two paragraphs into `.claude/rules/ai-cli-invocation.md` and `docs/issue-tracker.md` in the same change. `feedback_mise_strict_fake_env` is the second-order case the curation skill warns about: un-indexing the correction leaves two tracked files still asserting the thing it corrects. Everything else I would let go, with the caveat that this is an un-index, not a delete — the files survive on disk and `memory-index` lists them as Unindexed, so the cost is findability, not the facts.

### Lens 2

Broadly defensible in shape, wrong in several specific calls. The organising principle is sound and I verified it holds for a majority of the cuts: zero-skip, verify-before-advancing, use-tool-builtins, research-release-notes, probe-fixture-can-be-rigged, apt-snapshot, devcontainer-docs, codex-invocation, gh-watch and 07-31-c's DIRTY-PR trap are each demonstrably carried by an eager `.claude/rules/*.md`, a skill, or a tracked doc — dropping them from the index costs nothing, especially since unindexing leaves the files on disk. The failure is that the coverage test was applied to the *principles* and not to the *mechanics*: at least four cut entries have zero hits anywhere in `.claude/rules/`, one of them (07-24-l's \"a control arm that returns SKIP is NOT armed\") governing a live gate (`mise.toml:923`, `tests/test_eval_cases.py:165`) on the single theme this index has invested in most. The survivorship read is worse than the removal read: five OMC entries for a plugin absent from `enabledPlugins` survive a byte-constrained trim while entries backing live workflow are cut, and one whole theme — the #354 suites.toml contracts program, five sessions deep, backing a gate AGENTS.md names three times — leaves no index entry at all. Recommendation: keep the cut list as-is except restore four lines (07-24-l, 07-31-b, feedback_adversarial_review_convention, and one #354 entry), pay for them by cutting the five OMC lines, and re-caption MEMORY.md:41 so the surviving negative result is not an orphan.

## Verification-phase findings (reference-integrity + fact-provenance)

- **BLOCKED — feedback_gh_run_watch.md** → downgrade to `keep`
  - ORPHANED CLAIM. Hook: "`--exit-status`; cross-verify the conclusion". `--exit-status` IS in the file (3 hits). "cross-verify the conclusion" is NOT: `grep -cin conclusion feedback_gh_run_watch.md` -> 0, against the control `grep -cin conclusion feedback_gh_cli_watch_flag.md` -> 1 (same command shape, same corpus), so the probe discriminates. The file's only `--json` line is `gh run list ... --json databaseId`, i.e. run discovery, not conclusion cross-verification. The file in fact asserts the OPPOSITE reliability: "returns a non-zero exit code on failure so bash can branch on it" — with no caveat at all. The caveat is attributed TO this file by its sibling (feedback_gh_cli_watch_flag.md line 119: "**Caveat**: per `feedback_gh_run_watch.md`, `--exit-status` has been unreliable at run boundaries — always cross-verify with `gh run view <id> --json conclusion`") — a citation to text that was never written there. MITIGATION (why this is low-severity): the fact survives outside memory, in the tracked eager rule `.claude/rules/gh-cli-watch.md` ("⚠️ `gh run watch --exit-status` has reported **0 prematurely** — always cross-verify with `gh run view <id> --json conclusion`") and in `do-not.md` #6. RECOMMENDATION: migrate one sentence into feedback_gh_run_watch.md (it currently over-claims reliability, which is worse than the missing hook), then unindex freely.

## GitHub repos touched

- [ray-manaloto/dotfiles](https://github.com/ray-manaloto/dotfiles) — `.claude/rules/**`, `.claude/settings.json`, `hk.pkl`, `mise.toml`, `python/**`, `tests/**`, `docs/**`, and the auto-memory corpus at `~/.claude/projects/…/memory/`.
- [ray-manaloto/knowledge-base](https://github.com/ray-manaloto/knowledge-base) — cited for the graph-retrieval ladder reports and `kb_setup` module paths.
