# PREMISE VERIFICATION — spec-800-r3.md

Repo `/Users/rmanaloto/dev/github/ray-manaloto/dotfiles`, branch `fix/800-sync-id-labels`, HEAD `6309e53`, tree clean. Every row read fresh from the working tree.

## Part 7 rows

| # | Row | Verdict | Evidence |
|---|---|---|---|
| 1 | I `container_image_id` fallback — sync.py:451-453 | **CONFIRMED (line numbers CORRECTED)** | The code is exactly as described but at **sync.py:457-459**, not 451-453: `457: cid = _run(["docker","ps","-q",*filters]).stdout.strip()` / `458: if not cid:` / `459: cid = _run(["docker","ps","-aq",*filters]).stdout.strip()`. H1 says "deleting the fallback lines (sync.py:452-453)" — the lines to delete for the mutation are **458-459**. |
| 2 | I `local_platforms(image_ref) -> frozenset[str]` — sync.py:552-580 | **CONFIRMED** | `def local_platforms` at sync.py:552; loop over `published_targets()` at :565; `_run(["docker","image","inspect","--platform",target.platform,image_ref,"--format","{{.Id}}"])` :566-577; `if res.returncode == 0: present.add(target.platform)` :578-579; `return frozenset(present)` :580. |
| 3 | I `refresh_local_tag` union — sync.py:608-611 | **CONFIRMED** | `608: wanted = {resolve_platform()} | local_platforms(image_ref)`; `609-611: platforms = ",".join(target.platform for target in published_targets() if target.platform in wanted)`. `def refresh_local_tag` at :583. |
| 4 | I check mode — sync.py:783-793; `decide_action` at :795 | **CONFIRMED, exact** | `783: if opts.check_only:`; `787-789: if status.registry_digest is None: sys.stdout.write("check: UNKNOWN — registry unreachable\n"); return 2`; `790-792: sys.stdout.write(f"check: {'STALE — sync would rebuild' if status.stale else 'current'}\n")`; `793: return 1 if status.stale else 0`; `795: action = decide_action(status, force=opts.force)`. |
| 5 | I `DevContainerManager.down()` — docker.py:263-287, lookup at :279 | **CONFIRMED (span CORRECTED)** | Lookup **is** at docker.py:279 (`container_ids = teardown_container_ids(resolve_names(workspace=abs_root))`) exactly as stated. The method span is **264-288** (`def down` at :264, `def test` at :290), not 263-287. Empty→`logger.info("No active or exited devcontainers found for this project.")` :282; `docker stop` / `docker rm -f` per id via `subprocess.run(..., check=False)` :287-288. |
| 6 | I `_docker_ps_ids` — devcontainer_names.py:561-568, `check=True` | **CONFIRMED, exact** | `561: def _docker_ps_ids(*filters: str) -> list[str]:` … `566: check=True,` … `568: return [...]`. (The `DOCKER_HOST` probe itself is report-sourced, not re-run here; `check=True` on a `docker ps` is sufficient for the raise.) |
| 7 | L existing tests | **CONFIRMED, all six line numbers exact** | test_sync.py:220 `test_stopped_container_with_stale_overlay_is_rebuilt_not_reused`, :234 `test_stopped_container_matching_the_record_is_reused_via_up`, :323 `test_container_state_filters_on_both_id_labels_not_local_folder`, :504/:528/:547 the three `test_refresh_local_tag_*`. test_docker.py:154 `test_down_stops_and_removes_ids_from_teardown_container_ids`, :189 `test_down_with_no_containers_issues_nothing` — both stub `docker.teardown_container_ids` **and** `docker.resolve_names` and `docker.shutil.which`. |
| 8 | L prune comment — mise.toml:1002-1006 | **CONFIRMED, exact** | :1002-1006; the "happens to carry both" sentence spans :1005-1006 (`# all). sort -u de-dupes a container that happens to carry both, which` / `` # `docker rm -f` would otherwise choke on mid-prune under set -e. ``). |
| 9 | L `no_platform_literals` scans tracked `.py` prose too | **REFUTED for `tests/**` — CONFIRMED only for `python/src/**`** | `platform_target.py:133-138`: `_SCAN_EXCLUDED_PREFIXES = ("docs/", "missions/", "plugins/", "tests/")`, applied by `_should_scan` at :439. **Control arm:** `tests/test_sync.py:22` and `:25` already contain the literal triples `"linux/amd64/v2"` and `"linux/arm64/v8"`, and the 21 literal-gate tests pass on this tree (`pytest -k literal` → `21 passed`, rc=0). The codex-r2 docstring failure was in `python/src/` prose, which **is** scanned (`_SCANNED_SUFFIXES` includes `.py`, only `platform_target.py` itself is path-exempt). |
| 10 | E stdout: new check-mode line + rc 1; consumers mise.toml:850, `pr.py:882` unaffected | **CONFIRMED with a scope gap (see M-1)** | `pr.py:882: rc = sync_main(workspace, SyncOptions(full=surface))` — no `check_only`, unaffected ✓. Repo-wide grep for a `--check` sync consumer finds **no** script, workflow or hook — only prose: mise.toml:850, `.claude/skills/devcontainer-sync/SKILL.md:25` and `:59`, `main.py:780` (argparse help), plus two cross-references (`renovate_dryrun.py:337`, `main.py:1351`) that describe sync's rc convention. `main.py:1657` passes `check_only=args.check`. So nothing breaks mechanically; three prose sites become inaccurate. |
| 11 | E log: `logger.error("could not list devcontainers: %s", exc)` | **CONFIRMED feasible** | `logger` exists in docker.py (used at :275/:282/:286) and `import subprocess` is at **docker.py:19**, so `except subprocess.CalledProcessError` needs no new import. (Answers the caller's question (4): **no new import required**.) |
| 12 | E exception: `ValueError` from `refresh_local_tag` | **CONFIRMED as to non-secrecy; propagation UNSTATED (see M-2)** | `resolve_platform()` returns an env value or the host triple — non-secret ✓. |

## Answers to the four named questions

1. **Does `--check` returning 1 for an outdated overlay break a caller?** No mechanical caller exists. `sync_main(..., check_only=True)` has exactly one production call site (`main.py:1652-1660`, `sys.exit(...)`); `pr.py:882` never sets `check_only`. Grep across `.yml/.yaml/.toml/.sh/.py/.md` (excluding `docs/research`) finds no script or CI step invoking `sync -- --check`. **Doc drift only** — see M-1.
2. **Would the existing `test_sync_check_mode_*` tests flip?** **No.** `_status()` (test_sync.py:55-73) leaves `container_image_id` at its dataclass default `None`, and `SyncStatus.container_current` (sync.py:159-201) returns `True` when `container_image_id is None` (:190-191). So `test_sync_check_mode_current_rc0` (:618, `_status()`) stays rc 0 / `current`, and `test_sync_check_mode_reports_without_converging` (:603, `_status(local=_DIGEST_OLD)`) stays rc 1 via the STALE arm. Neither needs editing; the spec's "update the check-mode tests" is optional, the new OUTDATED test is the only required addition (it must pass `container_image_id=` explicitly, since `_status()` cannot currently express it — **`_status` has no `container_image_id` parameter**, so the new test must either extend the helper or build `sync.SyncStatus(...)` directly; see M-3).
3. **Other callers of `refresh_local_tag` affected by a `ValueError`?** One production call site: **sync.py:717**, inside `_converge`'s `rebuild` branch (`if status.stale and not refresh_local_tag(status.image_ref):`). Nothing between there and the process boundary catches it — `_converge` → `sync_main` → `main.py:1651 sys.exit(sync_main(...))` and, in the land path, `pr.py:882`. See M-2. Test call sites (test_sync.py:522/542/561) stub `local_platforms` and always include `resolve_platform()` in the union, so they are unaffected.
4. **Does the `CalledProcessError` catch need `subprocess` imported in docker.py?** Already imported at **docker.py:19**. No change needed.

## MISSING premises

- **M-1 (medium) — three prose sites document the old `--check` rc contract and are outside the spec's file list.** `mise.toml:850` "(rc 1 if stale)", `.claude/skills/devcontainer-sync/SKILL.md:25` (same text) and `:59` (`| rc 1 from --check | Stale — a real sync would rebuild |`), and the argparse help at `python/src/dotfiles_setup/main.py:780` `"Dry-run: report staleness only (rc 1 if stale), change nothing"`. Spec §2 permits only `mise.toml:1002-1006`. Either widen §2 to these four lines or state that the drift is accepted — note the skill is bound by contract `workflow.sync-wiring` (suites.toml:1076-1106) so it is a maintained surface, and `.claude/skills/**` edits pull in the `mise run lint-docs` row of the check matrix.
- **M-2 (medium) — L1's `ValueError` is an UNCAUGHT traceback, not a diagnosed failure.** `_converge` returns `(ok, detail)` for every other failure mode; a raise from :717 escapes `sync_main` entirely, so `mise run sync` and `mise run land` (`pr.py:882`) print a Python traceback rather than a `FAIL  converge:` line. That may be intended ("loud"), but the spec's E row says only "non-secret", not "propagates as an unhandled exception through the land path". Decide explicitly: raise-and-traceback, or catch in `_converge` and return `(False, str(exc))`.
- **M-3 (low, mechanical) — the `_status()` test helper cannot express the new OUTDATED case.** `_status` (test_sync.py:55-73) has no `container_image_id` parameter, and `container_current` needs `container_image_id` non-None **plus** a `synced_state` record whose `containers`/`legacy_container_image_id` disagrees (sync.py:189-201) — a record lacking this arch with a non-None legacy id, or a mismatching per-arch id. "record lacking this arch" alone is not enough: `recorded is None` after the legacy fallback returns **False** (:199-200) which *is* not-current — that works, but only when `container_image_id` is set. The spec's M3 test sentence ("not stale + running + record lacking this arch") therefore needs a fourth condition (a non-None `container_image_id`) and a helper change.
- **M-4 (low) — H2/L1's anti-literal contortion is unnecessary.** Because `tests/` is excluded from the literal scan (row 9), the new tests may name `linux/amd64/v2` directly, as `tests/test_sync.py:22` already does. Deriving from `published_targets()` is still fine and arguably better, but the spec presents it as a gate requirement, which is false; L1's `f"linux/{arch}"` construction in particular is complexity bought for nothing.
- **M-5 (low) — L1's premise that an empty `platforms` is reachable is unproven on this host.** `wanted` always contains `resolve_platform()`, which is either the `DOTFILES_PLATFORM` pin (mise.toml sets a published triple) or `host_platform()` (`platform_target.py:333-336`, always a published triple by construction). So the empty case requires an operator-set unpublished spelling. The guard is still correct — just note it is defensive, not a live bug, and the L1 test must set the env var (or pass `env=`) to reach it; `resolve_platform()` reads `os.environ` by default (:351), so `monkeypatch.setenv` is the lever.
- **M-6 (informational) — H3's `docker = self._get_bin("docker")` runs at :276, BEFORE the lookup at :279.** If `_get_bin` can raise when docker is absent from PATH, an unreachable-daemon scenario may fail there first rather than in the catch. The two existing `down()` tests stub `docker.shutil.which`, so the new test must too, or it will not reach the lookup at all.

## GitHub repos touched

_None._
