# Premise verification — spec A-1 (fable-orchestrator:premise-verifier, 2026-09-09 22:50Z)

Part 1 (verbatim from the lane's final message; part 2 appended below when received).

13 of 15 listed rows CONFIRMED, 1 REFUTED, 1 CONFIRMED-with-corrections, plus 17 unlisted premises found.

| Row | Verdict | Evidence |
|---|---|---|
| L1 | CONFIRMED | sub-agents.md:292-308 carries exactly those 17 rows in that order; :306 lists exactly red, blue, green, yellow, purple, orange, pink, cyan |
| L2 | CONFIRMED | `AskUserQuestion` at :382, inside the first-filter list opened at :379 |
| L3 | CONFIRMED | scope table :580-584, local at :584; Read/Write/Edit auto-enabled at :592 |
| L4 | CONFIRMED | full content injected :553; `disable-model-invocation` exclusion :555 |
| L5 | CONFIRMED | workflows.md:194, :199, :309 |
| L6 | CONFIRMED | all three clauses hold across the nine files |
| L7 | CONFIRMED, citation partly wrong | `PREMISES` abort :40, `COMMIT:` :36 and :52, `PREMISES-VERIFIED:` :42, `cat` of the marked file :61. But `EFFORT:` and `TIMEOUT:` are settled at :141 and :136, not inside the cited :36-42 |
| L8 | CONFIRMED | exclude:15; gitignore grep rc=1 with control arm hitting graphify-out at .gitignore:89 |
| I1 | CONFIRMED | main.py:647-652 and :2406 verbatim |
| I2 | CONFIRMED | mise.toml:1247-1249 verbatim |
| I3 | CONFIRMED, off by one | the suite body is 1387-1407; line 1409 opens the next suite |
| I4 | CONFIRMED, wiring is elsewhere | see M-4 |
| I5 | CONFIRMED, two behaviour caveats | see M-7 and M-8 |
| P1 | CONFIRMED | adversarial-critic.md declares `disallowedTools` and no `tools`, so Write is inherited |
| A1 | REFUTED | see M-2 |
| A2 | CONFIRMED, two corrections | see M-9 |

## MISSING premises, by teeth

M-1. `agentType` is undocumented and unused, and the whole of sections 3c and 3d rest on it. The workflows doc describes `agent(prompt, {schema, label})` at :292-298 and defers the full input list to the SDK reference at :320 and the bundled authoring skill at :309. The only saved workflow on this machine passes `{label, phase, model, effort, schema}` at all ten call sites and never `agentType`. Nothing evidences that a workflow may name a project agent, still less a plugin-scoped one. Load the `workflow-authoring` skill before writing either script.

M-2. A1 is refuted, and its remedy cannot work. The docs say subagents invoke "project, user, and plugin skills" through the Skill tool at :299 and :553, and ground the only preload exclusion in "preloading draws from the same set of skills Claude can invoke" at :555, which points the other way from A1. Moot for the roster, since no plugin-namespaced skill is listed. The remedy is not moot: no agent in the table lists `Skill` in `tools`, and :553 says omitting it is exactly how you stop a subagent invoking skills.

M-3. The pipeline stage signature in 3f is wrong for the first stage. The repo's own comment at modernization-audit.js:135 records the runtime contract, and the guard beneath it exists because stage one's second argument is the index, not a string. Stage one receives item then index; later stages receive prev, item, index.

M-4. I4 binds a definition, not a call site. `excludePaths` occurs exactly once in hk-common.pkl, at its definition on :42. The wiring is `exclude = common.excludePaths` at hk.pkl:26 and hk-image.pkl:26. A contract token pointed at the cited range asserts the list exists, not that any builtin reads it, which is the defect class suites.toml:1411 was written to prevent.

M-5. The 1,536 cap is real for agents, but the gate section 4 names is blind to them. The classifier returns None for that path, so the cap is applied only to skills. This repo already states it: suites.toml:2196 says md-budget is "blind to `.claude/agents/*.md` entirely (classify() returns None for that path)". The gate that does catch it is the doctor's listing check.

M-6. That listing budget is already breached. Measured just now: listing size 39,164 chars; declared ceiling 34,000 chars; entries 108. Seven new agent descript… [truncated — continued in part 2]

## Part 2 (verbatim resend, M-6 to close)

**M-6. That listing budget is already breached.** Listing size 39,164 chars; declared ceiling 34,000 chars; entries 108. Seven new agent descriptions push it further. The doctor reports drift and exits 0, so nothing blocks; the ceiling should move in the same reviewed diff, or the descriptions stay tight.

**M-7. The STEP 0 string is not unique.** The command appears at modernization-audit.js:195 and again at :229 inside the round-four patch note, with a third mention at :112. Changing only the first leaves the retired path live. It is also a template literal interpolating the audit dir, not the plain string section 2 quotes, and the new CLI takes flags, so the replacement carries `--audit-dir` and `--toml`.

**M-8. The aggregator's inputs do not travel, and the port is not verbatim.** The audit root is hardcoded and cwd-relative at aggregate.py:12; there is no audit-dir flag, so both new parameters are added behaviour. The whole tree is gitignored, confirmed by check-ignore against .gitignore:106. The regenerate-and-compare step and any test reading it pass only on this machine. The comparison target does exist; my first listing truncated it and an unbounded find settles that.

**M-9. Node is 26.8.2, not 26.8.1, and it is not repo-pinned.** It resolves from the user-global mise config, so the JS dry-run test skips on any machine without one, including CI. A test that can only skip in CI is not a gate.

**M-10. Memory auto-enables Edit on three agents that disallow it.** The doc at :592 enables Read, Write and Edit when memory is on; cold-reviewer, graphify-researcher and spec-scribe all deny Edit. Precedence is undocumented. Either they cannot edit their memory files, or the never-edits-the-tree contract in each body is false.

**M-11. Memory is a no-op when auto memory is off**, per :586. Nothing in the spec checks that setting.

**M-12. Section 3b's effort rule collides with an existing value.** One codex agent already declares high effort with a haiku model pin, which the rule would force to low while section 3b also says keep every other line byte-identical. More substantially, those five agents pin haiku because the Claude wrapper only supervises a process; the reasoning runs on the CLI. Re-run the codex agent parity gate after touching them, since it reads that frontmatter.

**M-13. agnix accepts the full key set. Probe-confirmed, both arms.** A scratch agent file carrying model, effort, tools, disallowedTools, maxTurns, color, memory and a skills list produced zero findings under strict mode; the control arm with no frontmatter produced the expected error and rc=1.

**M-14. Both unusual model and effort pairings have precedent on this machine**: the saved workflow runs opus at xhigh and haiku at low.

**M-15. The five preload skills all exist and none sets `disable-model-invocation`**, so L4's constraint is satisfied for the chosen roster.

**M-16. Two runtime facts sections 3c and 3d ignore.** An agent call resolves to null on stop or unrecoverable API error, per :305; the spec reads every stage return as an object with no null guard, while the repo's own workflow filters and carries a consecutive-null bail-out. The run caps are 4,096 items per fan-out, 1,000 agents per run, 16 concurrent, at :349-352.

**M-17. The meta block shape is unspecified.** The doc requires a pure object literal with name and description at :315, and phase entries whose titles match the calls exactly at :317. The repo's saved workflow uses a `whenToUse` key and phase entries as title-plus-detail objects.

Safe: the `.claude/CLAUDE.md` edit target has room (103 lines, 6,049 bytes vs 200/24,000); the host is Claude Code 2.1.267. Files read: the spec, the two KB docs, the plugin wrapper (1.21.0), `.claude/agents/*.md`, `.claude/workflows/modernization-audit.js`, `.agent/kb/audit/aggregate.py`, `main.py`, `mise.toml`, `suites.toml`, `hk.pkl`, `hk-common.pkl`, `.agnix.toml`, `.gitignore`, `.git/info/exclude`, installed `kb_setup/md_budget.py`. Nothing edited.
