# PR briefs — modernization audit 2026-09-09

From `docs/research/kb/reports/modernization-audit-2026-09-09.md` (§ "Scheduling
proposal", § "Rule refactors proposed") under `.agent/plans/grilling-2026-09-09.md`.
Re-extracted bucket ids matched the report's own counts exactly (118/151/16/116)
— the control arm. Full ask text and evidence for any row live in the report under
its id; bare `file.md:N` anchors are relative to
`~/dev/…/knowledge-base/sources/agent-harness-docs/docs/`.

**Every scheduled row survived** (only survived rows are scheduled), so survival
is not a discriminator and the signal is dissent: `†` = one lens dissented (3/4),
else 3/3. ⚠️ = **CONDITIONAL**, carrying a citation repair — read "Citation
corrections" and re-run its probe first.

## PR A — #994 in-scope

73 of 118, by theme.

### a. Agent frontmatter — the roster spec (all 10 `agents-*`)

- `agents-2` — `tools`/`disallowedTools`/`model`/`color`/`effort` supported, all omitted. `sub-agents.md:294-295`
- `agents-1` — `color` takes 8 values; `teal` is silently ineffective. `:306`
- `agents-9` — `effort` overrides session effort; `/tasks` shows it (2.1.242+). `:304`
- `agents-5` — `maxTurns` returns output MARKED PARTIAL and resumable (2.1.246+). `:298`
- `agents-6` — explicit `tools:` is the narrow; omitting `Agent` kills spawning. `:294`, `:452`
- `agents-3`† — `skills:` preloads a skill's FULL content at startup. `:299`, `:537-556`
- `agents-4`† — `memory:`; `project` is the documented default. `:302`, `:563-598`
- `agents-10` — `permissionMode`; `dontAsk` is the unattended posture. `:297`, `:518-525`
- `agents-7` — `permissions.deny` `Agent(<name>)` disables a named subagent.
- `agents-8`† — enabled `codex@openai-codex` 1.0.6 already ships `codex-rescue`; read it first.

Ray pins `memory: local` on judgment roles while `agents-4` calls `project` the
default — deliberate; justify it in the PR body.

⚠️ **`agents-12` is `unknown`; run its probe BEFORE scoping #994** (item 1):
whether codex 0.152.1's `codex agents` dashboard and `@`-mention task referencing
gave `.codex/agents/` a consumer on the `codex exec` path decides whether
`codex_agent_parity.py` guards a live surface.

### b. Workflows — `gated-implementation`, `graphify-refresh`

- `mod-codex_lane-1` — `codex exec review` derives the diff; keeps `--output-schema`/`-o`.
- `mod-codex_lane-2`† — `-c model_reasoning_effort="xhigh"` is the documented form.
- `mod-codex_verdict-1`† — verdict as a `msgspec.Struct`; DERIVE `--output-schema`.
- `mod-codex_verdict-2`† — same codec path for `LaneRecord`; typed CAS fields.
- `mod-codex_verdict-3` — `codex exec --json` gives terminal `turn.completed`/`turn.failed`.
- `mod-dag_tick-1`† — the supervisor already restarts a crashed background session.
- `skills-2-8` — the bundled `/loop` skill covers repeat invocation.

Write the workflow bodies against the **corrected** flags from theme d, not the
current `ai-cli-invocation.md` text.

### c. mise tasks, hk steps, suites the PR must touch

- `mod-codex_agent_parity-1` — agnix CC-AG-007 + CC-AG-001 already cover `.claude/agents/**`.
- `mod-codex_agent_parity-4` — hk `taplo` already lints the five `.codex/agents/*.toml`.
- `mod-codex_agent_parity-5`† — taplo `#:schema` closes the required-key gap.
- `mod-codex_agent_parity-9` — reuse `rule_registry._read_source` (BOM-safe).
- `mod-skillopt_provenance-1` — chain via mise `depends`/`depends_post` or hk.
- `mod-skillopt_provenance-2` — reuse `session_gate.mutation_sentinel()`.
- `mod-skillopt_provenance-3` — one `gh api graphql` batches all of it.
- `mod-session_gate-1` — argparse `nargs="+"` strips `--`, fails closed rc=2.
- `mod-session_gate-2`† — fnox's global `--non-interactive`.
- `mod-session_gate-3` — the `lint.py` runner pattern (`start_new_session`/`killpg`).
- `mod-session_gate-4` — mise's native per-task `timeout` key.
- `mod-session_gate-5`† — `gh issue view <n> -R <repo> --json`.
- `mod-session_gate-6` — `SessionStore._atomic_write`; strictly stronger.
- `mod-handoff_check-1`† — `mise tasks ls -J/--json`.
- `mod-handoff_check-4` — `SessionStart` with the `clear` source matcher.
- `mod-session_ledger-1`† — Agent SDK `list_sessions(...)`.
- `mod-session_ledger-2` — codex `thread/list` filters server-side.
- `mod-session_ledger-3` — `CLAUDE_CODE_SESSION_ID` is set automatically.
- `mod-session_ledger-4` — codex `sourceKinds` is the typed vocabulary.
- `mod-session_ledger-5`† — gh reads `GH_TOKEN`/`GITHUB_TOKEN` from the env.
- `skills-1-5` — the three-layer stack is the shape every new task takes.
- `skills-1-6` — `mise run lint` is the gate the `gate-runner` role fronts.

### d. Rules whose refactor BLOCKS the roster

Change + motivating defect in § "Rule refactors".

- `rule-agent-report-persistence-1` … `-5`. Rule **1c**'s file-role contract is carried by `-2`: `SubagentStart` `hookSpecificOutput.additionalContext` (`hooks.md:2304-2313`) pushes it into ad-hoc briefs mechanically.
- `rule-clarify-before-acting-1`, `-2`†, `-3`†. **`-2` is the hard blocker**: the harness strips `AskUserQuestion` from *every subagent* — "the first filter removes these tools, even when listed in the `tools` field" (`sub-agents.md:380-382`) — so no role can obey the unconditional mandate.
- `rule-agent-artifact-conventions-1` … `-7`. ⚠️ `-2` is CONDITIONAL.
- `rule-notepad-enforcement-1`†, `-2`, `-3`, `-4`†.
- `rule-ai-cli-invocation-1` … `-7` (`-4`†). `.claude/agents/codex-operator.md:75-78` already says `--full-auto` "does not exist on codex 0.152.0" while the rule prescribes it.
- `rule-md-size-budgets-1` … `-5`. `-2` binds `agents-3`: the cap covers `description` **plus `when_to_use` combined**.

⚠️ **`rule-mise-tasks-only-*` is NOT in #994.** Its four survivors (`-1`, `-3`†,
`-4`†, `-6`†) sit in **#986**; `-2` was REFUTED (3/5, did not survive). Changing
its redirect semantics is a cross-PR dependency, not a #994 row.

### e. Skills twins under `.agents/skills` with dead `.Codex/rules/*` pointers

- `skills-1-7` — six of eleven `SKILL.md` have a stale hand-kept twin; the pinned `skills` CLI (`npm:skills` 1.5.23) links them natively. Probe `mise exec -- skills --help` rc=0.
- `skills-3-7` — twins came from a `claude`→`Codex` substitution; Codex reads `.agents/skills` AS-IS. `codex/build-skills.md:135`
- `skills-1-2` — retire `find-docs` and its twin; `context7-cli` + pinned `ctx7` cover it.

⚠️ **The worst drift has NO finding** (item 4, a #994 checklist row): in
`skills-2`, **10 of 11** members byte-diverged from their `.agents/skills/**` twin
and **9** of those twins point at a `.Codex/rules/` path **that does not exist**.
Widen the unit list to `.agents/**` rather than run a third partial pass.

## PR A — deferred to the epic

The other 45. No row waits on a probe; the only gating probe is `agents-12`.

**MNO — touches a module this PR does not open (10):** `mod-ai-1` mise
which/ls --missing · `mod-ai-2` tool `postinstall` · `mod-ai-3` both already
native · `mod-dag_project-1` `gh label create --force` · `mod-dag_project-5`
argparse mutually-exclusive group · `mod-eval_cases-1` `${CLAUDE_PLUGIN_ROOT}` ·
`mod-instructions_observer-2`† uv `--no-sync` · `mod-instructions_report-1`†
`rule_registry.build_registry` · `mod-memory_index-1`† harness measures MEMORY.md ·
`mod-memory_index-2` three ignored storage inputs

**NRB — real, but blocks neither the roster nor the workflows (35):**
`mod-session_review-1` mdschema pinned, unwired · `mod-session_state-2`
`git status --porcelain -z` · `mod-session_state-3` `gh pr checks` tallies ·
`mod-session_store-2` the `codec` seam · `rule-goal-history-2`† `Stop` hook ·
`rule-goal-history-4` native `/goal` · `rule-research-doc-sources-1` re-derive the
figure · `rule-research-doc-sources-2` `find-docs` covers step 3 ·
`rule-research-doc-sources-3`† built-in `WebFetch` · `rule-research-doc-sources-4`
tool search is conditional · `rule-research-doc-sources-5`† the banner is narrow ·
`rule-research-doc-sources-6` built-in `WebSearch` ·
`rule-research-repo-enumeration-1` an hk step is now non-vacuous ·
`rule-research-repo-enumeration-2`† mdschema validates sections ·
`rule-research-repo-enumeration-3` a subagent body IS its prompt · `skills-1-1`
`devcontainer features info verbose` · `skills-1-3` three mechanisms own it ·
`skills-1-4` do-not + local-first tiers · `skills-1-8`† `paths:` limits activation ·
`skills-2-1`† thin-layer tasks exist · `skills-2-2`† sizing + `context: fork` ·
`skills-2-3` MCP tools are deferred · `skills-2-4` same as `skills-2-3` ·
`skills-2-5` mise config is the source · `skills-2-6` `/teleport` + Remote Control ·
`skills-2-9` `${CLAUDE_SKILL_DIR}` sidecar · `skills-2-10` `paths:` frontmatter ·
`skills-2-11` pklr refuses the identifier · `skills-3-1` teleport + "Continue in" ·
`skills-3-2`† vendor documents tmux · `skills-3-3` OpenSSH `IgnoreUnknown` ·
`skills-3-4` hk content-keys its cache · `skills-3-5` agent teams vs subagents ·
`skills-3-6` `/clear` preserves the session · `skills-3-9` mise `packslip:` GA

## PR B — #986 in-scope

14 of 151 — only the pipefail-aware exit-code-masking class and what carries it.
`hk-steps-2-4` is the core row and matches Ray's ruling exactly: keep both step
NAMES, widen the globs, route the check into `workflow_hooks.py`.

- `hk-steps-2-4` — `require_pipefail` (`hk.pkl:206-210`) and `no_grep_q_under_pipefail` (`hk.pkl:232-236`) are multi-statement inline bash; move them to `check = "uv run --project python dotfiles-setup <subcommand>"` over a python module.
- `hk-steps-2-6` — `bash_budget.py` cannot see the bash it most needs to; extend it to parse `check`/`fix` bodies out of the tracked `.pkl` configs.
- `mod-workflow_hooks-1`† — the 970-line per-job policy check; hk 1.57.0's PROJECT config can declare the runner-conditioned skip itself.
- `mod-workflow_hooks-2` — replace the hand-rolled task-table reader and transitive solver with `mise tasks ls --json`.
- `mod-workflow_hooks-3` — `_ARGV_GIT_WRITE` is a regex over raw python text; pinned ast-grep 0.45.3 matches it structurally.
- `mod-bash_budget-3` — declare the step as hk `Config.CommandSpec { effect = "read" }`, not a bare shell string.
- `hk-steps-1-3` — `Builtins.shellcheck` bare; the builtin sets `batch = true` itself.
- `hk-steps-1-8` — the capability already rides the bare assignment; make it explicit.
- `hk-steps-2-7` — `hk-image.pkl` overrides a `batch = true` the builtin already sets.
- `mod-hook_selfcheck-1` — `Builtins.shellcheck` already reports shell PARSE errors.
- `suites-2-2`† — fourteen `ci.*` suites assert workflow STRUCTURE by whitespace-, indent- and key-order-sensitive substrings; the parser this PR adds is the fix.
- `suites-3-5` — `require_tokens` (34 of 38) matches UNANCHORED substrings; the new canary must not inherit that.
- `suites-3-6` — definition-only bindings survive deletion of the wiring they protect; bind a CALL SITE.
- `suites-3-10` — `contract_token_uniqueness` audits only `require_tokens` suites, so the new tokens need a uniqueness check that reaches them.

Also (item 3): **`["editorconfig-checker"]` (hk.pkl:42) has no disposition** —
retire-shaped, covered only as evidence inside a `keep` row; it belongs to #986
once it gets a finding.

## PR B — deferred

137 rows by unit, suffixes given so every id is recoverable. None is about
workflow-YAML pipefail, `workflow_hooks.py`, shellcheck/actionlint wiring, or the
contracts binding them — PR B is deliberately one class.

**Module this PR does not open (41):** `mod-doctor` -1,-2,-3,-5,-6 ·
`mod-rule_registry` -1,-2,-4,-5,-6 · `mod-child_env` -1…-4 · `mod-lint_delta`
-1…-4 · `mod-listing_budget` -1…-4 · `mod-command_audit` -1,-2,-4 ·
`mod-env_blob_scan` -1,-3,-4 · `mod-hook_guard` -1,-2,-3 · `mod-hook_selfcheck`
-2,-3,-5 · `mod-branch_guard` -2,-3 · `mod-hk_builtins_audit` -1,-3 ·
`mod-process_env` -1,-4 · `mod-script_guard` -3,-4 · `mod-classifier_tables` -2 ·
`mod-codec` -4 · `mod-doc_refs` -3 · `mod-token_audit` -2

**Rule refactor, no gate wiring here (44):** `rule-long-running-command-hangs`
-1,-2,-3,-4,-6,-7 · `rule-verify-before-advancing` -1,-2,-3,-4,-5,-7 ·
`rule-ci-local-parity` -1…-5 · `rule-do-not` -1…-5 ·
`rule-probes-need-a-control-arm` -1…-4 · `rule-clean-git-state` -1,-2,-3 ·
`rule-secrets-out-of-the-shell-env` -1,-2,-3 · `rule-zero-skip-policy` -1,-2,-3 ·
`rule-gh-cli-watch` -1,-3 · `rule-zero-bash-logic` -2

**Other hk steps / builtins (15):** `hk-steps-1` -1,-2,-5,-7 (⚠️ -2, -5) ·
`hk-steps-2` -2,-3,-5,-8 · `hk-steps-3` -4,-5,-6,-8 · `hk-steps-4` -2,-3,-4

**Contracts beyond the new canary (17):** `suites-3` -1,-2,-3,-4,-7,-9 ·
`suites-2` -1,-3,-4,-5 · `suites-4` -1,-2,-3,-5 · `suites-1` -1,-2,-7

**Scripts, not the YAML class (9):** `shell-scripts` -1…-9

**Named exceptions (11):** `rule-mise-tasks-only` -1,-3,-4,-6 (a #994 cross-PR
dependency) · `mod-ask_quality` -2,-4,-5 (⚠️ all CONDITIONAL; PreToolUse surface) ·
`mod-verify` -1,-2,-3 (the verify CLI itself; changing it mid-class risks the gate)
· `mod-lint` -1,-2 (mise-native task timeout supersedes the wrapper)

## PR C — #997 graphify refresh

All 16 in-scope; the graphify surface is what this PR opens.

- `mod-graphify-1` — derive the expected version from `importlib.metadata.requires(...)`.
- `mod-graphify-2` ⚠️ — retire the shrink guard; graphify refuses incomplete builds (0.9.51/0.9.53).
- `mod-graphify-3`† — the harness owns the transport budget natively.
- `mod-graphify-6` — graphify already computes per-file staleness in its own guard.
- `mod-graphify-7` — `explain` and `path` are first-class commands beside `query`.
- `mod-graphify_skill-1` — `install._platform_skill_destination(..., project=True)`.
- `mod-graphify_skill-2`† — `install._copy_skill_file(...)`, the vendor's own copier.
- `mod-graphify_skill-3` — `install._canonical_platform(name)` resolves CLI aliases.
- `mod-graph_bakeoff-1` — graphify already writes those two numbers atomically.
- `mod-graph_bakeoff-2` — `graphify provider add|list|show|remove` writes that file.
- `mod-graph_bakeoff-3` — `kb_setup.evals`, already pinned as a uv git dep.
- `rule-graphify-first-1` — a stale FACT; the user-global pin moved.
- `rule-graphify-first-2`† — `permissions.deny` on `Bash(graphify *)` and bare `Bash(graphify)`.
- `rule-graphify-first-3` — `PreToolUse` + `additionalContext`, already wired here.
- `rule-graphify-first-4` — stale wiring copy that must follow the code.
- `rule-graphify-first-5`† — derive the version with stdlib `importlib.metadata`.

⚠️ **Re-read `sources/graphify/CHANGELOG.md` before scoping #997** (item 5): it was
rewritten mid-audit and now carries `## 0.9.56` and `## 0.9.57 (unreleased)`
directly, so the `graphify-readme-0.9.56` gap fetch — recorded in Method as OLDER
than the offline baseline — is no longer the best source.

**Four items in Ray's PR C ruling have NO finding**: `graphify-remember` /
`graphify-reflect` tasks, per-artifact export-format rc receipts, `graphify-doctor`
extending `graphify_health` + `doctor.toml`, and the `graphify-mcp` registration.
Ruling-derived scope; expect no finding id.

## Rule refactors that PR A must ship

"Defect" is the motivating failure quoted from the report, so
`codex-adversarial-critic` can replay it: a refactor that lets that defect recur is
a bad refactor however current its citation.

**`.claude/rules/agent-report-persistence.md`** — `-1`…`-5`.
*Change:* keep the tracked destination (nothing native replaces it) but move the
TRIGGER off habit and onto hooks — `SubagentStop` writes `last_assistant_message`
to the rule's own path, `SubagentStart` `additionalContext` carries the
incremental-persistence and file-role contract into ad-hoc briefs, `TeammateIdle`
(which CAN block) covers teammates, rule 5 reads the `subagents/` roster, and `-3`
downgrades "total loss" to "recoverable via `agent_transcript_path`, but untracked".
*Defect:* 2026-07-05, an 11-agent sweep produced 13 reports that existed only in
the context window, one `/clear` from loss; 2026-08-03, incremental persistence
went into none of four briefs and two agents died leaving nothing.

**`.claude/rules/clarify-before-acting.md`** — `-1`, `-2`, `-3`.
*Change:* re-anchor three stale citations and add the bounded fallback rule 1
already wants — when `AskUserQuestion` is absent or denied (every subagent,
`dontAsk`, `-p`, `--permission-prompts none`), present options in prose and STOP;
name `askUserQuestionTimeout` so an auto-continue is not read as an answer.
*Defect:* 2026-06-29, the chosen approach (per-step hk timeouts) was impossible
because hk has no timeout support, and only surfacing that before building avoided
shipping the wrong thing.

**`.claude/rules/agent-artifact-conventions.md`** — `-1`…`-7` (⚠️ `-2`).
*Change:* adopt `plansDirectory: ".agent/plans"` so the harness writes where the
rule says, fix two stale skill-loader facts, replace the dead
`.agent/project-memory.json` row with the native subagent `memory:` scopes, and add
the six undeclared artifact locations plus the worktree/`isolation` case.
*Defect:* ad-hoc agent working directories — the tree was renamed from `.omc/`
because it was named after a plugin nobody enabled, and five eager rules cited
research that had never been tracked, so every reader off that machine hit a dead
link.

**`.claude/rules/notepad-enforcement.md`** — `-1`…`-4`.
*Change:* stop being markdown-only — carry it with `SubagentStop`, use the ENABLED
`planning-with-files` plugin's `findings.md` (re-injected on `clear`/`compact`) for
the context-loss half, set `memory:` on the nine named agent definitions (zero set
it today), and account for `Explore` skipping CLAUDE.md.
*Defect:* 2026-04-05, agents did extensive analysis and wrote nothing to the
notepad, so every finding had to be re-derived when context was lost; the rule then
named MCP tools from a disabled plugin, measured at 0 invocations.

**`.claude/rules/ai-cli-invocation.md`** — `-1`…`-7`.
*Change:* replace `--full-auto` with `-s workspace-write` (plus `--approve-for-me`),
move the positional prompt out of "will silently fail" (documented, and stdin now
composes as a `<stdin>` block), keep the confirmed `gemini "prompt"` warning, point
the Gemini lane at the pinned `agy`/antigravity path, and collapse the five
hand-kept copies of the invocation into one.
*Defect:* wrong CLI flags that fail silently — `codex -p` is `--profile`, not a
prompt; `gemini "prompt"` hangs interactively. Reproduced, not stylistic: the
rule's own write-lane invocation exits rc=2 on the pinned CLI.

**`.claude/rules/md-size-budgets.md`** — `-1`…`-5`.
*Change:* drop the quotation that no longer exists in the corpus (its replacement
contradicts the bolded conclusion), correct the number that is now a settings
default attributed to the wrong field and name the harsher non-deterministic cliff,
add `/context` as the native measurement, document the 1,000-expanded-pattern
`paths:` budget, and fix "not re-injected after `/compact`".
*Defect:* the predecessor step enforced 200 lines and 12,000 bytes captioned "per
Claude Code memory docs" — the number is real but it is **Windsurf's**, a true fact
that travelled without its provenance and was then machine-enforced against files
its real owner never governed.

## Counts

| bucket | total | in-scope | deferred |
|---|---|---|---|
| #994 orchestration-infra (PR A) | 118 | 73 | 45 |
| #986 gate (PR B) | 151 | 14 | 137 |
| #997 graphify refresh (PR C) | 16 | 16 | 0 |
| #993 later epic checklist row | 116 | 0 | 116 |
| **total** | **401** | **103** | **298** |

Rule rows inside #994: 42 — 31 in-scope across the six rules above, 11 deferred.
Conditional rows by bucket: #994 = 1, #986 = 5, #997 = 1, #993 = 7.

## GitHub repos touched

_None beyond this repo._
