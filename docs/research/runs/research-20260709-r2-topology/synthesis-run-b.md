# Moved

This file's content was promoted verbatim to `report.md` in this directory
(the subagent Write guard blocked the `report.md` filename; the parent
session performed the rename). See `./report.md`.
