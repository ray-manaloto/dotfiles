# Session handoff — 2026-07-11 (web → desktop)

Written by the Claude-web (cloud) session that ran r2+r3. This session was
**git-bricked** (no Python ≥3.14 → PreToolUse guard fails closed → Bash/git
blocked), so everything was delivered via the GitHub API, and this handoff is
API-pushed rather than `git`-committed.

## State at handoff
- Branch: `claude/refine-local-plan-7knttu` · PR: **#197** (draft) · HEAD: `f5992d2`
- Gates on `f5992d2` (from GitHub check API `conclusion`, not a tail):
  lint ✅ · ci-gate ✅ · contract-preflight ✅ · changes ✅ · autofix ✅ ·
  build-publish / promote / failure-report ⏭️ skipped (docs-only changes).
- Autonomous processes (do NOT wait on): Renovate (hosted Mend) opens/merges its
  own PRs on its schedule; CodeRabbit posts a draft-skip auto-comment on each
  push (noise, no action). `main` is bot-advanced — just `git pull`.

## What shipped this session
r2 (prior) was already on the branch. This session added:
- **r3 research** — 5 domain reports + a cross-domain synthesis (Mermaid +
  refuted-claims roll-up + P0/P1/P2 open questions) + a 5-phase implementation
  plan, all under `docs/research/runs/research-20260710-r3-*/`
  (synthesis + plan at `.../research-20260710-r3-synthesis/report.md` + `plan.md`).
- **`docs/web-brick-fix-handoff.md`** — step-by-step to un-brick web sessions.
- **`/handoff` + `/resume` skills** (`.claude/skills/{handoff,resume}/SKILL.md`)
  + `docs/handoffs/README.md` — cross-surface session-handoff automation.
- **`.agnix.toml`** — excluded `docs/research/runs/**` and `docs/handoffs/**`
  from agnix (prose, not agent docs).

## Next task (as Ray directed)
1. **Apply the web-brick fix** — follow `docs/web-brick-fix-handoff.md`:
   edit `.claude/settings.json` (rewire PreToolUse to
   `bash scripts/pretooluse-guard.sh` + add the `CLAUDE_CODE_REMOTE`-gated
   SessionStart hook), and set the claude.ai/code **setup-script** field to
   `scripts/web-setup.sh`'s contents (browser). Validate with `mise run lint`,
   `pytest`, `dotfiles-setup verify run`, then commit + push.
2. **Start Phase 0 of the r3 plan** —
   `docs/research/runs/research-20260710-r3-synthesis/plan.md` Phase 0:
   fix the conda claim in `.claude/rules/tool-currency-and-native-first.md`;
   add `"schedule": ["at any time"]` to `renovate.json`; fix the
   `mise-runtime.lock` omission in `.github/workflows/refresh.yml`; add a
   `DIVE_VERSION` Renovate customManager; let Renovate bump `MISE_VERSION`→2026.7.5.

## Open decisions (awaiting Ray — from the synthesis P0/P1/P2)
1. **P0:** fix the wrong conda claim in `tool-currency-and-native-first.md`?
   *Rec: yes — it currently licenses a wrong retirement of the snapshot machinery.*
2. **P0:** `renovate.json "schedule": ["at any time"]` to kill Friday-only? *Rec: yes.*
3. **P0:** bump `MISE_VERSION`→2026.7.5 THEN drop `experimental=true` (that order)? *Rec: yes.*
4. **P1:** reusable workflow (not composite) for `refresh`; adopt native
   `[bootstrap.macos.launchd.agents]` for Run F's plist layer. *Rec: yes to both.*
5. **P2:** confirm the subagent GO bar (≤0.35 token ratio); approve the graphify
   seed/cadence before spending build tokens. *Rec: accept bar to falsify; explicit yes/no on graphify.*

## Gotchas
- **The web brick** is why this session used API pushes. Fixing it (task 1) makes
  future web sessions run under normal git. On the Mac, git already works.
- **agnix `xml_balance`** parses `<placeholder>` angle brackets in `.claude/skills/**`
  and `.claude/agents/**` as unclosed XML tags — use `{placeholder}` in skill bodies.
  (Cost a lint failure this session; fixed in `f5992d2`.)
- **`docs/research/runs/**` and `docs/handoffs/**` are agnix-excluded** — research
  prose in an `agents/` subdir would otherwise be validated as agent definitions.
- **`.claude/settings.json` self-edits are classifier-blocked** for the agent —
  the human applies that diff (both web and desktop agents hit this).
- **Don't retire the custom conda-snapshot machinery** — the rule that licenses it
  is factually wrong (verified 0/3 this round); fix the rule instead.

## Resume
Plain prompt (robust — the `/resume` skill lives only on this unmerged branch and
may not be loaded on a fresh checkout, so do NOT rely on the slash command yet):

> Check out the branch `claude/refine-local-plan-7knttu` (git fetch, checkout,
> pull), then read `docs/handoffs/session-2026-07-11.md` and continue from it.

After PR #197 merges to `main`, `/resume` + `/handoff` are loaded on every
session and become the one-word path; the plain prompt stays the fallback.
