# SPEC A-1 — orchestration substrate: seven saved subagents, two saved workflows, the audit aggregator promoted to python (#994, part 1 of PR A)

Repo: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles · branch: feat/orchestration-modernization-audit (already checked out; base origin/main @ 62f416f; HEAD e1fb19b carries the audit report and `.claude/workflows/modernization-audit.js`).

## 1. Objective

Make the orchestration this repo re-derives every session a set of tracked, reusable assets: seven saved subagent definitions with per-role model/effort/tool/turn budgets, two saved Workflow scripts (`gated-implementation` — the spec → codex-implementer → gates → cold review → critic loop; `graphify-refresh` — an ordered operator pipeline with verification), and the audit's deterministic aggregator moved from the gitignored scratch file `.agent/kb/audit/aggregate.py` into `python/src/dotfiles_setup/` behind a mise task, a verification contract and tests. Also repair the frontmatter drift the audit found in the nine existing agents. Failure prevented: an architect session typing the same roster and loop by hand at the most expensive lane's prices, and agents launched with silently ineffective fields (an invalid `color`, no `maxTurns` so a wedged lane runs to the session cap, a reviewer that inherits Edit because `tools:` was omitted). Outcome, not keystrokes: after this change `/gated-implementation` and `/graphify-refresh` run from a fresh clone, every agent file passes `mise run lint-docs`, and `mise run audit-aggregate` regenerates the committed TOML byte-for-byte.

## 2. Files

CREATE
- `.claude/agents/gate-runner.md`, `cold-reviewer.md`, `graphify-operator.md`, `graphify-researcher.md`, `spec-scribe.md`, `pwf-scribe.md`, `issue-filer.md`
- `.claude/workflows/gated-implementation.js`, `.claude/workflows/graphify-refresh.js`
- `python/src/dotfiles_setup/modernization_audit.py` (the aggregator; source of truth to port VERBATIM in behaviour: `/Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/audit/aggregate.py`)
- `tests/test_modernization_audit.py` (fixture-driven: a tiny synthetic audit tree under `tests/fixtures/modernization_audit/` — two units, three lens files, one malformed verdict file, one corrections entry — plus its EXPECTED TOML/summary committed alongside; both arms: the expected output regenerates byte-for-byte, and a mutated verdict flips a survival flag. `.agent/` is gitignored, so no test may read it), `tests/test_workflows_js.py`
MODIFY
- `python/src/dotfiles_setup/main.py` (register + dispatch `audit-aggregate`), `mise.toml` (task `audit-aggregate`), `python/verification/suites.toml` (contract `workflow.modernization-audit-wiring`), `.gitignore` (add `.claude/agent-memory-local/`), `.claude/workflows/modernization-audit.js` (EVERY occurrence of the aggregator command — the template literal `python3 ${A.auditDir}/aggregate.py ${A.reportToml}` at :195 and :229 and the mention at :112 — becomes `mise run audit-aggregate -- --audit-dir ${A.auditDir} --toml ${A.reportToml}`; nothing else in that file changes; `grep -c aggregate.py` on the result must be 0), `.claude/CLAUDE.md` (the "Cross-vendor orchestration" lane table gains one row per new role and a line naming the two workflows — this file, never the root `CLAUDE.md`), the nine existing `.claude/agents/*.md` frontmatters (see Interfaces).
DO NOT TOUCH: `.claude/rules/**` (a sibling task owns the rule refactors), `AGENTS.md`, root `CLAUDE.md`, `.codex/**`, `.agents/**`, `hk*.pkl`.

## 3. Interfaces

### 3a. New agent frontmatter (exact values; body prose is yours, constraints in §4)

| name | model | effort | tools | disallowedTools | maxTurns | color | memory | skills (preload) |
|---|---|---|---|---|---|---|---|---|
| gate-runner | haiku | low | Bash, Read, Grep, Glob, Skill | Edit, Write, NotebookEdit | 40 | green | — | lint-delta |
| cold-reviewer | opus | xhigh | Bash, Read, Grep, Glob, Write, Edit, Skill | NotebookEdit | 60 | blue | local | adversarial-review |
| graphify-operator | sonnet | medium | Bash, Read, Grep, Glob, Write, Skill | Edit, NotebookEdit | 60 | purple | — | graphify, blast-radius |
| graphify-researcher | opus | high | Bash, Read, Grep, Glob, Write, Edit, Skill | NotebookEdit | 80 | purple | local | graphify |
| spec-scribe | opus | high | Read, Grep, Glob, Write, Edit, Skill | NotebookEdit, Bash | 40 | cyan | local | fable-orchestrator:orchestration |
| pwf-scribe | sonnet | medium | Read, Grep, Glob, Write, Edit, Skill | NotebookEdit, Bash | 40 | yellow | — | planning-with-files:planning-with-files |
| issue-filer | sonnet | medium | Bash, Read, Grep, Glob, Write, Skill | Edit, NotebookEdit | 30 | orange | — | pr-workflow |

Role contracts (what each body must say, in its own words):
- gate-runner: runs the check matrix it is given (`mise run lint`, `uv run --project python pytest …`, `mise run verify`, `mise run pin-actions`, `mise run lint-docs`) with a FILE-based rc (`cmd > <log> 2>&1; echo "rc=$?" >> <log>`), never a pipe into tail/head; reports one row per gate: command, rc read from the file, log path, first failing step name. Never fixes anything.
- cold-reviewer: reviews a diff BY REF (a commit SHA or base branch it resolves itself), cold — the caller gives no description of intent; returns findings as severity + one-line claim + file:line, every claim cited or labelled unverified; writes the full report to `docs/research/kb/reports/agents/cold-review-<ref7>-<stamp>.md` incrementally; never edits the tree. Consults its memory first, updates it last.
- graphify-operator: runs exactly the ordered list of mise tasks it is handed (`mise run graphify-health`, `graphify-update`, `graphify-skill-install -- <platform>`, `bakeoff`, and the tasks a later change adds), each with a file-based rc, stops at the first failure, and reports per task rc + log + the node/edge/community delta read from `graphify-out/GRAPH_REPORT.md` line 8 before/after. Never a bare `graphify` binary (`graphify-first.md`).
- graphify-researcher: read-only feature inventory of the INSTALLED graphify package (`python/.venv/lib/python3.14/site-packages/graphify/`), `uv run --project python graphify --help`, and the release notes it is pointed at; output one row per command/flag/export/MCP tool with a disposition; persists to `docs/research/kb/reports/agents/graphify-feature-inventory-<stamp>.md` incrementally, `## GitHub repos touched` at the end.
- spec-scribe: DRAFTS the seven-part spec (objective, files, interfaces, constraints, verification, commit, PREMISES) from the architect's decision notes into a scratchpad file; never dispatches it; every PREMISES row must cite a file:line the scribe itself read this session or be marked `A`; the architect ratifies before dispatch.
- pwf-scribe: writes `findings.md` and `progress.md` (root planning-with-files files) as instructed and emits a `task_plan` DELTA to `.agent/plans/task_plan-delta-<stamp>.md` for the coordinator to apply — it never edits `task_plan.md` itself (that file is coordinator-only and operator-attested).
- issue-filer: drafts issue/comment bodies to a scratch file, and files them with `gh issue create` / `gh issue comment` ONLY when its prompt contains the literal line `FILE ISSUES: yes`; otherwise it returns the drafts. Uses `-R` explicitly, asserts an edit anchor occurs exactly once before any body replace, never creates PRs (`gh pr create` is guard-denied; `mise run ship` owns that).
The three `memory: local` roles keep `Edit` ALLOWED (memory auto-enables Read/Write/Edit for the memory directory, and the precedence over `disallowedTools` is undocumented — sub-agents.md:592); their "never edits the tree" promise is a body contract: the only paths they may write are their own report file and `.claude/agent-memory-local/<name>/`. Every memory body also says: if the memory instructions are absent (auto memory disabled — `autoMemoryEnabled` / `CLAUDE_CODE_DISABLE_AUTO_MEMORY`, sub-agents.md:586), proceed without memory and say so in the report.
Every body ends with a short "Deliver before idle" section: write the report file first, then return the report path plus a ≤10-line summary as the final message; if `AskUserQuestion` is needed (it is stripped from every subagent), present the options in prose and STOP.

### 3b. Existing agent frontmatter repairs
- `codex-advisor.md`: `color: teal` → `color: purple` (teal is not an accepted value).
- `dockerfile-reviewer.md`: add `model: sonnet`, `tools: Bash, Read, Grep, Glob`, `disallowedTools: Edit, Write, NotebookEdit`, `maxTurns: 40`, `color: blue` (it is a read-only checklist that today inherits every tool).
- all nine: add `maxTurns` where absent (60 for the opus reviewers/auditors, 40 for the five `codex-*` lanes and dockerfile-reviewer). Do NOT touch `model`/`effort` on the five `codex-*` lanes: they pin haiku deliberately (the Claude side only supervises a codex process; `codex-claude-code-expert.md` already carries `effort: high` on haiku). Add `effort: high` only to `claude-code-expert.md` and `staleness-auditor.md` (opus roles without one). After touching any `codex-*` file run `mise run codex-agent-parity` (it reads that frontmatter).
Keep every other line byte-identical. (The four `codex-*` lanes list `tools` without `Skill` on purpose — they shell out to codex and never invoke skills; leave that.)

### 3c. `.claude/workflows/gated-implementation.js`
`export const meta` is a PURE literal: `{ name: 'gated-implementation', description: <one line>, whenToUse: <one line>, phases: [{ title: 'Implement', detail }, { title: 'Gates', detail }, { title: 'Review', detail }, { title: 'Critique', detail }] }`, titles matching the `phase()` calls exactly. `args` (all strings unless noted): `specFile` (absolute path, required), `premises` (the PREMISES block text, required), `attestation` (absolute path or ""), `effort` (default "xhigh"), `timeout` (seconds, default 3600), `verify` (array of shell commands, required), `reviewRef` ("" → use the commit the implementer reports), `criticProposal` ("" → skip Critique), `issue`, `branch`.
Stage 1: ONE `agent()` call with `agentType: 'fable-orchestrator:codex-implementer'` whose prompt is exactly: a `SPEC FILE: <specFile>` line; the `premises` text restated verbatim under a line `(reproduced from the spec file)`; `EFFORT: <effort>`; `TIMEOUT: <timeout>`; and `PREMISES-VERIFIED: <attestation>` only when `attestation` is non-empty. No other task text. Returns the CODEX REPORT as a string; parse `COMMIT:` from it.
Stage 2: `agentType: 'gate-runner'`, schema-returned `[{cmd, rc, log, firstFailure}]` for every `verify` command. Never skip a gate because an earlier one failed — run them all, report them all. (The platform requires an object-rooted tool schema, so the wire schema is `{gates: [{cmd, rc, log, firstFailure}]}` — a pure transport wrapper; the call site unwraps `.gates` immediately, so the in-script `gates` value stays the bare array described above.)
Stage 3: `agentType: 'cold-reviewer'`, prompt carries ONLY the ref (`reviewRef` or the parsed commit) and the instruction to review cold; schema `{findings:[{severity, claim, file, line, cited}], reportPath}`.
Stage 4 (only when `criticProposal` non-empty): `agentType: 'codex-adversarial-critic'` on the proposal text; schema `{verdicts:[{proposal, catchesMotivatingDefect, evidence}], reportPath}`.
Every `agent()` result may be `null` (user stop or terminal API error): guard each stage — a null implementer report ends the run with `{status:'implementer-null'}` and nothing further; a null gate-runner result is reported as `gates: null` (never as passed); a null review is `review: null`. Return `{status, implementerReport, commit, gates, review, critic}`. No loops: respec rounds are a new invocation by the architect. `model`/`effort` opts are NOT passed for custom agentTypes (their frontmatter owns them). Use `log()` at each phase boundary.

### 3d. `.claude/workflows/graphify-refresh.js`
`export const meta` pure literal `{ name: 'graphify-refresh', description, whenToUse, phases: [{ title: 'Research', detail }, { title: 'Operate', detail }, { title: 'Audit', detail }] }`; the same null guards as 3c (a null operator result stops the task list and is reported as such). `args`: `tasks` (array of `{name, cmd, expectRc?: number}` run IN ORDER by graphify-operator, stopping at the first non-expected rc), `researchBrief` ("" → skip Research; else one graphify-researcher call with that brief), `staleTerms` (array; empty → skip Audit; else one `agentType: 'codex-staleness-auditor'` call listing them). Returns `{research, tasks:[{name, rc, log, delta}], audit}`. The task list is data so a later change can add tasks without editing the script. (The platform requires an object-rooted tool schema, so the wire schema is `{tasks: [{name, rc, log, delta}]}` — a pure transport wrapper; the call site unwraps `.tasks` immediately, so the returned `tasks` value stays the bare array described above.)

### 3e. `dotfiles_setup/modernization_audit.py`
Port `.agent/kb/audit/aggregate.py` (read it; that script hardcodes a cwd-relative `.agent/kb/audit` at :12 and takes the TOML path as argv[1] — the port ADDS `--audit-dir`/`--toml` as explicit parameters, defaults equal to the old behaviour; otherwise behaviour identical, including `corrections.json` application and the `summary.json`/`coverage.json`/`existing-verdicts.json`/`aggregate.json` side outputs and the exact JSON stdout line) into: `aggregate(audit_dir: Path, toml_out: Path | None) -> AggregateResult` (a frozen dataclass or msgspec.Struct with the stdout counts) and `modernization_audit_main(project_root: Path, *, audit_dir: str = ".agent/kb/audit", toml_out: str | None = None) -> int`. CLI: `dotfiles-setup audit-aggregate [--audit-dir DIR] [--toml PATH]` registered in `main.py` next to `bash-budget` (subparser at the `subparsers.add_parser("bash-budget", …)` site, dispatch at the `"bash-budget": lambda …` site). mise: `[tasks.audit-aggregate]` with `run = "uv run --project python dotfiles-setup audit-aggregate"`, description one line, comment naming the module. Contract `workflow.modernization-audit-wiring` modelled on `workflow.bash-logic-enforcement` (suites.toml:1387-1409): `per_path_tokens` binding the CALL SITES — the mise `run =` line, the two `main.py` sites, `def aggregate(` and `def modernization_audit_main(` in the module, the test file's import, and the string `mise run audit-aggregate` inside `.claude/workflows/modernization-audit.js`.

### 3f. Listing budget (the gate that actually sees agent descriptions)
The `md_size_budget` step is BLIND to `.claude/agents/*.md` (`classify()` returns None for that path — suites.toml:2196); the gate that measures agent descriptions is the doctor's `listing-budget` check (`doctor.toml` `[listing]`, `max_chars = 34000`), and it is ALREADY breached before this change: `mise run doctor -- --verbose` reports 39,164 chars across 108 entries (third-party plugin growth — the `antigravity-delegate` description alone is 1,789 chars, over the 1,536 hard cap). Therefore: (a) every NEW agent `description` ≤ 320 characters, trigger keywords first; (b) trim the nine existing local agents' descriptions to ≤ 320 characters where longer, keeping their trigger keywords and the "never edits"/"reports with evidence" claims; (c) raise `max_chars` in `doctor.toml` to the post-change measurement plus ~4% headroom, in this diff, with a comment recording the before/after numbers and that the excess is plugin-driven (the doctor's own comment explains why the ceiling carries headroom rather than pinning). Verification 7 below measures it.

### 3g. `tests/test_workflows_js.py`
For every `.claude/workflows/*.js`: strip the leading `export `, wrap the body in an async function with stubbed `agent/parallel/pipeline/log/phase/args/budget/workflow` (stubs return schema-shaped objects keyed by the agent label prefix; `pipeline` passes `(item, index)` to the FIRST stage and `(prev, item, index)` to every later stage, exactly like the runtime — see the guard comment at `.claude/workflows/modernization-audit.js:135`) and run it with the REPO-PINNED `bun` (`mise exec -- bun run <tmpfile>`; bun 1.4.0 is in `.config/mise/conf.d/shared.toml`, node is only user-global and would make the test skip in CI); assert rc 0 and that every `agentType`/label used is one of the known registry names. Include a FAIL arm: a fixture script with a top-level syntax error must fail. Never skip: bun is pinned, so a missing bun is a failure, not a skip.

## 4. Constraints and invariants
- Rulings (`.agent/plans/grilling-2026-09-09.md`, ratified 2026-09-09): `memory: local` on cold-reviewer / graphify-researcher / spec-scribe (NOT `project`, although sub-agents.md recommends project — justify in the agent body: `.claude/agent-memory/` would be tracked prose under the typo-fixer and size gates); NO `permissionMode`, `hooks`, `mcpServers`, `isolation`, `background` keys on any agent.
- `description` frontmatter ≤ 1,536 characters (the loader truncates silently); every agent must pass `mise run lint-docs` (agnix) and the existing `md_size_budget` step; frontmatter keys only from the documented set.
- The workflow scripts are plain JavaScript (no TypeScript), begin with a PURE-LITERAL `export const meta`, never call `Date.now()`/`Math.random()`/`new Date()`, and pass timestamps via `args`.
- `gated-implementation.js` must not paraphrase, wrap or "clarify" the spec: the implementer's prompt is the control lines + the file pointer + the restated PREMISES, nothing else (the wrapper cats the file itself).
- Nothing new in bash: no `.sh` files (`bash_logic_budget` fails a new one); the aggregator is python; tests are pytest.
- `.gitignore` gains `.claude/agent-memory-local/` (today only `.git/info/exclude:15` hides it, per clone).
- Preserve `.claude/workflows/modernization-audit.js` except the STEP 0 command string; the committed TOML must remain byte-identical when regenerated through the new task (this is a verification step).
- Secrets: never print a credential value; presence flags only.
- Licensed dissent: if your reading of the harness docs contradicts a frontmatter value here (e.g. a key the field table does not list), stop and report instead of guessing.

## 5. Verification (smallest bundle; capture each rc to a file)
1. `uv run --project python pytest tests/test_modernization_audit.py tests/test_workflows_js.py -q` → rc 0.
2. `mise run audit-aggregate -- --audit-dir .agent/kb/audit --toml /tmp/regen.toml` → rc 0 AND `cmp /tmp/regen.toml docs/research/kb/reports/modernization-audit-2026-09-09.toml` → identical (control arm: `cmp` against `/dev/null` differs).
3. `mise run lint` → rc 0 (read `rc=` from the log file, never a piped tail).
4. `mise run lint-docs` → rc 0.
5. `mise run verify` → `0 failed`.
6. `uv run --project python pytest tests/ -x -q` → all pass.
7. `mise run doctor -- --verbose` → no `DRIFT doctor[listing-budget]` line for the listing size (the per-plugin over-cap description line for `antigravity-delegate` is not ours and may remain — say so in the report); record the measured chars/entries before and after in the commit body.

## 6. Commit
Lane commits on the current branch. Subject: `feat(orchestration): seven saved subagents, gated-implementation and graphify-refresh workflows, audit aggregator as a mise task (#994)`. One commit.

## 7. PREMISES
- L1 — subagent frontmatter field table (name, description, tools, disallowedTools, model, permissionMode, maxTurns, skills, mcpServers, hooks, memory, background, effort, isolation, color, initialPrompt, experimental) — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/sub-agents.md:292-308; `color` accepts exactly red, blue, green, yellow, purple, orange, pink, cyan — :306.
- L2 — `AskUserQuestion` is removed from every subagent even when listed in `tools` — same file :378-384.
- L3 — `memory` scopes: `local` = `.claude/agent-memory-local/<name>/`, not checked in; enabling memory auto-enables Read/Write/Edit for the memory dir — same file :563-598.
- L4 — `skills` preloads the FULL content of each listed skill; skills with `disable-model-invocation: true` cannot be preloaded — same file :537-556.
- L5 — saved workflows live at `.claude/workflows/` and run as `/<name>`; the saved file is `.js` — /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/claude-code/workflows.md:194-208 and :309.
- L6 — existing agent frontmatter inventory read this session: `codex-advisor.md` has `color: teal`; `dockerfile-reviewer.md` declares only name/description; `adversarial-critic.md`, `claude-code-expert.md`, `staleness-auditor.md` carry `disallowedTools: Edit, NotebookEdit` and no `tools` — /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.claude/agents/*.md frontmatter blocks.
- L7 — the codex-implementer wrapper reads `SPEC FILE:`, `EFFORT:`, `TIMEOUT:`, `COMMIT:`, `PREMISES-VERIFIED:` from the DISPATCH text, cats a marked spec file verbatim, aborts without a `PREMISES` heading — /Users/rmanaloto/.claude/plugins/cache/fable-orchestrator/fable-orchestrator/1.21.0/agents/codex-implementer.md:36-42 (PREMISES abort :40, COMMIT :36/:52, attestation :42), :52, :61 (cat of the marked file), :136 (TIMEOUT), :141 (EFFORT).
- L8 — `.claude/agent-memory-local` is excluded only per clone: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.git/info/exclude:15; `.gitignore` has no `agent-memory` line (grep, 0 hits; control: `graphify-out/` at .gitignore:89).
- I1 — CLI registration site `subparsers.add_parser("bash-budget", …)` at python/src/dotfiles_setup/main.py:647-652; dispatch `"bash-budget": lambda: sys.exit(bash_budget_main(project_root))` at :2406.
- I2 — python-backed mise task shape `[tasks.hk-audit]` … `run = "uv run --project python dotfiles-setup hk-builtins-audit"` at mise.toml:1247-1249.
- I3 — contract shape `workflow.bash-logic-enforcement` with `per_path_tokens` binding call sites at python/verification/suites.toml:1387-1407 (1409 opens the next suite).
- I4 — `docs/research/kb/**` is in `excludePaths` (defined hk-common.pkl:42-52) and the list is WIRED by `exclude = common.excludePaths` at hk.pkl:26 and hk-image.pkl:26 — bind those call sites, never the definition.
- I5 — the aggregator to port: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles/.agent/kb/audit/aggregate.py (its stdout line and side files are what `.claude/workflows/modernization-audit.js` STEP 0 consumes — see that file's `synthPrompt`).
- P1 — precedent for a read-only reviewer that still writes its report: `.claude/agents/adversarial-critic.md` frontmatter (`disallowedTools: Edit, NotebookEdit`, Write allowed) — data match: same role shape (reports, never edits the tree).
- L9 — subagents invoke project, user AND plugin skills through the Skill tool, and preloading draws from that same set (sub-agents.md:299, :553, :555) — so plugin-namespaced skills ARE preloadable, and a `tools:` list that omits `Skill` is exactly how skill invocation is disabled (:553). Every roster row therefore lists `Skill`.
- L10 — the Workflow runtime accepts `opts.agentType` ("uses a custom subagent type … resolved from the same registry as the Agent tool; composes with schema") — the bundled `workflow-authoring` skill text loaded in this session (the KB's workflows.md :292-298 documents only `{schema, label}` and defers to that skill at :309); plugin-scoped names resolve there too (this session launched `fable-orchestrator:premise-verifier` through the Agent tool by that name).
- L11 — `md_size_budget` is blind to `.claude/agents/*.md` (suites.toml:2196); the doctor `[listing]` check (doctor.toml:138-160, `max_chars = 34000`) is the gate that sees them, and it currently reports 39,164 chars / 108 entries (`mise run doctor -- --verbose`, this session).
- L12 — `bun = "1.4.0"` is a repo pin in `.config/mise/conf.d/shared.toml` (read this session); node is NOT repo-pinned (user-global 26.8.2 per the verifier), so the JS dry-run runs on bun.
