# SPEC A-2 — refactor the six stale rules the roster depends on, with evidence notes (#994, part 2 of PR A)

Repo: /Users/rmanaloto/dev/github/ray-manaloto/dotfiles · branch: feat/orchestration-modernization-audit (HEAD carries A-1's roster commit; do not touch what A-1 created).

## 1. Objective

Six `.claude/rules/*.md` ADRs assert harness facts or prescribe mechanisms that the 2026-09-09 audit showed are stale at Claude Code 2.1.267 / codex-cli 0.152.1, and two of them make the new subagent roster impossible to obey as written (an unconditional `AskUserQuestion` mandate no subagent can satisfy; a "coordinator-only" file contract carried by habit instead of a hook). Refactor each rule so it is TRUE at the installed versions and its enforcement rides the native mechanism, while the defect each rule was written for stays caught — a refactor that would let its motivating defect recur is wrong however current its citations. Ray's standing directive (memory `feedback_assume_stale_refactor_to_native`, 2026-09-09): assume existing rules are stale; refactor in the same PR, cite the evidence, add a rules-evidence note, and the change is replayed by an adversarial critic afterwards (the architect runs that replay; you do not).

## 2. Files

MODIFY (six rules): `.claude/rules/clarify-before-acting.md`, `.claude/rules/agent-report-persistence.md`, `.claude/rules/agent-artifact-conventions.md`, `.claude/rules/notepad-enforcement.md`, `.claude/rules/ai-cli-invocation.md`, `.claude/rules/md-size-budgets.md`.
MODIFY (five existing evidence notes, append a dated section): `docs/rules-evidence/clarify-before-acting.md`? — NOTE: check which exist; today `docs/rules-evidence/` holds notes for agent-artifact-conventions, agent-report-persistence, ai-cli-invocation, notepad-enforcement (and others) but NOT clarify-before-acting or md-size-budgets. CREATE the two missing notes: `docs/rules-evidence/clarify-before-acting.md`, `docs/rules-evidence/md-size-budgets.md`.
MODIFY `.claude/settings.json` ONLY IF a hook is added (see agent-report-persistence below) — no other settings key (no `plansDirectory`, no `askUserQuestionTimeout`, which is user/managed scope anyway) — and then `python/src/dotfiles_setup/hook_selfcheck.py`'s `_SETTINGS_WIRING` must learn the new registration in the same change (the ship/land selfcheck asserts settings wiring).
DO NOT TOUCH: `.claude/agents/**`, `.claude/workflows/**`, `AGENTS.md`, root `CLAUDE.md`, `.claude/CLAUDE.md`, `hk*.pkl`, `python/verification/suites.toml` (unless a contract must be updated because a token it binds moved — say so in the report).

## 3. Interfaces — what each rule must say afterwards (outcome; wording is yours)

Read first, in this order: `docs/research/kb/reports/agents/pr-briefs-2026-09-09.md` § "Rule refactors that PR A must ship" (the per-rule Change + motivating Defect), then the audit report `docs/research/kb/reports/modernization-audit-2026-09-09.md` subsections `### rule-clarify-before-acting` (lines 2254-2273), `### rule-agent-report-persistence` (2154-2183), `### rule-agent-artifact-conventions` (2112-2153), `### rule-notepad-enforcement` (2489-2513), `### rule-ai-cli-invocation` (2184-2223), `### rule-md-size-budgets` (2434-2463) — each finding's native anchor is a doc path:line under /Users/rmanaloto/dev/github/ray-manaloto/knowledge-base/sources/agent-harness-docs/docs/ that you re-read before relying on it (the report notes 23 citation corrections; a row marked ⚠️ CONDITIONAL is re-verified first).

1. `clarify-before-acting.md` — keep the standard (recommendation-first, PRO/CON, citation; the `ask_quality` gate is untouched) and add the bounded fallback: when `AskUserQuestion` is absent or denied — every subagent (the harness strips it, sub-agents.md:378-384), `permissionMode: dontAsk`, `-p`, `--permission-prompts none` — present the options in prose and STOP; two harness facts the text must carry exactly: (i) a FORK skips the tool filters and keeps the main conversation's tool pool (sub-agents.md:379), so a fork is the one subagent shape that can still ask; (ii) `askUserQuestionTimeout` is a USER-or-MANAGED setting (not project `.claude/settings.json`), values `60s|5m|10m|never`, default `never`, requires v2.1.200 (settings-reference.md:2759-2774), and when it fires it SUBMITS the options already selected — an auto-continue is a partial answer, never silence; `CLAUDE_AFK_TIMEOUT_MS` overrides it per session (:2766). Re-anchor the three stale citations the report names. Invariant: the 2026-06-29 hk-timeout case (an impossible approach surfaced BEFORE building) must still be the worked example the rule protects.
2. `agent-report-persistence.md` — keep the tracked destination (`docs/research/kb/reports/agents/…`, nothing native replaces it); move the TRIGGER from habit onto hooks: a `SubagentStart` hook (settings `hooks.SubagentStart`, hookSpecificOutput `additionalContext`, hooks.md:2304-2317) injects the incremental-persistence + file-role contract into every subagent so rule 1b/1c stop depending on a brief remembering them — registered UNSCOPED (matcher absent; matchers filter on agent TYPE, hooks.md:314) as ONE settings entry carrying every required substring (hook_selfcheck.py:199-215 rejects pooling across entries); a `SubagentStop` hook's `additionalContext` reaches the DELEGATE only (hooks.md:2346 — parent-side injection is a `PostToolUse` hook on the `Agent` tool), so use SubagentStop to enforce deliver-before-idle on the delegate and do not claim it notifies the coordinator; a broken SubagentStart hook fails SILENTLY (hooks.md:870, :888 — stderr lands in the subagent's transcript), so the `_SETTINGS_WIRING` row and a both-arms test are load-bearing, not tidy-up; rule 1c's table becomes "coordinator-only by default, or an agent whose definition says it emits a DELTA the coordinator applies" (the roster's `pwf-scribe` is that agent); rule 5 reads the `subagents/` roster; `-3` downgrades "total loss" to "recoverable via `agent_transcript_path`, but untracked". If you add the hook: the command is a thin python entrypoint (`python -m dotfiles_setup.<module>` per the observer precedent at `.claude/settings.json`'s InstructionsLoaded hook), `hook_selfcheck._SETTINGS_WIRING` (hook_selfcheck.py:87-131; today five rows: PreToolUse, SessionStart, SessionEnd, InstructionsLoaded, PostToolUse; `check_settings_wiring` :186-215 iterates only declared rows, so an undeclared hook is unprotected, not rejected) gains a row, and a test covers both arms (row present → pass; registration deleted → fail). Invariant: the 2026-07-05 and 2026-08-03 losses (13 reports only in context; two agents dead with nothing written) must be what the hook's injected text prevents.
3. `agent-artifact-conventions.md` — DECISION (architect, 2026-09-10): do NOT adopt `plansDirectory`. It is documented (`settings-reference.md:2709-2721`, default `~/.claude/plans` :2715) but pointing it at `.agent/plans` moves harness plan files into the tree this rule itself describes as swept by `git clean -xdf` (:13) — a durability regression (memory `feedback_plan_files_persist`: `~/.claude/plans` survives sessions). Instead the rule states the two locations plainly: harness plans stay at `~/.claude/plans` (default); `.agent/plans/` holds OUR handoffs/grilling outcomes; record the rejected adoption and its cost in the evidence note. Fix the two stale skill-loader facts the report names; replace the dead `.agent/project-memory.json` row (:23) with the native subagent `memory:` scopes (`local` → `.claude/agent-memory-local/<name>/`, gitignored by A-1); add the undeclared artifact locations the report lists and the worktree/`isolation` case. Invariant: the `.omc/` misnaming lesson (:7) and "promote anything a rule or eval will cite" stay.
4. `notepad-enforcement.md` — stop being markdown-only: name the `SubagentStop`/`SubagentStart` carriage from item 2, use the ENABLED planning-with-files plugin's `findings.md` (root file, re-injected on `clear`/`compact`) for the context-loss half, note that the nine named agent definitions now carry `memory:` where the roster set it, and account for `Explore`/`Plan` skipping CLAUDE.md. Remove the dead `oh-my-claudecode` MCP-tool guidance (:11-13) only after the replacement mechanism is written. Invariant: 2026-04-05 (findings re-derived after context loss) stays the motivating case.
5. `ai-cli-invocation.md` — replace `--full-auto` (does not exist on codex-cli 0.152.1: `.claude/agents/codex-operator.md:76-79` records the 0.152.0 error and the live probe on 0.152.1 this session shows `grep -c -- '--full-auto'` → 0 against `workspace-write` → 2) with the documented `-s workspace-write` form (and note `--approve-for-me` is mutually exclusive with `--sandbox`, codex-operator.md:73-74, reproduced live: `error: the argument '--approve-for-me' cannot be used with '--sandbox <SANDBOX_MODE>'`); move the positional prompt out of "will silently fail" (`codex exec --help`: `Usage: codex exec [OPTIONS] [PROMPT]` — "If not provided as an argument (or if `-` is used), instructions are read from stdin. If stdin is piped and a prompt is also provided, stdin is appended as a `<stdin>` block" — probe output saved at /private/tmp/claude-501/-Users-rmanaloto-dev-github-ray-manaloto-dotfiles/323f75d8-e472-46fe-85ba-769a82acf97c/scratchpad/codex-help.txt; keep the `-p, --profile` warning, which stays true); keep the confirmed `gemini "prompt"` hang warning; point the Gemini lane at the pinned `agy`/antigravity path; collapse the five hand-kept copies of the invocation to one canonical block and pointers. Invariant: "wrong flags fail silently — re-probe `codex exec --help`" stays the rule's spine, and every invocation you write is probed live against the pinned CLIs (`mise exec -- codex exec --help`, `agy --help`) with the probe output quoted in the evidence note.
6. `md-size-budgets.md` (NOTE: this file is `paths:`-SCOPED — frontmatter :1-8 — so its budget is the `rule_scoped` row at :95, 400 lines / 32,000 bytes, not :93; it is at 190 lines / 10,132 bytes today) — drop the quotation that no longer exists in the corpus, correct the number now attributed to the wrong settings field, name the non-deterministic cliff, add `/context` as the native measurement, document the 1,000-expanded-pattern `paths:` budget, and fix "not re-injected after `/compact`" — each per the report rows, each re-read at its doc anchor. Invariant: the Windsurf-misattribution lesson (:38-50, the block quote's source URL is on :50) stays; the budget table (:93/:95) stays machine-consistent with the gate — which is `kb-setup md-budget` (hk.pkl:617) whose constants live in the knowledge-base repo (`kb_setup/md_budget.py:95` `EAGER_BYTE_BACKSTOP = 24_000`, `:182`), so a number can only move via a knowledge-base PR: if one must move, STOP and report dissent. (`listing_budget.py` governs the skill/agent LISTING, a different gate.)

Every rule keeps its `## Applies to` / `## See also` shape, stays under its OWN load-class budget — the five unscoped rules: 200 lines / 24,000 bytes (md-size-budgets.md:93; today 44–116 lines, 1.9–6.0 KB); `md-size-budgets.md` itself: 400 lines / 32,000 bytes (:95; today 190 lines) — LINES bind before bytes, so measure `wc -l` and `wc -c` before and after — and passes agnix. Every evidence note gets a dated section "## 2026-09-09 — audit refactor" with: the finding ids applied, the doc anchors (path:line) re-read, the probe outputs, and one line naming the motivating defect and how the refactored rule still catches it.

## 4. Constraints and invariants
- One writer: A-1's files are frozen; if a rule refactor needs an agent body change, report it as a follow-up, do not edit `.claude/agents/**`.
- Re-read every doc anchor before citing it; the KB corpus stops at Claude Code 2.1.257 — for anything newer use `.agent/kb/raw/claude-code-changelog-2.1.258-2.1.266.md` (saved verbatim) and say so.
- No new bash; hooks call python entrypoints. No suppressions. No root `CLAUDE.md` edits (the stub gate).
- Secrets: never print a credential value.
- Licensed dissent: if a report row's native anchor does not say what the row claims after you re-read it, drop that change and record the refutation in the evidence note instead of guessing.

## 5. Verification (capture each rc to a file)
1. `mise run lint-docs` → rc 0 (agnix over the six rules + evidence notes).
2. `mise run lint` → rc 0 (md_size_budget, typos, rumdl over authored docs; read `rc=` from the log).
3. `uv run --project python pytest tests/test_hook_selfcheck.py tests/test_rule_registry.py tests/test_listing_budget.py -q` → rc 0 (plus any test you add for a new hook, both arms).
4. `mise run verify` → `0 failed`.
5. `uv run --project python pytest tests/ -x -q` → all pass.
6. For rule 5: the quoted `codex exec --help` / `agy --help` probe output in the evidence note matches a live re-run (`mise exec -- codex exec --help | grep -c workspace-write` ≥ 1 is the positive arm; `grep -c full-auto` = 0 the negative).

## 6. Commit
Lane commits on the current branch. Subject: `docs(rules): refactor six stale rules to the installed harness and record the evidence (#994)`. One commit.

## 7. PREMISES
- L1 — `clarify-before-acting.md:8-9` says the AskUserQuestion mandate is unconditional; `sub-agents.md:378-384` (KB) says the tool is removed from every subagent even when listed.
- L2 — `agent-report-persistence.md:31` (rule 1b, persist incrementally), `:45-55` (rule 1c file-role table, `task_plan.md` coordinator ONLY, "a delegate never writes task_plan.md").
- L3 — `agent-artifact-conventions.md:7` (renamed from `.omc/`), `:23` (`.agent/project-memory.json` row).
- L4 — `notepad-enforcement.md:11-13` (the `oh-my-claudecode` MCP tools, 0 invocations across 941 transcripts).
- L5 — `ai-cli-invocation.md:18` prescribes `codex exec --full-auto --sandbox workspace-write -`; `:28` and `:47` carry the `-p`/`gemini "prompt"` warnings; `.claude/agents/codex-operator.md:76-79` records `--full-auto` absent on codex 0.152.0 and `:73-74` records `--approve-for-me` mutually exclusive with `--sandbox`; both re-probed live on 0.152.1 this session (verifier part 4).
- L6 — `md-size-budgets.md:38-50` (the 12,000 / Windsurf misattribution, source URL on :50), `:93` (`rule_unscoped`: 200 lines / 24,000 bytes), `:95` (`rule_scoped`: 400 lines / 32,000 bytes); `md-size-budgets.md:1-8` carries `paths:` frontmatter, so it is itself `rule_scoped` (`kb_setup/md_budget.py:475` reclassifies on that).
- L7 — `docs/rules-evidence/` holds notes for agent-artifact-conventions, agent-report-persistence, ai-cli-invocation, notepad-enforcement (and 13 others) and NONE for clarify-before-acting or md-size-budgets (directory listing read this session).
- L8 — `hooks.md:2304-2317` (KB): `SubagentStart` cannot block but returns `hookSpecificOutput.additionalContext` (:2308) injected before the subagent's first prompt; `hooks.md:2346`: SubagentStop's additionalContext reaches the subagent, parent-side injection is PostToolUse on `Agent`; `hooks.md:314`: SubagentStart matchers are agent types; `hooks.md:870`/`:888`: a failing SubagentStart hook only shows stderr in the subagent's transcript.
- L10 — `settings-reference.md:2709-2721` documents `plansDirectory` (default `~/.claude/plans` :2715; added 2.1.9 per changelog.md:4782); `:2759-2774` documents `askUserQuestionTimeout` (user/managed scope :2763, default `never` :2765, submits already-selected options :2761).
- L11 — `hook_selfcheck.py:87-131` `_SETTINGS_WIRING` five rows; `:186-215` iterates only declared rows; `:199-215` one-entry requirement. `.claude/settings.json` wires exactly five events today.
- L12 — the md budget gate is `kb-setup md-budget` (hk.pkl:617); constants in knowledge-base `kb_setup/md_budget.py:95` and `:182`.
- L13 — host versions: Claude Code 2.1.267, codex-cli 0.152.1 (`claude --version`, `codex --version`, verifier part 2).
- L9 — the report's per-rule subsections start at lines 2112, 2154, 2184, 2254, 2434, 2489 of `docs/research/kb/reports/modernization-audit-2026-09-09.md` (heading lines read this session).
- A1 — the per-rule mechanisms above are the AUDIT's proposals (report-sourced, verified by three lenses there, not re-read line-by-line by the architect): treat each as a hypothesis to confirm at its doc anchor before writing it into a rule.
- A2 — (resolved → decision) `plansDirectory` is documented (L10) and deliberately NOT adopted here for the durability reason stated in §3 item 3.
