# Add this include by array merge; never replace the actual global config.
[task_config]
includes = ["/Users/rmanaloto/dev/github/ray-manaloto/dotfiles.worktrees/agentsview-native-service/.config/mise/agentsview-native/tasks.toml"]
